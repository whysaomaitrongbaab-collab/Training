# Raw JSON Data Log

บันทึกทุกครั้งที่มีการแก้ไข/เขียนทับ raw JSON ของ raw data (ไฟล์ที่คุ้มครองตาม [rule_of_tune.md](rule_of_tune.md) กฎข้อที่ 1) — **ต้องเขียน log ที่นี่ก่อนหรือพร้อมกับการแก้ทุกครั้ง ห้ามแก้แล้วลืมบันทึก**

ไฟล์ที่เข้าข่ายต้องบันทึก: `raw/image/<house>/qwen-output/<house>_หน้าNN.json`, `_document_map.json`, `_run_summary.json` และไฟล์ผลลัพธ์อื่นๆ จาก `run_pipeline.py`/`build_document_map.py`/`analyze_folder.py` — และ (เพิ่ม 2026-07-10) **`raw_json_ตัวที่ใช้งานจริง/0N<house>/*.json`** (repo root) ตามที่ขยายขอบเขตไว้ใน `rule_of_tune.md` กฎข้อที่ 1

## รายการแก้ไข

| วันที่ | ไฟล์ raw JSON ที่แก้ไข | AI ที่ใช้ทำ | ผู้แก้ไข | หมายเหตุ |
|---|---|---|---|---|
| 2026-07-02 | `raw/image/บ้าน_เล็ก_1ชั้น_01/qwen-output/บ้าน_เล็ก_1ชั้น_01_หน้า19.json` | Claude (Sonnet 5, Claude Code) — เทียบภาพจริง `image/บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า19.png` กับ JSON เดิมที่ Qwen สกัด | มะขาม (อนุมัติในบทสนทนา) | element `F1,C1`: แก้ `count` 9→11, แก้ `grid_refs` (เอา `C-2` ออก เพราะจริงๆ เป็นคนละ element, เพิ่ม `D-1,D-2,D-3` ที่ Qwen มองข้ามทั้งแถว) + เพิ่ม element `F2,C1` (footing ที่ grid C-2) ที่หายไปทั้งตัวจากผล extraction เดิม — ทั้ง 2 element ติด flag `claude_reviewed` ไว้ในข้อมูล |
| 2026-07-10 | `raw_json_ตัวที่ใช้งานจริง/01บ้าน_เล็ก_1ชั้น_01/*.json` (101 ไฟล์ ใหม่ทั้งหมด ไม่ใช่แก้ทับของเดิม) | Claude (Sonnet 5, Claude Code) — อ่านภาพจริงทุกหน้าอิสระ (personal read หน้า 18-25/61 + 5 background agent อ่านหน้าที่เหลือ) ตามสเปค `20260708draft of prime rawjson.md` (Gen 4 + กติกาเพิ่มเติม 2026-07-08) | มะขาม (สั่งถอดแบบและยืนยันผลตลอด session) | รอบ extract เต็มรูปแบบครั้งที่ 4 ของบ้านนี้ (ต่อจาก `mk_test/t1/t2/t3`) ใช้ 13-pattern taxonomy ใหม่ + field `source_image` บังคับทุกไฟล์ + กติกา dummy grid ปรับปรุง — ก็อปมาจาก `training-data/mk_test/t4/` (ต้นฉบับ) มาไว้ที่นี่ในชื่อโฟลเดอร์ที่มีเลขลำดับ (`01`) เป็นจุดเริ่มของโครงสร้าง "raw JSON ที่ใช้งานจริง" แยกจากรอบทดลอง — validate แล้วครบ 101/101 ไฟล์ JSON valid, มี `source_image`/`source_pages` ครบ, ครบ 62/62 หน้า (00-61), ครบ 13 pattern |

## วิธีเพิ่มแถวใหม่

ก่อนแก้ raw JSON ทุกครั้ง (หลังได้รับอนุญาตตามกฎข้อ 1-2 ใน `rule_of_tune.md` แล้ว) ให้เพิ่มแถวใหม่ในตารางด้านบนทันที:

- **วันที่** — วันที่แก้จริง (ISO format `YYYY-MM-DD`)
- **ไฟล์ raw JSON ที่แก้ไข** — path เต็มของไฟล์ เช่น `raw/image/บ้าน_เล็ก_1ชั้น_01/qwen-output/บ้าน_เล็ก_1ชั้น_01_หน้า19.json`
- **AI ที่ใช้ทำ** — ชื่อ/รุ่นโมเดลที่ใช้สกัด/แก้ (เช่น `qwen-vl-max` ของเดิม, `Claude (Sonnet 5, Claude Code)` ถ้าเป็นรอบใหม่)
- **ผู้แก้ไข** — คนที่สั่ง/อนุมัติการแก้ในบทสนทนานั้น (เช่น มะขาม, คิง)
- **หมายเหตุ** — สรุปสั้นๆ ว่าแก้อะไร ทำไม (เช่น "แก้เพราะ Qwen พลาด element F2,C1 หายไปทั้งตัว, ขาด grid แถว D")
