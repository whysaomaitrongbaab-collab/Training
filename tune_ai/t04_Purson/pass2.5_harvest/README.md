# Pass 2.5 — self-harvest

**ไม่ใช้ AI เลย — ไม่มี prompt** เจตนา: CV มั่วไม่เป็น ใช้เป็นคนตรวจ ไม่ใช่คนอ่านแทน

เอา detection ของหน้านั้นเอง (ผ่านคลังกลางมาแล้ว) มาเป็น template ใหม่ กวาดซ้ำแบบเข้ม (0.90 —
ไอคอนพิมพ์เดียวกันหน้าเดียวกัน match เกือบ 1.0) หา element ที่คลังกลางจับข้ามซีรีส์ไม่ติด

**พิสูจน์แล้วด้วยตา 2 หลัง** (บ้าน_ใหญ่_2ชั้น_08 15→16, บ้าน_เล็ก_2ชั้น_19 17→18 = ตรง GT เป๊ะทั้งคู่)
**เพดานโดยหลักการ:** กู้ได้เฉพาะชนิดที่คลังจับติดแล้ว — บ้านที่คลังหาไม่เจอเลยสักตัวยังช่วยไม่ได้

ผลลัพธ์เข้าเป็น **บัญชีตั้งต้นของ pass 3** ([`pass3_takeoff/`](../pass3_takeoff/))

ของจริงอยู่ที่: [`tools/cv_scan.py`](../../../tools/cv_scan.py) (`--pass25`)

    python tools/cv_scan.py <ภาพ> --pass25   # เพิ่ม self_harvest_points ใน sidecar JSON
