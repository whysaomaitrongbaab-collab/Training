# Destrier บนบ้านจริงที่ไม่อยู่ในชุดเทรน — บ้านครอบครัวไทยเป็นสุข 3 (งาน 83b8e52c)

**วันที่:** 27 ก.ย. 69 · **ผู้เขียน:** Claude (session กับมะขาม)

**เฉลย:** `Constistant/tests/fixtures/real-house-truth/83b8e52c/` เกิดจาก Claude อ่านแยกกันอิสระสองรอบ แล้วรวมเป็นชุดเดียว (ตัดสินจุดที่ไม่ตรง 3 จุดจากการดูภาพเอง)
**ยังไม่มีคนเซ็นรับรองเฉลยชุดนี้** — ถ้าจะใช้ตัดสินโมเดลจริงจัง ควรให้คนดูหน้า 19–25 ก่อน

**ผลโมเดล:** งาน `83b8e52c` รันวันที่ 23 ก.ย. ด้วย render 180 dpi (ก่อนแก้เป็น 200 dpi)

## เทียบระดับชิ้นส่วน

| | เฉลย | Destrier | ปัญหา |
|---|---|---|---|
| ฐานราก (หน้า 19) | 14 ฐาน · 8 มาร์ค (F2–F8, F10) · **ไม่มี F1** | 9 ฐาน · F1–F5 · F1 ×4 ที่ไม่มีจริง | อ่านมาร์คและตำแหน่งผิดเกือบทั้งหน้า |
| เสา | C1 ×11 · C2 ×3 · CN ×2 | CN ×1 | บนแปลนพิมพ์รวมเป็น "C1,F4" โมเดลเก็บแค่ฐานราก เสาจึงหายทั้งหมด |
| คาน | 66 ช่วง ใน F1/F2/RF | 42 ช่วง **ไม่มี F2 เลย** | หน้า 20 มีสองแปลน ("ชั้นล่าง, ชั้นบน") แต่โมเดลใส่ทุกชิ้นเป็น F1 (โมเดลเขียนเตือนไว้เองใน warnings) |
| คานจากหน้าแบบขยาย S-04..S-06 | ไม่นับซ้ำ | นับซ้ำกับแปลน S-02 | pass0 ติดป้ายหน้าแบบขยายเป็นแปลน |
| มิติฐานราก | กว้าง × ยาว × หนา (เช่น F2 1.10×0.55×0.20) | 1100 × 550 ไม่มีความหนา | เป็นธรรมเนียมเดียวกับ GT ส่วนใหญ่ ดู `json_แก้ไขแล้ว/สิ่งที่ต้องแก้.md` ข้อ 67 |
| pattern ของหน้ารูปตัด | `section` | 3/15 หน้า (ทุกงานจริงรวมกัน) ตั้งชื่อเอง: `detail`, `footing_plan_and_detail`, `sanitary_plan` | สเปกจากหน้าพวกนี้หายไปเงียบๆ (ฝั่งเว็บรับชื่อที่บอกชัดว่าเป็นรูปตัดแล้ว) |

## เทียบยอด BOQ (ผ่าน pipeline จริงของ Constistant)

| | เฉลย | Destrier ก่อนแก้เว็บ | Destrier หลังแก้เว็บ 27 ก.ย. |
|---|---|---|---|
| คอนกรีตคาน (m³) | 10.46 | 15.54 | 11.85 |
| คอนกรีตฐานราก (m³) | 6.39 | 11.78 | 0 ติดธง |
| เสา | ฿27,018 | 0 | ~฿1,276 |
| **รวม** | **฿188,842** | ฿226,076 | ฿141,374 |

ตัวเลข "ก่อนแก้" ดูใกล้เฉลย แต่เป็นเพราะความผิดสองทางหักล้างกันเท่านั้น
ตัวเลข "หลังแก้" ห่างจากเฉลยมากขึ้น แต่ส่วนที่ขาดทุกบาทมีธงให้ตรวจแล้ว:
- ฐานรากไม่มีความหนา
- แผ่นแปลนหลายชั้นที่ผลอ่านใส่ไว้ชั้นเดียว
- คานที่ใช้ความยาวสมมติ

## โจทย์สำหรับรอบทูนหน้า (แก้ที่ข้อมูลเทรนหรือ prompt ไม่ใช่ที่เว็บ)

1. **ถ้าหน้าหนึ่งมีหลายแปลน ต้องแยก view ตามชั้น** — จะแยกใน pass0/organize หรือให้ใส่ `floor_level` ราย element ก็ได้
2. **ป้ายรวม "C1,F4"** — ต้องออกมาเป็นสอง element คือเสาหนึ่งตัวและฐานรากหนึ่งตัว บนตำแหน่งเดียวกัน
3. **มิติฐานรากต้องมีกติกาเดียวทุกบ้าน** — แปลนกับความหนาต้องไม่ปนกัน (ข้อ 67) และต้องอ่านความหนาจากรูปตัดทุกครั้ง
4. **บังคับ `pattern` ให้อยู่ในคำศัพท์ที่กำหนด** — ห้ามตั้งชื่อใหม่เอง
5. **หน้า 19 ของบ้านนี้ควรเป็นเคสทดสอบ** — แผ่นนี้มีฐานรากหลายขนาดปนกัน และโมเดลอ่านผิดเกือบทั้งแผ่น
