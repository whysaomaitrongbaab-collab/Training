# t03 pass design v2 — 2026-08-26 (มะขามสั่ง reset การนับ pass)

แทนที่การนับเดิม 0/1/2/3 ในไฟล์ `pass_design.csv` — ตัวนั้นยังใช้อ่าน objective ราย subtask ได้อยู่
แต่ **เลข pass ในนั้นเป็นของเก่าแล้ว** ให้ยึดไฟล์นี้เป็นหลัก

> ⚠️ ยังไม่ลงมือ — เอกสารนี้คือแผน รอมะขามเคาะ 3 ข้อท้ายไฟล์ก่อน

---

## สายพานใหม่

| pass | ใคร | ทำอะไร | สถานะ |
|---|---|---|---|
| **0** | Qwen | คัดหน้า → subtask + views + where | **เดิมเป๊ะ** ไม่แตะ |
| **1** | โค้ด | ตัดรูปราย view + แยกโฟลเดอร์ (`organize.py`) | **เดิมเป๊ะ** ไม่แตะ |
| **1.5** | CV | เฉพาะหน้าที่ pass 3 จะกิน → **พิกัด pixel ราย element** | มีของแล้ว 70% |
| **2** | Qwen | อ่านด้วย prompt เดิม **ไม่มี hint จาก CV** | = พฤติกรรมปัจจุบัน (ตัวคุม) |
| **2.4** | Qwen | อ่านด้วย prompt เดิม **+ hint พิกัดจาก 1.5** | ใหม่ |
| **2.5** | CV | เอาของที่ 2/2.4 เจอ ไปกวาด CV ซ้ำ หา element ที่ 1.5 ตกหล่น | ใหม่ |
| **3** | Qwen | ถอดระยะ/เหล็ก — **ห้ามลบ element ที่ 2.5 เจอ เพิ่มได้อย่างเดียว** | ใหม่ |
| ~~**4**~~ | ~~Qwen~~ | ~~หน้ารอง (สถาปัตย์/ไฟฟ้า/สุขาฯ/ปก/สารบัญ)~~ | ❌ ยกเลิก 2026-08-29 (มะขาม: "หน้าพวกนั้นไม่จำเป็น") |

**หัวใจของ v2:** pass 2 กับ 2.4 คือ **การทดลอง A/B** — ยิงหน้าเดียวกันสองรอบ ต่างกันแค่มี/ไม่มี hint
แล้ววัดว่า hint ช่วยจริงหรือทำให้หลอนหนักขึ้น ไม่ใช่เดาเอา

---

## ของที่มีอยู่แล้ว ไม่ต้องเริ่มจาก 0

| ของ | อยู่ไหน | ใช้กับ pass |
|---|---|---|
| เครื่อง CV (มาร์คฐานราก/เสา/คาน) | `tools/pattern_recognition.py` | 1.5, 2.5 |
| ตัวสแกน → JSON พิกัด | `tools/cv_scan.py` | 1.5 |
| คลัง template 15 ตัว + ตัวเก็บเพิ่ม | `tools/templates/`, `harvest_templates.py` | 1.5, 2.5 |
| prompt pass 2 (7 subtask) + `_common.md` + glossary ไทย | `tune_ai/t03/pass2_used/` | 2, 2.4, 3 |
| prompt หน้ารอง | `tune_ai/t03/pass3_unused/extract.md` | 4 (แค่ย้าย/เปลี่ยนเลข) |
| ตัววัดผล (แก้ metric แล้ว) | `data_before_tune/infer_house_t03.py` | เทียบ 2 vs 2.4 |

---

## งานที่ต้องทำ เรียงตามลำดับ

### เฟส A — เปลี่ยนเลข (ถูก ทำก่อนกันสับสน)
1. `pass3_unused/` → `pass4_unused/`; อัปเดต `PASS_OF` ใน `organize.py`, `pass_design.csv`, README
2. `pass_design.csv` เพิ่มคอลัมน์ `pass_v2` ไม่ลบของเก่า (กันของเดิมอ้างพัง)

### เฟส B — pass 1.5 ให้ใช้งานได้จริง
3. `cv_scan.py` เพิ่มโหมด `--manifest work/` → เดินเฉพาะโฟลเดอร์ subtask ที่ pass 3 จะกิน
   (`plan_footing`/`plan_column`/`plan_beam`/`plan_slab`) ข้ามที่เหลือ
4. เปลี่ยน output ให้เป็น **รายชิ้นมีเลขกำกับ** ไม่ใช่แค่ list พิกัด:
   `{"class":"column","n":2,"cx":123,"cy":456,"w":40,"h":40,"score":0.81}`
   — เลข `n` ต้องคงที่ (เรียงบน→ล่าง ซ้าย→ขวา) ไม่งั้นเทียบผลระหว่างรอบไม่ได้
5. เขียน `cv_hint_text()` แปลง JSON → บล็อกข้อความสั้นๆ สำหรับยัด prompt pass 2.4
6. **เติมคลัง template ให้ครบ** — ตอนนี้พอใช้ 23/44 หลัง อีก 19 หลังยังขาด (งานคนคัด ไม่ใช่งานโค้ด)

### เฟส C — pass 2 vs 2.4 (การทดลอง)
7. pass 2 = ของเดิม ไม่ต้องเขียนอะไร ใช้เป็นตัวคุม
8. pass 2.4: แทรกบล็อก hint ลง prompt + กฎกันหลอน 2 ข้อ
   - "hint คือสิ่งที่เครื่องเห็น **ไม่ใช่คำตอบ** — เห็นแล้วอ่านไม่ออกก็ยัง `null` ได้"
   - "hint บอกว่ามี 14 จุด แต่แกอ่านได้ 9 → ตอบ 9 แล้วเขียนใน `warnings[]` ห้ามแต่งอีก 5 ตัวมาเติม"
9. ยิงบ้านชุดทดสอบชุดเดียวกันทั้ง 2 ทาง → เทียบด้วย metric ที่แก้แล้ว
   (recall_printed, จำนวน element, จำนวนที่หลอน)

### เฟส D — pass 2.5
10. เอา element ที่ Qwen ตอบมา crop เป็น template ใหม่ → กวาดทั้งหน้าอีกรอบ
    (**self-harvest** — แก้ปัญหาคลัง template ไม่ครบ 19 หลังได้เองอัตโนมัติ)
11. รวมผล 1.5 + 2.5 เป็น "บัญชี element ฉบับสุดท้าย" พร้อมพิกัด

### เฟส E — pass 3
**2026-08-28: ที่อยู่ถาวรของโฟลเดอร์ pass ทั้งหมด (0-4) ย้ายไป `tune_ai/t04_Purson/` แล้ว**
(สร้างโดยมะขาม, ตั้งชื่อขำๆ "Purson") — `pass3_takeoff/` ย้ายจาก `tune_ai/t03/` ไปอยู่ที่นั่น
จริง prompt.md เต็มแล้ว ดู [`t04_Purson/README.md`](../t04_Purson/README.md) เป็นดัชนี
ไฟล์นี้ (`pass_design_v2.md`) ยังเป็นแหล่งอ้างอิงแผน/เหตุผล ไม่ย้าย

12. prompt ใหม่: ป้อนบัญชีจาก 2.5 + รูป → ถอดระยะ/เหล็ก
13. **บังคับ "ห้ามลบ" ด้วยโค้ด ไม่ใช่ prompt** — เขียน merge step: element ไหนหายไปจากคำตอบ
    ให้ใส่กลับพร้อม `confidence_flags: ["dropped_by_pass3"]` และฟิลด์ระยะเป็น `null`
    (โมเดลเชื่อไม่ได้ในเรื่องนี้ ต้องกันด้วยโค้ด)

---

## 3 ข้อที่ต้องเคาะก่อนเริ่ม

**1. ✅ เคาะแล้ว** — "pass 0.5" ที่พิมพ์ในข้อ 2/2.4 = **pass 1.5** ไม่มี pass 0.5 ในสายพานนี้

**2. ✅ เคาะแล้ว** — pass 2.5 = **self-harvest template**: เอาสิ่งที่ Qwen ชี้ว่าเป็นเสา ไป crop
เป็น template ใหม่ แล้วกวาดหาตัวที่เหมือนกันทั้งหน้า → แก้ปัญหาคลัง template ขาด 19 หลังได้เอง

**3. hint ต้องมีอะไรบ้าง — คำตอบอยู่ข้างล่าง (§ hint design)**
มะขามทักถูก: บอกแค่ "มี element กี่ตัว" ไม่พอแน่นอน ออกแบบใหม่ทั้งบล็อกแล้ว

---

# § hint design — pass 2.4 ป้อนอะไรให้ Qwen

## ปัญหา: จำนวนอย่างเดียวไร้ประโยชน์

บอกว่า "หน้านี้มีเสา 14 ต้น" โมเดลทำอะไรกับข้อมูลนี้ไม่ได้เลย นอกจากเดาเติมให้ครบ 14 —
ซึ่งคือ **การเชิญให้หลอน** ตรงๆ เลวร้ายกว่าไม่บอกอะไรเสียอีก

## ทางออก: ส่ง 2 ช่องทางพร้อมกัน — มาร์คบนภาพ + ตารางข้อความ

**ช่องที่ 1 — มาร์คลงบนภาพจริง (Set-of-Mark)**
วาดกรอบ + **เลขกำกับ** ลงบนจุดที่ CV เจอ แล้วส่งภาพที่มาร์คแล้วให้โมเดลแทนภาพเปล่า

มีงานวิจัยรองรับตรงๆ: **Set-of-Mark Prompting** (Microsoft Research, arXiv 2310.11441) —
เอาเลข/กรอบไปแปะบนภาพ แล้ว GPT-4V แบบ zero-shot ชนะโมเดลที่ fine-tune มาเฉพาะทาง
บน RefCOCOg ได้ หลักการคือ **โมเดลอ่าน "ตรงเลข 3" ง่ายกว่าอ่าน "ที่พิกัด x=340,y=210" มาก** —
พิกัดตัวเลขในข้อความ โมเดลต้องแปลงกลับเป็นตำแหน่งบนภาพเอง ซึ่งมันทำได้แย่

และนี่คือสิ่งที่มะขามสั่งไว้ตั้งแต่แรกอยู่แล้ว: *"ผมอยากให้มันไปทำการไฮไลด์หรือทำ mark ไว้เลย"*
— `pattern_recognition.py` วาดมาร์คเป็นอยู่แล้ว แค่ยังไม่เคยส่งภาพที่มาร์คแล้วเข้าโมเดล

**ช่องที่ 2 — ตารางข้อความสั้นๆ กำกับ**

```
### สิ่งที่เครื่องตรวจภาพเห็น — hint ไม่ใช่คำตอบ
บนภาพมีกรอบเลขกำกับจุดที่เครื่องเห็น ให้อ่านป้ายที่จุดเหล่านั้น

column (14): #1 g≈C-1 · #2 g≈C-2 · #3 g≈C-3 · ...
footing (14): #15 g≈C-1 · #16 g≈C-2 · ...
beam: แถบนอน 9 · แถบตั้ง 7
```

`g≈` = กริดที่เดาจากพิกัด (ดู "แปลง pixel → grid ref" ข้างล่าง) ใส่ `?` เมื่อยังไม่มี grid master

## ~~grid ref ใน hint~~ — แก้เป็นแถว/ช่องเชิงลำดับ (พบตอนลงมือ 2026-08-26)

ตอนวางแผนเขียนว่า "grid master มีตำแหน่ง pixel ของทุกเส้นกริด เอามาคูณพิกัด CV ได้ grid ref ฟรี"
— **ผิด** ตรวจ GT จริงแล้ว grid master เก็บ `pos_m` (เมตร) ไม่มีพิกัด pixel เลย

hint v1 จึงใช้ **แถวเชิงลำดับ** แทน: `แถวละ [4, 4, 4, 2]` — โมเดลอ่านป้ายกริดจากภาพเอง
(เป็นหน้าที่มันอยู่แล้ว) จุดของ hint ตาม SoM คือ anchor ความสนใจ ไม่ใช่ตอบ grid แทน

TODO ถ้าอยากได้ตัวอักษรกริดจริง: detect เส้นกริดในภาพ (เส้นยาวทะลุหน้า) → ได้ px →
จับคู่ "ตามลำดับ" กับ x_lines/y_lines ใน grid master เมื่อจำนวนตรงกันเป๊ะ ไม่ตรง = `?` ห้ามเดา

## ❌ สิ่งที่ห้ามใส่ใน hint เด็ดขาด: ระยะ

CV วัดความยาวคานจากพิกเซลได้ก็จริง แต่ **ห้ามส่งเข้า prompt** เพราะขัดกฎเหล็กข้อ 1
"ห้ามเดาระยะจากรูปร่างที่เห็นบนแบบ" — ส่งเข้าไปคือสอนโมเดลให้แหกกฎนั้นด้วยมือเราเอง

ระยะที่ CV วัดได้ให้ใช้เป็น **ตัวจับผิดฝั่งโค้ด** ใน pass 2.5/3 แทน:
โมเดลตอบ span 3.0 ม. แต่ CV+กริดบอก ~8 ม. → ยกธง `span_contradicts_pixels` ให้คนดู
ไม่ใช่แก้ตัวเลขให้

## กฎกันหลอน 4 ข้อ ที่ต้องอยู่ในบล็อก hint

1. hint คือ**สิ่งที่เครื่องเห็น ไม่ใช่คำตอบ** — จุดไหนอ่านป้ายไม่ออก ให้ `element_id` เป็นชื่อบรรยาย
   + ติดธง **ห้ามแต่งชื่อมาร์คขึ้นมาเอง**
2. **hint ไม่ใช่เพดาน** — เห็นมากกว่าที่ hint บอก ให้ตอบตามที่เห็น
3. **hint ไม่ใช่พื้น** — hint ชี้ 14 จุด แต่แกอ่านได้จริง 9 → ตอบ 9 แล้วเขียนใน `warnings[]`
   ห้ามเติมอีก 5 ตัวให้ครบ
4. จุดที่ hint ชี้แล้วไม่มีอะไรจริง → บอกใน `warnings[]` (นี่คือ feedback ว่า template เรามั่ว)

**และมีตัวจับผิดฝั่งโค้ดคู่กันเสมอ** — เทียบจำนวนที่โมเดลตอบ vs ที่ CV เห็น ต่างกันทั้งสองทิศ
ให้ยกธง ไม่ใช่แก้ให้ตรง

## การทดลอง: 3 แขน ไม่ใช่ 2

หน้าเดียวกัน โมเดลตัวเดียวกัน ต่างกันแค่ input:

| แขน | ภาพ | ข้อความ hint |
|---|---|---|
| **2** | เปล่า | ไม่มี | ← ตัวคุม = พฤติกรรมวันนี้ |
| **2.4a** | เปล่า | มี (ตาราง+กริด) | แยกผลของ "ข้อความ" ล้วนๆ |
| ~~**2.4b**~~ | ~~มาร์คเลข~~ | ~~มี~~ | ❌ ยกเลิก 2026-08-29 (มะขาม: "ใส่แต่ hint พอ") |

~~ต้องมี 3 แขนเพราะถ้าเทียบแค่ 2 vs 2.4b แล้วดีขึ้น จะไม่รู้เลยว่าดีขึ้นเพราะมาร์คบนภาพ
หรือเพราะข้อความ~~ → เหลือ 2 แขน: เทียบ 2 vs 2.4a ตอบคำถามเดียว "hint ข้อความช่วยไหม"
(ภาพมาร์ค `_marked.png` ยังถูกสร้างโดย cv_scan.py — ใช้ให้คนตรวจด้วยตา ไม่ป้อนโมเดล)

**วัดอะไร:** `recall_printed` (ตัวหลัก) · จำนวน element ที่ตอบ · จำนวนที่หลอน
(ตอบ id ที่ไม่มีใน GT) · จำนวนที่ยอมตอบ `null` อย่างซื่อสัตย์

## เรื่อง "โมเดลไม่เคยเห็น hint ตอนเทรน" — ยังเป็นข้อกังวลจริง

SoM ใช้ได้ zero-shot กับ VLM ทั่วไป แต่โมเดลเรา fine-tune มาแล้ว ซึ่งอาจทำให้มันแคบลง
จนมองข้าม hint — **ไม่มีใครรู้จนกว่าจะยิง** และนั่นคือเหตุผลที่ทำ 3 แขนก่อนตัดสินใจเทรน

ข่าวดี: ถ้าแขนไหนชนะ การเอาไปใส่ในชุดเทรนถูกมาก — มาร์คภาพเราวาดเอง `build_dataset_t03.py`
ก็มีภาพครบอยู่แล้ว แค่รัน CV แล้วแปะ ไม่ต้องขอข้อมูลใหม่จากใคร เสียแค่ค่า GPU รอบเทรน


---

# § สถานะลงมือ 2026-08-26 (GO รอบแรก)

| งาน | สถานะ |
|---|---|
| เฟส A เปลี่ยนเลข (pass4_unused, PASS_OF, csv +pass_v2, README) | ✅ |
| เฟส B pass 1.5: `cv_scan.py` เลขกำกับ #n คงที่ + `--manifest` + ภาพมาร์ค SoM + hint text | ✅ demo ผ่าน + eyeball แล้ว |
| เฟส C ฝั่งโค้ด: `apply_arm()` + `--arm 2/2.4a --cv-dir` ใน infer_house_t03.py (2.4b ตัดออก 2026-08-29) | ✅ unit test ผ่าน (ไม่ใช้ GPU) |
| เฟส C ยิงจริง 2 แขน | ⏸ รอ GPU (เครดิต Vast) |
| เฟส D pass 2.5 self-harvest (`--pass25`) | ✅ พิสูจน์ด้วยตา 2 หลัง |
| เฟส E `tools/merge_guard.py` (ห้ามลบ บังคับด้วยโค้ด) + prompt `t04_Purson/pass3_takeoff/` | ✅ self-check ผ่าน / prompt เขียนเต็มแล้ว ยังไม่เคยรัน |
| เติมคลัง template 19 หลังที่ขาด | ⏸ งานคนคัด |

**บั๊กที่จับได้ด้วยตา (unit test มองไม่เห็นทั้งคู่):** (1) ป้ายเสา #1-#14 โดนพื้นขาวของป้าย
ฐานรากทับหาย → ป้ายเสาย้ายลงใต้กล่อง (2) พื้นขาวของป้ายทับเลข "1" ของมาร์ค F1 ที่พิมพ์บนแบบ
— hint บังคำตอบที่โมเดลต้องอ่านเอง! → ป้าย point-class ย้ายไปขวาของกล่อง (มาร์คพิมพ์อยู่ซ้ายบนเสมอ)

**self-harvest — วัดจริงแล้ว มีทั้งผลงานและเพดาน:**
- เกณฑ์แรก (seed ≥0.85 / match 0.65) ผิดข้างสิ้นเชิง: match ข้ามซีรีส์ได้แค่ 0.72-0.80 → seed ไม่มีวันเกิด
  กลับตรรกะเป็น seed หลวม + match เข้ม 0.90 (ไอคอนพิมพ์เดียวกันหน้าเดียวกัน match 0.97-1.0 วัดจริง)
- **ได้ผล:** บ้าน_ใหญ่_2ชั้น_08 15→16 = GT เป๊ะ, บ้าน_เล็ก_2ชั้น_19 17→18 = GT เป๊ะ — ตัวที่เพิ่ม
  eyeball แล้วเป็นฐานรากจริงทั้งคู่ (F3/C3, F2/C2) พร้อม provenance `self_harvest_points` ใน sidecar
- **เพดาน (โดยหลักการ ไม่ใช่บั๊ก):** กู้ได้เฉพาะ "ชนิดเดียวกับที่คลังจับติดแล้ว" — บ้าน 10 หน้า23
  seed match ตัวเองได้ 4 จุดเป๊ะ (GT 20): อีก 16 ตัวเป็นไอคอนคนละชนิด ต้องรอ (ก) คนเติมคลัง
  หรือ (ข) pass 2.5 เต็มรูป ที่เอาตำแหน่งจากคำตอบ Qwen (ผ่าน cv_mark) มาเป็น seed เพิ่ม
