# Primary Raw JSON Schema — Edit Log

บันทึกทุกครั้งที่มีการแก้ไข [`00file_for_making_rawjson_from_claude/primary_rawjson_schema.md`](00file_for_making_rawjson_from_claude/primary_rawjson_schema.md) — คู่กับ [`No_touch_box/docs/raw_json_data_log.md`](../No_touch_box/docs/raw_json_data_log.md) ที่บันทึกการแก้ไข raw JSON ข้อมูลบ้านแต่ละหลัง (ไฟล์นี้บันทึกเฉพาะการแก้ตัวสเปคเอง)

ตาม `rule_of_tune.md` กฎข้อ 7 — `primary_rawjson_schema.md` เป็นเอกสาร .md ไม่เข้าข่ายกฎข้อ 1 (แก้ได้โดยไม่ต้องขออนุญาต) แต่ทุกการแก้ต้องอ้างอิงกฎนี้และจดไว้ที่นี่เสมอ ห้ามแก้แล้วไม่บันทึก

## รายการแก้ไข

| วันที่ | หัวข้อที่แก้ | AI ที่ใช้ทำ | ผู้แก้ไข/อนุมัติ | หมายเหตุ |
|---|---|---|---|---|
| 2026-07-13 | เพิ่ม section 13 "`site_plan` — `element_type` not standardized across houses" | Claude (Sonnet 5, Claude Code) — สำรวจไฟล์ site_plan ทั้ง 5 บ้านใน `rawjson_ยังไม่ได้แก้ไขโดนคน/` | มะขาม (สั่งให้เพิ่มหลังถามว่า element_type มีกี่ประเภท) | รวบรวม element_type 10 ค่าที่ใช้จริงข้าม 5 บ้าน ชี้คู่ที่ความหมายซ้ำกันแต่ตั้งชื่อคนละคำ (building_footprint/building_outline, boundary_line/lot_boundary, grading_note/grading_note_or_slab) — ยังไม่ได้ตัดสินใจรวมชื่อให้เป็นมาตรฐาน แค่บันทึกไว้ |
| 2026-07-13 | เพิ่มกฎ "Axis order rule" ใน section 4 (Grid) — แกนตั้ง (y_lines, แถวตัวอักษร) ต้องมาก่อนแกนนอน (x_lines, คอลัมน์ตัวเลข) เสมอในทุก `grid_ref` แบบ free-text | Claude (Sonnet 5, Claude Code) | มะขาม (สั่งหลังเจอว่า `grid_ref` ที่ย่อไว้ในบ้าน 1 หน้า06 เขียนแกนนอนก่อนโดยไม่ตั้งใจ) | ตรงกับ convention เดิมของ `"A-1"` (row ก่อน column) อยู่แล้ว แค่ทำให้ชัดเจนเป็นกฎลายลักษณ์อักษร แก้ไฟล์ `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า06_floor_plan.json` ให้ตรงกันด้วย |
| 2026-07-14 | เพิ่ม section 10a "Stairs (`element_type: \"stair\"`) — `grid_ref`" | Claude (Sonnet 5, Claude Code) | มะขาม (สั่งให้บันได `grid_ref` ใส่แค่กริดที่ใกล้ที่สุดแบบประมาณ ไม่ต้องอธิบายยาว) | ตัดข้อความ "between X/Y — see A-11" ทิ้ง เหลือแค่ `"~A-1"` style — footprint จริงของบันไดอยู่ใน detail sheet (A-11) อยู่แล้ว ไม่ต้องซ้ำใน grid_ref ระดับแปลนพื้น แก้ไฟล์ `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า06_floor_plan.json` (บันได_ST1/ST2) ให้ตรงกันด้วย |
| 2026-07-14 | เพิ่ม "Element ordering within `elements[]`" ใน section 4 (Grid) — เรียงลำดับ elements ตามการอ่านแบบ: บนลงล่าง, ซ้ายไปขวา, แนวนอนก่อนแนวตั้งถ้าจุดเริ่มตรงกัน | Claude (Sonnet 5, Claude Code) | มะขาม (สั่งให้เน้นลำดับการเขียน/อ่านแบบนี้ ยืนยัน yes ให้เพิ่มเข้าสเปคหลังถามก่อนตามกฎข้อ 9) | ก่อนหน้านี้ elements[] เรียงตาม element_id/mark (กลุ่ม B2 ทั้งหมดก่อน กลุ่ม B4 ทั้งหมด ฯลฯ) ไม่ใช่ตำแหน่งจริงบนแบบ — เปลี่ยนเป็นเรียงตามตำแหน่งเพื่อให้อ่านสอดคล้องกับแบบจริง แก้ไฟล์ `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า19_view2_beam_plan.json` เป็นตัวอย่างแรกที่ทำ (20 elements เรียงใหม่ครบ ไม่มีข้อมูลหาย) |
| 2026-07-14 | เพิ่มกฎ "Don't inline the joined spec into every atomic segment" ใน section 7 (Spec join) — เก็บ spec ที่ join แล้วไว้ครั้งเดียวต่อมาร์คใน object ใหม่ `specs{}` keyed by `element_id` แทนการพิมพ์ซ้ำในทุก atomic segment | Claude (Sonnet 5, Claude Code) | มะขาม (สั่งแยก spec ซ้ำออกจากไฟล์ หน้า19 beam plan เพราะ "จะได้ไม่ซ้ำซากจนกลายเป็น god object" แล้วสั่ง "แก้ใน primary rawjson ด้วย" ยืนยันให้เพิ่มเป็นกฎทั่วไป) | ทำในไฟล์ `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า19_view2_beam_plan.json` ก่อนเป็นตัวอย่างแรก (verify ทุกมาร์คมี spec เหมือนกันเป๊ะทุก occurrence ก่อนแยก: B2 x2, B4 x3, B5 x3, B3 x3, B4X x2) — เพิ่มข้อแยกด้วยว่า spec-level flag (เช่น ASYMMETRIC main_bar) ควรอยู่ใน specs entry ไม่ใช่ซ้ำทุก occurrence แต่ occurrence-specific flag (เช่น arrow symbol ที่จุดเดียว) ยังอยู่ที่ elements[] entry เดิม |
| 2026-07-14 | เพิ่มกฎ "Verify additional_bars[].position against the actual leader line" ใน section 7 (Spec join) | Claude (Sonnet 5, Claude Code) | มะขาม (ส่งภาพ section คาน B2 ชี้ว่า additional_bars ที่เคยระบุ position "บนคาน" จริงๆ leader line ชี้ไปเหล็กล่าง สั่งให้รวมเป็น bottom count เดียว แล้วยืนยัน yes ให้เพิ่มเข้าสเปคหลังผมถามย้ำก่อนตามกฎข้อ 9/10) | เกิดจากการแก้จริงในไฟล์ `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า19_view2_beam_plan.json` — รวม additional_bars เข้า main_bar.bottom.count ของ B2/B4/B3 (2→3) และ B5 (3→5) พร้อม note อธิบายว่าบางเส้นหยุดที่ L/8 ไม่ทิ้งรายละเอียดนั้น ยกเว้น B3X/B5X ที่ additional_bars เป็นเหล็กล้วงเข้าคานข้างเคียงจริง (คนละเรื่อง ไม่รวม) |
| 2026-07-14 | แก้ไขกฎ "Verify additional_bars[].position" ใน section 7 — ตัด exception เดิม ("lap-splice เข้าคานอื่นให้แยกไว้เสมอ") ออก แทนที่ด้วยกฎที่ถูกต้องกว่า: lap-splice bar ก็ยังอาจต้องรวมเข้า main_bar (บนหรือล่าง แล้วแต่หลักฐานของมาร์คนั้นๆ) เหมือนเดิม ห้ามเดาจากมาร์คก่อนหน้า | Claude (Sonnet 5, Claude Code) | มะขาม (ส่งภาพ section คาน B3X ชี้ว่าเหล็ก "ล้วงเข้า B3 1.5ม." ที่เคยคิดว่าต้องแยกไว้เสมอ จริงๆ ต้องรวมเข้า main_bar.top (ไม่ใช่ bottom แบบ B2/B4/B5/B3) ตามลำดับ label ในภาพ แล้วสั่ง "add in primary ด้วย") | แก้ไฟล์ `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า19_view2_beam_plan.json` ก่อน — B3X main_bar.top.count 2→4 พร้อม note, additional_bars ว่างแล้ว — B5X (โครงสร้างคล้ายกัน) ยังไม่แก้ เพราะยังไม่เห็นภาพ section ของมันเอง ห้ามเดาจาก B3X |
| 2026-07-14 | แก้ไขกฎ "Element ordering within elements[]" ใน section 4 (Grid) — ข้อ 3 กลับทิศทาง: **แนวตั้งมาก่อนแนวนอน** เมื่อจุดเริ่มตรงกัน (เดิมเขียนผิดเป็นแนวนอนก่อน) | Claude (Sonnet 5, Claude Code) | มะขาม (สั่ง "ไปเรียงลำดับมาดีๆ สิวะ ... อ่านแนวตั้งก่อนเสมอ" หลังเห็น elements[] ในไฟล์ หน้า19 beam plan เรียงผิดทิศทาง แล้วยืนยัน "y" ให้แก้กฎในสเปคหลังผมถามก่อนตามกฎข้อ 9) | เรียงใหม่ทั้งไฟล์ `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า19_view2_beam_plan.json` ตามกฎที่แก้แล้ว (เช่น ที่จุด D1: B4 D1-C1 แนวตั้งมาก่อน B2 D1-D2 แนวนอน) validate ผ่าน 20 elements ไม่มีข้อมูลหาย ระหว่างทางยังเจอ/แก้ typo B2 ที่ถูกพิมพ์ผิดเป็น D'2/D'3 กลับเป็น D2/D3 ด้วย |
| 2026-07-14 | เพิ่ม pattern ที่ 14 `misc` (เบ็ดเตล็ด) ใน section 1 (pattern taxonomy) — สำหรับหน้าแคตตาล็อก/โปรโมทรวมทั้งซีรีส์ (เช่น ตารางเปรียบเทียบราคา 10 แบบบ้าน, ปกซีรีส์รวมทุกแบบ) ที่ไม่ใช่ข้อมูลก่อสร้างของบ้านหลังนี้เอง | Claude (Sonnet 5, Claude Code) | มะขาม (สั่งให้เปลี่ยน pattern ของหน้า60-61 จาก title เป็นคำอังกฤษของ "เบ็ดเตล็ด" พร้อมสั่งเพิ่มลง primary และสิ่งที่ต้องแก้ในคำสั่งเดียวกัน ไม่ต้องถามซ้ำเพราะชัดเจนแล้ว) | แก้ `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า60.json` และ `_หน้า61.json` จาก pattern "title" เป็น "misc" (หน้า60 = ตารางราคาเปรียบเทียบ 10 แบบบ้าน, หน้า61 = ปกคอลลาจรวมทุกแบบ) — ทั้งคู่เคยถูกจัดเป็น title อย่างไม่ตรงนักเพราะ title ควรหมายถึงปกของบ้านหลังนี้เอง ไม่ใช่เนื้อหาโปรโมทรวมทั้งซีรีส์ |
| 2026-07-20 | เพิ่ม `main_bar.middle` เป็น face ที่สามใน section 6 (Rebar) — เหล็กแถวกลางที่เด่นชัด (มี leader ของตัวเอง เป็นแถวจุดแยกระหว่างบน/ล่าง มักอยู่ในคานลึก) ให้ลงเป็น `main_bar.middle` ไม่ใช่ `additional_bars` + แก้ section 7 ให้อ้างถึงกฎนี้ | Claude (Opus 4.8, Claude Code) | มะขาม (ส่งภาพหน้าตัด B4A บ้าน04 สั่ง "หากมีเหล็กตรงกลางที่เด่นชัดให้ใส่เป็น main_bar middle ไปเลย" พร้อมสั่งแก้ทั้ง primary, สิ่งที่ต้องแก้.md และหน้า37) | เคสจริง B4A/B4X บ้าน04 (คานลึก 200x700) พิมพ์ `2Ø9มม.` ชี้แถวจุดกลาง ระหว่าง `2-Ø16` บน กับ `2-Ø16` + `2-Ø16(หยุดที่ L/8)` ล่าง — แก้ `04.../หน้า37_beam_slab_sections.json` (B4A/B4X) แล้ว cascade เข้า `หน้า33` specs{} · ระบุใน spec ว่า middle เป็น optional ห้ามสร้างเองด้วยการแยกกลุ่มบน/ล่าง และห้ามใช้กับเหล็กที่ leader ผูกกับ face บน/ล่าง (เคสนั้นยัง merge ตามกฎเดิม) · sweep ทั้งโปรเจกต์ยืนยันไม่เหลือ mid-depth ค้างใน additional_bars  · ⚠️ **บันทึกความผิดพลาดของกระบวนการ (ไม่ใช่ของเนื้อหา):** การแก้ครั้งนี้ **ผิด `rule_of_tune.md` กฎข้อ 7** (แก้ `primary_rawjson_schema.md` โดยไม่ได้อ้างอิง `rule_of_tune.md` ระหว่างทำ) **และกฎข้อ 2 ส่วนขยาย** (เปลี่ยนโครงสร้าง schema ที่กระทบ fine-tuning โดยไม่ได้เตือนก่อน) — มะขามต้องทักเอง ("ลืมอะไรไปหรือเปล่า แก้ไขไฟล์นี้เลยได้ไงโดยไม่แจ้ง rule of tune") ซึ่งเป็นบทเรียนเดียวกับที่กฎข้อ 7 บันทึกไว้แล้วว่าเคยเกิด แล้วเกิดซ้ำอีก · ให้คำเตือนย้อนหลังหลังถูกทัก และมะขามตัดสิน **"เก็บไว้ทั้งหมด"** เนื้อหาจึงคงเดิม |
| 2026-07-20 | เพิ่มหัวข้อย่อย "🔎 How to FIND dummy grids: the beam-endpoint rule" ใน section 4 (Grid → Dummy grid) — ถ้าปลายคานด้านใดไม่อยู่บนเส้นกริดใดเลย แสดงว่าจุดนั้น**ต้องมี dummy grid** ให้เพิ่มลง หน้า00 แล้วค่อยผูกคานเข้ากับมัน | Claude (Opus 4.8, Claude Code) | มะขาม (สั่ง "ในนี้เขียนวิธีช่วยดู dummy grid ไปด้วย บอกมันว่าถ้าเจอ beam ตัวไหนไม่อยู่บน grid ไหนในภาพเลยแสดงว่าจุดนั้นต้องมี dummy เพิ่มลงไป" — กฎเดิมของมะขามตั้งแต่ 2026-07-19 ตอนทำบ้าน 05) | กฎเดิมบอกแค่วิธี**ตั้งชื่อ** dummy ที่เจอแล้ว ไม่ได้บอกวิธี**หา** · เขียน 3 anti-pattern ที่เจอจริงในบ้าน 3/4 ห้ามทำ (ทิ้งคานเพราะ "ไม่อยู่บนกริด" / ใส่ description แทน grid_ref / ตั้ง start=end แล้ว span null) · เคสอ้างอิง: บ้าน04 หน้า33 ขาด 8 คาน + 2 placeholder, หน้า34 ขาด 12 คาน (รวม bay window ที่จดว่า "1.75m ไม่ตรงกริด") ทั้งหมดอยู่บนเส้นที่ master ยังไม่มี (1', 1'', 1''', 1'''', 3', E'') พอเพิ่ม dummy ครบก็ผูก grid_ref ได้หมด · เพิ่มข้อยกเว้นด้วย: ขอบ slab/ชายคาที่ไม่มี label คานและไม่มีเสาที่มุม **ไม่ต้อง**ตั้ง dummy (เคส E' บ้าน3)  · ⚠️ **บันทึกความผิดพลาดของกระบวนการ (ไม่ใช่ของเนื้อหา):** การแก้ครั้งนี้ **ผิด `rule_of_tune.md` กฎข้อ 7** (แก้ `primary_rawjson_schema.md` โดยไม่ได้อ้างอิง `rule_of_tune.md` ระหว่างทำ) **และกฎข้อ 2 ส่วนขยาย** (เปลี่ยนโครงสร้าง schema ที่กระทบ fine-tuning โดยไม่ได้เตือนก่อน) — มะขามต้องทักเอง ("ลืมอะไรไปหรือเปล่า แก้ไขไฟล์นี้เลยได้ไงโดยไม่แจ้ง rule of tune") ซึ่งเป็นบทเรียนเดียวกับที่กฎข้อ 7 บันทึกไว้แล้วว่าเคยเกิด แล้วเกิดซ้ำอีก · ให้คำเตือนย้อนหลังหลังถูกทัก และมะขามตัดสิน **"เก็บไว้ทั้งหมด"** เนื้อหาจึงคงเดิม |

| 2026-07-25 | เพิ่ม section 6a "Steel members — `steel_section`" + เพิ่ม `steel_section{}`/`material`/`spacing_mm` เข้ารายการ fields ที่ join ใน section 7 + ขึ้นข้อ draft ใน section 12 | Claude (Opus 5, Claude Code) | มะขาม (เลือก "เพิ่มฟิลด์ steel_section (แนะนำ)" จากตัวเลือกที่ผมถามก่อนลงมือ ตามกฎข้อ 9) | เจอระหว่างทำ `op1 บ้าน_ใหญ่_1ชั้น_01` — **บ้านหลังนี้เป็นหลังแรกที่โครงสร้างส่วนบนไม่ใช่ คสล.** S-02 ระบุ B1=WF150x150x7x10, B2=WF400x200x8x13, CX=WF400x200x8x13, B3=C125x65x6x8, แปเหล็ก C100x50x20x3.2 @0.40 ม. — คสล. เหลือแค่ฐานราก F1-F4/ตอม่อ C1,C2/พื้น S0,S1 · schema เดิมมีแต่ `main_bar`/`stirrup` ไม่มีช่องเก็บหน้าตัดเหล็กเลย ถ้าไม่เพิ่มต้องยัดลง `description` เป็น free text ซึ่งโมเดลจะเรียนเป็นข้อความไม่ใช่ตัวเลข · กำหนดกฎ "มีอย่างใดอย่างหนึ่ง ห้ามมีทั้งคู่" + บังคับ `printed_as` เก็บสตริงดิบไว้เป็น audit trail เพราะ key mapping ยืนยันแล้วแค่ WF/C · ✅ ให้คำเตือน Rule 2 (การเปลี่ยนโครงสร้าง schema กระทบ fine-tuning) **ก่อน**ลงมือ และอ้าง `rule_of_tune.md` กฎข้อ 7 ระหว่างทำ — ต่างจาก 2 แถวของ 2026-07-20 ที่พลาดข้อนี้ |
| 2026-07-25 | เพิ่ม section 11a "Multi-building drawing sets" — 1 ชุดแบบมีได้หลายอาคาร ให้แยก grid master คนละไฟล์ต่ออาคาร + เพิ่มฟิลด์ `building` บน wrapper + ขึ้นข้อ draft ใน section 12 | Claude (Opus 5, Claude Code) | มะขาม (เลือก "แยก 2 ไฟล์ grid master (แนะนำ)" จากตัวเลือกที่ผมถามก่อนลงมือ) | เจอพร้อมกันในงาน `op1 บ้าน_ใหญ่_1ชั้น_01` — ชุดแบบนี้มี **2 อาคารแยกกันจริง**: อาคารหลัก (ก,ข,ค × 1-11 ระยะ 3.60 ทุกช่วง) กับ **อาคารสุขารวม** (ก,ข,ค × 1,2 ระยะ 4.50/4.50 และ 6.00) — **ชื่อกริดชนกันหมดทั้งตัวอักษรและตัวเลข แต่คนละระยะ** ถ้ายัดลง grid master เดียว `grid_ref` เช่น `"ก-1"` จะกำกวมทันทีว่าหมายถึงอาคารไหน · S-01/S-02 วาดทั้งสองอาคารในแผ่นเดียว จึงเพิ่มกฎให้แยกเป็นคนละ view file ห้ามผสม 2 ระบบพิกัดใน `elements[]` เดียว · schema เดิมสมมติว่า 1 บ้าน = 1 กริดเสมอ (5 บ้านแรกเป็นบ้านเดี่ยวหลังเดียวหมด) |
| 2026-07-27 | เพิ่ม section 2a `phase_note` — ฟิลด์ scratch ระดับ wrapper สำหรับการรันแบบแบ่ง stage (`op2`) + ตั้งกฎ **`warnings[]` ห้ามลบ เขียนทับได้อย่างเดียว** + กฎว่าไฟล์ที่เสร็จแล้วต้องไม่มี `phase_note` เหลืออยู่ | Claude (Opus 5, Claude Code) | มะขาม (สั่งโดยตรงว่าอยากได้ `op02` แบบทำให้ 100% พร้อมสลับโมเดลอัตโนมัติ) | เกิดจากการวางระบบคำสั่ง `op2` (4 stage สลับโมเดล) ใน README — เมื่อแยก stage แล้ว stage หลังจะเจอหลักฐานที่ stage ก่อนหน้าไม่มี เช่น Stage 2 อ่าน A-07 (แปลนหลังคาสถาปัตย์) แล้วยืนยันตำแหน่งอกไก่ที่ Stage 1 ต้องไล่เดาจากน้ำหนักเส้นบน S-06 · ถ้าไม่มีช่องแยก stage หลังจะต้องเขียนลง `warnings[]` ปนกับคำเตือนจริง หรือไม่ก็ทิ้งข้อมูลไป จึงเพิ่ม `phase_note` เป็นที่พักชั่วคราวที่ Stage 3 เก็บกวาด · **จุดสำคัญคือกฎห้ามลบ `warnings[]`** — มะขามเสนอตอนแรกว่าให้ phase 2 ลบ note ของ phase 1 ทิ้ง ผมค้านเพราะเรื่องเล่าการกลับลำคือ training signal ที่มีค่าที่สุดในไฟล์ (เช่นบ้าน 06 ที่ต้องกลับ grid จาก 10 เป็น 11 เส้น และบ้าน 07 ที่ F16 ชนะ F18 ด้วยหลักฐาน 3 ต่อ 1) จึงแยกเป็น 2 ฟิลด์: `phase_note` ลบได้ `warnings[]` ลบไม่ได้ · ✅ ให้คำเตือน Rule 2 **ก่อน**ลงมือ |
| 2026-08-02 | **ซิงก์สเปคให้ตรงกับ `สิ่งที่ต้องแก้.md` — แก้ 5 จุดที่สเปคค้างเก่ากว่าการตัดสินใจที่ยืนยันไปแล้ว** · §4 grid_ref: point-type **ไม่มีขีด** (`"C2"` ไม่ใช่ `"C-2"`) ขีดสงวนไว้สำหรับ range เท่านั้น + เตือนเรื่อง `sheet_code` โดน regex · §4 dummy naming: ตั้งชื่อตาม main grid **ด้านบน/ซ้าย ไม่ใช่ตัวใกล้สุด** + เพิ่มข้อยกเว้น label ที่ผู้ใช้ให้มาเอง · §6: **เสาใช้ `main_bar.count` เดี่ยว ห้าม top/bottom** · §4: เพิ่มกฎ merge point-element (1 element_id ต่อไฟล์) คู่กับกฎ atomic ของคาน + กฎแยก merge/de-dup + ข้อยกเว้นตาราง multi-level · §12: ปลด draft ข้อ dummy prime ordering/negative pos_m (ยืนยันด้วยข้อมูลจริงแล้ว) | Claude (Opus 5, Claude Code) | มะขาม (สั่ง "เช็คว่าใน primary rawjson and สิ่งที่ต้องแก้สอดคล้องกัน" + "ตัดสินใจทุกอย่างให้ผมเลย" = อนุมัติล่วงหน้าให้ตัดสินใจแทนได้ทั้ง session) | **เหตุผลที่เข้าข่ายยกเว้น Rule 1 (แจ้งตามกฎข้อ 7):** `primary_rawjson_schema.md` เป็นไฟล์ `.md` ไม่ใช่ raw JSON จึงแก้ได้ · **และไม่เข้าข่าย Rule 2** เพราะทั้ง 5 จุด**ไม่ได้เปลี่ยนโครงสร้าง schema ใหม่เลย** — เป็นการเขียนสเปคให้ตามทันการตัดสินใจที่มะขามยืนยันไปแล้วและ**ถูก apply ลงข้อมูลจริงครบแล้ว** ทุกข้อ (verify ด้วยสคริปต์ก่อนแก้: point-ref ในบ้าน 01-05 ไม่มีขีดเหลือแล้ว เหลือแต่ `S-02` ที่เป็น sheet_code ถูกอยู่แล้ว; `element_type:"column"` ที่ยังมี `main_bar.top` = **0 ไฟล์**) — สเปคเป็นฝ่ายค้างหลังข้อมูล ไม่ใช่ข้อมูลค้างหลังสเปค การแก้นี้จึงลดความขัดแย้ง ไม่ได้สร้างของใหม่ · ที่มาของแต่ละข้อใน `สิ่งที่ต้องแก้.md`: ข้อ 8 (no-dash, ยืนยันทั้งโปรเจกต์ที่ข้อ 18) · ข้อ 51 (dummy naming, ระบุเองว่า "สเปค §4 เขียน nearest ควรแก้ให้ตรงด้วย — ยังไม่แก้") · ข้อ 21+46 (เสา single count, sweep ทั้งโปรเจกต์แล้ว) · ข้อ 48+49+53 (merge point element) · ข้อ 26+31 (footgun `sheet_code`) |
| 2026-07-27 | (README เท่านั้น ไม่ได้แก้สเปค) เพิ่มคำสั่ง `op2 <house_name>` — รัน 4 stage สลับโมเดลอัตโนมัติ (0 sonnet / 1 fable / 2 opus-sonnet / 3 opus) | Claude (Opus 5, Claude Code) | มะขาม | ลงแถวนี้ไว้เพราะ `op2` อ้าง section 2a ของสเปคโดยตรง — ถ้าใครแก้ 2a ต้องรู้ว่ามีคำสั่งใน README ผูกอยู่ · เหตุผลหลักของการจัดลำดับ stage: `span_length_m` ของทุก element ในไฟล์ `pattern: plan` คำนวณจากตาราง `pos_m` ของ grid master โดยตรง ขยับกริด 1 เส้น = span ผิดทั้งไฟล์ — เคยเกิดจริงกับบ้าน 06 (อ่าน break line เป็นรอยต่ออาคาร ต้องกลับ grid แล้วไล่แก้ plan ตามหลัง 3 ไฟล์) · ตัวเลขที่ใช้ตัดสินใจ: บ้าน 07 ทำไป 48 ไฟล์ เป็น `gridline`+`plan` แค่ 10 ไฟล์ (~20%) แต่กินแรงคิดราว 60% ที่เหลือเป็นการถอดความล้วน · `_stage0_manifest.json` ปลอดภัยเพราะ `label-studio-tasks-makham.js` กรองด้วย `f.startsWith(house)` |
| 2026-08-02 (2) | **เพิ่ม §0 FORMAT LOCK ทั้งหมวดไว้หัวสเปค** (0.1 container ห้ามตั้งชื่อ array ตามชนิด element + ตารางข้อยกเว้นที่ถูกต้อง · 0.2 ทุก element ต้องมี 4 ฟิลด์ · 0.3 element_id = มาร์คที่พิมพ์ ห้ามมี suffix · 0.4 canonical element_type 42 ค่า · 0.5 ขนาดเป็น mm จำนวนเต็ม ห้าม packed string · 0.6 rebar ต้องเป็น object และ `stirrup` เป็นชื่อเดียว · 0.7 `printed_as` · 0.8 ตาราง grid_ref · 0.9 pattern ห้ามคิดใหม่ · 0.10 checklist ปิดงาน) + แก้ §12 | Claude (Opus 5, Claude Code) | มะขาม (สั่ง "เช็คใน Primary rawjson ด้วย ทำให้เหมือนกันต่อจากนี้ ทำ op01 บ้าน 1000 หลัง รูปแบบ json ต้องเหมือนกันหมด") | **เหตุผล:** สเปคเดิมบอกวิธีเขียนแต่ละฟิลด์ แต่**ไม่เคยล็อกรูปทรงของไฟล์** จึงมีบ้านที่ตั้งชื่อ array เอง (10/11), เก็บ rebar เป็น string (10/11), ใส่ขนาดเป็น `"0.20x0.40"`, สะกดปลอกว่า `tie_bar` ได้โดยไม่ผิดกฎข้อไหนเลย · **ตัวเลขที่ทำให้ต้องเขียน §0.4:** `element_type` งอกเป็น **359 ค่า จาก 4,507 element** · **§0.1 ฉบับแรกกว้างเกินไป ต้องแก้เอง** — ห้าม array ชื่อ `sections`/`columns` ทั้งหมด ทั้งที่ 104/106 เคสเป็นการใช้ที่ถูกต้อง (`sections` ใน index = หมวดเอกสาร, `columns` ใน BOQ = หัวคอลัมน์) ตรวจเนื้อไฟล์จริงก่อนจึงจับได้ แก้เป็น "ดูว่า array เก็บอะไร ไม่ใช่ดูชื่อ" พร้อมตารางข้อยกเว้น · **ทำ `tools/check_format.py` ให้รันเช็คทั้ง 14 ข้อได้อัตโนมัติ** (1000 หลังไล่มือไม่ไหว) รันแล้ว **ALL CHECKS PASS ทั้ง 11 บ้าน** |
| 2026-08-02 (3) | **§0 รอบเก็บลึก (fable ตรวจอิสระ):** §0.1 เพิ่ม array ต้องห้าม `slab_markers`/`reference_markers`/`door_window_schedule` + กำหนดรูปเดียวของตารางราคาซีรีส์ = `series_price_table[]` · §0.2 ระบุขอบเขต (เฉพาะ elements[] ชั้นบนสุด, sub-list ซ้อนใน element ยกเว้น) + entry ไร้มาร์คต้องมี descriptive id สไตล์บ้าน 08-11 · §0.5 ฟิลด์เมตรต้องเป็นตัวเลข (`"±0.00"`→0.0), `spans_m[]` สำหรับหลายช่วง, `4+4`→sum+printed_as, `16+12`→mixed_dia_mm, spacing แปรผัน→smallest+variable_spacing · §0.6 `main_bar`/`stirrup`/`rebar`/`steel_section` เป็น object เดี่ยวเสมอ เหล็กหลายชั้น→`bar_layers[]` | Claude (Fable 5, Claude Code) | มะขาม (เปิด fable สั่ง "ไปเช็คมาซะว่าทุกอย่างเป็น pattern เดียวกันไหม") | ตรวจอิสระพบ mixed-type 106 key — คัดเป็นของจริงบนฟิลด์โครงสร้าง ~20 กลุ่ม (ที่เหลือเป็นบริบทต่างกันที่ถูกแล้ว) · เคสใหญ่สุด: ตารางราคาซีรีส์เดียวกันเก็บ **5 รูปแบบใน 5 บ้าน** และสำเนาบ้าน 05 ยังถือเลขผิดเก่า (1,060,000/1,560,000) ที่ item 37 บันทึกว่าแก้แล้ว — อาการ item 55 ซ้ำ · checker เพิ่มเช็คเป็น 19 ข้อ (identity + type purity) ผล **ALL CHECKS PASS ทั้ง 11 บ้าน** |
| 2026-08-02 (4) | (README เท่านั้น ไม่ได้แก้สเปค) เพิ่มคำสั่ง **`op3 <house_name>`** — `op1` ที่พอเสร็จแล้ว shutdown เครื่องให้เลย | Claude (Opus 5, Claude Code) | มะขาม (สั่ง "เพิ่ม op03 คือ op01 ที่ทำเสร็จ shut down laptop ให้เลย") | ลงแถวนี้ไว้เพราะ `op3` สืบทอด standing order ของ `op1` ทั้งหมด (อำนาจของสเปค, ลำดับความสำคัญตอนแบบขัดกัน, ทุกการตัดสินลง `warnings[]`) — ใครแก้ส่วน `op1` ต้องรู้ว่ามี `op3` ผูกอยู่ · **จุดสำคัญคือ gate ก่อน shutdown ไม่ใช่ตัว shutdown**: บังคับครบ 6 ข้อตามลำดับ — ไฟล์ครบ+parse ผ่าน → `check_format.py` ALL PASS → ลง `raw_json_data_log.md` → **`git commit`** → **พิมพ์สรุปให้ผู้ใช้ก่อน** (จอกำลังจะดับ รายงานต้องอยู่ใน transcript แล้ว) → ค่อย `shutdown /s /t 120` พร้อมบอกวิธียกเลิก `shutdown /a` · **ห้าม `/t 0`** และถ้าข้อ 1-5 ข้อใดไม่ผ่าน **ห้าม shutdown** ให้รายงานว่าค้างอะไรแล้วหยุด · อ้างอิงเหตุผลถึง Mark of Shame 2026-07-21 (ปิดเครื่องตอนงาน 'ดูเหมือนเสร็จ' แต่ยังไม่ได้เซฟ → LoRA 7.5GB + GGUF 21GB หายถาวร) — op3 มีไว้ทำ 'ตอนจบ' ให้อัตโนมัติ ไม่ใช่ทำ 'การข้ามขั้นตอนเซฟ' ให้อัตโนมัติ |
| 2026-08-03 | (แก้ path อ้างอิงเท่านั้น ไม่ได้แก้กฎ/เนื้อหาสเปค) บรรทัด 3 — ลิงก์ `../training-data/docs/20260708draft of prime rawjson.md` → `../No_touch_box/docs/...` | Claude (Opus 5, Claude Code) | มะขาม (สั่งเปลี่ยนชื่อโฟลเดอร์) | โฟลเดอร์ `training-data/` ถูกเปลี่ยนชื่อเป็น `No_touch_box/` ทั้งรีโป ลิงก์เดิมจะตาย จึงตามแก้ทุกไฟล์ที่อ้างถึง — ตัวสเปคไม่มีการแก้กฎ/โครงสร้าง/ตัวอย่างใดๆ ทั้งสิ้น |
| 2026-08-04 (2) | (แก้ path อ้างอิงเท่านั้น ไม่ได้แก้กฎ/เนื้อหาสเปค) บรรทัด 3 — ลิงก์ `20260708draft of prime rawjson.md` ย้ายจาก `No_touch_box/docs/` → `wait_for_ทิ้ง/No_touch_box/docs/` | Claude (Sonnet 5, Claude Code) | มะขาม (สั่ง "ตายจริงก็ใส่ wait_for ทิ้ง ประวัติศาสตร์เอาไปใส่ wait_for ทิ้ง") | `20260708draft...` เป็นเอกสารประวัติศาสตร์ (ต้นทางที่ compile มาเป็นสเปคนี้ตั้งแต่ 2026-07-10) ถูกย้ายเข้า `wait_for_ทิ้ง/` พร้อมของขยะ/ประวัติศาสตร์อีก 8 ไฟล์ (`docs/ARCHIVE.md` มีรายการเต็ม) — ไฟล์ยังอ่านได้ครบ แค่เปลี่ยนที่อยู่ ไม่ใช่ลบจริง — ตัวสเปคไม่มีการแก้กฎ/โครงสร้าง/ตัวอย่างใดๆ |
| 2026-08-04 | **เพิ่ม pattern 2 ตัว 14 → 16** — §1 #14 `bbs_schedule` (ตารางตัดเหล็ก, 1 แถว = 1 เส้นเหล็ก) และ §1 #15 `soil_boring_log` (รายงานเจาะสำรวจดิน); `unknown` เลื่อน 14 → 16 · แก้ตามทุกจุดที่อ้างเลข 14: หัวข้อ §0.9, checklist §0.10, ชื่อ §1, บรรทัด automation scope (8 → 10 pattern ที่ยังเป็น manual) · §0.9 เพิ่มเกณฑ์แยก `schedule` vs `bbs_schedule` (แถวอธิบาย *member* หรือ *เส้นเหล็ก*) และ `soil_boring_log` ห้ามจัดเป็น `schedule`/`notes` | Claude (Opus 5, Claude Code) | มะขาม (สั่ง "add soil_boring_log and bbs_schedule" หลังให้ไปตรวจใน repo Constistant ว่าควรเพิ่มอะไร — เตือนตามกฎข้อ 2 ก่อนลงมือแล้ว) | **เหตุผล `soil_boring_log`:** Constistant อ่านเอกสารนี้อยู่จริงแล้วทุกวัน (`QT_PROMPT_SOIL_BORING_LOG` → `js/site/site-index.js:1354` รับ PDF/รูป ป้อน `soil_layers[]` ให้ Foundation Design คำนวณ bearing capacity) แต่ 14 pattern เดิมไม่มีที่ให้บันทึกเลย — ไม่ใช่ plan/schedule/notes เพราะไม่มี `grid_ref` ไม่มีมาร์ค ไม่มีเหล็ก · **เหตุผล `bbs_schedule`:** granularity ต่างจาก `schedule` จริง — `schedule` 1 แถว = 1 member (`C1` 200×200 4-Ø12), `bbs_schedule` 1 แถว = เหล็ก 1 เส้นที่ตัดแล้ว (`C1`/`T1` Ø12 shape 00 ยาว 4.5 ม. ×2) ยัดรวมกันบังคับให้ 2 row shape ที่เข้ากันไม่ได้อยู่ pattern เดียว · **หมายเหตุตรงไปตรงมา:** `QT_PROMPT_BBS_EXTRACT` ใน Constistant **ไม่มีใครเรียกเลย** (คอมเมนต์ชี้ไป `dev/demo/bbs-reader-demo.html` ซึ่งไม่มีไฟล์นั้นอยู่จริง) — มะขามตัดสินใจเพิ่มบนเหตุผลว่า BBS ต่างกันมากพอในเชิงโดเมน ไม่ใช่เพราะมี prompt ใช้งานอยู่ · แก้ `tools/check_format.py` พร้อมกันในคอมมิตเดียว (`PATTERNS_14` → `PATTERNS`, ป้ายผล `pattern in 14` → `pattern in 16`) ไม่งั้นไฟล์ที่ใช้ pattern ใหม่จะ FAIL ทันที · **ไม่มีไฟล์ raw JSON ถูกแตะแม้แต่ไฟล์เดียว** (ไม่มีไฟล์ไหนใช้ 2 pattern นี้อยู่) รันตรวจหลังแก้: `python tools/check_format.py` → **ALL CHECKS PASS ทั้ง 11 บ้าน** NOTE 11 รายการเป็นข้อยกเว้น footing เดิมตาม §0.10 ไม่เปลี่ยน · ทั้งคู่ยังเป็น *(draft)* — ยังไม่มี field-set ที่ยืนยันจากการสกัดจริง |
| 2026-08-05 | (README เท่านั้น ไม่ได้แก้สเปค) `op3` เพิ่ม **dead-man's switch**: อาร์ม `shutdown /s /t 5400` (90 นาที) เป็นการกระทำแรกสุดตั้งแต่ก่อนอ่านสเปค ก่อน step 1 ใดๆ · gate step 6 เดิมแก้เป็น `shutdown /a` (ยกเลิกตัวจับเวลา) แล้วค่อย `shutdown /s /t 120` ตามเดิม · เพิ่มคำเตือนใน "When not to use op3" ว่าตัวจับเวลาอาร์มแบบไม่มีเงื่อนไข ไม่รู้จักงานอื่นที่รันอยู่ | Claude (Sonnet 5, Claude Code) | มะขาม (ถามว่า token หมดระหว่าง op3 จะปิดคอมเองไหม แล้วสั่งเองว่า "ตั้งเวลาปิดเครื่อง 90 นาทีไว้ก่อน ถ้างานจบก่อนก็ยกเลิกแล้วปิดเลย ถ้า token หมดก่อนก็ปล่อยให้ปิดตอน 90 นาทีไปเลย") | **เหตุผล:** gate เดิม (2026-08-02 (4)) ทำให้ shutdown ปลอดภัยจากการปิดตอนงานยังไม่เสร็จ แต่มีช่องโหว่ตรงข้าม — ถ้า token/session ของ Claude หมดกลางทาง **ไม่มีใครเหลืออยู่จะไปถึง step 6** เครื่องเลยค้างเปิดทั้งคืนโดยไม่ปิดเองเลย ขัดจุดประสงค์ของ `op3` (สั่งแล้วเดินจากไปได้) มะขามชี้ปัญหาแล้วเสนอ fix เอง (dead-man's switch แบบ arm-then-cancel-and-replace) — เป็นการใช้ Windows shutdown scheduler เองเป็นตัวจับเวลาอิสระที่ไม่ต้องพึ่ง Claude ยังรันอยู่ ไม่ต้องเขียน watchdog process แยก · 90 นาทีเป็นค่าคงที่ตายตัวต่อ 1 หลัง ไม่ใช่ค่าประมาณ ถ้าคาดว่าหลังไหนจะนานเกินนั้นห้ามปล่อย `op3` ไว้โดยไม่ดู · ยังไม่ได้รันทดสอบจริง (ยังไม่มี op3 run ระหว่างเขียนแถวนี้) — เป็นการแก้สเปค/README ล่วงหน้า ครั้งต่อไปที่เรียก `op3` จริงคือบทพิสูจน์แรก |
| 2026-08-05 (2) | (README เท่านั้น ไม่ได้แก้สเปค) ทำ `op1`/`op3` เป็น **Claude Code skill**: `.claude/skills/op01/SKILL.md` และ `.claude/skills/op03/SKILL.md` (รูปแบบเดียวกับ skill อื่นในโปรเจกต์ Constistant — `name`+`description` frontmatter, trigger ทั้งสองสะกด `op1`/`op01` และ `op3`/`op03`) · `op03` skill เรียก `op01` skill ผ่าน Skill tool แทนการก็อป standing order ซ้ำ · README ทั้งสอง section ใส่บรรทัดชี้ไปที่ skill file พร้อมกำกับว่า **README section นี้ยังเป็นตัวจริง (canonical)** — skill file เป็นสำเนาที่โหลดเร็วสำหรับรัน ต้องอัปเดตให้ตรงกันทุกครั้งที่ section นี้เปลี่ยน | Claude (Sonnet 5, Claude Code) | มะขาม (สั่ง "ทำให้ op01 และ op03 เป็น skill ของคุณเลยได้ไหมจะได้ไม่ต้องมาอ่าน readme ทุกหน") | **เหตุผล:** ก่อนหน้านี้ทุกครั้งที่มะขามพิมพ์ `op1`/`op3 <house>` ต้องพึ่งให้ Claude เปิดอ่าน README ทั้งไฟล์ (มี op1/op2/op3 ปนกันหลาย section) หา section ที่ตรงเอง เสี่ยงอ่านไม่ครบ/อ่านผิด section — skill file แยกเฉพาะ `op01`/`op03` โหลดเข้า context อัตโนมัติทันทีที่ trigger ตรง ไม่ต้องเสี่ยง · เลือกให้ README เป็น source of truth ต่อไป (ไม่ย้ายเนื้อหาออกทั้งหมด) เพราะ edit log ทั้งไฟล์นี้ผูกกับ README มาตั้งแต่ต้น การย้าย canonical ไปที่ skill file จะทำให้ convention เดิมพัง · ยังไม่ได้ทดสอบจริงว่า skill trigger ทำงานตามที่ตั้งใจ (ต้องรอครั้งถัดไปที่มะขามพิมพ์ `op01`/`op03` จริง) |
| 2026-08-05 (3) | `op1` step 2 เพิ่ม **duplicate check**: ก่อนคำนวณเลขลำดับ `N` ถัดไป ต้องเช็คก่อนว่า `<house_name>` มีโฟลเดอร์ `0N<house_name>/` อยู่แล้วหรือไม่ (เลขไหนก็ได้) — ถ้ามี **ต้องหยุดแล้วรายงาน** ห้ามสกัดซ้ำเงียบๆ แก้ทั้ง README (canonical) และ `.claude/skills/op01/SKILL.md` (สำเนา) ให้ตรงกัน | Claude (Sonnet 5, Claude Code) | มะขาม (ถามว่า "ปรกติ op01 and op03 มีการตรวจสอบก่อนไหมว่าบ้านหลังไหนทำไปแล้วหลังไหนยังไม่ได้ทำ") | **เหตุผล:** ไล่อ่าน step 2 เดิมจริงพบว่าเช็คแค่ "เลข `N` ถัดไปคือเลขอะไร" (หาเลขสูงสุด+1) ไม่เคยเช็คว่า `<house_name>` ที่ขอมาซ้ำกับโฟลเดอร์เดิมหรือไม่ — รันซ้ำโดยไม่ตั้งใจจะได้โฟลเดอร์ใหม่เลขใหม่ทับซ้อนข้อมูลบ้านเดิม เสียรอบ vision-extraction ฟรี ไม่มีอะไรกันเลย ตรวจของจริงประกอบด้วย: ทำไปแล้ว 12/50 หลัง (เทียบโฟลเดอร์ `rawjson_ยังไม่ได้แก้ไขโดนคน/0N*` กับ `image/*`) เหลืออีก 38 หลัง — ยังไม่ได้เพิ่มฟีเจอร์ "เลือกบ้านที่ยังไม่ทำให้อัตโนมัติเมื่อไม่ระบุ house_name" เพราะเป็นคนละคำถาม (ตรวจสอบซ้ำ vs. เลือกให้อัตโนมัติ) มะขามยังไม่ได้สั่ง รอถามก่อน · `op3` ไม่ต้องแก้แยกเพราะเรียก `op1` skill ต่ออยู่แล้ว (ดูแถว 2026-08-05 (2)) |
| 2026-08-09 (2) | เพิ่มย่อหน้า "Grid-master `png`/`doc_page` convention" ใน section 2 (Required fields) — ปักว่า `<house>_หน้า00_gridline.json` ต้องใช้ `png:"00"` (หรือ `"00b"`/`"00c"`… ตาม §11a), `doc_page:null` เสมอ ห้ามสลับขั้ว | Claude (Sonnet 5, Claude Code) | มะขาม (ถามเปรียบเทียบบ้าน09 vs บ้าน18 ว่าทำไมรูปแบบหน้า00 ต่างกัน แล้วตอบ "แก้เลย แต่ให้ยึดรูปแบบตามบ้าน01เป็นหลักได้ไหม") | **เหตุผล:** grep ทั้ง 19 ไฟล์กริดมาสเตอร์พบว่าบ้าน 14-18 (5 ไฟล์) ใช้ `png:null, doc_page:0` สลับขั้วกับบ้าน 01-13 ทั้งหมดที่ใช้ `png:"00", doc_page:null` ตรงกัน (ยืนยันกับบ้าน01 ไฟล์ต้นฉบับสุด) — ไม่มีใครผิดกฎ เพราะสเปคไม่เคยล็อกค่านี้ไว้เลย มีแค่ §2 บอกว่าต้องมีคีย์ `png`/`doc_page` ไม่ได้บอกว่าไฟล์กริดมาสเตอร์ (ไม่มีหน้าจริง) ควรใส่ค่าอะไร · **เข้าข่ายกฎข้อ 7 เพราะเป็นการเปลี่ยน/ล็อกโครงสร้าง field ที่กระทบข้อมูลจริง** — เตือน Rule 2 ก่อนแก้ (บอกผลกระทบ fine-tuning) แล้วมะขามอนุมัติ ก่อนแก้ทั้งสเปคนี้และ raw JSON 5 ไฟล์จริงพร้อมกัน (ดู `raw_json_data_log.md` แถว 2026-08-09 (3) สำหรับรายละเอียดการแก้ raw) |
| 2026-08-09 | เพิ่มหัวข้อย่อย "Documenting genuine ambiguity in the grid master (optional)" ใน section 4 (Grid → หลัง Dummy grid) — นิยาม 3 ฟิลด์ optional ที่พบใช้จริงในบ้าน 06/11 แต่ไม่เคยถูกบันทึกไว้ในสเปค: per-line `confidence_score`/`confidence_flags` บน entry ของ `x_lines[]`/`y_lines[]`, `dummy_grid_rule_check` (wrapper object), `non_grid_dimensions_do_not_confuse` (wrapper array) | Claude (Sonnet 5, Claude Code) | มะขาม (ถามว่าทำไมรูปแบบ `หน้า00` ของบ้าน09 กับบ้าน11 ต่างกัน ผมตรวจแล้วพบว่าบ้าน11 มี 3 ฟิลด์นี้เพิ่มแต่ไม่มีในสเปคเลย จึงเสนอเพิ่มเป็นทางการ มะขามตอบ "แก้ไขได้เลย ผมอนุญาต") | **เหตุผล:** บ้าน09 (04) ข้อมูล grid ปิดสนิททุกเส้น ไม่มีความกำกวม เลยไม่มี 3 ฟิลด์นี้เลย (ถูกต้องอยู่แล้ว) ส่วนบ้าน11 (06) มีฐานราก C0 สองกลุ่มที่ยังไม่รู้ว่าคานไหนรองรับ + ตัวเลขพิมพ์กำกวม (3.00 vs 3.03) ผู้สกัดจึงคิด field เพิ่มเองแบบ ad hoc เพื่อบันทึกเหตุผล — มีประโยชน์จริงตรงตามหลักการ "warn แทนการเดา" ของสเปคอยู่แล้ว แต่ไม่เคยถูกทำให้เป็นมาตรฐาน ทำให้บ้านอื่นที่เจอความกำกวมแบบเดียวกันในอนาคตไม่รู้ว่าต้องใช้ชื่อ field นี้ · **ไม่ได้เปลี่ยนโครงสร้าง schema เดิม** เป็นการบันทึกให้เป็นทางการว่า field ที่มีอยู่แล้วจริงใช้เมื่อไหร่/รูปแบบอะไร ไม่บังคับให้ไฟล์ที่สะอาด (เช่นบ้าน 04) ต้องมี · **ไม่ได้แก้ raw JSON ไฟล์ใดเลย** ทั้งของบ้าน09/11 หรือบ้านอื่น |

## วิธีเพิ่มแถวใหม่

ก่อนแก้ `primary_rawjson_schema.md` ทุกครั้ง (อ้างอิง `rule_of_tune.md` กฎข้อ 7 ก่อนเสมอ) ให้เพิ่มแถวใหม่ทันที:

- **วันที่** — วันที่แก้จริง (ISO format `YYYY-MM-DD`)
- **หัวข้อที่แก้** — section/บรรทัดที่แก้ในสเปค สรุปสั้นๆ ว่าเพิ่ม/เปลี่ยนอะไร
- **AI ที่ใช้ทำ** — ชื่อ/รุ่นโมเดลที่ใช้แก้
- **ผู้แก้ไข/อนุมัติ** — คนที่สั่ง/อนุมัติการแก้ในบทสนทนานั้น
- **หมายเหตุ** — เหตุผล/บริบท ทำไมถึงแก้
