# Things To Fix

Detailed log of every correction instruction Makham gave in `json_แก้ไขแล้ว\` — written detailed enough to **be applied directly to newly-imported files without needing to re-read the original conversation** (full path + full field + old value → new value + reasoning/evidence).

Separate from `README.md` (a short summary log of what's been fixed) — this file is the **detailed spec of each individual fix**, meant to be reusable.

---

## 01บ้าน_เล็ก_1ชั้น_01

### 1. Grid master — dummy grid `3'` and `3''`

**File:** `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า00_gridline.json`
**Field:** `grid.x_lines[]` (x-grid array)

**Before:**
```json
{ "id": "3'", "pos_m": 8.5, "type": "dummy", ... }
```
(only one line)

**After:**
```json
{ "id": "3'", "pos_m": 7.6, "type": "dummy", "confidence_score": 1, ... },
{ "id": "3''", "pos_m": 8.5, "type": "dummy", "confidence_score": 1, ... }
```

**Reasoning/evidence:**
- `3'` (7.6 = grid3[7.0] + 0.60): page05 (A-03 site plan, `element_id: "building_footprint"`) states directly that the building projects only 0.60m past grid 3 — a real wall/tie-beam edge, confirmed by the tie-beam corner projection of 0.60m on the tie-beam plan (page20)
- `3''` (8.5 = grid3[7.0] + 1.50): page19 (S-02 beam plan) beam B4X runs the full 1.5m from C-3/B-3 — a beam projecting out to support the eave/canopy, further beyond the real wall edge (3'); a different line from the true 3'
- **The old value (8.5 alone) was wrong** because it originally cited B4X as "independent evidence #2" confirming 8.5, when in fact B4X's `span_source: "grid_table"` = derived back from the grid master itself (circular reference), not an independent measurement from the drawing
- Also updated `dummy_grid_rule_check_2026-07-08.prime_ordering_rule_3.1.1` to note that `3'`/`3''` sit in the same slot (like A'/A'') ordered by distance from grid 3
- Updated the `non_grid_dimensions_do_not_confuse[]` entry that used to say "0.60 is not 3'" → now the same point

---

### 2. Beam plan — beam B4X

**File:** `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า19_view2_beam_plan.json`
**Field:** `elements[]` — 2 entries with `element_id: "B4X"`

**Before (first entry):**
```json
{ "element_id": "B4X", "grid_ref_start": "C-3", "grid_ref_end": "C-3'", "span_length_m": 1.5, "span_source": "grid_table", "confidence_flags": ["uses_dummy_grid_3'_from_page00_master"] }
```
**After:**
```json
{ "element_id": "B4X", "grid_ref_start": "C-3", "grid_ref_end": "C-3''", "span_length_m": 1.5, "span_source": "grid_table", "confidence_flags": ["uses_dummy_grid_3''_from_page00_master", "CORRECTED_2026-07-13_reassigned_from_3'_to_3''..."] }
```
(the paired entry `B-3`/`B-3'` → `B-3''` gets the identical treatment)

**Reasoning:** cascading fix from item 1 — B4X actually ends at `3''` (8.5, the eave-projection end), not `3'` (7.6, the wall edge), so grid_ref_end must move and span_length_m reverts to 1.5 (the original value, before it was mistakenly changed to 0.6 when only one grid line was assumed to exist)

---

### 3. Site plan — `boundary_line_right`

**File:** `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า05.json`
**Field:** `element[]` array, entry with `element_id: "boundary_line_right"`

**Before:**
```json
{ "element_id": "boundary_line_right", "dimension_m": null, "confidence_score": 0.75 }
```
**After:**
```json
{ "element_id": "boundary_line_right", "dimension_m": 14.6, "confidence_score": 1, "confidence_flags": ["CORRECTED_2026-07-13_by_user_mirrored_from_boundary_line_left_14.60..."] }
```

**Reasoning:** the lot is rectangular; the left side (`boundary_line_left`) has "14.60" printed clearly on the drawing, the right side has no repeated printed number but the depth must be equal (mirrored) — Makham directly instructed filling in this value.

---

### 4. Convention: `confidence_score` for values Makham directly confirms

**Rule:** any time Makham directly instructs/confirms a value (not Claude guessing/estimating from the image) → always set `confidence_score: 1`, plus add `confidence_flags` explaining "corrected/confirmed by user" + date + brief reason.

**Applies to:** items 1, 2, 3 above (and every file fixed from here on).

---

### 5. `grid_ref` — shorten free text to be concise

**File:** `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า06_floor_plan.json`
**Field:** `element[]` array, every entry with a string `grid_ref` field

**Old format (long, full words):**
```
"grid1 - dummy1' x gridD - gridC"
"dummy1' - grid2 x gridD - gridC"
"near grid1 x gridC - gridB, see detail A-09/A-10"
"grid1'/2 - grid3 x gridC - gridB (approx)"
"beyond grid3 to dummy3' x gridD - gridC (open utility extension)"
"grid1 - approx mid x gridB - gridA (carport, open on one side)"
"approx mid - grid3 x gridB - gridA"
"grid2-3 area, below gridA (into dummy A' zone)"
"near grid1, between จอดรถ and เฉลียง — see detail A-11"
"near grid3/C row, between เตรียมอาหาร and ซักล้าง — see detail A-11"
```

**New format (short, drops "grid"/"dummy", uses `-` for ranges, `x` between axes, `~` for "approx/near", vertical axis (y_lines, letter rows) always comes first):**
```
"D-C x 1-2"
"D-C x 1'-2"
"C-B x ~1, see A-09/A-10"
"C-B x 1'/2-3 (approx)"
"D-C x 3-3' (open utility extension)"
"B-A x 1-~mid (carport, open one side)"
"B-A x ~mid-3"
"below A x 2-3 (into A' zone)"
"~1, between จอดรถ/เฉลียง — see A-11"
"C x ~3, between เตรียมอาหาร/ซักล้าง — see A-11"
```

**General rule for other not-yet-fixed files (use this format going forward for every file in this house, and other houses if the same pattern appears) — also written into `primary_rawjson_schema.md` section 4:**
- Always strip `grid`/`dummy` from grid letters/numbers (`grid1` → `1`, `dummy1'` → `1'`, `gridD` → `D`)
- Use `x` between the two axis ranges — **the vertical axis (y_lines, letter rows A/B/C/D) always comes first, followed by the horizontal axis (x_lines, numbered columns)**, e.g. `"D-C x 1-2"` not `"1-2 x D-C"` — matches the existing convention for `"A-1"` (row before column)
- Use `~` in place of a leading "near"/"approx"
- Genuinely necessary extra description (e.g. "see detail A-09/A-10", "(open utility extension)") can stay appended in parentheses or after a comma — don't strip it, only strip the redundant grid-notation wording itself
- **Fixed 2026-07-13**: the first shortening pass unintentionally wrote the horizontal axis (x) first (e.g. `"1-1' x D-C"`); Makham had it corrected back to vertical-axis-first per above — if a newly-imported file has a long-form `grid_ref`, shorten it with vertical-axis-first from the very first pass, don't wait for a second correction round.

---

### 6. Grid master — added dummy grid `D'` (y-axis) + cascading fix across every related page

**Main file:** `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า00_gridline.json`
**Field:** `grid.y_lines[]` — new entry added

**Added:**
```json
{ "id": "D'", "pos_m": 2.8, "type": "dummy", "confidence_score": 1, "confidence_flags": ["added_2026-07-13_by_user_confidence_set_to_1_per_user_verification", "NOT_YET_confirmed_by_beam_plan_หน้า19_which_only_has_beam_rows_at_D_C_B_A_no_beam_at_this_interior_line"] }
```

**Reasoning/evidence:** on page06 (A-04, floor 1 plan) the main D-C span (4.00m) subdivides into 2.80+1.20, matching a full-width partition wall between the bedroom row (นอน1/นอน2) and the room row behind it — Makham instructed this addition directly.

**⚠️ Caveat (not yet confirmed by the beam plan):** checked the beam plan (หน้า19_view2_beam_plan.json) and no beam row sits at this position at all (only rows D/C/B/A exist) — unlike `1'`, which is confirmed by beam B1. It's possible `D'` is a light partition wall, not a real structural beam line — flagged in the file, not deleted, since Makham confirmed adding it.

**Cascading fix — files that needed updating once `D'` was added:**

1. **`หน้า06_floor_plan.json`** — นอน1/นอน2's original `grid_ref` was `"D-C x ..."` (the full D-C span); had to become `"D-D' x ..."` since the room is really only 2.80m tall, not the full 4.00m:
   - นอน1: `"D-C x 1-2"` → `"D-D' x 1-2"`
   - นอน2: `"D-C x 2-3"` → `"D-D' x 2-3"`
   - **ห้องน้ำ_WC1: was about to be changed to `"D'-C x ~1"`** (assuming the bathroom sits in the remaining D'-C span), **but checking `หน้า11_wc1_plan.json`** (the dedicated WC1 detail sheet) **showed it stated directly that the room is "between gridB and gridC"** — i.e. `C-B`, the same as before, not `D'-C` as guessed — reverted to `"C-B x ~1"` as before (only the axis-order format was fixed) — **lesson: before cascading a grid_ref change from a new dummy grid, always check any related expanded/detail sheet first**, if one exists, since it may confirm or contradict the guess.
2. **`หน้า06_roof_plan.json`** — no field directly references D', but fixed the `grid_ref` format on every entry to match convention (stripped `grid`/`gridA`, vertical axis first) at the same time, e.g. `"covers grid1-3 x gridA-D..."` → `"D-A x 1-3..."`
3. **`หน้า11_wc1_plan.json`** — no value change needed (already correct), just fixed format: `"between gridB and gridC"` → `"C-B"`, also stripped the `grid` prefix from other fields in the same file.
4. **`หน้า19_view2_beam_plan.json`, `หน้า20_view1_tie_beam_plan.json`** — checked, **no change needed** since real beams span the full D-C range (no beam stops at D'), consistent with D' being a partition wall, not a beam line.

**General rule (added to convention):** every time a new/changed dummy grid is added to the grid master, must **check every file in the same house whose `grid_ref` spans that range** to see if it needs updating too — if a detail/expanded sheet for the same spot exists (e.g. WC1 has both หน้า06 and หน้า11), treat that detail file as the deciding evidence before guessing.

---

### 7. Grid master — added dummy grid `1''` (x-axis)

**File:** `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า00_gridline.json`
**Field:** `grid.x_lines[]` — new entry added in the same slot as `1'` (between grid 1 and grid 2)

**Added:**
```json
{ "id": "1''", "pos_m": 3.45, "type": "dummy", "confidence_score": 1, "confidence_flags": ["added_2026-07-13_by_user_confidence_set_to_1_per_user_verification", "exact_source_dimension_line_not_independently_pinpointed_by_Claude_beyond_general_0.55_feature_near_this_x_position"] }
```

**Reasoning:** Makham instructed this addition directly — likely from a wall/edge between the WC1 zone and the food-prep zone on page06 (near the printed "0.55" dimension), but Claude has not independently pinpointed the exact dimension line with 100% certainty — added following prime-ordering (1' at 2.55 closer, 1'' at 3.45 farther, in the same slot).

**⚠️ Not yet cascaded to other pages** — not yet checked whether the food-prep room / ห้องน้ำ_WC1 (หน้า06/หน้า11) should reference `1''` in their grid_ref (food-prep is currently still `"1'/2-3"` as an approximation) — waiting for a further instruction or clearer evidence before proceeding, per the lesson in item 6 (check the detail sheet before guessing).

---

### 8. New convention: point-type `grid_ref` uses no dash — dash reserved for line/area ranges only

**Rule:** a grid reference that is a **single point** (a footing/column position, or one end of a beam) should be written with no dash between the letter row and the number column, e.g. `"C2"` not `"C-2"` — the dash is reserved specifically for referencing a **line/area**, a range between 2 positions on the actual same axis, e.g. `"D-C"` (the range from D to C) or `"1-2"` (the range from grid 1 to grid 2).

**Makham's reasoning:** "it's just a point, not a line — only a line or area needs a dash."

**Files already fixed:**
1. **`หน้า19_view1_footing_plan.json`** — `grid_refs[]` for F1/F2/C1 (footing/column) changed from `["D-1","D-2",...]` → `["D1","D2",...]`
2. **`หน้า19_view2_beam_plan.json`** — `grid_ref_start`/`grid_ref_end` for every beam (B2/B4/B5/B3/B4X/B1/B3X/B5X) changed from `"D-1"` → `"D1"` throughout (each one is a single beam-endpoint point, not a range itself — the beam's own span/line is already expressed by having 2 separate start/end fields, so the point itself doesn't need its own dash). Values with a trailing prime (`C-1'` → `C1'`, `C-3''` → `C3''`) keep the prime as-is, only the row-column dash is stripped.

**⚠️ Not yet applied project-wide** — already asked Makham whether this should become a new convention for the whole project (every file, every house) or just house 1, currently being reviewed; still awaiting a clear answer, and `primary_rawjson_schema.md` section 4 (which currently says the opposite, that point-type uses `"A-1"` with a dash) has not been updated yet — must wait for confirmation first, per rule_of_tune.md rules 7/9.

**⚠️ Other files in house 1 not yet updated to this convention** (still have dashed point-type grid_ref): `หน้า20_view1_tie_beam_plan.json` (has the same grid_ref_start/end style as beam_plan) and other files that may have point-type `grid_refs`/`grid_ref` — not yet checked across the whole house.

---

### 9. `หน้า19_view2_beam_plan.json` — re-ordered elements[] + split duplicated specs into `specs{}`

**File:** `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า19_view2_beam_plan.json`

**(a) Re-ordering** — previously grouped by `element_id`/mark (all of B2 first, then all of B4, ...), changed to ordering by actual drawing position: top to bottom (D→C→B→A), left to right within each row, horizontal before vertical when the start point coincides — added as a general rule in `primary_rawjson_schema.md` section 4 ("Element ordering within elements[]"); Makham confirmed yes to adding it.

**(b) Split duplicated specs into a new `specs{}` object (keyed by element_id)** — previously, every occurrence of the same beam mark (e.g. B4's 3 segments) repeated the identical `width_mm/height_mm/main_bar/stirrup/additional_bars/concrete_grade/steel_grade/spec_source/spec_confidence_score` block verbatim (a god-object-per-row-per-mark) — moved this out into `specs.B2`, `specs.B4`, ... just once per mark, leaving `elements[]` with only position (`grid_ref_start/end`, `span_length_m`, `span_source`) + that entry's own confidence — verified beforehand that every occurrence of each mark really has an identical spec (B2 x2, B4 x3, B5 x3, B3 x3, B4X x2, confirmed). **Have not yet asked Makham whether to add this pattern to `primary_rawjson_schema.md` too (left pending at the end of a message before Makham said they were tired — needs asking again next round).**

**⚠️ Important — pending work for syncing back to raw_json:** items 6 (D'/1'' dummy grid), 8 (point-ref with no dash), and 9 (re-ordering + splitting specs) in this file were all fixed **only** in `json_แก้ไขแล้ว` — `rawjson_ยังไม่ได้แก้ไขโดนคน/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า19_view2_beam_plan.json` (the real original) **has not been touched at all** — still has the old dashes (`"D-1"`), still ordered by element_id, still has the duplicated spec everywhere. When syncing back over the original, must follow rule_of_tune.md rules 1-3 in full (warn first + log in `raw_json_data_log.md`) — that step has not been done yet.

---

---

### 10. `หน้า19_view2_beam_plan.json` — fixed a wrongly-labeled additional-rebar position (top of beam → bottom of beam), merged into a single bottom-rebar count

**File:** `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า19_view2_beam_plan.json` (`specs{}`)

Makham sent a photo of beam B2's section (1:20, from page21/S-04) pointing out that the `additional_bars` previously labeled `position: "บนคาน หยุดที่ L/8"` (top) are actually rebar at the **bottom of the beam** (visible from the leader line in the photo pointing at the bottom corner of the cross-section, alongside the other 2 main bottom bars) — instructed to merge into a single `main_bar.bottom.count`:

| Mark | Before (bottom + additional) | Fixed to |
|---|---|---|
| B2 | 2 + 1 | **3** |
| B4 | 2 + 1 | **3** |
| B5 | 3 + 2 | **5** |
| B3 | 2 + 1 | **3** |

Kept the L/8-stop detail as a `main_bar.bottom.note` instead of a separate `additional_bars` entry (e.g. "2 full-length bars + 1 bar stopped at L/8") — not discarded, since it matters for real rebar-quantity calculation (not every bar is the same length).

**Update (later): B3X/B5X needed the same merge too, just into the top side instead of the bottom** — see items 11-12 below (initially misunderstood that lap-splice bars must always stay separate).

---

### 11. `หน้า19_view2_beam_plan.json` — B3X: lap-splice bar merges into the top rebar (not bottom)

Makham sent a photo of beam B3X's section (1:20) pointing out that the `additional_bars` ("2-Ø16mm lapping into B3, 1.5m") are actually on the **top** side (label order in the photo: top-continuous / lapping-in / stirrup / bottom-continuous) — instructed "do the same as before but for the top rebar."

- `main_bar.top.count`: 2 → **4** (2 continuous from B3 + 2 lapping into B3 1.5m)
- `main_bar.bottom.count`: 2 (unchanged)
- `additional_bars`: []

**Important lesson**: the earlier conclusion in item 10 that "lap-splice bars into another beam should always stay separate" was **wrong** — a lap-splice bar still needs its own mark's label order checked to determine top vs bottom, not a fixed rule that it must always stay separate. Updated `primary_rawjson_schema.md` accordingly (removed the old exception).

### 12. `หน้า19_view2_beam_plan.json` — B5X: identical to B3X in every respect (Claude found the image itself from page21)

Makham said B5X's structure resembles B3X but no photo was provided yet — **Claude went and read page21.png (S-04) directly** rather than waiting for another cropped image (this page has every beam mark combined on one sheet: B1/B2/B3/B3X/B4/B4X/B5/B5X/B6), and found B5X's label order matches B3X exactly (top-continuous '2-Ø16(continuous from B5)' / lapping-in '1-Ø16 lapping into B5, 1.00m' / stirrup / bottom-continuous '3-Ø16(continuous from B5)')

- `main_bar.top.count`: 2 → **3** (2 continuous + 1 lapping-in)
- `main_bar.bottom.count`: 3 (unchanged)
- The result becomes **symmetric** (top 3 / bottom 3) — removed the `ASYMMETRIC_main_bar_top2_bottom3` flag since it's no longer true after the fix.
- Took the opportunity to re-check B2/B4/B3/B5 (the earlier bottom-merges) against this same full page21 image too — **all confirmed correct**, no further changes needed.

**This house's `หน้า19_view2_beam_plan.json` now has all 8 marks complete** — no additional_bars remain pending review.

---

**Have not yet asked Makham whether to check other files/other houses for the same wrong pattern** (e.g. additional_bars with the wrong labeled position, or lap-splice bars not yet merged into main_bar) — should ask before proceeding to fix other houses.

---

### 13. Added new pattern `misc` — page60/61

**Files:** `หน้า60.json` (10-house-design price comparison table), `หน้า61.json` (collage cover of every design)

**Fix:** `"pattern": "title"` → `"pattern": "misc"` on both files.

**Reasoning:** both pages are promotional/reference content for the whole 10-design series, not construction data or this specific house's own cover — `title` should be reserved for this house's own cover only. Added pattern #14 `misc` to `primary_rawjson_schema.md` (also logged in `primary_rawjson_schema_edit_log.md`).

**⚠️ Not yet checked across other houses** — houses 2-5 may have similar pages (price tables / whole-series collage covers) still classified as `title` — should be checked later for consistency.

---

### 14. Element ordering direction reversed: vertical before horizontal (not horizontal-first as originally written)

Makham pointed out that when elements[] was ordered (item 9), the rule was written in the wrong direction — the correct rule is **vertical before horizontal** when the start point coincides (e.g. at D1: `B4 D1-C1` (vertical) comes before `B2 D1-D2` (horizontal)). Re-ordered the whole `หน้า19_view2_beam_plan.json` accordingly (validated all 20 elements, nothing lost) — fixed `primary_rawjson_schema.md` section 4's direction to match (confirmed "y" before editing, per rule 9).

Along the way found and fixed: a B2 entry mistyped as `D'2/D'3`, corrected back to `D2/D3` (confirmed from the image: row D really only has 2 B2 lines).

### 15. `หน้า19_view2_beam_plan.json` — added 7 missing beams (from a clearer new full-page image) + fixed B4X's 3'/3'' back correctly

Makham sent a clearer full-page image of page19 pointing out 7 missing beams — added all of them (confidence 1 on every one, full detail in the chat transcript, not repeated here to save tokens): `B2 D'2-D'3`, `B4 B1-B2`, `B4 B2-B3`, `B2 B1''-A1''`, `B2 C3'-B3'`, `B3 A1''-A3`, `B3 A'1''-A'3`

Along the way, found that both B4X segments had been changed back to label `3'` (spans 0.6/1.5 no longer consistent with each other), even though the grid master itself already recorded that both segments should be `3''` (span 1.5 for both) — fixed back to match the grid master.

### 16. Synced the fix between page19 ↔ page20/21 (files sharing the same spec/mark)

- **`หน้า20_view1_tie_beam_plan.json`** (B2×15, B6×2): stripped point-ref dashes, re-ordered (vertical-first), split into `specs{}`, merged B2's additional_bars into bottom (2→3) same as B2 in page19
- **`หน้า20_view2_roof_frame_plan.json`**: only stripped dashes in `grid_refs` (a different pattern, no duplicated spec/additional_bars)
- **`หน้า21_view1_section.json`** (the source spec for every beam): synced all the additional_bars merges from page19 (B2/B3/B4/B5 bottom, B3X/B5X top) — **found 1 real conflict**: this file reads only 1 additional bar for B4, but page19's merge assumed 2 (bottom 2→4) — kept both values with a flag for a human to check, did not silently pick one side.
- **`หน้า21_view2_schedule.json`** (column C1 table): found a different kind of problem — column C1 used beam-style `main_bar.top/bottom` structure (top4+bottom4 = wrongly totaling 8), even though a column has no beam-style "top face/bottom face," only 4 corner bars around the column (4-Ø12mm, a single count from the image) — Makham pointed out to fix it to a single `main_bar: {count:4, ...}`, not split top/bottom — **have not yet asked whether to add this rule (columns don't use top/bottom) to primary_rawjson_schema.md project-wide** — left pending.

### 17. `หน้า22.json` — rejected an incorrect suggestion from an external AI (F2 is not DB/SD30)

Makham forwarded a suggestion from another AI (analyzing the F1/F2 image) proposing to change F2's main bar from RB/SR24 to DB/SD30 (claiming 12mm is usually DB per general standards) — **Claude checked the image itself and rejected this suggestion**, because the symbol actually printed in the image (Ø) is identical on both F1 (Ø9) and F2 (Ø12), differing only in size — matching the existing rule in `primary_rawjson_schema.md` section 6 ("Ø always = RB, never infer from the diameter number") — did not change the file, since the original data (RB/SR24) was already correct. **Lesson: an external AI can give incorrect advice — always check the original source image yourself before trusting it.**

**⚠️ Important pending items before moving to the next house:**
1. Not yet answered: the no-dash point-ref convention — project-wide, or just house 1? (If project-wide, `primary_rawjson_schema.md` section 4 needs to be reverted back for the point-type format too, since section 4 currently still says point-type uses `"A-1"` with a dash, contradicting what's actually done in house 1.)
2. Not yet answered: columns (column) don't use top/bottom — should this become a general rule in primary_rawjson_schema.md?
3. **Have not yet gone through all the remaining files in house 1** to match the new convention fully (checked/fixed only: หน้า00, 05, 06, 06_roof, 11, 19×2, 20×2, 21×2, 22, 60, 61 — dozens of files still remain in house 1 itself).
4. **The biggest task, not started at all**: syncing all the fixes in `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/` back over the original `rawjson_ยังไม่ได้แก้ไขโดนคน/01บ้าน_เล็ก_1ชั้น_01/` (following rule_of_tune.md rules 1-3 in full) — not yet agreed on when to do this sync.
5. **The next house (house #6) review has not started** — if starting it, should first create `json_แก้ไขแล้ว/06<house name>/` (copied from raw), then work through the convention confirmed in house 1 (strip point-ref dashes, vertical-axis-first, vertical-before-horizontal element ordering, `specs{}` splitting duplicate specs, always check additional_bars position against the leader line, columns don't use top/bottom).

---

## 02บ้าน_เล็ก_1ชั้น_02

**Scope this round:** two paired tasks — (1) apply the convention confirmed from house 1 (items 1-17 above) across every file in house 2, `json_แก้ไขแล้ว/02บ้าน_เล็ก_1ชั้น_02/`, (2) deep-recheck only the files where `"pattern": "plan"` (15 files) against the source images in `image/บ้าน_เล็ก_1ชั้น_02/`, per Makham's emphasis — the instruction "apply สิ่งที่ต้องแก้.md to house 2" is treated as clear confirmation that these conventions (especially the no-dash point-ref rule) apply project-wide, not just house 1.

### 18. Grid_ref format convention (items 5 and 8) — applied across house 2

Stripped `grid`/`dummy` prefixes, vertical axis (row) always before horizontal axis (column), `~` for near/approx, point-refs with dashes stripped — files fixed (checked against the source image for every pattern=plan file; other files got format-only fixes without re-opening the image):
- `หน้า06_floor_plan.json` — 12 grid_refs, checked every room's position against หน้า06.png, all correct
- `หน้า07_roof_plan.json` — 7 grid_refs (including one spot originally mistyped `D-E`→fixed to `E-D`, correcting the direction to match F>E>D>C>B>A)
- `หน้า10_section1.json`, `หน้า10_section2.json` — found `gridD-E`/`gridE-F` in the source **already written in the wrong direction** (should be E-D, F-E in top-to-bottom order) — fixed at the same time
- `หน้า12_wc1_plan.json`, `หน้า14_wc2_plan.json` — bathroom grid_refs + fixture positions checked against the 1:25 image, correct
- `หน้า16_stair_st1.json`, `หน้า17_stair_st2.json` — checked against the image, correct; ST2 has a local-only axis label "6" that isn't a real grid line (the original analysis was already correct, no content change, format only)
- `หน้า23_spread_footing_plan.json`, `หน้า24_pile_footing_plan.json` — every point-ref (`F-1`→`F1` etc.), checked all 24 footing/column points against the image, 100% correct
- `หน้า39_floor_plan.json`, `หน้า40_roof_drainage_plan.json` — sanitary-element grid_refs checked against the image, correct

**⚠️ House 2 has not been fully swept yet** — checking found more remaining in `หน้า11_door_window_schedule`, `หน้า18-22`, `หน้า30-38`, `หน้า41-45`, `หน้า49-70` (most non-pattern=plan files have not been opened for checking) — pending work for next round.

### 19. `หน้า25_beam_plan.json` — found the additional_bars wrong-side-position bug (exactly like house 1 items 10-12)

**How it was checked:** zoomed into the image behind `หน้า28_beam_slab_sections.json` (S-07 beam-detail sheet) to look at the actual dot pattern in B2/B2X's real cross-sections (not trusting the "top of beam"/"middle of beam" labels recorded in the existing data, per the additional_bars rule in `primary_rawjson_schema.md` section 7)

| Mark | Existing data | Actual image (counting dots in the section) | Fixed to |
|---|---|---|---|
| B2 | additional 2-Ø16 "top of beam" (kept separate from main_bar) | the "stopped at L/8" dots sit right next to the main bottom-rebar dots (4 dots together in the bottom half of the section; top only has 2 dots) | merged into `main_bar.bottom.count`: 2→**4** |
| B2X | additional 1-Ø16 "middle of beam (lap splice)" | the lapping-in rebar's dot sits in the top half (3 dots together at top, bottom only has 2 dots) | merged into `main_bar.top.count`: 2→**3** |

Fixed at the source (`หน้า28_beam_slab_sections.json`) then cascaded into `หน้า25_beam_plan.json`'s `specs.B2`/`specs.B2X` — RB2 (asymmetric top2/bottom3) was checked and is already correct, no change needed.

**At the same time:** did the `specs{}` extraction (item 9) for B1/B2/B2X in `หน้า25`, and re-ordered `elements[]` per the vertical-before-horizontal rule (item 14) — validated all 20 beam elements + S1/SO markers, nothing lost.

### 20. `หน้า26_tie_beam_plan.json` — found a genuine mislabel (what was recorded as RB1A is actually RB1)

Zoomed into the S-05 image (tie-beam plan) around the small room below grid C (col2-col3): clearly shows the vertical member on the col3 side (from C3 down to B3) printed with the label "RB1" at both ends of the run, not "RB1A" as previously recorded — the "RB1A" label (the heavier 16mm rebar) only really starts on the segment below grid B. Fixed element_id from RB1A→RB1 (and its spec accordingly, 150x400/12mm), which makes this small room symmetric (RB1 on both the col2 and col3 sides), matching the equivalent room in `หน้า25` (S-04).

Did the `specs{}` extraction (RB1/RB1A/RB2) + re-ordered `elements[]` (vertical-before-horizontal) for this file too — validated all 28 beam elements + SX/S1 markers.

### 21. `หน้า29_column_schedule_stair_fin.json` — columns use a single main_bar.count (the rule left unconfirmed in house 1 item 16 → now confirmed this round)

The existing data used a beam-style `main_bar:{top:{count:4},bottom:{count:4}}` structure (with a self-warning flag already noting "the real total is 4, not 8," but the structure hadn't been changed) — fixed to a single `main_bar:{count:4, dia_mm:12, type:"RB", note:"main rebar around the 4 column corners"}`, as verified against the image (a single set of 4-Ø12mm bars around the column, no top/bottom), for both C1A and C1 — **this is the answer to the question pending from house 1 item 21.2: yes, columns should use a single count project-wide.**

### 22. Added `misc` pattern (item 13) — page71/72

- `หน้า71.json` (10-house price table): `"pattern": "unknown"` → `"misc"` (fits the existing pattern exactly, not genuinely unclassifiable)
- `หน้า72.json` (whole-series collage cover): `"pattern": "title"` → `"misc"` (matches house 1's page60/61 precedent exactly)

**⚠️ Pending before the next house:**
1. The rest of house 2's files (หน้า03-05, 08-09, 11, 13, 15, 18-22, 30-38, 41-45, 49-70) have not all been fully checked/fixed for format yet.
2. Syncing back to `rawjson_ยังไม่ได้แก้ไขโดนคน/02บ้าน_เล็ก_1ชั้น_02/` has not started (must follow rule_of_tune.md rules 1-3).
3. The additional_bars bug found in house 2 (item 19) raises the question of whether house 1 has other spots that need the same zoom-into-the-dot-pattern re-check (house 1 was checked mainly by label order, not by zooming to count dots in every single cross-section directly) — should be considered before syncing.

### 23. Grid master — added 6 more dummy grid lines (1', 1'', 3', 4', F', F'') + cascaded into pattern=plan pages

**File:** `บ้าน_เล็ก_1ชั้น_02_หน้า00_gridline.json`

Makham gave these 6 values directly (explicitly instructed not to over-interpret): x-axis `1'`=-1.0 and `1''`=1.8 (sourced from page12), `3'`=7.7 and `4'`=10.6 (sourced from page06); y-axis `F'`=-0.6 and `F''`=0.9 (sourced from page06). Added as dummy entries, `confidence_score: 1`, minimal added commentary per Makham's instruction.

**Cascaded into (checked against the source image before changing each):**
- `หน้า06_floor_plan.json`:
  - ซักล้าง: `"F x 1-2 (partial, top ~1.50m strip...)"` → `"F-F'' x 1-2"` — zoomed into the F-row dimension chain and found the "0.60"+"0.90" ticks are actually two *separate* features sharing one dot at gridline F (0.60 exterior, 0.90 interior), not one continuous 1.50m overhang as the file previously concluded — F'' (0.9m south of F) is the real ซักล้าง/ห้องครัว partition, replacing the old ~1.50m guess.
  - ห้องครัว: `"F-E x 1-2"` → `"F''-E x 1-2"` (same boundary, other side)
  - ห้องนอน1: `"...(sub-split 1.30+1.90=3.20)"` → `"...(internal partition at 3')"` — 6.4+1.30=7.7 matches 3' exactly
  - ห้องน้ำ1 (floor-plan-level box): `"E-D x ~1"` → `"E-D x 1'-1''"`
  - Rewrote the file's own long grid-master-relevant warning paragraph, which had previously concluded "no dummy grid line is warranted from this sheet" — that conclusion is now superseded by Makham's direct instruction.
- `หน้า12_wc1_plan.json`: main room `"E-D x ~1"` → `"E-D x 1'-1''"` — the room's own `dimension_breakdown_width_m: [1.00, 1.80]` sums to 2.80, matching 1'-to-grid1 (1.00) and grid1-to-1'' (1.80) almost exactly; this is very likely the literal source of these two dummy grids.
- `หน้า39_floor_plan.json`: grease_trap `"F-E x ~1"` → `"F''-E x ~1"` (its own description already says "adjacent to ห้องครัว", i.e. the F''-E zone specifically).

**⚠️ Found but NOT changed (flagged for human review only, not silently resolved):** while cascading into `หน้า12_wc1_plan.json`, noticed its `dimension_m` field claims "2.70 (E-D span)" but the grid master's actual E-D distance is 2.0m (E=4.0, D=6.0) — a pre-existing mismatch unrelated to this round's dummy-grid addition, not resolved here.

**⚠️ Not cascaded (insufficient evidence to pin exactly, left alone rather than guessed):**
- `หน้า06_floor_plan.json`'s บันได_ST2 (`"F x ~1, beside ซักล้าง"`) — plausibly relates to F'/1' but no clear dimension ties it to those exact points.
- `หน้า39_floor_plan.json`'s first septic_tank (`"F-E x ~1"`) — description says "between ซักล้าง and the F-row perimeter run," ambiguous between the F-F'' and F''-E zones.
- ~~`หน้า07_roof_plan.json`'s west/east eave overhangs (1.30m each side) — checked, these do not match 1'/4', so left untouched.~~ **Superseded by the addendum below — partially wrong.**

**Addendum (later 2026-07-16) — pages 05/06/07 fully re-cascaded per Makham's follow-up instruction:**
- `หน้า05_site_plan.json`: the lot-width breakdown's two 1.00m strips ([1.00, 3.20×3, 1.00]) are the same building-edge feature as dummy grids `1'`/`4'` — noted in `min_lot_width_note` as a 3rd independent confirming sheet (distinct from the 'A' lot setback beyond them).
- `หน้า06_floor_plan.json`: ห้องน้ำ2 `"E-D x ~4"` → `"E-D x ~4 (extends to 4')"` — outer edge pinned to `4'`(10.6) per A-12's own top chain "1.80 | 1.00" straddling grid4, mirroring ห้องน้ำ1's `1'` side; interior boundary (~7.8, unnamed) left approx. Everything else in หน้า06 was already cascaded in the first round.
- `หน้า07_roof_plan.json`: the earlier "not related to 1'/4'" conclusion was **half wrong** — the metal-roof *eaves* (1.30m) are indeed a different figure, but the two +3.75 concrete side canopies (E-D rows) ARE the roofs over the bathroom extensions and pin exactly to `1'`/`4'`: changed `"E-D x beyond 1"`→`"E-D x 1-1'"` and `"E-D x beyond 4"`→`"E-D x 4-4'"`. Geometry reconciles as 1.30 eave = 1.00 (to 1'/4') + 0.30 roof projection past the slab edge. Also removed the north-overhang's stale "(sub-split 0.60+0.90)" note — that lumping was the same error already corrected in หน้า06 (0.60=exterior F', 0.90=interior F'', two separate features, not sub-parts of the 1.50 roof overhang).

**Addendum 2 (2026-07-19) — dummy grids cascaded into the structural beam plan `หน้า25_beam_plan.json`, with 2-way structural/architecture cross-confirmation:**

Makham asked to bring the 2026-07-16 dummy grids (`1'`/`1''`/`3'`/`4'`/`F'`/`F''`) into `หน้า25_beam_plan.json` (S-04, the ground-floor beam plan the grid master itself derives from). Zoomed into the E-D band and the F-row area of หน้า25.png at 3x and found real structural evidence pinning most of them:

- **B2X re-identified**: previously recorded as 4 vertical E1-D1/E4-D4 beams — actually the B2X label sits mid-segment on the E-row and D-row *horizontal* beams' 1.00m extension zones beyond grid1 (to `1'`) and beyond grid4 (to `4'`), matching B2X's own rebar note "1 เส้นล้วงเข้า B2 1.00 ม." (the lap length = the extension length exactly). Corrected to 4 horizontal B2X segments: `E1'-E1`, `E4-E4'`, `D1'-D1`, `D4-D4'`.
- **E-row and D-row horizontal beams were missing from `elements[]` entirely** (their B2 labels had been misattributed to vertical members) — added `E1-E2`, `E2-E3`, `E3-E4` and `D1-D2`, `D2-D3`, `D3-D4`.
- **New vertical B1 divider/edge beams added** at dummy grids `1'`, `1''`, `3'` (0.1m tolerance caveat vs A-12's ~7.8m, left unresolved same as หน้า06/หน้า12), and `4'`, plus a `F''` edge beam under the ซักล้าง slab strip — all zoom-verified.
- **Vertical members at grids 2 and 3 in the E-D band corrected from B2 to B1** (rotated labels misread on the first pass).
- **`F'` NOT structurally confirmed**: the only candidate is the ST2 stair-landing edge (~0.5-0.6m beyond F), but it's undimensioned on this sheet and its adjacent B1 label belongs to the ordinary F1-F2 beam — flagged, not resolved.

Updated `หน้า00_gridline.json`'s warnings with a new note: 5 of the 6 dummy grids (`1'`/`1''`/`4'`/`F''`, plus `3'` within tolerance) are now confirmed by both an architecture sheet AND a structural sheet independently; `F'` remains architecture-only.

**Addendum 3 (2026-07-19) — missing beam `B1 B2-B3` found by Makham, added:**

Makham spotted (via a screenshot of หน้า25.png) that beam `B1` from grid `B2` to `B3` (span 3.2m, the south edge of the small room below grid C, sitting directly on the row-B gridline) was missing from `elements[]` entirely — it had been omitted rather than misattributed. Added with `confidence_score: 1`.

**Addendum 4 (2026-07-19) — added dummy grid `B'` (y-axis)**

**File:** `บ้าน_เล็ก_1ชั้น_02_หน้า00_gridline.json`, `grid.y_lines[]`

Makham gave the value directly as a formula: `pos_m = 11.4 + 3.15 - 2.5 = 12.05` (between named grids B[11.4] and A[14.55], splitting the 3.15m B-A span into 0.65 + 2.5). Added as a dummy entry, `confidence_score: 1`, no added interpretation per the same "don't over-decide for me" posture as the item-23 batch.

**⚠️ Not yet cascaded:** `B'` at 12.05 sits inside the exact zone `หน้า25_beam_plan.json` already flags as low-confidence/uncertain — beam `B3-A3` (span 3.15, `needs_human_review_zoom_required`, "carport_or_porch_slab_extension_south_of_grid_B... two_SO_slab_edge_markers... beyond_grid_A") — likely relevant to resolving that flag, but not cascaded into that file yet since Makham only instructed adding the grid line itself this round; needs a follow-up pass.

**Addendum 5 (2026-07-19) — cascaded the E-D band + B' dummy grids into `หน้า26_tie_beam_plan.json` (S-05); resolved the old ambiguous RB2 guess**

Makham gave 5 tie-beam additions directly: `RB1 at E1'-E1`, `RB1 at E1'-D1'`, `RB1 at D1'-D1 and E4-E4' and E4'-D4' and D4-D4'`, `RB2 at C4-B4 and B4-A4`, `RB1A at B'3-B'4 and A3-A4`.

- Added 6 new `RB1` tie-beams mirroring the E-D band extension zone already confirmed on `หน้า25_beam_plan.json` (1'/4' dummy grids): `E1'-D1'`, `E1'-E1`, `D1'-D1`, `E4-E4'`, `E4'-D4'`, `D4-D4'` — all `confidence_score: 1`.
- **Corrected (not just added) RB2**: the file previously had a single low-confidence placeholder (`grid_ref B3-A3, span 2.5, confidence 0.4`, flagged "not clearly on the named grid line"). Makham's instruction gives its real position — the column4 edge of the extension room south of grid B (mirroring the column3 RB1/RB1A run) — replaced with two segments: `RB2 C4-B4` (span 1.8) and `RB2 B4-A4` (span 3.15), both `confidence_score: 1`.
- **Added new `RB1A B'3-B'4`** (span 3.2, the row-B' horizontal partition from the just-added dummy grid `B'`) — noted that `B'(12.05) = A(14.55) - 2.50`, which is exactly the old "2.50m" local dimension that used to be attached to the wrong (now-corrected) RB2 guess — likely the real source of that number.
- **Updated existing `RB1A A3-A4`**: span was previously `null`/`"unresolved"` ("exact far end grid position unclear") — confirmed to the full named row-A 3-4 bay, 3.2m, `confidence_score: 1`.
- **Left untouched**: `RB1A B3-A3` (column3 side, span 3.15, confidence 0.6, still `needs_human_review_zoom_required`) — not explicitly re-instructed this round despite the new B'/RB2 context possibly being relevant; flagged rather than assumed.
- Bumped file `confidence_score` 0.82→0.9; removed the now-stale "RB2 exact grid position is uncertain" warning (superseded by the correction above); added a consolidated CORRECTED_2026-07-19 warning summarizing all 4 changes.

## 03บ้าน_เล็ก_2ชั้น_01

**Scope this round:** first 2-story house reviewed — Makham asked to apply สิ่งที่ต้องแก้.md conventions to `json_แก้ไขแล้ว/03บ้าน_เล็ก_2ชั้น_01/` and `04บ้าน_เล็ก_2ชั้น_02/`. Confirmed (per AskUserQuestion) to go full depth on both houses rather than a lighter mechanical-only pass.

### 24. Grid master — this house has *genuine* structural dummy grids (1'/3'), confirmed against the architecture floor plan

Unlike house #2 (whose roof overhangs had no dummy grids since nothing was structurally built into them), this house's grid master already recorded `1'`(-1.1) and `3'`(7.0) as provisional, sourced from S-04/S-05 showing a real beam (B2) + slab (S0) built into a small bay-window extension at row E. Cross-checked against the architecture ground-floor plan (`หน้า06_floor_plan_ground.json`), whose own dimension chain independently reads the same 1.10m/0.50m offsets — bumped confidence 0.7→0.9 on both dummy grid entries and the file overall (0.88→0.92), marked `CONFIRMED_2026-07-16`.

**Addendum (2026-07-19):** added dummy grid `1''`=2.25 (x-axis, between named grid1[0.0] and grid2[3.5]) — value given directly by Makham, `confidence_score: 1`, no interpretation added. Also added `A'`=12.75 and `A''`=13.7 (y-axis, both beyond named grid A[10.7]) same way. None of these 3 cascaded into other house-03 files yet.

### 25. Beam spec sheet (`หน้า35_column_beam_sections.json`, S-08) — found the same additional_bars position bug twice (B3X, B4)

This file's own author had already flagged suspicion on B4 ("unusual for bottom bars to curtail at L/8"). Zoomed into the actual cross-section dot patterns to resolve both:
- **B4**: "หยุดที่ L/8" bars' leader points into the same 4-dot bottom cluster as the plain bottom-bar leader (top has only 2 dots) → merged into `main_bar.bottom.count`: 2→**4**.
- **B3X**: lap-splice bar's leader points into the same 3-dot top cluster as the "ต่อเนื่องจาก B3" top-bar leader (bottom has only 2/3 dots per the "3Ø16" label) → merged into `main_bar.top.count`: 2→**3**.

Cascaded both fixes into `specs{}` in `หน้า31_beam_plan_floor1.json` and `หน้า32_beam_plan_floor2.json` (both floors share this same S-08 spec sheet) — did full `specs{}` extraction + vertical-before-horizontal reordering for both beam-plan files and the tie-beam plan (`หน้า33_tie_beam_plan.json`) at the same time, matching house #2's established pattern.

### 26. Point-ref dash stripping — mechanical, but caught a real footgun

Used `perl -pi` regex substitution (`"([A-Z])-([0-9]+'*)"` → `"$1$2"`) to strip dashes from point-refs across structural files quickly — **this also matched `"sheet_code": "S-04"`-style values** (e.g. `S-04`→`S04`), since a single-letter-dash-digit sheet code fits the same pattern as a grid point. Caught and reverted on every file this was run against (`หน้า31`, `หน้า29`, `หน้า30`) by checking `grep -n sheet_code` immediately after each substitution. **Lesson for next house: check sheet_code after any bulk dash-stripping regex, every time.**

### 27. Misc pattern — 3 whole-series pages found and fixed

- `หน้า87.json` (project credits/committee roster): `unknown`→`misc`
- `หน้า88.json` (10-house price table): `unknown`→`misc`
- `หน้า89.json` (whole-series cover banner): `title`→`misc`

### 28. Cross-house data bug found and fixed: house #2's price table had 2 wrong digits for house #10's row

While reading `หน้า88.json`'s price table, house #10's pile-foundation price (2,945,000) didn't match house #2's independent transcription of the *same* printed table (`02บ้าน_เล็ก_1ชั้น_02/บ้าน_เล็ก_1ชั้น_02_หน้า71.json`, which had 2,145,000) — an 800,000 baht gap. Zoomed into this house's own page88 image and read the Thai numerals unambiguously: `๒,๙๔๕,๐๐๐` = 2,945,000, confirming this file's read was right. Also caught a second, smaller mismatch on the same row's spread-footing price (2,035,000 vs the correct 2,034,000). **Fixed both figures in house #2's `หน้า71.json`**, with a flag explaining the cross-house discovery. This is the first cross-house consistency check performed in this project — worth doing again whenever two houses share an identical whole-series table.

**Files touched:** `หน้า00_gridline.json`, `หน้า06_floor_plan_ground.json`, `หน้า07_floor_plan_upper.json`, `หน้า18_wc_plan.json`, `หน้า20_stair_st1.json`, `หน้า21_stair_st2_plans.json`, `หน้า29_spread_footing_plan.json`, `หน้า30_pile_footing_plan.json`, `หน้า31_beam_plan_floor1.json`, `หน้า32_beam_plan_floor2.json`, `หน้า33_tie_beam_plan.json`, `หน้า34_roof_frame_plan.json`, `หน้า35_column_beam_sections.json`, `หน้า50_ground_floor_plan.json`, `หน้า51_upper_floor_plan.json`, `หน้า87.json`, `หน้า88.json`, `หน้า89.json` — plus `02บ้าน_เล็ก_1ชั้น_02/บ้าน_เล็ก_1ชั้น_02_หน้า71.json` (cross-house fix).

**⚠️ Not yet done for house 03:**
1. Lighter-touch spot-check only (no exhaustive zoom) on the electrical/AC pattern=plan pages (58-63) and WC2/sibling detail (52_wc2) — these had no grid_ref fields to fix and looked internally consistent, but weren't given the same beam-cross-section-level scrutiny as the structural files.
2. Files not opened at all this round: หน้а03-05, 08-17, 19, 22-28, 36-49 (door/window schedule, elevations, sections, guardrail/gate/fence/eave details, pile/spread footing construction details, precast plank layouts, MEP schedules/notes/index pages) — mostly detail/schedule sheets unlikely to carry grid_ref data, but not individually confirmed.
3. Sync back to `rawjson_ยังไม่ได้แก้ไขโดนคน/03บ้าน_เล็ก_2ชั้น_01/` not started.

## 04บ้าน_เล็ก_2ชั้น_02

**Scope this round:** second 2-story house, same session as house #3 (per Makham's "full depth on both" answer to the pacing check).

### 29. Grid master — dummy grid E' cross-confirmed on a 3rd independent structural sheet

E' (-1.9m, a short C0-marked column/footing row before named grid E) was already confirmed on S-02+S-03. The column schedule (`หน้า38_column_schedule.json`, S-09) independently confirms C0 as a real short column (200x200, terminates at floor 1, doesn't rise to floor 2/roof) — a 3rd agreeing structural sheet. Bumped confidence 0.8→0.85. Separately, the architecture floor plan (`หน้า06`) does NOT confirm this specific 1.9m offset — its own ~2.00m zone above grid E is a different feature at a different offset (flagged, not conflated). **This house has a genuinely unresolved 3-way discrepancy across its own structural sheets** for this same area: S-02/S-03 read 1.90m, S-04 (floor-1 beam plan) reads 2.00m, S-05 (floor-2 beam plan) reads 1.10m for a smaller, differently-shaped bay window — all three sheets' own authors flagged this themselves rather than silently reconciling; left as-is, real ambiguity, not guessed away.

### 30. Beam spec sheet (`หน้า37_beam_slab_sections.json`, S-08) — same additional_bars bug found on 4 marks (B4, B4A, B4X, B5)

By this point the pattern was recognizable from its own name in the printed note ("(หยุดที่ L/8)"), and this file's own author had already flagged it as "looking like a genuine, deliberate design detail... no longer treating as OCR error" without actually resolving it. Zoomed into each cross-section:
- **B4**: bottom 2→**4** (L/8 bars co-located with plain bottom bars, 4 dots total at bottom vs 2 at top).
- **B4A / B4X** (700mm deep beams): bottom 2→**4** each, same merge — but these ALSO have genuine 2-Ø9mm mid-depth skin/waist bars (a real deep-beam detailing practice, visually a distinct dot row between top and bottom clusters) which correctly stay as separate `additional_bars` — the fix only touches the bottom-face L/8 bars, not the skin bars.
- **B5**: found a *compounding* error — bottom was recorded as just 2 (only the L/8-curtailed count), **entirely dropping the separate "3-Ø16มม." bottom-main label** visible in the image. Corrected to bottom=**5** (3 main + 2 curtailed).
- **B5X**: zoomed to check to be safe — confirmed genuinely top:2/bottom:2 both labeled "ต่อเนื่องจาก B5," no error (not every bar in B5 continues into B5X, which is plausible and doesn't need forcing to match B5's counts).

Cascaded all fixes into `หน้า33_beam_plan_floor1.json` and `หน้า34_beam_plan_floor2.json` (floor2's inline B4 entries were still carrying the pre-fix bottom:2 — this house doesn't use a shared spec_source-only pattern as cleanly as house 3, so each floor's beam plan needed its own inline correction, not just the master spec sheet). Did full `specs{}` extraction + vertical-before-horizontal reordering for both beam plans and the tie-beam plan (`หน้า35`) at the same time.

**Column schedule (`หน้า38_column_schedule.json`) was already correct** — uses single `main_bar.count` (not top/bottom) for all 4 column marks (C0/C1A/C1/C2), no fix needed. Good sign the convention from house 1/2's correction is now baked into how these get extracted.

### 31. Point-ref dash-stripping — repeated the sheet_code footgun from house #3, same fix

Same `perl -pi` regex trick, same `"sheet_code": "S-02"` → `"S02"` corruption on every file it touched (`หน้า29` -- wait, `หน้า31`, `หน้า32`). Caught and reverted immediately each time via `grep -n sheet_code` right after. This house also has an `E'` prefix (e.g. `E'-1`) that needed the regex extended to `"([A-Z]'?)-([0-9]+'*)"` to catch primed row letters too.

### 32. Misc pattern — 3 whole-series pages, same as house #3's pattern

- `หน้า92_project_credits.json`: `unknown`→`misc`
- `หน้า93_price_reference_table.json`: `unknown`→`misc`
- `หน้า94_series_cover.json`: `title`→`misc`

### 33. Cross-house price-table check, round 2 — found 2 more digit errors, all now resolved with 2-3 way agreement

Re-ran the same whole-series-table cross-check from house #3 (item 28) using this house's own `หน้า93`:
- **House #7's pile-foundation price**: this house's own extraction read 1,572,000 — but houses #2 and #3 both independently read 1,528,000. Zoomed into this house's own page93 image: clearly reads `๑,๕๒๘,๐๐๐` = 1,528,000. **Fixed `หน้า93` itself** (the error was on this house's side this time, not the others').
- **House #10's spread-footing price**: a lingering loose end from house #3's round — house #3's zoomed re-check had read 2,034,000 against a 2,035,000 majority (house #2 original + this house's independent read), so I hadn't fully trusted the single zoomed read. Zoomed into *this* house's own page93 image as a tie-breaker: clearly reads `๒,๐๓๔,๐๐๐` = 2,034,000, matching house #3's zoomed read 2-to-1. **Fixed house #2's `หน้า71.json`** (already had the 2,945,000 pile fix from before; added the 2,034,000 spread-footing fix now) and confirmed `หน้า93` itself already reads correctly.

**This is now a validated technique worth repeating for houses 5-10 when they're reviewed**: whenever a house's own price-reference-table page disagrees with an earlier house's transcription of the *same* printed table, zoom into whichever house's own image is being actively worked on and treat that as the tie-breaker — cheap to do since the image is already open, and it's caught 3 real digit errors across 2 houses so far.

**Files touched:** `หน้า00_gridline.json`, `หน้า06_floor_plan_ground.json`, `หน้า07_floor_plan_upper.json`, `หน้า18_view1_wc1_plan.json`, `หน้า21/22/23×2/25` (grid_ref arrays), `หน้า31_spread_footing_plan.json`, `หน้า32_pile_footing_plan.json`, `หน้า33_beam_plan_floor1.json`, `หน้า34_beam_plan_floor2.json`, `หน้า35_tie_beam_plan.json`, `หน้า37_beam_slab_sections.json`, `หน้า92/93/94` — plus `02บ้าน_เล็ก_1ชั้น_02/บ้าน_เล็ก_1ชั้น_02_หน้า71.json` (2nd cross-house fix).

**⚠️ Not yet done for house 04:**
1. Remaining pattern=plan files not individually image-verified this round: `หน้า18` view2 (wc1 section), `หน้า19/20` (wc2/wc3 plans), `หน้า21/22/23×2/25` (stair/balcony plans — only did the mechanical grid-array format fix, not a full image recheck), `หน้า53-55` (sanitary floor plans + WC insets), `หน้า61-66` (lighting/outlet/AC-ventilation floor1+floor2) — these had no grid_ref format issues on a grep pass and looked internally well-hedged on a skim, but weren't given the same beam-cross-section-level scrutiny as the structural files.
2. Files not opened at all: หน้า03-05, 08-17, 24, 26-30, 36 (roof frame — checked, clean), 39-52, 56-60, 67-91 (elevations, sections, door/window/fixture schedules, gate/fence/eave details, footing construction details, precast plank layouts, MEP schedules/notes/index, BOQ sheets 67-91) — not individually confirmed.
3. Sync back to `rawjson_ยังไม่ได้แก้ไขโดนคน/04บ้าน_เล็ก_2ชั้น_02/` not started (same as houses 1-3).
4. Houses 6-10 in this series have not been started (house 5 first pass done 2026-07-19, see items 34-35 below).

When in doubt, ask Makham.


**Addendum (2026-07-19, houses 03/04 follow-up pass — cascade of the 2026-07-19 dummy grids + pattern=plan re-check):**

House 03 — cascaded `1''`(2.25)/`A'`(12.75)/`A''`(13.7) into the plan sheets:
- **Source/evidence found for all 3**: `A'` = the "2.05 below gridA" printed on หน้า05/06/07 (ทางเดิน/entry-zone outer boundary, zoom-verified on 06+07); `A''` = หน้า05 site plan's left chain "2.05 | 0.95" → A'+0.95 = 13.7 (the ONLY sheet printing the 0.95 segment); `1''` = the โถง stair/hall partition — หน้า06 prints "1.15" west from grid2 → wall east FACE at 2.35 = 1''(2.25) + one wall thickness (0.10m gap flagged, not resolved), and หน้า21's ST2 flight sub-splits (1.10 approach + 2.25 flight + 1.05 landing) land exactly on 1'(-1.1)→grid1→1''(2.25) if the flight starts at grid1 (noted as supporting evidence).
- Files updated: หน้า00 (evidence flags per entry), หน้า05 (A'/A'' identification in min_lot_depth_note), หน้า06/07 (โถง/บันได/กันสาด grid_refs + CASCADE warnings; A'' explicitly NOT present on 06/07), หน้า08 (1.50 south overhang ≠ A', nothing cascaded), หน้า20 (ST1's 2.05 zone = A-A'), หน้า21 (ST2 sub-split correspondence), หน้า31 (south-extension S0 zone can now use A'/A''; 1'' trimmer check pending), หน้า32 (ST void east edge likely 1''), หน้า33/34 (checked, nothing at the new dummies), หน้า50/58 (stale "no y-axis dummy grids" note fixed; trailing 2.05 = A-A').

House 04 — หน้า00 warnings were stale ("not yet cross-checked against S-04/S-05/architecture" — all done 2026-07-16): refreshed to record the E' 3-way offset discrepancy (1.90 S-02/S-03 vs 2.00 S-04 vs 1.10 S-05) as the single open issue; no new dummy candidates found in the pattern=plan files.

**Addendum (2026-08-01) — added dummy grid `2'`=5.2 (x-axis)**

**File:** `บ้าน_เล็ก_2ชั้น_02_หน้า00_gridline.json`, `grid.x_lines[]` (new entry between named grid `2`[5.0] and `3`[8.5])

Value given directly by Makham (`add 2'=5.2`), `confidence_score: 1`, no interpretation added — same "record as given, don't guess" posture as the other dummy-grid additions in this house.

**⚠️ Not yet cascaded** — not checked against any pattern=plan file in this house (หน้า31/32 footing plans, หน้า33/34 beam plans, etc.) or against a source image; per the general rule in item 6, must check every file whose grid_ref spans this range (2-to-3) before treating this as settled.

## 05บ้าน_เล็ก_2ชั้น_03

**Scope this round (2026-07-19):** first pass, per Makham's instruction — หน้า00 + pattern=plan files ONLY, applying Makham's new rule: **"if a plan has a beam whose start or end is NOT on a main grid line, a dummy grid line must exist there."** Folder copied fresh from `rawjson_ยังไม่ได้แก้ไขโดนคน/` (which stays untouched).

### 34. Mechanical convention normalization (30 pattern=plan files)

This house predates the houses-1-4 conventions: point refs were written `grid2-gridF` (prefix + dash) and the dummy row was named `gridAprime`. Perl-normalized all 30 pattern=plan files: `grid2-gridF`→`F2`, `gridD-gridC`→`D-C`, `grid2-grid3`→`2-3`, `gridAprime`→`A'`, bare `gridF`→`F`. sheet_code checked after (S-04 etc. intact — the house-03 footgun regex was avoided by requiring the `grid` prefix); all 138 JSONs parse valid.

### 35. Grid master rebuilt + 2 new dummy grids found by Makham's beam-endpoint rule (F', 4')

- Structure: `x_lines`/`y_lines` nested under `grid{}` (matching houses 02-04); `Aprime`→`A'`.
- **`F'` = F+1.00 (pos 14.40) ADDED**: both S-04 and S-05 print '1.00' above row F and draw a real beam along that line spanning grids 2-4 (B3 floor-1 / B2 floor-2) with B2X 1.00m vertical extension segments at grids 2/4 — the earlier extraction had misread this block as a "notch between F and E"; หน้า32/33 elements corrected (B3_top/B2_top → F'2-F'4; B4_notch/B2_notch → F2-F4; B2X "corner markers" → real F→F' extension beams — same X-suffix pattern as house #2). Architecture หน้า06's "1.00 north overhang" = the same bay, cascade note added.
- **`4'` = 9.6 ADDED**: S-04's BX1 beam stub at row B extends 0.60 (printed) east of grid4 → its endpoint gets a named line per the rule. Stub's y-extent still needs zoom.
- **NOT added (rule says beams only)**: carport SO slab edge at A-1.50 (printed, no beam), floor-2 balcony edge at A-0.40 (printed, no beam), 0.60/0.50 top-chain eave overhangs (no beam).
- **Dummies REQUIRED but position unknown (flagged in master)**: stair-landing beams in the stairwell zone 3-4 x D-C (off-grid B1 divider ~0.7-0.8 below D + off-grid B2 east wall ~1.75 east of grid3, per pixel estimate only; S-05's "ที่ระดับชานพัก" callouts confirm they're landing-level) — positions must come from stair details (S-11/S-12) before naming; also S-04's SOA-block west wall (~1.5 east of grid1, no beam label confirmed).
- **Axis-origin deviation flagged, not changed**: this master anchors y at row A=0 growing northward; houses 02-04 anchor the topmost row. Makham to decide on renumbering (would be F=0...A=13.4, A'=15.45, F'=-1.0).

**⚠️ Not done for house 05 (out of this round's scope):** specs{} dedup on the beam plans (no S-08 join yet), additional_bars zoom verification, elevations/sections/schedules/details/BOQ pages, cross-house price-table check, sync back to the protected folder.


### 36. House 05 follow-up (2026-07-19, autonomous continuation per Makham's "keep working" instruction)

- **Stairwell dummies pinned**: `3'`=8.35 (landing west edge; A-20's chain 0.10+2.25 from grid3 = S-11's grid4-0.65, exact agreement) and `D'`=6.60 (stair lane-divider beam; A-20's right chain 0.10|0.90|0.20|0.90|0.10) — resolves the "position unknown" flag from item 35; earlier pixel estimates were off 0.4-0.6m, printed chains win. หน้า22/32/33 + master updated.
- **S-08 (หน้า36) dot-count pass** — 5th house in a row with the additional_bars bug: B2X top 2→**3** (2 continuous + 1 lap 1.00m); B3X additional 3-Ø16 was a **phantom** (plain 2/2, label bled from neighboring B3); B4 bottom 2+add3 → **5** (3 full bottom row + 2 L/8 second layer); B5 bottom 3→**5** (the 2-Ø20 L/8 bars were missing entirely — same compounding error as house #4's B5). specs{} blocks (corrected values) added to หน้า32/33 per the houses-2-4 convention.
- **Misc reclassification**: หน้า88 (notes→misc), หน้า89 (material_list→misc), หน้า90 (title→misc) per item-13/22/27/32 precedent.

### 37. ⚠️ MAJOR: series price-table 8x re-verification — REVERSES the 2026-07-16 house-#10 corrections + 4 more digit fixes (all 4 copies agree)

Trigger: house-05's copy (หน้า89) zoomed cleanly at 6x and showed `๒,๑๔๕,๐๐๐` for #10 pile — the exact value the 2026-07-16 session had "corrected away" on houses 02/03/04. Re-zoomed ALL FOUR copies (02 หน้า71, 03 หน้า88, 04 หน้า93, 05 หน้า89) at 6-8x with **in-sheet reference glyphs** (๙๗๒,๐๐๐ and ๑,๙๑๐,๐๐๐ give the true ๙ shape = loop + tall spike; ๒,๑๔๕ itself shows ๔ open-flag vs ๕ closed-curl side by side):

- **#10 pile = 2,145,000 / spread = 2,035,000** on every copy. The 2026-07-16 zoom (item 28/33) misread ๑ as ๙ and ๕ as ๔; house-02's ORIGINAL extraction was right all along. Also sanity: 2,145,000 fits the size-price progression (#9 180sqm=2,001,000 < #10 224sqm=2,145,000); 2,945,000 was an unexplained 45% outlier that house-04's own warning had even guessed was "possibly a digit misread of 2,145,000".
- **#1 pile = 1,020,000** (was 1,060,000 in houses 02/04/05's files and 1,080,000 in house 03's — two different misreads of the wavy-headed ๒).
- **#2 pile = 1,460,000** (was 1,560,000 in every file — ๔/๕ misread; this is house-02's OWN headline price).
- **#4 spread = 1,824,000** (houses 03/02 already had it right; houses 04(own row!)/05 had 1,828,000 — ๔/๘ misread).
- **#3/#8 size = 156 sqm** (house 03's file had 146 — ๕/๔ misread; all other copies read 156).
- **#6 = 1,102,000/1,227,000 confirmed as printed** (the spread>pile inversion is genuinely on paper — flag stays). **#7 pile = 1,528,000 stands** (2-copy zoom agreement, unchanged).

All 4 files fixed with RE-CORRECTED/CORRECTED_2026-07-19 flags; JSON.parse validated. **Lesson recorded**: when verifying a digit, always compare against known reference glyphs ON THE SAME SHEET rather than judging a glyph in isolation — the 16-07 zooms judged in isolation and flipped the wrong way; and when the same table appears in every house, verify on 3+ copies before "resolving" a conflict.

**Addendum to item 37 (same day): 5th copy (house 01, หน้า60) checked and fixed too.** Its full-view read agrees with the other 4 copies. Its raw_text had its own unique misreads fixed: #1 pile 1,060,000→1,020,000; #2 pile 1,560,000→1,460,000; #8 spread 1,662,000→1,622,000 (unique to this copy); #10 spread 2,235,000→2,035,000 (unique). Notably house-01 had #10 PILE = 2,145,000 correct from the start — a 2nd file that always agreed with the now-restored value. Price table is now consistent across all 5 houses: #1 1,020,000/972,000; #2 1,460,000/1,381,000; #3 1,668,000/1,588,000 (156sqm); #4 1,910,000/1,824,000; #5 2,009,000/1,899,000; #6 1,102,000/1,227,000 (inversion as printed); #7 1,528,000/1,459,000; #8 1,703,000/1,622,000 (156sqm); #9 2,001,000/1,872,000; #10 2,145,000/2,035,000.

**Addendum (2026-07-19, house 03): added dummy grid E-prime = -1.5** (y-axis, 1.5m north of origin row E) — value given directly by Makham, confidence 1, not cascaded yet.

## 03บ้าน_เล็ก_2ชั้น_01 (รอบตรวจภาพต้นฉบับ 2026-07-20)

### 38. ⚠️ ต้องให้มะขามตัดสิน: คานตะวันออกช่องบันได ชั้น1 กับ ชั้น2 ไม่ตรงกัน 0.29 m

วัดด้วยสเกลเดียวกันทั้งสองแผ่น (calibrate จากจุดศูนย์กลางเสา grid1 x=1434.3px, grid2 x=1707.7px → 78.1 px/m):

| แผ่น | หน้าคานตะวันตก | หน้าคานตะวันออก | centerline |
|---|---|---|---|
| S-04 ชั้น1 (หน้า31) | 2.162 | **2.347** | **2.25** |
| S-05 ชั้น2 (หน้า32) | 1.868 | 2.054 | **1.96** |

- ชั้น1 ตรงกับ `1''` ใน หน้า00 เป๊ะ (face 2.35 / centerline 2.25) → ค่าที่มะขามตั้งไว้ **ถูก** สำหรับชั้น1
- ชั้น2 เยื้องไปทางตะวันตก 0.29 m — ต่างกัน ~23 px เกินความคลาดเคลื่อนการอ่านเยอะ ไม่ใช่ error การวัด
- ตอนนี้บันทึกคานชั้น2 ไว้ที่ `1''` ชั่วคราว + flag ดังไว้ในไฟล์ **ถ้าผิดต้องเพิ่ม dummy ใหม่ของชั้น2 ที่ ~1.96**

### 39. ถอนคำ: ค่าที่วัดได้เรื่อง `2'` เมื่อ 2026-07-19 ผิดเอง

เมื่อวานจดว่า "วัด 2' ได้ ~4.9-5.0 m ต่างจาก master 4.6 อยู่ 0.3-0.4 m" — **ผิด เกิดจาก pixel read ที่ไม่ได้ calibrate** วัดใหม่: ชั้น1 = 4.69, ชั้น2 = 4.59 → `2'=4.6` ของมะขามถูกอยู่แล้ว ไม่มี gap. ลบ flag นั้นออกจากหน้า31 แล้ว

**บทเรียน (ต่อจากข้อ 37):** อย่าใช้ค่าที่วัดจาก pixel ถ้ายังไม่ผูกสเกลกับจุดอ้างอิงที่รู้ค่าจริง (จุดศูนย์กลางเสาสองต้นที่รู้ระยะ) — วัดลอย ๆ พลาดได้ถึง 0.4 m

### 40. หน้า31 + หน้า32 ตรวจภาพต้นฉบับเต็มแผ่น — เจอของหายและ label ผิดเพียบ

**หน้า32 (S-05) สกัดใหม่ทั้งแผ่น 20 → 32 element:**
- ขาด **แถวคานแนวนอนเส้น D ทั้งแถว** (B3X 1'-1, B3 1-2, B2 2-3)
- ขาด B3X เส้น C ช่วง 1'-1 และ C1-C2 ติด label ผิดเป็น B3X (จริงคือ B3)
- ขาดคานตั้ง grid2 ในแถบ D-C (B1), ขาดคานตั้งที่ `2'`, ขาดคานตั้ง grid2 ในแถบ B-A
- ขาด **คาน B1 ที่เส้น `B'` (B'2-B'3)** ← นี่คือของที่ cascade B' ต้องได้
- B4X จด B3-A3 span 1.5 → จริง **B3-B'3 span 1.0** (คานหยุดที่ B' ไม่ถึงแถว A) — แก้ข้อสงสัยเก่าเรื่อง "ชั้น2 ยื่นเลย A3 ไหม" ว่า **ไม่ยื่น**
- คานเส้น A ช่วง 1-2 จด B2 → จริง **B1**
- ขาด BX (ตอม่อสั้นฝั่งตะวันออก grid3) และ marker **S2** หายทั้งตัว

**หน้า31 (S-04) แก้ 7 จุด:**
- D1-C1 → **D1'-C1'** (ไม่มีคานตั้งที่ grid1 ในแถบ D-C ทั้งสองชั้น ช่องบันไดพาดข้าม)
- D1-D2 จด B3X → **B3**, เพิ่ม **B3X D1'-D1** (1.1)
- C1-C2 จด B3X → **B3**, เพิ่ม **B3X C1'-C1** (1.1)
- C2-C3 จด B3 → **B2**
- เพิ่มคานตั้ง **B1 B2-A2** (1.5)
- ปลด flag "อาจเป็นขอบ void" ของ B2 D2-D3 กับ B1 D3-C3 (ยืนยันแล้วว่าเป็นคานจริง) conf 0.55 → 0.8/0.7

### 41. ค้าง (ยังไม่แตะ)
- **`B'` บนชั้น1**: warning เดิมเขียนว่า "ไม่มีอะไรที่ B' บนหน้า31" — **ผิด** มีเส้นแนวนอนที่ y=10.07 (x 3.62→6.52) เป็นขอบเหนือโซน S0 แต่ไม่มี label ที่อ่านออก และอยู่ในโซนลานนอกอาคารที่ flag ไว้แต่เดิมว่าต้องซูมอ่านต่างหาก → ยังไม่ลงเป็น element แก้ warning ให้ตรงแล้ว
- คานตั้งขอบตะวันออกที่ `3'` (7.04 m) ในแถบ D-C หน้า32 — ไม่มี mark ที่อ่านออก ยังไม่ลง
- คาน `B1(ชพ)` สองตัวในกล่องบันไดหน้า32 — ลงไว้แล้วแต่ conf 0.45 (ระยะ 3.06 เดาจากขอบกล่อง ไม่ใช่ dim ที่พิมพ์) รอ S-11/S-12
- โซน S0 ใต้แถว A + "ดูขยาย 2" หน้า31 ยังไม่ซูมอ่าน (ของค้างเดิม)

### 42. บ้าน3 หน้าอื่นที่รับผลจากการแก้ B'(10.2→10.1): หน้า00 + หน้า32 ปรับตามแล้ว

มะขามให้ค่า B' ใหม่ = 3+2.2+4+0.9 = **10.1** (เดิม 10.2) แก้ที่หน้า00 แล้ว cascade ผลกระทบ:
- หน้า32 (S-05): B4X (B3→B'3) span 1.0 → **0.9**

### 43. หน้า31: ลบ 2 element ตามคำสั่งมะขาม (ส่วนต่อขยายแถว E ฝั่งตะวันตก/ตะวันออก)

ลบ `B2 E1'-E1`(1.1) และ `B2 E3-E3'`(0.5) — เหลือ 27 element ผ่าน JSON

### 44. หน้า33 (S-06 คานอะเส) ตรวจภาพต้นฉบับเต็มแผ่นแบบเดียวกับหน้า31/32 — 17→23 element

**แก้ label ผิด:** `D1'-C1'` → **`D1-C1`** (ซูม 10x วัดแล้วเสาอยู่ตรงเส้น grid1 พอดี ไม่มี offset ไปทาง `1'` เหมือนที่คิดตอนแรก — ต่างจาก S-04/S-05 เพราะคานอะเสเป็นวงรอบอาคารต่อเนื่อง ไม่ขาดเพราะช่องบันไดชั้นล่าง)

**เพิ่มที่ขาด (รูปแบบเดียวกับ S-04/S-05):**
- คานตั้ง `D2-C2` (2.2) — หายไปทั้งเส้น
- คานตั้ง `B2-A2` (1.5) — หายไปทั้งเส้น
- คาน `B'2-B'3` (3.0, แนวนอน) — ขอบใต้กล่อง S1 เส้นประ (ขอบบน/เส้น B ไม่มีคาน เปิดโล่ง, ขอบล่าง/เส้น B' มีคานจริง) ← **cascade ของ B' ที่ตามหา**
- คาน `B3-B'3` (0.9, แนวตั้งสั้น) — ปลายมุมตัดก่อนถึงแถว A ตรงกับ footprint ที่ไม่มีเสาที่ A3 (conf ต่ำกว่าเพราะไม่เห็น label แยกชัด)

ยกระดับ confidence คาน B1-B2/B2-B3 จาก 0.7→0.85 (ยืนยันแล้วว่าเป็นคานจริง ไม่ใช่กล่อง S1)

**ค้าง:** ไม่มี — หน้า33 ตรวจครบทุกแถบแล้ว (E-D, D-C, C-B, B-A, ใต้ A)

### 45. หน้า33 แก้เพิ่มตามคำสั่งมะขามตรง — เพิ่ม 4 connector segment (D1'-D1, C1'-C1, D3-D3', C3-C3')

มะขามยืนยันว่าแถบ D-C **มี** ส่วนต่อขยาย 1'/3' จริง (ผมซูมแล้วสรุปผิดว่าไม่มี เพราะ calibrate สเกลจากหน้า31/32 มาใช้กับหน้า33 โดยไม่ตรวจ origin ของแผ่นนี้เองซ้ำ) เพิ่ม 4 เส้นสั้นตามแบบ B3X ของ S-04/S-05:
- `D1'-D1` (1.1), `C1'-C1` (1.1) — ฝั่งตะวันตก
- `D3-D3'` (0.5), `C3-C3'` (0.5) — ฝั่งตะวันออก

ทั้งหมด conf 1 (user-confirmed) — 23→27 element

**บทเรียน:** อย่าเอาค่า calibrate สเกล (px/m, ตำแหน่งอ้างอิง) จากแผ่นหนึ่งไปใช้กับอีกแผ่นโดยไม่ verify origin ของแผ่นใหม่เอง แม้ขนาดภาพจะเท่ากันก็ตาม

### 46. Column rebar convention bug — swept whole project, fixed 8 files (not just หน้า35)

มะขามจี้ที่หน้า35 (C1A อะเส) ว่ายังใช้ `top:4/bottom:4` (=8 รวม) ทั้งที่ตกลงกันแล้ว (item 21) ว่าเสาต้องใช้ `main_bar:{count:4}` ตัวเดียว — ตรวจแล้วพบว่าอนุสัญญานี้**หลุดไปหลายจุด**:

**หน้า35 (S-08, บ้าน3) — แก้ 5 จุด:** C1A/C1 ระดับอะเส, C1A/C1 ระดับพื้นชั้น1/ตอม่อ/ฐานราก, CN — ทั้งหมด `top:4/bottom:4` → `count:4`

**เสารั้ว C1 (fence column) — แก้ 6 ไฟล์ทั่วโปรเจกต์:** บ้าน1 หน้า16/17, บ้าน2 หน้า20/21, บ้าน3 หน้า26/27 — ไฟล์เหล่านี้เชื่อว่า schema "บังคับ" ให้มี top/bottom เสมอ (จึงเลี่ยงบั๊กด้วยการเติม field `total_main_bar_count` แทนที่จะแก้โครงสร้างจริง) แก้เป็น `count:4` ตัวเดียวทั้งหมด ลบ `total_main_bar_count` ที่ไม่จำเป็นแล้วออก

**เพิ่มเติม:** หน้า31 (S-04) มี element `CN` (เสาสั้นช่วงบันได) แต่ไม่เคยมี `specs.CN` เลย — เพิ่มเข้าไปโดยดึงจากหน้า35 ที่แก้แล้ว (ตามแบบเดียวกับที่ B1-B5 มี specs{} ของตัวเอง)

**Sweep ยืนยัน:** ค้นทั้งโปรเจกต์ (ทุกบ้าน ทุกไฟล์) หา `element_type:"column"` ที่ยังมี `main_bar.top` เหลืออยู่ — **ไม่พบอีกแล้ว** ครบทุกไฟล์

**บทเรียน:** อนุสัญญาที่ "ตกลงกันแล้ว" ในบันทึกไม่ได้แปลว่าถูก apply ครบทุกไฟล์จริง — ควร grep sweep ทั้งโปรเจกต์เป็นระยะเมื่อพบบั๊กเชิงระบบแบบนี้ ไม่ใช่แก้แค่ไฟล์ที่ถูกชี้มา

### 47. บ้าน3: ตรวจทุกไฟล์ pattern=plan (20 ไฟล์) เทียบกับ gridline master ที่แก้วันนี้ — เจอ 4 ไฟล์มีปัญหา

มะขามสั่งให้ไล่ตรวจทุกหน้า pattern:plan ในบ้าน3 เทียบกับหน้า00 (ซึ่งวันนี้แก้ไปหลายจุด: 1''=2.35, 2'=4.6, B'=10.1, ลบ element ใน 31/32). Sweep ครบ 20 ไฟล์:

**แก้แล้ว (4 ไฟล์):**
- **หน้า06, หน้า07** (floor plan ground/upper): มีข้อความอ้าง "1''(2.25)" ค้างจากก่อนแก้ (ตอนนั้น 1'' ยังไม่ยุติว่า face=2.35 หรือ centerline=2.25) — ปรับคำอธิบายให้ชัดว่า master ปัจจุบันคือ 2.35 (face) ตรงกับที่แผ่นเหล่านี้วัดเองอยู่แล้ว ไม่มี gap ค้างอีกต่อไป
- **หน้า21** (stair ST2 plan): ข้อความอ้าง "2.25" ในบริบทความยาวขั้นบันได (คนละความหมายกับ 1'' แต่เขียนแบบกำกวม) — ปรับให้ชัดว่า 2.25 คือ**เส้นกึ่งกลางผนัง** (centerline) ไม่ใช่ค่า master หลัก (2.35=face)
- **หน้า07 (จุดที่ 2):** เจอบั๊กจริง — grid_ref เขียนว่า `"A-B'"` ทั้งที่คำอธิบายในตัว element เองบอกชัดว่าอยู่ในช่วง A-to-A' (ใต้แถว A) ไม่เกี่ยวกับ B' เลย (B' อยู่เหนือแถว A ระหว่าง B-A คนละตำแหน่ง) แก้เป็น `"A-A'"`

**ตรวจแล้วสะอาด ไม่ต้องแก้ (16 ไฟล์):** หน้า18, หน้า20 (A' อ้างอิงถูกต้องอยู่แล้ว), หน้า29/30/31/32/33 (แก้ไปแล้วในรอบก่อนหน้าวันนี้), หน้า50/51/58 (มี grid_dimensions_as_printed.note ที่อ้างค่า 1'/3'/A'/A'' ตรงกับ master ทุกตัว), หน้า52(wc1/wc2)/59/60/61/62/63 (ไม่มีค่า dummy grid จริง — เจอ apostrophe ที่ match แค่จาก "house #2's" ภาษาอังกฤษ ไม่ใช่ grid reference)

**Validate:** ทั้ง 20 ไฟล์ parse ผ่าน JSON ครบ

## กฎทั่วไป (ใช้ทุกบ้าน)

### 48. ⚠️ กฎ: element_id เดียวกันในแผ่นเดียวกัน = **1 entry เท่านั้น** (รวม count + grid_refs)

**มะขามสั่ง 2026-07-20** — ถ้า element_id เดียวกันโผล่หลาย entry ในไฟล์เดียวกัน ให้**รวมเป็น entry เดียว** count บวกกัน grid_refs ต่อกัน:

```
// ❌ ผิด — แยก 2 entry
{ "element_id": "F0.60x0.60", "count": 2, "grid_refs": ["E'1", "E'2"] },
{ "element_id": "F0.60x0.60", "count": 2, "grid_refs": ["A2", "A3"] }

// ✅ ถูก — รวมเป็นอันเดียว
{ "element_id": "F0.60x0.60", "count": 4, "grid_refs": ["E'1", "E'2", "A2", "A3"] }
```

**ใช้แม้ว่า column pairing จะต่างกัน** — เดิมไฟล์บ้าน 2/3/4 เขียน warning ไว้ว่า "kept as 2 separate entries since column pairing differs" อันนั้น**เลิกใช้** เพราะ element เป็น positional-only อยู่แล้ว (spec อยู่ใน `specs{}` แยก) การที่เสาข้างๆ ต่างมาร์คไม่ใช่เหตุผลให้แยก footing entry

**⚠️ ห้ามสับสนกับกรณีนับซ้ำ (ข้อ 38/บ้าน3 หน้า29/30):** ถ้า grid_ref **ซ้ำกัน**ระหว่าง entry (เช่น B3 อยู่ทั้งสองอัน) = บั๊กนับซ้ำจริง ต้อง**ลบ**ตัวซ้ำออก ไม่ใช่บวก count เข้าไป
- grid_refs **ไม่ทับกัน** → merge (บวก count)
- grid_refs **ทับกัน** → ลบตัวซ้ำ (count ไม่เพิ่ม)

**ยังไม่ได้ sweep ทั้งโปรเจกต์** — แก้แล้วเฉพาะบ้าน 04 หน้า31/32 (ข้อ 49) บ้านอื่นยังไม่ตรวจ

**⚠️ ข้อยกเว้น: ตาราง schedule หลายระดับ** — กฎนี้ใช้กับ element ที่มี**ตำแหน่ง**บนแปลน (ฐานราก คาน) เท่านั้น ตารางเสาที่มีหลาย level (เช่น หน้า38 บ้าน04, หน้า35 บ้าน03) **ต้อง**ซ้ำ element_id ได้ 1 ครั้งต่อ level ตาม schema §8 ห้าม merge

### 49. บ้าน04 หน้า31/32 — merge ตามกฎข้อ 48 (4 มาร์คต่อไฟล์)

**หน้า31 (S-02 ฐานรากแผ่)** 12→8 element:
- `F0.60x0.60`: [E'1,E'2] + [A2,A3] → count 4
- `F1.30x1.30`: [E1] + [D1,D3,C1,C3,B2,B3] → count 7
- `F1.60x1.60`: [E2] + [D2,C2] → count 3
- `F1.00x1.00`: [E3] + [B1] → count 2 (ลบ warning "kept as 2 separate entries" ทิ้ง)

**หน้า32 (S-03 ฐานรากเสาเข็ม)** 12→8 element (layout เดียวกัน ต่างแค่มาร์ค):
- `F2`: [E'1,E'2] + [A2,A3] → count 4
- `F9`: [E1] + [D1,D3,C1,C3,B2,B3] → count 7
- `F12`: [E2] + [D2,C2] → count 3
- `F5`: [E3] + [B1] → count 2

เสา (C0/C1A/C1/C2) ทั้งสองไฟล์ merge ถูกอยู่แล้ว ไม่ต้องแก้

**Validate:** ทั้งสองไฟล์ element 12→8, footing entry 8→4, count รวม 16 เท่าเดิม, ไม่มี element_id ซ้ำ, ไม่มี grid_ref ซ้ำ, และ count ของทุก entry = จำนวน grid_refs พอดี

### 50. ⚠️ กฎใหม่: เหล็กกลางที่เด่นชัด → `main_bar.middle` (ไม่ใช่ `additional_bars`)

**มะขามสั่ง 2026-07-20** (ดูรูปหน้าตัด B4A บ้าน04) — ถ้าหน้าตัดมี**แถวเหล็กกลางที่ชัดเจน** (มี leader ของตัวเอง เป็นแถวจุดแยกระหว่างกลุ่มบนกับกลุ่มล่าง มักเจอในคานลึก) ให้ลงเป็น face ที่สามของ `main_bar` ไปเลย:

```json
"main_bar": {
  "top":    { "count": 2, "dia_mm": 16, "type": "RB" },
  "middle": { "count": 2, "dia_mm": 9,  "type": "RB" },
  "bottom": { "count": 4, "dia_mm": 16, "type": "RB", "note": "2 เต็ม + 2 หยุดที่ L/8" }
}
```

**เคสจริง:** B4A/B4X บ้าน04 (คานลึก 200x700) พิมพ์ `2Ø9มม.` ชี้ไปแถวจุดกลาง ระหว่าง `2-Ø16` บน กับ `2-Ø16` + `2-Ø16(หยุดที่ L/8)` ล่าง

**ข้อจำกัด:** `middle` เป็น optional ใส่เฉพาะที่มีจริง — ห้ามสร้างขึ้นมาเองด้วยการแยกกลุ่มบน/ล่าง และห้ามใช้กับเหล็กที่แค่*วาด*อยู่ตรงกลางแต่ leader ผูกกับ face บน/ล่าง (เคสนั้นยัง merge เข้า top/bottom ตามกฎเดิม §7)

**ผลต่อ `additional_bars`:** ตอนนี้เหลือไว้เฉพาะเหล็กที่**ไม่สังกัด face ไหนเลย** — "ไม่ใช่บนไม่ใช่ล่าง" ไม่ใช่เหตุผลให้อยู่ใน additional_bars อีกต่อไป

**แก้แล้ว:** `primary_rawjson_schema.md` §6 (+ แก้ §7 ให้อ้างถึง middle), บ้าน04 หน้า37 (B4A/B4X) + cascade เข้าหน้า33 `specs{}`

**Sweep ยืนยัน:** ค้นทั้งโปรเจกต์หา additional_bars ที่มีคำว่า mid-depth/skin/waist — ไม่เหลือแล้ว มีแค่ 4 จุดที่เป็น `main_bar.middle` (B4A/B4X × 2 ไฟล์) บ้านอื่นไม่มีคานลึกแบบนี้

## 05บ้าน_เล็ก_2ชั้น_03 (รอบ F=0 re-origin + item 48, 2026-07-24)

**Scope รอบนี้:** ปิด flag ที่ค้างจากข้อ 35 (axis-origin) + apply item 48 (footing merge) ที่ยังไม่เคยถึงบ้าน 05 — เฉพาะ หน้า00 + หน้า30-33. หน้า34/35 ตรวจแล้วไม่ต้องแก้ ID.

### 51. Grid master — ตัดสิน axis-origin: re-base เป็น F=0 (ปิด flag ข้อ 35) + เพิ่ม dummy 7 เส้น

**File:** `บ้าน_เล็ก_2ชั้น_03_หน้า00_gridline.json`
**Field:** `y_lines[]`, `x_lines[]`

มะขามให้ค่า y-axis ตรงเป็นสูตร (อ่านจาก dimension chain แบบจริง หน้า32.png, top→bottom): `F=0, E=2, D=2+3.7=5.7, C=+2.2=7.9, B=13.4-1.8=11.6, A=13.4, A'=13.4+2.05=15.45`, และ `f'=-1` (1.00 เหนือ F). แทนที่ orientation เดิม (A=0 ที่ข้อ 35 ตั้ง provisional ไว้).

**Before (A=0 origin):** `Aprime(-2.05), A(0.0), B(1.80), C(5.50), D(7.70), E(11.40), F(13.40)`
**After (F=0 origin):** `f'(-1.0), F(0.0), E(2.0), e'(4.4), D(5.7), C(7.9), B(11.6), A(13.4), A'(15.45)`

- `Aprime` → เปลี่ยนชื่อเป็น `A'` + re-base pos_m -2.05 → 15.45 (เส้นเดียวกันทางกายภาพ, note carport ground-floor-only เดิมเก็บไว้)
- **x-axis เพิ่ม 5 dummy** (มะขามให้ตรง): `1'=-0.6, 1''=1.5, 1'''=2.0, 3'=6.3, 4'=9.5` (1'/4' = overhang 0.60/0.50 ที่ปลาย grid1/grid4 ตรงกับ `overhang_dims_m` ในหน้า32)
- **y-axis เพิ่ม 2 dummy**: `e'=4.4` (ระหว่าง E/D), `f'=-1.0` (เหนือ F)
- ทั้งหมด confidence ตามที่มะขามยืนยัน, ใส่ค่าตรงตามที่ให้ ไม่ตีความเพิ่ม (แบบเดียวกับ posture ข้อ 23/24)

**หมายเหตุ naming:** ตอนแรก e'/f' ดูขัดกฎ "main grid ด้านบน/ซ้าย" ภายใต้ orientation เก่า (A=0) — แต่พอ re-base เป็น F=0 ทั้งคู่ชื่อถูกตามกฎ (e' อยู่ระหว่าง F–E ในลำดับอ่าน, f' อยู่ก่อน origin F). ยืนยันกฎที่มะขามแก้ให้: **dummy ตั้งชื่อตาม main grid ที่อยู่ด้านบน (แกน y) / ทางซ้าย (แกน x) — ไม่ใช่ตัวที่ใกล้สุด** (สเปค primary_rawjson_schema.md §4 เขียน "nearest" ควรแก้ให้ตรงด้วย — ยังไม่แก้).

### 52. Cascade F=0 + Aprime→A' เข้า หน้า32/33 (beam plan ชั้น1/ชั้น2)

**สิ่งที่เปลี่ยนจริง = แค่ `gridAprime`→`gridA'`** เพราะ named grid ตัวอักษร (F..A) ไม่เปลี่ยนชื่อ (เปลี่ยนแค่ pos_m ซึ่งอยู่ในหน้า00 อย่างเดียว) — beam ref ที่ใช้ gridF/gridE/... ยังถูกทั้งหมด ไม่ต้องแตะ.

- **หน้า32**: `grid1-gridAprime`→`grid1-gridA'` (beam `B1_extension_1_A_Ap`), `grid1-gridAprime`/`grid2-gridAprime`→`gridA'` (beam `B2_extension_bottom_12_Ap`), slab marker SO "gridA-gridAprime"→"gridA-gridA'"; refresh warning เดิม ("gridAprime NOT yet added to master, pending") → resolved เป็น A' pos 15.45
- **หน้า33**: ไม่มี beam ใช้ ID `gridAprime` (อ้าง extension เป็น text) — refresh แค่ warning ที่ยังเขียน "gridAprime at -2.05 flagged provisionally"
- **หน้า34/35**: ไม่มี ID `gridAprime` (อ้าง extension เป็น text ที่ยังถูก) — ไม่แก้

**ตรวจภาพยืนยัน:** หน้า32.png dimension chain แกน y = 1.00/2.00/3.70/2.20/3.70/1.80/2.05 ตรงกับค่า F=0 ที่ลงในหน้า00 เป๊ะ

**⚠️ ยังไม่ทำ (ต้องมะขามชี้ beam→dummy):** dummy ใหม่ 6 เส้น (1'/1''/1'''/3'/4'/e'/f') ยังไม่มี beam ไหนใน 32-35 อ้างถึง — beam ปัจจุบันใช้ named grid หรือ text คลุมเครือ ("grid2 area near gridD"). การผูก beam เข้ากับ dummy ต้องอ่านแบบ/มะขามระบุ ไม่กุใส่ ground truth เอง.

### 53. item 48 (merge duplicate element_id) — apply กับ footing plan หน้า30/31

ตาม template บ้าน 04 ข้อ 49: แยกแต่ละ entry footing+column รวม เป็น (ก) footing entry คีย์ตามขนาด/pile mark + (ข) column entry คีย์ตามมาร์ค — `column_mark` ที่เดิมฝังใน footing ไม่หาย กลายเป็น entry ของตัวเอง.

**หน้า30 (S-02 ฐานรากแผ่)** 17 → 5 footing + 5 column:
- footing: `F1.0x1.0`[F2,F4,B4]=3, `F1.30x1.30`[E1,E4,D4,B1]=4, `F1.80x1.80`[E2,C2]=2, `F1.50x1.50`[E3,D1,D3,C1,C4,B2]=6, `F0.80x0.80`[A1,A2]=2
- column: `C0`[F2,F4]=2, `C1A`[E1,E3,E4,A1,A2]=5, `C2A`[E2]=1, `C1`[D1,D4,C1,C4,B1,B2,B4]=7, `C2`[D3,C2]=2
- คง flag E3 offset (needs zoom) ไว้บน entry F1.50x1.50

**หน้า31 (S-03 ฐานรากเสาเข็ม)** 17 → 6 footing + 5 column (layout เสาตรงกับหน้า30):
- footing: `F6`[F2,F4,B4]=3, `F8`[E1,E4,D4,B1]=4, `F16`[E2,C2]=2, `F11`[E3,D1,C1,C4,B2]=5, `F12`[D3]=1, `F4`[A1,A2]=2
- column: เหมือนหน้า30

grid_ref format ปรับ row-first พร้อมกัน (`grid2-gridF`→`F2`) ตาม item 34.

**Validate:** ทั้งสองไฟล์ footing รวม 17 (เท่าเดิม), column รวม 17, count=len(grid_refs) ทุก entry, ไม่มี grid_ref ซ้ำ (pure merge ไม่ใช่ double-count), JSON parse ผ่าน.

### 54. ค้าง (ยังไม่ทำสำหรับบ้าน 05)
1. ผูก beam→dummy ใหม่ (e'/f'/1'/1''/1'''/3'/4') ใน หน้า32-35 — รอมะขามชี้ตำแหน่งจากแบบ
2. 12 ไฟล์ non-plan ที่ยังเขียน "Aprime" (หน้า05/06/10/12/13/14/20×2/53/61/62/63) — ส่วนใหญ่อยู่ใน warning/text prose ไม่ใช่ grid_ref จริง; กวาด→A' หรือไม่ ยังไม่ตัดสิน
3. ตรวจภาพต้นฉบับ elevations/sections/schedules/BOQ (ยังไม่เปิด รอบนี้)
4. sync กลับ `rawjson_ยังไม่ได้แก้ไขโดนคน/05บ้าน_เล็ก_2ชั้น_03/` (ตาม rule_of_tune ข้อ 1-3 เต็ม) — ยังไม่เริ่ม

### 55. ⚠️ พบว่า item 34-37 ที่บันทึกไว้ **ไม่ได้อยู่ในไฟล์จริง** — ต้องทำใหม่

ตอนเทียบ format บ้าน04 กับ 05 พบว่าสิ่งที่ item 34-36 บันทึกว่าทำแล้ว ไม่มีในไฟล์ `json_แก้ไขแล้ว/05.../` เลย:

| item บันทึกว่าทำ | สภาพไฟล์จริง (2026-07-24) |
|---|---|
| specs{} เพิ่มใน หน้า32/33 | **ไม่มี specs{}** เลย (0 block) |
| element_id เป็น mark สะอาด | ยังเป็น descriptive: `B3_top`, `B4_notch`, `B2X_corner_2F`, `BX1_local_34B` |
| normalize `grid2-gridF`→`F2` | ยังเป็น `grid2-gridF` แบบเก่า |
| หน้า36 B2X top 2→3 | ไฟล์จริง top **2** + additional_bars ยังแยก |
| หน้า36 B4 bottom→5 | ไฟล์จริง bottom **2** + additional_bars [3-Ø16] |
| หน้า36 B5 bottom→5 | ไฟล์จริง bottom **3** (เหล็ก 2-Ø20 L/8 หายทั้งคู่) |

สันนิษฐาน: โฟลเดอร์ถูก re-copy จาก raw ทับหลังเขียน doc หรือ edit ไม่เคยเซฟลงชุดนี้ **บทเรียน: บันทึกใน doc ไม่ได้การันตีว่าไฟล์ถูกแก้จริง — ควร verify ไฟล์ก่อนเชื่อ log (เหมือนบทเรียน item 46 เรื่องอนุสัญญาที่ตกลงแล้วแต่ไม่ได้ apply ครบ)**

### 56. หน้า36 (S-08) แก้ additional_bars 4 จุด — **ยืนยันจากภาพต้นฉบับ ไม่ได้เชื่อ doc อย่างเดียว**

ซูมอ่าน `หน้า36.png` ทีละหน้าตัด ตรวจลำดับ label เอง ผลตรงกับที่ item 36 เคยบันทึกทั้ง 4 จุด:

| mark | label บนแบบ (บน→ล่าง) | เดิมในไฟล์ | แก้เป็น |
|---|---|---|---|
| **B2X** | `2Ø16(ต่อเนื่องจาก B2)` / `1Ø16 ล้วงเข้า B2 ระยะ1.00ม.` / ป.Ø6@0.20 / `2Ø16(ต่อเนื่องจาก B2)` | top 2 + additional 1 | **top 3** (lap อยู่ face บน ตามลำดับ label เดียวกับบ้าน1 item 11) |
| **B3X** | `2Ø16(ต่อเนื่องจากB3)` / ป.Ø6@0.20 / `2Ø16(ต่อเนื่องจากB3)` — **ไม่มี label 3Ø16 เลย** | additional 3-Ø16 | **ลบทิ้ง (phantom)** label เพี้ยนมาจาก B3 ข้างๆ ที่ bottom เป็น `3Ø16` |
| **B4** | `3Ø16` / ป.Ø6@0.20 / `2Ø16 หยุดที่ L/8` / `3Ø16` | bottom 2 + additional 3 | **bottom 5** (3 เต็ม + 2 หยุด L/8) |
| **B5** | `3Ø20` / ป.Ø6@0.15 / `2Ø20 หยุดที่ L/8` / `3Ø20` | bottom 3 (คู่ L/8 หายหมด) | **bottom 5** (3 เต็ม + 2 หยุด L/8) |

ตรวจตัวอื่นด้วย: B1/B2/B3/B4X/BX/BX1/X1-X1/RB1/RB2/RB3 **ถูกอยู่แล้ว** ไม่ต้องแก้ (RB3 asymmetric top16/bottom20 ตรงกับแบบจริง)

### 57. หน้า32/33 — apply format บ้าน04 ครบทั้ง 4 อนุสัญญา (item 9/14/19/25/30)

**(ก) specs{} join จาก S-08** (หน้า36 ที่แก้ตามข้อ 56 แล้ว) — ค่าที่แก้ไหลเข้า specs อัตโนมัติ
- หน้า32: 11 marks — B1,B2,B2X,B3,B3X,B4,B4X,B5,BX,BX1,CN → **ทุก element มี spec ครบ**
- หน้า33: 7 marks (เหลือ `ST` = stair opening ไม่ใช่คาน ไม่ต้องมี spec)
- **`CN` ดึงจาก column schedule หน้า37** ตาม precedent บ้าน03/04 item 46 — ใช้ `main_bar:{count:4}` เดียว ไม่ใช่ top/bottom (150x150, ป.Ø6@0.20, เสาสั้นชั้น1→ระดับชานพัก)
- ลบ `spec_source` ออกจาก elements[] (ย้ายไปอยู่ใน specs{} แทน = จุดที่ dedup)

**(ข) element_id → mark สะอาด** (ข้อมูลตำแหน่งไม่หาย เพราะอยู่ใน grid_ref/element_type อยู่แล้ว):
`B3_top`→`B3`, `B4_notch`→`B4`, `B2X_corner_2F`→`B2X`, `BX_local_2D`→`BX`, `BX1_local_34B`→`BX1`, `CN_column_4D`→`CN`, `B3X_CN_4D`→`B3X` ฯลฯ

**(ค) เรียง elements[]**: คาน (มี grid_ref_start/end) = atomic ตาม schema ไม่ merge; เรียง top→bottom (แถวตาม y_lines) → ซ้าย→ขวา (คอลัมน์ตาม x_lines) → **แนวตั้งก่อนแนวนอน** ที่จุดเริ่มเดียวกัน (item 14)

**(ง) merge point-element ตาม item 48**: corner marker / column ที่มี grid_ref เดี่ยว → รวมตาม (mark, element_type) เป็น count + grid_refs
- หน้า32: 35 → **31 entry** (คาน atomic 22 + point merged 4 + free-text 5)
- หน้า33: 29 → **26 entry** (คาน 21 + point merged 2 + free-text 3)
- **ตำแหน่งไม่หายสักจุด**: 35→35, 29→29 (count รวมของ merged entry = จำนวนเดิม)

**(จ) JSON style**: reformat เป็น compact one-line-per-element แบบบ้าน04 (เขียน serializer แล้ว round-trip test กับไฟล์บ้าน04 ตรงเป๊ะก่อนใช้ — ต่างแค่บรรทัดว่างคั่นกลุ่มแถวที่บ้าน04 ใส่มือไว้ ซึ่ง JSON.parse ทิ้งอยู่แล้ว)

**Validate:** ทั้ง 2 ไฟล์ point-ref resolve กับหน้า00 ครบ 100% ไม่มี dangling, JSON parse ผ่าน, element/position count ตรง

### 58. ⚠️ dummy grid 7 เส้นยังไม่มีคานอ้างถึง — สัญญาณว่า "คานหาย" แบบเดียวกับบ้าน04 item 49

เช็ค หน้า32/33 เทียบ หน้า00: point-ref ทุกตัว resolve ได้หมด **แต่ dummy ที่เพิ่งเพิ่ม 7 เส้น (`1'`,`1''`,`1'''`,`3'`,`4'`,`f'`,`e'`) ไม่มี element ไหนอ้างถึงเลย** (มีแค่ `A'` ที่หน้า32 ใช้ 2 คาน)

ขัดกับกฎ beam-endpoint โดยตรง — dummy มีได้เพราะมีปลายคานมาลง ถ้าไม่มีใครใช้แปลว่า **คานที่ควรลงเส้นพวกนี้ยังหายอยู่** ตรงกับ pattern บ้าน04 item 49 เป๊ะ ("8 คานหายเพราะ master ยังไม่มี dummy ให้ชื่อ — พอเพิ่ม dummy แล้วคานที่หายถึงโผล่")

**free-text 10 จุดที่เหลือน่าจะคือคานพวกนั้นพอดี:**
| free-text | น่าจะเป็น dummy |
|---|---|
| `B4_notch`/`B2_notch_24` "sub-level between gridF and gridE" | `f'` หรือเส้นที่ยังไม่มี |
| `B1_small_rooms_34D`, `BX1_local_34B` "grid3-grid4 area" | `3'`(6.3) หรือ `4'`(9.5) |
| `B1_notch_local`, `BX_local_2D` "grid2 area near gridD" | `e'`(4.4) |
| `B1_small_rooms_DC` "grid1-grid2 area" | `1''`(1.5) หรือ `1'''`(2.0) |

**ยังไม่จับคู่** — ต้องอ่านแบบ/มะขามชี้ว่าคานตัวไหนลงเส้นไหน จับผิด = คานไปอยู่ผิดที่ใน ground truth

---

## 59. ⚠️ ตรวจสอบความสอดคล้อง `primary_rawjson_schema.md` ↔ ไฟล์นี้ — เจอสเปคค้างหลังการตัดสินใจ 4 จุด แก้ครบแล้ว (2026-08-02)

**คำสั่ง:** "เช็คว่าใน primary rawjson and สิ่งที่ต้องแก้สอดคล้องกัน" — ผลคือ **ไม่สอดคล้อง 4 จุด และทุกจุดสเปคเป็นฝ่ายผิด** (ไฟล์นี้ถูก, ข้อมูลจริงก็ทำตามไฟล์นี้ไปแล้ว แต่ไม่มีใครกลับไปแก้สเปค)

**verify กับข้อมูลจริงก่อนแก้ทุกข้อ ไม่ได้เชื่อ log อย่างเดียว** (บทเรียนข้อ 55 — "บันทึกใน doc ไม่ได้การันตีว่าไฟล์ถูกแก้จริง"):
- point-ref ในบ้าน 01-05: ไม่เหลือแบบมีขีดแล้ว ที่ match regex คือ `"S-02"`/`"S-03"` ซึ่งเป็น `sheet_code` ถูกต้องอยู่แล้ว
- `element_type: "column"` ที่ยังมี `main_bar.top`: **0 ไฟล์** ทั้งโปรเจกต์ (ยืนยันข้อ 46 ว่า sweep ครบจริง)

| # | สเปคเขียนว่า (ผิด/ค้าง) | ไฟล์นี้ + ข้อมูลจริง (ถูก) | ที่มา |
|---|---|---|---|
| 1 | §4: `grid_ref` point-type = `"A-1"` มีขีด | point-type **ไม่มีขีด** `"C2"` — ขีดสงวนสำหรับ range เท่านั้น | ข้อ 8 (ตั้งกฎ), ข้อ 18 (ยืนยันทั้งโปรเจกต์) |
| 2 | §4: dummy ตั้งชื่อตาม main grid **ที่ใกล้ที่สุด** | ตั้งชื่อตามตัวที่อยู่ **ด้านบน (y) / ทางซ้าย (x)** — ทิศทาง ไม่ใช่ระยะ | ข้อ 51 (ระบุเองว่าสเปคควรแก้ แต่ยังไม่แก้) |
| 3 | §6: "ต้องแยก top/bottom เสมอ" ไม่มีข้อยกเว้นเสาเลย | **เสาใช้ `main_bar.count` เดี่ยว** ห้าม top/bottom (ไม่งั้นนับเป็น 8 แทน 4) | ข้อ 21 (ตอบคำถามค้างจากข้อ 16), ข้อ 46 (sweep 8 ไฟล์) |
| 4 | §4: มีแต่กฎ atomic ของคาน ไม่มีกฎ merge เลย | point element (ฐานราก/เสา) = **1 element_id ต่อไฟล์** รวม count+grid_refs | ข้อ 48, 49, 53 |

**แก้ที่สเปคแล้วทั้ง 4 ข้อ** (+ ข้อ 5: ปลด §12 draft เรื่อง dummy prime ordering/negative `pos_m` เพราะบ้าน 04 มี `1'`→`1''''` ครบ 4 เส้นในช่องเดียว และ `1'`=-0.6 ก่อน origin จริง = มีเคสจริงแล้ว) พร้อมเพิ่มของที่กฎเดิมไม่มี:
- **เตือน footgun `sheet_code`** เข้าไปใน §4 ตรงๆ (ข้อ 26/31 เจอซ้ำ 2 บ้าน ควรอยู่ในสเปคไม่ใช่แค่ในบันทึกนี้)
- **ข้อยกเว้น dummy naming**: label ที่มะขามให้มาเองบันทึกตามนั้น ห้าม normalize (เคส `A'`(1.6)/`E'''`(4.2) บ้าน 04 ที่ไม่ตรงทั้งกฎใกล้สุดและกฎบน/ซ้าย)
- **กฎแยก merge vs de-dup** ใน §4: grid_refs ไม่ทับ = merge บวก count / ทับ = บั๊กนับซ้ำ ต้องลบ ห้ามบวก
- **ข้อยกเว้นตาราง multi-level** ใน §4: ตารางเสาหลาย level ต้องซ้ำ element_id ตาม §8 ห้าม merge

บันทึกใน `primary_rawjson_schema_edit_log.md` แล้วตามกฎข้อ 7 ของ `rule_of_tune.md` พร้อมระบุเหตุผลการยกเว้น Rule 1/Rule 2 (เป็นไฟล์ `.md` + ไม่ได้เปลี่ยนโครงสร้าง schema ใหม่ แค่ให้สเปคตามทันข้อมูลที่ apply ไปแล้ว)

**ปิดคำถามค้างที่ค้างมานานด้วย:** ข้อ 17.1 ("no-dash เป็นกฎทั้งโปรเจกต์หรือแค่บ้าน 1?") และ ข้อ 17.2 ("เสาไม่ใช้ top/bottom ควรเป็นกฎทั่วไปไหม?") — ทั้งคู่ตอบแล้วว่า **ใช่ ทั้งโปรเจกต์** และตอนนี้อยู่ในสเปคเรียบร้อย ไม่ต้องถามซ้ำอีก

---

## 60. บ้าน 01-11: ตรวจ+ทำ pattern=plan / footing plan / grid master / section ให้เป็นรูปแบบเดียวกัน (2026-08-02)

**คำสั่ง:** "บ้าน 01-11 เช็คให้ pattern plan และ footing plan and grid master and section มีรูปแบบ json เดียวกัน"

**สิ่งแรกที่ต้องรู้: บ้าน 06-11 ไม่เคยมีใน `json_แก้ไขแล้ว/` มาก่อน** — copy สดจาก raw เข้ามาก่อนเริ่ม (571 ไฟล์) ตามขั้นตอนเดิมของทุกบ้าน (ข้อ 18/34) **`rawjson_ยังไม่ได้แก้ไขโดนคน/` ไม่ถูกแตะเลยแม้แต่ไฟล์เดียว** — ที่แก้ในนั้นมีแค่ `.md` 2 ไฟล์ (สเปค+edit log) ซึ่งกฎข้อ 1 ยกเว้นให้อยู่แล้ว

**สแกนครบ 1,183 ไฟล์** พบว่าไม่สม่ำเสมอหนักกว่าที่เคยรู้ **แก้ไปแล้ว 4 เรื่อง ครบทั้ง 11 บ้าน:**

### (ก) pattern นอกสเปค — บ้าน 07 เป็นบ้านเดียวที่ผิด (21 ไฟล์)
บ้าน 07 ประดิษฐ์ pattern เองนอก 14 ค่า: `detail`×13, `elevation`×4, `diagram`×2 และเอา site plan/roof plan ไปกองรวมเป็น `plan` เฉยๆ อีก 2 — อีก 10 บ้านสะอาดหมด **แก้ตามเสียงข้างมาก 10:1**
- `elevation` → `side_profile` (10 บ้านใช้ค่านี้กับรูปด้าน)
- `detail` → `section` (บ้าน 01-05 ลง `fixture_riser_details`/`tread_riser_detail` เป็น `section` หมด)
- `diagram` → `side_profile` — **ตัดสินจากหลักฐานตรง ไม่ได้เดา**: `sheet_name` ของบ้าน 07 คือ `ไดอะแกรมระบบสุขาภิบาลแนวดิ่ง` **ตรงกับบ้าน 05 ตัวอักษรต่อตัวอักษร** และบ้าน 03/04/05 ลงเป็น `side_profile` ทั้งสามหลัง
- `plan` → `site_plan` / `roof_plan` สำหรับ 2 ไฟล์ที่ชื่อบอกอยู่แล้ว

### (ข) grid master ซ้อนไม่เหมือนกัน — แก้ 7 ไฟล์
บ้าน 01-05 (ที่รีวิวแล้ว) เก็บ `x_lines`/`y_lines` ซ้อนใน `grid{}`, บ้าน 06-11 วางไว้ระดับบนสุด **nest ให้ 06-11 ตามแบบที่รีวิวแล้ว** (ข้อ 35 ระบุว่าเป็นการตัดสินใจตั้งใจ) — ครบ 12 ไฟล์ (บ้าน 06 มี 2 อาคาร) ข้อมูลไม่หายสักเส้น

### (ค) footing: กฎ item 48 ไปไม่ถึงบ้าน 06-11 — แก้ 11 ไฟล์
บ้าน 01-05 ทำแล้ว (ID ซ้ำ 0), บ้าน 06-10 ยังใช้ `grid_ref` เดี่ยวไม่มี `count`, บ้าน 11 มี array แต่ไม่ merge **รวมให้เหลือ 1 entry ต่อ mark + strip ขีดของ point-ref ไปพร้อมกัน** (`D-1`→`D1`)

| บ้าน | ก่อน→หลัง (element) |
|---|---|
| 06 | 33→4, 6→2 |
| 07 | 18→11 |
| 08 | 12→2, 12→2 |
| 09 | 20→6, 20→6 |
| 10 | 20→5, 20→5 |
| 11 | 15→10, 15→9 |

**merge แบบอนุรักษ์นิยม — รวมเฉพาะเมื่อฟิลด์ที่ไม่ใช่ตำแหน่งเหมือนกันเป๊ะ ไม่แตะคาน/span element เลย** (กฎ atomic ยังอยู่)

**⚠️ เหลือ 3 ไฟล์ที่ mark ยังซ้ำ และ "ตั้งใจไม่รวม" ไม่ใช่ลืม:**
- บ้าน 06 `F2`: 2 entry มี confidence ต่างกัน (0.88 vs 0.75) และตัวหลัง flag `inferred_from_repeating_module` — รวมแล้ว**ข้อมูลว่า 2 ตำแหน่งนี้เดาจาก pattern ไม่ได้อ่านจากแบบ จะหายทันที**
- บ้าน 07 `F1`: entry หนึ่งมี `note` อีกอันมี `offset_note` เป็นการสังเกตคนละตำแหน่งจริง
- บ้าน 09 `F0.8x0.8`: 4 entry แต่ละอันมี note รายตำแหน่งของตัวเอง
→ ต้องให้คนตัดสินว่าจะยุบ note ยังไง **ห้ามให้สคริปต์ตัดสินแทน** เพราะนี่คือ failure mode ที่ข้อ 48 เตือนไว้เอง (นับซ้ำ/ทำข้อมูลหาย)

### (ง) ⚠️ บ้าน 10/11 เก็บ `main_bar`/`stirrup` เป็น **string ล้วน** — แก้ 65 ค่า
นี่คือข้อที่อันตรายที่สุดที่เจอรอบนี้ อีก 9 บ้านเก็บเป็น object ซ้อนตามสเปค §6 แต่บ้าน 10/11 เก็บเป็นข้อความ เช่น `"2-Ø16มม. top + 2-Ø16มม. bottom"`, `"Ø6มม.@0.125ม."`, `"Ø9มม. รัดโดยรอบ"` — โมเดลจะเรียนเป็น "ข้อความ" ไม่ใช่ "ตัวเลข" ทั้งที่ไฟล์อื่นสอนเป็นตัวเลข = สอนขัดกันเอง

แปลงเป็น object ตามสเปค §6 ครบ **65/65 ค่า ไม่มีตกค้าง** (main_bar 21, stirrup 36, tie 8) โดย:
- **เก็บสตริงเดิมไว้ครบทุกตัวใน `printed_as`** (ตาม convention §6a) — ไม่ทำลายของเดิม ย้อนตรวจกับแบบได้เสมอ
- `Ø` → `type: "RB"` ตามกฎสเปค §6 (ห้ามเดาจากขนาด)
- `@0.15ม.` → `spacing_mm: 150`
- หมายเหตุไทยในวงเล็บ (`ล้วงเข้า B2`, `ต่อเนื่องจาก B2`) ย้ายเข้า `note` ไม่ทิ้ง
- `tie`/`tie_bar` → เปลี่ยนชื่อเป็น `stirrup` ให้ตรงกับอีก 9 บ้าน
- **⚠️ เหล็กที่พิมพ์ว่า `หยุดที่ L/8`/mid-span ยังไม่ merge เข้า face** — ต้องซูมดู leader line ตามกฎ §7 ซึ่งบ้านนี้ยังไม่เคยทำ ปักธงไว้ในไฟล์แล้ว
- **บั๊กของผมเองที่เจอตอนตรวจผล**: ตัว parser ดูดหน่วย `มม.` ติดเข้าไปใน `note` (`"มม. รัดโดยรอบ"` แทนที่จะเป็น `"รัดโดยรอบ"`) — แก้แล้ว 6 จุด

### ผลลัพธ์รวม — scorecard ทั้ง 11 บ้าน
| เรื่อง | ผล |
|---|---|
| JSON parse ได้ | **1,183/1,183** |
| pattern อยู่ใน 14 ค่าของสเปค | **ผ่านครบ 11 บ้าน** |
| grid master ซ้อนใน `grid{}` | **ผ่านครบ 12 ไฟล์** |
| footing 1 entry ต่อ mark | ผ่าน 17/20 ไฟล์ (3 ไฟล์ตั้งใจไม่รวม ดูข้างบน) |
| rebar เป็น object ไม่ใช่ string | **ผ่านครบ 11 บ้าน** |

### ⚠️ ที่ยังไม่ได้แก้ (ตั้งใจ ไม่ใช่ลืม — ต้องอ่านแบบจริง ห้ามให้สคริปต์เดา)
1. **บ้าน 10/11 ไฟล์ `pattern=section` ไม่มี `elements[]` เลย** — ตั้งชื่อ array เองรายไฟล์ (`beams`/`columns`/`slabs`/`footing_types`/`details`/`sections`/`fixture_symbol_legend`) และรูปร่าง element ก็คนละแบบ (`sections[0]` มี `name`/`cut_ref`/`scale` ไม่มี `element_id`/`element_type`) — เปลี่ยนชื่อ array เฉยๆ ไม่พอ ต้องแมป field ทีละตัวเทียบกับแบบ
2. **`specs{}` ยังไม่ทั่วถึง** — ใช้แค่ 24/1,183 ไฟล์ (บ้าน 06×4, 08×8, 09×11, 10×1) บ้าน 07/11 ยังไม่มีเลย ต้อง join จากไฟล์ section ของแต่ละบ้านก่อน
3. **ชื่อ/หน่วยฟิลด์ยังแตก** — `width_mm`/`width_m`, `height_mm`/`height_m`/`depth_m`/`thickness_mm`/`section_mm` **แต่ตรวจแล้วว่าค่าไม่ผิดหน่วย**: สแกนทุกฟิลด์ `_mm` ที่ค่า <5 (83 ตัว = เหล็กบางจริง 3.2มม./4มม.) และทุกฟิลด์ `_m` ที่ค่า >50 (**0 ตัว**) — เป็นปัญหาการตั้งชื่อ ไม่ใช่ข้อมูลเสีย
4. **เสาหลายชั้น**: บ้าน 04/05 ใช้ `levels[]` ซ้อน, บ้าน 01/02/03 ใช้แถวแบน + `level` ตาม §8 (§8 คือฝั่งที่ถูก)
5. **`phase_note` ค้าง 5 ไฟล์** (บ้าน 09×3, 10×2) — ตาม §2a ไฟล์ที่ยังมีฟิลด์นี้ = ยังไม่เสร็จ
6. **beam plan ของบ้าน 06-11 น่าจะมีคานหายเหมือนบ้าน 01-05** (ตอนรีวิวบ้าน 01-05 เติมคานที่หายไป 20→27, 22→36, 30→38, 5→21) — ยังไม่ได้ตรวจสักหลัง ต้องใช้กฎ beam-endpoint + ซูมภาพจริง
7. **บ้าน 05 เติม suffix ใส่ `element_id`** (`F1.30x1.30_E1`) ทำให้ join ข้ามหน้าพัง — ยังไม่แก้
8. **sync กลับ `rawjson_ยังไม่ได้แก้ไขโดนคน/` ยังไม่ทำสักบ้าน** (ค้างมาตั้งแต่บ้าน 1) — ต้องทำตามกฎข้อ 1-3 เต็มรูปแบบ

---

## 61. Apply สิ่งที่ต้องแก้.md ให้ครบบ้าน 01-11 — รอบที่ 2 (2026-08-02)

**คำสั่ง:** "apply สิ่งที่ต้องแก้.md กับบ้าน 01-11 ให้เรียบร้อย ทุกหลัง format json เดียวกัน ยึดสิ่งที่ต้องแก้.md เป็นหลัก สงสัยไม่มั่นใจให้ถาม"

**แจ้ง Rule 2 + ถามก่อนลงมือ 3 ข้อ** (งานนี้เปลี่ยนโครงสร้าง schema จริง ไม่ใช่แค่จัดรูปแบบ) มะขามตอบ: แปลงบ้าน 10/11 **เต็มรูปแบบ**, phase_note **ย้ายเข้า warnings แล้วลบ**, element_id **ตัด underscore ทั้งหมด**

### (ก) บ้าน 10/11: พับ array เฉพาะกิจเข้า `elements[]` — 30 ไฟล์
สองบ้านนี้ไม่มี `elements[]` เลย ตั้งชื่อ array เองรายไฟล์ อีก 9 บ้านใช้ `elements[]` หมด

| array เดิม | → element_type | หมายเหตุ |
|---|---|---|
| `beams` | `beam` | `mark`→`element_id` |
| `column_sections` | **`beam`** | ⚠️ **ชื่อ array ตั้งผิดมาแต่แรก** — cut_ref อ้าง B1/B2/B3/R1/RB0/B4/B5/B6X **ทุกตัวเป็นคาน ไม่มีเสาสักตัว** ตรวจครบ 17 รายการ เก็บ label หน้าตัดไว้ใน `section_ref` |
| `columns` | `column` | |
| `footing_types` | `footing` | |
| `slabs` | `slab` | |
| `structural_elements` | จาก field `type` ไทย | เสา→column, คาน→beam |
| `details` / `sections` | `detail_view` / `section_view` | ใช้ `name` เป็น id |
| `fixture_symbol_legend`, `fixture_install_height_standard` | **ไม่แปลง** | เป็นตาราง legend/อ้างอิง ไม่ใช่ element จริง |

พร้อมกัน: `size_m:"0.20x0.40"`→`width_mm`+`height_mm`, `thickness_m`→`thickness_mm`, `rebar`/`top`/`bottom` ที่เป็น string→object — **เก็บของเดิมไว้ใน `*_printed_as` ทุกตัว**

**`confidence_score` ตั้งเป็น `null` ไม่ได้กุตัวเลขขึ้นมา** — บ้านนี้ไม่เคยบันทึกไว้ การใส่ตัวเลขเองคือแต่งข้อมูลคุณภาพ (ผิด rule_of_tune บทเรียนข้อ 4)

**ตรวจไม่มีข้อมูลหาย เทียบกับ raw ที่ไม่ถูกแตะ**: บ้าน 10 = 21 ไฟล์/**69 entry** ทั้งก่อนและหลัง, บ้าน 11 = 17 ไฟล์/**74 entry** ทั้งก่อนและหลัง

### (ข) บ้าน 05: `levels[]` ซ้อน → แถวแบนตาม §8 (20 แถว)
บ้าน 05 เป็นบ้านเดียวที่เอาหลาย level ยัดใน element เดียว §8 ห้ามไว้ตรงๆ อีก 5 บ้าน (01/02/03/04/07) ทำแบบแบนอยู่แล้ว — แตกเป็น 1 แถวต่อ (mark, level) + `section_mm`→`width_mm`/`height_mm` + `tie`→`stirrup`

### (ค) phase_note → warnings[] แล้วลบ field (5 ไฟล์ 7 note)
ตาม §2a "ไฟล์ที่ยังมี phase_note = ยังไม่เสร็จ" — **เนื้อหาไม่ได้ถูก resolve** (ต้องเปิดแบบถึงจะตอบได้ เช่น "เสาที่ C-1/C-4 เป็น C1 บนแผ่นนี้แต่ C1A บน S-03") เก็บข้อความเดิมครบใน warnings[] ซึ่งกฎห้ามลบ

### (ง) ⚠️ element_id ตัด underscore — ทำแล้ว **แต่ต้องย้อน 37 จุด เพราะจะทำข้อมูลหาย**
ตัดสำเร็จ **82 id** (เช่น `F1.30x1.30_E1`→`F1.30x1.30`, `CN_joint_detail`→`CN`, `B0_section_0-0`→`B0`) เก็บของเดิมใน `element_id_printed_as` ทุกตัว

**แต่ตรวจแล้วพบว่า 17 จุดทำให้ id ชนกัน = ของคนละอย่างกลายเป็นตัวเดียวกัน:**

| ชนกันเป็น | ของเดิมที่จะหาย |
|---|---|
| `ST1` | `บันได_ST1` (ตัวบันได) กับ `steps_ST1` (ลูกตั้งลูกนอน) |
| `B1` | `B1` (คานปกติ) กับ `B1_ชพ`/`B1_ชานพัก` (**คานชานพัก คนละตัวจริงๆ**) |
| `R1` | `flashing_R1`, `หลังคาเหล็กรีดลอน_R1`, `เชิงชายไม้ไฟเบอร์ซีเมนต์_R1` (3 วัสดุที่ชายคาเดียวกัน) |
| `C150x75x25` | `rafter_...`, `..._overhang_east`, `..._overhang_west` (3 จันทันคนละตัว) |

**ย้อนกลับ 37 id ใน 5 บ้าน** เหลือ suffix ไว้พร้อม flag อธิบายว่าทำไมไม่ตัด — **ผลตรวจสุดท้าย: ชนกัน 0 จุด**

อีก **39 id ไม่มีมาร์คให้ตัด** (`pile_under_F`, `ครีบ_คสล_รอบอาคาร`, `fence1_*`, `C_unmarked`) = เป็นชื่อบรรยายของสิ่งที่แบบไม่เคยตีมาร์คไว้ ปล่อยไว้ + flag ไม่เดาแทน
**บทเรียน:** ตอนแรกผมยิงกว้างไปโดน 1,059 id (dimension/room/window ที่ชื่อบรรยายถูกอยู่แล้ว) ใส่ warning รก 205 ไฟล์ — ลบออกแล้ว เหลือ warning เฉพาะ element โครงสร้างที่กระทบการ join จริง

### Scorecard สุดท้าย — ทั้ง 11 บ้าน
| ข้อ | ผล |
|---|---|
| JSON parse | **1,183 / 1,183** |
| pattern อยู่ใน 14 ค่า | ✅ ผ่าน |
| grid master ซ้อนใน `grid{}` | ✅ ผ่าน |
| rebar เป็น object ไม่ใช่ string | ✅ ผ่าน (รวม `top`/`bottom` ที่รอบแรกตกหล่น) |
| `pattern=section` ใช้ `elements[]` | ✅ ผ่าน (เหลือ 2 ไฟล์ที่**ตั้งใจ**ไม่แปลง = legend) |
| ไม่มี `phase_note` ค้าง | ✅ ผ่าน |
| เสาแบนตาม §8 | ✅ ผ่าน |
| element_id ไม่ชนกัน | ✅ ผ่าน (0 จุด) |
| footing 1 entry ต่อ mark | 12/20 ไฟล์ — อีก 8 ไฟล์**ตั้งใจไม่รวม** (ดูล่าง) |

**8 ไฟล์ที่ยังมี mark ซ้ำ — ตั้งใจทั้งหมด ไม่ใช่ลืม** (บ้าน 06/07/09/10/11): ทุกคู่ต่างกันที่ `confidence_score` หรือ `note` รายตำแหน่งจริง เช่น บ้าน 06 `F2` มี 2 entry ที่ 0.88 กับ 0.75 และตัวหลัง flag `inferred_from_repeating_module` — รวมแล้ว**ข้อมูลว่าตำแหน่งไหนเดา ตำแหน่งไหนอ่านจากแบบ จะหายทันที** ต้องให้คนดูแบบตัดสิน ห้ามให้สคริปต์ตัดสิน (เป็น failure mode ที่ข้อ 48 เตือนไว้เอง)

### ⚠️ ยังเหลือ — ต้องเปิดแบบจริง สคริปต์ทำแทนไม่ได้
1. **`specs{}` ยังไม่ทั่วถึง** (24/1,183 ไฟล์) — ต้อง join จากไฟล์ section ของแต่ละบ้านก่อน
2. **beam plan บ้าน 06-11 ยังไม่เคยตรวจว่าคานหาย** — บ้าน 01-05 ตอนตรวจเจอคานหายทุกหลัง (20→27, 22→36, 30→38, 5→21) คาดว่าบ้าน 06-11 พอกัน
3. **เหล็กที่พิมพ์ `หยุดที่ L/8`/mid-span ในบ้าน 10/11 ยังไม่ merge เข้า face** — ต้องซูมดู leader line ตาม §7
4. **ชื่อ/หน่วยฟิลด์ยังแตก** (`width_mm`/`width_m`/`depth_m`) — แต่ยืนยันแล้วว่า**ค่าไม่ผิดหน่วย** (`_m` ที่ค่า >50 = 0 ตัว)
5. **sync กลับ `rawjson_ยังไม่ได้แก้ไขโดนคน/` ยังไม่ทำสักบ้าน** — ต้องทำตามกฎข้อ 1-3 เต็มรูปแบบ

**`rawjson_ยังไม่ได้แก้ไขโดนคน/` ไม่ถูกแตะเลยแม้แต่ไฟล์ JSON เดียวตลอดงานนี้**

---

## 62. สัญกรณ์ `grid_ref` ให้เหมือนกันทุกบ้าน — ปิดเรื่อง "เดี๋ยว C1 เดี๋ยว c-1" (2026-08-02)

**คำสั่ง:** "หน้าที่มี pattern เดียวกันต้องมีรูปแบบเดียวกัน ไม่เอาเดี๋ยว C1 เดี๋ยว c-1 ยึดบ้าน 01-05 เป็นหลัก"

**สำรวจก่อนพบว่ารอบที่แล้วผมแก้ไม่ครบ** — ตอนนั้นแก้แต่ไฟล์ที่ชื่อมีคำว่า `footing` เท่านั้น ไฟล์ plan อื่นของบ้าน 06-11 ยังเป็น `C-1` อยู่ทั้งหมด

| รูปแบบ | ก่อน | หลัง |
|---|---|---|
| point มีขีด (`C-1`, `ค-1`) | บ้าน 06=163, 07=123, 08=98, 09=162, 10=214, 11=49 | **0** |
| คำว่า `grid`/`dummy` ปนใน value | บ้าน 05=127, 04=49, 03=23, 01=10, 08/09/11=17 | **0** |
| ตัวพิมพ์เล็ก | บ้าน 05=4 | **0** |

**แก้ 809 point ref + 264 ค่าที่มีคำว่า grid + 2 ค่าที่ใช้คำว่า "gridline"** รวม 87 ไฟล์

**ข้อสังเกตที่ขัดกับความคาดหมาย:** คำสั่งบอกให้ยึดบ้าน 01-05 แต่เรื่องคำว่า `grid` ปนใน value **บ้าน 01-05 คือฝ่ายที่แย่ที่สุด** (บ้าน 05 มี 127 ค่า) ส่วนบ้าน 06 มี **0** มาแต่แรก — จึงยึด*กฎ*ที่บ้าน 01-05 ตั้งไว้ (ข้อ 5) ไม่ใช่ยึด*สภาพไฟล์*ของบ้าน 01-05 เพราะกฎถูกแต่ไม่เคยถูก apply ครบ

**ความปลอดภัย:** แตะเฉพาะ value ใต้ key `grid_ref`/`grid_ref_start`/`grid_ref_end`/`grid_refs` เท่านั้น จึง**ไม่มีทางโดน `sheet_code`** (footgun ข้อ 26/31) โดยโครงสร้างของสคริปต์เอง ไม่ใช่แค่ระวังเอา

**ตรวจคุณภาพจริง ไม่ใช่แค่นับ:** resolve **point ref ทั้ง 2,007 ตัวของทั้ง 11 บ้านกับ grid master ของบ้านนั้นๆ — ไม่มี dangling สักตัว (0)** แปลว่าที่แก้ไปไม่ได้แค่เปลี่ยนตัวอักษร แต่ยังชี้ไปยังเส้นกริดที่มีอยู่จริง

**ผลสุดท้าย: non-conforming grid_ref = 0 ทั้ง 11 บ้าน** · JSON 1,183/1,183 parse ผ่าน

---

## 63. ล็อกรูปแบบในสเปค + ทำ checker อัตโนมัติ — เตรียมรับ op1 บ้าน 1000 หลัง (2026-08-02)

**คำสั่ง:** "วันนี้เช็คแค่รูปแบบเท่านั้น เช็คใน Primary rawjson ด้วย ทำให้เหมือนกันต่อจากนี้ ทำ op01 บ้าน 1000 หลัง รูปแบบ json ต้องเหมือนกันหมด"

**ปัญหาที่แท้จริง:** สเปคเดิมบอกวิธีเขียน*แต่ละฟิลด์* แต่**ไม่เคยล็อก*รูปทรงของไฟล์*** — บ้านจึงตั้งชื่อ array เอง / เก็บ rebar เป็น string / ใส่ขนาดเป็น `"0.20x0.40"` / สะกดปลอกว่า `tie_bar` **ได้โดยไม่ผิดกฎข้อไหนเลย** ถ้าไม่ปิดช่องนี้ บ้านที่ 12-1000 จะ drift ซ้ำรอยเดิม

### เพิ่ม §0 FORMAT LOCK ไว้หัวสเปค (10 หัวข้อย่อย)
`0.1` container ห้ามตั้งชื่อ array ตามชนิด element · `0.2` ทุก element ต้องมี 4 ฟิลด์ (confidence เป็น `null` ได้ **ห้ามกุตัวเลข**) · `0.3` `element_id` = มาร์คที่พิมพ์ ห้ามมี suffix ตำแหน่ง/ระดับ/หน้าตัด และห้ามชนกัน · `0.4` **canonical `element_type` 42 ค่า** · `0.5` ขนาดเป็น mm จำนวนเต็ม · `0.6` rebar ต้องเป็น object + `stirrup` เป็นชื่อเดียว · `0.7` `printed_as` · `0.8` ตาราง `grid_ref` · `0.9` `pattern` ห้ามคิดใหม่ · `0.10` checklist ปิดงาน

**ตัวเลขที่ทำให้ต้องมี §0.4: `element_type` งอกเป็น 359 ค่า จาก 4,507 element** — 34 ค่าหลัก + หางยาว 325 ค่าที่ใช้ครั้งสองครั้ง

### ⚠️ §0.1 ฉบับแรกของผมกว้างเกินไป ต้องแก้เอง
เขียนห้าม array ชื่อ `sections`/`columns`/`details` ทั้งหมด — checker แจ้ง **106 ไฟล์ผิด** แต่พอเปิดดูเนื้อจริง **104 ไฟล์เป็นการใช้ที่ถูกต้อง**:

| array | อยู่ใน | จริงๆ เก็บอะไร |
|---|---|---|
| `sections` | `index` | หมวดเอกสาร (`แบบสถาปัตยกรรม`, `A-01 ถึง A-15`) |
| `sections` | `notes` | หัวข้อหมายเหตุ (`1. ข้อกำหนดทั่วไป`) |
| `columns` | `schedule`/`material_list` | **หัวคอลัมน์ตาราง** (`"ลำดับที่"`) |

**แก้สเปคกับ checker ไม่ใช่แก้ข้อมูล** — เปลี่ยนกฎเป็น "ดูว่า array เก็บอะไร ไม่ใช่ดูชื่อ" (มี `element_id`/`element_type` = เป็น element / เป็นโครงสร้างเอกสารหรือหัวตาราง = ปล่อยไว้) พร้อมตารางข้อยกเว้น **เหลือของจริงแค่ 2 ไฟล์** (บ้าน 10/11 `pattern=side_profile` ที่ `sections` เก็บรูปตัดจริง) พับเข้า `elements[]` แล้ว

### ทำ `tools/check_format.py` — รันเช็ค 14 ข้ออัตโนมัติ
1000 หลังไล่มือไม่ไหว · `python tools/check_format.py` เช็คทั้งชุด, `python tools/check_format.py <folder>` เช็คหลังเดียว, **exit 1 เมื่อไม่ผ่าน** (เอาไป gate op1 ได้)

**checker จับของค้างที่ scan มือของผมพลาดไป 3 กลุ่ม:**
- **packed size string 113 จุด** (`section_mm:"200x200"`, `size_m:"0.20x0.40"`, `footing_size_m`, `plan_size_m`) → `width_mm`+`height_mm` เก็บของเดิมใน `*_printed_as`
- **`tie`/`tie_bar` 22 จุด** → `stirrup` (รอบก่อนผมแก้แค่บ้าน 10/11 กับบ้าน 05)
- **`warnings` หายไป 6 ไฟล์** → เติม `[]`

**และ checker ฉบับแรกก็เช็คแคบไปเอง** — ตอนแรกตัดสินว่า footing 4 กลุ่มควร merge เพราะดูแค่ `confidence_score`/`note` แต่จริงๆ แต่ละ entry มี **`confidence_flags` คนละเหตุผล** (`dummy_grid_A'_position_unresolved` vs `dummy_grid_D'_x-position_approximate`) = provenance รายตำแหน่งจริง แก้ checker ให้นับ `confidence_flags` ด้วย

**บ้าน 10 footing 2 ไฟล์**: entry ต่างกันแค่ `column_mark` (C1 vs C1A) ซึ่ง**ข้อ 48 บอกชัดว่าไม่ใช่เหตุผลให้แยก** — merge แล้ว**แยกเสาออกเป็น element ของตัวเองตามแบบข้อ 53** เพื่อไม่ให้ข้อมูลเสาหาย (5→6 element/ไฟล์)

### ผล
```
python tools/check_format.py   →   ALL CHECKS PASS (exit 0)
```
ครบ 14 ข้อ ทั้ง 11 บ้าน · `json_แก้ไขแล้ว/` กับ `rawjson_ยังไม่ได้แก้ไขโดนคน/` ต่างกัน **0 ไฟล์**

**11 กลุ่มที่ checker รายงานเป็น NOTE (ไม่ใช่ FAIL)** = footing ที่ตั้งใจไม่ merge เพราะ provenance รายตำแหน่งต่างกันจริง — เขียนเป็นข้อยกเว้นไว้ใน §0.10 แล้ว รอบหน้าจะได้ไม่มีใครมา "แก้" ให้ผิด

---

## 64. ตรวจอิสระรอบ fable — "ทุกอย่างเป็น pattern เดียวกันไหม?" คำตอบตอนเริ่ม: ยัง / คำตอบตอนจบ: ใช่ (2026-08-02)

**คำสั่ง:** มะขามเปิด fable แล้วสั่ง "ไปเช็คมาซะว่าทุกอย่างเป็น pattern เดียวกันไหม" — จงใจตรวจแบบอิสระ ไม่พึ่ง `check_format.py` เดิม (มันเช็คได้แค่กฎที่มันรู้จัก) ใช้วิธีไล่เทียบ**ชนิดข้อมูล (JSON type) ของ key เดียวกันข้ามทุกไฟล์ทุกบ้าน**

**ผลดิบ: 106 key ปนชนิด** — คัดแล้วส่วนใหญ่เป็นบริบทต่างกันที่ถูกอยู่แล้ว (เช่น `top` ตัวเลขในมิติบันได vs `top` object ใน main_bar) แต่มี **ของจริงบนฟิลด์โครงสร้าง ~20 กลุ่ม** ที่ checker เดิมมองไม่เห็น แก้หมดแล้วทั้งสองโฟลเดอร์:

### ของที่เจอและแก้ (เก็บของเดิมใน `*_printed_as`/flag ทุกจุด)
| เรื่อง | ขนาด | แก้เป็น |
|---|---|---|
| element ไม่มี `element_id` (บ้าน 01-07 — **08-11 ครบอยู่แล้ว** สวนทางสมมติฐาน "ยึด 01-05") | ~500 entry | id จากมาร์คพิมพ์ / item_no / descriptive สไตล์ 08-11 (`ceiling_fan_นอน1`) + flag `auto_id` ทุกตัว |
| BOQ บ้าน 04 เป็น `pattern=unknown` + `elements[]` | 10 ไฟล์ | `material_list` + `categories[].items[]` (qty→quantity) เหมือนบ้านอื่น |
| **ตารางราคาซีรีส์เดียวกันเก็บ 5 รูปแบบ** (02 `house_catalog` / 03 `house_series` / 04 `elements` / 05 `categories` / 08-10 `series_price_table`) | 5 บ้าน | รวมเป็น `series_price_table[]` (เสียงข้างมาก 08-10) — บ้าน 01 ยังเป็น raw_text ปักธง PENDING ไม่เดา |
| หน้าเครดิต pattern แตก (01 title / 02 unknown / 05,07 notes) | 4 ไฟล์ | `misc` ตามเสียงข้างมาก 5 บ้าน + กติกา item 13/27/32 |
| `slab_markers[]`/`reference_markers[]` (05), `door_window_schedule[]` (10/11) | 41 แถว | เข้า `elements[]` — ประตู/หน้าต่างใช้ id `D1`/`W1` สไตล์ 01-05 |
| `steel_section` เป็น string (บ้าน 01 โครงหลังคา) | 4 ค่า | object ตาม §6a (`C100x50x20mm` → designation C + d/b/lip) |
| `main_bar`/`rebar` เป็น **array** (บันได — เหล็กหลายชั้น) | 16 ค่า | ย้ายเข้า key ใหม่ `bar_layers[]` เพื่อให้ `main_bar` เป็น object เสมอ |
| `count:"4+4"` (ตะแกรงสองทาง), `dia_mm:"16+12"` (เหล็กผสม), `spacing_mm:[100,250]` | ~25 ค่า | sum+`count_printed_as` / `mixed_dia_mm` / smallest+`variable_spacing` |
| `level_m` เป็น string (`"+0.60"`, `"±0.00"`, มีวงเล็บอธิบาย) | ~170 ค่า | ตัวเลข + `level_m_printed_as` เมื่อมีข้อความ |
| `grid_ref` เป็น array (`['A','B','2','3']`, `['1-B',...]` กลับด้าน, `['1-2 @ D',...]`) | 30 ค่า | notation string/`grid_refs` ตาม §0.8 |
| `span_m` เป็น array (คานหลายช่วง) | 14 ค่า | `spans_m[]` (พหูพจน์) |

### ⚠️ เจอเกินรูปแบบ 1 เรื่อง — ราคาผิดเก่าโผล่กลับมา (อาการ item 55 ซ้ำรอบที่สอง)
**สำเนาตารางราคาบ้าน 05 (หน้า89) ยังถือเลขผิดชุดเก่า 5 ค่า** — เช่น แบบ 1 เข็ม 1,060,000 / แบบ 2 เข็ม 1,560,000 — ทั้งที่ **item 37 บันทึกว่า "แก้ครบทั้ง 5 สำเนา"** เป็น 1,020,000/1,460,000 (ยืนยัน 8x zoom เทียบ glyph) ตรวจบ้าน 03/04 แล้วถูกตาม consensus จริง มีแค่สำเนา 05 ที่หลุด — **กู้เป็นค่า consensus ของ item 37** (เป็นการ execute การตัดสินใจเดิมที่ verify แล้ว ไม่ใช่ตัดสินใหม่) เก็บเลขผิดใน `*_printed_as` + flag `RE-CORRECTED_per_item37` ทุกจุด · **บทเรียน item 55 ต้องอ่านใหม่: "log บอกแก้แล้ว" ต้อง verify รายสำเนา ไม่ใช่รายเรื่อง**

### เคสที่ตั้งใจ "ไม่ยุบ" — W1 บ้าน 10
แบบพิมพ์เลข **1** บนหน้าต่าง 2 บานที่ต่างกันจริง (บานหลัก + บาน variant) — เก็บเป็น `W1` กับ `W1_variant` พร้อม flag ตามกฎ §0.3 (ยุบ = ควบสองบานจริงเป็นบานเดียว)

### สเปค + checker อัปเดตตาม
§0.1 (array ต้องห้ามเพิ่ม + รูปเดียวของตารางราคา) · §0.2 (ขอบเขต + descriptive id) · §0.5 (เมตรต้องเป็นตัวเลข, `spans_m`, `4+4`, `16+12`) · §0.6 (`bar_layers[]`) · checker ขยาย 14→**19 ข้อ** (identity + type purity)

### ผลจบ
```
python tools/check_format.py  →  ALL 19 CHECKS PASS (ทั้ง 11 บ้าน)
parse 2,366/2,366 · สองโฟลเดอร์ต่างกัน 0 ไฟล์
```
**ค้างที่รู้ตัว:** บ้าน 01 หน้า60 ตารางราคายังเป็น raw_text (ปักธง PENDING_STRUCTURING — แปลงต้องอ่าน text blob ใหม่ = งานเนื้อหา) · งานเนื้อหาเดิมทั้งหมด (`specs{}`, คานหายบ้าน 06-11, เหล็ก L/8) ยังไม่แตะตามคำสั่ง "วันนี้เช็คแค่รูปแบบ"

---

### 59. บ้าน 41 (บ้าน_เล็ก_2ชั้น_17) — แถว dummy แกน y ผิดทั้งชุด: `A'` ย้าย 1.60 → **-1.10** + เพิ่ม `A''`=0.80, `A'''`=2.00

**มะขามชี้เอง (2026-08-24):** "ในแกน y จาก A ไป B มี dummy ระยะ 0.8 กับ 2.2 และข้างบน A มีระยะ 1.1 — นั่นคือ A'" แล้วย้ำอีกรอบว่า 0.8/2.2 มันอยู่ในกริดไลน์ ไม่ใช่ของนอกระบบ **ถูกทั้งสองข้อ**

ไฟล์เดิมเขียนไว้ผิด 2 ชั้น:
1. `A'` = 1.60 (จาก dimension `1.60` ที่ margin ซ้ายหน้า15) — แต่ 1.60 นั้นคือ**ขอบเส้นประของกันสาด/ที่จอดรถ** ไม่มีคานไม่มีเสา (เคส §4 "ห้ามสร้าง dummy ให้ขอบ slab")
2. warning เดิมเหมาเรียก tick `✕---ค่า` ทุกตัวใน margin ว่า "site/setback หรือ survey benchmark" แล้วโยนลง `unassigned_dimensions` — **ผิด** มันคือ dimension chain จริงที่ปิดพอดีกับกริดหลัก

**หลักฐาน (วัด pixel จาก PNG ต้นฉบับ ไม่ใช้สายตา):**
- หน้า16 margin ซ้ายพิมพ์ `1.10` (เหนือ A) · `0.80 + 2.20 = 3.00` (A-B) · `1.15 + 1.85 = 3.00` (B-C) · `0.40` (ใต้ C) · `2.00` (C-D)
- หน้า16 แถว A/B/C/D อยู่ที่ y = 768/1085/1404/1618 px → 105.7-107.0 px/m สม่ำเสมอ · tick ช่วง A-B อยู่ที่ y=853 = **A+0.80 พอดี**
- หน้า15 margin ซ้ายพิมพ์ `1.60` (เหนือ A) · `2.00 + 1.00 = 3.00` (A-B) · margin ขวาพิมพ์ `1.10` เหนือ A และ `0.80` ใต้ A ซ้ำอีกแผ่น

**ผลลัพธ์ — 3 แถว dummy ใหม่ เรียง prime จากบนลงล่างตาม §Dummy grid:**

| id | pos_m | คานที่ยืนยัน |
|---|---|---|
| `A'` | **-1.10** | คานขอบนอกส่วนยื่นบันได/ทางเข้าทิศเหนือ — B1(+3.65) หน้า16 (หน้าคานที่ y=652/673), B1(+0.40) หน้า15 (y=686/707) |
| `A''` | **0.80** | คานขอบในของส่วนยื่นเดียวกัน — B1(+3.65) หน้า16 (y=852/873), B1(+0.40) หน้า15 (y=886/907) |
| `A'''` | **2.00** | คานซอยภายในส่วนต่อเติมครัว B1(+0.35) หน้า15 (y=994/1015) — ตัวที่เคยผูกผิดไว้ที่ A'=1.60 |

`pos_m` ติดลบเพราะ A' อยู่เหนือแถว origin (กฎ origin) · ชื่อ `A'` ใช้ตามที่มะขามให้มาตรง

**ผลข้างเคียงที่เจอระหว่างทาง — คานแถว A ช่วง grid2-3 ไม่มีอยู่จริง:** scan แถว A ในช่วง grid2-grid3 เจอแต่เส้น dash-dot บาง ๆ (c≈130-135) ไม่ใช่เส้นคู่ของคาน ขณะที่ช่วง grid1-2 และ grid3-4 แถวเดียวกันเห็นหน้าคานคู่ชัด → entry `B1 A2-A3` ทั้งหน้า15 และหน้า16 **แยกเป็น 2 ตัวที่ A' กับ A''** (ช่องบันไดคร่อมแถว A ไม่ได้เริ่มที่แถว A)

**ไฟล์ที่แก้:** `หน้า00_gridline.json` (y_lines + 3 dimension_chains ใหม่ + unassigned_dimensions + warnings) · `หน้า15_plan_beam_floor1.json` (คาน A2-A3 → A'/A'' · A'1-A'2 → A'''1-A'''2 · S0 grid_refs · warning) · `หน้า16_plan_beam_floor2.json` (คาน A2-A3 → A'/A'' · S1 grid_refs · warnings) — parse ผ่านทั้ง 10 ไฟล์ในบ้าน

**ค้างไว้ให้มะขามตัดสิน (บันทึกใน warning "STILL OPEN" ของ gridline):** tick ที่เหลือยังไม่เลื่อนขั้นเป็น dummy — `B+1.15 = 4.15` (หน้า16, **มีคานจริง** หน้าคานที่ y=1189/1206 ช่วง grid2-3 = ขอบชานพักบันได น่าจะสมควรเป็น dummy) · `B+0.55 = 3.55` · `C-0.40 = 5.60` · `D-0.40 = 7.60` · `D+0.60 = 8.60` (หน้า15) · `C+0.40 = 6.40` (หน้า16 — scan ไม่เจอคาน) · `C+0.50 = 6.50` (หน้า16 margin ขวา ต่างจาก margin ซ้าย 0.10) · และ B4 แนวตั้ง 2 ตัวขนาบช่องบันไดหน้า16 ยังเป็น A2-B2/A3-B3 อยู่ ควรเริ่มที่ A' — ยังไม่แตะเพราะ run break อย่างเดียวชี้จุดต่อคานไม่ชัดพอ

**บทเรียน:** warning ที่เขียนว่า "ตรวจแล้ว ไม่ใช่ข้อมูลกริด" มีน้ำหนักพอ ๆ กับข้อมูลเอง — ถ้าสรุปผิด มันจะกันไม่ให้ใครกลับไปดูอีก รอบนี้ `0.80/2.20/1.15/1.85` ถูกพิมพ์ชัดในแบบตั้งแต่แรก และปิด chain ลงตัวพอดี (0.80+2.20=3.00) แต่โดนตีตราเป็น survey mark ไปทั้งชุด

**Addendum (2026-08-24) — เพิ่ม dummy `B'` = 4.15 (แกน y) ตามคำสั่งมะขามตรง**

มะขามดูภาพ crop แล้วยืนยัน "ควรมี" — เพิ่มเข้า `หน้า00_gridline.json` พร้อม chain `B→B' 1.15` / `B'→C 1.85` (ย้ายออกจาก `unassigned_dimensions`)

**ที่มา:** margin ซ้ายหน้า16 พิมพ์ `1.15 + 1.85 = 3.00` ปิดช่วง B-C พอดี · tick 1.15 (y=1207 px) ไปจบที่หน้าล่างของกล่อง `RB1'(+3.65)` ข้าง S1 +3.75 (ชานพักบันได) ช่วง grid2-3 หน้าคานวัดได้ y=1189-1191 / 1206-1208 · หน้า15 ไม่มีอะไรที่ 4.15 (scan ช่วง grid2-3 ที่ y=1242 ว่าง) → B' มาจากหน้า16 แผ่นเดียว

**ผลที่ได้ตรงตามเจตนา §4:** `RB1'` ในหน้า16 เคยเป็น `grid_ref: "~B2"` confidence 0.5 + flag "approximate position" เพราะไม่มีเส้นให้เกาะ → ตอนนี้เป็น `B'2` confidence 0.75 ระบุตำแหน่งได้แน่นอน

**⚠️ ปักธงไว้ — `B'` อาจต้องเปลี่ยนชื่อเป็น `B''` วันหลัง:** ระหว่างเช็คเจอว่า tick `0.55` ใต้ B บนหน้า15 (= 3.55) **ก็มีคานเหมือนกัน** — ไปจบที่หน้าล่างของคาน `B2(+0.40)` ช่วง grid2-3 (หน้าคาน y=1154-1157/1176-1178, แถว B อยู่ y=1121) คือคานตัวนี้บนชั้น 1 **เยื้องลงมาจากแถว B 0.31-0.55 m** ขณะที่คู่แฝดชั้น 2 บนหน้า16 อยู่กลางแถว B พอดี (y=1073-1075/1094-1096, B=1085) ยังไม่เลื่อนขั้น 3.55 เป็น dummy เพราะเรื่องเยื้องชั้น1/ชั้น2 ยังอธิบายไม่ได้ ต้องกลับไปอ่านใหม่จริงจัง ไม่ใช่เดา — แต่ถ้าวันไหนเลื่อนขั้น กฎเรียง prime บนลงล่างจะบังคับให้ 3.55 = `B'` และ 4.15 ต้องกลายเป็น `B''` (ซ้ำรอย 3'/3'' ใน item 1-2 เป๊ะ) เขียน warning เตือนไว้แล้วทั้งใน gridline และหน้า16

**Addendum 2 (2026-08-24) — ตรวจคานทุกตัวหน้า15/หน้า16 ด้วยการวัด pixel: ผิดตำแหน่ง 4 + คานหาย 3 + `B'`→`B''`**

มะขามสั่ง "คาน B4 หน้า16 ต้องยาว 4.10 m" แล้วให้ไล่เช็คคานทุกตัวว่ามีตัวไหนไม่มั่นใจอีก — ผลคือเจอของผิดเพิ่มอีกชุด **ทั้งหมดอยู่หน้า15 แผ่นเดียว** (หน้า16 จบแล้ว)

**วิธีวัด:** เขียน scanner 2 ตัว (LockBits, ไม่ใช้ GetPixel) — `scan.ps1` กวาดหาเส้นที่ coverage ≥ 0.75 ในช่วงที่กำหนด, `runs.ps1` ไล่ run ต่อเนื่องตามแนวเส้น · แยก "เส้นคู่ = คาน" ออกจาก "เส้นเดี่ยว coverage ~0.78 = dash-dot กริด" ได้ชัด · pin grid ก่อนทุกครั้ง: หน้า15 x = 1076/1386/1615/1925 (310/229/310 px ÷ 3.00/2.20/3.00 = 103.3/104.1/103.3 px/m)

**คาน B4/B3 ที่ขนาบส่วนยื่นบันได — เริ่มที่ A' ไม่ใช่ A (ตามที่มะขามบอก):** span 3.00 → **4.10** ทั้ง 4 ตัว (หน้า16 มาร์ค B4 ×2, หน้า15 มาร์ค B3 ×2) · ยืนยันโดยไล่ตาม**หน้านอก**ของคาน (หน้าในถูกคานขวางตัดเป็นท่อน ใช้ไม่ได้ — บทเรียนจากรอบแรกที่ผมไม่กล้าฟันธงเพราะดูหน้าใน): grid2 x=1384 วิ่งไม่ขาด y=652..1416, grid3 x=1638 วิ่ง y=653..1500 · grid1 (x=1072/1093) กับ grid4 (x=1927/1948) เริ่มที่ y=758 = แถว A จริง → **เฉพาะ 2 เส้นที่ขนาบส่วนยื่นเท่านั้นที่ขึ้นถึง A'**

**ผิดตำแหน่ง 4 ตัว (หน้า15):**

| เดิม | แก้เป็น | หลักฐาน |
|---|---|---|
| `B1` B2→B3 (+0.40) | **`B2` B'2→B'3** (+0.40) 2.20 | แถว B ทั้ง grid1-2 และ grid2-3 มีแต่เส้นเดี่ยว (0.797/0.781) ไม่มีคาน · ตัวจริงอยู่ที่ 3.55 หน้าคาน y=1154-1158/1174-1180 · zoom 9x อ่าน label ได้ **B2** ไม่ใช่ B1 |
| `B2` C1→C2 (+2.10) 3.00 | **C2→C3** 2.20 | label `B2(+2.10)` พิมพ์อยู่ระหว่าง grid 2-3 · grid1-2 แถว C ไม่มีคานเลย · ตรงกับหน้า16 C2-C3 (+2.10) พอดี |
| `B1` ~C1→~C2 south | **D3→D4** 3.00 | ลงกริดพอดี ไม่ต้องใช้ `~` |
| `RB1'` ~C1/~C2 south | **D3 / D4** | conf 0.45 → 0.75 |

**คานหาย 3 ตัว (หน้า15) — เพิ่มแล้ว:** `B1` B3→B4 (+0.40) 3.00 · `B1` C3→D3 (+0.30) 2.00 · `B1` C4→D4 (+0.30) 2.00

**⚠️ warning เดิมของหน้า15 เขียนกลับด้านกับแบบ 2 ข้อ — เขียนใหม่หมด:**
1. *"Row B (between grid 1-2 and grid 3-4) prints NO beam line at all... in contrast to the clearly double-lined B1(+0.40) for the grid 2-3 segment"* — **สลับ bay กันหมด** ของจริงคือ grid3-4 ต่างหากที่มีคาน ส่วน grid2-3 ไม่มี
2. *"South recessed room... between grid 1-2... has NO matching footing on หน้า14 at its south corners, and no printed dimension gives its south projection"* — **ผิดทั้ง 3 ข้อ** มันอยู่ grid**3-4** × C-D ลงกริดพอดี, แถว D เป็นกริดหลักที่มี dimension 2.00 พิมพ์อยู่, และ[หน้า14](41บ้าน_เล็ก_2ชั้น_17/บ้าน_เล็ก_2ชั้น_17_หน้า14_plan_footing.json) มี F1A ที่ **D3 และ D4** ตรงมุมกล่องเป๊ะ

**`B'`(4.15) → `B''`(4.15) + เพิ่ม `B'`=3.55:** พอยืนยันว่า 3.55 มีคานจริง (มาร์ค B2) กฎเรียง prime บนลงล่างบังคับให้สลับ — ซ้ำรอย 3'/3'' ใน item 1-2 ตรงตามที่ปักธงเตือนไว้ในรอบก่อนหน้าเดียวกัน · cascade เข้า `RB1'` หน้า16 เป็น `B''2`

**เรื่องที่บันทึกไว้เป็นความรู้ใหม่ของบ้านนี้:** คาน B'(3.55) เยื้องจากแถว B จริง ๆ และเป็นความต่างชั้น1/ชั้น2 ของจริง — ชั้น1 เยื้องลง 0.31-0.55 m ส่วนชั้น2 อยู่กลางแถว B พอดี (y=1073-1075/1094-1096, B=1085)

**warning เรื่อง mark pattern แก้ด้วย:** ไฟล์เขียนว่า "B1=คานนอน, B2=คานตั้งริม, B3=คานตั้งใน" — เป็นแนวโน้ม ไม่ใช่กฎ เจอข้อยกเว้นยืนยันแล้ว 2 ตัว ทั้งคู่เป็นคาน**นอน**ที่พิมพ์ว่า B2 (ตัว 3.55 กับตัว +2.10) · **ห้ามใช้ pattern นี้เดามาร์คที่อ่านไม่ออก**

**ยังไม่มั่นใจ เหลือ 3:** `RB1' ~A2` หน้า15 (stub สั้นข้างช่องบันได ยังไม่มีเส้นให้เกาะ ต่างจากตัวหน้า16 ที่ได้ B'' ไปแล้ว) · คานระเบียงหน้า16 ใต้แถว C 3 ตัว (cantilever ไม่มี dimension ระยะยื่นทั้งชุด) · `B1 C3-C4` หน้า16 (วัดเจอคานจริง แต่ตัวอักษรมาร์คถูกตัดขอบ conf 0.55 คงไว้)

parse ผ่านครบ 10 ไฟล์

**Addendum 3 (2026-08-24) — หน้า17 (ผังโครงสร้างหลังคา) ตรวจต่อ: คานหาย 1, คำบรรยายทรงหลังคาผิด, dimension ทั้งแผ่นไม่เคยบันทึก**

หน้า17 เป็นแผ่นสุดท้ายของบ้าน 41 ที่ยังไม่เคยตรวจ ไล่ด้วย scanner ชุดเดิม (grid: 1=1039, 2=1350, 3=1579, 4=1892 / A=888, B=1205, C=1525, ~104-106 px/m)

**1. `แปกลวาไนซ์` หาย** — แบบมี callout 10 อัน ไฟล์บันทึกไว้ 9 ตัวเดียว zoom 6x อ่าน label ชัด · เป็นตัวเดียวในแผ่นที่**ไม่มีขนาดและไม่มีระยะพิมพ์ไว้** (ตัวอื่นมี `2C 125x50x20x2.3mm.` หรือ `C 100x50x20x2.3mm. @1.00m.` หมด) → `steel_section: null, spacing_mm: null` ไม่เดา ไม่ก๊อปจากบ้านอื่น · ใช้ `element_type: "purlin"` (มีใช้อยู่แล้ว 49 ครั้งในชุดข้อมูล)

**2. คำบรรยายทรงหลังคาผิด — แบบมีส่วนยื่น 2 ก้อน ไฟล์บรรยายแค่ก้อนเดียว**

warning เดิมเขียนว่า *"plus a smaller secondary hip over a setback extension to the south/southeast (matching the ground-floor entry/stair-landing canopy)"* — วัดจริงแล้ว:

| ส่วนยื่น | ขอบเขตที่วัดได้ | ตรงกับ dimension ที่พิมพ์ |
|---|---|---|
| **เหนือ** (ไฟล์ไม่ได้พูดถึงเลย) | y=709 px = **1.70 m เหนือแถว A** · x=1311..1614 = **grid2−0.40 ถึง grid3+0.40** | `0.60 + 1.10` ที่ margin ซ้าย และ `0.40` 2 ตัวที่ grid 2/3 บน margin บน — เป๊ะทั้งคู่ |
| **ใต้** | x=1508..1964 = **grid3−0.70 ถึง grid4+0.70** | `0.70` 2 ตัวบน margin ล่าง (แต่ละตัวจับคู่กับ `0.30` ที่กริดเดียวกัน) — เป๊ะ |

ก้อนเหนือคือหลังคาคลุมส่วนยื่นบันได/ทางเข้า ที่แถว `A'`(-1.10)/`A''`(0.80) ขนาบอยู่บนหน้า15/16 · เป็น**เส้น outline เปล่า ๆ ไม่มีเหล็กเขียนข้างใน** · ก้อนใต้คลุมห้อง grid3-4 × C-D ก้อนเดียวกับที่เพิ่งแก้คานทั้ง 4 ด้านใน Addendum 2 ไม่ใช่ "entry canopy"

**3. dimension ทั้งแผ่นไม่เคยเข้า grid master เลย** — เพิ่ม 13 รายการเข้า `unassigned_dimensions` (11 → 31 รายการ): ชายคายื่น **1.10 รอบทั้ง 4 ด้าน** (x=928/2003, y=774/1640 px วัดได้ 1.06-1.09 ทุกด้าน), `0.60`(เหนือ), `0.40`(ใต้), `0.50`(margin ขวา ใต้ C = y 6.50), `0.40`×2 (ขอบก้อนเหนือ), `0.70`/`0.30`×2 (ขอบก้อนใต้)

**หมายเหตุที่มีน้ำหนัก:** `1.10` เหนือแถว A บนหน้า17 = ตำแหน่งเดียวกับ `A'`(-1.10) เป๊ะ → **แผ่นที่ 3 ที่พิมพ์ 1.10 เหนือ A** (คู่กับ margin ซ้ายหน้า16 และ margin ขวาหน้า15) · และ `0.50` ใต้ C บนหน้า17 = `6.50` ตัวเดียวกับ margin ขวาหน้า16 — 2 แผ่นตรงกัน แต่ยังหาคานไม่เจอทั้งคู่ ยังไม่เลื่อนขั้นเป็น dummy

**4. warning ของ gridline เคลมเกิน** — เขียนว่า y_lines A-B-C-D = 8.00 "พิมพ์เหมือนกันบนทุกแผ่นโครงสร้าง (หน้า14/15/16/17)" · **หน้า17 เป็นข้อยกเว้น** margin ของมันรวมได้ 6.00 หยุดที่แถว C เพราะหลังคาคลุมแค่ grid 1-4 × A-C ไม่ถึงแถว D — แก้แล้ว

parse ผ่านครบ 10 ไฟล์ · **บ้าน 41 ตรวจคานครบทุกแผ่นแล้ว** (หน้า14/15/16/17)

---

### 60. บ้าน 42 (บ้าน_เล็ก_2ชั้น_18) — ตรวจคาน: ยืนยัน "ไม่มีผังคาน" จริง + หน้า18 ผิด 2 กลุ่ม (คานหาย 4, ผูกช่วงผิด 2)

มะขามสั่งตรวจคานบ้าน 42 ต่อจากบ้าน 41

**ข้อแรก — ยืนยันคำเคลมของ `_scope.json` ว่า "GENUINE GAP: ชุดแบบไม่มีผังคานเลย" ⟶ ถูกต้อง** หลังจากที่ warning บ้าน 41 ผิดมาหลายข้อ ข้อนี้ต้องตรวจตรง ทำ contact sheet ทั้ง 24 หน้าดูรวดเดียว: แผ่นที่เป็น "ผัง" มีแค่ **p17 (ฐานราก)** กับ **p18 (หลังคา)** เท่านั้น · p20/p21 ที่ดูเผิน ๆ เหมือนมีหมายเลขกริดด้านบน จริง ๆ วงกลม ① ② คือ**เครื่องหมายตัดหน้าตัด** (ชี้ไป SECTION_1-1 / SECTION_2-2) และ LONG SECTION ใช้ `L`, `L/4`, `L/5` แบบ generic ไม่มีระยะจริง → คาน 14 มาร์ค (p20: B1,B2,B3,B4,B6,B10A · p21: B5A,B6A,B7A,B8A,B1',B2',B3',B4') มี spec ครบแต่ไม่มีตำแหน่งจริง ๆ **บันทึกแบบ spec-only ถูกต้องตาม §7 แล้ว ไม่ต้องแก้**

**ข้อสอง — หน้า18 `อะเส` ฝั่งซ้าย/ขวา ผูกช่วงผิด (แก้แล้ว)**

warning เดิมเขียนว่าเส้น grid1 และ grid4 *"drawn as ONE continuous line with no visible support-circle at row C"* แล้วบันทึกเป็น `B1-D1` / `B4-D4` ช่วงละ 7.00 m confidence 0.6 — **ผิด มีวงกลมที่ C1 และ C4 ทั้งคู่** ขนาด/ลายเส้นประเหมือนอีก 8 ตัวบนแถว B/D เป๊ะ พร้อมขีดขวางสั้นแบบเดียวกัน · trace แนวนอนตามแถว C (y=1200) ได้ signature ทึบกว้าง **93 px ที่ x=830..922** และ **96 px ที่ x=1940..2035** ตรงกลาง grid1 (868) กับ grid4 (1982) พอดี ขณะที่ทั้งแถวที่เหลือเป็นเศษ dash-dot 34-41 px · ยืนยันด้วยตาที่ zoom 8x อีกชั้น

→ แยกเป็น 4 ท่อน `B1-C1`, `C1-D1`, `B4-C4`, `C4-D4` ท่อนละ 3.50 m confidence 0.6 → 0.85

**ข้อสาม — หน้า18 มีคาน 4 ตัวที่ไฟล์ไม่ได้บันทึกเลย (เพิ่มแล้ว แต่ระบุมาร์คไม่ได้)**

เป็นคานขนานชายคา อยู่**กึ่งกลางลาดหลังคาพอดี**: y=1015 px (B=831, C=1200 → 184/185 px ตรงกลางเป๊ะ = y 4.25 m) และ y=1384 px (C=1200, D=1571 → 184/187 = y 7.75 m) · แต่ละเส้นเป็น **2 ตัว** พาด grid1-2 กับ grid3-4 **ไม่มีอะไรระหว่าง grid2-3** (trace ได้ x=881..1277 / 1524..1976 และ x=880..1276 / 1524..1975) · เส้นหนาสามเส้นเท่ากับ `อะเส` 2C และมีวงกลม `จุดตั้งดั้ง` นั่งอยู่บนมันตรงจุดที่ตะเข้สันตัดผ่าน — คือคานที่ดั้งถ่ายน้ำหนักลงนั่นเอง

⚠️ **ที่ยังไม่มั่นใจ: แบบไม่ได้ตั้งมาร์คและไม่ได้ให้ขนาดกับคาน 4 ตัวนี้** และมันไม่ได้ถูกคลุมโดย element สรุป `จันทัน`/`แปกัลวาไนซ์` ด้วย (สองตัวนั้นเป็น spacing-based repeat ส่วนนี่เป็นคาน 4 ตัวแยกชิ้นบนช่วงกริดที่ระบุได้) → บันทึกด้วย id เชิงบรรยาย `คานกลางลาด_*`, `steel_section: null` (**ไม่ก๊อปจากตัวข้างเคียง**), confidence 0.55 — ตำแหน่งวัดได้ แต่ตัวตนยังไม่รู้

**หมายเหตุ:** ตรวจจุดบรรจบตะเข้สันแล้ว — ไฟล์เคลมว่าลงที่ C2/C3 พอดี **ถูกต้อง** (ตะเข้สันวิ่ง 4.00 m ในแกน x ต่อ 3.50 m ในแกน y คือไม่ใช่ 45° ในผัง แปลว่าลาดด้านสกัดชันกว่าลาดด้านยาว — เป็นทรงปั้นหยาที่ถูกต้อง ไม่ใช่ความคลาดเคลื่อน) · วงกลมทึบที่เห็นกลางเส้นทแยงคือ `จุดตั้งดั้ง` ไม่ใช่ปลายตะเข้สัน

parse ผ่านครบ 12 ไฟล์

**Addendum (2026-08-24) — ลบบ้าน 42 ออกจาก `json_แก้ไขแล้ว` ตามคำสั่งมะขาม**

เหตุผล: ชุดแบบบ้าน 42 **ไม่มีผังคานพื้นเลย** (ยืนยันแล้วจาก contact sheet ทั้ง 24 หน้า — แผ่นผังมีแค่ p17 ฐานราก กับ p18 หลังคา) คานพื้น 14 มาร์คมีแต่ spec ไม่มี grid_ref สักตัว → ใช้สอน subtask ผังคานไม่ได้ ซึ่งเป็นเป้าหมายหลักของ dataset

- ลบเฉพาะ `json_แก้ไขแล้ว/42บ้าน_เล็ก_2ชั้น_18/` · **`rawjson_ยังไม่ได้แก้ไขโดนคน/42บ้าน_เล็ก_2ชั้น_18/` ยังอยู่ครบ 12 ไฟล์ ไม่ได้แตะ**
- ต่างกันแค่ไฟล์เดียวคือ `หน้า18_plan_roof_frame.json` (งานที่แก้ในข้อ 60) — ถ้าจะกู้ ต้องเอาจาก backup ไม่ใช่จาก rawjson
- สำรอง `json_แก้ไขแล้ว` เวอร์ชันที่แก้แล้วไว้ที่ scratchpad ของ session นี้ก่อนลบ

**⚠️ item 60 ที่บันทึกไว้ก่อนหน้ายังคงไว้เป็นประวัติ** — เนื้อหาที่ค้นพบ (อะเสต้องแยกที่ C1/C4, คานกลางลาด 4 ตัว, ยืนยันว่าไม่มีผังคาน) ยังมีค่าถ้าวันหลังกลับมาทำบ้าน 42 ใหม่ หรือเจอบ้านรุ่นเดียวกันในซีรีส์

**หมายเหตุ:** ตอนแรกมะขามสั่งลบ "บ้าน 41" แต่เป็นการสลับเลขบ้าน — บ้าน 41 (`..._2ชั้น_17`) **มีผังคานครบ 2 แผ่น** (หน้า15 ชั้นล่าง, หน้า16 ชั้นบน) และเป็นบ้านที่ไล่แก้กันทั้งวัน ไม่ได้ลบ · ที่ไม่มีผังคานคือบ้าน 42 (`..._2ชั้น_18`) · **ชื่อโฟลเดอร์กับชื่อรุ่นต่างกัน 1 (บ้านลำดับ 41 = รุ่น "2ชั้น_17") เป็นจุดที่สับสนง่าย**

---

### 61. บ้าน 43 (บ้าน_เล็ก_2ชั้น_19) — ผังคาน 2 แผ่น: เพิ่ม dummy 7 เส้น, คานหาย 6, ผูกแถวผิด 4

มะขามสั่ง "ไล่หน้าที่มีคานให้หมด กริดนายแก้เอง เราดูแค่ความยาวกับจำนวน" · ไฟล์เดิมทั้งบ้าน **ไม่มี dummy สักเส้น** และผังคาน 2 แผ่นถูกปักธงไว้เองว่า conf 0.5-0.65 เพราะแบบถ่ายเอกสารทึบ

**วิธี (เหมือนบ้าน 41/42):** pin กริดจากแบบก่อนเสมอ แล้วแยก "คาน = หน้าคานคู่ coverage 1.0" ออกจาก "เส้น dash-dot กริด = เส้นเดี่ยว 0.75-0.83"
- หน้า23: x 1=981/2=1305/3=1681/4=2004 px (107.7 px/m) · y A=720/B=932/C=1303/D=1674/E=1886 (106.0 px/m)
- หน้า24: x 1=876/2=1194/3=1571/4=1890 · y A=655/B=867/C=1238/D=1609/E=1821 (106 px/m)

**dummy 7 เส้น (อ่านจากระยะที่พิมพ์ทุกเส้น ไม่มีเดา):**

| แกน | id | pos_m | ที่มา |
|---|---|---|---|
| y | `A'` | −1.00 | margin ขวาหน้า23 พิมพ์ `1.00` เหนือแถว A |
| y | `B'` | 4.20 | margin ขวาหน้า24 พิมพ์ `2.20` จากแถว B |
| y | `B''` | 4.30 | margin ซ้ายทั้ง 2 แผ่น พิมพ์ `2.30` จากแถว B |
| y | `C'` | 5.90 | margin ซ้ายหน้า24 พิมพ์ `0.40` ใต้แถว C |
| x | `1'` | 1.00 | margin บนหน้า23 พิมพ์ `1.00` จาก grid1 |
| x | `2'` | 4.40 | margin บนหน้า23 พิมพ์ `1.40` จาก grid2 |
| x | `2''` | 5.90 | margin บนหน้า23 พิมพ์ `0.60` ก่อน grid3 |

**หน้า23 (คานชั้นล่าง) 27 → 32 ตัว**
- ผูกแถวผิด 2: `B1 A3-A4` → **`A'3-A'4`** (bay grid3-4 ไม่มีคานที่แถว A เลย หน้าคานวัดได้ y=615/636 = A−1.00) · `B1 C1-C2` → **`B''1-B''2`** (bay grid1-2 ไม่มีคานที่แถว C หน้าคานอยู่ y=1175/1196)
- คานหาย 5: `B3' A'3-A3` และ `B3' A'4-A4` (span 1.00, ด้านข้างส่วนยื่นเหนือ) · `B1` 3 ตัวบน dummy column `1'`/`2'`/`2''` span 2.00 ทุกตัว (หน้าคานวัดได้ y=732..924 = แถว A ถึงแถว B เป๊ะ)
- **ไม่ซอยคานนอนแถว A/B ที่ dummy column** — มาร์คเปลี่ยนที่กริดหลักเท่านั้น (B2 ช่วง 1-2, B3 ช่วง 2-3) จึงคงความยาวเต็มตามมาร์ค ตามกติกาที่มะขามวางไว้ตอนบ้าน 41 (B4 = 4.10)

**หน้า24 (คานชั้นบน) 27 → 28 ตัว**
- **แถว C มีคานเฉพาะ bay grid2-3** · bay 1-2 เส้น dash-dot ทะลุกลางช่อง S1 · bay 3-4 ทะลุระหว่างมาร์คเสา C2/C1
- ผูกแถวผิด 2 + มาร์คผิด 2: `B3 C1-C2` → **`B1 B''1-B''2`** (ขอบบนกล่อง S1) · `B3 C3-C4` → **`B1 B'3-B'4`** — zoom 5x อ่านได้ `B1(+3.65)` ทั้งคู่ ไม่ใช่ B3
- คานหาย 1: **`B1 C'1-C'2`** span 3.00 (ขอบล่างกล่อง S1 ที่ 5.90)
- เช็คแยกแล้วว่าแถว A ของหน้า24 **มีคานที่ bay grid3-4 จริง** (หน้าคาน y=643/665) — ส่วนยื่นเหนือเป็นของชั้นล่างอย่างเดียว ไม่ได้เดาจากความสมมาตร

**⚠️ เรื่องที่ค้างให้มะขามตัดสิน — `B'`(4.20) กับ `B''`(4.30) ห่างกัน 0.10 m**

ตอนแรกนึกว่า margin ซ้าย/ขวาของหน้า24 ขัดกันเอง (ซ้าย `2.30+1.20` ขวา `2.20+1.30`) **แต่ไม่ใช่ — มันคือคานคนละตัวคนละแถว** bay grid3-4 หน้าคานอยู่ B+2.18 (ตรงกับ margin ขวา 2.20) ส่วน bay grid1-2 อยู่ B+2.29 (ตรงกับ margin ซ้าย 2.30) แต่ละฝั่ง margin ตรงกับคานฝั่งตัวเอง · บันทึกเป็น 2 แถวตามที่แบบพิมพ์และวัดได้ **ถ้ามะขามเห็นว่าเป็นความคลาดของการเขียนแบบ ควรยุบเป็นเส้นเดียว** — ไม่ยุบเองเพราะหลักฐานสองทางชี้ว่าเป็นคนละเส้น

**หน้า26 โรงรถ — ตรวจแล้ว ไม่ต้องแก้** คาน 4 ตัวมีครบจริง (เส้นประคู่ทั้ง 4 ด้าน: เส้นตั้ง x=486/496 กับ 1083/1094 · เส้นนอน y=554/565 กับ 938/948) ลง X1/X2/Y1/Y2 พอดี spans 5.50/3.50/3.50/5.50 ✔ · ที่ยังกำกวมคือ**มาร์ค**ของคานนอน 2 ตัว (แบบเขียน `B1` ที่มุมซ้ายทั้งสอง `B1'` ที่มุมขวาทั้งสอง อ่านไม่ออกว่าตัวไหนคุมคานไหน) — flag ไว้ที่ conf 0.5 เดิมถูกต้องแล้ว

**หน้า25 หลังคาหลัก** — element เป็นแบบสรุปทั้งผัง (`A-D x 1-4`) ไม่มี span รายตัว จึงไม่มีอะไรให้ตรวจตามเกณฑ์ความยาว/จำนวน

parse ผ่านครบ 16 ไฟล์

---

### 62. บ้าน 44 (บ้าน_เล็ก_2ชั้น_20) — หน้า18 คานชั้นล่าง: เพิ่ม dummy x 2 เส้น, ปลด `~` ได้ 3 ตัว, ลบคานที่ไม่มีจริง 1

**หน้า18 (เสร็จแล้ว)** — pin กริด: x 1=1088/2=1399/3=1765 px (104 px/m) · y A=652/B=865/B'=1055/C=1288/D=1607/E=1872 (106 px/m)

**หมายเหตุ:** `B'` ของบ้านนี้เป็น**กริดที่พิมพ์จริง** (มีวงกลม Ⓑ' ที่ margin) ไฟล์เดิมใส่ `type: "named"` ถูกต้องแล้ว ไม่ใช่ dummy

**dummy แกน x 2 เส้น (จาก margin บน):** `1'` = **−1.00** (ระยะ `1.00` ก่อน grid1) · `1''` = **1.90** (จาก sub-chain `1.90 + 1.10 = 3.00`)

**ปลด `~` / เติม span ได้ 3 ตัว** — ช่วง B'-C มีเส้นตั้งเรียงกัน **4 ตัว** อ่านมาร์คที่ 5x ได้ครบ: `1'`(−1.00)=**B1** · grid1=**B10A** · `1''`(1.90)=**B2** · grid2=**B8A** · ไฟล์เดิมปล่อย B10A ลอยที่ `~1-2`, B1 ไม่มี span, และ **B2 หายทั้งตัว** · ส่วนเส้นตั้งที่ลอย `~1-2` ในช่วง A-B ก็คือ column `1''` เดียวกัน (หน้าคาน x=1268/1284)

**ลบคานที่ไม่มีจริง 1 ตัว:** `B2 D1-D2 span 3.00` — แถว D ช่วง grid1-2 มีแต่เส้น dash-dot (coverage 0.789) zoom 4x เห็นมาร์คเสา C1 สองตัวกับ B0 แล้วว่างเปล่าระหว่างกลาง · คานแถว D ตัวจริงคือ **`B3 D2-D3` span 3.50** (เดิมเป็น `~D2-D3` ไม่มี span)

**ผลกวาดรายแถว (ไว้ตรวจจำนวน):** แถว A มีคานทั้ง 2 bay · แถว B ทั้ง 2 · แถว B' **เฉพาะ bay grid1-2** · แถว C ทั้ง 2 · แถว D **เฉพาะ bay grid2-3** · แถว E เฉพาะ grid2-3

**ยังปล่อย `~` ไว้ (ยากจริง):** กลุ่มซุ้มโค้งระเบียงด้านใต้ — `B0 ~D1`, `B1 D1-~E1`, `B1' ~E1`, `B1' ~E3`, `B3 ~D2-E2` (ซุ้มครึ่งวงกลม ไม่มีช่วงตรงระหว่างกริดและไม่มีระยะพิมพ์) · ผนังเฉียง 45° ฝั่งตะวันออกระหว่างแถว C-D ก็เคสเดียวกัน — ช่วง C-D มีเส้นตั้งเฉพาะที่ grid2 (grid1 กับ grid3 เป็นเส้นเปล่าทั้งคู่) เพราะฝั่งตะวันออกวิ่งตามแนวเฉียงแทน

**⏸ หน้า19 (คานชั้นบน) — สำรวจแล้ว ยังไม่ได้แก้** เป็นแผ่นที่หนากว่า p18 มาก margin มี sub-chain 3 ชั้นบนแกน x และ 2 ชั้นบนแกน y · pin กริดได้แล้ว: x 1=1224/2=1537/3=1899 px · y A=668/B=880/B'=1071/C=1304/D=1622/E=1887 (106 px/m) · สิ่งที่กวาดเจอเบื้องต้น:
- ต้องเพิ่ม dummy y อย่างน้อย **0.90** (margin ซ้าย `0.90` ใต้ A — มีคานจริง หน้าคาน y=764/785) และ **10.20** (`1.20` ใต้ D — มีคานจริง หน้าคาน y=1730/1751)
- **แถว B ช่วง grid1-2 ไม่มีคาน** (coverage 0.811 เส้นเดียว) แต่ไฟล์มี `B3 B1-B2 3.00`
- margin ยังให้ tick อีกหลายตัวที่ยังไม่ได้ไล่: x `4.10`, `5.00`, `6.80`, `7.10` · y `5.50`, `7.90`
- **หน้า20 (คานหลังคา) ยังไม่ได้แตะ**

**Addendum (2026-08-24) — บ้าน 44 หน้า19 (คานชั้นบน) แก้แล้ว**

pin กริด: x 1=1224/2=1537/3=1899 px (104 px/m) · y A=668/B=880/B'=1071/C=1304/D=1622/E=1887 (106 px/m)

**dummy เพิ่มอีก 4 เส้น (จากระยะที่พิมพ์ทั้งหมด):** y `A'`=**0.90** (margin ซ้าย `0.90` ใต้ A แล้ว 1.10 ถึง B) · y `C'`=**7.90** (`1.90` ใต้ C แล้ว 1.10 ถึง D) · y `D'`=**10.20** (`1.20` ใต้ D) · x `2'`=**4.10** (grid2 + `1.10` จาก sub-chain ชั้นที่ 2)

**⚠️ แถว B ไม่มีคานเลยทั้งแผ่น** — กวาดทั้ง 2 bay ได้เส้นเดี่ยวเปล่า ๆ (0.811 และ 0.774) ไม่มีหน้าคานคู่ · zoom 4x เห็นแค่มาร์คเสา C1/C2/C1 นั่งบนเส้น dash-dot · **สิ่งที่รอบแรกอ่านว่าเป็นคานแถว B คือคู่ `B2(+3.65)` ที่อยู่สูงขึ้นไปหนึ่งแถวบน A'(0.90)** (หน้าคาน y=763/785) ส่วนมาร์ค `B1`/`B4`/`B1` คือ**เสาสั้น 3 ตัวที่ห้อยจาก A' ลงมาถึงแถว B** ไม่ใช่คานแถว B
- ย้าย `B2 A1-A2` → `A'1-A'2` · `B2 A2-A3` → `A'2-A'3`
- `B4 ~A2` (ลอย conf 0.35) → **`B4 A'2-B2` span 1.10** ระบุได้แล้ว
- **ลบ `B3 B1-B2 span 3.00`** — ไม่มีคานตรงนั้น

**แถว E ก็มีคานเฉพาะ bay grid2-3** → `B3 E1-E3` (ไม่มี span) → **`B3 E2-E3` span 3.50**

**เติม span ที่หาย:** `B1 B'1-B'2` = 3.00 · `B3 B'2-C2` = 2.20

**เพิ่มคานหาย 2 ตัว:** บน `C'`(7.90) ช่วง grid2-3 span 3.50 (หน้าคาน y=1504/1526) · บน `D'`(10.20) ช่วง grid1-2 span 3.00 (หน้าคาน y=1730/1751) — **⚠️ มาร์คอ่านไม่ออกทั้งคู่** ใส่ตาม pattern เพื่อนบ้านและ flag ไว้รายตัว ตำแหน่งวัดได้ แต่ชื่อมาร์คไม่ได้

**⚠️ ที่ยังไม่ฟันธง (ไม่เดา):**
1. `B3 B'1-C1` — เติม span 2.20 จากกริดแล้ว แต่**ตำแหน่งน่าสงสัย**: ช่วง B'-C ที่ grid1 ได้เส้นเปล่า 0.822 ขณะที่ `1'`(−1.00), `1''`(1.90), `2'`(4.10) และ grid2 ได้หน้าคานคู่สะอาดทั้งหมด — อาจอยู่คนละ column · conf ลดเหลือ 0.45 + flag
2. `B5 B2-B3` บันทึกไว้ที่แถว B แต่คานที่ใกล้ที่สุดใน bay grid2-3 วัดได้ y=1101/1122 = **ต่ำกว่าแถว B' ราว 0.3 m** ไม่ตรง tick ที่พิมพ์ไหนเลย — ปล่อยไว้ ไม่สร้างแถวใหม่มั่ว
3. **เสาช่วง A-B ขาดที่ A' ทุกตัว** (trace: grid1 670..767 แล้ว 788..846 · grid2 681..767 แล้ว 787..893 · grid3 680..767 แล้ว 786..1104) → span 2.00 ที่บันทึกไว้จริง ๆ คือ 2 ท่อน — ยังไม่ซอยเพราะมาร์คของท่อนบนอ่านไม่ออก
4. กลุ่มลอยรอบระเบียงแถว E และช่องบันได (`B1' ~B1`, `B2' ~1B-1B'`, `B6 ~2-3 x B'-C`, `B2' ~E1`, `B2' ~E3`, `B2 ~2 x E`) ยังไม่แตะ

parse ผ่านครบ 13 ไฟล์ · **หน้า20 (คานหลังคา) ยังไม่ได้แตะ**

---

### 63. บ้าน 45 (บ้าน_ใหญ่_1ชั้น_02) หน้า28 ผังคาน — เจอความผิดพลาดเชิงระบบ 2 เรื่อง + คานหาย 4

มะขามสั่งทำเฉพาะหน้าผังคาน (หน้า28) · แผ่นนี้เป็น **CAD สะอาด** ไม่ใช่สแกนถ่ายเอกสาร กริดจึง pin ได้เป๊ะ: x 1=918/2=1309/3=1624/**3'=1725** px (78.4 px/m) · y E=705/D=1017/C=1213/B=1525/A=1720 (78.1 px/m) — สองแกนต่างกันไม่ถึง 0.5%

**ความผิดพลาดเชิงระบบ 2 เรื่อง (แก้ครบทั้ง 3 แถบแล้ว)**

1. **คาน `B2` ทุกตัววิ่งบน grid 3' (10.10) ไม่ใช่ grid 3** — หน้าคานวัดได้ x=1696/1711 ขณะที่ grid 3 อยู่ที่ 1624 · สอดคล้องกับ `B7X`/`B6X` ที่เป็นท่อนต่อ 1.10 m จาก grid3 ออกไป 3' ซึ่งมีอยู่ในไฟล์แล้ว
2. **ตัวที่อยู่บน grid 3 จริง ๆ คือ `B5`** (แถบ E-D และ C-B) หรือ **`B1`** (แถบ D-C) — ไฟล์เดิมปล่อยลอยเป็น `~2-3 interior`

**คานบน grid 2 หาย/ผูกผิด 2 ตัว** — แถบ D-C ตัวที่เขียนว่า `~1-2 interior` จริง ๆ คือคานบน grid 2 (หน้าคาน x=1302/1318 มีกล่องเสาที่ D2 และ C2 ทั้งสองปลาย) · แถบ C-B **คานบน grid 2 หายทั้งตัว** · **แต่แถบ E-D ไม่มีคานที่ grid 2 จริง ๆ** — trace ได้ลาย dash ไม่ใช่เส้นทึบ ระหว่างเสา E2 กับ D2

**จำนวนคานภายในต่อ bay ผิด (แถบ E-D)** — bay grid1-2 มี **2 ตัว** (x=1072, 1226 px ≈ 1.96 และ 3.93 มาร์ค B3 แล้ว B4) ไม่ใช่ 1 · bay grid2-3 มี **1 ตัว** (x=1428 ≈ 6.49 มาร์ค B4) ไม่ใช่ 2 → เพิ่ม `B3` ที่หายไป

**ผลรวม: 33 → 37 คาน** (เพิ่ม 4, ย้ายเส้น 6)

**⚠️ ทำไมคานภายในยังใช้ `~` อยู่:** ตำแหน่ง x ของมัน**ไม่มีระยะพิมพ์ที่ไหนเลยในชุดแบบโครงสร้าง** — margin บนให้แค่ 5.00/4.00/1.10 และ[ผังฐานรากหน้า27](45บ้าน_ใหญ่_1ชั้น_02/บ้าน_ใหญ่_1ชั้น_02_หน้า27_plan_footing.json) ยืนยันว่า**มีเสาเฉพาะที่จุดตัดกริดหลัก** ไม่มีเสาภายในให้ dimension · วัดแล้วแยกไม่ออกระหว่าง 1.95 กับ 2.00 (±0.05 ที่สเกลนี้) → ตามกฎ "`pos_m` อ่านจากระยะที่พิมพ์ ห้ามเดา" จึงไม่สร้าง dummy column · บันทึกค่าที่วัดได้ไว้ใน note ของแต่ละ element เป็นหลักฐานแทน

**สำคัญ: เรื่องนี้ไม่กระทบความถูกต้องของ span เลย** — คานภายในทุกตัวเป็น**คานตั้ง**พาดเต็มแถบ ความยาวจึงเท่ากับความสูงแถบ (4.00 / 2.50 / 4.00) ไม่ว่าจะอยู่ x เท่าไหร่

**⚠️ ไม่แตะ ปักธงไว้:** แถบ B-A (กล่องเฉลียง) เส้นตั้ง 2 ตัววัดได้ x=979 และ 1247 px ≈ **0.78** และ **4.20** — ไม่อยู่ทั้ง grid1 (918) และ grid2 (1309) แต่ไฟล์บันทึก `B1 B1-A1` ไว้ที่ grid 1 · กล่องนี้ไม่มี dimension แบบเดียวกับคานภายใน จึงปล่อยไว้ ไม่ย้ายตามการเดา

parse ผ่านครบ 20 ไฟล์
