# stale_value_logs — ย้ายออกจาก training data 2026-08-25

## ทำไมย้าย

ไฟล์ในโฟลเดอร์นี้เป็น **บันทึกค่าเก่า → ค่าใหม่ รายไฟล์รายบ้าน** ที่เคยวางอยู่ในโฟลเดอร์
training data โดยตรง เป็นช่องทางปนเปื้อน (hallucination) ที่ชัดเจน: โมเดลที่กำลังสกัดบ้านหลังหนึ่ง
อ่านเจอตัวเลขของอีกหลังหนึ่ง (เช่น `dummy grid 3' แก้จาก 8.5 → 7.6` ของบ้าน 01) แล้วหยิบไปใช้ผิดหลังได้

ย้ายตามคำสั่งมะขาม (code=att1235) — **ไม่ได้ลบ** เก็บไว้ที่นี่เผื่อต้องย้อนดูประวัติ

## ไฟล์

| ไฟล์ | เดิมอยู่ที่ | ขนาด |
|---|---|---|
| `สิ่งที่ต้องแก้.md` | `json_แก้ไขแล้ว/สิ่งที่ต้องแก้.md` | 1,220 บรรทัด |

## ตัวชี้ที่ค้างอยู่

`primary_rawjson_schema.md` อ้างถึงไฟล์นี้ด้วยเลขข้อ (items 21/46/48/59-63) หลายแห่ง —
ตัวชี้พวกนั้นยังใช้ได้ แค่เปลี่ยน path เป็น `wait_for_ทิ้ง/stale_value_logs/สิ่งที่ต้องแก้.md`
