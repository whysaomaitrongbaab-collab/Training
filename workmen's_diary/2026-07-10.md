## Makham (working together with Claude Code — continuing from the July 9 session, see `2026-07-09.md`)

**Context:** Continuing from setting up Label Studio + reorganizing files (`label_studio_stuff/`, `docs/`) yesterday — today's step is **creating a condensed spec for prompting** and **setting up a "production raw JSON" structure** separate from the test rounds, along with starting to plan expansion to other houses.

**Work done (in order):**

### 1. Created `primary_rawjson_schema` — 2 rounds of trial and error
- Round 1: created `primary_rawjson_schema.json` (condensed from the 45KB `20260708draft of prime rawjson.md` down to 10KB) — Makham said **"this looks too short"**, not enough, wanted a `.md` file instead.
- Round 2: Makham clarified further — **don't delete the original**, include the content in full (not a brutal condensation), only condense the parts that save tokens, and consider writing in English if it genuinely saves tokens → rewrote as `primary_rawjson_schema.md` (11.8KB, ~26% of the original), keeping every operational rule intact, cutting only the history/reasoning behind the Gen2-vs-Makham decisions.
- Deleted the first `.json` file per Makham's order after confirming to proceed with `.md`.

### 2. Set up the new structure `raw_json_ตัวที่ใช้งานจริง/` (production raw JSON) (repo root)
Makham had already created this folder himself (`00file_for_making_rawjson_from_claude/` — initially spelled without the `00` prefix; Makham ordered renaming it to add the `00` prefix mid-task) — moved `primary_rawjson_schema.md` in there, then **copied** (not moved) `training-data/mk_test/t4/` → `raw_json_ตัวที่ใช้งานจริง/01บ้าน_เล็ก_1ชั้น_01/` (production_raw_json/01House_Small_1Story_01/) (all 101 files) as the starting point for "the first production house".

Wrote a `README.md` explaining the full workflow (folder structure + the `0N<house>` naming convention + the extraction steps + the steps for preparing data into Label Studio) — initially had a warning that the generator script was still tied only to `mk_test/`, not yet aware of this folder.

### 3. Wired `label-studio-tasks-makham.js` to automatically read from `raw_json_ตัวที่ใช้งานจริง/`
Makham confirmed "y" to proceed — added a `findProductionDir()` function that looks first for a subfolder whose name ends with the house name inside `raw_json_ตัวที่ใช้งานจริง/` (no need to type the `01` number manually), falling back to the old `mk_test/t2` if not found (backward compatible) — tested both modes worked correctly (auto-find produced 34/39/26/99, same as manual; fallback still pulled from `mk_test/t2` normally).

### 4. Copied work into `raw_json_ตัวที่ใช้งานจริง/00.../` — changed mind about the move + deleted an old file
- Makham ordered moving `primary_rawjson_schema.json` (still json at that point) into `00file_for_making_rawjson_from_claude/` — mid-task, Makham ordered the folder name fixed to `00file_...` (adding the `00` prefix) before moving.
- Later changed to `.md` (see item 1), then deleted the old `.json` per instruction.

### 5. A real question: Thai vs English, which saves more tokens
Makham asked directly — answered that **English typically saves 2-4x** (the tokenizer is trained primarily on English data, with far more subword merges available for English words; Thai often gets cut into small syllable-level pieces), with the caveat that JSON field names are already in English — the part that genuinely benefits is prose/descriptions.

### 6. Fully translated `primary_rawjson_schema.md` + `README.md` into English
Makham ordered translating both files fully into English, **except JSON code blocks, which must not be touched** — translated all 12 sections + kept every JSON shape/example exactly intact, not changing even a single character. While translating the README, also fixed a stale warning (the original said the script was still tied only to `mk_test/`, which was no longer true after item 3).

### 7. Checked for missing content — compared `primary_rawjson_schema.md` against the full original
Compared section by section against `20260708draft of prime rawjson.md` (219 lines) and found **3 genuine rules that had been dropped** (not just deliberately trimmed history), added them all back:
1. **Automation scope** — `run_pipeline.py` auto-extracts only 6 patterns (plan/section/schedule/notes/gridline/material_list); the other 7 types are manual-only.
2. **Atomic segment rule** — beams must be reported as atomic entries per grid span, must not be pre-aggregated by count before submitting (the code groups them automatically afterward).
3. **Open question about element_id colliding across multiple section files** (e.g. B2 in S-04 and S-05) — no decision rule exists yet.

### 8. Hypothetical walkthrough — if someone edited data in `01บ้านเล็ก...`
Makham asked hypothetically to explain the steps that would be taken if someone requested a data edit — answered with 9 steps, referencing `rule_of_tune.md` directly (check the file first, confirm correctness before editing rather than just trusting the claim, always warn before editing, edit surgically, check spec-join/multi-view files elsewhere that duplicate the same value, update confidence flags, **never touch `mk_test/t4`** since it's a copied file and would become 2 conflicting data sets, validate JSON, warn about task JSON in Label Studio not auto-syncing) — Makham praised how well rule_of_tune was cited.

### 9. Expanded `rule_of_tune.md` to cover `raw_json_ตัวที่ใช้งานจริง/`
Following that praise, Makham ordered making this official — added `raw_json_ตัวที่ใช้งานจริง/0N<house>/*.json` into the scope of "raw JSON raw data" (rule 1, given the same protection as files from Qwen), and expanded the "must read before starting work" line to cover the new folder as well — added a warning box linking to `rule_of_tune.md` at the top of `raw_json_ตัวที่ใช้งานจริง/README.md`.

### 10. Backed up the whole project to `E:\constitant\20260710\`
Found there was already an existing backup convention at `E:\constitant\20260708\` (mirroring all of `D:\00mk\steel project\`) — asked Makham what scope to use; Makham chose **only the 2 real repos** (Constistant + Training), excluding unrelated folders (`wait_for_ทิ้ง` (wait_for_discard) etc.) — ran `robocopy` as 2 background jobs (Constistant 2.5GB, Training 2.0GB), succeeded 100% (FAILED=0 for both, checked the logs to confirm — the system initially auto-flagged exit code 1 as "failed" but actually 1 = robocopy's "success, files copied" code, not an error).

### 11. Checked readiness for the "name a house → get complete raw JSON + Label Studio" workflow
Makham asked about 2 things: (1) checking whether `20260708draft of prime rawjson.md` was still intact — confirmed all 218 lines, untouched. (2) what else was missing to do the next house end-to-end — checked and found other houses (บ้าน_เล็ก_1ชั้น_02 (House_Small_1Story_02), บ้าน_เล็ก_2ชั้น_01 (House_Small_2Story_01), etc.) already had images ready in `image/<house>/`, no need to wait for PDF conversion, **but found 2 genuine blockers if doing house #2 right now:**
- **Not yet decided** whether Label Studio would use a separate project per house, or combine all houses in the existing project.
- **`label-studio-tasks-makham.js`'s output filenames aren't tied to the house name** — if re-run for house #2, it would immediately overwrite house #1's output files (`element/label-studio-tasks-makham-elements.json` has no house name in the filename) — **not yet fixed**.

### 12. Added a record in `raw_json_data_log.md` (the log Makham+King had previously kept)
Makham remembered this kind of log already existed (`training-data/docs/raw_json_data_log.md`, per `rule_of_tune.md` rule 3) — expanded the scope description to cover `raw_json_ตัวที่ใช้งานจริง/` too, then added a row logging today's creation of `01บ้าน_เล็ก_1ชั้น_01/` (noting the AI used, the process of personal reads + 5 agents, its origin from `mk_test/t4`, the full 101/101 validation result).

**Files changed/created today:**
- `raw_json_ตัวที่ใช้งานจริง/00file_for_making_rawjson_from_claude/primary_rawjson_schema.md` (new — went through 3 rounds of editing: condensed→full→translated to English→added missing rules).
- `raw_json_ตัวที่ใช้งานจริง/README.md` (new — translated to English + fixed a stale warning + added a rule_of_tune callout).
- `raw_json_ตัวที่ใช้งานจริง/01บ้าน_เล็ก_1ชั้น_01/` (new — copied from `mk_test/t4`, 101 files).
- `training-data/label_studio_stuff/label-studio-tasks-makham.js` — added `findProductionDir()` auto-lookup.
- `training-data/docs/rule_of_tune.md` — expanded protection scope to cover `raw_json_ตัวที่ใช้งานจริง/`.
- `training-data/docs/raw_json_data_log.md` — expanded scope + added today's log row.
- `E:\constitant\20260710\` (new — full backup of Constistant + Training, outside the repo).

**Impact/precautions:**
- **`training-data/mk_test/t4/` and `raw_json_ตัวที่ใช้งานจริง/01บ้าน_เล็ก_1ชั้น_01/` are the same data set duplicated in 2 places** — no official decision yet on which one is the real one to continue editing (the assumption used to answer item 8 was that `01บ้านเล็ก...` is the real one, but this hasn't been officially confirmed).
- **Doing house #2 right now is not safe yet** — the script needs to be fixed to split output filenames per house first, or it will overwrite house #1's results.
- `label-studio-import-annotations.js` / `-import-repeater-annotations.js` still hasn't been adapted for the Gen4/Makham schema (pending from an earlier entry, still untouched).
- There's still no standard, repeatable validation script — one-off ad-hoc scripts have been written each round so far.

**Left pending / to continue:**
1. Decide + fix the script so multiple houses can be done without colliding (Label Studio project strategy + output filenames tied to house name).
2. Officially decide between `mk_test/t4` vs `01บ้านเล็ก...` — which is the source of truth going forward.
3. Write/adapt the import-back script to actually support the Gen4/Makham schema.
4. Write a standard validation script (checking JSON valid + source_image + page coverage + pattern completeness) instead of one-off ad-hoc scripts.
5. Commit all the work (nothing committed at all since the t4 round — pending for several days now).
6. Untouched prior pending work from earlier entries: the 3 Label Studio projects still not fully reviewed, the old stale `Constistant/training-data/` duplicate folder.

---

## [19:13] Makham (working together with Claude Code — same day's work, continuing from the entry above)

**Context:** Continuing the same session on the same day (a git divergent-history incident happened earlier in this conversation in a separate context, already resolved by resetting local `main` of the `Constistant` repo to match `origin/main` after finding someone had force-rewritten the whole repo's history — not directly related to this raw_json work, so not logged in detail in this diary). The main task is fixing pending item #1 from this morning's entry (the script still didn't support multiple houses), then defining a shorthand command "op1" to invoke the full workflow in a single command.

**Work done:**
1. Fixed `label-studio-tasks-makham.js` — changed output filenames from fixed names (`label-studio-tasks-makham-elements.json` etc.) to house-tied names (`label-studio-tasks-makham-<house>-elements.json` etc.), preventing a re-run for house #2 from overwriting house #1's results — warned Makham before editing per `rule_of_tune.md` rule 2 (editing a script that affects data flowing toward tuning); Makham confirmed "i confirm", then the edit was made.
2. Tested re-running against `บ้าน_เล็ก_1ชั้น_01` (House_Small_1Story_01) — got the same numbers as before (34/39/26/99), confirming the logic hadn't changed, only the filenames.
3. Deleted the old fixed-name output files that were left over (`element/label-studio-tasks-makham-elements.json`, `material_list/label-studio-tasks-makham-material_list.json`, `single/label-studio-tasks-makham-single.json`, `label-studio-tasks-makham-all.json`) — no permission needed since these are regeneratable files (per the `rule_of_tune.md` exception).
4. Added a section **"Quick command: `op1 <house_name>`"** in `raw_json_ตัวที่ใช้งานจริง/README.md` (before the Step 1 section) — defined for an AI agent to interpret the command `op1 <house_name>` as shorthand for the full 7-step workflow (read the full spec → figure out the next sequence number itself → read every page's image for real and extract → save into `0N<house>/` → run the script to generate the 3 task files → report results + warn about manual import → add a row to `raw_json_data_log.md`) — explicitly stated this does not skip any part of `rule_of_tune.md`.
5. Updated the sample output filename in the README's existing Step 2 to match the new house-tied filenames.

**Files changed:**
- `training-data/label_studio_stuff/label-studio-tasks-makham.js` — output filenames tied to house name + fixed the header comment.
- `raw_json_ตัวที่ใช้งานจริง/README.md` — added the "op1" section + fixed the sample output filename in Step 2.
- Deleted old files: `training-data/label_studio_stuff/element/label-studio-tasks-makham-elements.json`, `.../material_list/label-studio-tasks-makham-material_list.json`, `.../single/label-studio-tasks-makham-single.json`, `.../label-studio-tasks-makham-all.json`.
- Newly created (regenerated): `.../label-studio-tasks-makham-บ้าน_เล็ก_1ชั้น_01-{elements,material_list,single,all}.json`.

**Impact/precautions:**
- Still no decision on the Label Studio project strategy (separate project per house, or combined) — the script now handles non-colliding output filenames, but the Label Studio project side still needs a separate decision.
- "op1" has never actually been tested against house #2 (nobody has actually invoked it yet) — this is just the definition/documentation so far.
- The old output files that were deleted were the same set already actually uploaded to Label Studio for house 1 (deleting the local files doesn't affect data already imported on Label Studio Cloud).

**Left pending / to continue:**
1. Actually test "op1 <house_name>" on house #2 once Makham is ready to order it.
2. Decide the Label Studio project strategy for multiple houses (separate/combined projects).
3. Untouched prior pending work from this morning's entry (see the list above): `mk_test/t4` vs `01บ้านเล็ก...` source-of-truth, the Gen4 import-back script, a standard validation script, committing all the work.

---

## [20:12] Makham (working together with Claude Code — same day's work, continuing from the entry above)

**Context:** The first time "op1" was actually invoked for real — Makham typed `op1 D:\00mk\steel project\training\Training\image\บ้าน_เล็ก_1ชั้น_02` ordering house #2 (`บ้าน_เล็ก_1ชั้น_02` (House_Small_1Story_02), 72 pages) to be extracted end-to-end following the workflow just defined in the README moments earlier.

**Work done:**
1. Surveyed the document structure (pages 1-2 = cover+table of contents) and computed a page→sheet-code mapping that matched 100%: cover(1)+contents(2)+architecture A-01..A-19(3-21)+structural S-01..S-14(22-35)+sanitary SN-01..08(36-43)+electrical EE-01..04(44-47)+mechanical M-01(48)+spread-footing BOQ(49-58)+pile-foundation BOQ(59-68)+site-plan criteria(69-72), totaling exactly 72 pages.
2. Created the grid master (`บ้าน_เล็ก_1ชั้น_02_หน้า00_gridline.json`) (House_Small_1Story_02_page00_gridline.json) from cross-reading S-02/S-03/S-04/S-05 — the grid matched across all 4 sheets, with no dummy grids at all (unlike house 1, which had 1'/3'/A'/A''), since the layout's asymmetry comes purely from the building's own shape, not from a dummy grid.
3. **Personally read the entire structural section (S-01 through S-14, pages 22-35, 14 sheets)** — chose to do this personally since it's the highest-risk rebar data, producing 15 files (including the grid master) covering: general notes, spread-footing+pile-foundation plans (2 parallel alternative systems), first-floor beam plan, tie-beam plan (beams RB1/RB1A/RB2 — found every bay between grids F-C was "open" with no floor slab, since these are column-tie beams at tie-beam level, not floor beams), roof-frame plan (structural steel sections, not rebar), beam/precast-slab enlarged details, column schedule + stairs + RC fins, enlarged details for both foundation systems (F0.60/F0.80/F1.00 for spread footings, F3/F4/F6 for pile foundations), enlarged precast-slab (SP) details across 2 sheets.
4. For parts read without full confidence (the southward extension beyond grid C in the beam/tie-beam plans, the position of RB2 not sitting on the main grid, roof/tie-beam rows in the column schedule that looked like blanks/arrows), assigned a low `confidence_score` + a clear `needs_human_review_zoom_required` flag instead of guessing, per `rule_of_tune.md`.
5. **Released 5 background agents simultaneously** to handle the remaining sections (split by discipline), each given a very long prompt specifying: must read the spec+rule_of_tune first, no guessing allowed, house 1's matching-pattern file as an example, the image path, the output path, the file-naming convention, the grid master (must not invent new dummy grids themselves — flag them back instead), and quality standards:
   - Agent 1: architecture (pages 3-21) → completed, 30 files, all 19/19 pages.
   - Agent 2: sanitary (pages 36-43) → completed, 13 files, all 8/8 pages (found this house has an SN-05 roof-drainage plan that house 1 didn't have).
   - Agent 3: electrical+mechanical (pages 44-48) → completed, 7 files, all 5/5 pages, the EE/M scope matched the estimate exactly.
   - Agent 4: spread-footing BOQ (pages 49-58) → **killed mid-task (either Makham stopped it himself, or the session closed mid-task — cause not yet confirmed)**, hadn't written a single file when killed (was at the stage of verifying codes ป1/น1/ฝ1/พ1/ผ1 (p1/n1/f1/ph1/w1) before writing) — **pages 49-58 (10 pages) still have no data at all**.
   - Agent 5: pile-foundation BOQ+site-plan criteria (pages 59-72) → completed, 23 files (195 BOQ items), all 14/14 pages — a significant find: pages 70-72, expected to continue the site-plan criteria, were actually a committee roster/price comparison table across all 10 house designs/a drawing-set cover page, not criteria at all — the agent caught this itself and correctly classified the pattern (unknown/title) instead of incorrectly stuffing it into notes.
6. Validated JSON on every file with `node -e` (no python on the machine) — **88/88 files all valid JSON (data so far, not yet including pages 49-58, which hasn't been done)**.

**Files changed/created today (this round):**
- `raw_json_ตัวที่ใช้งานจริง/02บ้าน_เล็ก_1ชั้น_02/` (02House_Small_1Story_02/) (entire folder new — 88 JSON files: grid master 1, structural 14, architectural 30, sanitary 13, electrical+mechanical 7, pile-BOQ+criteria 23).

**Impact/precautions:**
- **Pages 49-58 (spread-footing BOQ, 10 pages) still have no data** since the agent doing them was killed mid-task — need to re-dispatch before `02บ้าน_เล็ก_1ชั้น_02/` can be considered complete at 72/72 pages (already asked Makham whether to re-order this; no answer received as of writing this entry).
- There are points various agents flagged for further checking (see details in each file's `confidence_flags`/`warnings`): a dimension mismatch across sheets at elevation 3 vs. others (1.30 vs 1.00), a sanitary fixture label WC.2 misprinted as WC.1, a doorbell symbol that doesn't quite match the legend, a duplicate item code "ผ1" (ph1) in the BOQ, an anomalous price for house design #6 in the price comparison table (spread footing more expensive than pile foundation).
- `label-studio-tasks-makham.js` has not yet been run against this house (need to wait until pages 49-58 are done, or it could be run incomplete first if Makham wants).
- No row has been added to `raw_json_data_log.md` yet (per rule_of_tune.md rule 3) — waiting until the work is fully complete before adding it all at once.

**Left pending / to continue:**
1. **Decide on the not-yet-done spread-footing BOQ (pages 49-58)** — re-dispatch a new agent, or Makham wants to do it personally/some other method.
2. After pages 49-58 are done: final validation (JSON valid, source_image complete, 72/72 coverage, patterns complete), run `label-studio-tasks-makham.js บ้าน_เล็ก_1ชั้น_02`.
3. Add a row to `raw_json_data_log.md` logging the creation of `02บ้าน_เล็ก_1ชั้น_02/` (AI used, the process of personally reading the structural section + 5 background agents, the validation result).
4. Untouched prior pending work from earlier entries: `mk_test/t4` vs `01บ้านเล็ก...` source-of-truth, the Gen4 import-back script, a standard validation script, committing all the work, the multi-house Label Studio project strategy.

---
