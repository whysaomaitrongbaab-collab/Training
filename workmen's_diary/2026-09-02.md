# 2026-09-02

## จับตางาน house_extract จริง (claude, เฝ้าตาม op คำสั่ง มะขาม, att1235 เต็มสิทธิ์)

- job แรก (f63bc71e) จบสำเร็จ 9/9 แต่ 3 หน้าพัง (JSON เสีย): page_03_notes, page_12_plan_footing, page_13_notes
- **เจอ root cause จริง:** `serve_purson.py` (ฝั่ง server) มี `PAGE_TIMEOUT_S = 25*60` ของตัวเอง แยกจาก `worker.py` (client) ที่ขยับเป็น 2700s ไปแล้วเมื่อวาน — ตัวตัดจริงคือฝั่ง server ที่ไม่เคยแก้ ใช้เป็น `StoppingCriteria` ตัด generation กลางคัน ไม่สนว่า JSON ครบหรือไม่
- แก้: ยกเป็น `40*60` (2400s, ต่ำกว่า client 2700s) → restart server → resubmit บ้านทั้งหลังใหม่ (job 43323920, ใช้ path รูปเดิมจาก storage ไม่อัพซ้ำ)
- รอบ 2: 5/7 หน้าผ่าน (รวม page_03 ที่เคยพัง) — **หน้า 12 (ฐานราก+คาน) ยังพังทั้ง 2 มุม** แม้เพดาน 40 นาที เพราะเนื้อหาหนาแน่นจริง (หน้าเดียวมีทั้งฐานราก+คาน)
- เปิด raw text ดู: ฐานรากคือโมเดลตอบ elements ครบถูกแล้ว (7 รายการ) แล้ววนซ้ำอธิบายตัวเองไม่จบ (ประโยคเดิมซ้ำๆ) — คาน คือกำลังตอบจริงต่อเนื่อง (64 ท่อน) แต่โดนตัดก่อนจบ (เหลือ 144 ตัวอักษรสุดท้าย)
- **กู้คืนด้วยสคริปต์เอง** (`salvage_json.py` — bracket-depth walker หาจุดตัดปลอดภัยแล้วปิด JSON ให้ครบ, ไม่เดาค่า) ได้ครบทั้ง 2 ไฟล์
- รันชุดสุดท้าย 7 ไฟล์ผ่าน `qt_importRawExtractionFiles()` + `runPipeline()` จริง (ไม่ mock) — **BOQ 21 รายการ ฿170,396.09** สำเร็จ
- เซฟผลถาวรที่ `dev/demo/raw-extraction-samples/destrier-house2-thaisuk/` (7 ไฟล์ + README บันทึกที่มาแต่ละไฟล์ตรงๆ ว่าไฟล์ไหนกู้คืน + run-demo.mjs รันซ้ำได้)
- `npm test` ไม่ได้รันรอบนี้ (ไม่ได้แก้โค้ด production เพิ่มจากเมื่อวาน — แก้แค่ `serve_purson.py` ซึ่งไม่มีเทสคลุม)

**ยังไม่แก้ (บันทึกไว้):** หน้า 12 ยังไม่มีทางแก้ถาวร (crop แยกฐานราก/คานออกจากกันน่าจะช่วย ลดเนื้อหา/call) — กู้คืนด้วยมือรอบนี้ใช้ได้ผลแต่ไม่ใช่ automation จริง ต้องมีคนทำ crop เพิ่มถ้าจะให้ pipeline ผ่านหน้าแบบนี้เองในอนาคต

## checkpoint/resume จริง — เก็บผลทุก pass ระหว่างทาง ไม่ต้องเริ่ม 0 (มะขามสั่ง)

- `worker.py`: `save_checkpoint()` เขียน `result` (jsonb เดิม) ทับหลัง pass0/gridline/ทุกหน้าใน
  pass2/pass2.5 — ไม่รอจบงานทั้งใบ · `claim_next_job()` ดึง `result` มาด้วย → `run_house_extract`
  เช็คก่อนแต่ละ pass ว่ามี checkpoint เดิมไหม มีก็ข้าม ไม่ยิงโมเดลซ้ำ (resume จริง ไม่ใช่แค่ save)
- ฝั่งเว็บ: จำ job id ล่าสุดต่อโปรเจกต์ (localStorage เครื่องเดียว ไม่ sync คลาวด์ตั้งใจ) เปิด
  Drawing Intelligence เช็คอัตโนมัติ — งานพัง → ปุ่ม "🔁 ทำต่อ" (PATCH status เดิมกลับ pending
  ไม่สร้างงานใหม่) · งานยังวิ่งอยู่ (ปิดเว็บกลางทาง) → ต่อดูให้เอง
- เทสใหม่ครอบ save_checkpoint + claim_next_job select · `npm test` 1173/1173 · commit `cd6dccf`
- ยังไม่ได้ทดสอบ resume จริงกับงานที่พังจริงบนการ์ดจอ (แค่เทส unit) — รอบ house09 ข้างล่างจะได้เจอ

**อัปเดตหลังคืนการ์ดจอ:** รัน pass3 (วัดระยะจริง) ย้อนหลังกับ 2 ไฟล์ที่กู้คืนแล้ว — ผล `ok:false`
"หมุดไม่พอ" ถูกต้องแล้ว ไม่ใช่บั๊ก (หน้านี้ CV หาสัญลักษณ์ไม่เจอเลยสักจุด เพราะเป็นแบบเหล็ก C-channel
สไตล์พิเศษ ไม่มีจุดยึดพิกัดให้วัด) ไม่เติมอะไร ไม่กระทบ BOQ เดิม (ยัง ฿170,396.09 เท่าเดิม)
บันทึกผลไว้ที่ `pass3_measure.json` ในโฟลเดอร์ตัวอย่างแล้ว · เช็ค RB1/RB2 ซ้ำชื่อคนละขนาดด้วย —
BOQ จริงใช้ค่ารายการแรกเท่านั้น ไม่นับซ้ำ ปลอดภัย · การ์ดจอ (instance 49559299) คืนแล้วตั้งแต่คืนนี้

## Vast.ai workflow — ยังไม่ได้สอนมะขามทำเองรอบนี้
ตามที่บันทึกไว้ค้างจากรอบก่อน (`project_teach_vastai_workflow.md`) — งานนี้ทำผ่านทั้งหมดโดย claude เอง (setsid restart, upload fix, resubmit job ผ่าน REST API ตรง) ยังไม่ได้ walk-through สอนมะขามทำเองทีละสเต็ป เก็บไว้ทำรอบหน้า
