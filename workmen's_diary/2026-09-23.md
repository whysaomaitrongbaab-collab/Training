# 2026-09-23

## [session, exact clock time not logged] Tonton (Claude Code session)

**Work done:**
- Continued the `missing.md` gap-fix queue on `Constistant`'s `pipeline-optimize` branch (branched off `origin/Presentation-demo`, see the 2026-09-21 entry above). Confirmed `pipeline-optimize` was still exactly at `origin/Presentation-demo`'s tip (`cceb0c8`) — nothing new to sync in from upstream before starting.
- Audited items 3-6 of `missing.md` against the actual current code (not the doc's claims) before touching anything:
  - **Item 3** (real vertical-height field instead of silent 3.0m default) — confirmed already done via `resolvePrismaticLength()` in `pipeline.js`.
  - **Item 4** (extend `assessSpecReadability()`'s confidence gate to check `span_length_m`/height) — found the `checkVerticalSpan` param exists in `bbs-rebar-confidence.js` but is **never called with `true` anywhere** (dead code). Traced why: the actual height-assumption warning already flows through a *different*, correct mechanism — `resolvePrismaticLength().isDefault` feeding `review_reason: 'default_vertical_height_assumed'` in BOQ and a `shape_note` suffix in BBS (both part of item 3's fix). Wiring `checkVerticalSpan` into `resolveEffectiveSpec()`/`findDonorLib()` as literally written in missing.md would have been wrong — it would conflate a height problem (lives on the `element`) with a rebar-spec problem (lives on the `lib`), causing a column with perfectly good rebar spec but merely unknown height to get its *rebar spec* discarded and replaced by a borrowed/assumed one for no reason. Decision (confirmed with Ton): leave `checkVerticalSpan` as intentional dead code, do not wire it in. Documented this in `missing.md` item 4 (marked done via the other mechanism).
  - **Item 6** (skip-and-flag instead of silent `|| 0`) — confirmed already done via `missingDims`/`missingRebar` flags in `computeBOQ()`.
  - **Item 5** (bring BBS's `extracted`/`borrowed`/`assumed_standard` confidence gate into `computeBOQ()`) — confirmed NOT done; `computeBOQ()` was still reading `beamLibraryById[el.beam_library_id]` raw, never through `resolveEffectiveSpec()`.
- Investigated who actually designed the `extracted`/`borrowed`/`assumed_standard` gate in the first place, since Ton suspected he'd proposed this before. Confirmed via `git log --all --author=tonton` — **Ton himself** wrote `bbs-rebar-confidence.js`/`bbs-rebar-assumptions.js`/`resolveEffectiveSpec()` on 2026-08-24 (commits `4ade015`, `611612e`, `22a6907`, `2d8dc1a`), with a design spec at `docs/superpowers/specs/2026-08-24-bbs-confidence-fallback-design.md` that explicitly says: *"เฟสนี้แตะเฉพาะ BBS — `computeBOQ()` ไม่แตะ assumption ไม่ไหลเข้าเงินเด็ดขาด เป็นงานเฟสถัดไป (ถ้าต้องการ)"*. Mail's `missing.md` item 5 (2026-09-17) is exactly that deferred next phase Ton had already scoped out — not a new ask from Mail, a continuation of Ton's own plan.
- Implemented item 5, **Option B** (assumption flows into real BOQ pricing, not just a flag on a zero-quantity row):
  - `js/shared/schema.js`: added `rebar_source`/`rebar_source_note` fields to `createBOQItem()`, mirroring the existing `BBSItem` fields.
  - `js/shared/pipeline.js`, `computeBOQ()`: now calls `resolveEffectiveSpec()` (the same gate `computeBBS()` already used) instead of reading `beamLibraryById[...]` directly. All three BOQ rows per element (concrete/rebar/formwork) now carry `rebar_source`/`rebar_source_note`, and `status`/`review_reason` are forced to `needs_review` with a new reason (`rebar_source_borrowed` / `rebar_source_assumed_standard`) whenever the effective spec isn't `'extracted'`. The old "no spec at all → quantity 0, flagged, never priced" path (`no_spec_found`, from the 2026-08-29 P1/F4 fix) now only fires for an `element_type` with no entry in `TYPICAL_SECTION_MM` at all (i.e. not column/beam/girder/slab/footing/staircase) — every known structural type now gets a real, priced row even with zero spec data, because `assumeRebarFromRules()` always has a typical-section fallback for those types.
  - Also fixed a consistency gap the change would otherwise have introduced: the pre-pass in `computeBOQ()` that computes `beamSoffitAreaByFloor` (used to deduct beam-soffit area from slab formwork so it isn't double-counted) was still reading the raw `lib` directly and skipping beams with no lib — meaning a beam that now gets a real formwork line (via borrowed/assumed spec) would no longer be deducted from the slab's formwork area, double-counting that beam's soffit. Fixed by routing that pre-pass through `resolveEffectiveSpec()` too.
  - `js/boq/boq-summary.js`: added Thai labels for the two new `review_reason` values to `REVIEW_REASON_TH`, and made the row-level review flag's tooltip append `rebar_source_note` (the actual "borrowed from X" / "assumed from rule Y" explanation) so the reason a row's price is uncertain is visible where an engineer would actually see it, not just an enum string.
  - Updated the stale in-code comment in `computeBBS()` that said "computeBOQ ด้านบนไม่แตะ" — no longer true.
- Updated `missing.md`: items 3, 4, 6 marked done (with a note on *how* item 4 was actually achieved and why `checkVerticalSpan` stays dark); item 5 marked done with a summary of what changed.
- Updated the test suite for the new behavior (traced by hand, not run — see below):
  - `tests/pipeline-safeguards.test.js`: the two tests that asserted `missing_cross_section_dimensions`/`missing_rebar_spec` for an element with a partially-incomplete lib now expect `rebar_source_assumed_standard` with a real non-zero quantity instead (since `resolveEffectiveSpec()` fills in the missing dims from `TYPICAL_SECTION_MM` before `missingDims`/`missingRebar` ever get evaluated, so those two flags are now effectively unreachable for any known element type). Added a new test confirming the `borrowed` path (a same-type element elsewhere in the project with a complete spec) is picked over `assumed_standard` when one exists.
  - `tests/boq-no-spec-rows.test.js`: rewritten — a footing/slab with `beam_library_id: null` now gets a full assumed-standard-priced row set, not a `no_spec_found` placeholder; kept one test using a fictional unsupported `element_type` to prove the old P1/F4 escape hatch still exists for genuinely unknown types.
  - `tests/bbs-assumption-pipeline.test.js`: rewrote the one test that explicitly asserted "computeBOQ ไม่รับ assumption เข้าเงิน" (quantity/amount forced to 0) — that was the literal old behavior item 5 reverses. Now asserts 3 rows, all `needs_review`, `rebar_source: 'assumed_standard'`, non-zero quantity/amount.
  - `tests/raw-extraction-golden.test.js`: **left as an honest TODO, not guessed.** This is an end-to-end golden-number test against a real 8-file fixture (`finetune_output_contract/finetune_output_contract/examples/`) run through the real adapter + `runPipeline()`. `boqItemCount: 66` almost certainly needs to increase now (spec-less slabs in that fixture go from 1 row each to 3), but the exact new number depends on how many of the fixture's real elements are lib-less and of a `TYPICAL_SECTION_MM`-covered type — not something safely hand-traceable from raw JSON without actually running the code. Left a comment explaining this and what whoever picks it up next needs to do (run the test, read the real "got X", re-pin with a comment, per that file's own "do not silently re-pin" rule).
  - Manually re-traced every other test file that calls `computeBOQ()`/`runPipeline()` (10+ files) to confirm their fixture `beam_library_id` specs are all complete (`extracted` verdict) and therefore unaffected by this change — takeoff-audit, zoom-research-v2, standards-pipeline, audit-constants, boq-material-engine-resilience, and the various `runPipeline()`-driven integration tests (demo-seed data is hand-authored and complete for every element type, including the `width_mm: null` slab entries which are fine because slabs don't require `width_mm`).

**Files changed:**
- `Constistant/js/shared/schema.js` — `createBOQItem()` gained `rebar_source`/`rebar_source_note`.
- `Constistant/js/shared/pipeline.js` — `computeBOQ()` now resolves specs through `resolveEffectiveSpec()`; new `specNotExtracted` flag feeds `status`/`review_reason` on all 3 BOQ rows per element; `beamSoffitAreaByFloor` pre-pass fixed to match; stale comment in `computeBBS()` corrected.
- `Constistant/js/boq/boq-summary.js` — `REVIEW_REASON_TH` gained `rebar_source_borrowed`/`rebar_source_assumed_standard`; `reviewFlagCell()` tooltip now appends `rebar_source_note`.
- `Constistant/missing.md` — items 3/4/6 marked done with explanation; item 5 marked done.
- `Constistant/tests/pipeline-safeguards.test.js`, `tests/boq-no-spec-rows.test.js`, `tests/bbs-assumption-pipeline.test.js` — updated for the new behavior.
- `Constistant/tests/raw-extraction-golden.test.js` — `boqItemCount` flagged stale with a detailed comment, value left unchanged pending a real test run.

**Impact/precautions:**
- **This is a real behavior change for BOQ pricing, not just a flag change.** Before this, an element with no readable spec (or an incomplete one) priced at ฿0 and never contributed to the project total. Now it gets a real, non-zero, typical-section-based price — visibly flagged `needs_review` with a Thai explanation of where the number came from, but it *does* flow into the total and into any export/PDF unless the reviewer catches the flag first. This is the deliberate trade Ton's own 2026-08-24 design doc scoped for "phase 2" and what this session executed — but it's worth a second pair of eyes (Mail/King) checking that the `needs_review` + `rebar_source_note` surfacing is loud enough in the actual UI/exports before this is trusted on a real project's numbers.
- **No test was actually run in this session — there is no Node.js runtime available in this sandbox environment** (checked `node`/`npm` on PATH and common install locations, none found). Every test-file edit above was derived by hand-tracing the actual code paths (`resolveEffectiveSpec`/`assumeRebarFromRules`/`findDonorLib`/`TYPICAL_SECTION_MM`/`TYPICAL_MIN_REINFORCEMENT`) against each fixture's exact input values, which is a real risk of an off-by-one or a missed edge case slipping through. **Whoever picks this up next should run `npm test` for real before trusting any of it**, starting with `tests/raw-extraction-golden.test.js` (the one known-stale number) and then the full suite for anything else this hand-trace missed.
- `js/shared/bbs-rebar-assumptions.js`'s `findDonorLib()` skips donors sharing the same `element_id` (mark) as the element being resolved — a real trap when writing a borrowed-spec test with two elements of the same mark on different floors (that's a documented no-op, not a bug); had to use two different marks in the new borrowed-path test in `pipeline-safeguards.test.js`.

**Left pending / to continue:**
- Run the real test suite (`npm test`) in an environment with Node installed and fix whatever the hand-trace above got wrong, starting with `tests/raw-extraction-golden.test.js`'s `boqItemCount`.
- Consider whether the BOQ export variants (`js/boq/boq-export-variants.js`) should also surface `rebar_source_note` as a footnote (the way TPSO citations already do) — not done this session, flagged as a real risk in the original design-discussion but not yet acted on.
- Decide (Mail/King) whether the `needs_review` + tooltip surfacing on the BOQ table is sufficiently visible before this ships to a real project, given it now changes real cost totals rather than just flagging zero-cost gaps.
- Push `pipeline-optimize` to `origin` (was local-only as of the 2026-09-21 entry) so Mail can see today's work — done at the end of this session, see the commit this entry's changes landed in.

---
