## [morning] Claude — session with Makham (merge conflict resolution)

**Work done:**
- Resolved a diverged-branch merge conflict on `main` (local vs origin) in this repo.

**Files changed:**
- `json_แก้ไขแล้ว/42บ้าน_เล็ก_2ชั้น_18/` (whole house folder, 12 files) — moved to `wait_for_ทิ้ง/42บ้าน_เล็ก_2ชั้น_18/`. Makham had deleted this house locally (uncommitted) while origin still had it with newer edits from a teammate; per Makham's call, parked the house in `wait_for_ทิ้ง/` instead of hard-deleting, so nothing is lost if it's needed again.
- `json_แก้ไขแล้ว/26บ้าน_เล็ก_1ชั้น_07/บ้าน_เล็ก_1ชั้น_07_หน้า25_view1_section_footing_spread.json` — merged two conflicting versions: kept Makham's JSON shape (`element_id: null` + separate `element_name_assigned`), kept the teammate's rebar numbers (F0.80 main_bar count 5 not 8; F1.00/F1.50 `depth_m` 1.2 not 1.5; F1.00 gained a `main_bar` block it was missing). Per Makham's rule: when a teammate edited real drawing data, their numbers win; format/schema stays Makham's.
- `CLAUDE.md` in `Constistant` repo (separate repo, same session) — unrelated merge, two work-log entries from Makham and Ton both appended in the same spot; kept both, ordered by commit time.

**Impact/precautions:**
- `wait_for_ทิ้ง/42บ้าน_เล็ก_2ชั้น_18/` is a holding area, not deleted — someone should decide later whether to actually discard it or restore it.
- The footing-spread merge changes real rebar counts/depths for house 07's F1.00 and F1.50 footings — anything already computed off the old (wrong) local numbers needs a re-check.

**Left pending / to continue:**
- Decide the final fate of `wait_for_ทิ้ง/42บ้าน_เล็ก_2ชั้น_18/` (discard for real, or restore).

## [afternoon] Claude — session with Makham (format-fix incident affecting Ark's checked houses, verification, apology sent)

**Work done:**
- Makham decided to bring house 42 (`42บ้าน_เล็ก_2ชั้น_18`) back to `json_แก้ไขแล้ว/` rather than leave it parked — keep Ark's data, reformat to Makham's JSON shape. Investigation started to confirm no one had actually re-edited that house's data first (still open, see pending below).
- While tracing house 42's history, found the earlier merge commit `be9ad80` ("แก้ระบบ") — a repo-wide JSON pretty-print pass — had touched 343 files under `json_แก้ไขแล้ว/`, including 16 files across 3 houses (24, 26, 27) that Ark had just finished checking (commits `35e0d70`/`9f6f141`/`d036bf2`).
- Ran a deep semantic diff (Node script, normalizes key order and treats `element_id:"name"` vs `element_id:null`+`element_name_assigned` as equivalent) across all 343 files, then specifically the 16 files Ark touched: **0 real data differences** — every rebar count, dimension, and note is byte-identical to Ark's originals. Only the JSON formatting shape changed.
- Makham asked Ark to hold off on push/pull and save/send his local copies of houses 24, 26, 27 before doing anything else, as a precaution while this got sorted.
- Wrote and published an incident note + formal apology letter (Thai) as an artifact, from Claude and Makham jointly, explaining what happened and apologizing for the process (running a repo-wide format pass without checking whose in-progress/just-finished work it would touch), not for data loss (verified there wasn't any).

**Files changed:**
- None in this repo this session — investigation and verification only, no code/data touched. The apology artifact lives outside the repo (published as a Claude Artifact, not committed here).

**Impact/precautions:**
- Confirms the earlier concern in the morning entry above (`be9ad80`'s repo-wide pass) did not corrupt Ark's data, but it did run without asking him first — the actual issue was process, not correctness. Future format-only passes across `json_แก้ไขแล้ว/` should check for other people's fresh commits first, or ask before running broad.

**Left pending / to continue:**
- Still need to confirm no one re-edited house 42's data after Makham's original deletion (`35f6286`, 2026-08-25) before restoring it from `wait_for_ทิ้ง/` back to `json_แก้ไขแล้ว/` — reason for the original deletion was genuine (no beam floor-plan sheet in that house's drawing set, can't teach the beam-plan subtask from it).
- Waiting on Ark's reply/backup before anyone touches `main` further.

---

## [late night, carried into early morning] Claude — session with Makham (GPU/model research wrap-up: scheduled shutdown, teaching PDF)

**Context:** Continuation of the previous night's long GPU/model-selection research session (full detail already logged in `2026-08-25.md`'s [19:30-01:30] entry). This entry covers the two remaining small tasks from that session plus a follow-up expansion.

**Work done:**
- Set a 60-minute scheduled shutdown on Makham's dev machine (`shutdown /s /t 3600`) at his request, as a safety timer — verified it actually took via the Windows System event log (event ID 1074, logged 01:26:36).
- Wrote a PDF explainer for Makham, "AI ในตลาด" (`สอน_AI_ในตลาด_2026-08-26.pdf`), covering: what a VLM model and its "brain" (LLM backbone) are, explained with zero jargon; the plan_beam 0%-recall investigation and its real cause (answer length causing one-bad-token JSON collapse, not image downscaling — visually proven with a real before/after crop comparison from a training image); every non-Qwen 100–250B model candidate checked with the reason each was rejected (Qwen backbone hidden inside InternVL3/InternVL3.5, non-commercial licenses, no working fine-tune path); how any resulting model plugs into Constistant's existing OpenAI-compatible endpoint with zero code changes; and live GPU rental pricing.
- Makham asked for more depth ("ขอละเอียดกว่านี้"); rewrote the PDF from a condensed 5-page version to a 13-page one — added the full investigation narrative with embedded real crop images, all 8 rejected/surviving model candidates (not just the 2 survivors), a live pricing table, and a prioritized next-steps list.
- Rendered via headless Edge (`msedge.exe --headless --print-to-pdf`), the same approach `tools/md_to_book_pdf.py` uses elsewhere in this repo — required for correct Thai text shaping (vowel/tone-mark stacking), which a non-browser PDF library doesn't handle.
- Also published the same content as a Claude Artifact (web page) for easy sharing/reading without opening a PDF.
- Later in the same session, Makham asked to cancel the shutdown timer — ran `shutdown /a`, confirmed it actually cancelled by re-running `shutdown /a` and getting error 1116 ("no shutdown was in progress") back.

**Files changed:**
- `workmen's_diary/สอน_AI_ในตลาด_2026-08-26.pdf` (new, 13 pages, ~1.2MB) — not committed to git yet as of this entry.
- No `Training` or `Constistant` source files touched; this was research + a teaching artifact only.

**Impact/precautions:**
- The PDF/artifact content reflects a point-in-time snapshot of vast.ai spot pricing and GitHub issue statuses (both checked live on 2026-08-26 ~01:00–01:36) — re-verify before acting on any specific price or "this framework has a bug" claim if reading this much later.
- The scheduled-shutdown timer was set then later cancelled within the same session — the machine did not actually shut down.

**Left pending / to continue:**
- None of the GPU-research recommendations (splitting plan_beam into per-grid-zone sub-answers, trying `FINETUNE_VISION=1`, a zero-shot Thai-read test before committing to GLM-4.6V or Llama-4-Scout) have been acted on yet — see the original [19:30-01:30] entry in `2026-08-25.md` for the full technical reasoning behind each.

---

## [Claude] Dictionary ไทย→อังกฤษ แทรกใน prompt (DIP) — ข้อ 1 ของอาจารย์

**คำสั่ง:** "ผมว่าแทรกศัพท์ไปเลย ได้ลงมือทำเลย" — DIP = แทรกศัพท์ ไม่แปลทั้ง prompt

- ขุดศัพท์ไทยจาก GT จริง 2,171 ไฟล์ (ไม่ได้นั่งเดา) → นับความถี่ก่อนเลือกลงตาราง
- `_common.md` เพิ่มหัวข้อ "Thai → field glossary" 3 ตาราง: คำ→`element_type` / คำ→field / หน่วย+ตัวย่อ
- วางไว้ชิดกฎ §0.7 "ห้ามแปล label ที่พิมพ์บนแบบ" — กัน glossary ทำให้โมเดลเริ่มแปลของที่ห้ามแปล
- `pass0_classify/prompt.md` เพิ่มตาราง "ชื่อในtitle block → subtask" จาก sheet_name จริง (ผังบริเวณ/แปลนโครงหลังคา/รูปด้าน/แบบขยาย/สารบัญ ฯลฯ)
- `build_dataset_t03.py`: glossary คั่นด้วย HTML comment → ตัดออกเฉพาะ `gridline`

**ทำไมต้องตัด gridline ออก:**
- ใส่ทุก subtask → seq ยาวสุด 44,606 → 45,592 → ต้องดัน MAX_LENGTH 47,104 → 48,128 = VRAM ขยับตาม (เหลือ margin 4 GB อยู่แล้ว)
- วัดแล้วพบตัวยาวสุด 8 อันดับแรกเป็น `gridline` ล้วน — และ gridline หาแต่เส้นกริด ไม่แตะ element_type/เหล็กเลย
- ตัดออก → seq 44,607 = เท่าเดิม, **MAX_LENGTH/VRAM ไม่ต้องแตะ**

**ตรวจแล้ว:** rebuild 1,108 ตัวอย่าง — gridline 0/49 มี glossary, ที่เหลือ 1,059/1,059 มีครบ, ไม่มี `{{GLOSSARY}}` หลุด

**⚠️ กระทบข้อมูลเทรน (rule 2):** `train/val/test.jsonl` ถูก regenerate ใหม่ — สร้างจาก GT ได้เสมอ ไม่ใช่การลบทิ้ง แต่ jsonl ชุดเก่าหายไปแล้ว (รอบนี้กิน `strip_assigned` + GT ที่แก้ under att1235 เข้าไปด้วย ซึ่งเป็นงานค้างที่ต้องทำก่อนเทรนอยู่แล้ว)

**เหลือ:** `infer_house_t03.py` อ่าน prompt จาก jsonl → ได้ glossary อัตโนมัติ ไม่ต้องแก้ · ยังไม่รู้ว่าช่วยจริงแค่ไหนจนกว่าจะเทรนรอบหน้า

---

## [Claude] สายพาน v2 ลงมือรอบแรก (มะขามสั่ง GO) — pass 0/1 เดิม, 1.5/2.4/2.5/3 ใหม่, pass3เก่า→pass4

- เฟส A: `pass3_unused/`→`pass4_unused/`, `PASS_OF` 3→4, `pass_design.csv` +คอลัมน์ `pass_v2` +4 แถว pass ใหม่
- เฟส B (pass 1.5): `cv_scan.py` ใหม่ — เลขกำกับ #n คงที่ (เรียงชนิด→แถว→ซ้ายขวา), ภาพมาร์ค Set-of-Mark, hint text+กฎกันหลอน 4 ข้อ, โหมด `--manifest <workroot>` เดิน 4 โฟลเดอร์ผังจาก organize.py → เขียนลง `cv/`
- เฟส C: `apply_arm()` + `--arm 2|2.4a|2.4b --cv-dir` ใน infer_house_t03.py (unit test ไม่ใช้ GPU ผ่าน; ผลแยกโฟลเดอร์ `__arm*` กันทับ) — ยิงจริงรอเครดิต Vast
- เฟส D (pass 2.5): `--pass25` self-harvest + provenance `self_harvest_points`
- เฟส E: `tools/merge_guard.py` บังคับ "ห้ามลบ" ด้วยโค้ด (stub + `dropped_by_pass3`) + โครง prompt `pass3_takeoff/prompt.md` (ยังไม่เคยรันกับโมเดล)
- บั๊กจับได้ด้วยตา 2 ตัว (unit test มองไม่เห็น): ป้ายเสาโดนป้ายฐานรากทับ / ป้ายทับมาร์ค F1 ที่พิมพ์บนแบบ → ย้ายตำแหน่งป้าย
- self-harvest เกณฑ์แรกผิดข้าง (seed 0.85 ไม่มีวันเกิด — ข้ามซีรีส์ได้แค่ 0.72-0.80) → กลับเป็น seed หลวม/match เข้ม 0.90: บ้าน_ใหญ่_2ชั้น_08 15→16=GT, บ้าน_เล็ก_2ชั้น_19 17→18=GT, eyeball เป็นฐานรากจริงทั้งคู่; เพดานจริง: กู้ได้เฉพาะชนิดที่คลังจับติดแล้ว (บ้าน10: อีก 16 ตัวคนละชนิด — ถูกต้องที่ +0)
- แก้แผนผิด: grid master มีแต่ `pos_m` ไม่มี pixel → hint ใช้แถวเชิงลำดับแทน g≈
- graphify: CLI ไม่มีในเครื่อง (command not found) — ข้าม
- เหลือ: ยิง 3 แขนบน GPU · เติมคลัง 19 หลัง (คนคัด) · commit
