# 2026-07-02

> This file did not exist on 2026-07-02 — the diary itself only started 2026-07-05 (see
> `README.md`). Created retroactively on 2026-08-04, solely to hold archival notes for files
> dated/created 2026-07-02, per Makham's instruction to file an archived doc's record against the
> date written in the file (or the file's creation date if none is written), not the date it was
> archived. No real work from 2026-07-02 is otherwise recorded here.

## [retroactive note, added 2026-08-04] Claude — archival record for 3 files dated/created this day

Not a real-time entry. Added while cleaning up `No_touch_box/` docs — see `2026-08-04.md` for
that real session.

**Files archived:**
- `No_touch_box/vast-template/README.md` + `onstart.sh` → `wait_for_ทิ้ง/No_touch_box/vast-template/`
  (both first added 2026-07-02, confirmed via `git log --diff-filter=A --all`)
- `No_touch_box/raw/image/บ้าน_เล็ก_1ชั้น_01/claude_output_01/_inventory_pass_pilot.md` and
  `_pilot_comparison_summary.md` → same path under `wait_for_ทิ้ง/` (dated 2026-07-02 in their own
  content, not just file metadata)

**What they were:**
- `vast-template/` — a GPU rental template for an RTX 3090 24GB instance, defaulting to
  `Qwen3-VL-8B-Instruct` and deferring the real model choice to "King's sign-off" — a
  decision-making process and a model that don't match what actually happened. The real tuning
  (t01/t02, see `tune_ai/`) was run by Makham + Claude directly, on a rented RTX PRO 6000 96GB,
  landing on `Qwen3.6-35B-A3B` (t01, the one that worked).
- The pilot docs — the first real Claude-vs-Qwen comparison, 4 pages of house 01 (plan/section/
  section/BOQ). Explicitly marked in its own header as a draft never reviewed, not usable as
  ground truth directly. Found early evidence of `main_bar_type` misreading DB↔RB systematically
  on one page while the same symbol read correctly for stirrups on the same page — this exact
  finding is now lesson #6 in `No_touch_box/CLAUDE.md`, still cited there today (path reference
  in that doc updated to the new archive location in the same 2026-08-04 session).

**Why archived, not deleted:** `vast-template/` documents a process nothing else in the repo still
follows (safe to delete for real, see `ARCHIVE.md`). The pilot docs are the origin of a still-cited
lesson — kept for that reason, not meant to be deleted.

---
