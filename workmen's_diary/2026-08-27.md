# 2026-08-27

## [00:02] makham — house 37 GT correction (reconstructed from git log, not a live session)

**Work done:**
- `1d01d98` "บ้าน 37 final" — house 37 (บ้าน_เล็ก_2ชั้น_13): gridline fix, beam floor1/floor2 view corrections, footing-beam section fix, and `หน้า26_section_slab_stair.json` deleted (156 lines — likely folded into another page or found invalid).

**Note:** Reconstructed from `git log` only, no session transcript available.

---

## [12:18–13:00] Claude Opus 5 (1M context) — graphify knowledge-graph setup

**Work done:**
- Built a knowledge graph over `tools/` (125 nodes, 226 edges, 11 communities) via graphify.
- Wired graphify into Claude Code: `CLAUDE.md` now instructs sessions to run `graphify query/path/explain` before falling back to raw grep/read (same pattern already used in the `Constistant` repo).
- `.claude/settings.json` — added PreToolUse hooks routing search/read through the graph.
- `.gitignore` — excludes machine-local interpreter paths and the rebuild cache.
- Second commit (13:00) expanded graph coverage to `tune_ai/` (t01–t03).
- Also added `tune_ai/t03/pass3_unused/extract.md` (Pass 3 prompt template) as a side addition in the same commit.

**Files changed:** `graphify-out/graph.json`, `graph.html`, `GRAPH_REPORT.md`, `.claude/settings.json`, `CLAUDE.md`, `.gitignore`.

**Impact/precautions:**
- This is why an earlier session on 2026-08-26 got `graphify: command not found` — it wasn't installed in this repo yet at that point. Sessions from now on should have the CLI available; if `graphify` still isn't found, check whether the install is machine-local (per-user PATH) rather than repo-checked-in.

---

## [18:58] makham — house 50 milestone (reconstructed from git log, not a live session)

**Work done:**
- `f8e2ab8` "[50 หลัง]" — house 50 (บ้าน_เล็ก_1ชั้น_14) newly extracted: `_scope.json`, gridline, notes, footing plan (spread + pile views), seismic zone pages — 18 files, 1213 insertions, net-new extraction (not a correction pass).

**Impact:** **Milestone — 50th house now has raw JSON ground truth.**

**Note:** Reconstructed from `git log` only.
