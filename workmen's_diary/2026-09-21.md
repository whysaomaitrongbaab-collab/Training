# 2026-09-21

## [session, exact clock time not logged] Tonton (Claude Code session)

**Work done:**
- Started planning "pipeline optimization" work: define the pipeline's expected data contract first (ordered, correct), then make AI extraction results conform to it — instead of the past pattern of reactively patching the pipeline every time a new AI result shape shows up.
- Before writing any code, checked git history by author (`tonton888` vs `whysaomaitrongbaab`) across all branches, per Ton's explicit request to always warn when duplicate work is about to happen between him and Mail.
- Found Mail had already done closely-related work on `origin/Presentation-demo` on 2026-09-17 (commits `00d2f65`, `2defb6f`, `500f346`): a full gap-analysis doc `missing.md` (what `pipeline.js`/`computeBOQ()`/`computeBBS()` need vs. what raw JSON actually provides — column/vertical-element height has no source field at all and silently defaults to 3.0m; `computeBOQ()` had no confidence gate at all, unlike `computeBBS()`), a `pipeline-detail.md` writeup of the pipeline's current formulas, and a code fix flagging `needs_review` instead of silently guessing zero for missing `width_mm`/`height_mm`/`main_bar_*`.
- Attempted to bring that work onto Ton's `pipeline-optimize` branch (created off `tonton/trial-and-error`). A full-branch merge produced 16 conflicting files (including `pipeline.js`, `schema.js`, `raw-extraction-adapter.js`, `raw-extraction-import.js`) — too risky to resolve blindly, aborted. A narrower cherry-pick of just Mail's 3 commits also conflicted, because those commits depend on an entire `formula-registry` subsystem (`resolveParam`/`resolveFormula`/`fp()`/`fx()` in `pipeline.js`) that exists on `Presentation-demo` but was never merged onto `tonton/trial-and-error` — `tonton/trial-and-error`'s unique commits (63 total) are almost entirely Purson-worker/vast.ai tooling (`server/purson-worker/*`), not app pipeline logic, so it had fallen a long way behind the real pipeline development line.
- Resolved by **deleting and recreating `pipeline-optimize` branched directly off `origin/Presentation-demo`** instead of `tonton/trial-and-error`. Zero conflicts; the branch now includes Mail's `missing.md`/`pipeline-detail.md`/fix as its starting point. `tonton/trial-and-error` itself was left untouched.
- Produced a Mermaid flowchart summarizing the whole `runPipeline()` data flow (BOQ → BBS/Schedule/Resources → Readiness, localStorage keys, `PIPELINE_EVENT` fan-out to feature tabs, `ElementProgress` re-apply) with the two known gap points (no vertical-height field, `computeBOQ()` missing a confidence gate) highlighted, to give a shared mental model before continuing the gap-fix work from `missing.md` item 4 (still open: fix `prismaticLengthM()` to use a real height field when present instead of the 3.0m default, extend `assessSpecReadability()`/the confidence-gate pattern to `computeBOQ()`, change `|| 0` fallbacks to "skip + flag").

**Files changed:**
- No app code changed in `Constistant` this session (investigation + branch setup only).
- Local git branch `pipeline-optimize` in the `Constistant` working copy: deleted (had no unique commits, safe to drop) and recreated from `origin/Presentation-demo` (commit `cceb0c8`). Not pushed to origin — local only.

**Impact/precautions:**
- `pipeline-optimize` is currently identical to `Presentation-demo`, which is itself unmerged to `main`. Anyone continuing this work should know it is **not** based on `tonton/trial-and-error` anymore, and `tonton/trial-and-error`'s Purson-worker tooling has not been reconciled into this line.
- `boq-summary.js` on `Presentation-demo` (and therefore `pipeline-optimize`) has a `renderPlausibilityBanner()` referencing `PLAUSIBILITY_METRICS` from `js/shared/plausibility.js` (formula-registry feature) — works fine here since that module is present on this branch; only breaks if someone cherry-picks Mail's 3 commits onto a branch that lacks formula-registry, as was attempted and abandoned earlier in this session.

**Left pending / to continue:**
- The actual pipeline fixes from `missing.md` §4 have not been implemented yet: (1) source a real vertical-height field for columns/vertical elements instead of the 3.0m default, (2) extend the BBS-style confidence gate to `computeBOQ()`, (3) change silent `|| 0` fallbacks (`width_mm`/`height_mm`/`main_bar_dia_mm`/`main_bar_count`) to skip-and-flag.
- Whether/how `tonton/trial-and-error`'s Purson-worker tooling should reunite with `pipeline-optimize`/`Presentation-demo`, and whether `Presentation-demo` merges to `main`, are open decisions for Ton/Mail/King — not decided or acted on this session.

---
