## [afternoon] Claude — session with Makham (research only, per explicit instruction — nothing implemented, no code touched)

**Context:** มะขามประชุมกับอาจารย์วันนี้ ได้ 4 ประเด็นมา สั่งชัดว่า "ยังไม่ต้องทำ แค่หาข้อมูลลง workmen diary พอ". Below is research findings only — each item ends with what's still an open question for มะขาม/อาจารย์ to decide, not a recommendation to build yet.

---

### 1. Thai→English dictionary for prompts (prompt is English, model output is Thai-domain)

**Current state, verified by reading the actual prompt files:** every prompt in this project (`No_touch_box/Prompt/**/prompt.md`, `tune_ai/t03/_common.md`, `pass0_classify/prompt.md`, `primary_rawjson_schema.md`) is written in English, with an explicit standing rule (`_common.md:93,106`): *"Thai stays Thai. Never translate a printed [label]... the drawing's own alphabet — Thai ก/ข/ค stays Thai."* So today there is no dictionary layer at all — Thai text from the drawing is preserved verbatim in the output, and the English prompt just describes *what field* to put it in.

**Matching published technique found:** **Dictionary Insertion Prompting (DIP)** — arXiv 2411.01141 (2024), "Dictionary Insertion Prompting for Multilingual Reasoning on Multilingual LLMs". Inserts short English gloss/definition entries for key non-English terms **directly into the prompt** (not translating the whole input), giving an English-centric LLM explicit lexical grounding before it reasons. This is the closest existing research to what the teacher described — a Thai construction-term → English glossary injected alongside the prompt, distinct from the "never translate the drawing text itself" rule already in place (glossary sits *beside* the raw Thai as a reasoning aid, doesn't replace it in the output).

**Counter-finding worth knowing before building this:** a separate study found translating the **whole prompt** into English can score **~8% lower** than a native-language prompt, because translation loses context the native phrasing carried. So the safe version of this idea is DIP's *targeted term-glossary insertion* (a handful of `เสา = column`, `คาน = beam`, `ฐานราก = footing`, `เหล็กปลอก / ป. = stirrup`, `ตอม่อ = pedestal/tie-beam`, `DB/RB` rebar-type codes, etc.), not a wholesale prompt translation pass.

**Open question for อาจารย์:** which problem is the dictionary meant to solve — (a) helping the model map a Thai term it reads on the drawing to the *correct English schema enum* (`element_type`, `discipline`, etc.) — this is what DIP targets — or (b) something else (e.g. output i18n)? Current prompts give zero worked example of a glossary today, so this needs a decision before writing one.

---

### 2. pattern_recognition.py + cv2 template matching

**Searched, does not exist:** grepped/globbed `Constistant`, `Training` (incl. `No_touch_box`, `tune_ai/*`), and the sibling KU project `ku_project_jop` (incl. its full `git log --all` history) for `pattern_recognition` / `template_match` / `matchTemplate` — **zero hits anywhere, including deleted files.** This is a new idea from today's meeting, not a script to resurrect.

**Read the linked tutorial** (docs.opencv.org/3.4.7, API unchanged through 4.x): `cv2.matchTemplate(image, template, method)` slides the template over the image and returns a similarity map; 6 methods — `TM_SQDIFF`/`TM_SQDIFF_NORMED` (take the **min**), `TM_CCORR`/`TM_CCORR_NORMED`, `TM_CCOEFF`/`TM_CCOEFF_NORMED` (take the **max**, `_NORMED` variants are 0–1 and comparable across images). `cv2.minMaxLoc()` finds the single best match; for **multiple** instances of the same symbol on a page, threshold the similarity map (~0.8–0.9) instead of taking the single best.

**Limitations researched (not in the old tutorial, found separately):** plain template matching is **not rotation/scale invariant** — a symbol printed slightly bigger/smaller or rotated won't match well. Workarounds (a rotated+scaled template bank) are expensive and still weaker than feature-based or deep-learning detectors for that failure mode ([Medium: rotation/scale-invariant template matching](https://medium.com/@coders.stop/template-matching-beyond-basics-rotation-and-scale-invariant-detection-2ae78d8fa190), [arXiv 1907.01150](https://arxiv.org/pdf/1907.01150) multi-scale variant).

**Directly relevant domain research found:**
- **SkeySpot** (arXiv 2508.10449, 2025) — service-key/symbol detection on electrical layout construction drawings. Uses **YOLOv8** (deep learning), explicitly because classical template matching "struggles with variations in symbol appearance, rotation, scale, and image quality" common in real drawing sets and needs a template per variant.
- General floor-plan symbol-detection literature (search summary): normalized-correlation template matching (threshold ~0.9) **does** work well when the symbol is a small, fixed, exactly-repeated graphic (a legend icon, a north arrow, a grid bubble) — much weaker on hand-drawn or stylistically-varying elements.

**How this could plausibly fit our pipeline (observation only, nothing decided):** the thing this project currently struggles with by *manual* pixel-measurement is exactly the small-fixed-repeated-symbol case template matching is good at — see the 2026-08-24 diary entries where Makham hand-measured grid-row y-pixel positions (e.g. `y=768/1085/1404/1618 px`) directly off the PNG to correct a gridline extraction. A grid crosshair/tick template match could auto-locate those pixel positions as a classical-CV cross-check next to the VLM's gridline JSON, rather than replacing the VLM on variable content like beam/column marks (which SkeySpot's finding says template matching is the wrong tool for).

**Local precedent for this kind of hybrid, already in a sibling project:** `ku_project_jop` (เสาพัง, T-BHI bridge inspection) already combines classical CV + deep learning — `crack_scale.py` uses **ArUco marker detection** (`cv2.aruco`) for pixel→mm scale calibration, alongside a YOLOv8-seg model (`vision.py`) for the actual damage detection. Same "classical CV for precise fixed measurement, DL/VLM for variable content" split — just a different classical technique (marker detection, not template matching).

**Open question for อาจารย์:** confirm whether "pattern_recognition.py" refers to a file/example from a lecture (not currently in any repo we have access to) or is meant as a from-scratch build — and whether the target use is grid/symbol localization (matches the evidence above) or something else like page-orientation/logo detection.

---

### 3. max_frames currently 768 — where it actually is, and whether it's the right knob

**Traced the literal number:** `tune_ai/t03/ผล_t03_run/lora/processor_config.json` → `video_processor.max_frames: 768`. Confirmed via [GitHub QwenLM/Qwen3-VL#852](https://github.com/QwenLM/Qwen3-VL/issues/852) this is the **well-known hardcoded default** in Qwen2.5-VL/Qwen3-VL's **video** frame sampler (that issue is literally about this same constant — for a 2-hour video, 768 frames ≈ 1 frame every 9.4s, and it asks for it to be configurable via a `QWEN_VL_MAX_FRAMES` env var).

**Why it doesn't apply here:** grepped the whole repo for `max_frames` — the only hit is this one auto-saved config file. Our training/inference code (`train_t03.py` and every prior round) sends every drawing page as a still **image** content item (`{"type": "image", "image": PIL.Image...}`), never as `"video"` — the video processor / `max_frames` path is never invoked. **Raising this value would have zero effect on this project as currently built.**

**The two knobs that actually govern "how much the model reads at once" here** (both in `train_t03.py`, confirmed by reading the file):
- **`MAX_PIXELS = 5120 * 1024`** — visual-token budget *per image* (~5,120 tokens/image). This is the one t01 got silently wrong (defaulted to 512px/~266 tokens, rule_of_tune #12) and tanked accuracy — proven load-bearing.
- **`MAX_LENGTH = 32768`** — total sequence length (prompt + all images + JSON answer), already raised this round from t01/t02's 24,576 because gridline examples (4 images) measured ~26,791 tokens and were getting truncated mid-JSON.

**Researched the model's real ceiling:** base model `unsloth/Qwen3.6-35B-A3B` has a **native 262,144-token context window** ([Unsloth docs](https://unsloth.ai/docs/models/qwen3.6), [HF model card](https://huggingface.co/unsloth/Qwen3.6-35B-A3B)), extensible to ~1,010,000 via YaRN. So `MAX_LENGTH=32768` is nowhere near the *model's* limit — the actual ceiling right now is **GPU VRAM on the rented single card**: t03 already had to cut LoRA rank 32→16 to fit 32,768-length sequences in a 95GB GPU, and even then the eval step OOM'd from fp32 logit upcasting at that same length (both documented in `train_t03.py`'s own comments, 2026-08-24). Raising `MAX_LENGTH` further (e.g. more gridline images per example) is legitimately possible against the *model*, but costs more VRAM per step against the *hardware* already near its limit.

**What to bring back to อาจารย์:** clarify whether "max_frames" meant loosely "how much can the model read at once" (→ real answer: `MAX_LENGTH`, already raised once this round, model has far more headroom than the rented GPU does) or genuinely video frame sampling (→ not applicable, this project never sends video, and touching `processor_config.json`'s `max_frames` would do nothing).

---

### 4. Increase Qwen model size (bigger B)

**What's actually in use, checked against the training scripts:** t01/t03 use `unsloth/Qwen3.6-35B-A3B` (35B total / 3B active, MoE); t02 used a different, older-generation family, `Qwen3-VL-30B-A3B`.

**Qwen3.6 family ceiling, researched:** open-weight Qwen3.6 has exactly **two** sizes — **27B dense** and **35B-A3B MoE** (already what we use). The only thing bigger in the Qwen3.6 line is **Qwen3.6-Max-Preview**, which is **closed-weight, API-only** — not downloadable, not fine-tunable. So within this exact model family, **there is no bigger open-weight option to move to** — 35B-A3B is already the ceiling.

**Checked the older Qwen3-VL family (what t02 used) for a bigger open step:** sizes are 2B/4B/8B/32B dense, 30B-A3B/**235B-A22B** MoE. The next step up from t02's 30B-A3B is **Qwen3-VL-235B-A22B** — but researched hardware cost is a different order of magnitude: **~513GB+ VRAM for inference alone (8×80GB GPUs)**, and LoRA fine-tuning ~50% more on top (**~770GB+**) per current pricing guides ([Spheron](https://www.spheron.network/tools/gpu-recommender/Qwen/Qwen3-235B-A22B/), [Novita](https://blogs.novita.ai/qwen3-vl-235b-a22b-vram-how-to-avoid-huge-hardware-costs/)). That's a jump from today's single rented GPU (~$1.1/hr, one RTX PRO 6000 96GB) to an **8-GPU multi-node rental** — multi-GPU distributed training, not "rent one bigger card."

**Bottom line to bring back:** "increase B a lot" isn't a free dial here. Inside the model family already in use there's nowhere bigger (open-weight) to go. A genuinely much-bigger open VLM (235B-A22B) means ~8x the GPUs and a correspondingly much higher $/hr plus multi-GPU training complexity neither t01/t02/t03 have needed so far — a real budget/scope decision for อาจารย์+มะขาม to make explicitly, not a config tweak.

---

**Left pending / to continue:**
- All 4 items above are research only — no code, prompt, or config changed today.
- Item 1 needs อาจารย์'s clarification on which problem the glossary targets before any glossary file gets written.
- Item 2 needs อาจารย์'s clarification on whether "pattern_recognition.py" is a lecture artifact we don't have, before deciding what to build.
- Item 3's finding (max_frames ≠ our real bottleneck) should be relayed back to อาจารย์ — worth confirming what was actually meant before touching anything.
- Item 4's finding (35B-A3B already the family ceiling; next real step up needs multi-GPU) should also be relayed back before committing to any rental budget change.

**Sources referenced:**
- https://docs.opencv.org/3.4.7/d4/dc6/tutorial_py_template_matching.html
- https://arxiv.org/pdf/2411.01141 (Dictionary Insertion Prompting)
- https://arxiv.org/pdf/2508.10449 (SkeySpot)
- https://arxiv.org/pdf/1907.01150 (multi-scale template matching)
- https://medium.com/@coders.stop/template-matching-beyond-basics-rotation-and-scale-invariant-detection-2ae78d8fa190
- https://github.com/QwenLM/Qwen3-VL/issues/852
- https://unsloth.ai/docs/models/qwen3.6
- https://huggingface.co/unsloth/Qwen3.6-35B-A3B
- https://www.spheron.network/tools/gpu-recommender/Qwen/Qwen3-235B-A22B/
- https://blogs.novita.ai/qwen3-vl-235b-a22b-vram-how-to-avoid-huge-hardware-costs/

---

## [evening] Claude — session with Makham: built tools/pattern_recognition.py (cv2 template matching, item 2 from this morning's research now IMPLEMENTED per Makham's direct order)

**Makham's revised goal:** not counting — **highlight/mark เสา/คาน/ฐานราก directly on the drawing image** (motivation: element recall is very low; this gives a visual ground-truth-ish overlay per page).

**Built (new files, nothing else touched):**
- `tools/pattern_recognition.py` — standalone, reads images only, writes only to `tools/pattern_out/`. CLI: `python tools/pattern_recognition.py <page.png>` → `<page>_marked.png` (footing=red boxes, column=green boxes, beam=blue translucent bands + count label). `--demo` = self-check on บ้าน 17 (asserts 14 footings on หน้า14).
- `tools/templates/` — template bank: `tpl_footing.png` (บ้าน17 F1A nested-square), `tpl_footing2.png` (บ้าน12 F2 tall-rect + 2 pile dots), `tpl_footing3.png` (บ้าน12 F1), `tpl_column.png` (บ้าน17 C1 junction box). Beam template is synthesized in code (2 parallel dark lines, gap 21px measured from บ้าน17 หน้า16 faces y=758-760/777-779).

**Method:** `cv2.matchTemplate` TM_CCOEFF_NORMED → threshold (footing 0.60 / column 0.65 / beam 0.70) → greedy NMS. Footing = multi-scale (0.7–1.2) over the whole template bank, NMS'd together. Column = single-scale only (multi-scale on the tiny 28x34 template inflated 14→21 detections with noise on บ้าน17 — reverted). Beam = short double-line template matched everywhere, hits merged into long runs via mask + MORPH_CLOSE + contours, runs < 100px dropped.

**Verified visually (eyeballed every marked overlay, both houses):**
- บ้าน17 หน้า14 (ผังฐานราก): **14/14 footings, 14 columns nested inside them, 0 beams** — zero false positives (nothing on text/title block/signature).
- บ้าน17 หน้า16 (ผังคานชั้นบน): 14 columns on real C1 junctions, 17 beam bands covering essentially the whole beam network (incl. stair-void projection beams), 0 footings.
- บ้าน12 หน้า20 (ผังฐานราก, cross-house test): **12/12 footings** — including both icon types; correctly-shaped box per type.
- บ้าน12 หน้า22 (ผังคานชั้นสอง): 13 columns, 19 beam bands, 0 footings.

**Two real cross-house findings (each drove a fix, same session):**
1. **F1 vs F2 are different SHAPES, not scales** — บ้าน12's F2 = tall rectangle + 2 pile crosses; multi-scale alone still missed all F1s (6/12). Fix = template bank (`tpl_footing*.png` glob, candidates merged before one NMS). This is the scale/appearance-variance limitation from this morning's research showing up on literally the second house tested.
2. **F2's tall-rect template false-fired on beam plans** (vertical beam through a column box ≈ same shape; 3 FPs on บ้าน12 หน้า22). Fix = drop any footing whose center falls inside a detected beam band — provably safe because real foundation plans detect 0 beams.

**Honest limits, recorded:**
- `BEAM_GAP=21px` is calibrated to this scan batch's DPI + 1:75 scale (`--beam-gap` knob exposed; no auto-scale until a house actually needs it).
- Only 2 houses tested, both same BMA "แบบบ้านยิ้ม" series. Other series (บ้าน_ใหญ่_*, different drawing offices) untested — 07บ้าน_ใหญ่_2ชั้น_01 has no plan_footing/plan_beam GT jsons to compare against yet.
- Minor: 1 doubled column box at (B,2) บ้าน12 หน้า20 (13 vs 12 true); บ้าน17 หน้า15 shows 19 columns (not individually verified — some may be doubles).
- Marks locate classes only — no element IDs/sizes/rebar (that stays the VLM's job). Tool is verification/overlay only; feeding results into prompts/training data would be a rule_of_tune Rule 2 action and was NOT done.

**Left pending / to continue:**
- Test on a non-BMA-series house once one has comparable pages (visual check is enough — no GT needed for an overlay eyeball).
- Decide with อาจารย์/Makham how to use the overlays: (a) human review aid when correcting rawjson, (b) recall cross-check vs model output, or (c) prompt grounding hint (option c = Rule 2 territory, needs explicit approval).
- Slab (S1 X-box) template deliberately not built — Makham scoped to เสา/คาน/ฐานราก only.

---

## [night] Claude — normalize ทั้ง 2 tree ให้ตรง primary_rawjson_schema (att1235)

**คำสั่ง:** แก้ `json_แก้ไขแล้ว/` + `rawjson_ยังไม่ได้แก้ไขโดนคน/` ให้ตรงสเปก, ย้ายของที่ทำให้ฮาลูซิเนตไป `wait_for_ทิ้ง/`
**รายละเอียดเต็ม:** `json_แก้ไขแล้ว/README.md` หัวข้อ 2026-08-25 + `primary_rawjson_schema_edit_log.md` แถวล่างสุด

**ต้นเรื่อง:** มะขามถามทำไม gridmaster บ้าน 20 กับ 28 หน้าตาไม่เหมือนกัน → สาวไปเจอว่าสเปกถูกแก้ 43 รอบใน 5 สัปดาห์ แต่ไม่เคยไล่ retrofit ไฟล์บ้านเก่า

**Root cause ที่เจอ (สำคัญสุด):** `tools/check_format.py` ตอบ **ALL CHECKS PASS** ทั้งที่มี `architecture` สะกดผิด 345 ไฟล์ — เพราะสเปกแก้ 3 รอบเมื่อ 08-21 แต่ checker ไม่ได้แก้ตาม → เพิ่ม 5 ด่าน (discipline vocab / `source_image` / notes one-off key / structural roof-framing ต้องเป็น `plan` / gridmaster ขาด 3 array)

**แก้ 485 จุด / 423 ไฟล์ ทั้ง 49 หลัง** (รอบ 08-24 ทำแค่ 11 หลัง) — parse ผ่านครบ 4,366 ไฟล์ 0 พัง:
- `discipline` `architecture`→`architectural` 345 + นอก vocab 16
- `roof_plan`→`plan` อีก 7 ไฟล์ (ปิดบั๊ก BOQ คานหลังคาหาย ที่ 08-24 ทำไม่ครบ)
- notes container →`sections[]` 22, ดูดคีย์เฉพาะกิจเข้า `notes{}` 20
- **parse `fc_ksc`/`fy_main_ksc`/`fy_stirrup_ksc` เข้า `notes{}` ได้จริง 62 ค่า/26 ไฟล์** จาก `sections[]` ของไฟล์ตัวเอง เก็บข้อความพิมพ์จริงใน `notes.parsed_as` — **ครั้งแรกที่ค่าพวกนี้ถึง consumer** (§4a บอกเองว่าไม่เคยถึงสักหลัง)
- ไฟล์ truncated `31.../หน้า25_plan_footing.json` (พังทั้ง 2 tree) — ปิดให้ parse ได้ เก็บ F5 ที่ครบ ทิ้งเศษ F10 ไม่เดาค่าที่หาย + warning
- `grid`/`dummy` ใน `grid_ref` 3

**ย้ายออก:** `สิ่งที่ต้องแก้.md` (1,220 บรรทัด) → `wait_for_ทิ้ง/stale_value_logs/` + README อธิบาย — บันทึกค่าเก่า→ค่าใหม่รายบ้านที่นั่งในโฟลเดอร์ training data = ช่องปนเปื้อนข้ามหลัง (โมเดลสกัดบ้าน 30 อ่านเจอตัวเลขบ้าน 01 แล้วหยิบผิดหลัง)

**จงใจไม่ทำ (ไม่ใช่ลืม):**
- gridmaster 25 ไฟล์ยังไม่มี `dimension_chains[]`/`unassigned_dimensions[]` — ต้องกวาดรูปใหม่ทีละหน้า, เติม array ว่าง = โกหกว่า "อ่านแล้วไม่เจอ" ตาม §4
- `notes{}` อีก ~76 ไฟล์ไม่มี `sections[]` ให้ parse
- ไฟล์ที่พิมพ์ SD-40 กับ SD-50 ในหน้าเดียว → ปล่อยว่าง ไม่เลือกให้เอง

**เจอระหว่างทาง ไม่ได้แก้:** `raw-extraction-adapter.js` ของ Constistant อ่านเฉพาะ `pattern:"plan"` — ฝั่งโค้ดไม่ต้องแก้แล้วเพราะ data ถูกปรับให้ตรงแทน แต่ `ADAPTED_PATTERNS` ยังไม่รับ `roof_plan` ถ้ามีบ้านใหม่แท็กผิดอีกก็หายเงียบเหมือนเดิม

**Left pending:** ยังไม่ commit (รอมะขามดูก่อน) · 25 gridmaster รอกวาดรูป — งานนี้เข้าคู่กับ `tools/pattern_recognition.py` ที่ทำเมื่อเย็น (template match หา grid tick ได้ pixel position จริง)

---

## [15:00-15:25] Claude — session with Makham: teaching PDF for today's work (ตอนที่ 4)

Makham said he felt he was falling behind ("ผมให้คุณทำงานหนักเกินจนผมโง่ละ") and asked for a
teaching document — simple language but detailed. Per the standing convention this is the
`*(teach mk).md` series (ตอนที่ 1-3 already exist; this is **ตอนที่ 4**).

**Written:**
- `workmen's_diary/2026-08-25(teach mk).md` (~49KB) + `.pdf` (15 หน้า, 1.9MB) via existing `tools/md_to_book_pdf.py`
- `workmen's_diary/img_teach_2026-08-25/` — 4 figures downsized to 1400px (บ้าน17 footing/beam overlays, บ้าน12 overlay, 3-template comparison strip)

**Contents (10 chapters, Thai, zero-background assumed):** recall explained from scratch → the 4 meeting
topics each answered with a verdict (2 usable / 2 not, with the reasoning shown) → template matching
taught from first principles (sliding window, the 6 TM_* formulas and why TM_CCOEFF_NORMED, threshold,
NMS 235→14) → walkthrough of what was built → **both bugs written up as reasoning exercises** (the wrong
first hypothesis on F1-vs-F2 included, since being wrong then checking the real image is the lesson) →
how it could plug into the recall problem (3 options ranked by risk, incl. that option (c) is Rule 2
territory) → honest unknowns → glossary → runnable commands.

**Two real fixes to `tools/md_to_book_pdf.py` (shared tool, both additive, backwards-compat verified):**
1. **Images in a .md silently never appeared in the PDF.** The HTML is written to a temp dir (deliberately —
   Chromium mishandles Thai/space paths on Windows), so every relative `![](...)` resolved to nothing and
   Chromium skipped it without erroring. Added `absolutize_images()` — rewrites relative `src` to a full
   `file://` URI via `Path.as_uri()` (which also percent-encodes the Thai folder name), leaves
   http/data/file/absolute alone, and prints a warning for a genuinely missing file instead of silently
   dropping it. Verified: 0 → 4 images embedded.
2. Added an `img` CSS rule (max-width 100%, centered, no page-break inside) — without it a 1400px figure
   would overflow the A4 text column.
   Regression-checked on a temp .md with no images: still renders fine.

**Also caught by eyeballing the generated PDF (not by any check):** the first template-comparison figure
had F2 cropped off at the bottom (I had hardcoded the canvas height at 260px while the resized F2 is
~440px tall) — which would have destroyed the exact point the figure exists to make. Regenerated with
height derived from the tallest template.

**Note:** standalone PDF only — **not** appended to `สอน_AI_อ่านแบบบ้าน_รวม_21-24กค.pdf`. That book's name
says July but ตอนที่ 3 was already appended to it; left the decision to Makham
(`python tools/md_to_book_pdf.py "<md>" --append-to "<book>"` does it, with automatic .bak).

**Left pending / to continue:**
- Decide whether ตอนที่ 4 gets appended to the combined book (and whether that book should be renamed, since it now spans ก.ค.-ส.ค.).
- Everything from the previous entry still stands (test on non-BMA series, test on non-structural pages).

---

## [15:30-16:00] Claude — session with Makham: item 3 (max token) followed through — 2 real findings, 1 bug fixed

Makham said "ทำต่อเลย" on meeting item 3. Rather than raising a number blind (t03 already OOM'd
twice doing that), measured what is actually binding first.

**New file: `tune_ai/t03/data_before_tune/measure_capacity.py`** — read-only, no GPU, no model load.
Computes per-image visual tokens straight from file dimensions (patch 16 × merge 2 → 1 token = 1024 px,
taken from the real `processor_config.json`) and estimates sequence length with the same chars/2.2
heuristic `_est()` in train_t03.py already uses. Compares several MAX_PIXELS settings side by side.

**Finding 1 — every training image is being downscaled, not some of them.**
At the current `MAX_PIXELS = 5120*1024`: **973/973 image instances (100%) exceed the cap.**
Smallest image in train.jsonl is 3307×2339 = 7,554 tokens; largest 4631×3473 = **15,707 tokens,
losing 67%** of its detail. This is the arithmetic-exact half of the measurement (no heuristic).
**This contradicts `t03_workflow.md` row 7**, which recorded "ภาพจริง 2339×1654 ต่ำกว่า cap 5,120
อยู่แล้ว = ไม่ย่อเลย" — no image of that size exists anywhere in the current train set.
**Stated honestly rather than asserted:** train.jsonl on disk is timestamped 22:13 while the
TEST_STEPS run was 22:00, i.e. the set was rebuilt after that check, so I cannot prove what
`train_ds[0]` was at the time. Recorded as "re-read what the assert prints next run; do not trust
the old 3,778 figure" rather than as a proven past error.

**Finding 2 — the guard written specifically to prevent a t01-class disaster could no longer catch one.**
train_t03.py's first assert is commented "ตัวอย่างแรก (ภาพเดียว)" and does
`_tok = (grid[:,1]*grid[:,2]).sum() // 4` then `assert _tok > 1000`. But `train_ds[0]` in the op04
dataset has **4 images**, and `.sum()` totals across all of them while the threshold is a
single-image one → **4× too lenient**. Verified by direct simulation: t01's exact failure level
(266 tokens/image) × 4 images = 1,064 > 1,000 → **the assert passes**. The one guard that exists
to stop the most expensive bug in this project's history would have waved it through.

**Fixed (train_t03.py, verification code only — not a training value):** divide by `_gt.shape[0]`
so the threshold is genuinely per-image, print the configured cap alongside the measured value so
they can be eyeballed against each other, and add a non-fatal warning when the measured value falls
more than 25% below the cap (the case where the collator shrinks images on its own to fit
max_seq_length — which the raw assert cannot distinguish from a healthy run). `ast.parse` clean;
both branches simulated. **Rule 2 warning was stated to Makham before editing.**

**Options costed, NOT applied (needs Makham's decision — changing these costs a GPU rental to validate):**

| MAX_PIXELS | still downscaled | est. max seq | MAX_LENGTH needed |
|---|---|---|---|
| 5,120 (current) | 973/973 | 34,854 | 36,864 |
| 7,680 | 292/973 | 44,607 | 47,104 |
| 7,680 + gridline 4→3 images | 292/973 | 37,049 | 39,936 |
| 16,384 (no downscale at all) | 0/973 | 71,504 | 75,776 |

Only **gridline** examples push the ceiling (36 of the 37 over-length ones use 4 images); every other
subtask sits well under. So "7,680 + drop gridline to 3 images" buys +32% resolution on the bulk of
the set for the smallest MAX_LENGTH increase — and dropping a gridline image is already the OOM
remedy train_t03.py's own docstring recommends.

**⛔ The real ceiling is VRAM, not the model** (model takes 262,144; we use 12%). Last round 32,768 at
r=32 OOM'd for real and had to drop to r=16, and eval still OOM'd on fp32 logit upcast. Raising
MAX_LENGTH without lowering r or renting a bigger card will OOM again — written into the workflow doc.

**Files changed:**
- `tune_ai/t03/data_before_tune/measure_capacity.py` — new, read-only measurement tool
- `tune_ai/t03/data_before_tune/train_t03.py` — guard fix only (per-image divisor + cap printout + sub-cap warning)
- `tune_ai/t03/t03_workflow.md` — new appendix with the measurement table, the contradiction against row 7, the options table, and the VRAM warning

**Left pending / to continue:**
- **Makham/อาจารย์ to pick a MAX_PIXELS option** — nothing raised yet, deliberately.
- Whichever is picked needs a `TEST_STEPS=5` run to confirm VRAM before a full run (rule_of_tune Rule 4).
- Re-read what the fixed assert prints on the next real run and correct `t03_workflow.md` row 7 with the true per-image figure.
- The chars/2.2 text estimate is unvalidated against the real tokenizer — the visual half is exact, the sequence totals are not.

---

## [16:00-16:20] Claude — session with Makham: "เพิ่ม VRAM" → measured first, raised resolution without changing cards

Makham ordered "งั้นก็เพิ่ม vram ซะ". Did the sizing math before acting, and the answer changed the plan.

**Finding: the VRAM increase is not needed for most of the gain.** Decomposed peak VRAM from the
one real measurement we have (t03's peak 87.5 GB @ seq 32,768, r=16, card usable 94.97 GB):
weights bf16 74.0 + LoRA/grad/optim 5.7 + **activation 7.8** — only the last term grows with
sequence length, and grows linearly (Unsloth uses flash attention, so not O(n²)).

| MAX_PIXELS | MAX_LENGTH | activation | peak | on the current 96GB card |
|---|---|---|---|---|
| 5,120 (was) | 32,768 | 7.8 | 87.5 | ✅ 7.5 GB spare |
| **7,680 (now set)** | **47,104** | **11.3** | **90.9** | **✅ 4.0 GB spare** |
| 7,680 + gridline 4→3 | 39,936 | 9.5 | 89.2 | ✅ 5.8 GB spare (OOM fallback) |
| 16,384 (no downscale) | 75,776 | 18.1 | 97.8 | ❌ 2.8 GB over → needs H200 |

**Applied to `train_t03.py` (Rule 2 warning given first):** `MAX_PIXELS` 5120→**7680**,
`MAX_LENGTH` 32768→**47104**, `LORA_R` unchanged at 16. Effect: images downscaled goes from
**973/973 (100%) → 292/973 (30%)**; the 615 3309×2339 pages that are the bulk of the set now train
at full resolution for the first time. Verified: `ast.parse` clean; simulated the script's own
top-3-longest assert with the new constants — longest is 44,606 < 47,104, **passes with 5.3% margin**.

⚠️ **VRAM margin narrows 7.5 → 4.0 GB.** `TEST_STEPS=5` is mandatory before any full run
(rule_of_tune Rule 4; last round OOM'd with 470 MiB free). Documented fallback order if it OOMs:
cut gridline images 4→3 first (peak 89.2), then roll MAX_PIXELS back — **never lower MAX_LENGTH
alone without lowering MAX_PIXELS**, that truncates labels mid-JSON (t01 §0.4 bug class).

**Live price check via `vastai search offers` (read-only, nothing rented):** H200 140GB cheapest
**$3.293/hr** (Virginia US) → **$17.45 per 3-epoch run** vs **$5.87** on the current RTX PRO 6000
96GB. **⛔ Hard blocker stated plainly: credit on record is $8.91 — not enough to finish even one
H200 run.** Money has to go in before the 16,384 path is possible at all. Said explicitly rather
than filed as a to-do (Day of Shame lesson 3). What 140GB would actually buy: no image downscaling
at all **and** LoRA r back to 32 (peak 103.4, 36 GB spare) — the two things t03 had to give up.

**Files changed:** `tune_ai/t03/data_before_tune/train_t03.py` (the two constants, with full
provenance + fallback order in comments), `tune_ai/t03/t03_workflow.md` (decision record, VRAM
decomposition table, live GPU prices, the credit blocker).

**Left pending / to continue:**
- **`TEST_STEPS=5` on a rented card to confirm 90.9 GB estimate before any full run — not done, and the estimate is arithmetic, not measured.**
- The chars/2.2 text estimate behind seq 44,606 is still unvalidated against the real tokenizer; the visual half is exact.
- Top up Vast.ai credit if the full-resolution (16,384 + H200) path is wanted.
- Nothing was rented. No money spent this session.

---

## [16:20-17:00] Claude — session with Makham: "section 0% — can pattern recognition fix it?" → diagnosed first, answer is no (and why matters)

Makham asked whether the pattern-recognition tool could rescue section's 0% id-recall. Started by
trying it, then caught myself doing it in the wrong order — **building a fix before diagnosing the
failure.** Went back and diagnosed. The diagnosis changed the answer completely.

**Root cause found: `infer_house_t03.py:218` scores with `set(gt_ids) & set(pred_ids)` — exact
string match, no normalization — and GT `element_id` mixes two incompatible kinds of name.**
Some are marks genuinely printed on the drawing (`B1`, `B3X`, `C1`, `F1A`, `RB1'`). The rest are
**descriptive names the annotator invented** to tell sub-details apart: `gate_front`,
`SP_at_bathroom`, `purlin_rafter_connection`, `door_lock_detail`, `ห้องนอน`, `ระดับพื้นอาคาร`.
No model can produce those strings from an image — there is nothing on the paper that says them.

**New read-only tool `tune_ai/t03/data_before_tune/measure_id_ceiling.py`** classifies GT ids by
regex and reports the achievable ceiling per subtask:

| subtask | ids | printed-on-drawing | ceiling | t03 actual | reading |
|---|---|---|---|---|---|
| plan_beam | 2,484 | 2,175 | **88%** | 0% | 🔴 genuine model failure |
| plan_slab | 283 | 261 | 92% | 20% | 🔴 genuine |
| plan_footing | 462 | 345 | 75% | 33.3% | 🟡 room to improve |
| schedule | 662 | 403 | 61% | 21.9% | 🟡 room to improve |
| **section** | 2,436 | 902 | **37%** | 0% | 🟠 **metric half-broken** |
| notes | 272 | 5 | 2% | – | 🟠 metric ~fully broken |

**On the test house specifically (บ้าน 08) the section ceiling is 1/23 = 4%** — it is a
single-storey house whose section sheets are almost all fence/gate/cleanout details, not
beam/column cross-sections. **A perfect model scores at most 4% there**, so the reported 0%
carries almost no information about model capability.

**The two 0% numbers are not the same kind of problem, and conflating them would waste a whole
tuning round:** plan_beam's 0% sits under an 88-93% ceiling → real failure, and the
pattern-recognition tool built earlier today targets exactly that. section's 0% is mostly the
metric → pattern recognition cannot help, because the problem is not seeing, it is that the
target string was never on the page.

**Pattern recognition was actually tried on section before concluding — recorded so nobody repeats it:**

| approach | บ้าน 17 | บ้าน 12 | บ้าน 01 |
|---|---|---|---|
| HoughCircles on the circled marks (B1)(B2) | **5/5 ✅ zero FP** | 3/20 ❌ | 0/18 ❌ |

Cause: section sheets have no shared layout across design offices — house 17 circles its marks,
house 01 prints underlined text `B1 1:20` with no circle anywhere. Auto-detecting the cross-section
boxes instead got 7 of ~14, because grid/extension lines cross the boxes so the contour stops being
a clean 4-gon. Rebar-dot counting inside hand-cropped boxes scored 1/5, but the crops were
eyeballed and at least one clearly missed its box, so that number tests my crops, not the idea —
left explicitly inconclusive rather than written up as a failure.

**Proposed fixes — NOT applied, they touch ground truth (Rule 1/2):**
1. Split the metric: report id-recall over printed ids separately from annotator-invented ones.
   *(script-only change, touches no GT — safe to do on request)*
2. Normalize before comparing (strip/upper) so `"B1 "` stops missing `"B1"`.
3. Change the section `element_id` convention (edits GT — needs explicit permission).

**Honest limit of the measurement:** `PRINTED_ID` is a regex heuristic. `ห้องนอน` gets classed as
"invented" although it *is* printed — it is a room label, not an element mark. So the ceilings are
a **conservative estimate**, not a proven value. Written into the workflow doc as such.

**Files:** `tune_ai/t03/data_before_tune/measure_id_ceiling.py` (new, read-only),
`tune_ai/t03/t03_workflow.md` (new section with both tables, the failed section experiments, and the caveat).

**Left pending / to continue:**
- Makham/อาจารย์ to decide on metric fix #1 and #2 — until then **no section number is interpretable**, including any future round's.
- plan_beam remains the real target; the pattern-recognition tool still needs the non-BMA-series and non-structural-page tests from the earlier entry.
- The rebar-dot idea is unproven, not disproven — worth one clean retry driven by auto-detected boxes if section ever becomes a priority.

---

## [17:00-17:25] Claude — teaching PDF (ตอนที่ 4) brought up to date; one stale claim corrected

The teaching doc written at 15:00 had gone stale within the same session — worth recording *why*,
because it is the same failure mode as the workflow doc's row 7 that this session already caught.

**A claim in it was wrong, not merely incomplete.** §2.3 ended with "`MAX_LENGTH` เพิ่มได้ แต่ต้อง
เช่าการ์ดใหญ่ขึ้น = จ่ายแพงขึ้น" — written before the VRAM decomposition was done. The measurement
an hour later showed 79.7 of 87.5 GB does not scale with sequence length at all, so +32% resolution
fits the existing card with 4.0 GB to spare and **no extra cost**. That sentence was in a document
Makham may show his teacher, so it was corrected rather than left for a later revision.

**Rewritten / added:**
- §2.3.1 (new) — the 973/973 = 100% downscale measurement, with the honest note that the old
  "ไม่ย่อเลย" record came from measuring a single sample and generalising, i.e. `rule_of_tune` #12 repeating.
- §2.3.2 (new) — the 3-part VRAM decomposition, the options table, what was actually applied
  (7,680 / 47,104), live H200 pricing, and the $8.91 credit blocker stated plainly.
- **§7 (new chapter, 6 sub-sections)** — the section-0% diagnosis, taught as a *method* rather than
  a result: the two kinds of bad number (model genuinely fails vs metric measures the impossible),
  how the exact-string scoring was found, the ceiling table for all six subtasks, the side-by-side
  of plan_beam-0% vs section-0%, the three pattern-recognition attempts that failed on section
  (recorded so nobody repeats them), and the three proposed fixes with which ones touch GT.
- Header summary, §2 intro and §11 rewritten. §2's intro now makes the point that **อาจารย์'s item 3
  pointed at the wrong number but led to a real and bigger problem** — a question worth keeping.
- Chapters 7-10 renumbered to 8-11.

**PDF regenerated: 12 → 19 pages, 4 figures intact.** Verified by extracting text back out and
checking six markers across the new content (§7.3, ceiling table, §2.3.2, H200, `SP_at_bathroom`, §11).

**Left pending / to continue:**
- Still standalone; not appended to `สอน_AI_อ่านแบบบ้าน_รวม_21-24กค.pdf` (Makham's call — that book
  is named for July but already carries ตอนที่ 3, so it may want renaming first).
- Every "left pending" item from the three earlier entries today still stands.

---

## [18:30-18:50] Claude — session with Makham: metric fix applied ("ไปหาในเน็ตและแก้มาซะ")

Researched the standard first: KIE evaluation practice (KIEval, arXiv:2503.05488) reports exact
and relaxed matching as separate columns and normalizes before comparing — exactly the two safe
fixes proposed earlier. Applied to `infer_house_t03.py` (eval script only, **GT untouched**;
Rule 2 warning given — cross-round comparability changes, so the old metric is kept as a column):

- `norm_id()`: strip / collapse whitespace / casefold before set intersection.
- `score_ids()`: returns both **recall** (all ids — comparable with t02/t03 history) and
  **recall_printed** (only ids matching `PRINTED_ID`, i.e. names physically printed on the
  drawing — the number that actually judges the model). Per-task print line, per-subtask summary
  and `_summary.json` all carry both; old JSON keys preserved.
- Also measured first (before deciding): **300/455 section training examples (66%) teach the model
  to guess annotator-invented names** — half are 100% invented — so this is a training-data problem
  too, not only a measurement one. The GT-side fix (renaming convention / splitting section into
  structural-cross-section vs architectural-detail) is written up as proposed, NOT done (Rule 1).

**Self-check with real house-08 GT, no GPU (scorer is pure code):** perfect model → 100%/100%;
a model that reads only printed names → overall 12% but printed 100% (the two columns genuinely
separate the two diseases); a correct model with stray whitespace/case → 100%/100% where the old
scorer gave 0. All asserted, all passed.

Also left a loud comment on the infer script's `MAX_PIXELS`: it must move to 7680 before
evaluating t03b (kept at 5120 now to match the existing t03 adapter).

**Left pending:**
- GT-side convention change for section ids — needs Makham's explicit approval (Rule 1).
- Next training round will show whether plan_beam moves with the resolution fix; section's real
  capability will be visible for the first time via recall_printed.

---

## [เย็น] Claude — session with Ton (Constistant repo): ตรวจตาราง BBS ขั้นต่ำกับ PDF มยผ. 1103-64 ตัวจริง

- Ton สั่งให้ด่าน 3 ของ BBS fallback (assumeRebarFromRules) จบที่อ้าง มยผ. 1103 เพื่อความแม่นยำ
- เจอ PDF ตัวจริง (ei-auditor.com, มี text layer — ไม่ใช่ภาพสแกนอย่างที่บันทึกไว้เมื่อ 08-24) อ่านครบ 20 หน้า
- **ผล: มยผ. 1103-64 เป็นมาตรฐานวัสดุ/ฝีมืองาน ไม่มีค่าเหล็กขั้นต่ำเชิงออกแบบเลยทั้ง 5 ค่า** → คง ACI + flag `dpt1103: 'not_found'` ทุกแถว (คำตัดสิน Ton)
- verify ผ่าน: ข้อ 5.3.4.2 ปลายยื่นปลอกใน bbs-shapes.js ตรงเอกสารจริงทุกกรณี
- โบนัสพบ ข้อ 5.5.4.1 ระยะทาบ (กลม 40db / SD30 30db / SD40 36db / SD50 45db) ต่างจาก standards-config.js (36d/48d จาก วสท.) — ยกให้ Ton ตัดสินแยก ยังไม่แตะ
- npm test 1248/1248 · แก้: bbs-rebar-assumptions.js, bbs-shapes.js (comment), test, spec doc, CLAUDE.md

**[ต่อ] Ton สั่ง "ทุกอย่างอ้างอิง มยผ" → standards-config.js ย้ายทั้งทะเบียน:**
- อ่าน มยผ. 1101-64 เพิ่มครบ 31 หน้า (ระยะหุ้ม ข้อ 5.10 ตาราง 13)
- ทาบข้ออ้อยแยกเกรด 30/36/45d (เดิม 48d), กลม 40d (เดิม 36d); cover คาน 30/เสา 35/พื้น 20 (เดิม 40/40/25); ของอ 180° ขั้นต่ำ 60มม.
- ตัวเลข BBS เปลี่ยนจริง: คานตัวอย่าง main cut 4998→4749, ปลอก 952→1032 มม.
- ค่าที่ มยผ. ไม่มี (top-bar 1.3×, SDM Ld) คงเดิม+ระบุใน clause
- npm test 1249/1249 (อัปเดต regression pin 7 ไฟล์ คำนวณมือยืนยันทุกตัว)

---

## [19:00] Claude — ปิดวัน: สรุปงานค้างทั้งหมด (มะขามสั่งจด)

รายละเอียดเต็มอยู่ในบันทึกรายช่วงข้างบน (15:00-18:50) — นี่คือ index งานค้าง ณ สิ้นวัน

**🔴 รอมะขามเคาะ (Claude ทำต่อเองไม่ได้):**
1. แก้ convention `element_id` ของ section ใน GT — ชุดเทรน 66% สอนเดาชื่อที่เดาไม่ได้ (Rule 1 ต้องอนุมัติ) ← **คอขวดจริงของ section**
2. เติมเครดิต Vast.ai ไหม — เหลือ $8.91 พอการ์ดเดิม 1 รอบ ไม่พอ H200 ($17.45/รอบ)
3. ไฟล์สอนตอนที่ 4 ต่อท้ายเล่มรวมไหม (เล่มชื่อ ก.ค. อาจเปลี่ยนชื่อก่อน)
4. เรื่อง dictionary (ข้อ 1 อาจารย์) — รอถามอาจารย์ว่าจะแก้ปัญหาจุดไหน ยังไม่เริ่ม

**🟡 ด่านบังคับรอบทูนหน้า (t03b):**
5. TEST_STEPS=5 ยืนยัน peak ~90.9 GB ก่อนรันเต็ม (เลขคำนวณ ยังไม่ใช่เลขวัด margin เหลือ 4 GB) ← **ห้ามข้าม**
6. แก้ MAX_PIXELS ใน infer_house_t03.py → 7680 ก่อนวัด t03b (comment เตือนอยู่ในไฟล์)
7. อ่านค่า assert ใหม่ตอนรันจริง → แก้ตาราง workflow แถว 7 ที่เลขผิด

**🟢 ไม่เร่ง:**
8. pattern_recognition: ทดสอบหน้า non-structural + บ้านนอกซีรีส์ กทม.
9. เลือกวิธีใช้ผล pattern recognition (ก ช่วยคนตรวจ / ข เช็ค AI ตกหล่น / ค ป้อน prompt — ข้อ ค เข้า Rule 2)
10. จุดเล็ก: กล่องเสาเบิ้ล 1 จุดบ้าน 12, chars/2.2 ยังไม่ validate กับ tokenizer จริง

**💾 ยังไม่ commit:** measure_id_ceiling.py · infer_house_t03.py (ตัววัดใหม่) · measure_capacity.py ·
train_t03.py (7680/47104) · ไฟล์สอน .md+.pdf + img_teach/ · md_to_book_pdf.py (แก้รูปหาย) ·
t03_workflow.md · ไดอารี่วันนี้ (2 commit แรกของวันมะขาม commit ไปแล้ว: pattern recognition)

---

## [19:20-19:50] Claude — มะขามอนุมัติ att1235 ข้อ 1: แก้ convention element_id ของ section แล้วจริง

**คำสั่ง:** "1. i allow code: att1235" = อนุมัติแก้ GT ตัดสินเองทั้งหมด · ความเสี่ยงแจ้งแล้ว 1 บรรทัดก่อนทำ

**การตัดสินใจ (ตัดสินแล้ว ✅ + risk taken):** ใช้ convention ที่ repo ใช้อยู่แล้วทุกที่ —
"ไม่มีบนกระดาษ = null ไม่ใช่ชื่อแต่ง": element ที่ id เป็นชื่อคนตั้ง → `element_id: null` +
ชื่อเดิมย้ายเก็บใน field ใหม่ `element_name_assigned` (ความหมายมนุษย์ไม่หาย, ย้อนกลับได้ราย field)
เหตุที่ไม่เลือกทางอื่น: เปลี่ยนชื่อเป็นเลขลำดับ = ทำลายความหมาย, เพิ่ม id_source เฉยๆ = แก้แค่
ตัววัดแต่ builder ยัด GT ทั้งก้อนเป็น target → โมเดลยังถูกสอนเดาชื่ออยู่ดี

**ทำจริง:**
1. batch แก้ `json_แก้ไขแล้ว/` — **343 ไฟล์ section, ย้าย 1,534 element, คงชื่อบนแบบ 902** (ตรงกับที่วัดไว้เป๊ะ)
   เดินทั้ง elements[] และ views[].elements[] · idempotent (รันซ้ำ = 0) · git clean ก่อนรัน = backup
2. `build_dataset_t03.py` + `strip_assigned()` — ตัด element_name_assigned ออกจาก training target
   ตอน rebuild (พิสูจน์ด้วย dry-run กับไฟล์จริงบ้าน 08 + ไฟล์ views บ้าน 17: ไม่รั่วเลย)
3. `pass2_used/section.md` — สอนโมเดล: มี mark → ลอกตรงๆ / ไม่มี mark → null ห้ามแต่งชื่อ
4. `primary_rawjson_schema.md` §0.2 แก้ (เดิมสั่งตรงข้าม: "ตั้งชื่อบรรยาย ห้ามเว้น") + ลง
   `primary_rawjson_schema_edit_log.md` ตามกติกาข้อ 7
5. `No_touch_box/docs/raw_json_data_log.md` — บันทึก batch ครบ (ไฟล์/จำนวน/วิธี/ผู้อนุมัติ/ความเสี่ยง)

**verify:** check_format element checks PASS ทุกข้อ (25 ปัญหาที่เหลือ = ของเดิมก่อนแก้ พิสูจน์ด้วย
git stash เทียบ) · รันซ้ำ 0 · spot-check 2 ไฟล์

**ไม่แตะ:** `rawjson_ยังไม่ได้แก้ไขโดนคน/` + `No_touch_box/raw/` (raw layer verbatim ตามเดิม)

**risk ที่รับ:** regex เป็น heuristic — id ยาวที่จริงๆ พิมพ์บนแบบ (เช่น `เหล็ก WF 150x150x7x10`)
โดน null ไปด้วย กู้ได้รายตัวเพราะชื่อเก็บไว้ แต่ rebuild หน้าจะเทรนเป็น null สำหรับพวกนั้น

**ต่อจากนี้:** rebuild dataset (`python build_dataset_t03.py`) ก่อนเทรน t03b — ยังไม่รัน เพราะจะ
ทับ train/val/test.jsonl ปัจจุบันที่ผูกกับผลวัด t03 เดิม ให้ rebuild เป็นส่วนหนึ่งของ workflow t03b

---

## [19:30-01:30] Claude — GPU/model research for t03b, "middle B" question resolved to "task framing, not model size"

**Work done:** Multi-round research (no code/data changes) answering Makham's question "should I fine-tune a bigger model than Qwen3.6-35B-A3B, and what fits a ≤$4/hr GPU rental budget."

**Live GPU pricing, queried directly via `vastai search offers` CLI (not web-scraped, not aggregator):**
- RTX PRO 6000 96GB ×1 (current rig): $1.01–1.11/hr (rate confirmed correct, not overpaying)
- RTX PRO 6000 ×2 (192GB): $2.00–2.19/hr (only 1×/2× available on vast.ai right now; no 3×/4× listed)
- H200 141GB ×1: $3.29/hr
- 8× RTX 4090 (384GB, no NVLink, 18.9 GB/s PCIe): $2.99/hr — flagged as risky for MoE (expert routing needs interconnect bandwidth 4090s don't have)
- A widely-quoted "$0.67/hr RTX PRO 6000" figure was checked and refuted — not in vast.ai's live API results (cheapest real offer $1.01), likely a misread of the `min_bid` (interruptible) column, same trap as an earlier `$0.056` storage-vs-GPU misread.

**Corrected two things in `data_before_tune/train_t03.py`'s own assumptions:**
1. `LOAD_IN_4BIT = False` was already bf16, not 4-bit QLoRA as one research pass assumed.
2. Confirmed via `preprocessor_config.json` (HF API): Qwen3.6-35B-A3B uses `patch_size=16, merge_size=2` → 32×32 px/visual-token, matching `measure_capacity.py`'s own numbers exactly (7,680 cap → 292/973 images downscaled; 16,384 cap → 0/973 downscaled but MAX_LENGTH must rise to 75,776, pushing peak VRAM to ~97.9GB, over the 96GB card).

**Resolution-vs-recall test, done directly on real training data (no GPU needed):** cropped a real plan_beam training image (บ้าน_ใหญ่_1ชั้น_01 หน้า15, the largest/most-downscaled size in the set) both at native resolution and at the actual scale the model receives at MAX_PIXELS=7680. Visually compared — grid bubbles, beam marks (`B1`), column marks (`CX`), dimension text (`3.60`) were equally legible in both. **This contradicts the "downscaling causes the 0% plan_beam recall" hypothesis** that an earlier research pass had concluded from token-math alone.

**Real correlate found instead — GT JSON label length inversely tracks recall:**
| subtask | max GT length (chars) | ~label tokens | measured recall |
|---|---|---|---|
| plan_beam | 20,224 | ~9,192 | 0% |
| gridline | 19,114 | ~8,688 | (not scored) |
| plan_slab | 14,417 | ~6,553 | 20% |
| section | 13,833 | ~6,287 | 0%* |
| plan_footing | 8,871 | ~4,032 | 33% |
| schedule | 9,453 | ~4,296 | 22% |

(*section's 0% is separately explained by the metric-ceiling issue from the earlier [16:20-17:00] entry — not resolution or length.)

plan_footing and plan_beam read the same sheet at the same resolution, same text size — the only real difference is plan_beam's JSON answer is 2.3× longer (45 elements in one response vs footing's shorter list). **Working theory: the model is failing to produce one long, internally-consistent JSON blob in a single completion, not failing to read the image.** Recommended next step (not yet done): split plan_beam into per-grid-zone sub-answers merged by code, instead of buying more image resolution.

**Non-Qwen 100–250B candidate search, iterated three times as Makham widened the ask (122B±50 → 100-200B non-Qwen → 100-200B non-Qwen+commercial):**
- Verified real bf16 weight sizes via HF API `?blobs=true` for InternVL3-78B, GLM-4.6V, Llama-3.2-90B-Vision, Llama-4-Scout, Pixtral-Large, Molmo-72B, NVLM-D-72B, ERNIE-4.5-VL (28B and 424B — confirmed no mid-size ERNIE-VL variant exists).
- Molmo-72B's HF-listed 293GB turned out to be fp32 (83 safetensors files, ~3.51GB each, 73B×4 bytes ≈ 292GB) — real bf16 is ~147GB. Worth remembering when reading any HF repo's raw size: check dtype before assuming it's bf16.
- Framework support checked against real GitHub issues, not just doc claims: **GLM-4.6V has an open, unresolved LoRA-blocking bug** (hiyouga/LLaMA-Factory#9635, `NotImplementedError: get_input_embeddings`). **Llama-4-Scout** works via Unsloth but 4-bit-QLoRA-only, and via LLaMA-Factory with a real third-party confirmed run (Fixstars blog, 8×H100). **InternVL3-78B** works via LLaMA-Factory/ms-swift but **not Unsloth** (two open load-failure issues, #3401 #2341). **Pixtral-Large, Molmo-72B, NVLM-D-72B**: no mainstream framework (Unsloth/ms-swift/LLaMA-Factory) confirmed working.
- License check surfaced Pixtral-Large is Mistral **Research License** — commercial use is explicitly disallowed without a separate paid license, ruling it out outright.
- Checked InternVL's LLM backbone directly via each model's `config.json` `llm_config._name_or_path`: **InternVL3-78B wraps Qwen2.5-72B-Instruct**, and **InternVL3.5-241B-A28B wraps Qwen3-235B-A22B** — both inherit their Apache/Qwen license from the wrapped model, not an OpenGVLab-original LLM. Neither counts as "not Qwen" despite the InternVL branding.
- **Final answer for "100-200B, not Qwen (brand or backbone), commercial-use OK": only GLM-4.6V (MIT, but has the open LoRA bug above) and Llama-4-Scout (Meta license, MAU<700M free, works via Unsloth 4-bit or LLaMA-Factory) survive all three filters.**
- Neither has any published Thai-OCR evidence — recommended a zero-shot Thai-image read test before committing GPU spend to either.

**Impact / what to watch:** none of this touched training code or data — pure research + one live-pricing verification pass. If Makham decides to switch off Qwen3.6-35B-A3B, the two survivors both require either a different framework (LLaMA-Factory, not Unsloth) or a different training mode (4-bit QLoRA, not the current bf16 LoRA) — either is a real pipeline change, not a drop-in swap.

**Pending:** the resolution-vs-JSON-length hypothesis above is not yet tested with an actual model run (only inspected visually + correlated against GT length) — a real zero-shot inference test on a cropped image was recommended but not executed this session (no GPU access from this environment). Also pending: `FINETUNE_VISION=1` A/B run (now unblocked since GGUF export is no longer required), and splitting plan_beam into sub-answers.

---

## [20:00-21:00] Claude — มะขามเสนอ pass 0.5 / 1.5 + สั่งลงมือ: สร้าง stock + cv_scan.py

**ไอเดียมะขาม (บันทึกเป็นสถาปัตยกรรม):**
- **Pass 0.5** = CV สแกนทุกหน้าด้วย template stock → ลายนิ้วมือหน้า ไว้เช็คขวางป้าย pass0 (ด่านเสี่ยงสุด เดิมไม่มีใครตรวจ)
- **Pass 1.5** = CV บน crop ราย view ที่ pass1 ตัดแล้ว → จำนวน+พิกัด ไว้จับ "โมเดลตอบ elements ว่าง" หลัง pass2
- **แก้ความเข้าใจผิดของ Claude:** pass3 ไม่ใช่ "ทิ้ง" — เป็นการแกะหน้าที่ไม่จำเป็น (มี extract.md ของตัวเอง 8 subtask) จดแล้ว
- stock ยังไม่มี → มะขามสั่ง "เริ่มลงมือทำเลย"

**สร้างแล้ว:**
1. `tools/harvest_templates.py` — สร้าง stock จากบ้านของเราเอง: ชื่อไฟล์ GT บอกหน้าผังฐานราก 44 บ้าน
   → bank ปัจจุบันลองจับ → บ้านที่จับได้น้อย = สัญลักษณ์แบบใหม่ → contour หาไอคอนซ้ำ crop ลง
   staging/ **ห้าม auto-promote — ต้องผ่านตาเสมอ** (`--promote <ชื่อไฟล์>` ย้ายเข้า bank)
2. `tools/cv_scan.py` — ตัว Pass 0.5/1.5 จริง: ภาพ/โฟลเดอร์ → JSON sidecar
   {counts, hint, พิกัดทุก class} เครื่องมือเดียวใช้ทั้งสอง pass (ต่างแค่ภาพที่ป้อน)

**ผลจริง:** baseline bank 3 template (2 บ้าน) ครอบคลุม **25/44 บ้าน** · montage 57 candidate
ผ่านตาแล้วคัด promote 10 (ขยะเป็นตัวหนังสือไทย/วงกลม grid ~45 ไฟล์ — ยืนยันกติกาห้าม auto-promote)

**บั๊กจริง 3 ตัว เจอ-แก้ในรอบเดียวกัน:**
1. **template พิษ "+" 22px (บ้าน 11)** จับทุกจุดตัดเส้น → footing บวม 636 จุดในหน้าที่ไม่มีฐานราก
   → ถอดออก + ด่านใหม่ใน load_templates: ข้าม template < 28px พร้อมเตือน
2. **template แรเงาตัน 31px (บ้าน 05/33) คะแนนทับซ้อน** — ของแท้บนบ้านตัวเอง 0.87-1.00 แต่ขยะ
   ต่างบ้านขึ้นถึง 0.90 = เกณฑ์คั่นไม่ได้ **วินิจฉัยก่อนแก้** (ตาราง per-template: 2 ตัวนี้ปล่อย
   ขยะ 224 จุด อีก 9 ตัวสะอาด 0) → แก้ที่ต้นเหตุ: **crop ใหม่ให้ติดขอบขาวรอบไอคอน** (68px แทน 31px
   — ไอคอนจริงลอยเดี่ยวบนพื้นขาว ตัวหนังสือเบียดกัน บริบทคือตัวแยก) + ลบตัวซ้ำ (05/33 สัญลักษณ์
   เดียวกัน คะแนนเหมือนกันทุกหลัก) + FOOTING_THRESH 0.60→0.72
3. **ขยะเกาะขอบคาน 4px หนีตัวกรอง** → ขยายแถบคานในตัวกรอง footing-ในคาน 10px
   / hint ใน cv_scan ยอม FP หลักหน่วย (beam-plan-like ที่ f<=2)

**verify:** `pattern_recognition.py --demo` + `cv_scan.py --demo` ผ่านทั้งคู่หลังแก้ —
บ้าน 17: หน้า14 = 14/14 foundation_plan-like · หน้า16 = ขยะ 0, beam_plan-like
**ค้าง:** harvest รอบสองด้วย bank สะอาด 11 template รันอยู่ (background) — อ่านผลแล้วค่อยคัด
promote รอบถัดไป · beam bank ยังเป็น synthetic ตัวเดียว (ยังไม่ harvest ต่างซีรีส์) ·
column bank มีตัวเดียวจากบ้าน 17 · pass 1.5 ยังไม่ได้ต่อกับ organize.py จริง (รอ pipeline รัน)

---

## [21:00-21:30] Claude — ปิดรอบ stock วันแรก: ผลวัดสุจริต 23 พอดี / 19 ขาด / 2 เกิน

- harvest รอบสอง "44/44 ✅" เป็น**ตัวเลขปลอม** — เกณฑ์หลวม 0.55 + เช็คด้านเดียว (≥70% ของ GT)
  ขยะดันผ่านหมด (บ้าน 27: GT 20 จับ 293) → แก้: HARVEST_THRESH = FOOTING_THRESH (0.72) +
  วัดสองด้าน (✅ 70-160% / 🔶 ขาด / 🔺 เกิน) — บั๊กคลาสเดียวกับตัววัด section เมื่อหัวค่ำ
- re-crop template เล็กทั้งหมด (<50px) ด้วยสูตรขอบขาว (4→78px, 11→70px, 12→68px)
- tpl_footing9 (บ้าน 30, สี่เหลี่ยม+กากบาท) generic เกิน — บนบ้านตัวเองยังได้ 5/9 คะแนนกระโดด
  1.0→0.76 แต่ปล่อยขยะ 0.72-0.73 บนบ้านอื่น → **ถอดออก** (rejected_ ใน staging) บ้าน 30 กลับ
  เป็น "ขาด" อย่างซื่อสัตย์ + ขยายตัวกรอง footing-ในแถบคาน 10px + hint ทน FP หลักหน่วย
- **ผลสุดท้าย: พอดี 23 | ขาด 19 | เกิน 2 | bank 10 template @0.72** · demo ทั้งสองตัวผ่าน
  (บ้าน 17: 14/14 ขยะ 0, hint ถูกทั้งสองหน้า)
- หมายเหตุ: "ขาด" โต 12→19 เพราะเกณฑ์เข้มขึ้น (0.55→0.72) — ไม่ใช่ถอยหลัง แค่ซื่อสัตย์ขึ้น

**งานต่อ (รอบถัดไป):** คัด candidate 19 บ้านใน staging (วนรอบ: ตา→promote→วัด) · ไล่ 2 บ้าน
🔺 ที่เหลือ (08: 25/12, 18: 26/12 — น่าจะ template ข้ามซีรีส์ตี inset) · harvest ฝั่ง column/beam
ยังไม่เริ่ม (column มี 1 template จากบ้าน 17, beam เป็น synthetic) · ต่อ cv_scan เข้าสายพาน:
เริ่มจาก pass0 cross-check (ปลอดภัยสุด ไม่แตะเทรน) — ป้อน hint เข้า prompt = Rule 2 ต้องขอก่อน

---

## [21:30-22:00] Claude — มะขามจับได้ว่าชื่อไฟล์ทำให้เข้าใจผิด + เพิ่มตัวจัดอันดับ candidate

**มะขามถาม:** "ทำไมรูปนี้ถึงเป็นรูป footing ได้" (ชี้ `cand_footing__07..._g2_n8.png` ซึ่งเป็น
**วงกลมกริดตัวอักษร A**) — คำถามถูกจุด ชื่อไฟล์เดิมสื่อผิดจริง

- `cand_footing__` อ่านแล้วเหมือน "นี่คือฐานราก" แต่ความหมายจริงคือ "เก็บมาจาก**หน้า**ผังฐานราก"
  ตัวหาไม่รู้จักฐานราก มันหาแค่ "ไอคอนขนาดใกล้กันที่ซ้ำ ≥3 จุด" → วงกลมกริด A/B/C/D ซ้ำ 8 จุด
  เข้าเงื่อนไขเป๊ะ **→ เปลี่ยนชื่อเป็น `cand_fromFootingPage__`** + rename ไฟล์เดิม 75 ไฟล์
  + เขียนเตือนไว้ใน docstring

**เพิ่ม `--review`:** จัดอันดับ candidate ด้วย **|จำนวนซ้ำ − เฉลยของบ้านนั้น|** แล้วทำ montage
เรียงของน่าจะใช่ขึ้นก่อน — สัญญาณนี้พิสูจน์จากรอบแรก: ตัวที่ promote แล้วรอด n17/gt17, n17/gt17
(ตรงเป๊ะ) · ตัวที่ต้องถอดทีหลัง n31/gt9, n88/gt11 (ห่างลิบ = ตัวหนังสือ) **ไม่ตัดสินแทนตา
แค่เรียงให้ดูเร็วขึ้น**

**คัดด้วยตาจาก montage 79 ตัว → ของจริงมีแค่ 8** (ที่เหลือเป็นตัวอักษรไทย/วงกลมกริด/เส้น)
promote 4 ตัวที่มีขอบขาวบริบท ≥60px ตามบทเรียนวันนี้ (ตัว crop ชิด 27-31px ข้ามไป
เพราะเป็นไอคอนเดียวกันแบบไม่มีบริบท = ตัวที่เคยเป็นพิษ): tpl_footing14-17
จากบ้าน 02/09/04/22 · demo ทั้งสองตัวยังผ่าน (บ้าน 17: 14/14 ขยะ 0)

**ค้าง:** harvest วัดรอบใหม่ด้วย bank 14 template รันอยู่ (background) — ผลก่อนหน้าคือ
พอดี 23 / ขาด 19 / เกิน 2

---
