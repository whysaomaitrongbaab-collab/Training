## [Late night/early morning] Makham (working together with Claude Code — continuing house #1's review, see `2026-07-14.md`)

**Context:** Continuing from the "stopped here" point in `2026-07-14.md` (Makham took a short break), resumed the next day, going through page19 (further fixes), page20, page21, page22, page60-61 to completion, before moving on to start the next house (#6).

**Work done:**

### 1. Element ordering direction reversed: **vertical before horizontal** (had been written the wrong direction the first time)
Makham pointed out that the element-ordering rule set yesterday had the direction wrong — the correct order is vertical first (e.g. for D1: `B4 D1-C1` before `B2 D1-D2`) — re-ordered the whole `หน้า19_view2_beam_plan.json` file accordingly (validated all 20 elements intact), fixed `primary_rawjson_schema.md`'s direction to match (confirmed "y" before editing). Along the way, also found and fixed B2, which had been mistyped as `D'2/D'3`, back to `D2/D3`.

### 2. Added 7 more missing beams + fixed B4X 3'/3'' back to correct
Makham sent a full page19 image (clearer than before), pointing out 7 missing beams: `B2 D'2-D'3` (confirming D' is a real beam on the right side too, not just a wall), `B4 B1-B2`/`B4 B2-B3` (tie-beams for row B paired with row C), `B2 B1''-A1''` (resolving a previously unfound puzzle point), `B2 C3'-B3'`, `B3 A1''-A3`/`B3 A'1''-A'3` (tie-beams for row A/A') — all added with confidence 1, totaling 27 elements.

Found 2 spans of B4X had been mistakenly reverted to label `3'` (spans not matching each other, 0.6 vs 1.5), even though the grid master had already recorded that both should be `3''` (1.5 for both) — confirmed against the grid master (3-3'=0.6, genuinely correct) and fixed back.

### 3. Synced page20 + page21 to match page19 (same mark/spec)
- `หน้า20_view1_tie_beam_plan.json` (B2×15, B6×2): stripped dashes, reordered, split into `specs{}`, merged B2's additional_bars into bottom (2→3) the same way.
- `หน้า20_view2_roof_frame_plan.json`: stripped dashes in grid_refs only.
- `หน้า21_view1_section.json` (the spec source for every beam): synced all the merges from page19 — found 1 real conflict (B4, read from this image, has 1 additional bar, but page19 has 2) — flagged for human review, did not silently pick a side.
- `หน้า21_view2_schedule.json` (column C1 schedule): **Makham caught a different kind of problem** — the column used a beam-style `main_bar.top/bottom` structure (top4+bottom4=incorrectly totaling 8), even though a column has no "top/bottom face", only a single set of 4-corner rebar (4-Ø12mm) — fixed to a single `main_bar: {count:4}` — not yet asked whether to add this as a general rule.

### 4. Page22 — rejected incorrect advice from an external AI
Makham sent an analysis from another AI suggesting F2's main bar be changed from RB/SR24 to DB/SD30 (claiming 12mm is usually DB) — **Claude checked the original image itself first and rejected this**, since the symbol actually printed (Ø) is the same for both F1(Ø9)/F2(Ø12), differing only in size, matching the existing rule "Ø always=RB, never infer from size" — did not edit the file, since the existing data was already correct — **lesson: an external AI can give wrong advice, must always check the image itself before trusting it.**

### 5. Pages 60-61 — added a new pattern `misc` (miscellaneous)
Makham ordered the pattern changed from `title` to the English word for "miscellaneous" for these 2 pages (page60=10-house price comparison table, page61=collage cover combining all designs), since this is promotional content for the whole series, not a cover/data for this particular house — added the 14th pattern `misc` in `primary_rawjson_schema.md` (taxonomy from 13→14 types), per the direct order given in a single message (no need to re-ask since it was already clear).

**Files changed today:** `หน้า19_view2_beam_plan.json`, `หน้า20_view1_tie_beam_plan.json`, `หน้า20_view2_roof_frame_plan.json`, `หน้า21_view1_section.json`, `หน้า21_view2_schedule.json`, `หน้า60.json`, `หน้า61.json`, `primary_rawjson_schema.md` (×3 spots), `primary_rawjson_schema_edit_log.md`, `สิ่งที่ต้องแก้.md` (things_to_fix.md) (items 14-17).

**Summary before moving to house #6 — confirmed conventions that should be applied to the next house from the start:**
1. Point-ref has no dash (`C2`, not `C-2`) — the dash is reserved for line/area ranges (`D-C`).
2. The vertical axis (row) always comes before the horizontal axis (column) when writing a range.
3. `elements[]` is ordered by actual position: top to bottom, left to right, **vertical before horizontal** when the starting point matches.
4. A spec that's identical across every occurrence of the same mark → split out into `specs{}` keyed by element_id, not repeated inside elements[].
5. `additional_bars[].position` must always be checked against the real leader line — never trust the printed label as-is — merge into main_bar (top or bottom, per the evidence) if it's supplemental reinforcement/extends into the same or an adjacent beam.
6. Columns don't use beam-style `main_bar.top/bottom` — use a single `main_bar.count` (not yet formally added to the schema).
7. Pattern `misc` (miscellaneous) for promotional/whole-series-reference pages (not specific to this particular house), separate from `title` (this house's own cover).
8. Every time `primary_rawjson_schema.md` is edited, must cite `rule_of_tune.md` rule 7, actually stop for a real yes/no per rules 9-10 (never infer the answer from a short/ambiguous message), then always log it in `primary_rawjson_schema_edit_log.md`.

**⚠️ Still unanswered/not yet done before moving to house 6:**
- The scope of the point-ref convention (project-wide vs. house 1 only) — still not clearly confirmed.
- Whether to formally add the column no-top/bottom rule to the schema.
- The remaining files in house 1 (beyond หน้า00/05/06/06_roof/11/19×2/20×2/21×2/22/60/61) haven't all been gone through to match the new conventions yet.
- Syncing back to `raw_json_ตัวที่ใช้งานจริง/01บ้าน_เล็ก_1ชั้น_01/` hasn't started at all.

---

## [Afternoon/evening] Makham (working together with Claude Code — moved to starting house #2 instead of #6)

**Context:** Makham changed plans, ordered starting the review of `json_แก้ไขแล้ว/02บ้าน_เล็ก_1ชั้น_02/` (instead of moving to house #6 as planned at the end of the previous entry) — 2 instructions: (1) apply all of สิ่งที่ต้องแก้.md (things_to_fix.md)'s conventions to house 2, (2) re-check only the files with pattern=plan (15 files) against the original images.

**Work done:**

### 1. Went through and fixed the grid_ref format convention in 12 files
Stripped the `grid`/`dummy` prefix, put the vertical axis before the horizontal axis, used `~` for near/approx, stripped dashes from point-refs — pages 06,07,10(×2),12,14,16,17,23,24,39_floor,40, all checked against the original images for every pattern=plan file (every room/footing/column point matched the image correctly). Along the way, found pages 07/10_section2's original had written the row order D-E/E-F **already reversed** (should be E-D/F-E, per the top-to-bottom order F>E>D>C>B>A) — fixed this at the same time.

### 2. Found the same additional_bars wrong-side bug again (exactly like house 1) — pages 25/28
Zoomed into the beam-section images for B2/B2X in S-07 (หน้า28_beam_slab_sections.json), counting rebar points in the real section instead of trusting the recorded label: B2's "stops at L/8", previously recorded as "on top of the beam", is actually at the bottom (a point combined with the 4 main bottom bars) — fixed to bottom 2→4; B2X's lap-splice, previously recorded as "mid-span", is actually on top (3 points combined at the top) — fixed to top 2→3 — cascaded into `หน้า25_beam_plan.json`'s newly split-out specs{} at the same time (including reordering elements[] vertical-before-horizontal, 20 elements).

### 3. Found a real mislabel in หน้า26_tie_beam_plan.json (RB1A that's actually RB1)
Zoomed into the S-05 image around the small room under grid C: the vertical column col3 (C3-B3) actually prints the label "RB1" (not RB1A as recorded) — RB1A genuinely only starts below grid B — fixed the element_id+spec back, making the room symmetric with the col2 side (matching page25) — also did specs{} extraction + reordering for this file (28 elements).

### 4. Page29_column_schedule — confirmed the column rule uses a single main_bar.count (answering the open question from house 1)
The existing data used a beam-style top/bottom structure (with a self-noted flag that the real total was 4, not 8) — fixed to a single `main_bar:{count:4,...}` per the image check (4-Ø12mm around the column) — **this is the answer to the question pending from house 1: confirmed that columns don't use top/bottom project-wide.**

### 5. Added the misc pattern to pages 71/72 (price table + series-wide collage cover)
Exactly matching the house 1 precedent at pages 60/61 — page71 was `unknown`, changed to `misc`; page72 was `title`, changed to `misc`.

**Files changed today (this session):** pages 06,07,10_section1,10_section2,12_wc1,14_wc2,16_stair_st1,17_stair_st2,23_spread_footing,24_pile_footing,25_beam_plan,26_tie_beam_plan,27_roof_frame,28_beam_slab_sections,29_column_schedule,39_floor_plan,40_roof_drainage,71,72 — 19 files total in `json_แก้ไขแล้ว/02บ้าน_เล็ก_1ชั้น_02/`, logged in detail in `สิ่งที่ต้องแก้.md` under the "02บ้าน_เล็ก_1ชั้น_02" (02House_Small_1Story_02) section (items 18-22).

**⚠️ Pending work before the next house:**
- The remaining files in house 2 (pages 03-05,08-09,11,13,15,18-22,30-38,41-45,49-70) haven't been gone through/fixed for format yet.
- Syncing back to `raw_json_ตัวที่ใช้งานจริง/02บ้าน_เล็ก_1ชั้น_02/` hasn't started.
- Should consider going back and re-checking house 1 with the zoom-and-count-points method (the way the B2/B2X bug was found in house 2), since house 1's review mostly relied on label order and didn't zoom in to count every point — there may be similar mistakes still hiding there.

---
