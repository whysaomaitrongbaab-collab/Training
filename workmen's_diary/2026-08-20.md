# 2026-08-20

## [evening] Claude — session with Makham

**Work done:**
- Settled the question Makham opened the session with: does rawjson need new fields for the click-to-accept overlay? **No.** `grid_ref` / `grid_refs` (points) + `grid_ref_start`/`grid_ref_end` (lines) + grid master `pos_m` already carry everything the overlay needs, for both points and lines. Every house extracted since 2026-07-13 has them — no back-filling of old houses.
- Decided pixel/bbox positions stay **out** of rawjson: they're derived (meters → pixels is a pure calculation), tied to one specific scan (rescan/resize breaks them), and vary per calibration run. Same principle as the schema's own "position lives in grid_refs" rule.
- Proved it by rendering a real overlay: house 01 หน้า19 (S-02) beam plan, **24/24 beam segments resolved from grid_ref alone**, drawn on the actual scanned page. Calibration was plain OpenCV `HoughCircles` on the printed grid-label circles (1/2/3, D/C/B/A) + a linear fit per axis — 55.55 px/m (x) vs 55.65 px/m (y), close enough to confirm the scan isn't skewed, so no AI calibration call was needed. B4X (which runs past grid 3 out to dummy grid 3'') landed correctly, and the SO/SI/ST slab markers drew nothing, exactly as expected since they carry no grid_ref.
- Reviewed `King-try`'s `tools/gemini_overlay_prototype/` against current data and found 3 real gaps — all in the tooling, none in the data:
  1. `resolve_grid.py` reads `data["grid"]["x_lines"]`, but grid files now put `x_lines`/`y_lines` at top level — `KeyError` on any current file.
  2. `ELEMENT_TYPES = ("footing", "column")` is hardcoded; line elements (beams) aren't handled anywhere in diff/render/calibration.
  3. Click toggle only `console.log`s — nothing is persisted.
- Constistant: built the accept/reject gate into the raw-JSON import path. Selecting files now shows a checklist of every element (mark / type / floor / grid position / confidence) with everything ticked by default; only ticked rows get saved on confirm. Low-confidence (<0.5) and `needs_human_review` rows are highlighted but deliberately **not** auto-unticked — that call belongs to the person, not a heuristic.
- Split `qt_importRawExtractionFiles()` into `qt_buildRawExtractionPreview()` (parse + adapt, saves nothing) and `qt_saveRawExtractionPreview(preview, acceptedIds)` (saves only what's accepted). The old function stays as a thin wrapper passing `null` (= accept all), so existing tests and scripts are untouched. `npm test`: **768/768 passing**.

**Files changed:**
- Constistant `js/drawing/raw-extraction-import.js` — preview/save split + checklist UI + `qt_toggleAllRawReview` / `qt_confirmRawImport` / `qt_cancelRawImport` (**uncommitted**)
- Training: none — the overlay render was a scratch script, not added to the repo

**Left pending / to continue:**
- Makham's verdict on the Constistant checklist: not good enough. The original ask was clicking the **green overlay on the plan itself**, not a text list — the visual version still needs building (grid + elements as a clickable SVG, drawn from the `GridReference` already saved at import).
- The 3 `King-try` tooling gaps above are still open; nothing was cherry-picked onto `main` this session.
- Constistant login on the deployed site returned `Invalid login credentials` for Makham's account. Deploy and local both point at the same Supabase project, so it isn't a config split — most likely the account was simply never created (Supabase returns the same message for "no such user" and "wrong password"). Signing up should resolve it; demo projects need no login at all.

---

## [late night → early 2026-08-21] Claude — session with Makham

**Work done:**
- Ran t02's LoRA adapter (`Sicilian44/qwen3vl-30b-thai-rc`) as raw transformers/PEFT inference on a rented Vast.ai GPU, since t02 has no working GGUF (export failed, see `t02_workflow.md` header) — a first real test of t02 outside the eval harness, on houses never in its train/val set.
- Chose houses 08/09/10/11 (`บ้าน_เล็ก_1ชั้น_03/04/05/06`) after confirming via grep they're absent from `train.jsonl`/`val.jsonl` — a genuine generalization test, unlike an earlier draft plan that picked 01/03/04 (all in train/val, would've only measured memorization). Wrote `tune_ai/t02/ทดลองถอดแบบ_08_09_10_11_workflow.md` as the pre-flight doc (rule_of_tune §4).
- Two rented instances were bad: first had only 78GB disk (default slider too low, again — third time this trap has bitten); second (same listing) had a dead SSH connection outright. Third instance (host 154.59.156.33, RTX PRO 6000 96GB, 158GB disk, 1TB RAM, $1.792/hr — earlier misread as $0.058/hr, a 30x pricing error caught only after estimating an ~11hr/$20 run) worked.
- Real bug found and fixed: Unsloth's `from_pretrained` doesn't forward the `subfolder` kwarg to its internal `AutoConfig`/`PeftConfig` probes (adapter files live at `lora-adapter/` inside the HF repo, not root) — traced via `unsloth/models/loader.py` source, not guessed. Fix: pre-download via `snapshot_download(allow_patterns=[...])` then point `from_pretrained` at the resulting local path. Same fix applied to both `run_house_batch.py` and `infer_t02.py`.
- Scope was cut live several times as real per-page timing came in (1.5–15 min/page, huge variance, worse on unseen pages that hit the token cap): 4 houses → 2 houses (08 full + 09's plan/gridline pages only) → 08 alone after a 1am deadline started slipping → capped at 25 of 08's 63 pages. Makham asked to reorder mid-run (skip to `หน้า20_beam_plan` right after `หน้า15`) — added `PAGE_FILTER`'s list-as-explicit-order mode to `run_house_batch.py` for this. Ended with **19/25 pages of house 08 done** (1–18 + 20); houses 09/10/11 never started.
- A real near-miss: after the mid-run reorder restart, the SSH session dropped mid-`nohup`, so the new process never actually started while the old one was already killed — caught by checking `pgrep` after the fact rather than trusting the command's own exit code, not assumed fine.
- Downloaded all 19 completed pages to `tune_ai/t02/ผล/08บ้าน_เล็ก_1ชั้น_03/` **before** any destroy — Makham asked to destroy the instance twice mid-run; both times results were freshly re-synced first (Mark-of-Shame discipline). Instance confirmed destroyed (SSH now refuses the connection).
- Compared the 19 pages against human-reviewed ground truth (`json_แก้ไขแล้ว/08บ้าน_เล็ก_1ชั้น_03/`): **57.9% JSON valid (11/19)**, **11.0% element recall (9/82)**, **88.9% dimension-correct among matched elements (8/9)**. Recall is dragged down partly by real misreads (page 16/17 fence details hallucinated as column/footing/ground-beam) and partly by JSON failures on element-dense pages (`หน้า20_beam_plan` alone has 31 GT elements, all uncounted since that page's output never parsed). Also found the model systematically drops the letter prefix from `element_id` on schedule-type pages (GT `"D1"` → AI `"1"`) — matching had to key on `(element_type, trailing digit)` instead of exact id string.
- Built one demo overlay JPG (`tune_ai/t02/ผล/08บ้าน_เล็ก_1ชั้น_03/_overlay_หน้า10.jpg`) — original scan next to a color-coded GT-vs-AI table — for `หน้า10` (door/window schedule), the strongest page: 6/7 (86%) correct on type+dimensions.

**Files changed:**
- `tune_ai/t02/data_before_tune/run_house_batch.py` (new) — batch inference over a house's pages, resume-safe, `PAGE_FILTER` supports both filtering and explicit reordering
- `tune_ai/t02/data_before_tune/infer_t02.py` (new) — single-image inference for ad-hoc testing
- `tune_ai/t02/ทดลองถอดแบบ_08_09_10_11_workflow.md` (new) — pre-flight doc for this experiment; `ทดลองถอดแบบ_01_03_04_workflow.md` is an earlier superseded draft, left in place
- `tune_ai/t02/ผล/08บ้าน_เล็ก_1ชั้น_03/` (new) — 19 page JSONs + 1 overlay JPG
- Comparison/overlay scripts were written to the session scratchpad, not committed to the repo — not yet promoted to a real tool

**Left pending / to continue:**
- House 08: 6 pages short of the original 25-page cap (`หน้า16,17,18,19,21,22,23,24,25` minus whatever's already done), and only 25 of 63 total pages either way.
- Houses 09 (plan+gridline pages only, 6 pages), 10, 11: not started at all.
- The comparison/overlay scripts (`compare_house08.py`, `overlay_page10.py`) exist only in the session scratchpad — worth moving into the repo if this comparison gets repeated.
- `element_id` prefix-dropping on schedule-type pages looks systematic, not a one-off — worth checking whether it shows up in t01 too, or whether it's specific to how t02's training data structured schedule elements.

---
