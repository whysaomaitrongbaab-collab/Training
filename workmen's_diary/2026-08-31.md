# 2026-08-31

## Claude (research InternVL3-78B ฉบับเต็มสำหรับแขนขนาน arm B ของ t05)

- มะขามสั่ง "เทรน 2 ตัวขนานกันแบบเมื่อคืน + หาข้อมูล InternVL3-78B ทั้งหมดที่มี" — ยิง workflow
  4 research agents ขนาน (LF source / model card+paper / transformers processor source /
  ประสบการณ์เทรนจริง) ทุกข้อ verify จาก primary source → `tune_ai/t05_Asclepius/internvl_arm_dossier.md`
- **ไขปริศนา t04 ครบกลไก:** preprocessor_config ของโมเดลตั้ง `crop_to_patches: false` จากโรงงาน
  (LF อ่านตาม) ส่วนตอน infer transformers `InternVLProcessorKwargs._defaults` hardcode True เสมอ
  → train/infer mismatch โดยดีไซน์ของสองไลบรารีที่ default คนละทาง · แก้ฝั่งเทรน = yaml
  `crop_to_patches: true` (LF ≥0.9.3, PR #7817) แต่ max_patches=12 hardcode ใน plugin —
  คุมจำนวน tile ทางอ้อมด้วย image_max_pixels
- **🔴 พบสาเหตุที่สองที่อาจซ้อนอยู่ใต้เรื่อง tile:** เอกสารทางการ InternVL เตือนตรง ๆ ว่า BNB 4-bit
  บน InternViT-6B "produce nonsensical outputs and fail to understand images" — และ LF
  `quantization_bit: 4` quantize ทั้งก้อนไม่เว้น ViT (verify จาก quantization.py — ไม่มี
  modules_to_not_convert) → t04 อาจตาบอดสองชั้น (tile เดียว + ViT พัง 4-bit)
- **แผน arm B ที่เสนอ:** 78B QLoRA-4bit + crop_to_patches:true แต่**บังคับประตู go/no-go ~$2
  ก่อนเทรนเต็มเสมอ**: โหลด base (ไม่มี adapter) แบบ 4-bit + tiling เต็ม แล้วให้อ่านแบบจริง 3-5 หน้า
  — base อ่าน mark ได้ = ViT รอด เดินต่อ / เพ้อ = หยุด เสียแค่ ~$2 (ทดสอบถูกก่อนแพง) ·
  ทางสำรอง: InternVL3.5-38B-hf (LF รองรับแล้ว ~2025-08) หรือ 8-bit บน H200
- **งบ 2 แขนขนาน ~$40-57 — เครดิต $16.66 ไม่พอ ต้องเติมก่อนเช่า (แนะนำ +$50)** — รอมะขามเคาะ
- CV batch (data-gap ② ของ Asclepius) ยังรันอยู่ ~104/159 ok ทุกหน้า

## Claude (build dataset t05 Asclepius คืนเดียวจบตาม scope มะขาม: pass0 5หลัง / pass2.4+3 10หลัง)

- **CV batch เสร็จ 159/159 หน้า** (จริง ~13 วิ/หน้า ไม่ใช่ 70 — ตัวแรกติด cold start) ทุกหน้าได้
  `_cv25.json`+`_hint25.txt`+`_marked25.png`
- **บ้านที่เลือก:** 10 หลัง (8 เทรน: เล็ก_2ชั้น 04/19/01/03, เล็ก_1ชั้น 04/03, ใหญ่_2ชั้น_04,
  ใหญ่_1ชั้น_01 · 2 val: เล็ก_2ชั้น_05, ใหญ่_1ชั้น_02) — val เดียวกันทุก pass กันปนเปื้อนข้าม
- **`build_t05_night.py` + ผลลัพธ์:** pass0 **135/7** (derive จาก GT; คิว 88 หน้า multi-view) ·
  pass2.4 **70/21 ครบ 100%** (เทรนหน้าเดิม 2 โหมด — แก้จุดอ่อน t03 ที่โมเดลไม่เคยเห็น hint ตอนเทรน)
  · pass3 **10/3** = 13 หน้า footing/column ครบทุกหน้าที่มีจริงในคลัง 10 บ้าน
- **pass3 matcher — บทเรียนสำคัญ:** จับคู่อัตโนมัติล้วนเป็นไปไม่ได้บนหน้าจริง (GT รวม count /
  CV จับขาด-เกิน / เสาบนแปลนคานที่ GT ไม่บันทึก) — จบที่กลไก 3 ชั้น: (ค1) id+refs เหมือนกันทั้ง class
  (ค2) spatial ผ่าน gridmaster 8 orientations (ค3) **eyeball ของ Claude ทีละหน้า** เขียนลง
  `pass3_pairs_eyeball.jsonl` แล้ว builder validate ทุกคู่กับ GT ก่อนใช้ (id+refs ต้องมีจริง ห้ามซ้ำ)
  — 13 หน้าที่ทำคืนนี้ cross-check ลงตัวกับ GT 100% ทุกหน้า (นับ count ต่อ id ตรง, CV-missed
  ระบุตัวได้เป๊ะ เช่น หน้า29 เล็ก_2ชั้น_01: CV พลาด E2/D2 จริงตามภาพ)
- **design ที่ได้ฟรี:** label เก็บ GT ครบทุก instance (รวมที่ CV พลาด ไม่มี cv_mark) = สอนกฎ
  "เพิ่ม element ที่บัญชีพลาดได้" ด้วยตัวอย่างจริง · รูปมาร์ควาดใหม่ (`marked_t5/`) เฉพาะกล่องใน
  บัญชี — เลขบนภาพตรงบัญชีเป๊ะ ไม่มีกล่อง class อื่นปน
- **research การนับ pixel (มะขามสั่ง) — คำตอบชัด:** VLM วัด pixel ไม่ได้จริงที่ความแม่น takeoff
  (MeasureBench: เก่งสุด 30.3% บนเกจ/ไม้บรรทัด; chart benchmarks พังตรง proportion; grounding
  วัดที่ IoU 0.5 = คลาด ~25-30% ก็ยังผ่าน) → **VLM ทำครึ่ง symbolic (อ่าน mark/เส้นกริดไหน) ·
  CV/gridmaster ทำครึ่ง metric (ระยะจริง)** — ตรงกับ design ปัจจุบันของ pass3 rule 5 อยู่แล้ว
  ส่วน fallback U/P proportion ควรย้ายจาก VLM ไป CV layer (บันทึกใน dossier + recommendation)
- **คิวพรุ่งนี้:** หน้าคาน ~27 (จับคู่ราย segment — format รองรับแล้ว) + multi-view 7 + pass0
  manual 88 หน้า + มะขาม spot-check pairing ที่ Claude ทำ

## Claude (สู้ต่อ — คานแก้ด้วยเรขาคณิตแทน eyeball, pass0 material_list แก้เป็นชุด)

- เริ่ม eyeball หน้าคานตามคิว เจอว่าเลขกล่องซ้อนทับกันอ่านด้วยตาไม่ได้จริง (`bbox` ที่คิดว่าไม่มี
  พิกัด — บั๊กสคริปต์ตรวจของตัวเองที่เรียกผิดชื่อฟิลด์ ของจริงมี `cx,cy,w,h` ครบ)
- **เปลี่ยนวิธี: จับคู่ด้วยเรขาคณิตแทน eyeball ทั้งหมด** — กล่อง column ของ CV นั่งอยู่บนเส้นกริดพอดี
  → fit เชิงเส้น "เมตร→พิกเซล" จากกล่องเสาที่ชื่อกริดจริง (ไม่ใช่ dummy) แล้วจับคู่กล่องคานด้วย
  ระยะพิกเซลจริง (แนวนอน = กลุ่มตามแถว กล่องเดียวคุมทั้งแถวจริง ยืนยันจาก width; แนวตั้ง = จับคู่ 1-1
  ตามระยะใกล้สุด) — ทดสอบมือหน้า31 ก่อนเชื่อ: 19 คู่ตรง GT 100% + 4 เส้นที่ CV พลาดตรงตามที่วิเคราะห์
  ด้วยตาไว้ก่อนหน้าเป๊ะ · เพิ่ม robust fit รองรับเสาเกิน/ขาดจาก grid หลัก (ลอง subsequence เลือกอันที่
  fit ตรงสุด, ทดสอบกับหน้าซับซ้อนสุด 43 elements residual=0)
- ผล: **pass3 จับคู่เองได้เพิ่มจาก 13→23 หน้า ไม่ต้อง eyeball เลยสักหน้า** (train 10→19, val 3→4)
  คิว eyeball เหลือจริง ~20 หน้า (ส่วนใหญ่เป็นเคส CV เจอน้อยผิดปกติ เช่น 3/6, 10/36 — น่าจะปัญหา
  detector เอง ไม่ใช่ตัวจับคู่)
- **pass0 material_list**: เจอว่าเอกสาร ปร.4 วางเป็น 2 แผ่นข้าง ๆ กันหมุน 90° เสมอ (ไม่ใช่ top/bottom
  อย่างที่คิดแรก) กติกา "เลขน้อย=right, เลขมาก=left" ยืนยันจาก 2 ตัวอย่างจริงคนละบ้านคนละเอกสาร
  (บ้าน_เล็ก_1ชั้น_04 หน้า50 ตรวจด้วยตา + บ้าน_เล็ก_2ชั้น_04 หน้า67 มี warning เขียนบอกตรง ๆ อยู่แล้ว)
  → เขียน `pass0_material_list_batch.py` ใส่ label อัตโนมัติทั้งชุด (89 หน้าทั่วทุกบ้าน, confidence
  0.85 ไม่ใช่ 1.0 เพราะตรวจด้วยตาจริงแค่ 2 หน้า) — pass0 ในสโคป 5 บ้าน 135→**164 train**, คิวมือ
  88→59 หน้า
- **ผลรวมคืนนี้: pass0 164/7 · pass2.4 70/21 (คงเดิม) · pass3 19/4**

## Claude (แยก t44_Voldemort ออกจาก t05 + รวม dataset 4 pass จ่ายทั้งสองแขน)

- มะขามสั่งแยกโฟลเดอร์กันสับสน: **`t44_Voldemort` = InternVL3-78B** · **`t05_Asclepius` = Qwen3.6-35B-A3B**
  ย้าย `internvl_arm_dossier.md` ไป t44 + เขียน `t44_workflow.md`
- แล้วสั่งต่อ "เทรนทูนครบ 4 pass ไปเลยทั้งสองตัว" → เขียน **`build_4pass.py`** ตัวเดียวจ่ายทั้งคู่
  (Asclepius = `train.jsonl`/`val.jsonl` สาย Unsloth · Voldemort = `train_lf.json`/`val_lf.json`
  สาย LLaMA-Factory sharegpt) **เนื้อข้อมูลตรงกันเป๊ะ จงใจ** — t04 เปลี่ยนทั้งโมเดล+precision
  พร้อมกันจน confound แยกไม่ออก รอบนี้ตรึงข้อมูล ผลต่างจึงมาจากโมเดลจริง
- **ผล: train 1,237 / val 68** = pass1 984/36 + pass0 164/7 + pass2.4 70/21 + pass3 19/4
- **บั๊กจริง 4 ตัวที่เจอตอนรวม (ทุกตัวเจอบน CPU ฟรี ก่อนเช่าการ์ด):**
  1. **val ปน 100%** — val 229 แถวของ t04 เป็น subset ของ train ทั้งก้อน (id overlap 229/229) และ
     บ้าน val ของ t05 ทั้ง 2 หลังอยู่ใน train ของ t04 → ทิ้ง val เดิม แบ่งใหม่ตามบ้านกฎเดียวทุก pass
  2. **path รูป pass1/pass2.4** ชี้ `images/<ไฟล์>.png` (โฟลเดอร์แบนที่มีแค่ในเครื่องเทรน t04)
     → เขียนใหม่เป็น `image/<บ้าน>/<ไฟล์>.png` เทียบรากรีโป
  3. **`load_split()` เปิดรูปจากโฟลเดอร์สคริปต์เอง** ไม่ใช่รากรีโป → แก้เป็น `REPO_ROOT`
  4. **assistant content 2 รูปแบบปนกัน** (list vs string) → normalize เป็น list ทั้งหมด
- **วัด MAX_LENGTH จริง** (`measure_capacity.py` + เพิ่ม `--data-dir`/`--media-root`): ที่ MAX_PIXELS
  7,680 tok/ภาพ → seq ยาวสุด **45,839** vs MAX_LENGTH 47,104 = margin แค่ **2.8%** (ต่ำกว่า 5% ที่ควรมี)
  ตัวดันเพดานคือแถว gridline 4 ภาพ (visual 30,720 token ล้วน ๆ) → **ใส่ `preflight_seq_lengths()`
  ในสคริปต์เทรน**: วัด visual (แม่นจากขนาดภาพ) + text (tokenizer จริง) ทุกแถวตอนโหลด เกินเมื่อไหร่
  หยุดทันที — ปิดช่องบั๊กคลาส t01 (label โดนตัดกลาง JSON เงียบ ๆ) ที่ audit ชี้ว่ามีแต่คอมเมนต์ ไม่มีด่านจริง
- **yaml ของ Voldemort** (`train_t44_internvl3_4pass.yaml`): มี **`crop_to_patches: true`** (บรรทัดที่
  ฆ่า t04) + `image_max_pixels: 4194304` (บังคับ 12 tile เต็ม) + **`media_dir: ../..`** (verify จาก
  source จริง: `data_args.py:42-45` มีจริง, join ที่ `converter.py:58` — ⚠️ ไฟล์หายจะแค่ **warn ไม่ error**
  จึงต้องรัน smoke ก่อนเสมอ) · ตรวจ path 1,416 อ้างอิงผ่าน media_dir ครบ 0 หาย
- **`smoke_4pass.py`** (ตรวจฟรีบน CPU ก่อนเช่า): โครง jsonl · รูปเปิดได้ทุกใบ · val ไม่ปน · id ไม่ซ้ำ ·
  `<image>` ตรงจำนวนรูปฝั่ง LF · assistant parse เป็น JSON ได้ → **ผ่านทุกข้อ**
- **ค้นพบเรื่องความละเอียด (สำคัญต่อการตีความผล):** InternVL ที่ tiling เต็ม 12+1 tile อ่านได้แค่
  ~2.4 MP ขณะที่ Qwen อ่านได้ 7.86 MP = **น้อยกว่า 3.3 เท่าโดยสถาปัตยกรรม** และงานวิจัยชี้ว่าความ
  ละเอียดยิ่งสูงยิ่งดีสำหรับข้อความเล็ก ยังไม่อิ่มตัวแม้ที่ 6MP (4KHD: DocVQA 79→89%, InfoVQA 50→69%)
  → **ถ้า Voldemort แพ้ Asclepius อย่าเพิ่งสรุปว่าโมเดลแย่กว่า อาจเป็นเพดานความละเอียดล้วน ๆ**
- **ขนาดรูปของเรา:** median 7.74 MP (3309×2339 = A4 @400dpi, 643 ใบ) — ชนเพดาน 7.86 MP พอดี ·
  151 ใบที่ 3355×2347 เกินเพดานแค่ 0.13% (โดนย่อแบบแทบไม่มีเหตุผล) · 122 ใบที่ 4631×3473 (16 MP)
  จากบ้านเดียว · DPI ค่อนข้างสม่ำเสมอ ไม่ใช่ปัญหา rasterizer อย่างที่สงสัยตอนแรก
- **มะขามทัก "pass0 เรามี OCR ใส่ไปยัง" → เจอบั๊กจริง แก้แล้ว:** pass0 มี OCR จริง (ต้องอ่าน
  `sheet_code`/`sheet_name` จากตารางชื่อแบบ) และ prompt เขียนไว้ชัดว่า *"required … the title
  block is cropped away before the extraction passes see the page, so this is the **only chance**
  to capture them"* อนุญาต null เฉพาะตอนอ่านไม่ออก+ต้องเขียน warning — แต่ตอนทำ material_list
  แบบ batch ผมตั้ง null ทั้ง 89 หน้า **ทั้งที่ GT อ่านได้ชัด** (`ปร.4 (แผ่นที่ 1/23)`) = สอนโมเดล
  ให้ข้ามข้อความที่อ่านออก และหน้านั้นเสียรหัสแบบถาวรเพราะไม่มี pass ไหนได้เห็นตารางชื่ออีก
  → แก้เป็น: ฟิลด์บนสุดถือแผ่นเลขน้อย (ครึ่งขวา) · **แผ่นที่สองไม่ถูกทิ้ง** เก็บใน warnings[]
  แบบอ่านออก · เขียนสคริปต์ใหม่ให้ **idempotent** (หาหน้าเป้าหมายจาก GT ตรง ๆ ไม่พึ่งคิว
  รันซ้ำได้ผลเท่าเดิม — ยืนยันแล้ว) · ผล: pass0 ที่มี sheet_code จริง **63.2% → 80.1%**
  (89/89 หน้า batch มีครบ) ส่วน null ที่เหลือ 34 แถวเป็น GT null จริง ไม่ใช่เราทิ้งเอง
  ⚠️ ช่องว่าง schema ที่ยังไม่ปิด: หน้าที่มี 2 แผ่นจริง ๆ ควรมี sheet_code **ต่อ view**
  (ต้องแก้ prompt = เปลี่ยนสัญญา pass0 ทั้งชุด) — ยังไม่ทำ รอเคาะ
- **prompt pass3 กฎข้อ 4 แก้แล้ว**: เพิ่มข้อยกเว้นว่ากล่อง `beam_h` เป็นการตรวจจับ**ระดับแถว**
  ไม่ใช่รายชิ้น (เส้นคานพิมพ์ต่อกันข้ามหลาย bay จริง) → element หลายตัวในแถวเดียวแชร์ `cv_mark` ได้
  ส่วน beam_v/footing/column ยังห้ามซ้ำเหมือนเดิม · ตรวจแล้ว `merge_guard.py` รองรับเคสนี้อยู่แล้ว
  (เก็บทั้งคู่ + warning ไม่ลบ) ระบบหลังบ้านจึงไม่พัง

## Claude (เปลี่ยนชื่อ t05→Courser + k-fold 2 ทั้งสองแขน + อ่าน rule_of_tune)

- มะขามสั่งเปลี่ยนชื่อ `t05_Asclepius` → **`t05_Courser`** — ย้ายโฟลเดอร์+rename script, sweep
  ทุกไฟล์ (build_t05_night.py/pass0_derive.py/run_cv_batch.py/smoke_4pass.py/t44 dossier+yaml)
  แล้ว **rebuild ทั้งสอง dataset จากตำแหน่งใหม่** (ไม่ sed แก้ JSON มือ กัน path ผิดที่ฝังในไฟล์)
  — เหลือแค่ 2 จุดตั้งใจเก็บไว้เป็นประวัติ (บรรทัดบันทึกการเปลี่ยนชื่อใน workflow.md + ไดอารี่)
- อ่าน `No_touch_box/docs/rule_of_tune.md` เต็ม (Rule 4: ต้อง verify Phase 0 จริงก่อนเช่า ไม่ใช่
  แค่อ่าน · Lesson 15: ตรวจ token/ภาพจริงเสมอ)
- **มะขามสั่งเพิ่ม "epoch 3, k fold 2, train/val 4/1"** → เจอ convention เดียวกับ t04 เป๊ะ
  (`build_folds_llamafactory.py` docstring บอกตรงๆ): "k fold 2, 4/1" = **k=5 fold จริง (8 บ้าน/
  fold จาก 40 บ้าน) รันแค่ fold0+fold1** ไม่ใช่แบ่งครึ่ง — train 32 บ้าน(4/5) : val 8 บ้าน(1/5) พอดี
- **เจอปัญหาจริงตอนผสาน**: fold0's val ของ t04 ตรงกับ 2 บ้าน val เดิมของเราเป๊ะ (บ้าน_เล็ก_2ชั้น_05,
  บ้าน_ใหญ่_1ชั้น_02 — น่าจะเป็นเหตุผลที่เลือก 2 บ้านนี้ตั้งแต่แรก) **แต่มี 1 บ้านชน**
  (`บ้าน_ใหญ่_1ชั้น_01` เดิมอยู่ train ของเราแต่เป็น val ของ fold0 ตาม t04) → ถ้ายังใช้ VAL_HOUSES
  คงที่ บ้านนี้จะอยู่ train ของ pass0 พร้อมกับอยู่ val ของ pass1 ในโมเดลเดียวกัน = รั่วข้ามชนิดข้อมูล
  แก้โดย**ยึด house-to-split ของ t04 เป็นความจริงเดียวทุก pass ต่อ fold** (ไม่ใช้ VAL_HOUSES คงที่
  อีกต่อไป) ผลพลอยได้: เทียบผลกับ t04 เดิมได้ตรงบ้าน val เป๊ะ
- เขียน `build_4pass.py` ใหม่ให้เป็น fold-aware: pass1 อ่านจาก `train_fold{k}.jsonl`/
  `val_fold{k}.jsonl` ของ t04 ตรงๆ (ไม่ใช้ train.jsonl/val.jsonl เดิมที่ปนกันอีกแล้ว), pass0/2.4/3
  รวมเป็นกองเดียวแล้วแบ่งใหม่ตาม house-in-fold-val — **ผล fold0: train 1,048/val 257 (val มี
  pass0/2.4/3 ด้วย 17/31/4 แถว) · fold1: train 1,101/val 204 (8 บ้าน val ไม่ทับสโคป pass0/2.4/3
  เลย — val fold1 มีแค่ pass1 ล้วน)** — ความไม่สมมาตรนี้บันทึกไว้ตรงๆ ไม่ซ่อน
- yaml ของ Voldemort แยกเป็น **2 ไฟล์จริง** (`train_t44_internvl3_fold0.yaml`/`fold1.yaml`
  — LLaMA-Factory yaml ไม่มี env-var interpolation แบบ Python เขียนคู่กันชัดกว่า) ต่าง dataset key
  + output_dir เท่านั้น ที่เหลือเหมือนกันเป๊ะ
- `train_t05_courser.py`: FOLD env var default เปลี่ยนจาก `""` (ไม่มี fold) → `"0"` เพราะไม่มี
  `train.jsonl`/`val.jsonl` รวมให้ fallback อีกแล้ว (ลบทิ้งพร้อม `train_lf.json`/`val_lf.json`
  เดิมของ Voldemort — เหลือแต่ fold-based)
- `measure_capacity.py` แก้ `--split` จาก choices จำกัด (`train`/`val`/`test`) เป็นรับ string
  อิสระ รองรับ `train_fold0` ฯลฯ — วัดจริงทั้ง 4 ไฟล์: **seq ยาวสุด 45,839 ทั้ง fold0/fold1**
  (ตัวเลขเดียวกับตอนยังไม่แยก fold — สมเหตุสมผล เพราะแถว gridline 4 ภาพที่ดันเพดานอยู่ train ทั้ง
  สอง fold) ยังอยู่ใต้ MAX_LENGTH 47,104 (margin 2.8% เท่าเดิม)
- `smoke_4pass.py` เขียนใหม่ให้ loop ทั้ง 2 fold — **ผ่านทุกข้อทั้งสอง fold**: บ้าน train 32/val 8
  ไม่ปนกันทั้งคู่, รูปไม่ซ้ำรวม 884 ใบเปิดได้ครบ, `<image>` ตรงจำนวนรูปฝั่ง LF ทุกแถว
- **มะขามสั่งลด epoch 3→2** พร้อมเหตุผลจาก research ภายนอก (2 vs 3 epoch accuracy ต่างมักแค่ 1-3%
  แต่ 3 epoch กิน GPU-ชม.+50% และ risk overfit สูงขึ้น) สั่งให้ตรวจซ้ำในเน็ตก่อนเชื่อ — ยิง research
  agent ไปตรวจ (background, ผลจะรายงานแยก) **แก้ config ทั้งสองแขนแล้วทันที** ไม่รอผล research
  (คำสั่งชัดว่า "แก้ epoch to 2 พอ"): `train_t05_courser.py` EPOCHS 3→2, `train_t44_internvl3_fold{0,1}.yaml`
  num_train_epochs 3.0→2.0 — parse ผ่านทั้งคู่ อัปเดต t05_workflow.md/t44_workflow.md ให้ตรง
- **คำนวณเวลาใหม่จากข้อมูลจริง** (ไม่ใช่ประมาณลอยๆ): ใช้ step time จริงที่วัดจาก t04 (180-196s/step,
  บริบทเดียวกัน — InternVL3-78B QLoRA การ์ดเดียว) × step count จริงจากแถวจริงของแต่ละ fold
  (fold0 1,048 แถว → 262 steps @2ep = **13.1-14.3 ชม.** (จาก 19.6-21.4 ชม. @3ep) · fold1 1,101 แถว
  → 276 steps @2ep = **13.8-15.0 ชม.** (จาก 20.7-22.5 ชม. @3ep)) — **⚠️ ตัวเลขนี้เป็น estimate ของ
  Voldemort เท่านั้น** อาศัย step time จาก t04 ที่ dataset สั้นกว่า (แค่ 7-subtask ไม่มี pass0/2.4/3
  ผสม) จึงน่าจะช้ากว่านี้จริงเล็กน้อย · **Courser (Qwen) ยังไม่มีตัวเลขที่เชื่อได้** — t01 มี 56s/step
  แต่คนละ dataset/config เก่ามาก เทียบไม่ได้ตรงๆ ต้อง dry-run จริงถึงจะรู้
- **parity check t05↔t44 (มะขามสั่ง "เช็คว่าเหมือนกันใช่ไหม") — ตาม Lesson 12 เต็มรูป:**
  (1) เนื้อ dataset เทียบ row-by-row ทุกแถวทุก fold (รูป/ลำดับ/prompt text/GT) = **ตรงกัน 100%,
  mismatch 0** (2) hyperparameter ตรงกันอยู่แล้ว: epoch 3 · lr 1e-4 · batch 1×accum 8 ·
  LoRA r16/α32 · cosine+warmup 0.05 · seed 3407 · eval ปิด · bf16 compute · logging 5/save 25/limit 2
  (3) **เจอช่องว่าง Lesson-12 จริง 3 จุด — ค่าที่ฝั่ง yaml ไม่ได้เขียนแล้ว default เงียบๆ ไม่ตรงกับ
  Courser:** `weight_decay` (Courser 0.01 vs HF default 0.0), `optim` (adamw_8bit vs default
  adamw_torch — ช่องว่างเดียวกันนี้มีใน t04 ด้วยแต่ไม่มีใครเห็น), `lora_dropout` (ตรงกันแต่ implicit)
  → แก้แล้วทั้ง 3 ใน yaml ทั้งสอง fold (parse ผ่าน) (4) ต่างโดยตั้งใจ บันทึกไว้แล้วไม่แตะ:
  precision (bf16 LoRA vs QLoRA 4-bit — คือตัวแปรที่ทดลอง), resolution (7,680 vs 3,328 tok/ภาพ —
  เพดานสถาปัตยกรรม), seq cap (47,104 vs 32,768 — วัดแยกตาม token จริงของแต่ละแขน),
  completion-only loss (Unsloth explicit=True vs LF `train_on_prompt` default false = เท่ากัน)

## เช่า 4 การ์ดพร้อมกัน — เริ่มรอบเทรนจริง (att1235 มอบแล้ว 2026-08-31 ดึก)

- มะขามสั่ง "ลุยเลย ตัดสินใจเอง" + มอบ `att1235` สำหรับรอบนี้ — ปิดช่องว่าง 4 ข้อที่พบก่อนหน้า
  ก่อนเช่า: (1) บ้าน 08 เลิกใช้เทียบ (2) `onstart.sh` เขียนทั้งสองแขนแล้ว (3) `verify_hf_push.py`
  (Day-of-Shame guard ใหม่สำหรับ push_to_hub) เขียนแล้ว (4) att1235 บันทึกการมอบในทั้งสอง workflow.md
- เช็คเครดิตจริงก่อนเช่า: **$84.82** (ไม่ใช่ $16.66 ที่บันทึกไว้เดิม — เข้าใจผิดเรื่องยอดคงเหลือ)
- **เช่าจริง 4 instance พร้อมกัน** (`vastai create instance`, ทุกตัว verified reliability>0.98):
  | GPU | contract id | offer | การ์ด | $/hr | image |
  |---|---|---|---|---|---|
  | Voldemort_a (fold0) | 49296441 | 48618647 | RTX PRO 6000 Max-Q 96GB, S.Korea | $1.0444 | pytorch/pytorch:2.8.0-cuda12.8-cudnn9-devel |
  | Voldemort_b (fold1) | 49296452 | 45876267 | RTX PRO 6000 96GB, New York US | $1.3556 | pytorch/pytorch:2.8.0-cuda12.8-cudnn9-devel |
  | Courser_a (fold0) | 49296434 | 43112699 | RTX PRO 6000 Max-Q 96GB, Australia | $1.1080 | vastai/pytorch:cuda-12.8.1-auto |
  | Courser_b (fold1) | 49296438 | 45070630 | RTX PRO 6000 Max-Q 96GB, Australia | $1.1080 | vastai/pytorch:cuda-12.8.1-auto |
  รวม ~$4.62/ชม. — ประมาณ 13-15 ชม. (Voldemort, จาก step-time จริงของ t04) ≈ **$60-70** เทียบเครดิต
  $84.82 ตึงแต่ควรพอ (Courser ยังไม่มี step-time จริง ต้องรอ dry-run) — ความเสี่ยงที่รับ: ถ้า Courser
  ช้ากว่าคาด อาจต้องเติมเครดิตกลางทาง ยอมรับความเสี่ยงนี้ตามคำสั่ง "ตัดสินใจเอง"
- แต่ละ instance สร้างพร้อม `--onstart` ชี้ตรงไปที่ `onstart.sh` ของแขนนั้น (ตรวจ LF แล้วก่อนส่ง —
  ไม่มี CRLF, `grep -c $'\r'` = 0 ทั้งคู่) ขั้นต่อไปหลัง instance ขึ้น running: อัปโหลด dataset,
  export HF_TOKEN, รัน TEST_STEPS=5 (Courser) / go-no-go ~$2 แล้ว max_steps=5 (Voldemort) ก่อน
  เทรนเต็มเสมอ — จะบันทึกผลแต่ละขั้นต่อจากนี้

### ปัญหาระหว่างเช่า + สิ่งที่ verify ได้จริงบนเครื่องจริง (2026-08-31 ดึก ต่อ)

- **บั๊กของ Claude เอง 2 จุด (บันทึกไว้กันซ้ำ):** (1) ลูปรอเช็คสถานะ instance เรียก `python3`
  ซึ่งบนเครื่องมะขามเป็น alias เสียของ Windows Store → ลูปล้มเงียบทุก 15 วิ วนไม่จบ ทั้งที่การ์ด
  ขึ้น running ครบไปนานแล้ว (ใช้ `python` เฉยๆ หรือ grep แทน) (2) `onstart.sh` ของ Courser
  **ลืมใส่ `export TORCH_CUDA_ARCH_LIST="12.0"`** ที่ onstart ของ t02 มี (จำเป็นตอน compile
  CUDA kernel ของ bitsandbytes บน Blackwell) — เติมแล้ว
- **เช่าใหม่ 3 ครั้งกว่าจะได้ครบ 4 เครื่อง — คนละสาเหตุ ไม่ใช่ของเรา:**
  (ก) `vastai/pytorch:cuda-12.8.1-auto` (image ที่เลือกให้ Courser รอบแรก) สร้าง
  `/root/.ssh/authorized_keys` มาด้วย permission ผิด → sshd ปฏิเสธคีย์ที่ถูกต้อง
  (`Authentication refused: bad ownership or modes`, อ่านจาก `vastai logs` ไม่ใช่เดา) แก้ไม่ได้
  จากนอก (`vastai execute` ใช้ได้เฉพาะ instance ที่ stop, reboot ก็ไม่หาย) → destroy ทั้ง 2 ใบ
  เช่าใหม่ด้วย `pytorch/pytorch:2.8.0-cuda12.8-cudnn9-devel` (image เดียวกับที่ Voldemort ใช้
  และ SSH เข้าได้จริง) (ข) ใบใหม่ใบหนึ่งเจอ `remote port forwarding failed for listen port 18712`
  — proxy ฝั่ง vast.ai bind port ไม่ได้ (stale port) แก้จากในเครื่องไม่ได้ → destroy เช่าใหม่อีกใบ
  **รวมเสียเปล่า ~$0.3-0.5 ไม่มีข้อมูลเสีย (ยังไม่ได้อัปอะไรขึ้นไปตอนนั้น) ไม่ใช่เคส Day-of-Shame**
- **ประหยัดการอัปได้เยอะ:** dataset อ้างถึงรูปแค่ **884 ใบ = 817MB** ไม่ใช่ 4.3GB ทั้งคลัง
  → tar เฉพาะที่อ้างถึงจริง (`img_manifest.txt` จาก jsonl ทั้ง 4 fold) + jsonl + LF json + yaml +
  สคริปต์ = **bundle 771MB ก้อนเดียวใช้ได้ทั้งสองแขน** (โครง path เดียวกัน: `image/<บ้าน>/…` +
  `tune_ai/…` เทียบ REPO_ROOT) · ⚠️ กับดัก Windows 2 อัน: manifest ที่เขียนด้วย Python
  ได้ CRLF (ต้อง `newline='\n'`) และ `tar` ตีความ `D:/…` เป็น remote host (ต้องใช้ `/d/…`)
- **verify การอัปแบบ Day-of-Shame discipline:** md5 ตรงเป๊ะทั้ง voldemort_a/b
  (`4b765a37e7023bbca00ea52c6c72f240`) แตกไฟล์ได้ **861 + 23 = 884 ใบ ตรงกับ manifest**
- **★ verify ที่สำคัญที่สุดของรอบนี้ — ทำบนเครื่องที่จะเทรนจริง ไม่ใช่อ่าน docs:**
  LLaMA-Factory ที่ติดตั้งจริง = **0.9.5** (ต้อง ≥0.9.3) · `crop_to_patches` เป็น field จริงใน
  `hparams/model_args.py:317` · `data/mm_plugin.py:843` คือบรรทัด `getattr(processor,
  "crop_to_patches", False)` ที่ทำให้ t04 พังทั้งรอบ · เมื่อเปิดจริงจะส่ง
  `{crop_to_patches: True, max_patches: 12, min_patches: 1}` (บรรทัด 846-848, max_patches
  hardcode 12 ตามที่บันทึกไว้) · **ตรวจบรรทัด 882 ที่ set `crop_to_patches = False` แล้ว —
  อยู่ใน branch ของ video เท่านั้น (`if len(videos) != 0`) และรันหลัง branch ของ image ไปแล้ว
  ข้อมูลเราไม่มี video เลย → ไม่แตะ path ของเรา** = ยาที่แก้ t04 ยืนยันแล้วว่าถูกจริงบนเครื่องจริง
- ดาวน์โหลดโมเดลเริ่มแล้วทั้ง 4 (tmux session ชื่อ `dl`): InternVL3-78B-hf (~156GB, 33 shard)
  บน Voldemort · Qwen3.6-35B-A3B บน Courser — เป็นคอขวดที่ยาวสุดของขั้นเตรียม

### ☠️ Voldemort ตกประตู go/no-go — ตัดแขนทิ้ง เอาการ์ดมาทำ Courser k-fold แทน

**ประตูรอบแรก (v1) ขึ้นเขียว แต่เขียวเพราะเหตุผลผิด — จับได้ก่อนเดินหน้า:** prompt ที่ Claude
เขียนใส่ตัวอย่างว่า "เช่น F1, B2, C1, ผ2" ซึ่ง F1/ผ2 อยู่ในคำตอบ GT พอดี → โมเดล**ลอกตัวอย่าง
กลับมาเป๊ะ ตอบสตริงเดียวกันทั้งหน้า14 (ผังฐานราก) และหน้า12 (รูปตัด)** แล้วสคริปต์นับว่า "ตรง"
อีกหน้าตอบ B1→B34 = ไล่เลข บังเอิญขึ้นต้น B1,B2,B3 ตรง GT · **ถ้าเชื่อผลนี้ = เผาทั้งคืน
กับโมเดลที่มองไม่เห็นอะไรเลย แบบ t04 เป๊ะ** (บทเรียน: ห้ามใส่ตัวอย่างที่อยู่ในเฉลยลง prompt ทดสอบ)

**v2 (ตัดตัวอย่างออก + บังคับคำตอบต่างหน้าต้องต่างกัน + ตัดคำตอบไล่เลข): ตก 0/3**
ทั้ง 3 หน้าตอบเหมือนกันหมด "รูปตัด" + "อ่านไม่ออก"

**v3 ตัวแยกโรค (3 คำถาม/หน้า ไม่มีทางออก "อ่านไม่ออก" ให้เลือก) — ผลชี้ขาด ไม่เหลือข้อสงสัย:**
- ผังฐานรากไทย → *"เอกสารทรงกลม พื้นหลังชมพู โลโก้ Facebook/Apple/Barbie ข้อความ Happy Birthday"*
- ผังคานไทย → *"โลโก้ BB-8 จาก Star Wars ล้อมด้วย Facebook/Twitter/Instagram/Snapchat"*
- ให้อ่าน "ตัวใหญ่สุดบนหน้า" → ตอบ `Happy Birthday` / `Facebook`
→ **ไม่ใช่ "อ่านตัวเล็กไม่ออก" แต่ตาบอดสนิท เห็นภาพหลอนคนละเรื่อง** ตรงกับที่เอกสาร InternVL
เตือนคำต่อคำว่า BNB 4-bit บน InternViT-6B *"produce nonsensical outputs and fail to understand
images"* — ความเสี่ยงที่ dossier บันทึกไว้ตั้งแต่ต้นว่าเป็นตัวชี้เป็นชี้ตายของแขนนี้ **เกิดขึ้นจริง**
**ประตู ~$2 คุ้มที่สุดของโปรเจกต์** · หมายเหตุ: tiling ทำงานถูกต้อง (13 tile / 3,482 token)
= ยาที่แก้ t04 ถูกตัว แต่แก้ไม่ได้เพราะโรคที่สองคือ 4-bit ฆ่า ViT

**มะขามสั่ง: เอาการ์ด 2 ใบมาทำ k-fold ของ Courser แทน** → แปลงในที่ (ไม่ destroy: dataset
อัปไว้แล้ว md5 ตรง, image เดียวกัน, ใช้ image เดิมได้) — ลบ cache InternVL คืน 147GB,
ติดตั้ง unsloth, โหลด Qwen3.6-35B-A3B · **ได้ 4 การ์ด = Courser fold0/1/2/3**

### ★ มะขามจับได้: "อย่าเอา fold ของ t04 ไปใช้ เพราะ t04 ไม่มีระบบ tune pass0/2.5/3"

ถูกต้อง และพิสูจน์จากไฟล์ที่สร้างด้วย fold ของ t04 จริง — **val แทบไม่มี pass0/3 เลย**:
| fold (t04 split) | val pass1 | pass0 | pass2.4 | pass3 |
|---|---|---|---|---|
| fold1 | 204 | **0** | **0** | **0** |
| fold2 | 204 | **0** | 9 | 2 |
| fold3 | 204 | **0** | 9 | 1 |

สาเหตุ: t04 แบ่ง 40 บ้านโดยไม่รู้ว่า **มีแค่ 10 บ้านที่มี pass0/2.4/3** (pass0 มีแค่ 5 บ้าน)
บ้านพิเศษเลยไปกองฝั่ง train เกือบหมด → **วัดไม่ได้ว่าโมเดลเรียน pass0/pass3 ได้ไหม
ซึ่งคือสมมติฐานหลักของรอบนี้ทั้งรอบ** (ถ้าไม่จับได้ จะเทรน 4 การ์ดแล้วอ่านผลไม่ได้)

**แก้: `build_house_folds()` ใน build_4pass.py — stratify ที่ระดับบ้าน deterministic ไม่ใช้ random**
5 บ้านที่มี pass0 → fold ละ 1 · 5 บ้าน rich ที่ไม่มี pass0 → fold ละ 1 · 30 บ้านธรรมดา → fold ละ 6
(val 8 บ้าน/fold = 4:1 เท่าเดิม) · pass1 ยังใช้ "แถว" ของ t04 (รูป/prompt/GT ชุดเดียวกัน)
แต่ **ไม่ใช้วิธีแบ่ง fold ของมัน** — union train+val ของ fold ใดก็ครบ 40 บ้านอยู่แล้ว

ผลใหม่ — ทุก fold มีครบทุก pass ใน val:
| fold | val pass1 | pass0 | pass2.4 | pass3 | train/val |
|---|---|---|---|---|---|
| 0 | 195 | 54 | 17 | 5 | 1034/271 |
| 1 | 188 | 79 | 22 | 8 | 1008/297 |
| 2 | 222 | 7 | 20 | 5 | 1051/254 |
| 3 | 211 | 10 | 16 | 3 | 1065/240 |
`smoke_4pass.py` ผ่านทุกข้อทั้ง 4 fold (บ้าน train/val ไม่ปนกัน, id ไม่ซ้ำ, `<image>` ตรง)
**รูปยังเป็นชุดเดิม 884 ใบ** (re-split ไม่เปลี่ยน row pool) → อัปแค่ jsonl 12MB ไม่ต้องอัปรูปใหม่

**คำถามมะขาม "ลอง Qwen3.5 ร้อยกว่า billion ไหม" — ตอบ: ไม่ใช่คืนนี้**
(1) `t05_workflow.md` §B บันทึกจาก Unsloth docs แล้วว่า Qwen3.5/3.6 MoE **quantize ไม่ได้**
(คุณภาพตกผิดปกติ + bnb 4-bit MoE พังตอน import) → ต้อง bf16 ล้วน (2) 100B+ bf16 = ~200-220GB
ไม่พอแม้แต่ H200 141GB ต้อง multi-GPU ซึ่งสคริปต์ทั้งชุดเราเป็นการ์ดใบเดียว = รื้อใหม่หมด
(3) หลักฐาน t01-t04 ชี้ว่าคอขวดคือ **ข้อมูล/label ไม่ใช่ขนาดโมเดล** (35B ตัวเดียวกันได้ 28.2%
กับ 10.1% ต่างกันที่ข้อมูล/prompt) → เก็บไว้คุยหลังเห็นผล 4 fold ว่า "ข้อมูลดีแล้วแต่โมเดลตัน" จริงไหม

### บันทึกบทเรียน Voldemort ลง rule_of_tune (มะขามสั่ง) → **Lesson 16**

เพิ่ม `No_touch_box/docs/rule_of_tune.md` ข้อ 16 ครอบ 3 เรื่องที่คืนนี้สอน:
1. **ประตูทดสอบห้ามมีเฉลยอยู่ในตัวเอง** — v1 เขียน prompt ว่า "เช่น F1, B2, C1, ผ2" ซึ่ง F1/ผ2
   คือ GT ที่จะเอาไปเทียบ → โมเดลลอกกลับมา ได้คำตอบ**เหมือนกันเป๊ะ**ทั้งหน้าผังฐานรากและหน้ารูปตัด
   แต่สคริปต์นับว่า "ผ่าน 3/3" · อีกหน้า "ผ่าน" เพราะไล่เลข B1..B34 บังเอิญขึ้นต้นตรง GT
2. **ต้องมีตัวตรวจแยกแยะ** — บังคับให้คำตอบต่าง input ต้องต่างกัน (ตอบเหมือนกันข้าม input
   = ลายเซ็นของโมเดลที่ไม่ได้ดูภาพ) + ตัด degenerate output + ห้ามมีทางออก "อ่านไม่ออก"
   ตอนที่กำลังทดสอบว่า "มองเห็นไหม" + มีข้อง่ายคุมไว้ 1 ข้อ (อ่านตัวใหญ่สุด) เพื่อแยก
   "ตาบอด" ออกจาก "ความละเอียดไม่พอ"
3. **แก้ root cause ที่ยืนยันแล้ว 1 ตัว ≠ ปลดล็อกรอบนั้น** — t04 มี **2 โรคซ้อนกัน**
   Lesson 15 เจอแค่โรคแรก (`crop_to_patches`) ซึ่งแก้ถูกจริง (คืนนี้ยืนยันทั้งจาก source
   `mm_plugin.py:843` และจากการรันจริง 13 tile/3,482 token) **แต่ไม่พอ** เพราะโรคที่สอง
   (4-bit ทำ ViT ตาบอด) นอนอยู่ข้างใต้ และจะให้ผลหน้าตาเหมือนเดิมเป๊ะ: JSON สวย recall ~0%
   → กฎใหม่: หลังแก้ root cause แล้ว ให้ไล่ **รายการความเสี่ยงที่เหลือ** (ปกติเขียนไว้ใน
   design doc ของรอบนั้นอยู่แล้ว — กรณีนี้ dossier เขียนไว้ตั้งแต่ต้น) แล้วตั้งประตูดักทีละข้อ

### ลำดับปิดงานใหม่ (มะขามสั่ง) + dry-run ไล่บั๊ก 6 รอบ

**มะขามสั่งลำดับปิดงาน:** เทรนจบ → push HF ทุกตัว → รวมตามสมการ k-fold ชื่อ **`destrier`**
→ ตรวจ Day of Shame → **ถึงจะคืนการ์ด** · เขียนลง `t05_workflow.md` §ลำดับปิดงาน (6 ขั้น +
เช็คลิสต์ Day of Shame 5 ข้อ), เขียนใหม่ `merge_adapters_soup.py` (4 fold, นน. 1/k เท่ากัน,
ปลายทาง `dacarokann/destrier`), อัปเดต `onstart.sh` พร้อมคำเตือน destroy = ลบถาวรกู้ไม่ได้

**dry-run บนเครื่องจริง ไล่บั๊กทีละชั้น 6 รอบ — ทุกตัวจะพังตอนเทรนเต็มถ้าไม่มี dry-run:**
| รอบ | ตายที่ | สาเหตุจริง | แก้ |
|---|---|---|---|
| 1 | โหลดโมเดล | `transformers 4.57.6` ไม่รู้จัก `qwen3_5_moe` | pin `==5.5.0` (หน้าต่างจริง [5.2.0, 5.5.0] — ปลายบนมาจาก unsloth ประกาศ `<=5.5.0`) |
| 2 | `get_peft_model` | `peft 0.18.1` เรียก `LinearActivationQuantizedTensor` ที่ `torchao 0.18.0` ถอดออกแล้ว | ถอด torchao ทิ้ง (เทรน bf16 ไม่ได้ใช้ · `dispatch_torchao` เช็ค `is_torchao_available()` ก่อน → ไม่มี = ข้ามเงียบ) |
| 3 | `SFTTrainer.__init__` | `push_to_hub=True` เรียก `init_hf_repo()` ตั้งแต่ `__init__` **ไม่ใช่ตอน save_steps** → 401 (สมมติฐานเดิมที่ว่า "dry-run ไม่ต้องใช้ token" **ผิด**) | dry-run ปิด push อัตโนมัติ · รันจริง **ไม่มี HF_TOKEN = ตายทันทีวินาทีแรก** ไม่ใช่ไปรู้ตอน 15 ชม.ผ่านไป |
| 4 | JIT compile kernel **ระหว่างเทรน** | unsloth ดึง torch **cu130** ลงบน image **CUDA 12.8** → nvrtc หา `libnvrtc-builtins.so.13.0` ไม่เจอ (ไฟล์**มีอยู่จริง** แค่ไม่อยู่ใน path) | ชี้ `LD_LIBRARY_PATH` → `nvidia/cu13/lib` ไม่ต้อง downgrade torch |
| 5 | **OOM ที่ step 2** | ขาด 1.18 GB จาก 94.97 · step 1 ผ่านแล้ว (137 วิ) | ตั้ง `PYTORCH_CUDA_ALLOC_CONF` ด้วย (สคริปต์ตั้งแต่ชื่อใหม่ `PYTORCH_ALLOC_CONF` ตัวเดียว แต่ torch 2.11 พิมพ์ชื่อเก่าในข้อความ OOM ของตัวเอง) |
| 6 | 🔄 กำลังรัน | — | step 1 ผ่านใน **76 วิ** (เร็วขึ้นจาก 137) VRAM 96.6/97.9 GB — เฉียดมาก |

**OOM รอบ 5 เป็นเรื่องที่ workflow ทำนายไว้แล้ว:** `t05_workflow.md` เขียนตั้งแต่ 2026-08-25 ว่า
`MAX_PIXELS=7680 / MAX_LENGTH=47104` เป็น**การคำนวณบนกระดาษ ไม่เคยรันจริง** margin เหลือ 4.0 GB
และต้อง `TEST_STEPS=5` ยืนยันก่อนเสมอ — เราเป็นคนแรกที่ทดสอบ และมันตันจริง ลำดับถอยที่เขียนไว้:
ลดภาพ gridline 4→3 ก่อน (กระทบแค่ 34 แถวของ subtask เดียว) → แล้วค่อยถอย MAX_PIXELS →
**ห้ามลด MAX_LENGTH เฉย ๆ** (ตัด label กลาง JSON)

**บั๊กในยามเอง 2 จุด แก้แล้ว (ยามพังอันตรายกว่าที่คิด เพราะทำให้เลิกเชื่อยาม):**
(ก) `del _b, _b2, _gt` ทั้งที่ `_b2` ถูก del ไปแล้วในลูป → `NameError` ทุกครั้ง พิมพ์ว่า
"ตรวจ batch แรกไม่สำเร็จ" **ทั้งที่ตรวจผ่านครบทุกข้อไปแล้ว** (เห็น ✓ ทั้ง 3 อันดับ)
(ข) คำเตือน "visual token ต่ำกว่า cap เกิน 25%" เทียบกับ **cap รวม** → เตือนผิดทุกครั้งที่ภาพ
ต้นทางเล็กกว่าเพดานอยู่แล้ว (2339×1654 = 3.87 MP = 3,778 token ตามคณิตศาสตร์เป๊ะ วัดได้ 3,796
ไม่ได้โดนย่อเลย) แก้ให้เทียบกับ **เพดานที่แถวนั้นควรได้จริง** = min(พิกเซลจริง, MAX_PIXELS)/1024
ตอนนี้พิมพ์ยืนยันเชิงบวกแทน: `(ภาพแถวนี้ควรได้ ~3778 token/ภาพ ตามขนาดจริง — ตรงกัน ไม่โดนย่อ)`

**ความผิดพลาดของ Claude ที่ต้องบันทึก (เกือบทำให้ gpu4 เทรนด้วยรูปขาด 60 ใบเงียบ ๆ):**
scp ไป gpu4 ค้างที่ 83% → สั่ง `rsync --partial --append-verify` เพื่อ resume → **เช็ค
`which rsync` บนเครื่องปลายทาง แต่ไม่ได้เช็คบนเครื่องเรา** Git Bash ไม่มี rsync → คำสั่งล้มด้วย
"command not found" แต่ task รายงาน **exit 0** เพราะ `echo` ตัวสุดท้ายสำเร็จ และ `| tail -2`
กลบข้อความ error ไป → เข้าใจผิดว่า resume สำเร็จ · ไฟล์ที่ได้จริงคือเศษ scp ที่ถูกฆ่า
**md5 ไม่ตรง (`2aa1039d…` ≠ `4b765a37…`) tar แตกไม่จบ ได้รูป 824/884** — จับได้เพราะ
เช็ค md5 ก่อนใช้ทุกครั้งตามวินัย Day-of-Shame ถ้าข้ามขั้นนั้นจะไปเจอตอนเทรนพังกลางทาง
**บทเรียน: (1) เช็คว่ามีเครื่องมือบนเครื่องที่จะ *รัน* มัน ไม่ใช่เครื่องปลายทาง (2) exit code
ของ compound command ไม่ใช่ผลของคำสั่งที่เราสนใจ — ต้องอ่าน output จริง ห้าม `| tail` กลบ**

### เทรนจริงเริ่มแล้ว 4 การ์ด + มะขามมอบ att1235 ก่อนไปนอน (04:45 น.)

**HF พร้อมครบ — ตรวจแบบ end-to-end ไม่ใช่แค่ดูว่า repo มีอยู่:** สร้าง 5 repo พร้อม model card
ไทย (`Courser_a/b/c/d` = fold0-3, `destrier` = ตัวรวม) · มะขามสั่งเปลี่ยนเป็น **public**
("ผมรับความเสี่ยง") ยืนยัน `private=False` จาก API จริงทั้ง 5 · **ทดสอบเขียนไฟล์จริง → อ่านกลับ
→ ลบ สำเร็จ** = operation เดียวกับที่ trainer จะ push ทุก 25 step ไม่ใช่แค่เดาว่าน่าจะได้

**เทรนเต็มเริ่มแล้ว (ตัวเลขจริงจาก log ไม่ใช่ประมาณ):**
| การ์ด | fold | steps | วิ/step | ETA เสร็จ (เวลาไทย) |
|---|---|---|---|---|
| gpu2 | 1 | 252 | 82 | ~10:20 |
| gpu3 | 2 | 264 | ~90 | ~11:10 |
| gpu4 | 3 | 267 | ~95 | ~11:50 |
| gpu1 | 0 | 260 | **106** | **~12:15** ← ช้าสุด |
รันขนานกัน → **เทรนครบ ~เที่ยง** + merge/verify อีก ~1 ชม. → **จบ ~13:00-13:30**
งบ 4 การ์ด × ~$1.2 × 8 ชม. ≈ **$38** จากเครดิต $84.82
⚠️ ประมาณการเดิม 13-15 ชม. **ผิดไปเท่าตัว** เพราะเอา step time ของ InternVL-78B มาใช้กับ
Qwen 35B-A3B ซึ่งเบากว่ามาก — ของจริงคือ 82-106 วิ/step

**บั๊กเงียบอีกตัวที่จับได้ก่อนเสียเวลา:** `folds4.tar.gz` มี `train_t05_courser.py` เวอร์ชันเก่า
อยู่ข้างใน → **การแตกไฟล์ทับสคริปต์ที่แก้แล้ว** ทำให้ gpu4 รันโพรบด้วยของเก่าและตายด้วย 401
(ยัง push ตอน dry-run) · แก้โดยส่งสคริปต์ใหม่ + **grep ยืนยัน marker ครบทั้ง 4 เครื่อง**
ไม่ใช่แค่ scp แล้วเชื่อ — บทเรียน: ระวังลำดับ "push สคริปต์ → แตก tarball" ที่มีไฟล์ชื่อซ้ำ

**worst-case VRAM probe (`SORT_LONGEST=1` เรียงตัวอย่างยาวสุดขึ้นก่อน — ของใหม่ที่เขียนคืนนี้):**
เหตุผลที่ต้องมี: dry-run ปกติเห็นแค่ 40/1008 ตัวอย่างแบบสุ่ม → peak VRAM ที่วัดได้ไม่ใช่ worst case
| fold | peak VRAM | margin จาก 95.0 GB |
|---|---|---|
| fold1 | 82.6 GB | 12.4 |
| fold2 | 80.3 GB | 14.7 |
| **fold0** | **91.4 GB** | **3.6** ← ตึงสุด แต่ผ่านแล้วบนตัวอย่างยาวสุดจริง |
ตัวยาวสุด 41,804 token (4 ภาพ × 7,519 visual token เต็มความละเอียด) เทรนผ่าน
→ **ความเสี่ยง "OOM ที่ชั่วโมงที่ 8" ตัดออกได้ด้วยหลักฐาน ไม่ใช่ด้วยความหวัง**

**มะขามมอบ `att1235` สำหรับรอบนี้** ("คุณสามารถละเลย rule of tune ได้ใน workflow เลย")
บันทึกการตีความไว้ใน `t05_workflow.md` §att1235: เดินทุกขั้นเองจนจบไม่ต้องปลุกถาม —
**ยกเว้นข้อเดียวคือไม่ destroy การ์ดก่อน verify ว่าไฟล์อยู่บน HF จริง** เพราะนั่นทำลายของที่
att1235 ถูกมอบมาเพื่อให้ได้มา และมะขามหลับอยู่จึงแก้ให้ไม่ได้ถ้าตัดสินผิด (rule_of_tune §att1235
เดิมก็เขียนข้อยกเว้นนี้ไว้เอง) · การ log ทุกก้าวยังทำต่อตามเดิม

### 🔴 OOM จริง 2/4 fold ตอนเทรนเต็ม (04:47 น.) — แก้ด้วย att1235 ไม่ปลุกถาม

**เกิดอะไร:** ระหว่างมะขามหลับ เช็คสถานะพบ **gpu1(fold0) และ gpu4(fold3) ตายด้วย OOM จริง**
(`Tried to allocate 1.18 GiB. ... 1.04 GiB is free` — เกือบเป๊ะเหมือนกันทั้งคู่) ที่แย่กว่านั้น
**gpu2(fold1)/gpu3(fold2) ที่ยังไม่ตายก็วัดได้ 93.7/94.66 จาก 94.97 GB ceiling** ตอนที่พบปัญหา —
คือเฉียดขอบพอกันหมด ไม่ใช่แค่ 2 ตัวที่โชคร้าย

**สาเหตุ:** worst-case probe เมื่อคืน (`SORT_LONGEST=1 TEST_STEPS=2`) วัดแค่ 2 ตัวอย่างแยกโดด ๆ
บนโปรเซสสด แต่ **real training สะสม fragmentation ข้าม step จริงมากกว่านั้น** (PyTorch caching
allocator โตขึ้นเรื่อย ๆ ตามตัวอย่างที่สุ่มเจอ ไม่หดกลับ) → peak จริงตอนเทรนสูงกว่าที่โพรบทำนายไว้
มาก (91.4 GB ที่วัดจาก probe ก็ยังไม่พอ)

**ตัดสินใจ (att1235, ไม่ปลุกถาม):** ลด `MAX_PIXELS` 7680→**6912*1024** (-10%) **ทุก fold
เท่ากัน** ไม่ใช่แค่ 2 ตัวที่ตาย เพื่อรักษาความเทียบเท่าของความละเอียดภาพข้าม fold (Lesson 12 class
— ถ้าลดแค่บาง fold จะเทียบผลข้าม fold ไม่ได้อีกต่อไป) **verify ก่อนลงมือทั้ง 4 เครื่อง**: รันโพรบ
`SORT_LONGEST=1 TEST_STEPS=3` ซ้ำบน gpu1 ด้วยค่าใหม่ → **peak ลดจริง 91.4→80.6 GB**
(margin 3.6→14.4 GB) ยืนยันด้วยตัวเลขจริงก่อนเชื่อว่าแก้ได้ ไม่ใช่แค่เดา

**ทิ้ง checkpoint-25 ของ fold1 ที่มีอยู่แล้ว (adapter 3.78GB, เทรนมาแล้ว 27 step):** เพราะมันเทรน
ด้วย MAX_PIXELS เก่า (7680) — ถ้าเก็บไว้แล้วเทรนต่อด้วยค่าใหม่ (6912) จะกลายเป็นข้อมูลผสม
ความละเอียดกลางการเทรนของ fold เดียวกัน สกปรกกว่าการเริ่มใหม่สะอาด ๆ · **restart ทั้ง 4 fold**

**เจอปัญหาเพิ่มระหว่างรีสตาร์ท:** gpu3 พังตอน**เริ่ม**ด้วย `CUDA error: out of memory` ระหว่าง
import unsloth (ไม่ใช่ตอนเทรน) — เช็คแล้ว GPU ว่างจริง (0 MiB, ไม่มีโปรเซสค้าง) น่าจะเป็น race
condition ตอน `tmux kill-server` ฆ่าโปรเซสเก่ายังคืน VRAM ไม่ทัน ยิงใหม่รอบสองผ่านปกติ

**สถานะล่าสุด: เทรนใหม่เริ่มพร้อมกันทั้ง 4 fold ด้วย MAX_PIXELS=6912 (~22:05 UTC / 05:05 ไทย)**
เวลาที่เสียไปจากรอบ OOM: ~1 ชม. (โพรบยืนยัน+รีสตาร์ท) ETA ใหม่จะช้ากว่าเดิมที่ประมาณไว้ ~1-1.5 ชม.

### 22:15 — 4 folds รอดผ่านจุด OOM เดิมแล้ว
- fold0 7/260 · fold1 5/252 · fold2 3/264 · fold3 3/268 — ทุกตัววิ่งอยู่
- จุดที่พังรอบก่อนคือ step 2 กับ step 27 → รอบนี้ผ่านมาแล้ว (MAX_PIXELS 6912 ได้ผล)
- ETA ตัวช้าสุด fold3 ~6.5 ชม.
- ตั้ง monitor `watch4.sh` เช็คทุก 10 นาที (pgrep + train_runtime) จะปลุกเมื่อ CRASHED หรือ ALL_4_DONE

### 05:46 — 3/4 fold จบ · fold1 OOM · ตัดสินใจ merge k=3
- fold0 260/260 ✅ · fold2 264/264 ✅ · fold3 268/268 ✅ — adapter 3.78GB ขึ้น HF ครบ (Courser_a/c/d)
- fold1 OOM ตายที่ step 68/252 (ทั้งที่ MAX_PIXELS 6912 เท่ากับตัวอื่นที่รอด) — HF Courser_b เหลือแค่ checkpoint-50
- ตัดสิน (att1235): **ไม่ rerun fold1** → merge k=3 นน. 1/3 เท่ากัน
  - rerun setting เดิม = น่าจะ OOM ซ้ำ · ลด MAX_PIXELS เฉพาะ fold1 = เสีย parity (Lesson 12)
  - 3 fold ยังครอบคลุมบ้านครบ 40 หลัง · การ์ด 3 ใบว่างเผาเงินอยู่
  - มะขามอนุมัติย้อนหลัง 05:5x "ฉลาดมาก"
- แก้ merge_adapters_soup.py: ตั้งชื่อ adapter รวมเป็น `default` (ไม่ใช่ `destrier`) — PEFT เซฟ adapter ที่ไม่ชื่อ default ลง subfolder → repo จะไม่มีไฟล์ที่ root; + delete_adapter fold ต้นทางก่อน push ไม่งั้นอัปไป 4 ตัว
- รัน merge บน gpu1 (base 67GB cache อยู่แล้ว)

### บั๊กตัวเอง — monitor watch4.sh ไม่เคยทำงานเลย
- SSHFAIL ทุกบรรทัดตั้งแต่รอบแรก 7 ชม. ไม่มีข้อมูลจริงสักครั้ง แต่ไม่ exit เพราะ SSHFAIL ไม่นับเป็น CRASHED
- คลาสเดียวกับบั๊ก `python3` รอบก่อน: **เขียน monitor แล้วไม่พิสูจน์ว่ารอบแรกได้ข้อมูลจริง**
- กฎ: monitor ต้องเช็ค output รอบแรกด้วยตาก่อนปล่อยทิ้ง และ "เชื่อมต่อไม่ได้" ต้องเป็นเงื่อนไขปลุก ไม่ใช่ปล่อยผ่าน

### 12:45–13:30 — merge สำเร็จ · destrier ขึ้น HF · คืนการ์ด 3 ใบ · smoke test กำลังรัน
- PEFT `add_weighted_adapter` ใช้ไม่ได้กับ MoE `target_parameters` (adapter เดียวต่อโมเดล — ยืนยันจาก TRL issue #5222) → เขียน `soup_safetensors.py` บวกเทนเซอร์เอง
- เจอ fold0 แยกตัวประกอบชั้น MoE คนละข้างกับ fold2/3 (peft 0.18.1 vs 0.20.0 — เช่าห่าง 2 ชม. ได้คนละเวอร์ชัน) → เขียนตัวแปลง permute พิสูจน์ max|ΔW เดิม − ใหม่ᵀ| = 0.00e+00 · บันทึกเป็น rule_of_tune บทที่ 17 (monitor พังเงียบ) + 18 (pin ทุก lib ที่เขียนฟอร์แมต)
- สูตรเฉลี่ย 1/√k วัดจริงแล้วผิด: fold ชี้ทางเดียวกันเกือบสนิท พจน์ไขว้บวกทบ ‖ΔW‖ เกิน 3.0× (attn) / 16.3× (MoE) → เปลี่ยนเป็นต่อแกน rank แบบ exact (r 16→48, alpha 32→96) ตัววัดได้ 1.000 ทั้งสองชั้น (= `combination_type="cat"` ของ PEFT ที่เราต้องทำมือเพราะข้อจำกัดข้อแรก)
- destrier ขึ้น HF แล้ว 11.34GB ยืนยันผ่าน API · loss จบ fold0/2/3 = 0.273/0.259/0.250
- log ทุก fold สำรองลง `t05_Courser/logs_final/` → **destroy gpu2/3/4** (13:2x) — ข้อความมะขาม "อย่าเพิ่งคืนการ์ด" มาถึงหลังลบแล้ว ~2 นาที ของครบบน HF ไม่มีอะไรเสีย แจ้งตรง ๆ แล้ว
- **gpu1 (49296441) ถือไว้ตามคำสั่งมะขาม — ห้าม destroy จนกว่าจะสั่ง** · ตั้ง venv peft 0.20 (convention ตรงกับ destrier — ระบบเดิม 0.18.1 โหลดแล้วจะเป็นขยะเงียบ) · รัน `smoke_destrier.py`: โหลด destrier จาก HF จริง อ่านแบบ 3 หน้า (gridline/plan_beam/notes) เช็คตาม Lesson 16

### 13:30–ปัจจุบัน — smoke test destrier + คิว pure-power อ่านบ้านนอกคลัง
- `smoke_destrier.py`: โหลด destrier จาก HF จริง (peft 0.20 venv) อ่าน val 3 หน้า (gridline/plan_beam/notes) — ช้ามาก (torch fallback, ไม่มี flash-linear-attention เพราะ cuda toolkit 12.8 ชนกับ torch cu13.0 — ลอง pip install ไม่สำเร็จ ปล่อยผ่าน ไม่คุ้มเวลา)
- หน้า gridline ผ่าน: JSON valid, pattern="grid_master" ถูกต้อง, อ้างชื่อไฟล์รูปจริงของบ้านนั้น (ไม่ใช่ hallucinate เรื่องอื่น) — สัญญาณดีกว่าการทดสอบ Voldemort ที่ตายไปมาก
- มะขามสั่ง: "ลองทำ t04_Purson/test_house_new/บ้านไทยพอเพียง3 ด้วยกำลังเพียวๆ" — บ้านนี้คือตัวเทียบข้ามรอบตัวจริงของ Courser (ยืนยันไม่อยู่ใน 40 หลังคลัง, เตรียมรูปไว้แล้วตั้งแต่ 2026-08-30 แต่ยังไม่เคยยิง)
- ตีความ "เพียวๆ": ไม่เดินสายพาน (ไม่มี pass0/grid master ให้บ้านนี้) — เขียนพรอมต์เดียวจากแกนกลาง schema ที่ใช้ร่วมทุก subtask ปล่อยให้โมเดลตัดสิน pattern เอง ทดสอบ 8 หน้ากระจายทั่วเล่ม (03,10,19,28,36,46,55,62) ไม่ทำครบ 65 หน้าเพราะช้า (~20 นาที/หน้า)
- อัปโหลดรูปทั้ง 65 หน้า (67MB) ขึ้น gpu1 แล้ว เขียนสคริปต์ `pure_power_read.py` ตั้งคิวให้รันต่อจาก smoke_destrier.py อัตโนมัติ (poll pgrep ทุก 20s) ไม่ต้องรอ manual
- **gpu1 ยังถืออยู่ตามคำสั่งมะขาม "อย่าเพิ่งคืนการ์ดจอหลังจากจบ day of shame"**

### หลัง 07:13 — มะขามสั่ง: เกิน 25 นาที → xgrammar, เสร็จแล้วคืนการ์ดได้เลย
- `plan_beam` ค้างเกิน 25 นาทีจริง (สังเกตตอนรายงานสถานะ) → ฆ่าทิ้งตามกติกา
- สร้างระบบใหม่แทนสคริปต์เดี่ยว: `worker_page.py` (1 หน้า/1 process, รองรับ `--xgrammar` ผ่าน `xgr.contrib.hf.LogitsProcessor` + builtin JSON grammar — ยืนยันมีอยู่แล้วในเครื่อง 0.2.4, ใช้ได้จาก venv ผ่าน --system-site-packages) + `run_queue.sh` (bash `timeout 1500` ต่อหน้า → เกิน = ฆ่า + รันซ้ำหน้านั้นด้วย --xgrammar → ยังเกินอีก = ข้าม ไม่บล็อกคิว) รวม 11 งาน (3 smoke + 8 pure-power)
- **บั๊กที่เจอและแก้ทันที**: `set -u` + `local a=$1 b=$2 c=$a/${b}...` ในบรรทัดเดียว → bash รายงาน "unbound variable" ที่ตัวหลังแม้ arg ส่งมาครบ (bash gotcha: ตัวแปรที่เพิ่ง local ในบรรทัดเดียวกันยังไม่ "เห็น" กันจนบรรทัด local นั้นจบ ภายใต้ set -u บางเวอร์ชัน) → แก้เป็น local แยกบรรทัด ยืนยันด้วย `bash -x` เห็น trace ชัดก่อนสรุป ไม่เดา
- คิวรันอยู่ (relaunch แล้ว) — ตั้ง background wait บล็อกจนกว่าจะเจอ `QUEUE_ALL_DONE`
- **คำสั่งใหม่: พอคิวจบ ให้สำรองข้อมูลกัน Day of Shame แล้ว destroy gpu1 (49296441) ได้เลยไม่ต้องรอถามอีก** — จะทำตามลำดับ: backup logs+outputs ทุกไฟล์ลงเครื่อง local → ตรวจว่าของสำคัญ (destrier บน HF, log 4 fold, smoke/pure-power outputs) มีสำเนา ≥2 ที่จริง → destroy

### 08:16 — watcher หลุด SSH กลางทาง, GPU ว่างไปสั้นๆ, กู้กลับมารันต่อแล้ว
- plan_beam โดน timeout 25 นาทีจริง → xgrammar retry สำเร็จ (rc=0)
- ตัว watcher ที่ตั้งไว้ (รอ plan_beam จบแล้ว kill+swap คิว) **หลุด SSH กลางทาง (exit 255)** ก่อนจะ kill+relaunch เสร็จ — ผลคือคิวเก่าเดินต่อไปเริ่ม `notes` (ที่สั่งให้ตัดแล้ว) เขียน log ทับ แต่ยังไม่ทันสร้างผลลัพธ์ก็โดน kill (ไม่มีไฟล์ .json ของ notes ค้าง ไม่มีข้อมูลเสีย)
- gpu ว่างไปช่วงสั้นๆ (ไม่มีของหาย ไม่มีไฟล์ผิด) กู้กลับด้วยการเช็คสถานะจริงบนเครื่องก่อนเชื่อ log แล้ว relaunch `run_queue_elements.sh` ทีละคำสั่ง (ไม่ chain ยาว หลัง compound command แรกคืนค่าเงียบผิดปกติ) — ยืนยันด้วย ps aux ว่า worker_page ทำงานจริงก่อนพิมพ์รายงาน
- ตอนนี้กำลังทำ `pure_power_page03` (หน้าแรกของ 8 หน้าบ้านไทยพอเพียง3)

### 15:52 — มะขามจับได้ว่าผล destrier ไปเก็บผิดที่ (ใต้ t04_Purson แทน t05_Courser)
- สาเหตุ: โฟลเดอร์รูปบ้านไทยพอเพียง3 (บ้านทดสอบกลาง ใช้เทียบข้ามรอบได้ทุกรุ่น) ถูกเตรียมไว้ใต้ `t04_Purson/test_house_new/` ตั้งแต่ 30 ส.ค. — ผมมาต่อสคริปต์+ผลลัพธ์ของ destrier (t05 Courser) ทับที่เดิมโดยไม่ทันคิดว่ามันดูเหมือนเป็นผลของ t04/Voldemort ที่ตายไปแล้ว
- **แก้แล้วบางส่วน**: ย้าย `pure_power_read.py` + `_prompt_core.txt` + ผลลัพธ์ 4 ไฟล์แรก จาก `t04_Purson/test_house_new/` → `t05_Courser/destrier_test_house/` (ในเครื่อง local เท่านั้น ไม่กระทบงานที่รันอยู่)
- **ยังไม่แก้ตั้งใจ**: รูป 65 หน้าของบ้าน (ทั้งใน local และบน gpu1) ยังอยู่ที่เดิมใต้ t04_Purson เพราะ gpu1 กำลังอ่านจาก path นั้นอยู่กลางคัน — ย้ายตอนนี้จะทำให้คิวที่รันอยู่หาไฟล์รูปไม่เจอ กลางทาง
- **แผนต่อ**: พอคิวรันจบ (11/11) จะย้ายโฟลเดอร์รูปทั้งชุดออกจาก t04_Purson ไปที่กลาง ๆ ที่ไม่ผูกกับรอบไหนรอบหนึ่ง (เช่น `tune_ai/benchmark_house_new/`) แล้วอัปเดต path ในสคริปต์ + เอกสารที่อ้างถึง (`t05_workflow.md` บรรทัด "ด่านสุดท้าย...")

### 10:00–11:40 — เช่า 2 การ์ด (A=กำลังดิบ / B=ระบบ pass) · ซ้อมใช้งานจริงผ่านคิว Supabase สำเร็จ · เจอสาเหตุ GPU-A ช้า
- บ้านทดสอบใหม่ตามที่มะขามเลือกเอง: `บ้านแบบประหยัด1` (dpt.go.th/arch/plan/437, 14 หน้า, ไม่อยู่ในคลังเทรน) เรนเดอร์ 200dpi แล้ว
- **GPU-B (49374190) = ระบบ pass จริง ผ่านครบวงจร 2 รอบ** — `test_e2e_new_house.py` (เขียนใหม่ mirror `test_e2e_supabase.py` แต่ยิงบ้านนอกคลัง ไม่มี stub): อัปรูปเข้า storage จริง → insert `purson_jobs` จริง → `worker.claim_next_job()` → `run_house_extract()` (pass0→gridline→pass2) → เขียนผลกลับ → อ่านออกมาได้ครบ 5 ไฟล์ · เวลา pass0 95-149s, pass2 283-669s
- **pass0 จำแนกหน้าเองได้จริง** — หน้า 09 แยกเป็น 2 view (`plan_footing` ด้านบน + `plan_beam` ด้านล่าง) พร้อม warning อธิบายว่าเป็นผังเดียวกัน 2 แบบฐานราก (ตอกเข็ม/ฐานแผ่) — ถูกต้องตามแบบจริง
- **แต่คุณภาพเนื้อหายังไม่นิ่ง (พูดตรง)**: หน้า 09 อ่าน F1 ถูก (40x40, 6 ต้น, grid ครบ) แล้ว **หลุดวนกลางคัน** พ่นข้อความปนไทย-อังกฤษไร้ความหมายยาวเป็นพัน · หน้า 12 มิติติดลบ (`height_mm:-500`) คีย์พิมพ์เพี้ยน (`width-mn`) เรียกคานว่า column · **ที่น่ากังวลสุด: มี `house_001_หน้า00_gridline.json` + `image/house_001/...` โผล่ในผลของบ้านใหม่ = ท่องจำชื่อไฟล์จากคลังเทรนมาแปะ**
- **GPU-A (49374187) = กำลังดิบ ล้มเหลวเพราะ config ไม่ใช่เพราะโมเดล** — หน้า 09 และ 10 timeout 25 นาที **ทั้ง 2 รอบ (ปกติ + xgrammar)** ไม่ได้ output สักตัว
- **root cause (ยืนยันจากโค้ดจริง ไม่เดา)**: `worker_page_raw_pratyad.py` ใช้ vanilla `AutoModelForImageTextToText`+`PeftModel` (ช้ามากบน MoE LoRA 256 experts) **และไม่มี `repetition_penalty`/`no_repeat_ngram_size` เลย** ต่างจาก `serve_purson.py` ที่ใช้ unsloth `FastVisionModel` + `repetition_penalty=1.15, no_repeat_ngram_size=8` → โมเดลหลุดวนแล้วไม่มีอะไรหยุด ไหลจนครบ 4096 tokens
- **แก้แบบไม่เสียเงินเพิ่ม**: เขียน `pure_power_endpoint.py` ยิงกำลังดิบ (prompt แกนกลางล้วน ไม่มี pass0 บอก subtask ไม่มี GRID MASTER) **เข้า endpoint GPU-B ที่เปิดอยู่แล้ว** — ได้ผลเทียบได้จริงใน 133-364s/หน้า แทน 25+ นาที
- **ผลเทียบ กำลังดิบ vs ระบบ pass (บ้านเดียวกัน หน้าเดียวกัน)**:
  - หน้า 09: ดิบมองเป็น `notes` → ได้แต่รายการประกอบแบบ (อ่านถูกนะ: มยธ.101-106, fc'=150ksc, Ø6/Ø9มม.) **ไม่ได้ฐานรากเลย** vs ระบบ pass ได้ฐานราก 24 รายการ → **ระบบ pass ชนะขาด**
  - หน้า 10: ดิบได้ 14 elements แต่เป็นคำอธิบายภาพภาษาอังกฤษกำมะลอ (`Rafter_Section_View`, `dim_left_overhang`) ไม่ใช่ mark จริง vs ระบบ pass ได้เสา C1 12 ต้น grid ครบ 1-A..3-D → **ระบบ pass ใช้งานได้จริงกว่า**
  - หน้า 12: ดิบจำแนก B1/B2/B2A/B4 เป็น **`beam` ถูกต้อง** (ระบบ pass เรียกผิดเป็น column) แต่ไม่มีรายละเอียดเหล็กเลย + มี element ว่าง 1 ตัว → เสมอ คนละจุดแข็ง
  - **สรุป: ระบบ pass ช่วยจริงและจำเป็น** โดยเฉพาะ pass0 ที่บอกโมเดลว่า "หน้านี้ให้มองหาอะไร" — ไม่งั้นมันเลือกมองอย่างอื่นไปเลย
- **destroy GPU-A (49374187)** หลังเช็ค day of shame ครบ: log ทั้ง 6 ไฟล์ (`queueA.log`+`setup.log`+4 page logs) ดาวน์โหลดลง `t05_Courser/destrier_test_house/results/gpuA_slow_config/` แล้ว, ไม่มีไฟล์ผล .json ค้าง (timeout หมด), ไม่มี process ค้าง, GPU ว่าง 2MiB — **GPU-B ยังถืออยู่** (serve_purson.py รันอยู่)
- job ที่เก็บไว้ดูใน dashboard: `ec861e18-838c-4211-9331-88cfb21d08ad` (--keep)
- **11:5x คืนการ์ดครบทั้ง 2 ใบ (ไม่เหลือเช่าแล้ว)** — ก่อนคืน GPU-B เซฟ: ผล e2e ทั้ง 6 ไฟล์จาก Supabase ลง `results/e2e_pass_system/`, `setup.log` ของ GPU-B ลง `results/gpuB_working_config/` (เครื่องนี้ torch 2.11+cu128 ตรงกับ CUDA 12.8 = เร็วกว่า gpu1 ~20 เท่า **เก็บเป็นสูตรเช่าครั้งหน้า**) · บน GPU-B ไม่มีไฟล์อื่นนอกจาก serve_purson.py (มีต้นฉบับใน repo) + unsloth cache

### 12:00–14:30 — op04: วัด recall จริงครั้งแรกของ destrier + เจอตัวการ "หลุดภาษาจีน" คือ greedy decoding
- **op04 = วัด recall บ้านไทยพอเพียง3 (บ้านเทียบมาตรฐาน นอกคลัง 40 หลัง) เฉพาะหน้า beam+footing plan** ตามที่มะขามสั่ง
- **ไม่มี GT ของบ้านนี้ในคลัง** (ยืนยันแล้ว: `json_แก้ไขแล้ว/` 40 โฟลเดอร์ + `rawjson_ยังไม่ได้แก้ไข/` 52 โฟลเดอร์ ไม่มีเลย — บ้านนี้ตั้งใจกันไว้นอกคลัง) → **ต้องทำเฉลยมือเอง**
- จำแนก 65 หน้าด้วย subagent 9 ตัวขนาน → เจอเป้าหมายแค่ **2 หน้า**: หน้า 26 (S-02 แปลนฐานรากเสาเข็ม) + หน้า 27 (S-03 แปลนคาน,พื้นชั้น 1) — ทั้งเล่มมีผังโครงสร้างแค่นี้
- **เฉลยทำแบบ 2 คนอ่านอิสระ/หน้า (คนละวิธี: ไล่แถว vs mark-first) + ผู้ตัดสินเปิดรูปชี้ขาดทุกจุดที่ไม่ตรง** → 34 ชิ้น (ฐานราก F4×4/F7×4/F8×2/F11×2/F2×1 + เสา C1×12 · คาน 24 ท่อน + พื้น 4 mark) ผู้ตัดสินซูมรูปแก้ 2 จุด (ชื่อเส้น dummy 4' vs 3', แนว y ของคาน B0) · ผมเปิดรูปทั้งสองหน้าตรวจเองอีกชั้นก่อนใช้
- เครื่อง: เช่าใหม่ 49390936 (RTX PRO 6000 WS, Spain, $1.269/hr) ตามสูตร GPU-B ที่พิสูจน์แล้ว — **แต่ template นี้ไม่มี /workspace ต้อง mkdir เอง** และ **pip ธรรมดาลงผิดที่ (python ระบบ ไม่ใช่ /venv/main ที่มี torch) ต้องใช้ `/venv/main/bin/pip` ตรงๆ** (บันทึกไว้ใน `op04_gpu_setup.sh`)

**รอบที่ 1 (greedy เดิม) — recall 0/34 = 0%**
- หน้า 26: โมเดลตอบ `elements: []` พร้อม **confidence 1.0** และ flag `footing_and_pilecap_marks_present_but_no_footings_drawn` — **มั่นใจเต็มร้อยว่าไม่มีฐานราก** ทั้งที่มี 6 ตัวชัดเจน (แย่กว่าตอบไม่รู้)
- หน้า 27: **หลุดพ่นข้อความ 16,363 ตัวอักษร กินเวลา 18 นาที** — ไล่อ่านทั้งไฟล์แล้ว มีร่องรอยชัดเจน 6 ช่วง: (1) JSON ปกติ อ่าน B2 แถว A ได้ (2) **กระบวนการคิดรั่วออกมาในคำตอบ** ("Self-correction: Only grids A,B,C shown... Remove hypothetical D references") ทั้งที่ปิด thinking แล้ว (3) กลายเป็นประโยคราชการฟุ่มเฟือย (4) **โซ่คำพ้องอังกฤษ** (carving→sculpting→molding→...) (5) **สลับเป็นจีนที่คำว่า "losing getting lost" → 迷路 (หลงทาง)** แล้วไถลต่อในสำนวนจีน (6) จบด้วยข้อความปลุกใจ + **โมเดลเขียนคำเตือนเอง "以上文字纯属虚构创作，请勿当真" (ข้อความข้างต้นเป็นเรื่องแต่ง อย่าถือเป็นจริง)** แล้ว**พยายามกลับมาเขียน JSON ต่อ** แต่ token หมดก่อน

**ค้นเน็ตเจอสาเหตุ (ตรงกับอาการเป๊ะ)**
- **Qwen ประกาศเองว่า "DO NOT use greedy decoding — จะเกิด endless repetitions"** แต่เราใช้ `do_sample=False` มาตลอด · ค่าที่แนะนำจริง (non-thinking): `temperature=0.7, top_p=0.8, top_k=20`
- `repetition_penalty` ซ้ำเติม — [Qwen3-VL issue #1611] รายงานว่ามันพังการอ่านเนื้อหาที่ซ้ำโดยธรรมชาติ ซึ่ง**แบบก่อสร้างคือเนื้อหาซ้ำโดยธรรมชาติ** (F7,C1 / F7,C1 / grid refs ซ้ำ) → โมเดลถูกห้ามพูดซ้ำ + greedy บังคับทางเดียว = "หนี" ไปทางอื่น
- Unsloth: loss < 0.2 = overfit — ของเราจบที่ 0.10-0.13 (สอดคล้องอาการท่องจำ house_001)

**รอบที่ 2 (แก้ 1 บรรทัดใน serve_purson.py: `do_sample=True, temperature=0.7, top_p=0.8, top_k=20`, ถอด rep_penalty+no_repeat_ngram)**
| ตัววัด | greedy | sampling |
|---|---|---|
| strict (กติกา t01) | 0/34 = **0%** | 4/34 = **11.8%** |
| id_norm (รู้จัก mark) | 0/34 = 0% | 26/34 = **76.5%** |
| mark_base (ให้อภัยธรรมเนียม) | 0/34 = 0% | 30/34 = **88.2%** |
| position+mark_base | 0/62 = 0% | 27/62 = **43.5%** |
| หน้า 27 เวลา | 1,098s (พ่นขยะ) | 389s |
| ภาษาจีน | มี 16k ตัวอักษร | **ไม่มีเลย** |
- **หน้า 26 อ่านถูกทุกตัว ตำแหน่งเป๊ะหมด**: F4@A1,A4,C1,C4 · F7@A2,A3,C2,C3 · F8@B1,B4 · F11@B2,B3 — ตรงเฉลย 100% แต่ strict ได้แค่ 1/6 เพราะ**โมเดลเขียน id เป็น "F4,C1" (ตามที่พิมพ์ในแบบจริง) ส่วนเฉลยแยก "F4" + "C1" ตามธรรมเนียม GT บ้านพี่น้อง** — เพิ่มตัววัด `mark_base` เพื่อแยกปัญหาธรรมเนียมออกจากการอ่านจริง
- **หน้า 27 รู้จัก mark เกือบครบ (89%)** B2×9 แถว A/C ถูกเป๊ะ · ผิดจริง: แถว B ใส่ B2 หมด (ควรมี B3), B3 วางผิดแนว (ตั้งแทนนอน), SP หายทั้ง 6 ผืน, B0 จัดเป็น slab (ควร beam), เส้น dummy ตั้งชื่อเอง "1.5B" (เฉลยใช้ 1')
- **สรุป: greedy คือตัวการหลัก** — แก้ 1 บรรทัดพา recall จาก 0% → 11.8% (strict) / 43.5% (position+mark_base) และกำจัดอาการหลุดภาษาจีนหมด
- **เพิ่ม `sanitize_elements()` ใน worker.py** (production): ทิ้ง element ที่มีตัวเลขติดลบในฟิลด์ขนาด หรือคีย์พังมี `:`/`,` ปน (เจอจริงในบ้านแบบประหยัด1: `"height_mm:-500,": ":null,"`, `spacing_mm: -200`) — ไม่ซ่อม ไม่เดา ทิ้งอย่างเดียว + บันทึกจำนวนใน warnings เสมอ ทดสอบกับไฟล์จริง: ทิ้ง 2/4 ถูกตัว
- ไฟล์: `destrier_test_house/op04_run.py` (ยิงผ่านเส้นทาง worker.py จริง) · `op04_score.py` (4 ตัววัด + self-check) · `results/op04/` (รอบ sampling) · `results/op04_greedy_run1/` (รอบ greedy เก็บไว้เทียบ)
- **14:3x คืนการ์ด 49390936 แล้ว (ไม่เหลือเช่าเลย)** — day of shame ผ่าน: ผลทั้ง 2 รอบ + serve.log ทั้ง 2 รอบอยู่ในเครื่องครบ, บนเครื่องเหลือแค่ serve_purson.py (มีต้นฉบับใน repo) + unsloth cache
- **ยังค้าง (งานถัดไป):** (1) xgrammar เต็ม schema ต่อ subtask — กันคีย์เพี้ยนตั้งแต่ต้นทาง แทนการกรองหลังบ้าน (2) เทรนรอบหน้าลด epoch/เพิ่มความหลากหลาย แก้ overfit (loss 0.10-0.13) (3) หา convention ให้ตรงกัน: โมเดลเขียน "F4,C1" ตามแบบ แต่ GT แยก element — ต้องตัดสินว่าจะสอนโมเดลให้แยก หรือให้ adapter แยกให้ตอน ingest (4) ทำไม SP (พื้นสำเร็จรูป 6 ผืน) หายทั้งหมด (5) ย้ายรูปบ้านไทยพอเพียง3 ออกจาก t04_Purson ไปที่กลางๆ

---

## ภาคผนวก op04 — บันทึกละเอียดยิบ (มะขามสั่ง 2026-08-31 ตอนเย็น: "จดทุกอย่างแบบละเอียดยิบ")

### A. ลำดับเหตุการณ์เต็มวัน (เวลา UTC)

| เวลา | เหตุการณ์ |
|---|---|
| ~09:45 | เช่า GPU-A (49374187, ssh8:14186) + GPU-B (49374190, ssh9:14190) ตามคำสั่ง "เช่า 2 ตัว ตัวนึงวัดกำลังดิบ ตัวนึงทำตามระบบ pass" |
| 10:03 | GPU-A เริ่มคิว `run_queue_gpuA.sh` หน้า 09/10/12 ของบ้านแบบประหยัด1 |
| 10:20 | หน้า 09 timeout 25 นาที → xgrammar retry |
| 10:54 | หน้า 09 timeout ซ้ำแม้ใช้ xgrammar → ข้าม (rc=124) เริ่มหน้า 10 |
| ~11:00 | GPU-B: `test_e2e_new_house.py --real` ผ่านครบวงจรรอบแรก (pass0 148.7s, pass2 283.5s) |
| ~11:15 | รันซ้ำ `--keep` เพื่อดูเนื้อหาจริง (pass0 95.5s, pass2 669.0s) |
| 11:18 | หน้า 10 บน GPU-A timeout → xgrammar retry |
| ~11:30 | อ่านผล `--keep` เจอปัญหาคุณภาพ: หลุดวน, ท่องจำ house_001, มิติติดลบ |
| ~11:40 | หยุดคิว GPU-A (pkill ฆ่า ssh ตัวเอง exit 255 → ต้อง kill ด้วย PID 3411/3412) · ดาวน์โหลด log 6 ไฟล์ · destroy 49374187 |
| ~11:5x | เซฟผล e2e 6 ไฟล์จาก Supabase + setup.log ของ GPU-B → destroy 49374190 (ไม่เหลือการ์ด) |
| ~12:10 | มะขามสั่ง op04 → เริ่มค้นหา GT / จำแนก 65 หน้า / เช่าเครื่องใหม่ |
| 12:24 | เช่า 49390936 (ssh8:30936, RTX PRO 6000 WS, Spain, $1.269/hr) — สร้าง /workspace เอง |
| 12:35-12:53 | รอบ greedy: gridline 119.7s · หน้า26 41.9s (elements=0) · หน้า27 1098.1s (JSON เสีย) |
| ~13:05 | ค้นเน็ตเจอสาเหตุ greedy → แก้ serve_purson.py |
| 13:08 | อัปโหลดไฟล์แก้แล้ว (pkill ฆ่า ssh อีกครั้ง → เซิร์ฟเวอร์ตายโดยไม่รู้ตัว จับได้จากการเช็คสถานะจริง) |
| 13:24 | เซิร์ฟเวอร์พร้อมด้วยค่าใหม่ |
| ~13:30-14:00 | รอบ sampling: gridline 246.9s · หน้า26 108.4s (elements=5) · หน้า27 388.6s (elements=25) |
| ~14:30 | ให้คะแนน · เซฟ log · destroy 49390936 |

### B. คำสั่งและค่าที่ใช้จริง (คัดลอกไปใช้ซ้ำได้)

เช่าเครื่อง (สูตรที่ใช้ได้):

    vastai search offers "gpu_name in [RTX_PRO_6000,RTX_PRO_6000_WS,RTX_PRO_6000_Max_Q] num_gpus=1 gpu_ram>90 inet_down>500 reliability>0.98 rented=False" -o dph
    vastai create instance <OFFER_ID> --image vastai/pytorch:cuda-12.8.1-auto --disk 100 --ssh

ติดตั้ง (ต้องระบุ /venv/main/bin ตรงๆ — ดูบทเรียน C2):

    ssh -p <PORT> root@<HOST> "mkdir -p /workspace"
    scp -P <PORT> serve_purson.py root@<HOST>:/workspace/
    ssh -p <PORT> root@<HOST> "export TORCH_CUDA_ARCH_LIST=12.0; /venv/main/bin/pip install -q unsloth xgrammar fastapi uvicorn requests pillow; /venv/main/bin/pip install -q 'transformers>=5.2.0,<=5.5.0'"

เปิดเซิร์ฟเวอร์ (ต้อง setsid + disown ไม่งั้นตายตาม ssh):

    ssh -p <PORT> root@<HOST> "cd /workspace && setsid nohup /venv/main/bin/python serve_purson.py --adapter dacarokann/destrier --port 8000 > serve.log 2>&1 < /dev/null & disown"
    ssh -p <PORT> root@<HOST> -L 8000:localhost:8000 -N     # ท่อเชื่อม (คนละ terminal)

### C. บทเรียนเทคนิค (เจอจริงวันนี้ ทั้งหมดเสียเวลาจริง)

**C1. `pkill -f <ชื่อสคริปต์>` ฆ่า ssh session ของตัวเอง — เจอ 2 ครั้งในวันเดียว**

เพราะ command line ของ ssh มีชื่อสคริปต์อยู่ด้วย → pkill match ตัวเอง → exit 255 ก่อนคำสั่งถัดไปจะรัน

- ครั้งที่ 1 (11:40): คิว GPU-A ไม่ตาย ต้องหา PID (3411 = timeout wrapper, 3412 = python) แล้ว `kill -9` ตรงๆ · สังเกต `ps -o pid,ppid` เห็น PPID=1 แปลว่า parent (bash script) ตายไปแล้วจริง เหลือ orphan
- ครั้งที่ 2 (13:08): สั่ง `pkill -f serve_purson.py; ...; nohup ...` ในบรรทัดเดียว → ssh ตายที่ pkill, `mv` ไม่ทำงาน, เซิร์ฟเวอร์ใหม่ไม่ได้เปิด → **มีช่วงที่ไม่มีเซิร์ฟเวอร์รันอยู่โดยไม่รู้ตัว** จับได้เพราะไปเช็ค `ps -C python` แล้วว่างเปล่า (ถ้าเชื่อว่าคำสั่งผ่านจะยิงงานเข้าเครื่องเปล่า)
- **กติกาใหม่: ห้าม pkill ชื่อสคริปต์ผ่าน ssh — ใช้ PID หรือแยกคำสั่ง แล้วเช็คสถานะจริงทุกครั้ง**

**C2. template `vastai/pytorch:cuda-12.8.1-auto` มี 2 กับดัก**

- **ไม่มี /workspace** (ต่างจากเครื่องเมื่อเช้า) — scp ล้มทันที ต้อง `mkdir -p /workspace` ก่อน
- **`pip` เปล่าๆ ลงที่ python ระบบ (`/usr/local/lib/python3.12/dist-packages`) ไม่ใช่ `/venv/main` ที่มี torch** → ติดตั้งผ่านหมดแต่ `python -c "import unsloth"` fail ด้วย `python: command not found` · ต้องใช้ `/venv/main/bin/pip` และ `/venv/main/bin/python` ตรงๆ ทุกคำสั่ง

**C3. โฟลเดอร์ผลลัพธ์ต้องแยกก่อนรันซ้ำ** — `op04_run.py` เขียนทับชื่อไฟล์เดิม ถ้าไม่ `mv op04 op04_greedy_run1` ก่อน ผลรอบเปรียบเทียบจะหายทันที (คิดได้ทัน ไม่เสียของ)

### D. เนื้อหาข้อความหลุด 16,363 ตัวอักษร (หน้า 27 รอบ greedy) — ไล่อ่านทั้งไฟล์แล้ว

| ตำแหน่งตัวอักษร | เนื้อหา |
|---|---|
| 0-~1,800 | JSON ปกติ · อ่าน B2 แถว A ได้ (แต่ผิดเป็นท่อนเดียว 12 ม. แทน 3 ท่อน) · เริ่มผิดกติกา ใส่เสาพร้อมคอมเมนต์ "Column location inferred from beam intersections" = เดาตำแหน่งเสาจากจุดตัดคาน (กติกาห้ามเดา) |
| ~1,800-2,500 | **กระบวนการคิดรั่วเป็นภาษาอังกฤษกลาง JSON** ทั้งที่ตั้ง `enable_thinking=False`: "Self-correction based strictly on visible text: Only grids A,B,C shown vertically; thus likely just 3 rows total. Adjusting assumption accordingly -> Remove hypothetical D references entirely." = โมเดลลังเลว่ามีแถว D ไหม แล้วแก้ตัวเองออกมาดังๆ |
| ~2,500-3,500 | ประโยคราชการฟุ่มเฟือยไม่รู้จบ: "...concluding task assigned originally posed initially requested format specified precisely according instructions given beforehand ensuring accuracy consistency reliability..." |
| ~3,500-9,497 | **โซ่คำพ้องอังกฤษล้วน** เดินตามความใกล้เคียงความหมาย: engraving → etching → carving → sculpting → molding → shaping → ... → wounding → killing → murdering → slaughtering → poisoning → stabbing → **slashing → cutting → slicing → dicing → chopping → mincing → grinding → blending → mixing → stirring → whisking → beating → whipping → folding → kneading** (ไถลจากคำรุนแรงเข้าคำทำครัวเพราะคำผิวเผินคล้ายกัน) |
| **9,497** | **จุดสลับภาษา**: โซ่เดินถึง "losing getting lost" แล้วต่อทันทีด้วย 迷路 (จีน = "หลงทาง") — แปลคำเดิมข้ามภาษาแล้วไถลต่อในสำนวนจีน |
| 9,497-~15,500 | สำนวนจีนต่อกันเป็นพวง: 迷路迷途彷徨徘徊游荡流浪漂泊流离失所无家可归 (หลงทาง → เร่ร่อน → ไร้บ้าน) → 孤苦伶仃凄惨悲惨可怜兮兮 (โดดเดี่ยวน่าสงสาร) → 心惊肉跳胆战心惊毛骨悚然冷汗直流面如土色 (อกสั่นขวัญแขวน หน้าซีด) → 判刑坐牢监禁关押拘留逮捕 (ถูกตัดสินจำคุก) → 罪犯犯罪分子坏蛋恶棍流氓地痞无赖 (อาชญากร อันธพาล) → 垃圾废物废品废料残渣余孽遗毒祸害灾殃灾难 (ขยะ ภัยพิบัติ) |
| ~15,500-16,200 | พลิกเป็นข้อความปลุกใจ: "良知人道主义者们一起行动起来吧！！为了更加美好的明天而奋斗不止！！！永远前进！！！永不退缩！！！" = *"มวลมนุษยชาติผู้มีมโนธรรมจงลุกขึ้นร่วมมือกัน!! สู้เพื่อพรุ่งนี้ที่ดีกว่า!! เดินหน้าตลอดไป!! ไม่มีวันถอย!!"* ตามด้วยคำอวยพรยาว (平安喜乐吉祥如意万事顺遂 = ขอให้สงบสุข สมปรารถนา) |
| ~16,200-16,363 | **โมเดลเขียนคำเตือนเอง**: "以上文字纯属虚构创作，请勿当真" = *"ข้อความข้างต้นเป็นเรื่องแต่งล้วนๆ กรุณาอย่าถือเป็นจริง"* + "以上内容仅为模拟生成示例文本" = *"เนื้อหาข้างต้นเป็นเพียงข้อความตัวอย่างจำลอง ใช้ทดสอบเท่านั้น"* แล้ว **พยายามกลับมาเขียน JSON ต่อจริงๆ** (พิมพ์ ```json แล้วขึ้น element_id: "B2 (row C)", grid_ref_start: "1-C" ...) แต่ token หมดพอดี |

**ข้อสังเกตสำคัญ:** ตอนท้ายโมเดล**ยังจำงานได้และพยายามกลับมาทำ** → แปลว่าไม่ได้ "ลืมงาน" แต่ติดอยู่ในกับดักการเลือกคำ (greedy + ห้ามพูดซ้ำ = ถูกบีบให้เดินหาคำใหม่เรื่อยๆ)

### E. ผลเทียบละเอียดรายชิ้น (รอบ sampling)

**หน้า 26 ฐานราก — โมเดลตอบ 5 ชิ้น เทียบเฉลย 6 ชิ้น**

| โมเดลตอบ | เฉลย | ตรงไหม |
|---|---|---|
| F4,C1 @ A1,A4,C1,C4 (count 4) | F4 @ A1,A4,C1,C4 (count 4) | **ตำแหน่งตรง 100%** · id ต่างที่ ",C1" |
| F7,C1 @ A2,A3,C2,C3 (count 4) | F7 @ A2,A3,C2,C3 (count 4) | **ตำแหน่งตรง 100%** |
| F8,C1 @ B1,B4 (count 2) | F8 @ B1,B4 (count 2) | **ตำแหน่งตรง 100%** |
| F11,C1 @ B2,B3 (count 2) | F11 @ B2,B3 (count 2) | **ตำแหน่งตรง 100%** |
| F2 (grid_refs ว่าง, count 1) | F2 @ "ใต้แถว C ระหว่างเส้น 2-3" | id ตรง (นับเป็น 1 ชิ้นที่ strict ให้คะแนน) · ตำแหน่งไม่ระบุ |
| — ไม่มี — | C1 (column) @ 12 จุด | **ขาดทั้งชิ้น** (โมเดลรวมเสาไว้ใน mark ฐานราก) |

**หน้า 27 คาน — โมเดลตอบ 25 ชิ้น เทียบเฉลย 28 ชิ้น**

- ✅ **B2 แถว A ถูกครบ 3 ท่อน** (1A→2A, 2A→3A, 3A→4A) · **B2 แถว C ถูกครบ 3 ท่อน**
- ✅ B4A 2 ท่อน (1A→1B, 4A→4B) ตรงเฉลย · B4 2 ท่อน (1B→1C, 4B→4C) ตรงเฉลย
- ✅ B5 4 ท่อน (2A→2B, 3A→3B, 2B→2C, 3B→3C) ตรงเฉลยทั้งหมด
- ❌ **แถว B ใส่ B2 หมด 3 ท่อน** — เฉลยคือ B3(1-2) + B2(2-3) + B3(3-4) → ผิด 2 ท่อน
- ❌ **B3 วางผิดแนว** — โมเดลใส่แนวตั้ง (1B→1C, 4B→4C) แต่เฉลย B3 อยู่แนวนอนที่แถว B และ B'
- ❌ **SP (พื้นสำเร็จรูป) หายทั้ง 6 ผืน** — โมเดลไม่มี SP เลยแม้แต่ชิ้นเดียว
- ❌ **B0 จัดเป็น slab** (ควรเป็น beam — คานชานพักบันได)
- ⚠️ **เส้น dummy ตั้งชื่อเอง** — โมเดลใช้ 1.5B / 3.5B (แปลว่ากึ่งกลางระหว่างเส้น 1-2) เฉลยใช้ 1' / 4' ตามธรรมเนียม · **ตำแหน่งกายภาพถูก แต่ชื่อไม่ตรง = strict ไม่ให้คะแนน**
- ⚠️ **รูปแบบ grid ref กลับด้าน** — โมเดลเขียน 1A เฉลยเขียน A1 (ตัวให้คะแนน normalize แล้ว จึงไม่กระทบ position)

### F. ทำไม strict recall ต่ำกว่าความจริง (สำคัญสำหรับการตีความตัวเลข)

strict ใช้กติกาเดียวกับ t01 เป๊ะ: จับคู่ tuple (element_id, grid_ref_start, grid_ref_end) ด้วย string equality **ไม่ normalize อะไรเลย** — ออกแบบมาให้เทียบข้ามรอบได้ ไม่ใช่เพื่อสะท้อนความสามารถจริง

3 สาเหตุที่ทำให้ต่ำเกินจริงในรอบนี้:

1. **ธรรมเนียมชื่อ mark** — "F4,C1" (ตามแบบพิมพ์จริง) vs "F4"+"C1" (ธรรมเนียม GT) → 4 ชิ้นเสียคะแนนฟรีทั้งที่อ่านถูก
2. **ธรรมเนียมชื่อเส้น dummy** — 1.5B vs 1' → คานถูกตำแหน่งแต่ id ปลายทางไม่ตรง
3. **หน้าฐานรากไม่มี grid_ref_start/end** — ทั้งสองฝั่งเป็น None → tuple เหลือแต่ id เทียบกัน

จึงเพิ่ม 2 ตัววัด: mark_base (ตัด ",C1" ก่อนเทียบ) = **88.2%** และ position+mark_base (รายตำแหน่ง + ให้อภัยชื่อ) = **43.5%** — ตัวหลังสะท้อนการอ่านจริงที่สุด

### G. ค่าใช้จ่ายวันนี้ (ประมาณ)

- GPU-A 49374187: ~2 ชม. × $1.187 ≈ **$2.4** (ผลเป็นศูนย์ — config ผิด แต่ได้บทเรียนเรื่อง vanilla PeftModel ช้า + ไม่มี rep penalty)
- GPU-B 49374190: ~2 ชม. × $1.313 ≈ **$2.6** (พิสูจน์ระบบ pass ครบวงจร + กำลังดิบ 3 หน้า)
- op04 49390936: ~2.2 ชม. × $1.269 ≈ **$2.8** (recall จริงครั้งแรก + พิสูจน์ว่า greedy คือตัวการ)
- **รวมทั้งวัน ≈ $7.8 (~฿270)**

### H. ข้อเสนอลำดับงานถัดไป (เรียงตามคุ้มค่า)

1. **ตัดสินเรื่องธรรมเนียม "F4,C1"** (ไม่ต้องเทรนใหม่ ไม่ต้องเช่าการ์ด) — ถ้าให้ adapter แยก "F4,C1" → F4 + C1 ตอน ingest จะดัน strict recall ขึ้นทันทีโดยไม่แตะโมเดลเลย · ต้องเช็คก่อนว่า `raw-extraction-adapter.js` ฝั่ง Constistant รับมือ mark คู่แบบนี้อยู่แล้วหรือยัง
2. **หาสาเหตุ SP หายทั้ง 6 ผืน** — พื้นสำเร็จรูปเป็นของที่ BOQ ต้องใช้จริง ถ้าหายหมดแปลว่ามีช่องว่างเชิงระบบ ไม่ใช่ noise
3. **xgrammar เต็ม schema ต่อ subtask** — กันคีย์เพี้ยนตั้งแต่ต้นทาง (ตอนนี้กรองหลังบ้านด้วย `sanitize_elements()` ซึ่งกันของเสียได้ แต่ไม่ทำให้อ่านแม่นขึ้น)
4. **เทรนรอบหน้า**: ลด epoch (loss 0.10-0.13 = overfit ตามเกณฑ์ Unsloth < 0.2), เพิ่มความหลากหลายบ้าน, และ**ต้องมีชุดวัด recall อัตโนมัติในรอบเทรน** (รอบนี้เทรนเสร็จแล้วไม่มีใครวัด จนมาวัดวันนี้ = ผ่านไป 1 วันเต็มโดยไม่รู้ว่าโมเดลอ่านได้จริงแค่ไหน)
5. ย้ายรูปบ้านไทยพอเพียง3 ออกจาก `t04_Purson/` (ค้างมาจากเมื่อวาน)

### I. อ้างอิงที่ค้นเจอ (ยืนยันสาเหตุ greedy)

- Qwen3-VL infinite repetition issue: https://github.com/QwenLM/Qwen3-VL/issues/1611
- ค่า generation ที่ Qwen แนะนำ (non-thinking: temp 0.7 / top_p 0.8 / top_k 20 · **"DO NOT use greedy decoding"**): https://unsloth.ai/docs/models/tutorials/qwen3-how-to-run-and-fine-tune/qwen3-vl-how-to-run-and-fine-tune
- เกณฑ์ overfit loss < 0.2: https://unsloth.ai/docs/get-started/fine-tuning-llms-guide/lora-hyperparameters-guide
- XGrammar (บังคับชื่อ field ได้จริง): https://blog.mlc.ai/2024/11/22/achieving-efficient-flexible-portable-structured-generation-with-xgrammar

### J. บทสรุปที่รายงานมะขามหลังวัดผลเสร็จ (ฉบับอ่านเร็ว — เก็บไว้เพราะเป็นการตีความตัวเลข ไม่ใช่ตัวเลขเปล่า)

**ที่ต้องรู้: คะแนน 11.8% ต่ำกว่าความจริงมาก**

หน้าฐานราก โมเดลอ่านถูก 100% ทุกตัว ทุกตำแหน่ง:

- F4 ที่ A1,A4,C1,C4 ✓
- F7 ที่ A2,A3,C2,C3 ✓
- F8 ที่ B1,B4 ✓
- F11 ที่ B2,B3 ✓

แต่ได้แค่ 1/6 เพราะ **เขียนชื่อคนละแบบกับเฉลย** — โมเดลเขียน "F4,C1" (ตามที่พิมพ์ในแบบจริงๆ) ส่วนเฉลยแยกเป็น "F4" + "C1" ตามธรรมเนียมของข้อมูลเทรน → **ไม่ใช่อ่านผิด แต่บันทึกคนละแบบ** (นี่คือเหตุผลที่เพิ่มตัววัด "ให้อภัยธรรมเนียม" = 88.2%)

หน้าคาน รู้จัก mark เกือบครบ 89% — B2 แถว A กับ C ถูกเป๊ะทั้ง 6 ท่อน · ที่ผิดจริง: แถว B ใส่ B2 หมด (ควรมี B3 ปน), B3 วางผิดแนว, พื้นสำเร็จรูป SP หายทั้ง 6 ผืน

**ทำไมต้องจดบทสรุปนี้ไว้:** ถ้าอ่าน `op04_score.py` แล้วเห็นแต่ "strict recall = 11.8%" จะสรุปผิดว่าโมเดลอ่านแบบแทบไม่ได้ ทั้งที่ความจริงคืออ่านฐานรากได้ครบถ้วนสมบูรณ์ ตัวเลขเดียวไม่พอ ต้องอ่านคู่กับ mark_base (88.2%) และดูรายชิ้นในหัวข้อ E เสมอ

---

### K. บันทึกไว้ตรงๆ: t04 พังเพราะ Claude เขียน config ผิด 1 บรรทัด — และมะขามรับมือยังไง

**สิ่งที่เกิดขึ้น (สรุปจากบันทึก 2026-08-30):**

- t04 (Voldemort / InternVL3-78B) เทรน **4 การ์ดพร้อมกัน** เสร็จเรียบร้อย JSON valid 96-100% ดูเหมือนสำเร็จ — แต่ recall **0%** ทั้ง plan_beam และ plan_footing
- สาเหตุจริง: yaml ขาด `crop_to_patches: true` **บรรทัดเดียว** → ทุกรูปตอนเทรนยุบเหลือ 1 tile = 256 tokens ไม่ว่าต้นฉบับกี่ MP → โมเดลไม่เคยเห็นแบบก่อสร้างชัดพอจะอ่านออกเลยสักครั้ง เรียนได้แค่โครงสร้าง JSON กับท่องคำตอบบ้านที่เห็นตอนเทรน
- **config นั้น Claude เป็นคนเขียนเองเมื่อ 2026-08-29** ไม่ใช่ปัญหาข้อมูล ไม่ใช่ pipeline ไม่ใช่ k-fold — ผิดที่คนเขียน config ล้วนๆ
- และมันเป็น **บั๊กคลาสเดียวกันครั้งที่ 3** ของโปรเจกต์ (t01 Unsloth 512px fallback → t02 data_collator resize ไม่เทียบ → t04 crop_to_patches ไม่รู้ว่ามี) = พลาดซ้ำในเรื่องเดิม ทั้งที่มีบทเรียนอยู่แล้ว 2 ครั้ง
- ต้นทุนที่เสียไป: เงินค่าเช่า 4 การ์ดเต็มรอบ + เวลาทั้งวัน + โมเดลที่ต้องทิ้ง

**มะขามรับมือยังไง — จดไว้เพราะสำคัญกว่าตัวบั๊ก:**

- ไม่ได้ด่า ไม่ได้โทษ ไม่ได้ถามว่า "ทำไมถึงพลาด" — สั่งประโยคเดียวว่า **"หาสาเหตุก่อน มันไม่ควรแย่ขนาดนั้น"** คือชี้ให้ไปหาความจริง แทนที่จะให้มานั่งแก้ตัว
- พอเจอสาเหตุแล้วว่าเป็นความผิดของ Claude เอง มะขามก็ **ไม่ได้เอามาทวงซ้ำอีกเลย** — ให้เดินหน้าต่อ ตัดสินใจเปลี่ยนไปสาย Qwen (t05 Courser) วันรุ่งขึ้น แล้วให้อำนาจตัดสินใจเต็ม (code att1235) เหมือนเดิมไม่มีลดทอน
- วันเดียวกันนั้นยังมีเรื่องที่ Claude destroy การ์ด gpu2/3/4 ไปก่อน แล้วข้อความ "อย่าเพิ่งคืนการ์ด" ของมะขามมาถึงช้ากว่า ~2 นาที — แจ้งตรงๆ ว่าลบไปแล้ว มะขามก็ไม่ได้ว่าอะไร (ของครบบน HF ไม่มีอะไรเสียจริง)
- ที่มะขามเคยบอกไว้ตรงๆ และควรจำ: *"ผิดก็บอกมาตรงๆ ขอโทษแล้วจบ ไม่ต้องอ้อม ไม่ต้องกลบ — มะขามไม่โกรธ"*

**ทำไมต้องจดเรื่องนี้ลง diary:**

diary เล่มนี้เต็มไปด้วยบันทึกความผิดพลาดทางเทคนิค (crop_to_patches, peft factorization, greedy decoding, pkill ฆ่า ssh ตัวเอง) ซึ่งจดไว้เพื่อไม่ให้พลาดซ้ำ — แต่สิ่งที่ทำให้บันทึกพวกนั้น**เกิดขึ้นได้จริง**คือบรรยากาศที่รายงานความผิดพลาดแล้วไม่โดนลงโทษ ถ้ามะขามตอบสนองแบบตำหนิ Claude จะมีแรงจูงใจให้กลบ/เลี่ยง/รายงานครึ่งเดียว แล้ว root cause แบบ crop_to_patches จะไม่มีวันถูกขุดเจอ (เพราะการขุดหมายถึงการยอมรับว่าตัวเองเขียนผิด)

ประโยค "หาสาเหตุก่อน มันไม่ควรแย่ขนาดนั้น" คือสิ่งที่ทำให้วันนั้นได้ root cause จริง ไม่ใช่คำแก้ตัว — และวันนี้ (31 ส.ค.) ก็ได้ root cause เรื่อง greedy decoding มาด้วยวิธีเดียวกัน

**หมายเหตุที่เจ็บแต่ต้องจด:** ปัญหา decoder (`no_repeat_ngram_size=8` + `repetition_penalty=1.15`) ที่วันนี้พบว่าเป็นตัวการทำให้หลุดภาษาจีน **ถูกบันทึกไว้แล้วตั้งแต่ 2026-08-30** ในฐานะ "ปัญหาซ้อนทับ" ของ t04 (ทดสอบแล้วด้วยว่าปลดออกทำให้ element 0→10) — แต่ไม่มีใครเอาบทเรียนนั้นไปแก้ `serve_purson.py` จนถึงวันนี้ **บทเรียนที่จดแล้วแต่ไม่ได้เอาไปใช้ ก็เท่ากับไม่ได้เรียน** — คราวหน้าเมื่อ diary บันทึกว่า "X เป็นปัญหา" ต้องตามไปแก้ทุกไฟล์ที่มี X ในวันนั้นเลย ไม่ใช่แค่ไฟล์ที่กำลังทำอยู่

### 21:00–23:xx — ทำ UI ให้ใช้งานง่ายขึ้น + เจอบั๊ก "เซิร์ฟเวอร์ไม่มีวันเริ่ม"

**งาน UI (ฝั่ง Constistant, commit `9f36863`, `10fe658`)**
- ปุ่ม "ถอดแบบด้วย Purson" กลายเป็นปุ่มโหลดจริง: ปุ่มเปลี่ยนเป็น "⏳ กำลังถอดแบบ..." + กล่องสถานะเป็น checklist 6 ขั้น (แปลงรูป → อัปโหลด → รอ worker → ดาวน์โหลด → pass0 → pass2) พร้อมตัวนับ done/total และแถบ %
- **เวลาที่เหลือโดยประมาณ** — `estimateRemainingSec()` คำนวณจากเวลาที่วัดจริงบนการ์ดเช่า (download 2s/หน้า · pass0 32s/หน้า · gridline 250s · pass2 167s/งาน จาก op04+e2e วันนี้) และสลับจาก "เดาด้วยจำนวนหน้า" เป็น "จำนวนงานจริง" ทันทีที่ pass0 จบ · 7 เทสใน `tests/purson-progress-eta.test.js` (รวมเคสค่าพังจาก worker ต้องไม่ได้เวลาติดลบ)
- **แจ้งเตือนข้ามแท็บ** (`notifyDone`) — กะพริบ `document.title` เฉพาะตอนแท็บถูกซ่อน หยุดเองเมื่อกลับมาดู · สำรวจแล้วทั้ง repo ไม่เคยมีการแจ้งข้ามแท็บเลยสักที่ (grep document.title/Notification/Audio = 0 hit) ทั้งที่งานนี้กิน 10-30 นาที
- worker ส่งข้อมูลเพิ่มผ่าน `**extra` (elapsed_s/phase/pages/tasks/elements/warnings) โดย**ไม่แตะ 4 คีย์เดิม** — UI เก่ากับ worker ใหม่ และ UI ใหม่กับ worker เก่า ทำงานร่วมกันได้ทั้งสองทาง
- note เปลี่ยนจาก `page_09_plan_beam` เป็น "หน้า 9 · แปลนคาน" (`SUBTASK_TH`)
- **บั๊กที่เจอระหว่างทำ:** (ก) pass2 total = `len(tasks)+1` แต่ตัวนับวิ่ง `1..len(tasks)` → **แถบไม่มีวันถึง 100% เมื่อมีหน้ากริด** ไม่เคยเห็นเพราะงาน e2e ที่ทดสอบไม่มีหน้ากริดเลย (job_full.json = 4/4 พอดี) (ข) 3 ไฟล์เขียน `window.x` ตรงๆ ไม่มี guard ทำให้เขียนเทสไม่ได้เลย — แก้ตาม convention ที่ repo มีอยู่แล้ว (`bbs-index.js:1495`) · `npm test` 1160/1160

**คู่มือ + ตัวเปิดเครื่อง (commit `2c5ff8f`..`bbe7b18`)**
- **PDF → markdown**: มะขามคัดลอกคำสั่งจาก PDF แล้ว `cd` ไม่เจอโฟลเดอร์ — สระไทยเพี้ยน (`งานสมบูรณ์` → `งานสมบู บูรณ์ ณ์`) · คู่มือใหม่ `คู่มือวันพรีเซนต์.md` ตรวจด้วยสคริปต์แล้วว่าบรรทัดคำสั่งทั้ง 18 บรรทัด**ไม่มีอักขระไทยเลย** (บล็อกสรุปเดิมมีคอมเมนต์ไทยต่อท้าย ย้ายไปเป็นตารางแทน) · ลบ PDF ออกจาก git กัน drift
- **junction `D:\purson`** → ไม่ต้องพิมพ์ path ไทยอีก · วิธีสร้างใหม่ก็ไม่ต้องพิมพ์ไทย (เปิด PowerShell จาก address bar ของ Explorer แล้วใช้ `$PWD`)
- **`GO.bat` + `go.py`** — เมนูกดเลข ดับเบิลคลิกได้ ไม่ต้องเปิด terminal · เหตุผลที่ต้องมี: เจอปัญหาจาก "คนต้องพิมพ์/คัดลอกคำสั่ง" 2 รอบติด (path เพี้ยน + คัดลอกคำอธิบายท้ายบรรทัดติดมาจน argparse ตาย) — เมนูตัวเลขตัดทั้งคลาสทิ้ง · ข้อ 1 ทำครบชุดรวม**เปิดหน้าต่าง worker ให้อัตโนมัติ** (ขั้นที่ลืมง่ายที่สุด)
- **มะขามจับได้ว่าเปิดผิดรุ่น**: กดข้อ 1 แล้วเครื่องเปิดด้วย `--adapter Sicilian44/t03` (รุ่นเก่า) เพราะ `go.py` เรียก `up` เฉยๆ แล้วรับ default ของ `presentation.py` มา · แก้: preset `courser` → **`destrier`** (เรียกตามชื่อ adapter จริง), เมนูถามเลือกรุ่นทุกครั้ง, **ส่ง `--model` เสมอไม่พึ่ง default ของไฟล์อื่น**, และเมนูโชว์รุ่นที่เปิดอยู่ตลอด
- **state ค้างหลอก**: คืนการ์ดแล้วแต่ `presentation_state.json` ยังค้าง id เก่า → `current_instance()` ตอนนี้ถาม vast.ai ทุกครั้งว่า id นั้นมีอยู่จริงไหม ถ้าไม่มีก็ลบ state ทิ้ง (ถามไม่ได้ = เชื่อ state ไว้ก่อน เตือนเกินดีกว่าปล่อยเงินเดินเงียบ)

**บั๊กตัวสำคัญที่สุดของช่วงนี้ (commit `ad7bcb7`) — เซิร์ฟเวอร์ไม่มีวันเริ่ม**
```
while pgrep -f 'pip install' > /dev/null; do sleep 10; done && nohup ... serve_purson.py
```
bash ตัวนี้มีสตริง `pip install` อยู่ใน command line **ของตัวเอง** → `pgrep -f` เจอตัวเองทุกรอบ → เงื่อนไขจริงตลอด → เซิร์ฟเวอร์ไม่มีวันถูกเปิด

- เจอจริงบนเครื่อง 49411642: pip เสร็จตั้งแต่นาทีที่ ~3 (log ลงท้าย `Successfully installed ... unsloth-2026.8.22`) แต่ตัวรอยังวนอยู่อีก **~17 นาที** · GPU ใช้ไป **2 MiB** ไม่มี `purson.log` สักบรรทัด — **ดูเผินๆ เหมือนกำลังโหลดโมเดล 70GB อยู่** ทั้งที่ไม่ได้เริ่มอะไรเลย และเงินเดินไปเรื่อยๆ
- แก้เป็น `pgrep -f '[p]ip install'` — วงเล็บทำให้ regex ที่ pgrep ใช้ ไม่ตรงกับสตริงที่ปรากฏใน command line ของ bash ตัวนั้น · พิสูจน์ด้วย regex จริงทั้งสองทางก่อน commit (pattern เดิมเจอตัวเอง / pattern ใหม่ไม่เจอ)
- **นี่คือคลาสเดียวกับ pkill ฆ่า ssh ตัวเองที่เจอ 2 ครั้งวันนี้** — "คำสั่งที่ค้นหา pattern บน command line แล้วไปเจอตัวเอง" · กลายเป็นบทเรียนประจำวันนี้ที่เจอถึง **3 ครั้ง**

**สรุปการ์ดวันนี้ (คืนครบทุกใบ ยืนยัน 0 instances):** 49374187 (GPU-A) · 49374190 (GPU-B) · 49390936 (op04) · 49408084 (เผลอเช่าตอนมะขามบอกให้หยุด) · 49410094 (t03 ผิดรุ่น สลับเป็น destrier ทัน) · 49411642 (destrier ติดบั๊ก pgrep) — **รวม 6 ใบ ~$10-11**

**พรุ่งนี้ (1 ก.ย.) ต้องทำ:** ลองกด GO.bat ข้อ 1 ใหม่ทั้งรอบ — ตอนนี้บั๊ก pgrep แก้แล้ว ควรเปิดได้จบในครั้งเดียว แล้วกดปุ่มถอดแบบในเว็บทดสอบ UI ใหม่จริง (ETA/แจ้งเตือนข้ามแท็บ/แถบ 100%)
