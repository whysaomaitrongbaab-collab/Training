# 2026-08-30

## Claude (มะขามเคาะ GPU strategy + เขียน converter/config จริง สำหรับ t04 InternVL3-78B)

- **แก้ความเข้าใจผิดของตัวเอง:** `Qwen3.5-122B-A10B` มี mmproj จริง เป็น multimodal (ไม่ใช่ text-only
  ตามที่บอกผิดไปก่อนหน้า) แต่เช็คแล้ว **แพงกว่า InternVL3-78B มาก** — bf16 LoRA ต้องการ 256GB VRAM
  ตามเอกสาร Unsloth เอง และ QLoRA 4-bit ใช้ไม่ได้ (MoE + bitsandbytes limitation เดียวกับ Qwen3.6)
  → ไม่ใช่ทางแก้ปัญหา GPU แพง ตกรอบ
- **มะขามเคาะ GPU strategy: QLoRA 4-bit การ์ดใบเดียว 96GB, คง InternVL3-78B (ไม่ลดเป็น 38B)**
- **เขียน+รันจริง `build_dataset_llamafactory.py`** — แปลง train.jsonl/val.jsonl (format Unsloth
  message-parts) → `train_lf.json`/`val_lf.json` (format sharegpt ของ LLaMA-Factory ยืนยันจาก
  data/mllm_demo.json+dataset_info.json จริงในรีโป ไม่ได้เดา) รัน**จริง 1020 train / 229 val
  แปลงผ่านหมด**, assert ยืนยันลำดับ image-ก่อน-text ทุกแถว, sanity-check token count = images
  count ทุกแถว, 0 ไฟล์รูปหาย — ไม่แตะ GT/รูปต้นฉบับเลย (แปลงแค่โครง)
- **เขียน `train_t04_internvl3_qlora.yaml`** (LLaMA-Factory config) ตาม official example จริง
  (`train_qlora/qwen3_lora_sft_otfq.yaml` + `train_lora/qwen3vl_lora_sft.yaml`) —
  `model_name_or_path: OpenGVLab/InternVL3-78B-hf` (ต้องเป็นตัว -hf เท่านั้น, ยืนยันจาก
  constants.py), `template: intern_vl`, `quantization_bit: 4`/`quantization_method: bnb`
  yaml parse ผ่าน · **⚠️ cutoff_len/image_max_pixels/lora_rank ยังเป็นค่าเริ่มต้นไม่ verify**
  ยกมาจาก Qwen ไม่ได้ (คนละ tokenizer/คนละกลไก resize) ต้อง dry-run วัดจริงก่อนเทรนเต็ม
- เขียน `onstart_llamafactory.sh` (คู่กับของเดิมที่ติด header ⛔ ไว้แล้วเป็น reference ยุค Qwen)
  `bash -n` ผ่าน — ยังไม่เคยรันจริงบนเครื่องเช่า
- อัปเดต `t04_workflow.md` Phase 0 ครบทุกข้อที่ทำวันนี้

## Claude (มะขามไปอาบน้ำ — code att1235 เตรียมของต่อ, ไม่แตะ Vast.ai credential ตามที่บอกไว้ก่อนหน้า)

- **เขียน `infer_house_t04.py`** — port `score_ids()`/`element_ids()`/`strip_fence()`/
  `apply_arm()`/`hide_grid_lines()` จาก `infer_house_t03.py` ตรงๆ (logic ล้วน ไม่ผูก Unsloth)
  เขียนใหม่ส่วนโหลดโมเดล/generate ด้วย transformers+bitsandbytes(4-bit)+peft (ตาม pattern
  มาตรฐานของ AutoModelForImageTextToText+AutoProcessor ที่ HF เอกสารไว้สำหรับ InternVL3-78B-hf)
- **`--selftest` ใหม่** เช็ค 5 ฟังก์ชัน pure logic ด้วยข้อมูลจำลอง (ไม่ต้องมี GPU) — รันจริง
  **ผ่านทุกข้อ** (เจอ assertion ที่ผมเขียนเองผิด 1 จุดระหว่างทดสอบ — ไม่ใช่บั๊กใน strip_fence,
  แก้ assertion แล้ว)
- **ยังไม่แตะ:** Vast.ai API key/credential (มะขามบอกไว้ก่อนหน้าจะหาให้เอง — เรื่องนี้อยู่นอก
  scope ของ att1235 ในไฟล์ rule_of_tune ซึ่งคุมแค่ raw JSON/tuning data ไม่ใช่ credential access)
  → ยังไม่เช่า GPU จริง ยังไม่ dry-run บนเครื่องเช่า
- **สถานะ Phase 0 ตอนนี้:** โมเดล/framework/GPU strategy/converter/สคริปต์เทรน/สคริปต์ infer
  พร้อมหมดในระดับ "เขียนแล้ว ผ่าน self-check บนเครื่องนี้" — เหลือ 3 อย่างที่ทำได้แค่บนเครื่องเช่าจริง
  เท่านั้น: (1) เช็คเครดิต/ราคา Vast.ai (2) dry-run บนการ์ดถูกยืนยัน cutoff_len/image_max_pixels/
  lora_rank/chat-template/tokenizer-for-xgrammar (3 เรื่องนี้เดาไม่ได้จากเครื่อง dev)

## Claude (มะขามสั่งเพิ่ม: GGUF checklist + ทดสอบบ้านใหม่หลังเทรนจบ + ตั้งชื่อ HF repo ตาม "Purson")

- **พบความเสี่ยงจริงเรื่อง GGUF ก่อนมะขามถาม:** `convert_hf_to_gguf.py` ของ llama.cpp รองรับ
  InternVL3 เฉพาะ checkpoint "non-hf" — แต่ pipeline ทั้งรอบ (LLaMA-Factory) บังคับเทรนบน `-hf`
  เท่านั้น → merge แล้วแปลง GGUF ได้จริงไหมยังไม่มีใครยืนยัน (คลาสเดียวกับ Day of Shame ของ t01)
  เพิ่มเป็น Phase 0 ข้อ 14 ใน `t04_workflow.md` — บังคับทดสอบด้วย InternVL3 ตัวเล็ก (2B/8B) ตอน
  dry-run ก่อน ห้ามรอเทรน 78B เต็มแล้วค่อยพิสูจน์
- **เพิ่มขั้นทดสอบท้ายรอบ (ข้อ 10 ของลำดับงานเช่าจริง):** หลังเทรน+วัดผล val แล้ว ต้องหาบ้าน 2 ชั้น
  **ที่ไม่เคยอยู่ในคลัง 40 หลังเลย** (ไม่ใช่แค่นอก train/val split) มาเดินสายพานเต็ม 7 ด่านจริง
  พิสูจน์ใช้งานได้ทั้งบ้าน ไม่ใช่แค่ตัวเลข recall บนตัวอย่างที่คัดมาแล้ว
- **ตั้งชื่อ HF repo ตาม "Purson" (ชื่อจริงของ t04 — มะขามย้ำ):** `Sicilian44/Purson-weights`
  (adapter, อัปเสมอ) + `Sicilian44/Purson-gguf` (เฉพาะถ้า verify ผ่านข้อ 14 — ห้ามอัป repo เปล่า
  ไว้ล่วงหน้า) — แก้ `onstart_llamafactory.sh` ให้ตรงแล้ว, `t04_workflow.md` ปรับลำดับขั้นตอน
  ท้ายรอบใหม่ทั้งหมด (14 ขั้นตอน ไม่ใช่ 11 เหมือนเดิม) `bash -n` ผ่าน

## Claude (มะขามสั่งเพิ่ม: คืนการ์ดจอครบทุกใบ)

- **ขยายข้อ 13 (คืนการ์ดจอ) ใน `t04_workflow.md`** ให้ชัดกว่าเดิม: รอบนี้เช่า 2 ใบต่างกัน
  (การ์ดถูกสำหรับ dry-run + การ์ดจริงสำหรับเทรนเต็ม) — ต้อง destroy ให้ครบทุกใบ ไม่ใช่แค่ใบสุดท้าย
  ย้ำ destroy ≠ stop (บทเรียนเดิม 2026-08-24) และเพิ่มขั้นยืนยัน `vastai show instances` = 0
  หลัง destroy จริง ไม่ใช่แค่เชื่อว่าคำสั่งรันผ่านเฉยๆ

## Claude (เช่าการ์ดจริง + เปลี่ยน epochs)

- **เช่า instance Purson (id 49160184)** RTX PRO 6000 Max-Q 97.9GB, $1.1048/hr (disk 250GB),
  image `pytorch/pytorch:2.8.0-cuda12.8-cudnn9-devel` (เลือกเพราะ PyTorch >=2.7.0 เท่านั้นที่รู้จัก
  sm_120/Blackwell — เช็คจริงก่อนเช่า ไม่ได้เดา) — เติมเครดิตเป็น $52.46 ก่อนเช่า
- **ยืนยัน env บนเครื่องจริงผ่าน SSH:** transformers 4.57.6 (xgrammar บังคับ `<5`, ลง 5.16.1 ไปก่อน
  แล้ว pip downgrade ให้เองตอนลง xgrammar — พฤติกรรมถูกต้อง ไม่ใช่บั๊ก), torch 2.8.0+cu128,
  bitsandbytes 0.50.2, llamafactory-cli 0.9.6.dev0 รันได้ปกติ — onstart script ผ่านครบไม่มี error
- **มะขามสั่งเปลี่ยน num_train_epochs: 3.0 → 5.0** ใน `train_t04_internvl3_qlora.yaml` (เหตุผล:
  ให้ตรงกับแนวคิด k-fold=5 จาก 40 หลัง÷8 หลัง/fold ที่คุยกันไว้ตอนต้น — ไม่ใช่ค่าที่วัด converge จริง
  หมายเหตุไว้ในคอมเมนต์ yaml: epochs กับ k-fold เป็นคนละแกน ไม่ได้ผูกกันทางเทคนิคจริง แต่เป็นการ
  ตัดสินใจของมะขามเอง ผลกระทบ: เวลา+ค่าใช้จ่ายเทรนเต็มเพิ่ม ~1.67x จากที่เคยประมาณไว้ (8-26ชม. →
  ~13-43ชม.) ต้องเฝ้า loss curve ตอน dry-run/เทรนจริงว่า overfit หลัง epoch 3-4 ไหม
