## [ช่วงเช้ามืด ~00:00–02:40] มะขาม (ทำงานร่วมกับ Claude — session นี้)

**งานที่ทำ:**
- อ่าน `rule_of_tune.md` และ `Makham's patter of rawjson20260705.md` (repo Training) ก่อนเริ่มงานตามกฎ
- มะขามสั่งให้ทำ fresh extraction (อ่านภาพ PNG ใหม่ทั้งหมด ไม่ใช้ข้อมูล Gen 1 เดิม) ของ `บ้าน_เล็ก_1ชั้น_01` หน้า 1-60 โดยใช้ schema Makham's Pattern (Gen 3) แล้วเก็บผลลัพธ์ใน `mk_test/`
- ก่อนเริ่ม ถามมะขามเพื่อตัดสินใจ 4 เรื่องที่ schema เดิมยังไม่ฟันธง (ตอบแล้วทั้งหมด):
  1. หน้าที่มีหลาย pattern ปนกัน (เช่น notes+index, section+schedule) → **แยกเป็นไฟล์ต่างหากต่อ pattern/view ในหน้าเดียวกัน** (ไม่ใช้ `views[]` แบบ Gen 2)
  2. footing หลายตำแหน่ง (เช่น 11 จุด) → **entry เดียว, `grid_ref` เป็น string คั่น comma** (ไม่แยก entry ทีละจุด)
  3. pattern ใหม่ 4 ชนิด (index/material_list/site_plan/site_profile) → **ทำทุก pattern ที่เจอจริง** (ขยาย scope จาก "structural only" เดิม)
  4. field เดิม (confidence_score, confidence_flags, doc_page, discipline) จาก Gen 1/2 → **เก็บไว้ทั้งหมด** เพิ่มจาก 4 field หลักของ Gen 3
  5. (คำถามรอบ 2 ระหว่างทำ) หน้าที่ไม่เข้า pattern ไหนใน 9 ชนิดเลย (หน้าปก, legend/สัญลักษณ์, ข้อกฎหมายล้วนๆ) → **สร้าง pattern ใหม่ชื่อ `unknown`** เก็บ raw_text + description แทน
- ทำ pilot อ่านภาพจริงหลายหน้าก่อน (หน้า 1,2,3,19,21,58) เพื่อยืนยัน pattern ที่เจอจริงตรงกับที่ออกแบบไว้ไหม — พบว่าหน้า 19 และ 21 มี 2 view ปนกันจริง (ยืนยันปัญหาที่เอกสาร Makham's Pattern เคยเตือนไว้)
- อ่านและเขียนไฟล์เองด้วยมือ (ไม่ได้ใช้ agent) สำหรับ**หน้า 1, 2, 3, 18, 19, 20, 21, 22, 23, 24, 25, 58, 59, 60** (ครอบคลุมส่วน structural ทั้งหมด S-01 ถึง S-08 ซึ่งเป็นข้อมูลที่มีค่าที่สุดสำหรับ fine-tuning) — รวมเป็นไฟล์ json ทั้งหมด ~24 ไฟล์ใน `mk_test/`
- Dispatch background agent (general-purpose) จำนวน 5 ตัวให้ทำหน้าที่เหลือแบบขนาน:
  - Agent 1: หน้า 4-10 (สถาปัตยกรรม A-02 ถึง A-08) → **เสร็จ 12 ไฟล์**
  - Agent 2: หน้า 11-17 (สถาปัตยกรรม A-09 ถึง A-15) → **เสร็จ 10 ไฟล์**
  - Agent 3: หน้า 26-37 (สุขาภิบาล SN-01..07 + ไฟฟ้า EE-01..04 + เครื่องกล M-01) → **เสร็จ 19 ไฟล์**
  - Agent 4: หน้า 38-47 (BOQ ฐานรากแผ่) → **ทำได้แค่หน้า 38, 39, 40 (หยุดเองกลางทาง 2 รอบ ต้อง resume ผ่าน SendMessage 2 ครั้ง) แล้วมะขามสั่งหยุดทั้งหมด (kill) ระหว่างทำหน้า 41 — หน้า 41-47 ยังไม่ได้ทำเลย**
  - Agent 5: หน้า 48-57 (BOQ ฐานรากเสาเข็ม) → **เสร็จ 10 ไฟล์**

**ไฟล์ที่เปลี่ยน:**
- `C:\00mk\steel project\training\Training\training-data\mk_test\*.json` — สร้างไฟล์ extraction ใหม่ทั้งหมด (schema Gen 3 / Makham's Pattern) สำหรับหน้า 1-40 และ 48-60 ของ `บ้าน_เล็ก_1ชั้น_01` (หน้า 41-47 ยังไม่มี)
- `C:\00mk\steel project\training\Training\training-data\mk_test\_PROGRESS_NOTES_หน้า38-47.md` — **ไฟล์ scratch ของ agent 4 ที่ทำ BOQ 38-47 ยังค้างอยู่ ยังไม่ได้ลบ** (ตั้งใจจะให้ agent ลบเองตอนเสร็จ แต่ถูก kill ก่อน) ต้องลบทิ้งทีหลังถ้าไม่ resume งานต่อ เพราะไม่ใช่ ground truth
- `C:\00mk\steel project\training\Training\training-data\Makham's patter of rawjson20260705.md` → **ย้ายออกจาก repo Training** ไปที่ `C:\00mk\steel project\งานสมบูรณ์\Constistant\mk's stuff\Makham's patter of rawjson20260705.md` (repo Constistant) ตามคำสั่งมะขาม — ไฟล์นี้**ไม่มีอยู่ที่ path เดิมใน repo Training แล้ว**
- `C:\00mk\steel project\งานสมบูรณ์\Constistant\workmen's_diary\README.md` — สร้างใหม่ อธิบายกติกาการเขียนไดอารี่นี้
- `C:\00mk\steel project\งานสมบูรณ์\Constistant\workmen's_diary\2026-07-05.md` — ไฟล์นี้เอง (entry แรกของไดอารี่)

**ผลกระทบ/ข้อควรระวัง:**
- **⚠️ พบข้อมูลขัดแย้งสำคัญ**: หน้า 18 (S-01 notes) อ่านสดแล้วได้ `concrete_strength_ksc = 210` (Fc'=210 กก./ตร.ซม. ระบุชัดในภาพ) แต่ข้อมูล Gen 1 เดิมใน `qwen-output/บ้าน_เล็ก_1ชั้น_01_หน้า18.json` บันทึกไว้ 240 — **ยังไม่ได้ตรวจสอบว่าอันไหนถูก** ต้องเช็คก่อนใช้เป็น ground truth จริง
- หน้าที่มีความเชื่อมั่นต่ำ (ต้องการคนซูมภาพตรวจซ้ำก่อนใช้จริง): หน้า 19 view2 (แปลนคาน, สัญลักษณ์ SI/SO/ST ไม่ทราบความหมาย), หน้า 21 (B5/B5X การแบ่งเหล็กบน-ล่าง), หน้า 11 (fixture mounting height), หน้า 14 (ระยะแนวตั้งกันสาด), หน้า 05 (site plan setback breakdown), หน้า 06 (ประตู-หน้าต่างนับจาก schedule ไม่ได้นับจากผังจริง), หน้า 09 (หลังคา 2 ระดับ +5.05/+4.05 อาจเป็น clerestory), หน้า 35 (EE-03 lighting layout label a-k), หน้า 36 (EE-04 อาจมีขนาดสายไฟไม่ตรงกันระหว่าง BTU 12,000 กับ 18,000), หน้า 51/53 (BOQ ตารางพื้น/สุขาภิบาล ที่ row-pairing ไม่ชัวร์)
- **เพิ่ม element_type ใหม่ที่ไม่มีใน spec เดิม (footing/beam/slab/column)**: `purlin`, `rafter` (หน้า 20 โครงหลังคาเหล็ก), `slab_edge`, `parapet` (หน้า 23), `precast_plank_detail` (หน้า 24-25), `room`/`door`/`window`/`wall` (หน้า 4-17 สถาปัตยกรรม) — ยังไม่ได้รับการยืนยันจากมะขามอย่างเป็นทางการว่าจะรับ vocabulary ใหม่นี้เข้า spec ถาวรไหม (คำถามเปิดข้อ 8 ของ Makham's Pattern doc เดิม)
- ไฟล์ `Makham's patter of rawjson20260705.md` ถูกย้ายข้าม repo (Training → Constistant) — ถ้ามีเอกสารอื่นใน repo Training อ้างอิง path เดิม (เช่น session handoff doc, หรือ note อื่นๆ) จะลิงก์เสีย ยังไม่ได้ไล่เช็ค

**ค้างไว้ / ทำต่อ:**
1. **หน้า 41-47 (BOQ ฐานรากแผ่) ยังไม่ได้ extract เลย** — ต้อง resume งานนี้ทีหลัง (ดูวิธี crop/pattern เลขแผ่นได้จาก `_PROGRESS_NOTES_หน้า38-47.md` ก่อนลบ ถ้ายังไม่ได้ทำต่อ)
2. ตรวจสอบ concrete grade 210 vs 240 ksc ให้แน่ชัด (เช็คจากภาพหน้า 18 อีกครั้ง หรือถามคนที่มีแบบต้นฉบับ)
3. ทบทวนหน้าที่ confidence ต่ำทั้งหมดที่ระบุไว้ข้างบน (ควรซูมภาพจริงหรือให้คนตรวจ)
4. ตัดสินใจอย่างเป็นทางการว่าจะรับ element_type ใหม่ๆ ที่เพิ่มเข้ามาไหม (purlin/rafter/slab_edge/parapet/precast_plank_detail/room/door/window/wall)
5. ยังไม่ได้ทำ final review/summary เปรียบเทียบความสอดคล้องของไฟล์ทั้งหมดใน `mk_test/` ข้ามกันเอง (เช่น เทียบ gridline ระหว่างหน้าสถาปัตยกรรมกับหน้าโครงสร้างว่าตรงกันไหม — มีจุดหนึ่งที่ agent เจอเองว่าตรงกันแล้วที่หน้า 6 กับหน้า 19)
6. ลบไฟล์ `_PROGRESS_NOTES_หน้า38-47.md` เมื่อไม่ต้องการใช้อ้างอิงแล้ว (ยังไม่ลบ ณ ตอนบันทึกนี้)

---

## Entry — Data Schema review + Phase A reorganization (Claude Code + taddy)

**ทำอะไร:**
- รีวิว Data Schema / Data Flow ทั้งแพลตฟอร์ม → เขียนผล + แผน 4 phase ไว้ที่ `docs/DATA_SCHEMA_REVIEW.md`
  (สรุป: นิยาม schema กระจาย 5 ที่, v2 factories ใน schema.js เป็น dead code, site/foundation/BBS-manual
  ประกาศ shape เองนอก schema.js, enum ราคา 3 ชุดไม่ตรงกัน + รายการ schema ที่ควรเพิ่มในอนาคต)
- ทำ Phase A เสร็จทั้งหมด:
  - แก้บั๊ก: `deleteProject()` ไม่ลบ key ของ Site/Foundation/manpower → ข้อมูล orphan (project-store.js)
  - สร้าง `js/shared/storage-keys.js` — single registry ของ localStorage key ทุกตัว; `PROJECT_SCOPED_KEYS`
    derive อัตโนมัติ; pipeline.js re-export `STORAGE_KEYS` เพื่อ backward compat; แก้ 14 feature module
    ให้เลิกประกาศ key string เอง
  - ย้าย SQL comment ใน schema.js → `database/resource_hub_tables.sql`
  - เก็บกวาด: ลบ `schema/` (โฟลเดอร์ว่าง 9 ใบ), `hetad.md` (ว่าง), `tmp-bbs-*.js`, `price/test.csv*`;
    ย้าย `hook-both-ends-test.json` → `tests/fixtures/`; รวม `test/` → `tests/`
  - npm test รันทั้ง `tests/` แล้ว (เดิมรันไฟล์เดียว) — แก้ stub DOM + float assertion ในเทสต์
    bbs-shape-allowance ที่ไม่เคยถูกรัน → 48/48 ผ่าน

**ค้างไว้ / ต่อไป:** Phase B–D ตาม `docs/DATA_SCHEMA_REVIEW.md` §5 — ตัวแรกที่ควรทำคือ
factory `createSiteInvestigation()` / `createFoundationDesign()` ใน schema.js แล้วนิยาม contract
Site → Foundation → Planner แทนการอ่าน localStorage ข้าม module ตรง ๆ
