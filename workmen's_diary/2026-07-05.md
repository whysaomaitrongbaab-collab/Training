## [Early morning ~00:00–02:40] Makham (working together with Claude — this session)

**Work done:**
- Read `rule_of_tune.md` and `Makham's patter of rawjson20260705.md` (Training repo) before starting work, per the rules.
- Makham ordered a fresh extraction (re-reading all PNG images from scratch, not reusing the old Gen 1 data) of `House_Small_1Story_01` (บ้าน_เล็ก_1ชั้น_01) pages 1-60, using the Makham's Pattern (Gen 3) schema, storing results in `mk_test/`.
- Before starting, asked Makham to decide on 4 points the existing schema hadn't nailed down yet (all answered):
  1. Pages that mix multiple patterns (e.g. notes+index, section+schedule) → **split into separate files per pattern/view within the same page** (not the Gen 2-style `views[]` array).
  2. Footings with multiple locations (e.g. 11 points) → **a single entry, `grid_ref` as a comma-separated string** (not a separate entry per point).
  3. 4 new pattern types (index/material_list/site_plan/site_profile) → **do every pattern actually encountered** (expand scope beyond the original "structural only").
  4. Old fields (confidence_score, confidence_flags, doc_page, discipline) from Gen 1/2 → **keep them all**, in addition to Gen 3's 4 core fields.
  5. (Round-2 question, asked mid-work) Pages that don't fit any of the 9 pattern types (cover page, legend/symbols, pure legal text) → **create a new pattern called `unknown`**, storing raw_text + description instead.
- Did a pilot read of several actual page images first (pages 1, 2, 3, 19, 21, 58) to confirm the patterns actually encountered matched what had been designed — found that pages 19 and 21 really do mix 2 views together (confirming an issue the Makham's Pattern document had already warned about).
- Read and wrote files by hand (no agent used) for **pages 1, 2, 3, 18, 19, 20, 21, 22, 23, 24, 25, 58, 59, 60** (covering the entire structural section S-01 through S-08, the most valuable data for fine-tuning) — totaling ~24 JSON files in `mk_test/`.
- Dispatched 5 background agents (general-purpose) to handle the rest in parallel:
  - Agent 1: pages 4-10 (architectural A-02 through A-08) → **completed 12 files**
  - Agent 2: pages 11-17 (architectural A-09 through A-15) → **completed 10 files**
  - Agent 3: pages 26-37 (sanitary SN-01..07 + electrical EE-01..04 + mechanical M-01) → **completed 19 files**
  - Agent 4: pages 38-47 (spread-footing BOQ) → **only managed pages 38, 39, 40 (stopped itself twice mid-task, had to be resumed via SendMessage twice), then Makham killed the whole thing while it was on page 41 — pages 41-47 were never done at all**
  - Agent 5: pages 48-57 (pile-foundation BOQ) → **completed 10 files**

**Files changed:**
- `C:\00mk\steel project\training\Training\No_touch_box\mk_test\*.json` — created all-new extraction files (Gen 3 / Makham's Pattern schema) for pages 1-40 and 48-60 of `House_Small_1Story_01` (บ้าน_เล็ก_1ชั้น_01) (pages 41-47 still missing).
- `C:\00mk\steel project\training\Training\No_touch_box\mk_test\_PROGRESS_NOTES_pages38-47.md` (_PROGRESS_NOTES_หน้า38-47.md) — **scratch file from agent 4's incomplete work on BOQ 38-47, not yet deleted** (intended for the agent to delete itself once done, but it was killed first) — needs deleting later if this work isn't resumed, since it isn't ground truth.
- `C:\00mk\steel project\training\Training\No_touch_box\Makham's patter of rawjson20260705.md` → **moved out of the Training repo** to `C:\00mk\steel project\งานสมบูรณ์\Constistant\mk's stuff\Makham's patter of rawjson20260705.md` (Constistant repo) per Makham's instruction — this file **no longer exists at its old path in the Training repo**.
- `C:\00mk\steel project\งานสมบูรณ์\Constistant\workmen's_diary\README.md` — newly created, explains this diary's writing rules.
- `C:\00mk\steel project\งานสมบูรณ์\Constistant\workmen's_diary\2026-07-05.md` — this file itself (the diary's first entry).

**Impact/precautions:**
- **⚠️ Found a significant data conflict**: page 18 (S-01 notes), read fresh, gives `concrete_strength_ksc = 210` (Fc'=210 kg/cm², clearly labeled in the image), but the old Gen 1 data in `qwen-output/บ้าน_เล็ก_1ชั้น_01_หน้า18.json` (House_Small_1Story_01_page18.json) recorded 240 — **not yet verified which one is correct**; must check before using either as real ground truth.
- Pages with low confidence (need someone to zoom into the image and re-verify before real use): page 19 view2 (beam plan, meaning of SI/SO/ST symbols unknown), page 21 (B5/B5X top/bottom rebar split), page 11 (fixture mounting height), page 14 (canopy vertical dimension), page 05 (site plan setback breakdown), page 06 (door/window counts taken from the schedule, not counted from the actual plan), page 09 (2-level roof +5.05/+4.05, possibly a clerestory), page 35 (EE-03 lighting layout labels a-k), page 36 (EE-04, possible wire-size mismatch between BTU 12,000 and 18,000), pages 51/53 (BOQ floor/sanitary tables where row-pairing isn't certain).
- **Added new element_types not in the original spec (footing/beam/slab/column)**: `purlin`, `rafter` (page 20, steel roof frame), `slab_edge`, `parapet` (page 23), `precast_plank_detail` (pages 24-25), `room`/`door`/`window`/`wall` (pages 4-17, architectural) — not yet officially confirmed by Makham whether this new vocabulary will be adopted into the permanent spec (open question #8 from the original Makham's Pattern doc).
- The file `Makham's patter of rawjson20260705.md` was moved across repos (Training → Constistant) — if any other document in the Training repo references the old path (e.g. a session handoff doc, or other notes), that link will break; not yet checked.

**Left pending / to continue:**
1. **Pages 41-47 (spread-footing BOQ) have not been extracted at all** — need to resume this work later (see the crop/page-number pattern method in `_PROGRESS_NOTES_pages38-47.md` before deleting it, if not yet resumed).
2. Verify concrete grade 210 vs 240 ksc for certain (recheck page 18's image, or ask someone with the original drawing).
3. Review all the low-confidence pages listed above (should zoom into the actual images or have someone check).
4. Officially decide whether to adopt the newly added element_types (purlin/rafter/slab_edge/parapet/precast_plank_detail/room/door/window/wall).
5. Have not yet done a final review/summary comparing consistency across all files in `mk_test/` against each other (e.g. comparing gridlines between architectural pages and structural pages to see if they match — the agent itself found one point where they do match, at page 6 vs. page 19).
6. Delete the file `_PROGRESS_NOTES_pages38-47.md` once it's no longer needed for reference (not yet deleted as of this entry).

---

## Entry — Data Schema review + Phase A reorganization (Claude Code + taddy)

**Work done:**
- Reviewed Data Schema / Data Flow across the whole platform → wrote up findings + a 4-phase plan in `docs/DATA_SCHEMA_REVIEW.md`
  (Summary: schema definitions scattered across 5 places, v2 factories in schema.js are dead code, site/foundation/BBS-manual
  declare their own shapes outside schema.js, 3 mismatched pricing enum sets + a list of schemas that should be added in future)
- Completed all of Phase A:
  - Fixed a bug: `deleteProject()` wasn't deleting Site/Foundation/manpower keys → orphaned data (project-store.js)
  - Created `js/shared/storage-keys.js` — single registry of every localStorage key; `PROJECT_SCOPED_KEYS`
    derived automatically; pipeline.js re-exports `STORAGE_KEYS` for backward compat; fixed 14 feature modules
    to stop declaring key strings themselves
  - Moved an SQL comment block in schema.js → `database/resource_hub_tables.sql`
  - Cleanup: deleted `schema/` (9 empty folders), `hetad.md` (empty), `tmp-bbs-*.js`, `price/test.csv*`;
    moved `hook-both-ends-test.json` → `tests/fixtures/`; merged `test/` → `tests/`
  - `npm test` now runs the whole `tests/` folder (previously ran a single file) — fixed a DOM stub + float assertion in the
    bbs-shape-allowance test that had never actually been run → 48/48 passing

**Left pending / next:** Phase B–D per `docs/DATA_SCHEMA_REVIEW.md` §5 — the first thing to do is the
`createSiteInvestigation()` / `createFoundationDesign()` factories in schema.js, then defining an explicit
Site → Foundation → Planner contract in place of reading localStorage directly across modules.

---

## [retroactive note, added 2026-08-04] Claude — archival record for a file dated this day

Not a real-time entry — added a month later while cleaning up `No_touch_box/` docs (see
`2026-08-04.md` for that real session). Filed here because the doc itself is dated 2026-07-05,
per Makham's instruction to log archival notes against the date written in the file, not the date
of the archiving action.

**File archived:** `No_touch_box/docs/Makham's patter of rawjson20260705.md` → moved to
`wait_for_ทิ้ง/No_touch_box/docs/` on 2026-08-04, not deleted.

**What it was:** the origin document for Generation 3 of the raw-JSON schema — first design of
`grid_ref` rules keyed by `element_type`, the dummy-grid + prime-notation convention (`1'`, `A''`)
and the per-house `หน้า00_gridline.json` master file, and spec-join (`plan` position + `section`/
`schedule` spec joined by shared `element_id`). Both dummy-grid-prime and spec-join are still
load-bearing rules in the live spec today (`primary_rawjson_schema.md`, compiled 2026-07-10) —
this doc is where they were designed first, before `merged_pattern_gen4_20260706.md` folded them
in the next day.

**Why archived, not deleted:** superseded as a working spec long ago, but it's the only place the
original reasoning for those two rules is written down.

---
