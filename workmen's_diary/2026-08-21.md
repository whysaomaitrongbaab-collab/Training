# 2026-08-21

## [afternoon] Claude — session with Makham

**Work done:**
- Reviewed 2026-08-20 diary with Makham; analyzed house 08's beam-plan page (หน้า20, S-03) which failed to parse (`ok:false`).
- Root cause found: `run_house_batch.py`/`infer_t02.py` used pure greedy decoding (`do_sample=False`) with no repetition guard — model looped repeating fake `B2` beam entries across nearly every grid cell until hitting `max_new_tokens`, never closing the JSON.
- Fixed both scripts: added `repetition_penalty=1.15, no_repeat_ngram_size=8` to the `generate()` call. **Not yet tested on GPU.**
- Makham announced a full rework: **t03** will replace t01/t02's single-shot extraction with a multi-pass pipeline (Pass 0 = classify page pattern, Pass 1 = orchestrate/queue non-AI, Pass 2 = one subtask per rawjson pattern). Built `tune_ai/t03/pass_design.csv` mapping this against the 16 patterns in `primary_rawjson_schema.md` §1.
- Traced Constistant's BOQ pipeline to confirm which rawjson patterns it actually consumes (`js/drawing/raw-extraction-adapter.js`): only `plan`/`section`/`schedule`/`notes`/`gridline`/`material_list` — informs which t03 subtasks matter most for BOQ.
- Two real corrections to the pass design, one from a mistake Claude made and caught by Makham:
  - Initially excluded `site_plan`/`side_profile` as grid-line sources for the "gridline" subtask, reasoning elevations have no top-down grid axis. **Wrong** — Makham showed a real elevation sheet (spec A-05, "รูปด้าน 1"/"รูปด้าน 2") printing grid markers `1/2/3` and `D/C/B/A` with full dimension chains, same as a plan sheet. Reverted.
  - `roof_plan` subtask was missing its own input row in Makham's draft table despite being a grid-master source — added `json ของ Gridmaster` back as its input for consistency with `plan`/`site_plan`/`side_profile`.
- Schema change requested by Makham ("record every dimension"): added a new **Dimension chain** section to `primary_rawjson_schema.md` §4 — optional `dimension_chains[]` array under the `gridline` pattern's `grid{}` container, capturing every printed dimension segment verbatim (including edge-to-first-grid offsets like `1.30` before grid `1`, which the existing beam-endpoint-only dummy-grid rule silently dropped since nothing sits on them).
- Discussed inference-speed options for t03 beyond the repetition-penalty fix: per-pattern pass split (already the t03 design), tuned `max_new_tokens` per pattern, vLLM/TensorRT-LLM serving, 4-bit quantization, reduced image resolution for simple patterns, grammar/JSON-constrained decoding (`outlines`/`xgrammar`) as the most robust but heaviest fix.

- Later in the session Makham split Pass 2 into two: **Pass 2 = the 7 patterns Constistant actually consumes** (`gridline`, `plan`, `section`, `schedule`, `notes`, `material_list`, `soil_boring_log`), **Pass 3 = the 8 patterns that still need extracting but nothing reads yet**. Also clarified Pass 1 is a real **physical grouping into one folder per subtask** (each folder holding the images plus whatever that subtask needs, e.g. the grid master), not just an ordering/queue — Claude had it wrong first time.
- Split the `plan` subtask by structural element on Makham's instruction: `plan_footing` / `plan_column` / `plan_beam` / `plan_slab` in Pass 2, and `plan_architectural` / `plan_electrical` / `plan_sanitary` in Pass 3. Floor level and building deliberately stay **fields** (`floor_level`, `building`), not subtasks, despite the filename drift (`beam_plan` / `beam_plan_floor1` / `beam_floor_plan` / `beam_plan_สุขา` are all one job).
- **Three real data problems found while doing that split** (counted across all 11 houses in `json_แก้ไขแล้ว/`), none caused by t03:
  1. **`roof_frame_plan` is split across two patterns and 8 of 11 houses' roof beams never reach the BOQ.** All 12 roof-framing files are `discipline: "structural"`, but 4 are `pattern: "plan"` and 8 are `pattern: "roof_plan"` — and Constistant's `buildElements()` only reads `pattern === 'plan'`. Live data-loss bug; needs fixing in the data before t03 output is compared against this ground truth.
  2. `discipline` has two spellings of one value — `"architectural"` (182 files) vs `"architecture"` (157). Nothing consumes `discipline` today so nothing is broken yet, but any future filter silently drops half the files.
  3. Precast plank layout sheets are `pattern: "section"` (18 files) despite `*_plank_layout*` filenames — probably correct, flagged so nobody "fixes" it blindly.

- **Second schema change, on Makham's instruction "make the grid master record every value it finds on every page":** three new arrays under `grid{}` in `primary_rawjson_schema.md` §4 — **`z_levels[]`** (the vertical axis, which had **no field anywhere in the schema** despite `+3.75 ระดับหลังคาน` / `+0.60 ระดับพื้นชั้น 1` / `±0.00 ระดับอ้างอิง` being printed on every elevation and section in the set; `x_lines`/`y_lines` are plan axes only), **`dimension_chains[]`** extended to the `z` axis, and **`unassigned_dimensions[]`** as the catch-all so any printed number that fits no category still gets recorded verbatim rather than dropped. Two checklist items added to §0 so the format checker enforces it. Notable: this is the **second** time assuming `side_profile`/elevations were low-value turned out wrong — they are the only place levels are printed at all.

- **Built Pass 1 (`tune_ai/t03/pass1_organize.py`) — written but deliberately NOT tested** (Makham: "ทำเลย แต่ไม่ต้องทดสอบนะ"). Reads pass 0's output, cuts multi-view pages into one image per view with OpenCV, scatters into `pass2/<subtask>/images/` and `pass3/<subtask>/images/`, writes a `manifest.json` per folder. **Nothing in it has touched a real image yet — run it on one house before trusting the cropping.**
- Measured the multi-view problem before building: **62 of ~1,100 pages (~6%) carry more than one view**, but they include the highest-value structural sheets (`footing_plan + beam_plan` on one page, `tie_beam_plan + roof_frame_plan` on another) and some carry three views, so it can't be skipped.
- Cropping is not a token saving, it's an accuracy lever: visual tokens are pinned at 5120/image to match training, so a half-page crop spends the same budget on half the area = double effective resolution on the thing being read.
- Four design calls recorded in the t03 README so they don't get re-litigated: grid master lives in `_shared/` and is referenced by each manifest's `needs` (it doesn't exist yet when Pass 1 runs, so copying it in is impossible); `sheet_code`/`sheet_name` travel as manifest text because the title block is stripped from every crop yet those are §2-required wrapper fields; a crop-count mismatch against Pass 0's view count sends the full page and flags it rather than guessing; crops are padded ~2% so the edge-sitting grid markers aren't clipped (losing them kills span calculation for the whole sheet).
- Wrote `tune_ai/t03/pass0_output_example.json` as the Pass 0 → Pass 1 contract. Notable consequence: **Pass 0 must classify straight to the fine-grained `subtask` label, not to one of the 16 `pattern` values** — otherwise Pass 1 cannot route a `plan` page to one of seven destination folders, and filename-based routing is ruled out by the naming drift already documented.

- **Read `rule_of_tune.md` on Makham's instruction and immediately found Claude had been violating Lesson #7 all day**: `primary_rawjson_schema.md` is exempt from Rule 1 (it's a `.md` doc, in the "Not protected" list) but Lesson #7 requires **stating the exemption out loud before editing** and **logging every edit in `primary_rawjson_schema_edit_log.md` immediately** — both of the day's earlier schema edits were made silently with no log entry. Logged both retroactively, marked as rule violations in the log itself, and gave the Rule 2 warning properly before the next edits.
- **Two more schema edits (this time warned first and logged immediately):** §1 pattern #12 `roof_plan` rewritten plus a new "⚠️ `roof_plan` vs `plan`" warning block under the §1 table pinning that structural roof framing is `plan`; §2 gained a closed `discipline` vocabulary of 11 values pinning `architectural` as canonical against `architecture`.
- **Wrote the t03 prompts** — ten files in `tune_ai/t03/prompts/`, all English, sourced from `primary_rawjson_schema.md` only. `pass2_plan.md` covers all four plan subtasks by substitution and `pass3.md` covers all eleven Pass 3 subtasks, deliberately rather than as fifteen near-identical files: copies drift, and drift between same-shaped files is the exact failure the spec's §0 exists to stop. **None has been run.**
- **Two more spec gaps found while writing the prompts, both recorded rather than invented around:**
  1. **The `notes` → Constistant path produces nothing at all.** `raw-extraction-adapter.js` reads `notes.fc_ksc` / `fy_main_ksc` / `fy_stirrup_ksc` / `cover_mm` to build `extractedNotes`, but `primary_rawjson_schema.md` **never defines that shape** — `fc_ksc` appears nowhere in the spec, and in exactly **1 of 55** real notes files. So the project-wide concrete/steel specification never reaches the pipeline for essentially any house. Fixing it is a spec decision (define the fields, or change the consumer), so `pass2_notes.md` writes the spec's `sections[]` transcript and explicitly forbids inventing value fields.
  2. **`notes` is the worst-drifted pattern in the whole data set** — 55 files carrying the same content under six container keys (`notes` 22, `notes_sections` 9, `sections` 8, `spec_notes` 3, `notes_text` 1, `raw_text` 1) plus one-offs. The spec names `sections[]`; 8 of 55 use it. Same class of problem as the `roof_frame_plan` bug, and larger.
- **Gap 1 closed the same session — Makham chose "define the fields in the spec" over changing the consumer.** New `primary_rawjson_schema.md` **§4a**, two layers following the precedent already set by `BeamLibraryEntry`'s flat `main_bar_*` aliases: a nested half recording what the drawing actually prints (`concrete{}`, `steel{round_bar{},deformed_bar{}}`, `cover{default_mm,by_condition[]}`) plus four flat one-way copies (`fc_ksc`/`fy_main_ksc`/`fy_stirrup_ksc`/`cover_mm`) so Constistant's adapter needs no change. **The design call worth remembering: `steel` splits by bar notation (RB/DB), not by role** — Thai notes sheets print "RB = SR-24 = 2400, DB = SD-40 = 4000" and never say "main bars are X, stirrups are Y", so the main/stirrup split the consumer wants is an interpretation and lives only in the derived layer, with the convention written down and explicitly overridable by what a sheet actually says. `cover` is pinned to **mm** per §0.5 (4 existing files carry `rebar_cover_m` in metres, which is a violation). A flat field whose source is absent is `null`, never a convention default. `pass2_notes.md` updated to match; §0.1's table and the §0 checklist updated too.

**Files changed:**
- `tune_ai/t02/data_before_tune/run_house_batch.py`, `infer_t02.py` (Constistant repo) — added `repetition_penalty=1.15, no_repeat_ngram_size=8` to `model.generate()`. (Later in the day someone also added grammar-constrained decoding — `--grammar-backend auto|xgr|lmfe|none` — to `run_house_batch.py`; not this session's work, noted so the file's history reads straight.)
- `rawjson_ยังไม่ได้แก้ไขโดนคน/primary_rawjson_schema_edit_log.md` — 4 entries added (2 retroactive for the morning's unlogged edits, 2 for the afternoon's)
- `tune_ai/t03/prompts/` (new, 10 files) — `_common.md`, `pass0.md`, `pass2_gridline.md`, `pass2_plan.md`, `pass2_section.md`, `pass2_schedule.md`, `pass2_notes.md`, `pass2_material_list.md`, `pass2_soil_boring_log.md`, `pass3.md`, plus a `README.md`
- `tune_ai/t03/pass1_organize.py` (new) — Pass 1, untested
- `tune_ai/t03/pass0_output_example.json` (new) — the Pass 0 → Pass 1 contract
- `rawjson_ยังไม่ได้แก้ไขโดนคน/00file_for_making_rawjson_from_claude/primary_rawjson_schema.md` — new "Dimension chain" subsection under §4 Grid
- `tune_ai/t03/pass_design.csv` (new) — pass/subtask table for t03's multi-pass design, referencing the 16-pattern taxonomy
- `tune_ai/t03/README.md` (new) — session notes/decisions for t03 (see file for full detail)

**Left pending / to continue:**
- Repetition-penalty fix not yet run on a real GPU — still need to re-run house 08's beam-plan page and confirm it actually closes valid JSON now.
- t03 training planned tonight 19:00; Makham hasn't yet described t03's full design beyond the pass table built this session (subtask-by-subtask model/prompt details still to come).
- Houses 09/10/11 (t02 generalization test) still not started at all; house 08 still short of its original 25-page cap.
- `site_plan.element_type` still has no canonical enum (10 distinct strings found across 5 houses) — needs a decision before any pattern-2 automation on that subtask.
- `material_list` → `boqSeed[]` still has no write path into Constistant's real BOQ storage (pre-existing gap, unrelated to t03, flagged again here so it isn't forgotten before t03 raises expectations).
- **Fix the `roof_frame_plan` pattern misclassification in the 8 affected houses** — until then those houses' roof beams are missing from any BOQ computed off this ground truth, and t03's accuracy against them would be measured against incomplete data.
- Decide one canonical spelling for `discipline` (`architectural` vs `architecture`) before anything starts filtering on it.
- **Run `pass1_organize.py` on one house.** It was written without a test pass on Makham's instruction — the cropping logic (title-block strip, divider detection, padding, the not-guessing fallback) has never seen a real drawing. The 62 multi-view pages are the test set and they're already on disk, so this costs nothing but time.
- ~~Write the Pass 0 prompt~~ — done, `prompts/pass0.md`. Still needs a hand-check of its output on a full house before Pass 1's folder tree can be trusted.
- **Migrate the 55 existing `notes` files** to the two names §4a now allows (`sections[]` + `notes{}`) — they currently use six different container keys, and none of them carries a `notes{}` object at all, so the newly-defined path is still empty in practice until they are rewritten. Also fix the 4 files carrying `rebar_cover_m` in metres (a §0.5 violation — cover is a member dimension and belongs in mm).
- Run one house end to end through the prompts before spending GPU time on eleven.

---

## [คืน → เช้า 2026-08-22] Claude — session with Makham

**Work done:**
- เช่า RTX PRO 6000 96GB รอบใหม่ ทดสอบ grammar-constrained decoding บน GPU จริง
- `infer_t02_xgrammar.py` + หน้า21 บ้าน08 (หน้าที่เคยพัง 2 รอบ) → valid JSON เต็มใบ ไม่ต้อง regex
- `run_house_batch.py --grammar-backend auto` ผ่านทั้งเส้น (--limit 2 ก่อนตามธรรมเนียม) → บ้าน08 ครบ 25 หน้า + บ้าน09 (บ้าน_เล็ก_1ชั้น_04) 6 หน้า plan โครงสร้าง
- **ผลรวม 30/31 valid (96.8%) vs 57.9% รอบไม่มี grammar** — ตัวเดียวที่พัง: บ้าน08 หน้า07 วนลูปใน string literal จนชน token cap (ช่องโหว่เดียวที่ grammar อุดไม่ได้)
- ดึงผลครบ 33/33 ไฟล์เทียบเครื่องเช่าก่อน destroy (Mark-of-Shame ผ่าน)
- ทำ overlay GT-vs-AI บ้าน09 หน้า24/25/26/27 (สคริปต์ `tune_ai/t02/overlay_gt_vs_ai_house09.py`, วิธีเดียวกับ overlay บ้าน01 20/8): GT วางได้ 45/48, 52/53 — ฝั่ง AI หน้า26 ยุบคานทั้งหน้าเหลือ 2 elements แบบ zone notation ไม่เกาะแนวคานจริง, หน้า27 วางไม่ได้เลย = หลักฐานตาเปล่าของ recall 11%
- อัปเดต SUMMARY.md + rule_of_tune ข้อ 13 (xgrammar = default แล้ว, lmfe = fallback)

**Files changed:**
- `tune_ai/t02/ผล/08บ้าน_เล็ก_1ชั้น_03/` (ครบ 25 หน้า), `ผล/09บ้าน_เล็ก_1ชั้น_04/` (ใหม่, 6 หน้า + overlay PNG 4 ไฟล์)
- `tune_ai/t02/overlay_gt_vs_ai_house09.py` (ใหม่), `tune_ai/t02/infer_results/SUMMARY.md`, `No_touch_box/docs/rule_of_tune.md` ข้อ 13

**Left pending / to continue:**
- หน้า07 บ้าน08 invalid ตัวเดียว — ถ้าอยากได้ครบ: re-generate หน้าเดียว
- หน้า28 บ้าน09 (roof frame) overlay ข้าม — วง grid บนแผ่นพิมพ์ไม่ครบ calibrate ไม่ได้
- คุณภาพเนื้อหาหน้า beam ยังเป็นปัญหา adapter → แก้ที่ t03 ตามแผนเดิม

---

## Late addition — t03 folder reorganized (after midnight, still this session)

Two folders one letter apart held two contradictory designs: `prompt/` (the 2-pass draft from
2026-08-04) and `prompts/` (today's 4-pass work). Nothing said which was live.

- One folder per pass now: `pass0_classify/` · `pass1_organize/` · `pass2_used/` · `pass3_unused/`
- `prompt/` → `_old_2026-08-04_two_pass/` with a ⛔ banner at the top of its README
- `_common.md` moved to t03 root (pass 2 and pass 3 both use it; its `Prepend` lines now say `../_common.md`)
- `pass2_*.md` prefixes dropped — folder already says the pass (`pass2_used/plan.md`)
- `pass0_output_example.json` → `pass0_classify/output_example.json`, `pass1_organize.py` → `pass1_organize/organize.py`
- `prompts/README.md` deleted — its three unique sections (why one prompt covers several subtasks, why the anti-loop wording exists, what to check before running) folded into `t03/README.md`; the spec-gap text was duplicated in both and would have drifted
- `t03/README.md` opens with a folder tree in Thai, one line per file
- All cross-refs updated; JSON re-parsed and `organize.py` re-compiled clean. **No prompt text changed** — moves and links only.

---

## Dataset sizing — ตอบคำถาม "ควรมีกี่หลัง" (ต่อจากด้านบน, ยังเซสชันเดียวกัน)

นับของจริงจาก `json_แก้ไขแล้ว/` 11 หลัง (1,180 ไฟล์ / 4,625 elements) แทนที่จะเดา — เขียนไว้ที่
`tune_ai/t03/dataset_sizing.md` + ลิงก์จาก t03 README

- **สรุป: เป้า 25-30 หลัง** (ตอนนี้ annotate 11, t02 เทรนจริงแค่ 4; ภาพดิบมี 50 หลัง)
- ตัวเลขที่อธิบายว่าทำไมหน้า beam พัง: **t02 เห็นแปลนคานแค่ ~13 ใบ** แต่เห็น material_list ~158 ใบ
  — หน้าที่มันทำได้ดี (door/window schedule 86%) คือหน้าที่เห็นเยอะ ความสัมพันธ์ตรงๆ ไม่ใช่บังเอิญ
- **material_list กิน 435 ไฟล์ = 37% ของงาน annotate ทั้งหมด แต่ให้ elements = 0** — หลังใหม่หยุดได้เลย
- ถ้า annotate เฉพาะ pattern ที่ Constistant ใช้จริง (ตัด material_list) = ~38 หน้า/หลัง แทน ~107
  → **+20 หลัง = annotate ~750 หน้า (ไม่ใช่ 2,140), plan_beam 35 → ~99** ประหยัดแรงคน ~65%
- **⚠️ เจอปัญหาที่ต้องตัดสินใจก่อนวาง t03: `plan_column` มีแค่ 2 ไฟล์ใน 11 หลัง** — เสาแทบไม่มีหน้า
  แปลนของตัวเอง (โผล่เป็น marker ในแปลนคาน/ฐานราก + ตารางเสาแทน) ถ้าตั้งเป็น subtask แยกตาม
  `pass_design.csv` จะไม่มีข้อมูลเทรนเลย ต้องเลือกรวมกับ plan_beam หรือย้ายไป schedule

**Files changed:** `tune_ai/t03/dataset_sizing.md` (ใหม่), `tune_ai/t03/README.md` (เพิ่มลิงก์ในผังโฟลเดอร์)

**Left pending:** ตัดสินใจเรื่อง `plan_column` subtask; เลือกว่าจะ annotate หลังไหนต่อจาก 39 หลังที่เหลือ
