## Makham + Claude Code — house 3 dummy grids continued + footing dedup + answering fine-tune questions (continuing from 2026-07-19.md)

1. **House3 หน้า00_gridline:** added dummy `2'`=4.6, `B'`=10.2 (3'=7.0 already existed) per Makham's values, confidence 1.
2. **House3 หน้า29 (S-02 spread footing):** removed a duplicate F0.90x0.90 at B3 (double-counted, the main entry already has B3); set confidence_score=1 for the whole page.
3. **House3 หน้า30 (S-03 pile footing):** removed a duplicate F5 at B3 (the same bug as page29).
4. **House2 หน้า25/26:** checked span_length_m for every beam against page00 (grid_ref_start→end) — all matched; fixed page25's specs to match page28.
5. **House3 หน้า31 (S-04) — Makham ordered "go check the original image and fix it":** zoomed into page31.png, found 2 missing vertical beams in the D-C strip → added `B2` (the staircase wall on the E side) at `1''`(2.35) span2.2, `B1` (the room-dividing member for S2) at `2'`(4.6) span2.2 (measured ~4.9-5.0 pixels vs. the master's 2'=4.6); confirmed E'(-1.5) is a slab edge with no beam, B'(10.2) doesn't appear on this sheet (S1 covers the full B-A span).
6. **House3 หน้า32 (S-05):** updated the note for the staircase bay 1''(2.25)→(2.35).
7. Explained the git push 403 (chaichanaju-dot's permissions don't have push access on the org repo — add as a collaborator via the GitHub website).
8. **Answered questions about fine-tuning Qwen/Unsloth:** the dataset unit is a pair (page image→JSON), not a house (~150 valid pairs, small); ⚠️ split val **by house**, not randomly by page (leakage risk); for now, train4/val1 or 5-fold; if labeling grows to 8-10 houses, go 80/10/10; measure field-level exact-match, not just eval loss; `finetune_vision_layers=True` + a high max_pixels/train on crops (line-thin distribution, and small labels vanish if downsized to 1000px).

**Files:** house3 pages 00/29/30/31/32, house2 pages 25/26.

---

## Afternoon round — cascading B'/E' turned into a full-sheet image check for pages 31+32

9. Set a **calibrated scale** before measuring (column-1 grid centerline x=1434.3px, grid2 x=1707.7px → 78.1 px/m, E y=756), then used the same scale across both sheets.
10. **Page32 (S-05) fully re-extracted, 20→32 elements:** missing the entire horizontal beam **along row D** (B3X 1'-1, B3 1-2, B2 2-3), missing the B3X section on row C from 1'-1, missing a vertical beam at grid2 in the D-C strip + at `2'` + grid2 in the B-A strip, missing **beam B1 at line `B'`** (the very piece the B' cascade needed), missing a BX + the S2 marker; fixed mislabeled ones: C1-C2 B3X→B3, row A span1-2 B2→B1, **B4X B3-A3(1.5)→B3-B'3(1.0)** (the beam stops at B' → resolving the old question "does the 2nd floor extend past A3?" = it doesn't).
11. **Page31 (S-04) fixed 7 spots:** D1-C1→**D1'-C1'** (no vertical beam at grid1 in the D-C strip on either floor), D1-D2 B3X→**B3** + added B3X D1'-D1, C1-C2 B3X→**B3** + added B3X C1'-C1, C2-C3 B3→**B2**, added vertical beam B1 B2-A2, released 2 "possibly void" flags.
12. **⚠️ Found a genuine difference between floors:** the beam east of the staircase bay, floor1 centerline **2.25**/face 2.35 (matching `1''` exactly), but floor2 is at **1.96** — a 0.29m (~23px) difference, beyond measurement tolerance — recorded provisionally under 1'' + flagged prominently, **awaiting Makham's decision on whether floor2 needs its own dummy grid** (สิ่งที่ต้องแก้ item 38).
13. **Retracted yesterday's note:** the note "2' measured 4.9-5.0, differing from master by 0.3-0.4m" was **wrong** — caused by uncalibrated pixel reading; re-measured floor1=4.69, floor2=4.59 → `2'=4.6` was correct all along, removed the flag (a lesson following from the price-table incident: don't trust a loose, uncalibrated measurement).
14. Fixed a warning on page31 that had incorrectly said "nothing at B'" — there is a horizontal line at y=10.07 (the edge above the S0 zone), but with no readable label, not yet recorded as an element.

**Files added:** house3 pages 31/32 (major fixes), สิ่งที่ต้องแก้.md (items 38-41).

---

## Evening round — B' correction, deleting elements per instruction, full-sheet check of page33

15. Makham corrected **`B'`=10.2→**10.1** (formula 3+2.2+4+0.9) — cascaded impact: page32's B4X span 1.0→0.9.
16. Deleted per Makham's instruction: page31's `B2 E1'-E1`(1.1) and `B2 E3-E3'`(0.5) — the row-E extension on both sides (down to 27 elements); page32's `B1_landing D1'-D1''`(3.06, a guessed distance from the staircase box edge, not an actually printed dimension — down to 31 elements).
17. **Page33 (S-06 tie beams) fully checked against the original image, 17→23 elements:** fixed a mislabel `D1'-C1'`→**`D1-C1`** (zoomed 10x, confirmed the column sits exactly on the grid1 line, no offset like S-04/S-05 — the tie beam is a continuous loop, not broken at the staircase bay); added missing vertical beams D2-C2(2.2) and B2-A2(1.5), entirely absent before; added beams **B'2-B'3(3.0)** + **B3-B'3(0.9)** — the B' cascade piece that had been sought (the S1 box's edge: line B is open with no beam, line B' has a real beam, the same pattern as page32).

18. Makham directly corrected me: the D-C strip on page33 **does** have a real 1'/3' extension (my earlier conclusion of "none" was from re-using the calibrated scale from pages 31/32 without checking page33's own origin) — added 4 short beams matching B3X: `D1'-D1`, `C1'-C1`, `D3-D3'`, `C3-C3'` (confidence 1) — 23→27 elements.

19. Makham pointed out page35 (C1A) still used `top:4/bottom:4`, even though it had already been agreed (item 21) that columns use a single `count:4` — asked Makham first which way to go, confirmed `count:4` as before, fixed all 5 spots on page35 (C1A/C1 tie-beam level, C1A/C1 floor1-pile cap-footing, CN), then **swept the whole project**, finding the same bug hiding in fence column C1 in 6 more files (house1 pages 16/17, house2 pages 20/21, house3 pages 26/27 — previously worked around by adding a `total_main_bar_count` field instead of a real fix) — fixed all of them + added the missing `specs.CN` on page31, re-swept to confirm no spot remained anywhere in the project.
20. Makham ordered adding `RB1` at `D1'-D1`, `C1'-C1`, `D3-D3'`, `C3-C3'` on page33 — added exactly as instructed (confidence 1), also fixed the schema_note/warnings that had previously incorrectly concluded the D-C strip has no 1'/3', to match the corrected data.

**Files added:** house3 page00 (B'), pages 31/32 (deleted elements + added specs.CN), page33 (fully re-checked + fixed per Makham), page35 (columns C1A/C1/CN), 6 fence-column-C1 files (houses 1/2/3), สิ่งที่ต้องแก้.md (items 42-46).

---

## Late-night round — sweeping every pattern=plan file in house3 against the gridline master

21. Checked all 20 pattern=plan files in house3 against page00, which had many spots fixed today — found 4 spots needing fixes: pages 06/07 had text referencing "1''(2.25)" left over from before the face/centerline distinction was settled (adjusted to match the master's current value=2.35 face); page21's description was ambiguous between centerline(2.25)/master(2.35), clarified; **page07 had a genuine bug** — grid_ref was written as `"A-B'"`, even though its own description said it's below row A (the A-to-A' span), unrelated to B' (located elsewhere, above row A) — fixed to `"A-A'"`.
22. The other 16 files (pages 18/20/29-33/50/51/52x2/58-63) were checked and found clean, no leftover stale values — most MEP/lighting/outlet files already had grid_dimensions matching the master, or had no real dummy-grid reference at all (the apostrophes found were just the English "house #2's").

**Files added:** house3 pages 06/07/21 (fixed old reference text + the grid_ref bug), สิ่งที่ต้องแก้.md (item 47).

**Pending:** the eastern edge line `3'` on page32 has no mark; beam B1(landing) on line C on page32 not yet decided for deletion; the S0 zone below row A + "see enlarged detail 2" on page31; column3 B3-A3 in house3; house5's axis-origin flip; syncing back to the raw folder.

**Pending:** the eastern edge line `3'` on page32 has no mark; beam B1(landing) on line C on page32 (the twin already deleted) not yet decided for deletion; the S0 zone below row A + "see enlarged detail 2" on page31 not yet zoomed into; column3 B3-A3 in house3; house5's axis-origin flip awaiting decision; syncing back to the raw folder.

---

## Claude — Qwen3.6-35B-A3B fine-tune session (Constistant repo, `tune_ai/t01/data_before_tune/`)

1. Built the dataset from `json_แก้ไขแล้ว/` (`build_dataset.js`): 1 image = 1 example, stripped out cross-page specs + work-log contamination, split by house rather than by page (train=01+02+04=226, val=03=88, per Makham's instruction).
2. Selected the model Qwen3.6-35B-A3B (bf16 LoRA, not 4-bit — Unsloth warns about quantization with this model family), MAX_PIXELS=5120 tokens (tested cropping and found this is the lowest point that still separates top/bottom rebar points).
3. Wrote `train_qwen36.py`, `verify_env.py`, updated `eval_fields.py` adding a QWEN36 preset.
4. **onstart.sh bug:** `pip install --upgrade pip` collided with the apt-installed pip → an error, and `set -euo pipefail` killed the whole script (the first instance crashed into a crash-loop) — fixed with `|| true`; the 2nd instance (id 45406175) ran through completely, confirmed from the logs.
5. Hit repeated "Jupyter connection refused" issues — root cause: onstart.sh doesn't start a Jupyter server itself / direct-HTTPS needs a cert installed; **switched the standard to SSH + scp + tmux** instead of Jupyter entirely (the standard method used worldwide for real training).

**Files:** `tune_ai/t01/data_before_tune/{build_dataset.js,train_qwen36.py,verify_env.py,eval_fields.py,onstart.sh,README.md}`.

**Pending:** Makham hasn't yet tried an SSH connection into instance 45406175; the real dataset hasn't been uploaded yet; verify_env.py/eval_fields.py --base has never been run on the actual machine.

---
