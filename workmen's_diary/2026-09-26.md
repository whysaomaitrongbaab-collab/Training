# 2026-09-26

## [Claude — session with Mail (Mel)] Traced the untracked `training-data/` folder to its exact cause, restored it under a proper `.gitignore` rule; confirmed BOQ/BBS/Planner/3D genuinely share one AI-extraction data path; found and fixed the root cause of "no grid position from live drawing reading"

**Context:** Continuation of the `pipeline-optimize` work from 2026-09-24/25. Four separate things happened in one session, each triggered by a direct question from Mail rather than planned work: (1) Mail noticed VSCode's Source Control panel showing ~2,300 pending changes out of nowhere and wanted the exact cause, not a guess; (2) once traced, Mail asked to bring the deleted folder back but properly ignored this time; (3) Mail asked for a status recap across BOQ/BBS/Planner/3D, which led to a real cross-branch merge-status correction; (4) that recap surfaced the `grid_refs` question again, and this time it was actually fixed rather than just re-explained.

**Work done:**

1. **Root-caused the 2,300 untracked files.** `git status --short` on `Constistant` showed only `?? training-data/`, but VSCode expands that to one row per file inside it. Traced it precisely rather than assuming: two commits merged in from a different branch's history (`6d8538e "try"`, `ed8d5c3 "แบบบ้าน"`, both 2026-07-27) once tracked files under the path `training-data/raw/image/`; a later commit (`467cf86 "Stop tracking training-data/ to unblock GitHub Pages deploy"`) untracked them via `git rm --cached` (files stay on disk, just leave the index); separately, King's 2026-09-24 reorg commit (`f057a8e`) renamed the matching `.gitignore` rule from `training-data/raw/image/` to `data/training/raw/image/` — pointing the ignore rule at a folder name that no longer matched where these particular files actually sat on disk. Net effect on this machine: today's earlier `git pull --ff-only origin pipeline-optimize` (22 commits, done in yesterday's/this session's sync) walked through that history and the folder ended up on disk again, untracked, and no longer covered by `.gitignore`. Confirmed with `git ls-tree`/`git log -S`/reflog timestamp cross-checks, not inferred from the commit messages alone.
2. **Deleted the folder on Mail's first instruction** (`rm -rf training-data/`, 3.3GB / 2,300 files — construction drawing PDFs + per-page PNG renders, e.g. `บ้าน_เล็ก_2ชั้น_04/บ้าน_เล็ก_2ชั้น_04.pdf` and `บ้าน_เล็ก_1ชั้น_10/บ้าน_เล็ก_1ชั้น_10_หน้า07.png`).
3. **Mail asked for it back, properly ignored this time.** Checked whether the full 2,300-file set was ever actually committed anywhere reachable from `pipeline-optimize` — it wasn't; the git-tracked commits under that path only ever held 11–31 PDFs at most, never the full page-image set. Found the real source instead: the identical data (same naming convention, `image/บ้าน_เล็ก_1ชั้น_01/...`) already lives in this `Training` repo at `image/` (3,536 files, 4.3GB — a slightly newer/larger set than what was deleted). Copied `Training/image/` → `Constistant/training-data/raw/image/`, then added a correct `training-data/` line to `Constistant/.gitignore` (the old rule still points at the now-abandoned `data/training/raw/image/` path from King's rename — left as-is since it may still matter for whatever that path was renamed for, and just added a second line for the actual path in use).
4. **Corrected an earlier (wrong) status claim about branch merge state.** Had told Mail that merging `pipeline-optimize` with `Presentation-demo` was still a meaningful open task; on direct question ("didn't my friend already merge this?") actually ran `git merge-base`/`git log HEAD..origin/Presentation-demo` instead of assuming, and found `pipeline-optimize` is 14 commits *ahead* of `Presentation-demo`, and `Presentation-demo` has exactly **one** commit not yet in `pipeline-optimize` — `d78a5f2`, a pure-docs commit (`server/purson-worker/DEPLOY-THIRD-PARTY.md`, a third-party-deployment checklist by Makham, zero code, zero secrets in the file). So the cross-branch merge work this diary has been flagging as pending for two days was, in practice, already done via prior merges — only that one harmless doc commit remains, and it was left un-pulled pending Mail's decision (not urgent, no conflict risk either way).
5. **Traced the real data-flow, feature by feature, instead of re-asserting the earlier summary claim.** Mail asked to confirm BOQ/BBS/Planner/3D genuinely consume real AI-extracted drawing data end to end, not a decorative/disconnected source (echoing the earlier-session finding that Planner's 3D view had silently been running on a fake showcase pipeline for weeks). Read the actual call chain rather than trusting memory:
   - `runPipeline()` (`js/shared/pipeline.js:2428`) calls `getInputData()` → `getProjectElements(project.id)` (`project-store.js:398`), which reads one localStorage key per project (`constistant_elements_v1__<id>`).
   - `computeBOQ(elements, beamLibraryById, ...)`, `computeBBS(elements, beamLibraryById, boq, ...)`, and `computeSchedule(elements, boq, ...)` (Planner's task generator) are all called from inside that same `runPipeline()` function body, on the exact same `elements`/`beamLibraryById` variables — not three separate reads.
   - `planner-3d-view.js:56,71` (the real 3D model, revived in `88e80b8` two days ago) independently calls `getProjectElements()` again — same function, same storage key, so it can't drift from what BOQ/BBS/Planner see.
   - Confirmed both writers of that storage key are genuinely AI-extraction-derived: the live "อ่านแบบสด" (Qwen) path writes through `qt_saveExtractionToProject()` (`drawing-bridge.js:207`), which reads `globalThis.qt_elementsData` — the actual output of the real multi-pass Qwen vision extraction (Pass 0–4 in `drawing-index.js`), not placeholder data; the raw-JSON import path (`raw-extraction-import.js`) writes through the same `saveProjectElements()` setter.
   - **Conclusion, stated to Mail directly: all four features are genuinely wired, verified by reading the actual call chain, not asserted from a prior summary.**
6. **Found and fixed why the live-read path has never produced `grid_refs`.** This had been raised earlier as "a known gap" without checking exactly why. Grepped `js/ai/prompts.js` directly: none of the four extraction passes (Pass 1 per-element detail, Pass 3 layout-ID survey, Pass 4 per-mark length-group counting) ever asked the model for a grid-line position — the JSON schema requested in each prompt simply never included that field. This is a prompt-design omission, not a model capability limit, and not the same issue as the raw-JSON import path (which already carries `grid_refs`/`grid_ref_start`/`grid_ref_end` because the Training team's own extraction schema defines them — separately verified working end to end by Ton on 2026-09-25, per `Constistant/docs/raw-json-gaps.md` §8).
   - **Fixed in `Constistant/js/ai/prompts.js`**: extended `qt_buildCountPrompt(elementId)` (Pass 3/4, the pass that already re-reads the layout/floor-plan page per element mark) to also ask for a `grid_refs` array per length-group, one entry per physical instance in reading order (left-to-right, top-to-bottom). Explicitly instructed the model to return **fewer entries than `qty` rather than guess** when some instances' positions aren't legible, flagging `grid_ref_partial`/`grid_ref_not_visible` instead.
   - **Fixed in `Constistant/js/drawing/drawing-index.js`**: the Pass 3/4 merge loop now accumulates `grid_refs` per length key across pages (parallel to the existing `qty` accumulator) and flattens them onto the final element as `el.grid_refs` before Pass 4's merge step.
   - **Fixed in `Constistant/js/drawing/drawing-bridge.js`**: `buildDrawingElement()` now threads `el.grid_refs` into the `DrawingElement` factory call for both the slab branch and the beam-like branch (defaulting to `[]`, never `null`/`undefined`).
   - **Important correctness check made along the way, not just assumed**: read `structure-model.js`'s `realGridTokensFor()` directly and found it requires `el.grid_refs.length === count` **exactly** — a partial match (e.g. the model only found 4 of 6 instances' positions) causes the *whole* element to fall back to the synthetic grid, not a partial real placement. This is existing, deliberate behavior (documented in that file's own comments — never mix real and synthetic positions within one element), so the new honest "send what you're sure of" prompt behavior is compatible with it, not fighting it: a partial live-read result will just be safely ignored rather than partially trusted. Caught and corrected a misleading code comment of my own that had implied `structure-model.js` "handles" partial `grid_refs` gracefully — it doesn't handle them, it discards them cleanly, which is a different and correct thing.
   - Added 3 new regression tests to `Constistant/tests/drawing-bridge.test.js` covering: `grid_refs` passthrough for a beam, defaulting to `[]` when the model returns none, and passthrough for a slab.
7. **Mail also asked how to get a browser-automation tool** (to eventually verify yesterday's BBS/3D fixes live) — explained that no MCP browser server is configured on this machine at all (`mcpServers: {}`), gave the exact `claude mcp add chrome-devtools -- npx -y chrome-devtools-mcp@latest` command and noted it requires closing/reopening the Claude Code session afterward for the new tool to load. Mail deferred this for now ("ยังก่อนละกัน") — not installed.

**Files changed (`Constistant` repo, `pipeline-optimize` branch, not yet committed as of end of session):**
- `.gitignore` — added a `training-data/` line (alongside the pre-existing, now-mismatched `data/training/raw/image/` line, left untouched).
- `js/ai/prompts.js` — `qt_buildCountPrompt()` extended to request `grid_refs` per length-group with explicit "don't guess" instructions.
- `js/drawing/drawing-index.js` — Pass 3/4 merge loop accumulates and flattens `grid_refs` onto each element.
- `js/drawing/drawing-bridge.js` — `buildDrawingElement()` threads `el.grid_refs` into both the slab and beam-like `DrawingElement` factory calls, plus a corrected code comment about `structure-model.js`'s exact-match behavior.
- `tests/drawing-bridge.test.js` — 3 new tests for `grid_refs` passthrough.
- `training-data/raw/image/` (local disk only, restored from `Training/image/`, still git-ignored) — not a repo file change, noted for completeness since it was the session's first topic.

**Impact/precautions:**
- `npm test`: **2,043 tests, 2,040 pass, 3 fail** — the 3 failures are the same pre-existing, unrelated `raw-extraction-golden.test.js` stale fixture-path issue documented in every prior entry (`data/finetune-output-contract/model-output/examples/` does not exist at that nested path). No regression from anything done today.
- The `grid_refs` fix has **not been exercised against a real Qwen call** — no browser/network available in this sandboxed session, same limitation as every prior 3D/AI-vision fix logged in `Constistant/CLAUDE.md`'s history. It is verified only at the function/data-shape level (unit tests), not against real model output. Whether Qwen can actually read grid-line positions reliably from a real floor-plan page is genuinely unknown until someone runs it live.
- Because `structure-model.js` requires an exact `grid_refs.length === count` match per element, expect the live-read path's real-position usage to be inconsistent at first (some marks will get real positions, most probably won't, until prompt-tuning improves the model's hit rate) — this is expected, honest behavior per the existing "don't guess" contract, not a sign the fix is broken.
- The training-data restoration used `Training/image/` (3,536 files) rather than the exact original 2,300-file set that was deleted — the Training folder is a superset/newer snapshot, so nothing was lost, but the two counts won't match if anyone compares them later.
- The one un-pulled `Presentation-demo` commit (`d78a5f2`, docs-only) is a decision left open for Mail, not blocking anything.

**Left pending / to continue:**
- Still waiting on a real, connected live web deployment (Makham's dependency, carried over from 2026-09-24/25) before any AI-vision-path fix in this codebase — including today's `grid_refs` prompt change — can be checked beyond the unit-test level.
- Decide whether to pull in `Presentation-demo`'s one remaining doc commit (`d78a5f2`) — low priority, zero risk either way.
- Commit today's five changed files on `pipeline-optimize` and push, once Mail gives the go-ahead (per the standing rule that pushes happen only on explicit instruction).
- If/when a browser MCP tool does get installed, the natural next check is: run a real drawing through "อ่านแบบสด" and see whether Pass 3/4 actually returns usable `grid_refs`, or whether the prompt needs another iteration.

---

## [Mail (Mel)] Got a real phone able to open the Constistant web app via Tailscale, and created a blank placeholder page for the mobile site-checklist feature — the tab is now open for Ark/Im to build the mobile-specific pipeline features and UX/UI

**Context:** Continuation of the same-day mobile-checklist-for-site-engineers discussion. King's rough plan (photographed from a `gridline.json` note, split by owner) named Mel's own piece as: get the web reachable from a real phone before anyone else's part (Ark/Im's pipeline-sync work, Ton's Resource Hub tie-in) can be tested against it. This entry covers getting that reachable and creating the first empty page for it — no feature logic yet, deliberately.

**Work done:**

1. **Decided on the approach with Claude first (discussion only):** the Constistant app is a static, no-build site with no server-side logic — the same URL always serves the same file regardless of device, so a mobile view needs its own dedicated page (not auto-detection) and no server can exist to route by device *unless* a real backend is running. Since Makham's own server deploy is separate infrastructure for AI-vision inference (Destrier/purson-worker GPU worker), not a general backend for this app, it does not change that constraint. Landed on: build a **separate, dedicated mobile page**, reuse the existing `work-packages.js` tick logic + the app's real per-user auth/cloud-sync so mobile ticks sync automatically with the desktop Planner (no new backend needed) — this is not yet implemented, just the direction agreed for later.
2. **Set up Tailscale as the temporary bridge for phone access** since there is no real server deployment yet: installed Tailscale on both the notebook and the phone, logged into the same tailnet. Understood and confirmed the tradeoffs directly (not just assumed): the notebook has to stay powered on and running the local dev server the whole time anyone wants to view it from a phone; anyone else (Ark/Im/Ton) who wants to view it would need to install Tailscale too and be invited into the same tailnet — this is fine for team-internal dev/testing, not something to hand to a real site engineer.
3. **Debugged Five Server / phone reachability end to end, methodically, not by guessing:**
   - Set Five Server's `Host` setting to `0.0.0.0` (was `localhost`, which only accepts connections from the machine itself) via VSCode/Antigravity IDE settings.
   - First attempt still failed from the phone — checked Windows Defender Firewall next: confirmed Tailscale's interface is classified under "Private networks" and that `Node.js JavaScript Runtime` was already allowed through for both Private and Public (so firewall was not the blocker, ruled out rather than assumed).
   - Verified the server was actually listening on the right interface via `netstat -an | findstr 5500` → confirmed `0.0.0.0:5500 LISTENING`, not stuck on `127.0.0.1` from before the Host setting change.
   - Confirmed the page opened fine from the notebook's own browser using the Tailscale IP directly (not `localhost`) — isolating that the server side was fully correct and any remaining problem was phone-side.
   - Hit one real, avoidable mistake along the way: typed/used the wrong Tailscale IP on the phone (`100.90.93.42`, which was not the notebook's actual IP) and got `ERR_CONNECTION_REFUSED` — resolved by running `tailscale ip -4` on the notebook to get the real, current address and using that exact IP on the phone instead.
   - **End result: the phone genuinely opens the Constistant web app now, live, through Tailscale.**
4. **Created the first file for the mobile feature: `Constistant/contistant-site-checklist.html`** — an intentionally empty placeholder ("หน้ากาก") per Mail's explicit instruction to build the skeleton first and add features later. Sits at the repo root alongside `contistant.html`. Has its own mobile-correct `<meta viewport>` (does not inherit the desktop shell's `body { min-width: 1280px; }` constraint), a dark theme reusing the app's existing color tokens loosely, and a "🚧 กำลังพัฒนา" placeholder card — zero logic, no imports, no localStorage/auth wiring yet.
5. **Confirmed the reachability question one more time for the team**, since it matters for planning: this Tailscale setup only lets people already inside the same tailnet reach the notebook — it is not yet a public URL anyone can open. Ark/Im/Ton would each need Tailscale installed and invited before they could open it themselves; this is expected to go away entirely once a real deployment exists.

**Files changed:**
- `Constistant/contistant-site-checklist.html` (new) — blank mobile placeholder page, no feature code yet.
- No other repo files changed by this session (the Tailscale/Five Server work was local machine configuration, not committed to any repo).

**Impact/precautions:**
- The mobile page has literally no functionality yet — it is safe to open/commit/reference, but nobody should expect ticking or data on it until Ark/Im build the pipeline-sync layer King's plan assigns them.
- The Tailscale-based access is per-device and requires the notebook to stay on — anyone testing against it needs to coordinate with Mail on timing (notebook up + Five Server running) rather than assuming it is always reachable.
- Nothing about this changes the desktop shell (`contistant.html`) at all — the new file is fully separate, per the earlier-agreed "dedicated page, not auto-detection" direction.

**Left pending / to continue:**
- **Handoff to Ark/Im, per King's plan**: now that the phone can genuinely open a real page served from the notebook, they can start on (a) the mobile-specific pipeline wiring — how a tick made on this page reaches/updates the same data the desktop Planner reads, kept in sync automatically — and (b) the actual mobile UX/UI design for the checklist itself. Both were blocked only on "can a phone even reach a page" being proven true, which is now done.
- Ton's Resource Hub tie-in is still separately pending on a conversation with Ton (per King's plan, unchanged from earlier today).
- Once Ark/Im's pipeline wiring exists, `contistant-site-checklist.html` needs the real UI + `work-packages.js`/auth wiring described in item 1 above — none of that exists yet, this session only proved the page can be reached and created its empty shell.
- The Tailscale bridge should be dropped once a real deployment (Makham's or otherwise) exists — it is explicitly a temporary dev/testing measure, not the final access method for a real site engineer.


---

## [Claude — session with makham, 25 ก.ย. ค่ำ → 26 ก.ย.] pass3 ใช้ไม่ได้จริง (0/9 หน้าใน production) → แก้แบบ report-only + เว็บใช้ความยาวคานจากแบบ + เจอทางใหม่จาก opentakeoff (ไม้บรรทัดจากเวกเตอร์ PDF) — ยังไม่ commit

**ทำไม:** มะขามสงสัยว่า pass3 (`pass3_measure.py`, Python ล้วน วัดระยะจากพิกเซลเทียบ grid master) ใช้ไม่ได้ สั่งทดสอบให้ละเอียด ห้ามเช่าการ์ด (att1235)

**วิธีทดสอบ:** โหลดผลงานจริง 5 งาน + รูป 81 หน้าจาก Supabase (อ่านอย่างเดียว) มารันซ้ำ offline ด้วยโค้ด worker จริง · จำลองด้วยเฉลย 40 บ้าน (`json_แก้ไขแล้ว`) เป็นคำตอบโมเดล · fuzz เรขาคณิต · ทีมหักล้างทุกข้อ → 31 ข้อค้นพบ

**ผลที่ยืนยันแล้ว:**
- production: pass3 วัดได้ **0/9 หน้า** ทุกงาน (83b8e52c 0/7 · 267d1989 0/1 · cbb90abe 0/1) — เทสเดิมเขียวเพราะไม่เคยเทสเส้นทาง shape-match ที่เป็นเคสจริงเลย
- ต่อให้โมเดลตอบถูกทุกข้อ (เฉลยบ้าน 21-40): วัดได้ 13/85 หน้า · **แปลนคาน/หลังคา 0/60** (ไม่มีหมุดฐานราก/เสาบนหน้า)
- **วัดผิดแต่ขึ้นว่าผ่าน:** grid ที่ origin อยู่ล่าง → จับคู่กลับด้านกระจก ผ่านด่าน residual (หน้าจริงบ้าน 06 หน้า14: ok, คลาด 0.03 ม. แต่แถวสลับหมด)
- `cv_mark` ของ Destrier = เลขนับตามลำดับ 1..N ไม่ได้ชี้กล่อง CV จริง
- **เว็บทิ้งทุกอย่างที่ pass3 เติม** (element ไม่มีชื่อถูกข้าม, `span_length_m` ไม่ถูกอ่าน) → BOQ มี/ไม่มี pass3 ต่างกัน ฿0 ใน 17/17 บ้าน
- **เว็บทิ้งความยาวคานที่โมเดลอ่านจากแบบ** (`local_dimension`) → ตก `DEFAULT_FLOOR_HEIGHT_M` 3.0 ม. สถานะ ok ไม่มีธง (83b8e52c หน้า24: 0.5/1.0/2.2 ม. → 3.0 ม. · 30 แถว ฿23,087)
- **grid master งานจริงผิด:** 83b8e52c แกน x 0/6/12 ทั้งที่แบบพิมพ์ 3.00+3.00 → BOQ **+฿54,587 (+30.8%)**, origin อยู่ล่าง · cbb90abe มี id `จ` ซ้ำ (เส้นบนจริงคือ `ฉ`) · 43323920/f63bc71e ตัวอักษร+ตัวเลขปนแกน x
- สมมติฐานแรกของ Claude (Python อ่าน `1ค` ไม่ได้แต่ JS อ่านได้) มีจริงแต่**ไม่ใช่สาเหตุ** — แก้แล้วผลจริงไม่เปลี่ยนสักหน้า
- **รูปที่เว็บส่ง Destrier ความละเอียดไม่ตรงเทรน:** เว็บ 180 dpi (`RENDER_SCALE = 2.5`) vs เทรน 200 dpi (PyMuPDF, ยืนยัน MAE 0.0) → CV ฐานราก/เสา recall 71% vs 91% · 83b8e52c หน้า19 เจอ 5/14 vs 14/14
- หน้า24 ของ 83b8e52c เป็นแบบขยายรูปตัด (S-06) แต่ pass0 จัดเป็น `plan_beam` → คาน 10 แถวนับซ้ำกับหน้า20

**ตัดสินแล้ว ✅ (att1235):**
- **D1** เว็บ: ความยาวคาน grid ก่อน → `local_dimension` → `scaled_from_grid` → `cv_measured` (คานเท่านั้น) → unresolved · ไม่ได้มาจาก grid = ติดธง `span_not_from_grid` · คานตก 3.0 ม. = ติดธง `default_beam_length_assumed` — *ความเสี่ยง:* เชื่อตัวเลขที่โมเดลอ่าน แต่ทุกแถวมีธงเห็นได้
- **D2** grammar ของ grid ref ชุดเดียวทั้ง JS/Python: `Constistant/tests/fixtures/grid-ref-vectors.json` (ตัวอักษร = แถว, ตัวเลข = คอลัมน์ ตัดสินจากชนิดตัวอักษร, หาแกนจาก grid จริง, กำกวม = null)
- **D3** pass3 = **รายงานอย่างเดียว** ไม่แก้ข้อมูล pass2 อีก — *ความเสี่ยง:* ถ้าวัดได้จริงก็ยังไม่เติมอะไรจนกว่าวิธีใหม่พิสูจน์ได้
- **D4** ทิศแกนกำกวม (grid สมมาตร) = ปฏิเสธ `orientation_ambiguous` — *ความเสี่ยง:* หน้าที่วัดได้ลดลง แลกกับไม่มีค่าผิดหลุดเงียบ
- **D5** แถวจากหน้าแบบขยาย/รูปตัด = ติดธง `plan_row_from_detail_sheet` (ไม่ลบทิ้ง)
- **C1/C2** `pass3_measure.json` v2 + `grid_master.json` มี `validation` (id ซ้ำ / แกนปน / origin / scale_suspect) → เว็บติดธงความยาวบนแกนที่น่าสงสัย
- ไม่แตะสิ่งที่โมเดลรับเข้า (prompt/hint/crop) ในรอบนี้ · RENDER_SCALE 200 dpi จะแก้พร้อมตอนต่อสาย vector sidecar (เปลี่ยนเฟรมพิกัดครั้งเดียว) — *ความเสี่ยง:* ยังไม่ทดสอบกับการ์ดจริง งานจริงรอบแรกหลังแก้ต้องดูใกล้ๆ, ถอยกลับได้บรรทัดเดียว

**opentakeoff (Kentucky-ai, Apache-2.0 — https://github.com/Kentucky-ai/opentakeoff, เว็บ https://opentakeoff.kentucky-ai.com):**
- หลักคิด "scale เป็นประตู ไม่ใช่ค่าเริ่มต้น" ตรงกับ D3 · วัดจากเวกเตอร์ PDF ไม่ใช่พิกเซล
- ทดลองกับ PDF ของเรา: 22/53 เล่มเป็นเวกเตอร์ (CAD), 28/53 เป็นภาพสแกน · ไม้บรรทัดจากเส้นกริดเวกเตอร์วัดได้ **51/98 หน้า** รวมแปลนคาน 25/47 (pass3 เดิม 0) · ทิศ +x ขวา +y ลง 31/31 · ป้ายมาตราส่วนที่พิมพ์เชื่อไม่ได้ (บ้าน01 พิมพ์ 1:100 แต่ PDF เป็น 1:141.4 = A3 พิมพ์ลง A4)
- แกนยาวสองเท่าจับได้ด้วย completeness gate ต่อแกนเท่านั้น (standard-scale snap จับไม่ได้)
- เว็บดึงเวกเตอร์ได้จริงด้วย pdf.js 3.11.174 ตัวที่ใช้อยู่: 1.3–2.6 KB gz/หน้า, ตรงกับ PyMuPDF 771/781 เส้น · bucket `purson-jobs` ไม่จำกัด MIME/ขนาด (เช็คแล้ว)

**ไฟล์ที่เปลี่ยน (Constistant, Presentation-demo, ยังไม่ commit):** `raw-extraction-adapter.js` · `raw-extraction-import.js` · `pipeline.js` · `schema.js` · `boq-summary.js` · `drawing-purson.js` · `structure-model.js` (prime normalisation ใน gridPosLookup — ไฟล์พื้นที่ Mail) · `pass3_measure.py` · `worker.py` · `test_worker.py` · ใหม่: `test_pass3.py`, `test_fixtures/`, `tests/fixtures/grid-ref-vectors.json`, `tests/fixtures/purson-real/`, tests 7 ไฟล์

**เทส:** npm **2168/2171** (3 ตกเดิม raw-extraction-golden) · `test_pass3.py` 33/33 · `test_worker.py` ผ่าน · `pass3_measure.py` self-check ผ่าน · รีวิวแบบหักล้าง 2 คน: เว็บ approve (8 minor), worker 9 ข้อ — รอบแก้ตามรีวิวหยุดกลางทางตอนปิดเซสชัน

**ผลกับงานจริง 83b8e52c (จากรีวิว):** BOQ ฿232,025 → ฿226,076 · แถว needs_review 31 → 61 · 0 แถวที่เปลี่ยนตัวเลขโดยไม่มีธง · demo 5 โปรเจกต์ตัวเลขเท่าเดิม

**Left pending:**
- ตรวจ diff ที่รอบแก้ไขทำค้าง + ปิด review issue ที่เหลือ → commit
- ไม้บรรทัดเวกเตอร์: `vector_ruler.py` (worker) + `pdf-vector-sidecar.js` (เว็บ) — agent หยุดก่อนสร้างไฟล์ ต้องรันใหม่ แล้วค่อยต่อสาย + RENDER_SCALE
- เฉลยบ้านไทยเป็นสุข3 (งาน 83b8e52c) สำหรับเทสงานจริง→BOQ: คนอ่าน 1 เสร็จ 11 ไฟล์ (scratch), คนอ่าน 2 ยังไม่เสร็จ → เทียบ
- ข้อมูลเทรนที่ต้องแก้ → ลง `json_แก้ไขแล้ว/สิ่งที่ต้องแก้.md`: บ้าน29 grid_ref `C2` ควรเป็น `C1'` · บ้านที่ grid origin อยู่ล่าง (06/07/10) · ตัวอักษรอยู่แกน x (30, 37)
- `structure-model.js` แก้ในพื้นที่ของ Mail — Mail แก้ `prompts.js`/`drawing-index.js`/`drawing-bridge.js` บน `pipeline-optimize` วันเดียวกัน ไฟล์ไม่ทับ แต่ต้องระวังตอน merge
- งาน `448530d2` (1 หน้า) ค้าง pending ใน queue ตั้งแต่ 25 ก.ย. — เปิด worker ครั้งหน้าจะหยิบก่อนและใช้การ์ด

## [Claude — session with makham, 26 ก.ย. ต่อ] ไม้บรรทัดเวกเตอร์ต่อสายครบ + เฉลยบ้านจริง 83b8e52c + แก้มิติฐานรากทั้งระบบ

**Commits (Constistant, Presentation-demo, local ยังไม่ push):** `8f31d25` เว็บส่ง vector sidecar · `a65f315` worker `vector_ruler.py` (รายงานอย่างเดียว) · `4573cca` มิติฐานราก + เทสต์บ้านจริง

- **ไม้บรรทัดเวกเตอร์:** worker อ่าน `vectors_v1.json` → เทียบเส้นกริดกับ grid master → `pass3_measure.json` key `vector_pages` · เฉลย 68/98 หน้าเวกเตอร์ (แปลนคาน 38/47) · grid ที่จงใจทำให้ผิด รับผิด 0 · ใช้ได้เฉพาะ PDF จาก CAD · **ยังไม่เคยรันกับการ์ดจริง**
- **เฉลย 83b8e52c:** อ่านสองรอบอิสระ ตรงกันเกือบหมด · ตัดสินเอง 3 จุด (เหล็กบนเหนือจุดรองรับรวมตาม §7 · F3 ไม่มีเหล็กรัดรอบ · ไม่นับ B1 ใกล้แนว ข) · อยู่ที่ `tests/fixtures/real-house-truth/83b8e52c/` + เทสต์ `tests/real-house-truth-boq.test.js` — ยังไม่มีคนเซ็น
- **โมเดล vs เฉลย:** ยอดรวมใกล้กัน (−5%) โดยบังเอิญ — คานเกิน +฿51k (อ่านหน้ารายละเอียดเป็นแปลนซ้ำ) · เสาหาย −฿27k (ป้าย "C1,F4") · ฐานรากเป็น 0 ติดธง · หน้า 19 อ่านได้ F1 ×4 ที่ไม่มีจริง
- **บั๊กฐานราก (ใหญ่):** GT ใช้ field มิติฐานรากสามแบบขัดกัน ส่วนใหญ่ไม่มีความหนา → ระบบเอาด้านแปลนเป็นความหนา (GT บ้าน07 117.8 m³) · แก้: ตัดสินจากค่า, ฐานผืนผ้าคิดสองด้าน, ไม่มีความหนา = 0 + ธง `footing_thickness_missing`, pile_cap → footing · GT 41 บ้าน ฿7.32M → ฿5.58M
- ลงรายการ GT ที่ต้องให้คนแก้ → `json_แก้ไขแล้ว/สิ่งที่ต้องแก้.md` ข้อ 67

**ตัดสินแล้ว ✅:** ไม่เดาความหนาฐานราก (ขึ้น 0 + ธงแทน) — *ความเสี่ยง:* บ้านที่แบบไม่ให้ความหนาจะเห็นฐานราก ฿0 จนกว่าจะกรอกเอง แต่เลขเดิมผิดหลายเท่า

**เทส:** npm 2194/2197 (3 ตกเดิม) · test_vector_ruler 28/28 · test_worker ผ่าน · test_pass3 33/33

**Left pending:**
- RENDER_SCALE 180→200 dpi — รอมะขามเลือก (ก) แก้เลย หรือ (ข) A/B บนการ์ดก่อน · ถูก safety block ตอนพยายามแก้เอง
- โฟลเดอร์ `docs/research/opentakeoff/` (นิทาน PDF) โผล่ใน Constistant ไม่ใช่ของ Claude เซสชันนี้ — ไม่ได้ commit
- โมเดลอ่านหน้ารายละเอียดเป็นแปลน + ป้ายรวม "C1,F4" → ต้องแก้ที่ข้อมูลเทรน/prompt
- เหล็กตะแกรงฐานผืนผ้ายังคิดทุกเส้นที่ด้านยาว (เกินจริง)
- งาน `448530d2` ยังค้าง pending ใน queue
