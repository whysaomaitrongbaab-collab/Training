# 2026-08-02

## Claude — session with Makham (midday)

**Work done:**
- Checked `primary_rawjson_schema.md` against `สิ่งที่ต้องแก้.md` — found 4 contradictions, **the spec was wrong in all 4**
- Normalized JSON format across houses 01-11 for gridline / plan / footing plan / section
- Converted this whole diary from Thai to English and deleted the parallel `en/` folder

**Files changed:**
- `rawjson_ยังไม่ได้แก้ไขโดนคน/00file_for_making_rawjson_from_claude/primary_rawjson_schema.md` — 5 fixes: point-ref takes **no dash** (`"C2"`, dash means a range); dummy grids are named after the grid **above/left, not the nearest**; **columns use a single `main_bar.count`, never top/bottom**; added the point-element merge rule (item 48) next to the beam atomic rule; cleared the §12 draft flag on dummy prime ordering. Also folded in the `sheet_code` regex footgun warning and the "user-supplied label is recorded as given" exception. Logged in `primary_rawjson_schema_edit_log.md` per rule_of_tune rule 7 (`.md` file → Rule 1 exempt; no new schema structure → Rule 2 not triggered, the spec was just catching up to data already applied)
- **Verified against the real data before editing the spec, did not trust the log** (lesson from item 55): no dashed point-refs left in houses 01-05, and **0 files** with `element_type: "column"` still carrying `main_bar.top`
- `json_แก้ไขแล้ว/` — houses **06-11 copied in fresh** from raw (571 files; they had never been in this folder). `rawjson_ยังไม่ได้แก้ไขโดนคน/` itself untouched apart from the 2 `.md` files above
- Format normalization, all 11 houses: house 07's 21 files using patterns outside the spec's 14 (`detail`/`elevation`/`diagram`) remapped to what the other 10 houses use · 7 grid masters re-nested under `grid{}` · 11 footing plans merged to one entry per mark + point-ref dashes stripped · **houses 10/11's 65 `main_bar`/`stirrup` values converted from plain strings to schema §6 objects**, original string kept in `printed_as` on every one
- `workmen's_diary/` — all 27 files now English; `README.md` gained an English-only writing rule (with the keep-Thai-verbatim list for filenames and drawing-quoted data); `en/` folder deleted (it had been stale since 2026-07-21, two folders that disagree is worse than one)
- Full detail for every fix is in `สิ่งที่ต้องแก้.md` items **59** (spec sync) and **60** (format normalization)

**Impact/precautions:**
- Scorecard across all 11 houses: 1,183/1,183 files parse · pattern vocabulary legal everywhere · grid masters all nested · rebar all objects · footing 17/20 files at one-entry-per-mark
- **The 3 footing files left un-merged are deliberate, not an oversight** — house 06 `F2`, 07 `F1`, 09 `F0.8x0.8` have entries that genuinely differ in confidence or per-position note (06's second entry is flagged `inferred_from_repeating_module`). Merging would erase the record that those positions were inferred rather than read off the drawing. A human decides how to fold those notes, not a script
- Checked for unit corruption and **found none**: every `_mm` field under 5 is real thin steel (3.2mm channel, 4mm mesh) and **zero** `_m` fields hold a millimetre value. The field-naming split (`height_mm` vs `height_m` vs `depth_m`) is a naming problem, not corrupt data
- Nothing in houses 06-11 has been checked against a source drawing image yet — this was format-only work

**Left pending / to continue:**
- Houses 10/11 `pattern=section` files have **no `elements[]` at all** — they invent a per-file array name (`beams`/`columns`/`slabs`/`footing_types`/`details`/`sections`) and the element shape differs too, so renaming the array is not enough
- `specs{}` used by only 24 of 1,183 files; houses 07/11 have none
- **Beam plans in houses 06-11 have never been checked for missing beams** — every one of houses 01-05 was missing real beams when reviewed (20→27, 22→36, 30→38, 5→21), so expect the same here
- 5 files still carry `phase_note` (houses 09, 10) = unfinished by §2a's own rule
- House 05 puts a suffix on `element_id` (`F1.30x1.30_E1`), which breaks cross-page joins
- Multi-level column schedules: houses 04/05 use nested `levels[]`, houses 01/02/03 use flat rows + `level` (§8 says flat is correct)
- Sync back to `rawjson_ยังไม่ได้แก้ไขโดนคน/` still not started for any house

---

## Claude — session with Makham (afternoon, round 2)

**Work done:**
- Applied `สิ่งที่ต้องแก้.md` properly across houses 01-11 so every house shares one JSON format
- Gave the Rule 2 warning and **stopped to ask 3 questions before touching anything** (this round changes real schema structure, not just formatting). Makham chose: full restructure for houses 10/11, fold `phase_note` into `warnings[]`, strip underscores from `element_id`

**Files changed:**
- **Houses 10/11 (30 files)** — they had no `elements[]` at all, inventing a per-file array name. Folded `beams`/`columns`/`footing_types`/`slabs`/`structural_elements`/`details`/`sections` into `elements[]` with `element_id`/`element_type`; `size_m`→`width_mm`+`height_mm`, `thickness_m`→`thickness_mm`, string rebar→objects, every original kept in a `*_printed_as` field. `fixture_symbol_legend` and `fixture_install_height_standard` deliberately left alone — they are legend tables, not elements
- **Found the array named `column_sections` actually holds BEAM sections** — all 17 `cut_ref` values name a B-mark (B1, B2A, B3, R1, RB0, B4, B5, B6X), not one column. Converted to `element_type: "beam"` with the cut label kept in `section_ref`
- `confidence_score` set to `null`, **not invented** — this house never recorded one, and making a number up would be fabricating data-quality info
- **House 05 (20 rows)** — multi-level columns were nested in `levels[]`; §8 forbids that and the other 5 houses use flat rows. Split into one row per (mark, level), `tie`→`stirrup`
- **5 files** — `phase_note` folded into `warnings[]` and the field removed per §2a. The questions inside are **not resolved**, just preserved verbatim
- **82 `element_id`s reduced** to the printed mark, original kept in `element_id_printed_as`

**Impact/precautions:**
- **The underscore-stripping instruction could not be followed literally — 17 cases collided and were reverted (37 ids across 5 houses).** `บันได_ST1` and `steps_ST1` both reduce to `ST1`; `B1_ชานพัก` (a landing beam, a genuinely different member) would have collapsed onto the ordinary `B1`; three eave materials at `R1` and three separate rafters at `C150x75x25` would each have become one. Those keep their suffix with a flag saying why. Final check: **0 collisions**
- My first pass was too broad — it flagged 1,059 ids that were legitimately descriptive (dimensions, rooms, windows) and polluted 205 files with noise warnings. Removed; warnings now only where a structural element's id really can't be joined
- **Data preservation verified against untouched raw**: house 10 = 21 files / 69 entries before and after, house 11 = 17 files / 74 entries before and after
- 8 footing files still have a repeated mark **on purpose** — each pair differs by real `confidence_score` or per-position `note` (house 06's `F2` has one entry flagged `inferred_from_repeating_module`). Merging would erase which positions were read off the drawing and which were inferred
- `rawjson_ยังไม่ได้แก้ไขโดนคน/` — **zero JSON files touched** all session

**Left pending / to continue:**
- `specs{}` still only in 24 of 1,183 files
- **Beam plans in houses 06-11 have still never been checked for missing beams** — every one of houses 01-05 was missing real beams when reviewed
- Bars printed with a `หยุดที่ L/8`/mid-span qualifier in houses 10/11 are not merged into a face yet — needs the §7 leader-line check against the drawing
- Field-name/unit split (`width_mm` vs `width_m` vs `depth_m`) — naming only, values confirmed correct
- Sync back to `rawjson_ยังไม่ได้แก้ไขโดนคน/` still not started for any house

---

## Claude — session with Makham (afternoon, round 3)

**Work done:**
- Made `grid_ref` notation identical across all 11 houses — Makham's complaint was "sometimes `C1`, sometimes `c-1`"

**Files changed (87 files):**
- **809 point refs** still written with a dash (`C-1`, and house 06's Thai `ค-1`) → `C1`/`ค1`. Round 2 had only fixed files whose *name* contained `footing`; every other plan file in houses 06-11 was still dashed
- **264 values** carrying a redundant `grid`/`dummy` word (`gridD-gridC` → `D-C`, `beyond grid 3` → `beyond 3`) per item 5
- **2 values** using the word `gridline` (`'D-E (y) at gridline 1 (x)'` → `'D-E x 1'`), direction confirmed against house 10's own grid master

**Impact/precautions:**
- **Worth knowing: the instruction said follow houses 01-05, but on the `grid`-word problem houses 01-05 were the worst offenders** (house 05 had 127, house 06 had zero). So I followed the *rule* houses 01-05 wrote down (item 5), not the *state* of their files — the rule was right, it had just never been applied everywhere
- Only values under `grid_ref`/`grid_ref_start`/`grid_ref_end`/`grid_refs` were touched, so the `sheet_code` regex footgun (items 26/31) was impossible by construction, not merely avoided by care
- **Quality checked, not just counted: all 2,007 point refs across all 11 houses resolve against their own house's grid master — zero dangling.** The rewrite points at grid lines that actually exist
- Final state: **0 non-conforming `grid_ref` in all 11 houses**; 1,183/1,183 files parse

**Left pending / to continue:**
- Unchanged from round 2 — `specs{}` coverage, missing beams in houses 06-11, `หยุดที่ L/8` bars in houses 10/11, sync back to `rawjson_ยังไม่ได้แก้ไขโดนคน/`

---

## Claude — session with Makham (evening, round 4)

**Work done:**
- Locked the format down in `primary_rawjson_schema.md` itself, so op1 on house #1000 comes out identical to house #1
- Synced the working copy back into `rawjson_ยังไม่ได้แก้ไขโดนคน/` — the first sync-back in the project's history

**Files changed:**
- **`primary_rawjson_schema.md` — new §0 FORMAT LOCK at the top of the spec** (10 sub-rules: container, required element fields, `element_id` = printed mark only, canonical `element_type` list, mm integers, rebar-must-be-an-object, `printed_as`, `grid_ref` notation table, no invented `pattern`, closing checklist). The old spec described *how to write each field* but never locked *the shape of the file* — which is exactly why houses could invent array names, store rebar as a string, or spell the stirrup `tie_bar` without breaking any written rule
- **`tools/check_format.py` (new)** — runs all 14 checks, exits 1 on failure so it can gate an op1 run. A 1000-house set cannot be hand-audited
- **raw JSON, 149 format fixes across 11 houses**: 113 packed size strings → `width_mm`/`height_mm`, 22 `tie`/`tie_bar` → `stirrup`, 6 missing `warnings`, 2 real `sections` arrays folded into `elements[]`, house 10's footing plans merged per item 48 with columns split out per item 53
- Earlier in the day: **362 files synced** into the raw folder after a full Rule 2 warning and explicit permission; logged in `raw_json_data_log.md` and `training-data/CLAUDE.md`, backed by git commit `0029264`

**Impact/precautions:**
- **My first draft of §0.1 was too broad and I had to correct myself.** It banned any array called `sections`/`columns`; the checker then flagged 106 files. Opening them showed **104 were legitimate** — `sections` in an `index` file means document sections, `columns` in a BOQ means table column headers. Fixed the *spec and the checker*, not the data. Only 2 files were real
- **The checker's own first version was also too narrow** — it judged footing duplicates by `confidence_score`/`note` alone and would have merged away 4 groups whose entries each carry a *different* `confidence_flags` reason. Now it counts `confidence_flags` too
- `element_type` had grown to **359 distinct values across 4,507 elements** — that is what §0.4's canonical list exists to stop
- Final: `python tools/check_format.py` → **ALL CHECKS PASS, exit 0**, all 11 houses; working copy and raw differ in **0 files**
- The 11 groups the checker reports as NOTE are footings deliberately left unmerged (real per-position provenance); §0.10 documents this as the one allowed exception so nobody "fixes" them later

**Left pending / to continue:**
- Content work, all still untouched and all needing the drawings open: `specs{}` coverage, **missing beams in houses 06-11**, `หยุดที่ L/8` bars in houses 10/11
- Today was format only, by instruction

---

## Claude — session with Makham (evening, round 5 — independent fable audit)

**Work done:**
- Makham switched to fable and ordered an independent re-check: "is everything really the same pattern?" Answer at the start: **no** — answer at the end: **yes**
- Audited by comparing the JSON type of every key across all 2,366 files, deliberately not relying on the existing checker (it only checks rules it knows)

**Found and fixed (both folders, originals kept in *_printed_as/flags):**
- ~500 elements without `element_id` in houses **01-07** (08-11 were already complete — the opposite of the "follow 01-05" assumption); ids backfilled from printed marks/item_no/descriptive style with per-entry flags
- House 04's 10 BOQ files were `pattern=unknown` with `elements[]` → `material_list` + `categories[].items[]`
- **The identical series price table was stored 5 different ways in 5 houses** → unified to `series_price_table[]` (majority form, houses 08-10); house 01's copy is still raw text, flagged PENDING rather than guessed
- Credits pages split across 4 patterns → `misc` per majority + standing rule
- `slab_markers`/`reference_markers`/`door_window_schedule` arrays folded into `elements[]` (D1/W1 ids per houses 01-05)
- `steel_section` strings (house 01) → §6a objects; `main_bar`/`rebar` arrays → new `bar_layers[]`; `count:"4+4"` → sum; `dia_mm:"16+12"` → `mixed_dia_mm`; ~170 string `level_m` → numbers; 30 array `grid_ref` → notation; `span_m` arrays → `spans_m`

**⚠️ Beyond format — a content regression caught:** house 05's price-table copy still carried the OLD wrong digits (1,060,000/1,560,000) that item 37 recorded as "fixed on all 5 copies" (verified at 8x zoom). Houses 03/04 checked correct; only 05's copy had escaped. Restored to the item-37 consensus — executing the already-verified decision, not making a new one — old digits kept in *_printed_as with RE-CORRECTED flags. **Item-55 lesson, round two: "the log says fixed" must be verified per copy, not per issue**
- Deliberately NOT collapsed: house 10 prints mark **1** on two genuinely different windows → kept `W1` and `W1_variant` per §0.3

**Result:** checker grew 14→**19 checks** (identity + type purity) — `ALL CHECKS PASS`, all 11 houses, 2,366/2,366 parse, folders differ in 0 files. Spec §0 updated to match; everything logged (edit log, data log, สิ่งที่ต้องแก้ item 64)

---

## Claude — session with Makham (night, round 6 — Label Studio cancelled)

**Work done:**
- Makham cancelled Label Studio entirely ("ไม่ต้องเก็บข้อมูลแล้ว") — tidied `training-data/` accordingly

**Files changed:**
- New staging folder **`wait_for_ทิ้ง/`** (repo root, with its own README) — everything directly Label Studio moved there pending real deletion, all via `git mv` so it's recoverable: `label_studio_stuff/` (generator + import scripts + XMLs + task JSONs for 10 houses), the mindmap PDF, `Prompt/stage-a/label-studio-config.xml`, `manifest.json` (LS pipeline state), `review.html` (pre-LS review UI), `annotated/` (held only SAMPLE-annotated.json — no real review data), `upload-to-supabase-storage.js` (existed only to build LS task files)
- `rawjson_ยังไม่ได้แก้ไขโดนคน/README.md` — **op1 step 5-6 rewritten**: no more `label-studio-tasks-makham.js` run or upload reminder; the post-extraction gate is now `python tools/check_format.py`. Step 2 (Prepare data for Label Studio) struck through with a cancellation notice; workflow summary redrawn (extract → check_format → log); op2's `_stage0_manifest` note updated (the `_` convention stays, its LS origin noted as history)
- `training-data/CLAUDE.md` — prominent cancellation banner at top; the 3 LS review-flow sections below it are now explicitly historical
- `training-data/docs/rule_of_tune.md` — dated note on the Ground Truth section: ground truth is now solely `rawjson_ยังไม่ได้แก้ไขโดนคน/` + `check_format.py`; the annotated/ format kept as the historical record of what t01/t02 trained on (its type rules still apply to future dataset assembly)

**Impact/precautions:**
- `op1`/`op2` on house #12+ will no longer produce any Label Studio artifacts
- Nothing was deleted — `wait_for_ทิ้ง/` waits for Makham's explicit confirmation; git history holds everything regardless
- `training-data/` now contains only the live pipeline: extraction scripts, Prompt/, docs/, raw/, vast-template/

---

## Claude — session with Makham (night, round 7 — folder rename)

**Work done:**
- Renamed `raw_json_ตัวที่ใช้งานจริง/` → **`rawjson_ยังไม่ได้แก้ไขโดนคน/`** (Makham's order) via `git mv`, then updated every reference

**Files changed:**
- **20 `.md`** — both folder READMEs, `สิ่งที่ต้องแก้.md`, `training-data/CLAUDE.md`, `rule_of_tune.md`, the schema edit log, the data log, and every diary entry that referenced the old name
- **4 code files** — `tools/check_format.py` (default scan path), `tune_ai/t01`/`t02` `build_dataset.js`, t01 README
- **454 JSON files** — `grid_source`/`spec_source` fields carry full paths beginning with the folder name; pure prefix replace so cross-file references don't dangle (logged in the data log per Rule 3)

**Impact/precautions:**
- Old name remains only inside `wait_for_ทิ้ง/label_studio_stuff/` task JSONs — dead files awaiting deletion, deliberately not updated
- Verified after: zero live references to the old name, `check_format.py` runs on the new path with **ALL CHECKS PASS**, 2,366/2,366 JSON parse

---

## Claude — session with Makham (night, round 8 — Label Studio deleted for real)

**Work done:**
- Went through `wait_for_ทิ้ง/` per Makham's "judge what's unnecessary and just throw it out", deleted the Label Studio material outright, and **corrected two files I had misfiled in round 6**

**Deleted (51 files, 22 MB — recoverable from git history):**
- `label_studio_stuff/` entire folder — the task generator, both import scripts, the 3 project XMLs, and **21 MB of generated task JSON for 10 houses** (`element/`, `material_list/`, `single/` + the `-all.json` per house). All of it was derived from the raw JSON by script, so nothing original was lost even before counting git
- `label-studio-mindmap-บ้าน_เล็ก_1ชั้น_01.pdf` (1.2 MB review-flow diagram), `label-studio-config.xml`, `upload-to-supabase-storage.js`
- `annotated/` — held only `SAMPLE-annotated.json`, a June template of an *older* shape (`qwen_extraction` wrapper, `human_reviewer` field) that no longer matches anything; the format actually used for t01/t02 is written out inline in `rule_of_tune.md`, so deleting the file loses no record
- The `wait_for_ทิ้ง/` folder itself is gone

**⚠️ Correction to round 6 — two files were moved out on wrong grounds and are now back in `training-data/`:**
- `manifest.json` — I moved it because the Label Studio import script updates it, but its actual owners are `pdf-processor.py` and `qwen-processor.js`, which **read it without creating it** (`open(MANIFEST_FILE, "r")`). Deleting it would have broken both scripts the next time anyone ran them
- `review.html` — not Label Studio at all: it is the pre-LS local review UI, fetching `../manifest.json`, `../processing/`, `../qwen-output/`

**Also:** removed 9 now-dangling Label Studio rows from `CLAUDE.md`'s Key files table; updated its cancellation banner and `rule_of_tune.md` to say deleted rather than staged (incl. striking `annotated/*.json` from the not-protected list, since the folder no longer exists)

**Left pending / to continue:**
- **Worth a decision separately:** `manifest.json` + `review.html` + `pdf-processor.py` + `qwen-processor.js` are all one older generation, superseded by `run_pipeline.py` + `build_document_map.py` (CLAUDE.md's Architecture section already says to use the newer one). They were kept because they are not Label Studio and still reference each other coherently — but if that whole generation is dead too, it is the next thing to clear out

---

## Claude — session with Makham (night, round 9 — op3)

**Work done:**
- Added a third quick command, **`op3 <house_name>`** = `op1` that shuts the laptop down when the house is finished (Makham wants to start a house and walk away)

**Files changed:**
- `rawjson_ยังไม่ได้แก้ไขโดนคน/README.md` — new `op3` section placed after `op2`, before Step 1. It inherits the whole `op1` standing order unchanged; the only new content is the ending
- `primary_rawjson_schema_edit_log.md` — logged (README-only row, same as the `op2` entry), because `op3` binds to the `op1` standing-order section: anyone editing `op1` needs to know `op3` rides on it

**Impact/precautions:**
- **The substance of `op3` is the gate, not the shutdown.** Six conditions in order before the machine may go down: files written and parsing → `check_format.py` ALL PASS → row in `raw_json_data_log.md` → **`git commit`** → **summary printed to the user first** (the screen is about to go dark, so the report has to already be in the transcript) → only then `shutdown /s /t 120` with `shutdown /a` named as the cancel in the same message
- **`/t 0` is banned** and if any of steps 1-5 fails, **no shutdown** — report what is unfinished and stop. A house that failed `check_format.py` is not finished, and shutting down on it buries the failure until the next session
- Written with the 2026-07-21 Mark of Shame cited in the README itself: a machine went down while work only *looked* done and nothing had been saved, costing a 7.5 GB LoRA and a 21 GB GGUF. `op3` automates the ending, not the skipping of the save
- Also documented: don't use `op3` when anything else is running on the machine (training job, upload, another agent) — it only knows about its own house

---

## Claude — session with Makham (session close, 01:30 on 2026-08-03)

Session ran past midnight; rounds 1-9 above are one continuous session, kept in this one file rather than split at the clock.

**State at close:**
- Working tree clean, everything committed through `719c4a2`
- All 11 houses on one JSON format — `check_format.py` ALL CHECKS PASS, 2,366/2,366 files parse, working copy and raw folder differ in 0 files, 2,007 point `grid_ref`s all resolve, 0 dangling
- Format is locked in `primary_rawjson_schema.md` §0 and machine-enforced, so house #1000 comes out the same shape as house #1

**Left pending — all of it content, deliberately not started (Makham: "วันนี้เช็ครูปแบบเท่านั้น"):**
- **Beam plans in houses 06-11 have never been checked for missing beams** — highest value item left. Every one of 01-05 was missing real beams when reviewed (20→27, 22→36, 30→38, 5→21), so assume 06-11 are too until checked
- `specs{}` used by only 24 of 1,183 files; houses 07 and 11 have none
- Bars printed `หยุดที่ L/8` / mid-span in houses 10-11 not yet merged into a face — needs the §7 leader-line check against the drawing
- House 01 `หน้า60` series price table still raw text, flagged `PENDING_STRUCTURING_2026-08-02`
- Open question, unanswered: is the older generation (`manifest.json`, `review.html`, `pdf-processor.py`, `qwen-processor.js`) dead too, now that Label Studio is gone?

**Known exceptions — these are decisions, not leftovers, don't "fix" them:**
- 11 footing groups left unmerged (entries carry genuinely different per-position `confidence_score`/`note`/`confidence_flags`)
- `W1` vs `W1_variant` in house 10 — two real windows printed with the same mark

---
