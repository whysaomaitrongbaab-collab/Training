# 2026-09-27

## [Claude — session with makham] ปิดข้อค้างของ 26 ก.ย. ทุกข้อ: render 200 dpi · ไม่นับหน้าแบบขยายซ้ำ · pattern นอกคำศัพท์ · ธงแปลนหลายชั้น · sync run_this

**Commits (local ยังไม่ push)**
- Constistant: `a1968f5` render 200 dpi + ไม่นับหน้าแบบขยายซ้ำ + pattern รูปตัดนอกคำศัพท์
- Constistant: `4ce9239` ธงแปลนหลายชั้น
- Constistant: `96319cb` นิทาน OpenTakeoff
- Constistant: `0332e6b` แทน NUL ใน CLAUDE.md
- Training: `314d4df` sync run_this

**งานที่ทำ**
- **render 200 dpi** — `RENDER_SCALE` เปลี่ยนจาก 2.5 เป็น 200/72 ให้ตรงกับตอนเทรน · vector sidecar ใช้ scale เดียวกัน worker จึงไม่ต้องแก้ · **ยังไม่เคยรันกับการ์ดจริง**
- **หน้าแบบขยายซ้ำกับแปลน** — แถวจากหน้าแบบขยายที่ซ้ำกับแปลนจะไม่ถูกนับ ส่วนมาร์คที่มีเฉพาะบนหน้าแบบขยายยังนับและติดธงเหมือนเดิม · คอนกรีตคาน 83b8e52c ลดจาก 15.54 เป็น 11.85 m³ (เฉลย 10.46)
- **pattern นอกคำศัพท์** — ชื่อที่โมเดลตั้งเองอย่าง `detail` หรือ `footing_plan_and_detail` ถือเป็น section ใช้แค่ดึงสเปก (งานจริง 3/15 หน้ารูปตัด)
- **แปลนหลายชั้นในแผ่นเดียว** — ถ้าหน้าแปลนมีหลายชั้นแต่ผลอ่านใส่ชั้นเดียว จะติดธงทุกแถว · เจอที่หน้า 20 ของ 83b8e52c: คานชั้นบน 26 ช่วงหายเข้าชั้นล่างทั้งหมด · GT ติดธง 2/381 ไฟล์ และทั้งสองไฟล์เป็นแผ่นหลายชั้นจริง
- **ผลรวม** — GT 41 บ้านไม่เปลี่ยนแม้แต่บาทเดียว งานจริงอีก 6 งานก็ไม่เปลี่ยน · 83b8e52c รวม ฿141,374 (เฉลย ฿188,842) ส่วนที่ขาดติดธงครบแล้ว
- **งาน `448530d2`** (1 หน้า, 180 dpi, ค้างตั้งแต่ 25 ก.ย.) — พักเป็น failed พร้อมเหตุผล ไม่ให้แซงคิว · กด "ทำต่อ" ได้ถ้าต้องการรัน
- **`docs/research/opentakeoff/`** — นิทาน PDF ที่มะขามสั่งทำในอีกเซสชันแต่ยังไม่ได้ commit · commit เก็บให้แล้ว
- **`run_this/`** — sync worker ให้ตรงกับตัวที่รันจริงที่ `D:\purson` (เทสต์ผ่านครบในโฟลเดอร์นั้น)
- เขียนผลเทียบ Destrier กับเฉลย พร้อมโจทย์รอบทูนหน้า ไว้ที่ `tune_ai/t05_Destrier/proof/02_ไทยเป็นสุข3_เทียบเฉลย.md`

**ตัดสินแล้ว ✅**
- **RENDER_SCALE เลือกทาง (ก) แก้เลย** — ทาง (ข) ต้องเช่าการ์ด ซึ่งมะขามบอกไม่เอา · *ความเสี่ยง:* ภาพใหญ่ขึ้น ~23% งานจริงรอบแรกต้องดูใกล้ๆ ถอยกลับได้บรรทัดเดียว
- **D5 เปลี่ยนจากติดธงอย่างเดียวเป็นไม่นับแถวซ้ำ** — *ความเสี่ยง:* ถ้าแปลนจริงนับไม่ครบ ส่วนที่หน้าแบบขยายนับไว้จะหายไปด้วย (มีคำเตือนบอกว่าตัดอะไรทิ้ง)

**เทส:** npm 2200/2203 (3 ตัวที่ตกเป็นของเดิม) · test_vector_ruler 28/28 · test_pass3 33/33 · test_worker ผ่าน

**Left pending**
- โมเดล: เสาหายเพราะป้ายรวม "C1,F4" · มิติฐานรากไม่มีความหนา · หน้าเดียวหลายแปลนไม่แยกชั้น · หน้า 19 อ่านผิด — ต้องแก้ที่ข้อมูลเทรน (ดู proof/02)
- GT ข้อ 67 (ธรรมเนียมมิติฐานราก) — ต้องให้คนเป็นผู้แก้
- เฉลย 83b8e52c ยังไม่มีคนเซ็นรับรอง
- supabase MCP ต้องยืนยันตัวตนใหม่ · GitHub MCP ต่อไม่ติด

---

## [Claude — session with makham] แผน A: worker.py รันบนการ์ดเช่าเอง (ขั้นเขียนโค้ด) + ปิด merge ไดอารี่ 26

**Commits (local ยังไม่ push)**
- Constistant: `bc78198` แผน A (presentation.py + go.py + เทส)
- Training: `e43676b` ปิด merge ไดอารี่ 26 · commit นี้ sync `run_this` + ไดอารี่

**งานที่ทำ**
- มะขามเลือกแผน A แบบในไดอารี่ 25 (ไม่ใช่ "โหมด A" เช่าถาวร) · ทำเองบนคอมมะขาม ไม่ใช้เครื่องอิม · ทำแค่ขั้นเขียนโค้ด ส่วนทดสอบบนการ์ดจริงรอเน็ตเสถียรก่อน
- `presentation.py worker-up / worker-log / worker-down`
  - รวม worker + prompt (จาก `PURSON_PROMPTS_DIR` ชุดเดียวกับที่คอมใช้) + organize.py / cv_scan.py / แม่แบบ CV เป็น tar ก้อนเดียว 41 ไฟล์ 0.14 MB
  - วางโครงเลียน Training repo เพราะ worker หา CV จาก path สัมพัทธ์
  - config สร้างในหน่วยความจำ เปลี่ยนแค่ GPU URL เป็น localhost ของการ์ด กับ path ของ prompt
  - dependency ลงแยกที่ `/workspace/wdeps` (`--target`) ไม่แตะ `/venv/main` ที่ตัวเสิร์ฟใช้
- `go.py` เมนู 1/3 และเมนูซ่อม 3
  - เปิดตัวรับงานบนการ์ดก่อน ถ้าไม่สำเร็จจะสั่ง `worker-down` แล้วถอยไปเปิดหน้าต่างบนคอมแบบเดิม
  - ถ้ามีหน้าต่างรับงานบนคอมค้างอยู่ จะไม่เปิดตัวบนการ์ดซ้อน (สองตัวยิง Unsloth พร้อมกัน = ค้าง)
  - เพิ่มเมนูซ่อม 8 ไว้ดู log บนการ์ด
- ข้อความในเมนูเปลี่ยนเป็น "ถอดแบบด้วย Destrier" ให้ตรงกับปุ่มบนเว็บ (เดิมเขียน Purson)
- **ไดอารี่ 26 merge ชน** (เอนทรีเรากับของ Ark ต่อท้ายพร้อมกัน) — เก็บทั้งสองเอนทรีครบทุกบรรทัด ตรวจ diff ทั้งสองทางแล้วมีแต่บรรทัดที่เพิ่ม ไม่มีบรรทัดไหนถูกลบ
- `run_this/` sync 5 ไฟล์ที่แก้วันนี้ · `GO.bat` ต่างกันแค่ชนิดการขึ้นบรรทัด เนื้อหาเหมือนกัน เลยปล่อยไว้

**ตัดสินแล้ว ✅**
- **ลง dependency ของ worker ตอน `worker-up` ไม่ใช่ตอน onstart** — ไม่แตะเส้นทางเปิดโมเดลที่พิสูจน์แล้ว · *ความเสี่ยง:* เปิดครั้งแรกช้าขึ้น ~1-2 นาที
- **ส่ง config ที่มี service key ขึ้นการ์ด** (chmod 600, ดิสก์ถูกลบตอน destroy) · *ความเสี่ยง:* ระหว่างเช่า โฮสต์เข้าถึงดิสก์ได้ในทางเทคนิค
- **ใช้ตัวรับงานบนการ์ดเป็นค่าเริ่มต้น ถอยไปบนคอมเองเมื่อพัง** · *ความเสี่ยง:* ยังไม่เคยรันบนการ์ดจริง

**เทส:** `test_worker_remote.py` ใหม่ (แตก bundle แล้ว import worker จากในนั้นจริง, ตกกับโค้ดเดิม) · `test_serve_presets` ข้อ 9 เพิ่ม 2 เคส (ถอยกลับ / ไม่เปิดซ้อน) · เทสในโฟลเดอร์ worker ผ่าน 8/8 · ตัวเช็คหน้าต่างบนคอมจับ process จริงได้

**Left pending**
- ขั้น 3 — ทดสอบบนการ์ดจริง 1 รอบ: ดู `worker-log`, ยิงงานจริง 1 งาน, ต้องไม่ขึ้น NOCV (CV ต้องทำงาน)
- ตอนเปิดการ์ด (30-45 นาที) ยังพึ่งเน็ตคอม ถ้า tunnel ขาดนาน `wait_healthy` อาจตีว่าเครื่องดีเป็นเครื่องเสีย (ปัญหาเดิม ไม่ใช่ของแผน A)
- แผน A ไม่มีเสียงเตือนตอนงานเสร็จ
- จำกัดคนใช้ (allowlist/quota) ก่อนบอกใครว่าใช้ได้ — ยังค้างจากไดอารี่ 25
