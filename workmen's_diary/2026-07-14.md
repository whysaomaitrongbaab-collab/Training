## [ต่อเนื่องทั้งวัน] มะขาม (ทำงานร่วมกับ Claude Code — รีวิว/แก้ไขบ้านหลังที่ 1 ต่อจากที่เริ่มไว้ 2026-07-13, ดู `2026-07-13.md`)

**บริบท:** ต่อเนื่องจากเมื่อวาน มะขามสร้าง `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/` (สำเนาสำหรับรีวิว/แก้ไข ไม่แตะ `raw_json_ตัวที่ใช้งานจริง/` ต้นฉบับ) แล้วไล่ตรวจ grid master + ไฟล์ที่เกี่ยวข้องทีละจุดต่อเนื่องข้ามวัน

**งานที่ทำ (เรียงตามลำดับ):**

### 1. เพิ่ม dummy grid ที่พลาดไปก่อนหน้า
- **`D'` = 2.8** (แกน y, ระหว่างกริด D-C) — ผนังกั้นเต็มความกว้างระหว่างแถวห้องนอนกับแถวห้องด้านหลัง (หน้า06) — cascade แก้ `หน้า06_floor_plan.json` (นอน1/นอน2 เปลี่ยนจาก `D-C` เป็น `D-D'`) แต่**จับผิดตัวเองได้ก่อนส่ง**: เกือบแก้ ห้องน้ำ_WC1 เป็น `D'-C` ตามการเดาต่อเนื่อง แต่เช็ค `หน้า11_wc1_plan.json` (แบบขยายเฉพาะ) ก่อนแล้วพบว่าระบุตรงๆ ว่าอยู่ `between gridB and gridC` — กลับไปใช้ `C-B` เดิม ไม่ใช้ค่าที่เดาไว้ (บทเรียน: เช็ค detail sheet ก่อน cascade เสมอ)
- **`1''` = 3.45** (แกน x, ช่องเดียวกับ `1'`) — มะขามสั่งเพิ่มตรงๆ ยังไม่ได้ pinpoint เส้น dimension ที่แน่ชัด 100% ด้วยตัวเอง ยังไม่ cascade ไปหน้าอื่นเพราะไม่มีหลักฐานชัด (รอคำสั่ง/หลักฐานเพิ่ม)
- ทั้งสองใส่ `confidence_score: 1` ตาม convention (ค่าที่มะขามยืนยันเอง)

### 2. Convention ใหม่: แกนตั้ง (row) มาก่อนแกนนอน (column) ใน `grid_ref` แบบ range
พบว่าตอนย่อ `grid_ref` ของหน้า06 รอบแรกเขียนแกนนอนก่อนโดยไม่ตั้งใจ (`"1-1' x D-C"`) มะขามสั่งกลับเป็นแกนตั้งก่อน (`"D-C x 1-1'"`) — ตรงกับ convention เดิมของ `"A-1"` (row ก่อน column) อยู่แล้ว เพิ่มเป็นกฎลายลักษณ์อักษรใน `primary_rawjson_schema.md` section 4 ด้วย (อ้างอิง `rule_of_tune.md` กฎข้อ 7 ก่อนแก้)

### 3. Convention ใหม่: point-type `grid_ref` ไม่ใช้ขีด (dash) เก็บขีดไว้แค่ line/area range
มะขามชี้ว่า footing/column (`grid_refs: ["C-2"]`) เป็นแค่ "จุด" ไม่ใช่ "เส้น/พื้นที่" ที่ต้องมีขีดคั่น — สั่งเปลี่ยนเป็น `"C2"` แล้วขยายไปถึงคาน (`grid_ref_start`/`grid_ref_end`) ด้วย เพราะแต่ละ field ก็เป็นจุดปลายคานจุดเดียว ไม่ใช่ตัวช่วงเอง (ช่วง/เส้นคานแสดงด้วยการมี 2 field แยกอยู่แล้ว) — แก้ `หน้า19_view1_footing_plan.json` และ `หน้า19_view2_beam_plan.json` ครบ (ใช้ regex script ตัดขีดระหว่างแถวตัวอักษร-คอลัมน์ตัวเลขในทุก grid_ref_start/end แล้ว verify ด้วย grep) **ยังไม่ได้แก้ `primary_rawjson_schema.md`** เพราะ section 4 ปัจจุบันเขียนตรงข้าม (บอกว่า point-type ใช้ `"A-1"` มีขีด) — ถามมะขามว่าจะเปลี่ยนทั้งโปรเจกต์หรือแค่บ้าน 1 ก่อน ยังไม่ได้คำตอบชัดตอนเขียน entry นี้

### 4. ลบ element ที่ไม่เกี่ยวข้อง + ลบ grid ที่ซ้ำซ้อน
- ลบ `doors_and_windows_summary` element ออกจาก `หน้า06_floor_plan.json` ตามคำสั่ง
- พบว่า `หน้า19_view1_footing_plan.json` มีทั้ง `grid_source` (ชี้ไป grid master ถูกต้อง) **และ** inline `"grid": {...}` ซ้ำซ้อน (ขัดกับสเปคเองที่บอกว่า "never re-embed the full grid inline inside every view") — มะขามจับได้ ลบ inline grid ออก เช็คไฟล์อื่นในบ้านแล้วไม่มีที่ไหนซ้ำแบบนี้อีก

### 5. บันทึกบทเรียน "อ้างกฎแล้วไม่หยุดถามจริง" ลง `rule_of_tune.md` (กฎข้อ 9 ใหม่)
มะขามจับได้ว่า Claude พูดอ้าง "ตาม rule_of_tune.md กฎข้อ 7" ก่อนแก้ `primary_rawjson_schema.md` (เพิ่ม section 10a เรื่องบันได) แต่พูดจบแล้วรีบลงมือแก้ต่อในข้อความเดียวกันเลย ไม่ได้หยุดรอ yes/no จริง — ถามตรงๆ ว่า "ทำไมแจ้ง rule of tune แล้วไม่กดขออนุญาติ yes/no วะ" สั่งให้เขียนเป็นกฎข้อ 9 ไว้ในกฎ "พฤติกรรมที่ควรเลี่ยง" ว่าอ้างกฎเมื่อไหร่ต้องหยุดถามจริง ห้ามอ้างแล้วแก้ต่อในข้อความเดียวกัน

### 6. เช็คว่าข้อมูล architecture-level (finishing materials, door/window schedule) ที่ถอดมามีคนใช้ในแอป Constistant ไหม
เช็ค 2 รอบ (รายการวัสดุ พื้น/ผนัง/ฝ้า/หลังคา แบบมีโค้ด item_no, และตารางประตู-หน้าต่าง D1-D3/W1-W4) — **ทั้งคู่ไม่มีใครใช้ในแอปเลย** ไม่มี schema/pricing/BOQ engine รองรับ concept นี้เลยตอนนี้ (เช็คผ่าน Explore agent ทั้ง 2 รอบ อ้างอิง file:line ครบ)

**ไฟล์ที่เปลี่ยนวันนี้:**
- `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า00_gridline.json` — เพิ่ม D', 1''
- `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า06_floor_plan.json` — cascade D', ลบ doors_and_windows_summary, ย่อ grid_ref (ผู้ใช้แก้เพิ่มเองบางส่วนด้วย)
- `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า06_roof_plan.json` — แก้ format grid_ref
- `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า11_wc1_plan.json` — แก้ format grid_ref
- `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า19_view1_footing_plan.json` — ลบ inline grid ซ้ำ, ตัดขีด point-ref
- `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า19_view2_beam_plan.json` — ตัดขีด point-ref ทุกคาน
- `json_แก้ไขแล้ว/สิ่งที่ต้องแก้.md` — เพิ่ม log ละเอียดข้อ 6-8
- `json_แก้ไขแล้ว/README.md` — log สั้น
- `raw_json_ตัวที่ใช้งานจริง/00file_for_making_rawjson_from_claude/primary_rawjson_schema.md` — เพิ่ม axis-order rule (section 4), section 10a (บันได)
- `raw_json_ตัวที่ใช้งานจริง/primary_rawjson_schema_edit_log.md` — ไฟล์ log ใหม่สำหรับการแก้สเปคเอง (ยังไม่เคยมีมาก่อน)
- `training-data/docs/rule_of_tune.md` — เพิ่มกฎข้อ 7 (อ้างอิงก่อนแก้ primary_rawjson_schema.md เสมอ + ผูก log ไฟล์ใหม่), กฎข้อ 8 (อย่าสร้าง memory ซ้ำ), กฎข้อ 9 (อ้างกฎต้องหยุดถามจริง)

**ผลกระทบ/ข้อควรระวัง:**
- **workflow ใหม่ที่ตั้งขึ้นวันนี้**: ทุกครั้งที่แก้ `primary_rawjson_schema.md` ต้อง (1) อ้างอิง `rule_of_tune.md` กฎข้อ 7 (2) **หยุดถาม yes/no จริง** ตามกฎข้อ 9 (3) log ลง `primary_rawjson_schema_edit_log.md` ตัวใหม่ — 3 ขั้นตอนนี้เพิ่งถูกบังคับใช้จริงวันนี้หลังพลาดไป 2 ครั้ง
- ยังมีคำถามค้าง (ยังไม่ตอบ): convention point-ref ไม่มีขีด จะใช้ทั้งโปรเจกต์หรือแค่บ้าน 1?
- ยังไม่ได้ไล่เช็คไฟล์อื่นในบ้าน 1 ที่อาจมี point-type grid_ref แบบมีขีดเหลืออยู่ (เช่น หน้า20_view1_tie_beam_plan.json)

### 7. `หน้า19_view2_beam_plan.json` — เรียงลำดับ elements[] ใหม่ตามการอ่านแบบ + แยก spec ซ้ำออกเป็น `specs{}`
มะขามสั่งให้เรียง element ในไฟล์ตามลำดับการอ่านจริง (บนลงล่าง, ซ้ายไปขวา, แนวนอนก่อนแนวตั้งถ้าจุดเริ่มตรงกัน) แทนการกลุ่มตาม element_id/mark แบบเดิม — เขียนใหม่ทั้ง 20 elements ตามลำดับตำแหน่งจริง (verify ครบไม่มีข้อมูลหาย) แล้วเพิ่มเป็นกฎทั่วไปใน `primary_rawjson_schema.md` section 4 ("Element ordering within elements[]") — มะขามยืนยัน "yes" ให้เพิ่ม (คราวนี้หยุดถามจริงก่อนแก้ตามกฎข้อ 9 ที่เพิ่งตั้ง)

จากนั้นมะขามชี้ต่อว่าทุก occurrence ของคานมาร์คเดียวกันพิมพ์ spec (`width_mm/height_mm/main_bar/stirrup/additional_bars/concrete_grade/steel_grade/spec_source/spec_confidence_score`) ซ้ำเหมือนกันทุกตัวอักษร กลายเป็น "god object" — สั่งแยกออกมาเป็น object ใหม่ `specs{}` keyed by `element_id` (ตรวจสอบก่อนว่าทุก occurrence ของแต่ละมาร์คมี spec เหมือนกันเป๊ะจริง: B2 x2, B4 x3, B5 x3, B3 x3, B4X x2 ยืนยันแล้ว) `elements[]` เหลือแค่ตำแหน่ง+confidence เฉพาะจุด — **ถามมะขามต่อว่าจะเพิ่ม pattern `specs{}` นี้ใน `primary_rawjson_schema.md` ด้วยไหม ยังไม่ได้คำตอบ** มะขามบอกเหนื่อยแล้วหยุดตรงนี้

**ไฟล์ที่เปลี่ยนเพิ่มรอบสุดท้ายวันนี้:**
- `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/บ้าน_เล็ก_1ชั้น_01_หน้า19_view2_beam_plan.json` — เรียงลำดับใหม่ + แยก `specs{}`
- `raw_json_ตัวที่ใช้งานจริง/00file_for_making_rawjson_from_claude/primary_rawjson_schema.md` — เพิ่ม "Element ordering within elements[]" ใน section 4
- `raw_json_ตัวที่ใช้งานจริง/primary_rawjson_schema_edit_log.md` — log การแก้นี้
- `json_แก้ไขแล้ว/สิ่งที่ต้องแก้.md` — เพิ่ม log ข้อ 9 ละเอียด

**⚠️ สำคัญที่สุดตอนหยุดงานวันนี้:** ทุกอย่างที่แก้ในบ้าน 1 วันนี้ (D'/1'' dummy grid, point-ref ไม่มีขีด, เรียงลำดับ elements, แยก specs) **อยู่ใน `json_แก้ไขแล้ว/` เท่านั้น** — `raw_json_ตัวที่ใช้งานจริง/01บ้าน_เล็ก_1ชั้น_01/` (ต้นฉบับจริงที่ใช้ทูน) **ยังไม่ถูกแตะเลยแม้แต่ไฟล์เดียว** วันนี้ ยังเป็นเวอร์ชันเดิมทั้งหมด (มีขีด, เรียงตาม element_id, spec ซ้ำทุกจุด) — งาน sync กลับไปทับต้นฉบับยังไม่เริ่ม ต้องทำตามกฎข้อ 1-3 ของ `rule_of_tune.md` เต็มรูปแบบเมื่อถึงเวลานั้น

**ค้างไว้ / ทำต่อ (จุดที่หยุดวันนี้ — มะขามเหนื่อยแล้ว):**
1. รอคำตอบมะขามเรื่อง scope ของ convention "point-ref ไม่มีขีด" (ทั้งโปรเจกต์ vs บ้าน 1) แล้วค่อยแก้ `primary_rawjson_schema.md` section 4 ตาม (ต้องหยุดถาม yes/no จริงก่อนแก้)
2. รอคำตอบมะขามเรื่องจะเพิ่ม pattern `specs{}` (แยก spec ซ้ำ) เข้า `primary_rawjson_schema.md` เป็น convention ทั่วไปไหม
3. ไล่เช็ค/แก้ไฟล์ที่เหลือในบ้าน 1 ให้ตรง convention ใหม่ทั้งหมด (point-ref ไม่มีขีด, แกนตั้งก่อน, element ordering, specs{} แยก) — ยังทำไม่ครบทั้งบ้าน (เช็คแล้วเฉพาะ หน้า00/06/06_roof/11/19x2 — ยังเหลืออีกหลายสิบไฟล์)
4. **งานใหญ่ที่ยังไม่เริ่มเลย**: sync ผลการแก้ทั้งหมดใน `json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/` กลับไปทับ `raw_json_ตัวที่ใช้งานจริง/01บ้าน_เล็ก_1ชั้น_01/` ต้นฉบับ (ตามกฎข้อ 1-3 rule_of_tune.md เต็มรูปแบบ) — ต้องรอให้รีวิวบ้าน 1 เสร็จสมบูรณ์ก่อนหรือมะขามสั่งให้ sync บางส่วนก่อนก็ได้ ยังไม่ตกลงกัน
5. งานค้างเดิมจาก entry ก่อนๆ ที่ยังไม่แตะ: บ้านหลังที่ 6 ยังไม่เริ่ม, import task เข้า Label Studio, ตัดสินใจเรื่อง roof_plan pattern/label-studio script columns-rows

---

## [ช่วงดึก, ต่อเนื่อง] มะขาม — รีวิวต่อ หน้า19-22, 60-61 จบก่อนขึ้นบ้านหลังที่ 6

**บริบท:** ต่อจาก "หยุดตรงนี้" ด้านบน มะขามพักแป๊บแล้วกลับมาทำต่อ ไล่รีวิว หน้า19 (แก้ต่อ), หน้า20, หน้า21, หน้า22, หน้า60-61 จนจบ ก่อนจะย้ายไปเริ่มบ้านหลังถัดไป (หลังที่ 6)

### 8. Element ordering แก้กลับทิศทาง: **แนวตั้งมาก่อนแนวนอน** (ที่เขียนไว้ตอนแรกผิด)
มะขามชี้ว่ากฎ element ordering ที่เพิ่งตั้ง (ข้อ 7) เขียนทิศผิด — ที่ถูกคือแนวตั้งมาก่อน (เช่น D1: `B4 D1-C1` ก่อน `B2 D1-D2`) เรียง `หน้า19_view2_beam_plan.json` ใหม่ทั้งไฟล์ตามนี้ (validate 20 elements ครบ) แก้ `primary_rawjson_schema.md` กลับทิศตามแล้ว (ยืนยัน "y" ก่อนแก้) ระหว่างทางเจอ+แก้ B2 ที่พิมพ์ผิดเป็น `D'2/D'3` กลับเป็น `D2/D3` ด้วย

### 9. เพิ่มคานที่หายไปอีก 7 เส้น + แก้ B4X 3'/3'' กลับให้ถูก
มะขามส่งภาพเต็มหน้า19 (ชัดกว่าเดิม) ชี้คานที่หายไป 7 เส้น: `B2 D'2-D'3` (ยืนยันว่า D' เป็นคานจริงฝั่งขวาด้วย ไม่ใช่แค่ผนัง), `B4 B1-B2`/`B4 B2-B3` (คานทับหลัง row B คู่กับ row C), `B2 B1''-A1''` (แก้ปริศนาจุดที่หาไม่เจอเมื่อก่อน), `B2 C3'-B3'`, `B3 A1''-A3`/`B3 A'1''-A'3` (คานทับหลัง row A/A') — เพิ่มครบ confidence 1 ทุกตัว รวม 27 elements

พบ B4X สองช่วงถูกแก้ label เพี้ยนกลับเป็น `3'` (span ไม่ตรงกันเอง 0.6 vs 1.5) ทั้งที่ grid master บันทึกไว้แล้วว่าทั้งคู่ควรเป็น `3''` (1.5 ทั้งคู่) — ยืนยันจาก grid master (3-3'=0.6 จริง) แล้วแก้กลับ

### 10. Sync หน้า20 + หน้า21 ให้ตรงกับหน้า19 (mark/spec เดียวกัน)
- `หน้า20_view1_tie_beam_plan.json` (B2×15, B6×2): ตัดขีด, เรียงลำดับใหม่, แยก `specs{}`, รวม additional_bars B2 เข้า bottom (2→3) เหมือนกัน
- `หน้า20_view2_roof_frame_plan.json`: ตัดขีดใน grid_refs อย่างเดียว
- `หน้า21_view1_section.json` (ต้นทาง spec ทุกคาน): sync การ merge ทั้งหมดจากหน้า19 — พบ 1 จุดขัดแย้งจริง (B4 อ่านจากภาพนี้ได้ additional bar 1 เส้น แต่หน้า19 มี 2) ใส่ flag ให้คนตรวจ ไม่เลือกฝ่ายใดเงียบๆ
- `หน้า21_view2_schedule.json` (ตารางเสา C1): **มะขามจับได้ปัญหาคนละแบบ** — เสาใช้โครงสร้าง `main_bar.top/bottom` แบบคาน (top4+bottom4=รวม 8 ผิด) ทั้งที่เสาไม่มี "หน้าบน-ล่าง" มีแค่เหล็กรอบเสา 4 มุมเส้นเดียว (4-Ø12มม.) — แก้เป็น `main_bar: {count:4}` เดี่ยว ยังไม่ถามว่าจะเพิ่มเป็นกฎทั่วไปไหม

### 11. หน้า22 — ปฏิเสธคำแนะนำผิดจาก AI ภายนอก
มะขามส่งคำวิเคราะห์จาก AI ตัวอื่นที่เสนอให้แก้ F2 main bar จาก RB/SR24 เป็น DB/SD30 (อ้างว่า 12mm มักเป็น DB) — **Claude เช็คภาพต้นฉบับเองก่อนแล้วปฏิเสธ** เพราะสัญลักษณ์ที่พิมพ์จริง (Ø) เหมือนกันทั้ง F1(Ø9)/F2(Ø12) ต่างแค่ขนาด ตรงกับกฎ "Ø เสมอ=RB ห้ามเดาจากขนาด" ที่มีอยู่แล้ว ไม่แก้ไฟล์เพราะข้อมูลเดิมถูกอยู่แล้ว — **บทเรียน: AI ภายนอกให้คำแนะนำผิดได้ ต้องเช็คภาพเองก่อนเชื่อทุกครั้ง**

### 12. หน้า60-61 — เพิ่ม pattern ใหม่ `misc` (เบ็ดเตล็ด)
มะขามสั่งเปลี่ยน pattern จาก `title` เป็นคำอังกฤษของ "เบ็ดเตล็ด" สำหรับ 2 หน้านี้ (หน้า60=ตารางเปรียบเทียบราคา 10 แบบบ้าน, หน้า61=ปกคอลลาจรวมทุกแบบ) เพราะเป็นเนื้อหาโปรโมทรวมทั้งซีรีส์ ไม่ใช่ปก/ข้อมูลของบ้านหลังนี้เอง — เพิ่ม pattern ที่ 14 `misc` ใน `primary_rawjson_schema.md` (taxonomy จาก 13→14 ประเภท) ตามคำสั่งตรงๆ ในข้อความเดียว (ไม่ต้องถามซ้ำเพราะชัดเจนแล้ว)

**ไฟล์ที่เปลี่ยนรอบนี้:** `หน้า19_view2_beam_plan.json`, `หน้า20_view1_tie_beam_plan.json`, `หน้า20_view2_roof_frame_plan.json`, `หน้า21_view1_section.json`, `หน้า21_view2_schedule.json`, `หน้า60.json`, `หน้า61.json`, `primary_rawjson_schema.md` (×3 จุด), `primary_rawjson_schema_edit_log.md`, `สิ่งที่ต้องแก้.md` (ข้อ 14-17)

**สรุปก่อนขึ้นบ้านหลังที่ 6 — convention ที่ยืนยันแล้วและควรใช้กับบ้านถัดไปตั้งแต่แรก:**
1. Point-ref ไม่มีขีด (`C2` ไม่ใช่ `C-2`) — ขีดสงวนไว้แค่ line/area range (`D-C`)
2. แกนตั้ง (row) มาก่อนแกนนอน (column) เสมอในการเขียน range
3. `elements[]` เรียงตามตำแหน่งจริง: บนลงล่าง, ซ้ายไปขวา, **แนวตั้งก่อนแนวนอน**เมื่อจุดเริ่มตรงกัน
4. Spec ที่ซ้ำกันทุก occurrence ของมาร์คเดียวกัน → แยกเป็น `specs{}` keyed by element_id ไม่พิมพ์ซ้ำใน elements[]
5. `additional_bars[].position` ต้องเช็คกับ leader line จริงเสมอ ห้ามเชื่อคำที่พิมพ์ไว้ — merge เข้า main_bar (บนหรือล่าง แล้วแต่หลักฐาน) ถ้าเป็นเหล็กเสริม/ล้วงเข้าคานเดียวกันหรือคานข้างเคียง
6. เสา (column) ไม่ใช้ `main_bar.top/bottom` แบบคาน — ใช้ `main_bar.count` เดี่ยว (ยังไม่ได้เพิ่มเข้า schema อย่างเป็นทางการ)
7. Pattern `misc` (เบ็ดเตล็ด) สำหรับหน้าโปรโมท/อ้างอิงรวมทั้งซีรีส์ (ไม่ใช่ของบ้านหลังนี้เอง) แยกจาก `title` (ปกของบ้านหลังนี้)
8. ทุกครั้งที่แก้ `primary_rawjson_schema.md` ต้องอ้างอิง `rule_of_tune.md` กฎข้อ 7, หยุดถาม yes/no จริงตามกฎข้อ 9-10 (ห้ามเดาคำตอบจากข้อความสั้น/กำกวม), แล้ว log ใน `primary_rawjson_schema_edit_log.md` เสมอ

**⚠️ ยังไม่ตอบ/ยังไม่ทำก่อนขึ้นบ้าน 6:**
- Scope ของ point-ref convention (ทั้งโปรเจกต์ vs บ้าน 1 อย่างเดียว) — ยังไม่ยืนยันชัด
- เพิ่มกฎเสาไม่ใช้ top/bottom เข้า schema อย่างเป็นทางการไหม
- ไฟล์ที่เหลือในบ้าน 1 (นอกเหนือจาก หน้า00/05/06/06_roof/11/19×2/20×2/21×2/22/60/61) ยังไม่ได้ไล่แก้ตาม convention ใหม่ทั้งหมด
- Sync กลับ `raw_json_ตัวที่ใช้งานจริง/01บ้าน_เล็ก_1ชั้น_01/` ยังไม่เริ่มเลย

---
