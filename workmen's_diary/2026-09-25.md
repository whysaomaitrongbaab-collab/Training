# 2026-09-25

## [Claude — session with Mail (Mel)] Committed + pushed yesterday's 3D structure-model fixes; explained what `npm test` actually verifies

**Context:** Continuation of the multi-day `pipeline-optimize` work logged throughout `2026-09-24.md`. Two small, distinct asks today: (1) commit and push the pending `structure-model.js` edge-case fixes that were still sitting uncommitted in the working tree from yesterday's session, and (2) after seeing "npm test: 1983/1986 passing" quoted before every push, Mail asked directly what that test count actually means — worth recording since it's a recurring point of confusion and the answer matters for how much trust to put in every "tested" claim in this diary.

**Work done:**
1. **Committed and pushed the structure-model.js fixes from the previous entry.** Before committing, found the `pipeline-optimize` branch had already picked up several merge commits from `origin/Presentation-demo` (purson-worker sync commits, `9afda0b`/`c591b6a`) that weren't part of this session's own work — branch was already `up to date with origin/pipeline-optimize` before touching anything, so these were pre-existing on the remote, not something this session introduced. Staged only the three files actually changed this session (`schema.js`, `structure-model.js`, `tests/structure-model.test.js`), re-ran the full suite once more on top of the current tree to confirm nothing regressed (1983/1986, same as before — the merge commits didn't touch anything this session's fix depends on), then committed with the full reasoning inline (z-fighting fix, multi-storey ground-slab fix, `elevation_offset_m`, and a summary of the two external-review triages) and pushed straight to `origin/pipeline-optimize` (`88e80b8`) after confirming no new commits had landed upstream in the meantime.
2. **Explained what the `npm test` number actually means**, since "X/Y passing" had been quoted after nearly every change today and Mail wanted to know what it was actually proving. The honest answer, recorded here because it's a recurring caveat worth having written down once properly rather than repeated informally each time: `npm test` runs the repo's Node-based unit test suite (~1986 cases across ~12 files under `tests/`) — each case feeds small, hand-constructed sample data into a pure function (e.g. `buildStructureModel()`, `computeBOQ()`) and asserts the output matches a value that was hand-calculated before the test was written. This verifies the **math/logic layer only**: no browser, no WebGL, no real project data, nothing rendered on a real screen. A green test suite means "the formulas compute what they're supposed to for the sample cases checked" — it does not mean "opened the real app and it looks right," which is the separate, still-unresolved verification gap this whole thread of work has been flagged against (see the "waiting on a live web deployment" note in the 2026-09-24 entry above). The 3/1986 failures are the same pre-existing, unrelated `raw-extraction-golden.test.js` fixture-path issue documented in every prior entry.

**Files changed:**
- `Constistant/js/shared/schema.js`, `Constistant/js/shared/structure-model.js`, `Constistant/tests/structure-model.test.js` — committed as `88e80b8` on `pipeline-optimize` (full technical description already in that commit message and in the 2026-09-24 diary entry above; not repeated here)
- This diary file (new)

**Impact/precautions:**
- `pipeline-optimize` is now fully synced with `origin/pipeline-optimize` (nothing local left uncommitted) — anyone pulling it gets the complete 3D-model fix set from yesterday plus everything already merged in from `Presentation-demo`.
- Worth remembering for future entries in this diary: "tests passing" should keep being read as "verified at the formula/logic level," not as "verified live" — the two are being kept explicitly separate in this project's own convention (see CLAUDE.md's own repeated "ยังไม่ได้ verify สดในเบราว์เซอร์" notes across its history), and today's conversation reaffirmed why that distinction matters in practice.

**Left pending / to continue:**
- Still waiting on a real, seriously-connected live web deployment (Makham's dependency, per yesterday's entry) before any of this session's or yesterday's work can be checked beyond the unit-test level.
- Merging `pipeline-optimize` with Ton's and the teammate's parallel work is still explicitly waiting on Mail's go-ahead, unchanged from yesterday.

---

## [session, exact clock time not logged] Tonton (Claude Code session)

**Work done:**
- Synced `pipeline-optimize` with the rest of the team's work across two rounds today, both requested by Ton ("sync change ล่าสุด"). Ended up needing real merge-conflict resolution, not a clean fast-forward, because of concurrent work on the same rebar-shape logic — documented in detail below since it's a repeat of the exact duplicate-work risk this diary and Ton's standing rule exist to catch.
- **Round 1 (small):** pulled 2 new commits from `origin/Presentation-demo` (makham) — a BIM/IFC export subsystem (`building-model.js`, `ifc-element-map.js`, expanded `ifc-export.js`) and an unrelated but real bug fix: `computeSchedule()` used to crash with an unreadable `RangeError: Invalid time value` when `project.start_date` was empty/invalid; now throws a Thai message telling the user to go fill in Input Hub's "ข้อมูลทั่วไป" instead. Clean auto-merge, `npm test` 1971/1974 (3 pre-existing unrelated failures), pushed.
- **Round 2 (small):** pulled 24 more commits, almost entirely purson-worker/vast.ai GPU-rental orchestration (ssh-hang guards, price ceiling, blacklist for dead rental boxes, token-limit recovery, etc. — makham). One item worth flagging for the BOQ-accuracy thread this branch cares about: `raw-extraction-adapter.js` gained a cross-file double-count detector — Purson's worker sometimes reads the same drawing sheet twice via two separate sub-prompts (`plan_footing`/`plan_beam`) and the model reports the same mark's count back both times; the adapter used to silently sum them (real incident 2026-09-23, job `cbb90abe`: a footing's count went 4+8=12, inflating that floor's concrete/rebar/formwork ~3x). Fixed as a warning surfaced on the raw-extraction review checklist (which also gained visible count/span columns it didn't have before) rather than a silent auto-resolution, since the adapter can't know which read (4 or 8) was correct. Clean auto-merge, 1977/1980, pushed.
- **Round 3 (the real one) — Ton asked to sync again and this fetch pulled in a much bigger, riskier set of changes:**
  - Mail had pushed **4 commits directly to `pipeline-optimize`** itself (not just `Presentation-demo`) — `f1534f3` "rebar cut-length hooks for ground/roof columns + wire dense stirrup zones", `cd5eb0a` "revive real 3D model, replace decorative showcase pipeline", plus a merge and `88e80b8` "stop beam/slab z-fighting, fix multi-storey ground slab placement". Fast-forwarded cleanly.
  - `origin/Presentation-demo` had **16 more commits**, including a large **repo-wide reorganization by King** (`f057a8e`, "chore: reorganize project structure and update local components") that moved almost every top-level file/folder — `missing.md` → `docs/raw-json-gaps.md`, `finetune_output_contract/` → `data/finetune-output-contract/`, CSS files, `README.md`, `contistant.html`, and a brand-new `Deals/` folder (pitch/investor materials, unrelated to app code) — plus more of makham's purson-worker work and two `ark`/makham commits titled literally "แก้bbs" / "แก้bbsของอันแรกจามเสี่ยเมล" (fixing BBS, per Mail).
- **This produced 16 real conflict hunks across 4 files** (`js/shared/pipeline.js` — 9, `js/shared/bbs-shape-rules.js` — 3, `js/boq/boq-summary.js` — 2, `tests/bbs-shape-rules.test.js` — 4). Root cause, confirmed by reading both sides: **Mail independently fixed the exact same bug (dense stirrup zones + ground/roof-column rebar hooks) on two different branches on the same day (2026-09-24)** — once via the direct push to `pipeline-optimize` (`f1534f3`), once via `Presentation-demo` — with different variable names and, in Presentation-demo's version, extra validation (a `tieCountConflictNote` when a fixed tie-count contradicts the stated spacing, and a `denseZoneNote` explaining the computed zones). Mail had even left a warning doc for exactly this (`bbs-pipeline-fixes-2026-09-24.md`, committed to `pipeline-optimize`): *"ถ้า branch ของเพื่อนแก้ฟังก์ชันพวกนี้เหมือนกัน... มีโอกาสชนกันตอน merge สูง — ควรดูของฝั่งนี้ก่อนเริ่มแก้ซ้ำ"* — the warning fired exactly as written, just after the fact rather than before.
  - **Stopped and asked Ton before touching any of the 16 hunks**, per his own standing rule to flag duplicate work with Mail. Ton's answer: "ขอ sync ให้เท่า ข้อมูลที่ mail มีพอ" (just sync to match what Mail has).
  - First attempt: resolved all 4 conflicted files with a blanket `git checkout --theirs <file>`. **This was a real mistake, caught immediately by running the test suite, not by review**: `checkout --theirs` on a conflicted file replaces the *entire* file with the incoming side, not just the conflicting hunks — it silently deleted Item 5's `resolveEffectiveSpec()` gate from `computeBOQ()` (committed earlier this week as `72da02f`) even though that code had nothing to do with the conflict and had already auto-merged correctly before being clobbered.
  - Aborted (`git merge --abort`, safe — nothing had been pushed) and redid it properly: resolved `js/shared/bbs-shape-rules.js` + its test with `--theirs` (confirmed via `git show 72da02f --stat` that Item 5 never touched these files, so no risk there); resolved `js/boq/boq-summary.js` and `js/shared/pipeline.js` by hand, per-hunk, combining both sides' additions rather than picking one — a second, cruder regex-based per-hunk resolution attempt on `pipeline.js` was tried first and **also failed** (a hunk boundary happened to sit in the middle of a larger, non-conflicting block — `computeBBS()`'s per-piece loop opening, the `resolveEffectiveSpec()` call, `if (!lib) continue`, and several `const` declarations — so taking "theirs" for just that hunk deleted the loop's opening brace while its matching closing brace survived, producing an `Illegal return statement` syntax error caught by `node --check`, not by eye). Final approach: took Mail's full `pipeline.js` as the base (syntax-valid on its own, confirmed with `node --check` before touching it) and manually reapplied Item 5's small, well-understood diff (the `resolveEffectiveSpec()` gate in `computeBOQ()`, the `specNotExtracted` flag threaded through `status`/`review_reason`, `rebar_source`/`rebar_source_note` on all three per-element BOQ rows) using targeted edits instead of blind hunk-taking.
  - **Found and fixed a genuine merge artifact, not something I introduced**: `tests/bbs-summary.test.js` auto-merged with zero conflicts (git saw no overlapping lines) but the result was internally self-contradictory — one 2026-09-24 fix had added a `B2`/`F3` beam specifically so column `C1` would sit at a middle floor and classify as a plain `ST` shape (keeping this aggregation test independent of roof-column shape-rule changes); a *different* 2026-09-24 fix's comment and assertion assumed `C1` *was* the roof floor and expected an `L-BEND` shape the fixture, as merged, could no longer produce. Two fixes, each internally consistent alone, combined into nonsense because they touched non-overlapping lines of the same test. Fixed by restoring the original intent (`ST` + `CLOSED-STIRRUP` only) and correcting a second, dependent staleness the same bug had hidden — the `total_bars` comparison used to exclude column rows from the expected count (back when `C1` was the odd-one-out `L-BEND` case), which is now wrong since `C1`'s main bar counts as `ST` too.
  - Verified with the real test suite at each step, not just by reading the diff — this is what actually caught both real mistakes above. Final state: **2037/2040 passing**, the same 3 pre-existing failures as always (`tests/raw-extraction-golden.test.js`'s fixture directory — see below, this reorg touched it but did not fix it).
- **Checked whether King's repo reorg accidentally fixed the long-standing broken golden-test fixture path — it didn't, it just moved the wrong thing.** The reorg relocated `finetune_output_contract/examples/` → `data/finetune-output-contract/examples/`, but that's the *old*, 7-file, wrong-pattern-vocabulary fixture set the test's own comment explicitly says not to use. Someone (makham, in the commits pulled today) already updated `tests/raw-extraction-golden.test.js`'s `FIXTURE_DIR` to point at `data/finetune-output-contract/model-output/examples/` — but that directory doesn't exist anywhere in the repo. The correct 8-file, new-pattern-vocabulary fixture set this test needs has never existed in this checkout, before or after today's reorg. Not fixed here (would mean fabricating test data); still an open, named gap.

**Files changed (Constistant repo):**
- `js/shared/pipeline.js` — merged; Item 5's `resolveEffectiveSpec()` gate in `computeBOQ()` manually reapplied on top of Mail's dense-stirrup-zone/ground-roof-column-hook rewrite of the same function.
- `js/boq/boq-summary.js` — merged by hand: combined our `rebar_source_borrowed`/`rebar_source_assumed_standard` `REVIEW_REASON_TH` entries with Mail's new `bent_shape_length_unresolved`/`stirrup_tie_count_conflict` entries; combined the `reviewFlagCell()` "strong flag" logic (Mail's `STRONG_REVIEW_REASONS` set check) with our tooltip enhancement (appending `rebar_source_note`).
- `js/shared/bbs-shape-rules.js`, `tests/bbs-shape-rules.test.js` — took Mail's version wholesale (confirmed no overlap with our work).
- `tests/bbs-summary.test.js` — fixed the self-contradicting merge artifact described above (separate commit `68dc69a`, not folded into the merge commit).
- Everything else in the diff (877 files touched overall across both merge rounds) is King's repo reorganization and makham's purson-worker/BIM work, taken as-is.

**Impact/precautions:**
- **The `checkout --theirs`-on-a-conflicted-file mistake is worth remembering as a pattern to avoid**: it looks like a safe, targeted per-file resolution but it actually discards the *entire* file's working-tree state (including cleanly auto-merged, non-conflicting content), not just the conflicting hunks. Only safe to use when you're certain the file has zero valuable content on the side you're discarding — verified via `git show <base-commit> --stat -- <file>` or equivalent, not assumed.
- **A hunk-boundary regex/scripted "take theirs per conflict marker" approach is also not safe** for a file with large, structurally significant hunks (loop openings, function bodies) — a hunk that looks self-contained in the diff can still depend on non-conflicting code just outside its markers. The syntax error it produced was only caught because `node --check`/`npm test` was run immediately after — this is exactly why "verify before declaring done" matters even for a git merge, not just for application code changes.
- **Auto-merged files are not automatically correct** — `tests/bbs-summary.test.js` had zero conflict markers and still shipped a self-contradicting assertion, because two same-day fixes on different branches touched non-overlapping lines of the same test with incompatible assumptions about the fixture. A green `git merge` exit code does not mean the result makes sense; only the test suite catching an actual assertion failure surfaced this one.
- Every merge step in this session was verified against the real `npm test` run (not just `node --check`) before pushing — this is the only reason both real mistakes were caught before landing on `origin/pipeline-optimize`.

**Left pending / to continue:**
- `tests/raw-extraction-golden.test.js` still cannot run for real — the correct 8-file fixture set (new pattern vocabulary: `grid_master`, `beam_plan`, `footing_plan`, two `section` files, `schedule`, `notes`, `material_list`) has never existed in this checkout. Whoever has access to the Training repo's real fixture data should either commit the correct set to `data/finetune-output-contract/` under whatever path the test expects, or fix the test's `FIXTURE_DIR` to point at wherever it actually lives.
- The duplicate-fix pattern that caused today's conflicts (Mail fixing the same bug on two branches the same day) is exactly the failure mode Ton's standing "check for duplicate work with Mail" rule exists to prevent — worth a direct conversation with Mail about which branch is the source of truth for BBS/rebar-shape work going forward (`pipeline-optimize` vs `Presentation-demo`), since right now both are live and both get direct pushes.
- `missing.md` is now `docs/raw-json-gaps.md` after King's reorg — same content, just moved; anyone who has it bookmarked by the old path needs to update.
- Not verified live in a browser this session — all verification was via `npm test` (2037/2040, 3 pre-existing unrelated failures) and `node --check`.

---

## [Claude Code session] Ton decided the 3 follow-ups from yesterday's problem list; `docs/raw-json-gaps.md` updated

**Context:** Continuing directly from the merge/conflict work logged above. After finishing the sync, I had summarized 3 outstanding items for Ton (framed earlier in the session as 🔴 recently-fixed-but-unverified 3D bugs, 🟡 an orphaned stub file, 🔵 two long-term structural data gaps) and asked which needed his decision. He answered all three via `AskUserQuestion`.

**Decisions made:**
1. **Orphaned stub `js/overview/overview-3d-modal(1).js`** — a dead, unreferenced leftover (confirmed via diff + grep: only the non-suffixed `overview-3d-modal.js` is ever imported) sitting in a file area Mail/makham are actively working in (the same "revive real 3D model" commit from `f057a8e`/`cd5eb0a`). **Ton's call: notify Mail, don't touch it myself.** Not deleted this session — flagging here so whoever talks to Mail next brings it up.
2. **`top_bottom_split`** (wiring `BeamLibraryEntry.main_bar.{top,middle,bottom}` — already stored in schema, just unread downstream — into the 3D spec card and IFC export) — genuinely cheap, app-only, no Training-team dependency. **Ton's call: hold off, not urgent** — specifically because `js/planner/planner-3d-view.js`/`ifc-export.js` are files Mail/makham just finished editing (24-25 Sep), and doing this now would be exactly the duplicate-work risk Ton's standing rule exists to catch. Parked as backlog, not started.
3. **`grid_refs` per-element grid position** (real grid *lines* exist via `GridReference`/`gridPosLookup()` since 2026-09-13, but no per-element field says which line a column/beam actually sits on — 3D/BIM still places pieces on a uniform, not-real-from-drawing grid) — **Ton's call: fold this into the same Training-team request as `docs/raw-json-gaps.md` items 1-2** (vertical-height field for columns/piles) rather than sending it separately, since it's the same ask (new extraction field) to the same team/file format.

**Work done:**
- Updated `docs/raw-json-gaps.md`: added a new §8 documenting the `grid_refs` gap (what already works — real grid lines via `GridReference`; what's missing — per-element grid assignment, never extracted by Qwen's Pass 2 survey step; a real inconsistency found in existing sample data where different files pair row/column letters oppositely, meaning any real extraction attempt needs a stated convention before starting, not after) and rewrote the closing summary (old §7 → §9) to explicitly say items 1-2 and 8 should go to the Training team together in one request. Doc-only change, no app code touched — `top_bottom_split` deliberately is **not** in this doc since it's an app-side wiring gap, not a raw-JSON schema gap.

**Files changed:**
- `Constistant/docs/raw-json-gaps.md` — added §8 (`grid_refs`), renumbered old §7 to §9, updated footer date note.
- This diary entry.

**Left pending / to continue:**
- Someone (Ton, when convenient) needs to actually hand `docs/raw-json-gaps.md` items 1-2+8 to the Training team as one combined ask — this session only prepared the doc, did not initiate that conversation.
- Someone needs to tell Mail about the dead `overview-3d-modal(1).js` file — not done this session per Ton's explicit choice to notify rather than act.
- `top_bottom_split` stays backlog until Mail's current 3D work settles — revisit then, don't start unprompted.
- Still true from the entry above: `tests/raw-extraction-golden.test.js`'s fixture path issue is unresolved; live-browser verification of yesterday's structure-model/3D fixes has not happened.

---

## [Claude Code session] ⚠️ Correction — the earlier `grid_refs` gap claim (this same day, above) was wrong; verified live and fixed

**Context:** Ton asked to actually pull up the real corrected JSON and the source PDF for a house and look at them, rather than keep reasoning from memory. Good call — doing that immediately surfaced that the `grid_refs` claim in the entry right above ("never extracted by AI, only demo data sets it") and the matching §8 just added to `docs/raw-json-gaps.md` were **both wrong**, carried forward from a stale, never-verified summary from before a context compaction earlier in the session.

**What was actually checked, with real data (`Training/json_แก้ไขแล้ว/01บ้าน_เล็ก_1ชั้น_01/`, real PDF at `Training/image/บ้าน_เล็ก_1ชั้น_01/`):**
1. `..._หน้า19_view1_footing_plan.json` — column mark `C1` really does carry `"grid_refs": ["D1","D2","D3","C1","C2","C3","B1","B2","B3","A1","A2","A3"]` (12 refs, matching `count: 12`). Footings the same. `..._หน้า19_view2_beam_plan.json` — every beam segment carries `grid_ref_start`/`grid_ref_end` (e.g. B2: `D1`→`D2`). The file's own 2026-07-14 correction note explicitly documents the convention: no-dash = a point (`"D1"`), dash = a line/range (`"D-C"`) — consistent across the whole house, not "inconsistent between files" as claimed.
2. `js/drawing/raw-extraction-adapter.js`'s `parseGridRef()` already handles both the no-dash (current, real-data) format and the older dashed format, plus a reversed-letter/number-order case fixed as recently as 2026-09-24 for a different real test house (comment in the code names the exact incident, job id, and impact).
3. `js/shared/structure-model.js`'s `extractedGridPositions()` (added 2026-08-22, per its own in-file comment) calls that same `parseGridRef()` and positions pieces using real grid-master coordinates (`gridPosLookup()`, added 2026-09-13) — not a synthetic uniform grid, when both are available.
4. **Ran it for real, not just read the code**: wrote a throwaway script (deleted after, not committed) that imported all 101 real JSON files for this house through `adaptRawExtraction()` → `buildStructureModel()`. Result: the 12 pieces of column mark `C1` landed on exactly 3 distinct real x-positions (`-3.88, 0.13, 3.13` m — spacing 4.0m/3.0m, matching the real grid's x-lines 1→2→3) and 4 distinct real y-positions (`-5.5, -1.5, 0.5, 4.0` m — spacing 4.0m/2.0m/3.5m, matching real y-lines D→C→B→A exactly). Beam `B2`'s 6 segments got real, differing lengths (2.0/3.0/3.5m) at real positions, not one averaged span repeated everywhere. This is definitively non-uniform, drawing-accurate placement — the feature works.

**Fix:** Rewrote `docs/raw-json-gaps.md` §8 in place (not silently deleted — recorded as an explicit correction, matching this repo's own convention for factual corrections) to state the verified truth: `grid_refs` needs no further Training-team request, the whole chain (raw JSON → adapter → 3D model) already works correctly. §9's closing summary and the doc's footer date-note were updated to match. Pushed as `Constistant` commit `81f5904`.

**Root cause of the error, worth naming so it doesn't repeat:** the wrong claim originated in a pre-compaction conversation summary that itself was probably reasoning from an even older, already-superseded understanding (the entries in CLAUDE.md from 2026-08-22/2026-09-13 that this diary-writer should have cross-checked but didn't before writing it into a fresh doc section). It was never independently re-verified against either the real training data or the current code before being written down and pushed twice (once in the doc, once in this diary). **Lesson recorded directly in the doc's §8 for future sessions**: before writing "field X is never extracted," open a real corrected JSON file and check, and/or actually run the relevant code against real data — don't trust a carried-forward summary.

**What is still a genuine, unverified-by-this-check gap:** `js/planner/planner-3d-view.js`'s `DISCLAIMER` string (`"ตำแหน่งบน grid เป็นระยะเท่ากัน ไม่ใช่ตำแหน่งจริงจากแบบ"`) is now stale — it's shown unconditionally even when real grid_refs positioning succeeded, so it understates what the viewer can now do. Not fixed this session: that file is one Ton explicitly asked to hold off touching today (same file area as the parked `top_bottom_split` decision), so this is left as a note for whoever next touches that file rather than acted on unilaterally.

**Files changed:**
- `Constistant/docs/raw-json-gaps.md` — §8 rewritten to record and correct the false claim; §9 updated to match.
- This diary entry (correction, not a silent edit of the entry above).

**Left pending / to continue:**
- `planner-3d-view.js`'s stale disclaimer text (noted above) — cosmetic, low priority, deferred by the same "don't touch that file today" logic as `top_bottom_split`.
- Everything else pending from the entries above (fixture-path issue, live-browser verification of yesterday's 3D fixes, telling Mail about the dead stub file) is unchanged by this correction.

---

## [Claude Code session, 2026-09-26] Recalculated Constistant's unit operating cost from real GPU/API data for the pitch deck

**Context:** Ton pasted a slide fragment ("Slide 10: Value Delivered per Project") and asked for the running-cost side of it, referencing training data / everything done so far, computed from the actual technology in use — not invented numbers.

**Found:** `Constistant/Deals/01_STORYTELLING_STRUCTURE.md` already has a Slide 11 (Unit Economics) with a claim: "< 50 บาท/โครงการ (< $1.50), ~3 นาที, Vast.ai GPU." Checked it against real evidence instead of taking it at face value:

1. **The slide describes the wrong system as production.** Constistant's actual live vision model today is Qwen-VL-Max via Alibaba DashScope cloud API (per `CLAUDE.md`'s own architecture section) — Vast.ai/Purson (the fine-tuned self-hosted model the slide's cost is based on) is still in eval/training, not deployed to real users.
2. **The "~3 minutes" figure is real, but mislabeled.** Pulled real GPU pricing (`server/purson-worker/คู่มือวันพรีเซนต์.md`: $1.07-1.13/hr for an RTX PRO 6000 96GB) and real per-page inference timing from actual eval runs (`tune_ai/t04_Purson/eval_results/fold0+fold3/_summary.json`, 47 valid page-level samples, mean 156.4s / median 143.5s — that's where "~3 min" comes from). But it's the time **per page**, not per project as the slide's header implies. Counted structural-discipline pages in one fully-corrected training house (`01บ้าน_เล็ก_1ชั้น_01`): 13 of 101 files. Recomputed: 13-25 structural pages × ~2.5-6 min/page ≈ 30-80 min of GPU time per project → **20-50 THB/project** — the slide's headline number roughly survives, but for a different reason than stated (multiplying page-count × per-page-time, not "3 minutes total").
3. Added an order-of-magnitude estimate for the actual production Qwen-VL-Max cost (pay-per-token cloud API, not hourly GPU rental) — well under 1 THB/project — sourced from a public pricing figure ($0.41/1M input tokens) found via web search, explicitly flagged as unverified against the real Alibaba console usage log and needing confirmation before being quoted to STECON.

**Work done:** Wrote `Constistant/Deals/06_UNIT_ECONOMICS_COST_CALCULATION.md` — full breakdown with cited sources for every real number, a clearly-labeled estimate section for the unverified Qwen token cost, a side-by-side comparison table (production vs. future self-hosted), a small "other infra" section (Supabase/Gemini/GitHub Pages/PDF server, all near-zero at current scale), and concrete action items (pull real DashScope usage logs; sample 3-5 more houses of different sizes to firm up the 13-25 page range; fix the "3 นาที ต่อโครงการ" wording in slide 11 itself). Committed to `pipeline-optimize` (this repo's Deals/ folder lives there, confirmed via `git branch --all --contains`) as `0beb1fe`, then merged a concurrent push from Mail (`b52c6d1` — live Qwen Pass 3 prompt now asks for `grid_ref` per length-group, a real production-side fix in the same area as yesterday's grid_refs correction, no file overlap) and pushed as `cd41e52`. `npm test`: 2040/2043 passing (same 3 pre-existing, unrelated `raw-extraction-golden.test.js` fixture-path failures as every entry this week).

**Left pending:** Someone with Alibaba Cloud Model Studio console access should pull the real DashScope usage log to replace the estimated Qwen-VL-Max cost with a measured one (§5 action item 1 in the new doc) — this is the single highest-value follow-up since it would turn an estimate into a verified number in about 10 minutes. The 13-page structural-discipline count is from one house only and should be sampled against a few more of different sizes before being quoted as a stable range.

---

## [Claude — session with อิม] Found: อิม's machine cannot return a rented vast.ai card on its own — environment gap, not a code bug

**Why this came up:** while checking whether a second machine could act as backup/operator for the rented GPU (return the card if makham's machine is unreachable), อิม tried the return step from his own machine and it failed. Checked what's actually missing before proposing a fix.

**Checked on อิม's machine, real state:**

| ต้องมี | สถานะบนเครื่องนี้ |
|---|---|
| `vastai` CLI | ไม่มี |
| `~/.vast_api_key` | ไม่มี |
| `python` in PATH | ไม่มี (ไม่เจอตั้งแต่ตอนแก้โค้ด ต้องใช้ `node` แทนมาตลอด) |
| `presentation_state.json` | ไม่มี — อยู่แต่บนเครื่องมะขามเท่านั้น (state ต่อเครื่อง ไม่ sync) |

**Root cause:** `presentation.py down` (the normal return-the-card path) needs the `vastai` CLI (itself needs `python`) plus the local `presentation_state.json` recording which instance is rented. None of the four exist on this machine — this is a **missing-tooling gap**, not a bug in `presentation.py`/`go.py` themselves.

**Two ways to unblock, not yet chosen:**
1. Install `vastai` CLI properly (needs `python` first, which this machine also lacks) — makes this machine a full second operator, matching makham's setup.
2. Hit vast.ai's REST API directly with `curl` instead of the CLI — no `python`/`vastai` install needed, but requires reading vast.ai's real API docs first for the correct destroy/instances endpoint rather than guessing at one (a guessed wrong endpoint against a billing API is not something to try blind).

**Not done yet** — this session only diagnosed the gap; อิม is deciding which path to take next.

**Left pending / to continue:**
- Decide install-CLI vs. raw-REST-API-via-curl for a second-operator machine.
- If curl path is chosen: read vast.ai's official API docs for the confirmed `destroy instance` endpoint + auth header format before writing anything, per the standing rule of never guessing at a billing-affecting API call.
- `presentation_state.json` being per-machine (not shared/synced) means any second operator needs their own way to know which instance id is currently rented — worth deciding whether that should live somewhere shared (e.g. read live from `vastai show instances`/the REST equivalent, which needs no local state file at all) rather than solving state-sync separately.

---

## [Claude — session with อิม] Follow-up: full diagnosis on อิม's machine — confirmed clean, one extra gap found (`worker_config.json`), one false lead ruled out

**อิม re-checked all 4 items himself with more detail, plus 2 extra checks:**

| ต้องมี | สถานะ |
|---|---|
| `vastai` CLI | ไม่มี — ไม่พบใน PATH ของทั้งสอง shell |
| `~/.vast_api_key` / `~/.config/vastai/` / `%APPDATA%\vastai\` | ว่างหมดทั้ง 3 ตำแหน่ง |
| `python` | **stub เท่านั้น ไม่ใช่ตัวจริง** — ดูรายละเอียดด้านล่าง |
| `presentation_state.json` | ไม่มี |
| `worker_config.json` | **ไม่มี — เจอเพิ่มวันนี้** |
| `blacklist.json` | มี (ตั้งใจ commit เข้ารีโป — เป็นความรู้ host ที่เสียเวลา ไม่ใช่ state รายเซสชัน) |
| `D:\purson` junction | ไม่มี — ยืนยันว่าเป็นของเครื่องมะขามเท่านั้น ไม่ใช่ของเครื่องนี้ |

**python — กับดักที่ควรรู้:** `Get-Command python` เจอไฟล์ที่ `C:\Users\ACER\AppData\Local\Microsoft\WindowsApps\python.exe` แต่นั่นคือ **stub ของ Microsoft Store** ไม่ใช่ Python จริง — เรียกจริงจะได้ "Python was not found; run without arguments to install from the Microsoft Store" (อิมเจอมาแล้วตอนแก้โค้ดเมื่อวาน เลยต้องสลับไปเขียนสคริปต์ด้วย `node` แทน). ไม่มี `pip`, ไม่มี `py` launcher, สแกน `C:\Python*`/`%LOCALAPPDATA%\Programs\Python\`/miniconda/anaconda แล้วไม่เจอ Python ตัวจริงที่ไหนในเครื่องเลย.

**`worker_config.json` ไม่มี — ผลที่ตามมาใหญ่กว่าที่คิด:** ไฟล์นี้เก็บ Supabase service key + `PURSON_GPU_URL` + `PURSON_PROMPTS_DIR` — ไม่มีไฟล์นี้แปลว่า**เครื่องนี้รัน `worker.py` ไม่ได้ด้วย ไม่ใช่แค่คืนการ์ดไม่ได้**. ไฟล์นี้ gitignored โดยเจตนา (มี secret จริง) ไม่ตามมากับ git ตามที่ควรเป็น — ไม่ใช่บั๊ก.

**สรุปสิ่งที่ต้องทำถ้าอยากให้เครื่องอิมทำงานได้เอง (3 อย่าง, ทำตามลำดับ):**
1. ลง Python จริงจาก python.org (**ห้ามลงจาก Microsoft Store** — จะชนกับ stub เดิม)
2. `pip install vastai` แล้ว `vastai set api-key <key>` — พิมพ์ key ใน terminal ของอิมเอง ไม่ส่งผ่านแชท
3. `worker_config.json` — ต้องขอ Supabase service key จากมะขาม, โฟลเดอร์ `tune_ai/t04_Purson` มีอยู่แล้วในเครื่องนี้ (387 MB) แค่ต้องชี้ path ให้ถูก

**ข้อ 1-2 พอสำหรับ "กดคืนการ์ด" อย่างเดียว** (สิ่งที่อิมต้องการจริงตอนนี้) — **ข้อ 3 จำเป็นเฉพาะถ้าจะให้เครื่องนี้รับงานถอดแบบแทนเครื่องมะขามด้วย** ไม่ใช่ทั้งหมดต้องทำพร้อมกัน

**ตัดสินใจแล้ว (มะขามสั่งอัพทันที):** ให้อิมลง Python + vastai CLI เอง — อิมเสนอให้ Claude ไล่คำสั่งทีละขั้นแล้วเช็คผลให้หลังลงเสร็จ (installer ต้องรันโดยอิมเอง Claude ลงให้ไม่ได้).

**Left pending:**
- รอ Claude (ในเซสชันของอิม) ไล่ขั้นตอนติดตั้ง Python จริง + `pip install vastai` + `vastai set api-key`
- `worker_config.json`/Supabase service key — ยังไม่ได้ขอ ทำเฉพาะถ้าต้องการให้เครื่องอิมรับงานถอดแบบด้วย (ไม่ใช่แค่คืนการ์ด)
- decide install-CLI vs. curl-REST-API ที่บันทึกไว้ก่อนหน้า — **ตอนนี้เลือกทาง install-CLI แล้ว** (ข้อนี้ปิด)

---

## 21:30-22:30 — อิม + Claude — วางแผนแผน A (worker.py บนเครื่องเช่า): ชนกับเอกสาร "โหมด A" ของมะขาม เลื่อนลงมือจนกว่าจะมี Python

**Why:** ต่อจากเอนทรี 25 ก.ย. ที่บันทึกไว้ว่าเครื่องอิม (checkout ที่สอง) ยังคืนการ์ดเองไม่ได้
(ไม่มี vastai CLI/python/key/state file) — อิมสั่งให้ทำแผน A จริง: ย้าย `worker.py` จาก
คอมมะขามไปรันบนการ์ดเช่าเลย เว็บไม่ต้องแก้อะไร (`PURSON_GPU_URL` ยังเป็น
`http://localhost:8000` ค่าเดิม แค่ "localhost" หมายถึงเครื่องเช่าแทน)

**พบระหว่างวางแผน — ของที่ต้องส่งขึ้นการ์ดเล็กกว่าที่คาดมาก:**

อ่าน `worker.py:135-137` (`ORGANIZE_PY`/`CV_SCAN_PY` ผูกกับ `PURSON_PROMPTS_DIR` โดยเดินขึ้น
ไปหาตามโครง Training repo) แล้ววัดจริงด้วย `find`/`du` แทนการเดา:

| ของที่ต้องส่งขึ้นการ์ด | ขนาดจริง |
|---|---|
| `worker.py` + `worker_config.json` | ~40 KB |
| prompt จาก `tune_ai/t04_Purson/**.md` (27 ไฟล์ที่ worker เปิดจริง) | 317 KB |
| CV เสริม (`organize.py`, `cv_scan.py`, `pattern_recognition.py`, `templates/`) | ~890 KB |
| **รวม** | **~1.2 MB** |

ไม่ใช่ 387 MB ของ `t04_Purson` ทั้งโฟลเดอร์ตามที่เคยพูดไว้ก่อนหน้า — 282 MB ในนั้นคือ
`data_before_tune/` ที่ไม่มีโค้ดตัวไหนแตะเลย · CV เป็นของ "มีก็ดี" เท่านั้น
(`_prep_workroot`: "คืน None ถ้าอะไรก็ตามพัง — ผู้เรียกต้อง fallback เป็นเต็มหน้า") ไม่ใช่
สิ่งที่ block การทำงาน

**พบระหว่างวางแผน (ก้อนที่สอง) — เอกสารใหม่ของมะขาม `DEPLOY-THIRD-PARTY.md` (commit
`d78a5f2`, push เข้ามาระหว่างเซสชันนี้พอดี) นิยาม "โหมด A" คนละอย่างกับ "แผน A" ที่อิม
กับ Claude คุยกันไว้ — ต้องแยกให้ชัดก่อนลงมือ ไม่งั้นจะทำผิดงาน:**

| | แผน A (ที่อิมสั่งวันนี้) | "โหมด A" ในเอกสารมะขาม |
|---|---|---|
| การ์ด | เช่ารายชั่วโมง เปิด-ปิดเอง | เช่า**ถาวร** มี IP คงที่ |
| worker.py | ย้ายไปรันบนการ์ดเช่า | ไม่ใช้เลย |
| เว็บเรียกยังไง | เหมือนเดิมทุกประการ (คิว) | Edge Function ตรง (`action: "single"`) |
| ใช้ `presentation.py` ไหม | ใช่ (เพิ่มความสามารถให้) | เอกสารเขาเขียนตรงๆ ว่า **"อย่าเอามาใช้"** |

**ข้อเท็จจริงที่ทำให้ข้ออ้าง "ไม่ต้องเขียนโค้ดเลย" ในเอกสารมะขามใช้ไม่ได้กับปุ่มจริงบนเว็บ:**
grep ทั้ง `js/`+`templates/` แล้ว `purson_analyzeSingle()` (เส้นทาง direct ที่เอกสารเขาอ้างถึง)
**ไม่มีใครเรียกเลยสักที่** โผล่แค่ใน docstring ของไฟล์ตัวเอง ปุ่ม "🤖 ถอดแบบด้วย Destrier"
บนเว็บเรียก `purson_submitHouseExtract()` = เข้าคิวเสมอ ([drawing-purson.js:331](../../../Githubbb/Constistant/js/drawing/drawing-purson.js#L331))
ตรงกับที่ README เดิมเขียนไว้ว่า "งานถอดทั้งหลังใช้คิวเสมอ เพราะกินเวลาเป็นชั่วโมง" —
เส้นทาง single เป็นของ debug/ยิงทีละภาพ ไม่ใช่ถอดบ้านทั้งหลัง **แปลว่าต่อให้ทำตามเอกสาร
มะขามครบ (เช่าถาวร + ตั้ง 3 secret) ปุ่มบนเว็บจะยังค้างที่ "รอ worker รับงาน" เหมือนเดิม
เพราะไม่มีใคร poll คิว — แผน A คือชิ้นที่ขาดของทั้งสองแนวทาง** (ถ้าจะเช่าถาวรจริงก็ยังต้องมี
worker รันอยู่บนนั้น ด้วย systemd แทน presentation.py)

**เริ่มเขียนโค้ดแล้วหยุด — เพราะเครื่องนี้ตรวจงานตัวเองไม่ได้:**

แก้ `presentation.py` เพิ่มค่าคงที่ของแผน A ไปชุดหนึ่ง (`REMOTE_WORK`/`REMOTE_TRAINING`/
`REMOTE_PROMPTS`/`REMOTE_WORKER_DEPS`) แล้วหยุด — เครื่องอิมไม่มี Python จริง มีแค่ stub
ของ Microsoft Store (`python`/`python3` ชี้ไป `WindowsApps\python.exe` แต่เรียกจริงแล้วเด้ง
ให้ไปลงจาก Store) ไม่มี `pip`/`py` launcher เลย สแกนตำแหน่งมาตรฐานทั้งหมดแล้วไม่เจอ Python
ตัวจริงในเครื่อง — แปลว่า `py_compile` ไม่ได้ รันเทส (`test_serve_presets.py`,
`test_blacklist.py`) ไม่ได้ ขัดกับ convention ของรีโปนี้ที่ตรวจทุกไฟล์ `.py` ก่อน commit
เสมอ **สั่งให้ revert ของที่แก้ไปแล้วทิ้ง** (`git checkout --`) แทนที่จะ commit โค้ดที่ไม่เคย
ถูกรันหรือตรวจแม้แต่ syntax — ของที่ revert เป็นแค่ constant ยังไม่มีใครเรียกใช้ ไม่เสียหาย

**ตอบคำถามของอิม (ยืนยันด้วยโค้ด ไม่ใช่เดา): ใช่ ทุกคนที่มีเว็บใช้ AI ถอดแบบได้จริง และแผน A
ไม่เปลี่ยนเรื่องนี้เลย:**

- `pages/signup.html:86` — `supabase.auth.signUp({email, password})` ตรงๆ ไม่มี invite
  code/อนุมัติ/whitelist ในโค้ดเลย ใครก็สมัครได้
- RLS บน `purson_jobs` เช็คแค่ `owner_profile_id = auth.uid()` ไม่เช็ค role/tier —
  สมัครเสร็จ = สั่งงานได้ทันที
- แผน A ย้ายแค่ "worker.py รันที่ไหน" ไม่แตะ auth/RLS แม้แต่นิดเดียว ช่องโหว่นี้ (ตรงกับ
  ช่องโหว่ข้อ 1 ใน `DEPLOY-THIRD-PARTY.md` ของมะขาม — `check_rate_limit()` มีอยู่แล้วแต่ยัง
  ไม่ได้ต่อเข้า) จะยังอยู่เป๊ะแม้แผน A เสร็จสมบูรณ์แล้ว
- จุดที่ตรวจจากโค้ดในรีโปไม่เห็น: การตั้งค่าฝั่ง Supabase Dashboard (เช่น ปิด email signup)
  ต้องเช็คแยกที่นั่น ไม่ใช่แค่ในโค้ด

**Files changed:** ไม่มี — `presentation.py` แก้แล้ว revert ทิ้ง working tree สะอาดทั้งสองรีโป
(ไฟล์นี้คือไฟล์เดียวที่ถูกแตะในเซสชันนี้)

**Verification:** ไม่มีโค้ดให้ตรวจ (revert ไปแล้ว) · คำตอบเรื่องสมัครเสรี/RLS ยืนยันด้วยการอ่าน
source จริง 3 จุด (`pages/signup.html`, migration ของ `purson_jobs`, `drawing-purson.js`)
ไม่ใช่การเดา

**Impact/precautions:**
- แผน A **ยังไม่มีบรรทัดโค้ดไหนอยู่ในรีโปเลย** — เซสชันนี้เป็นแค่วางแผน+เช็คข้อเท็จจริง
  คนถัดไปอย่าคิดว่ามีอะไรเริ่มไว้แล้วครึ่งทาง
- **แผน A กับ "โหมด A" ของมะขามเป็นคนละงาน อย่าสับสน** — ถ้าจะทำทั้งคู่ ต้องทำแผน A ก่อน
  (เพราะเป็นชิ้นที่คิวขาดอยู่จริง) แล้วค่อยตัดสินใจว่าจะเช่าถาวรเพิ่มไหม
- **ยังไม่มีสภาพแวดล้อมสำหรับแก้ `.py` บนเครื่องอิมได้เลย** — ต้องลง Python จริงจาก
  python.org ก่อน (ห้ามลงจาก Microsoft Store จะชนกับ stub เดิมที่มีอยู่) ติ๊ก "Add to PATH"
  ตอนติดตั้ง แล้ว `pip install vastai` + `vastai set api-key <key>` (พิมพ์เองใน terminal
  ไม่ผ่านแชท) — ทั้งสามอย่างนี้ทำแทนไม่ได้ ต้องเป็นอิมเอง
- ช่องโหว่ "ใครสมัครก็สั่งงานได้" มีอยู่**ตอนนี้เลย** ไม่ต้องรอแผน A เสร็จ — ถ้ามีคนอื่นรู้ URL
  เว็บและสมัครเข้ามา สั่งถอดแบบได้ทันทีที่การ์ดเปิดอยู่ ไม่ว่าจะเป็นแผน A หรือ B

**Left pending / to continue:**
- ลง Python จริงบนเครื่องอิมก่อน — ทางตันของทุกอย่างต่อจากนี้ (เขียน/ตรวจ `.py`,
  `vastai` CLI, กดเช่า/คืนการ์ดเอง)
- เขียนแผน A จริง: เพิ่มใน `presentation.py` ให้ส่ง `worker.py`+prompt+CV (~1.2 MB ตามที่
  วัดไว้ข้างบน) ขึ้นเครื่องเช่าด้วย `scp`, สร้างโครงโฟลเดอร์เลียน Training repo,
  ติดตั้ง `REMOTE_WORKER_DEPS` (`requests opencv-python-headless numpy`), แล้วสั่งรัน
  `worker.py` แบบเดียวกับที่ `upload_and_start_server()` เปิด `serve_purson.py` อยู่แล้ว
  (`setsid --fork nohup` — ขาด `--fork` แล้วค้างจริงมาแล้ว 23 ก.ย., ดูคอมเมนต์ใน
  `upload_and_start_server()`) + guard `pgrep` กันรันซ้อน + เทสใหม่ตาม convention
  (`test_remote_worker.py`, ต้องแดงกับ source เดิมก่อนแล้วค่อยเขียว)
- ตัดสินใจเรื่อง allowlist/quota ก่อนบอกใครว่า "ใช้ได้แล้ว" — `check_rate_limit()` มีอยู่แล้ว
  ใน `database/rate_limit.sql` แต่ยังไม่ต่อเข้ากับ `purson-vision` (บันทึกไว้ใน
  `DEPLOY-THIRD-PARTY.md` ข้อ 5.1 แล้วเหมือนกัน)
- คุยกับมะขามให้ตรงกันว่าเป้าหมายจริงคือแผน A (อิมกดใช้เองเป็นครั้งคราว) หรือ "โหมด A"
  ของเขา (เปิดถาวรให้บุคคลที่ 3) — สองทางนี้ต้องการเครื่องมือคนละชุด ทำสลับกันจะเสียเวลาฟรี

---

## 22:45-23:45 — อิม + Claude — ทำไมถอดแบบช้า + ช่องโหว่ใน workflow การอ่านแบบ: สรุปส่งต่อให้มะขาม (ยังไม่มีโค้ดเปลี่ยน)

**Why:** อิมถามว่าถอดแบบนานไป ทำยังไงให้เร็วขึ้น แล้วต่อด้วย workflow การอ่านแบบมีอะไรควรแก้ ·
เซสชันนี้**สืบ + วางแผนล้วน ไม่มีโค้ดเปลี่ยนสักบรรทัด** · ใช้ Explore agent 3 ตัวอ่านคู่ขนาน แล้วตรวจซ้ำ
จุดสำคัญเองก่อนเขียนลงที่นี่ · อิมตัดสินใจ **ไม่แตะ notes** และ **ส่งต่อให้มะขามลงมือ**

### 1. เวลารวมที่จดไว้ผิด — ~4 ชม. 50 นาที ไม่ใช่ 3 ชม. 50 นาที

job `83b8e52c`: `worker_run.log:2` claim `2026-09-23T17:50:26Z` = 00:50:26 เวลาไทย → เสร็จ 05:40 ≈ **4h50m**
ทั้ง `อ่านตอนเช้า_24กย.md:11` และ `2026-09-24.md:177` เขียน "3h50m" แต่ snapshot ในไฟล์เดียวกันขัดกันเอง
(pass0 21/29 ตอน ~01:00-01:25 · pass2 0/17 ตอน 01:45) · ตัวเลขจริงอยู่ใน row Supabase
`83b8e52c-1183-441c-bbdd-3391619610d5` (`claimed_at` / `updated_at` / `result.timings`)

### 2. เวลาหายไปไหน

| ขั้น | เวลา | สัดส่วน |
|---|---|---|
| pass2 — gridline + 16 คำขอ ต่อกันทีละตัว | ~3 ชม. 57 น. | **82%** |
| ↳ notes ที่วัดได้ 3 หน้า (p3 684 วิ · **p8 2,401 วิ ชนเพดาน กู้ได้ 0** · p18 91 วิ) | 53 น. | 18% |
| ↳ แปลน/section (~13.5 นาที/ชิ้น) + notes p19/p26 ที่ไม่ได้วัด | ~2 ชม. 56 น. | 61% |
| ↳ gridline (4 ภาพ, 485 วิ) | 8 น. | 3% |
| pass0 — 29 หน้า (~80 วิ/หน้า = 2.5× ค่าคงที่ 32 วิ — **ยังไม่รู้สาเหตุ**) | ~39 น. | 13% |
| CPU pass1/1.5/2.5 ขณะ GPU ว่าง (pass2.5 ชน timeout 300 วิ ได้ศูนย์) | ~11 น. | 4% |

งาน pass2 ของรอบนี้: footing ×4, beam ×3, section ×4, notes ×5 (หน้า 3/8/18/19/26) — ไม่มี schedule/slab
ลำดับจริง (`worker_run.log:62-83`): gridline → notes p3 → notes p8 → notes p18 → footing p19 → notes p19 → …

### 3. ต้นเหตุ — ตัวเสิร์ฟเขียนได้ ~4-8 token/วิ ทั้งที่การ์ดทำได้ ~300

**ทุกคำขอ ≈ ความยาวคำตอบ ÷ ความเร็ว decode** · หลักฐาน:
- `อ่านตอนเช้า_24กย.md:143` — 02:10 GPU **96%** แต่งานออกช้า = การ์ดทำงานเต็มแต่ไม่มีประสิทธิภาพ
- `อ่านตอนเช้า_24กย.md:147` "เขียน 6000 token ใช้เวลา 40 นาที" ≈ 2.5 tok/s · `serve_purson.py:113` S-01
  15,910 ตัวอักษร / 1,411 วิ ≈ 4.25 tok/s · `คู่มือวันพรีเซนต์.md:385-395` "~11 ตัวอักษร/วินาที คงที่"
- `README.md:99-100` "97-98% เป็น software overhead … ~6-8 token/วิ ทั้งที่การ์ดควรทำได้ ~300"

**สาเหตุที่ยืนยันแล้ว:**
1. **kernel ของ linear attention ไม่ได้ลง** — `tune_ai/t05_Destrier/proof/01_op04_sampling_ผ่าน/serve_sampling.log:11`
   "The fast path is not available because one of the required library is not installed. Falling back to
   torch implementation" (ต้องมี `flash-linear-attention` + `causal-conv1d`) · **30 ใน 40 ชั้น**ของ
   Qwen3.6-35B-A3B เป็น linear attention · deps ของ preset `destrier` (`presentation.py:93`) ไม่มีสองตัวนี้ ·
   FA2 ก็ปิด (`serve_sampling.log:7`)
2. **torch ไม่ pin** — `onstart_cmd()` (`presentation.py:256-259`) = `pip install -U {deps}` บน
   `vastai/pytorch:cuda-12.8.1-auto` (`:136`) → torch ที่ได้ขึ้นกับจังหวะเช่า · **MISTAKES.md:7476-7482 เคย
   ช้าลง ~20×** เพราะ torch cu13.0 บน image CUDA 12.8 — สาเหตุเดียวกับที่ลง fla ไม่สำเร็จวันที่ 31 ส.ค.
   (`2026-08-31.md:452`, `MISTAKES.md:8160`) · recipe เครื่องดี `results/gpuB_working_config/` ที่ MISTAKES
   อ้างถึง **ไม่มีใน checkout Training ของเครื่องอิม** — น่าจะอยู่เครื่องมะขาม
3. **ไม่มีอะไรบอกว่ารอบเช่านี้ได้ของดีหรือของเสีย** — ตัวเสิร์ฟไม่พิมพ์เวอร์ชัน torch/CUDA หรือสถานะ kernel
4. HF `generate` ไม่มี CUDA graph · `async def chat` เรียก `generate()` แบบ blocking (`serve_purson.py:134`,
   `:152`) · LoRA 11 GB ไม่ merge ทับ 256 experts ทุก forward

**เรื่อง "ดวงเครื่อง":** ฮาร์ดแวร์ไม่ใช่สาเหตุหลัก (เลือกการ์ดรุ่นเดียวกันทุกรอบ) — ดวงมีผลแค่ทางแย่:
(ก) เน็ตเครื่อง → กระทบแค่เวลาโหลดโมเดล (**แก้แล้ว** 23 ก.ย. net_check + blacklist) ·
(ข) เวอร์ชัน torch ที่สุ่มได้ → กระทบทุกหน้า (**ยังไม่แก้**) · ดวงดีแค่ไหนก็ได้แค่ "ช้าแบบปกติ"

### 4. เรื่อง notes — อิมตัดสินใจ "ไม่แตะ" (รันครบเหมือนเดิม)

บันทึกหลักฐานไว้เผื่อวันหนึ่งจะทบทวน:
- **ข้าม notes ได้โดย BOQ/BBS ไม่ขยับสักบาท:** `extracted_notes` ถูกเขียน (`raw-extraction-import.js:516`)
  แต่ไม่มีโค้ดไหนอ่าน · ฝั่งราคาอ่าน `design_standard_overrides` (`project-store.js:821`) ที่ไม่มีโค้ดไหนเขียน ·
  คอนกรีต hardcode 240 ksc (`pipeline.js:882`)
- **ตัดกลางทาง (cap) แย่กว่าข้าม:** prompt เขียน `sections[]` (transcript) ก่อน `notes{}` (ค่าที่ใช้ได้) ·
  ตัดกลางทาง = จ่ายค่า GPU ได้ศูนย์ (adapter ทิ้งไฟล์ที่ไม่มี `notes{}` — `raw-extraction-adapter.js:674`)
- notes รันก่อนแปลนโครงสร้างเพราะเรียงตามเลขหน้า (`worker.py:836-855`, ไม่มี sort ที่ไหน) — ข้อเสนอเรียงใหม่
  (`อ่านตอนเช้า_24กย.md:154`) **ไม่ทำ** ตามการตัดสินใจนี้
- worker ไม่มี config ให้ข้าม subtask — มีแค่ `TRAINED_SUBTASKS` hardcode (`worker.py:107-108`)

### 5. แผนเร่งความเร็วขั้นที่ 1 (เสนอ ยังไม่ทำ) — ลง kernel + ล็อกเวอร์ชัน + วัดจริง

1. `serve_purson.py` พิมพ์ตอนเปิดเครื่อง: torch version, `torch.version.cuda`, fla/causal_conv1d import ได้ไหม, FA2
2. `presentation.py` เพิ่ม `flash-linear-attention` + `causal-conv1d` ใน deps ของ `destrier` — ติดตั้งแยกคำสั่ง
   **ล้มได้โดยไม่พาเครื่องล้ม** (fallback = สถานะวันนี้) · `causal-conv1d` อาจต้อง compile สำหรับ sm_120
3. pin torch ให้ตรง cu12.8 — เลขเวอร์ชันจากข้อ 1 บนรอบเช่าจริง หรือจาก recipe gpuB ถ้าหาเจอ
4. เทสใน `test_serve_presets.py` (ต้องแดงกับ source เดิมก่อน)
5. วัด 1 รอบเช่า (~1-2 ชม. ≈ $2-3): 3 หน้าชุดเดิม ก่อน/หลังลง kernel → ตัวอักษร/วิ + **diff JSON ต้องตรงกัน**

**ผลคาดการณ์ตรงๆ: ไม่รู้จนกว่าจะวัด** — ถ้าคอขวดหลักคือชั้น MoE ที่ไม่ merge จะช่วยน้อย ·
แต่ข้อ 1+3 คุ้มแน่นอนไม่ว่าผลจะออกมายังไง (กันเคสช้า 20× แบบไม่มีอะไรเตือน)

### 6. ทางเร็ว merged + vLLM/SGLang — ไม่เคยรันจริง และจะพังตั้งแต่รอบแรก

- HF: `dacarokann/destrier-merged` ตอบ **401** ส่วน `dacarokann/destrier` ตอบ 200 → แทบแน่ใจว่ายังไม่มี ·
  `merge_lora_to_base.py` / `verify_merge*.py` ยัง untracked ไม่มี log ว่าเคยรัน · `README.md:127`
  "ทั้งสองทางยังไม่เคยรันจริงบนการ์ด"
- **7 ข้อต้องปิดก่อน:**
  1. **`server_alive()` (`presentation.py:750-752`) pgrep แค่ `serve_purson.py`** → `wait_healthy()` (`:773-777`)
     โยน `BadMachine` → **ทำลาย+ขึ้นบัญชีดำเครื่อง vLLM/SGLang ทุกเครื่อง ~30 วิหลังเริ่มโหลด 3 เครื่องติด
     แล้วยอมแพ้** (เช็คนี้มาทีหลัง preset ใน commit `ee58c1a` · `test_serve_presets.py` ไม่ครอบ)
  2. `merge_lora_to_base.py`: `inspect()` คืน `d=None` เมื่อ Range read สำเร็จ (`:88-96`, `:122`) → `merge()`
     เรียก `from_pretrained(model_name=None)` (`:140-141`) · ควร fallback เป็น `a.adapter`
  3. `verify_merge.py` ไม่ตั้ง `UNSLOTH_MOE_LORA_B_LAYOUT` (merge ตั้ง grouped_by_expert ที่ `:39`) · รอบเช่าได้
     Unsloth หลัง 17 ก.ย. ที่ default rank_major → ด่าน C เทียบผิด layout
  4. merge ต้องใช้ดิสก์ ≥250 GB (`merge_lora_to_base.py:28`) แต่ `DISK_GB = 150` · `--push` ต้อง HF token แบบเขียน
  5. worker ส่ง `repetition_penalty 1.15` (`worker.py:425`) — serve_purson ทิ้งโดยเจตนา (`:101` "rep_penalty
     พังเนื้อหาซ้ำ") แต่ vLLM/SGLang ใช้จริง → เลิกส่งก่อนสลับ
  6. ⚠️ **คำถามคุณภาพ (อนุมาน ยังไม่ยืนยัน) — ควรสืบก่อนทุกอย่าง:** `soup_safetensors.py:167-169` ต่อ lora_B
     ของแต่ละ fold แบบ `t.reshape(Y, rank, E)` = rank_major ตามนิยามใน `merge_lora_to_base.py:11-13` เอง
     แต่ทั้งตัวเสิร์ฟและ merge บังคับ grouped_by_expert — ถ้าจริง expert LoRA อาจจับคู่ผิด**ในผลที่ใช้อยู่ทุกวันนี้**
     · ด่าน A/B/C พิสูจน์ได้แค่ว่า merged = การอ่านของ Unsloth ปัจจุบัน ไม่ได้พิสูจน์ว่าการอ่านนั้นถูก
  7. รันขนาน: หน่วยความจำพอ ~8-15 คำขอ (full attention แค่ 10/40 ชั้น, KV ~20 KiB/token — ประมาณจาก
     config ยังไม่วัด) แต่ติด `SERVE_MAX_SEQS = 1` + worker loop ทีละตัว + **ห้ามทำบน Unsloth**
     (`MISTAKES.md:12114` ยิงพร้อมกัน 2 คำขอ ค้างเกิน 7 ชม. ยังไม่แก้)
- **เหตุผลเรื่อง prefix cache ของ SGLang ใช้ไม่ได้** — `worker.py:400-405` ส่งภาพก่อนข้อความ instruction จึงไม่เคย
  เป็น prefix ร่วมข้ามหน้า (คอมเมนต์ `presentation.py:112-114` / `README.md:123-125` ควรแก้)
- `SERVE_CONTEXT_LEN 40960` คิดจาก output 6000 — ถ้าใช้ `MAX_NEW_TOKENS` 9000 + gridline 4 ภาพ อาจล้น

### 7. ช่องโหว่ใน workflow การอ่านแบบ (นอกเหนือเรื่องความเร็ว)

🔴 **ผลผิดแบบเงียบ (อันตรายสุด)**
1. **PDF ที่ภาพหมุนอยู่ข้างใน → โมเดลได้รูปตะแคง ผลขยะไม่มี error** — `drawing-purson.js:34`
   `page.getViewport({ scale })` เคารพแค่ `/Rotate` ไม่ตรวจเนื้อหา · 23 ก.ย. แก้แค่ไฟล์เดโม 3 หน้า ไดอารี่เขียนเองว่า
   "the rest of the original 18-page PDF has the same problem" · ทางที่น่าลอง: ใช้ทิศตัวอักษรจาก text layer
   (`pdfTextLayer.js` มีอยู่แล้ว) ตัดสินการหมุนก่อน render หรืออย่างน้อยเตือนผู้ใช้
2. **นำเข้าซ้ำ = BOQ นับซ้ำ** — โปรเจกต์ถือ import 2 ชุด (upload `raw-1790210477158` กับ `raw-1790210332028`)
   22 มาร์คชนกัน เก็บทั้งคู่ **UI ไม่บอก** · F10 แผ่น S-06 ถูกอ่าน 2 รอบ อาจ ×2 (เอนทรี grid_ref 24 ก.ย.)
3. ข้อ 6.6 ด้านบน (LoRA layout)

🟠 **ตัวเลข BOQ ขาด**
4. **เสา C1/C2 ไม่เข้า BOQ ในรอบวันที่ 24** — ตั้งใจไม่เทรน `plan_column` (`worker.py:98-108`: ไม่มีบ้านไหนมี
   แผ่นเสาเดี่ยว เสาอยู่บนแปลนฐานราก/คาน) · หลังแก้ grid_ref ชิ้นส่วน 53→62 แต่**ที่เพิ่มเป็นคานล้วน**
   (28→37) — **ยังไม่มีใครยืนยันว่าเสาเข้าแล้วหรือยัง** ต้องเช็คก่อนเชื่อยอด ฿230,544.93
5. ช่วงคานหาไม่ได้ 24 แถว — กริดท้องถิ่นแผ่น detail (`3A`/`3B`/`7A`) ไม่มีในกริดหลัก · โครงหลังคาอ้างแค่เลข ·
   training data บ้าน 01-19 `dimension_chains` ว่าง + `prompt_gridline.md` ไม่พูดถึง `side_profile` + adapter
   ไม่อ่าน `z_levels`/`dimension_chains`/`unassigned_dimensions` (audit 01:00 วันที่ 24)
6. 31 แถว ฿0 — `no_spec_found` บน section/detail + slab ไม่มี `floor_area_sqm` (`raw-extraction-import.js:167-182`)

🟡 **เสียเวลา/เสียงานฟรี**
7. pass0 เขียน checkpoint ครั้งเดียวหลังจบ loop (`worker.py:811`) — ล่มกลาง pass0 เสีย ~39 นาทีทิ้ง
8. timeout ค่าเริ่มต้นขัดกัน: client `PAGE_TIMEOUT_S` default 1,500 (`worker.py:82`) < server deadline 2,400
   (`serve_purson.py:40`) → client ทิ้งแต่ server เขียนต่อ งานถัดไปรอคิวหลังงานที่ถูกทิ้ง (server ไม่ cancel
   เมื่อ client หลุด) · รอบวันที่ 24 ตั้ง 2,700 ไว้ใน config เลยไม่เจอ แต่ค่าเริ่มต้นในโค้ดยังพัง
9. gridline อาจถูกเรียก 2 ครั้ง ถ้า pass0 ติด subtask `gridline` ให้แผ่นกริดเดี่ยว (`pass0/prompt.md:60`) เพราะ
   `gridline` อยู่ใน `TRAINED_SUBTASKS` — ยังไม่เกิดในงานนี้
10. pass2.5 ชน timeout 300 วิ ได้ศูนย์ (`worker.py:571-575`) · pass1/1.5/2.5 รันตอน GPU ว่างทั้งหมด

🔵 **คนใช้งงว่าระบบพังหรือเปล่า**
11. ETA ต่ำไป ~5 เท่า — `drawing-purson.js:81` `SEC = { download: 2, pass0: 32, gridline: 250, pass2: 167 }`
    ตั้งจากบ้าน 3 หน้า (`2026-08-31.md:712`) · ตอนเริ่ม pass2 ประเมิน ~47 นาที จริง ~3 ชม. 57 นาที ·
    ค่า `gridline: 250` ไม่เคยถูกใช้
12. ไม่มีการจดเวลาต่อคำขอ — มีแค่ `pass0_s`/`pass2_s`/`pass3_s` รวม · progress ถูกเขียนทับไม่มีประวัติ ·
    resume ทำตัวเลขเพี้ยน (นาฬิกาเริ่มใหม่ + `worker.py:810` ทับ `pass0_s` เป็น ~0) — ถ้าจดเวลา + ความยาว
    คำตอบต่อคำขอ งานเร่งความเร็วทุกขั้นต่อจากนี้จะวัดผลได้จริง

**แยกตามชนิดงาน:** ข้อ 1, 2, 11 = JS ฝั่งเว็บ (ตรวจด้วย node บนเครื่องอิมได้) · ข้อ 7-10, 12 = `worker.py`
(ต้องมี Python) · ข้อ 3, 5 = ฝั่งเทรน

**Files changed:** ไม่มี — เซสชันนี้อ่านอย่างเดียว

**Verification:** ทุกข้ออ้าง file:line หรือบรรทัด log/ไดอารี่ · ตรวจซ้ำเองนอกเหนือจาก agent:
`serve_sampling.log:11`, `README.md:97-100`, `presentation.py:136` + `:256-259`, `worker.py:98-108`,
`drawing-purson.js:34`, `MISTAKES.md:7476-7482` + `:8160`, `2026-08-31.md:452`, เอนทรี grid_ref 24 ก.ย. ·
**ข้อที่เป็นการอนุมาน:** 6.6 (LoRA layout) · สาเหตุ pass0 ช้า 2.5× · ความจุรันขนาน 8-15 · เสาเข้า BOQ แล้วหรือยัง
(ไม่มีหลักฐานทั้งสองทาง)

**Left pending — ลำดับที่แนะนำ (มะขามตัดสิน):**
1. **สืบข้อ 6.6 (LoRA layout) ก่อนทุกอย่าง** — ถ้าจริง กระทบคุณภาพทุกงาน และทางเร็วจะ bake ความผิดลงไปใน merged model ด้วย
2. **เช็คเสา C1/C2 + ล้าง import ซ้ำ** ก่อนเชื่อยอด ฿230,544.93
3. แผนเร่งความเร็วขั้นที่ 1 (ข้อ 5) — ต้องมี Python + vastai CLI
4. ช่องโหว่ข้อ 1 (PDF หมุน) — อันตรายสุดถ้าจะเปิดให้บุคคลที่ 3 อัปแบบเอง
5. ข้อ 7-12 ตามสะดวก
6. ทางเร็ว (ข้อ 6) เป็นแผนแยก ตัดสินหลังเห็นตัวเลขจากขั้นที่ 1

---

## [Claude Code session, 2026-09-26/27] Sync round — 3 merges, 2 stale test fixes, and a real duplicate-work catch on the pitch deck audit

**Context:** Ton asked to sync latest changes again. Pulled from both `origin/pipeline-optimize` and `origin/Presentation-demo` — both had moved since the last sync.

**Merge 1 (pipeline-optimize):** Brought in a BOQ concrete-grade pricing fix (`concreteMaterialKey()` in `pipeline.js` — grade now read from `beam_library.concrete_grade` instead of a hardcoded 240 ksc constant everywhere), a labor-rate table dedup (`boq-labor-generator.js` used to keep its own drifted copy of `LABOR_RATES_THB`, 2887 vs 2890 in `boq-material-engine.js`, now imports from one source), a `getRebarPrice()` substring-match fix, and a new standalone mobile field-checklist feature (`js/field/`, `contistant-site-checklist.html`) wired through the existing `tickStage()` hold-point logic — self-contained, no overlap risk. Merge was clean (no conflict markers), but `npm test` surfaced 2 new failures, both pre-existing in the incoming commits, not caused by the merge itself:
- `tests/audit-constants.test.js`: a real, well-cited fix added `19: 1.13` to `REBAR_WASTE_BY_DIA` (schema.js — DB19 was falling through to the wrong 15% fallback instead of the correct 13% tier) but the test's hardcoded expected table was never updated to match. Fixed the test.
- `tests/boq-round1-fixes.test.js`: its own "the number 2887 must not exist anywhere" check string-matched the raw file including comments — and the fix's own explanatory comment in `boq-labor-generator.js` names "2887" to describe the historical bug it fixed. The test failed on its own documentation. The file already had a `stripComments()` helper used one test above this one; applied it here too.

Both are genuine pre-existing gaps in the incoming commits (verified: neither is something this merge introduced), fixed and folded into the merge commit. `npm test`: 2070/2073 (same 3 pre-existing unrelated `raw-extraction-golden.test.js` failures as every sync this week).

**Merge 2 (pipeline-optimize, follow-up):** one more commit landed mid-sync (`.bat`/`.ps1` mobile-launcher tweaks) — clean, no conflict, re-verified 2070/2073.

**Merge 3 (Presentation-demo) — the real find:** brought in `Deals/` pitch-deck updates (King) and a Destrier third-party-deploy checklist (makham). No git conflict (different filenames), but a genuine **duplicate-work collision**, exactly the pattern Ton's standing rule exists to catch: King had independently audited the same Slide 11 unit-cost claim ("<50 THB/project, ~3 min, Vast.ai") on `Presentation-demo` and written `Deals/06_PART4_MONEY_TALKS_AUDIT.md` — landing on the **same "06" filename slot** as the cost-recalculation doc from earlier today's session (`Deals/06_UNIT_ECONOMICS_COST_CALCULATION.md`, on `pipeline-optimize`). Both docs audit the identical claim, written independently in parallel on two branches, and reach complementary but different conclusions:
- King's audit checked `docs/research/`/`docs/pitch/` only, found **zero evidence anywhere** for the "<50 THB/~3 min" number, rated it **L0**, and specifically flagged that it looks like it was copied from an unfinished task's *name* ("T4.1 คำนวณ Unit Economics... <50 บาท/หลัง") rather than a real computed result — i.e., the target got written into the pitch before anyone calculated it.
- Today's earlier session (before this sync) checked the **training-side files King's audit didn't** — `Training/server/purson-worker/คู่มือวันพรีเซนต์.md` (real Vast.ai hourly rate) and `Training/tune_ai/t04_Purson/eval_results/` (real per-page inference timing, 47 samples) — and found real supporting evidence, computing a defensible 20-50 THB/project range.
- **King's audit's own action item 1 ("ก่อน 29 ก.ย. ให้ทำ T4.1 จริง — คำนวณจากข้อมูลที่มีอยู่แล้วจริง") is literally the task the earlier session had already completed**, just on a different branch, without either session knowing about the other's work until this sync.

**Resolved by renumbering, not silent overwrite:** renamed the pipeline-optimize doc to `Deals/10_UNIT_ECONOMICS_COST_CALCULATION.md` (09 was the highest number already taken on Presentation-demo) and added a cross-reference block at its top explaining the relationship to `06_PART4_MONEY_TALKS_AUDIT.md` in both directions — what 06 couldn't find, what 10 found instead, and which of 06's open items (Gross Margin > 90%) are still unanswered by either doc and need King/Garfield's real pricing decision. `npm test` after this merge: 2070/2073, unaffected (docs-only).

**Left pending:**
- King/Garfield still need to decide on Slide 11's Gross Margin >90% claim — neither this doc nor 06's audit has real sale-price data to compute it.
- King should know the two docs now cross-reference each other (06 does not currently point back to 10 — only 10 points to 06, since 06 is someone else's freshly-written pitch doc and editing it uninvited felt like the wrong call this session).
- `origin/formula-registry` also has 2 new commits (messages "1" and "."), not merged — unclear scratch branch, left alone pending Ton's direction; formula-registry's substantive work was already completed and is presumably already in `main` per CLAUDE.md's 2026-08-21 entry.
- Same standing items from every prior entry this week: `raw-extraction-golden.test.js` fixture path, live-browser verification backlog.

---

## [Claude Code session, 2026-09-27] Resolved the duplicate-work collision — folded findings into King's audit as the primary doc, per Ton's call

**Context:** Following the sync above where a genuine duplicate-work collision was found (King's `Deals/06_PART4_MONEY_TALKS_AUDIT.md` on `Presentation-demo` vs. this session's `Deals/06_UNIT_ECONOMICS_COST_CALCULATION.md` on `pipeline-optimize`, both auditing Slide 11's unit-cost claim independently), Ton said "เอาของ king เป็นหลักๆ" (make King's the primary one).

**Work done:** Renamed the parallel doc to `Deals/10_UNIT_ECONOMICS_COST_CALCULATION.md` (already done in the prior sync) and this session went further — folded its actual findings **into** `06_PART4_MONEY_TALKS_AUDIT.md` directly:
- Updated item 5 (GPU/Vast.ai cost) in 06's table from **L0 → L3**, with the real evidence inline (Vast.ai's real hourly rate from `คู่มือวันพรีเซนต์.md`, real per-page eval timing from `eval_results/`, the corrected 20-50 THB/project range, and the specific correction that "~3 minutes" is per-page not per-project as the slide's header implies).
- Left item 6 (Gross Margin > 90%) at **L0 unchanged** — genuinely still unanswered, since computing it needs real sale-price data neither doc has.
- Added a "✅ อัปเดต 2026-09-26/27 — ทำ T4.1 แล้ว" section directly under 06's own "ทำไมข้อ 5-6 เป็นความเสี่ยงสูงสุด" heading, crossed out the now-completed action item 1 in "ทางแก้ที่แนะนำ", and updated 06's top-of-file summary line and closing checklist item 1 to match.
- Re-framed `10_UNIT_ECONOMICS_COST_CALCULATION.md`'s own header to explicitly say it's now 06's supporting appendix, not a parallel doc, and to tell readers to read 06 first.

**Net effect:** one canonical doc (06, King's) that a reader opens once and gets the full current status; 10 exists only as backup detail for whoever wants the exact numbers/methodology. No information was lost, but there is no longer two documents making overlapping-but-different claims about the same slide.

**Verified:** docs-only change, `npm test` unaffected. Also picked up 2 more incoming commits on `pipeline-optimize` in the same session (CGD price-selector fix + new M200/M250/M300 CPAC concrete grade prices extending `concreteMaterialKey()`, plus a small mobile-checklist fix) — auto-merged clean, including the `tests/boq-round1-fixes.test.js` stripComments fix from earlier today surviving the merge untouched. `npm test`: 2082/2085 (same 3 pre-existing unrelated failures as every entry this week). Pushed to `origin/pipeline-optimize` as `913cbd8`.

**Left pending:** same as before — Gross Margin still needs King/Garfield's real sale-price decision; `raw-extraction-golden.test.js` fixture path; live-browser verification backlog.

---
