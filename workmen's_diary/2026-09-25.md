# 2026-09-25

## [Claude — session with Mail (Mel)] Committed + pushed yesterday's 3D structure-model fixes; explained what `npm test` actually verifies

**Context:** Continuation of the multi-day `pipeline-optimize` work logged throughout `2026-09-24.md`. Two small, distinct asks today: (1) commit and push the pending `structure-model.js` edge-case fixes that were still sitting uncommitted in the working tree from yesterday's session, and (2) after seeing "npm test: 1983/1986 passing" quoted before every push, Mail asked directly what that test count actually means — worth recording since it's a recurring point of confusion and the answer matters for how much trust to put in every "tested" claim in this diary.

**Work done:**
1. **Committed and pushed the structure-model.js fixes from the previous entry.** Before committing, found the `pipeline-optimize` branch had already picked up several merge commits from `origin/Presentation-demo` (purson-worker sync commits, `9afda0b`/`c591b6a`) that weren't part of this session's own work — branch was already `up to date with origin/pipeline-optimize` before touching anything, so these were pre-existing on the remote, not something this session introduced. Staged only the three files actually changed this session (`schema.js`, `structure-model.js`, `tests/structure-model.test.js`), re-ran the full suite once more on top of the current tree to confirm nothing regressed (1983/1986, same as before — the merge commits didn't touch anything this session's fix depends on), then committed with the full reasoning inline (z-fighting fix, multi-storey ground-slab fix, `elevation_offset_m`, and a summary of the two external-review triages) and pushed straight to `origin/pipeline-optimize` (`88e80b8`) after confirming no new commits had landed upstream in the meantime.
2. **Explained what the `npm test` number actually means**, since "X/Y passing" had been quoted after nearly every change today and Mail wanted to know what it was actually proving. The honest answer, recorded here because it's a recurring caveat worth having written down once properly rather than repeated informally each time: `npm test` runs the repo's Node-based unit test suite (~1986 cases across ~12 files under `tests/`) — each case feeds small, hand-constructed sample data into a pure function (e.g. `buildStructureModel()`, `computeBOQ()`) and asserts the output matches a value that was hand-calculated before the test was written. This verifies the **math/logic layer only**: no browser, no WebGL, no real project data, nothing rendered on a real screen. A green test suite means "the formulas compute what they're supposed to for the sample cases checked" — it does not mean "opened the real app and it looks right," which is the separate, still-unresolved verification gap this whole thread of work has been flagged against (see the "waiting on a live web deployment" note in the 2026-09-24 entry above). The 3/1986 failures are the same pre-existing, unrelated `raw-extraction-golden.test.js` fixture-path issue documented in every prior entry.

**Files changed:**
- `Constistant/js/shared/schema.js`, `Constistant/js/shared/structure-model.js`, `Constistant/tests/structure-model.test.js` — committed as `88e80b8` on `pipeline-optimize` (full technical description already in that commit message and in the 2026-09-24 diary entry above; not repeated here)
- This diary file (new)

**Impact/precautions:**
- `pipeline-optimize` is now fully synced with `origin/pipeline-optimize` (nothing local left uncommitted) — anyone pulling it gets the complete 3D-model fix set from yesterday plus everything already merged in from `Presentation-demo`.
- Worth remembering for future entries in this diary: "tests passing" should keep being read as "verified at the formula/logic level," not as "verified live" — the two are being kept explicitly separate in this project's own convention (see CLAUDE.md's own repeated "ยังไม่ได้ verify สดในเบราว์เซอร์" notes across its history), and today's conversation reaffirmed why that distinction matters in practice.

**Left pending / to continue:**
- Still waiting on a real, seriously-connected live web deployment (Makham's dependency, per yesterday's entry) before any of this session's or yesterday's work can be checked beyond the unit-test level.
- Merging `pipeline-optimize` with Ton's and the teammate's parallel work is still explicitly waiting on Mail's go-ahead, unchanged from yesterday.

---

## [session, exact clock time not logged] Tonton (Claude Code session)

**Work done:**
- Synced `pipeline-optimize` with the rest of the team's work across two rounds today, both requested by Ton ("sync change ล่าสุด"). Ended up needing real merge-conflict resolution, not a clean fast-forward, because of concurrent work on the same rebar-shape logic — documented in detail below since it's a repeat of the exact duplicate-work risk this diary and Ton's standing rule exist to catch.
- **Round 1 (small):** pulled 2 new commits from `origin/Presentation-demo` (makham) — a BIM/IFC export subsystem (`building-model.js`, `ifc-element-map.js`, expanded `ifc-export.js`) and an unrelated but real bug fix: `computeSchedule()` used to crash with an unreadable `RangeError: Invalid time value` when `project.start_date` was empty/invalid; now throws a Thai message telling the user to go fill in Input Hub's "ข้อมูลทั่วไป" instead. Clean auto-merge, `npm test` 1971/1974 (3 pre-existing unrelated failures), pushed.
- **Round 2 (small):** pulled 24 more commits, almost entirely purson-worker/vast.ai GPU-rental orchestration (ssh-hang guards, price ceiling, blacklist for dead rental boxes, token-limit recovery, etc. — makham). One item worth flagging for the BOQ-accuracy thread this branch cares about: `raw-extraction-adapter.js` gained a cross-file double-count detector — Purson's worker sometimes reads the same drawing sheet twice via two separate sub-prompts (`plan_footing`/`plan_beam`) and the model reports the same mark's count back both times; the adapter used to silently sum them (real incident 2026-09-23, job `cbb90abe`: a footing's count went 4+8=12, inflating that floor's concrete/rebar/formwork ~3x). Fixed as a warning surfaced on the raw-extraction review checklist (which also gained visible count/span columns it didn't have before) rather than a silent auto-resolution, since the adapter can't know which read (4 or 8) was correct. Clean auto-merge, 1977/1980, pushed.
- **Round 3 (the real one) — Ton asked to sync again and this fetch pulled in a much bigger, riskier set of changes:**
  - Mail had pushed **4 commits directly to `pipeline-optimize`** itself (not just `Presentation-demo`) — `f1534f3` "rebar cut-length hooks for ground/roof columns + wire dense stirrup zones", `cd5eb0a` "revive real 3D model, replace decorative showcase pipeline", plus a merge and `88e80b8` "stop beam/slab z-fighting, fix multi-storey ground slab placement". Fast-forwarded cleanly.
  - `origin/Presentation-demo` had **16 more commits**, including a large **repo-wide reorganization by King** (`f057a8e`, "chore: reorganize project structure and update local components") that moved almost every top-level file/folder — `missing.md` → `docs/raw-json-gaps.md`, `finetune_output_contract/` → `data/finetune-output-contract/`, CSS files, `README.md`, `contistant.html`, and a brand-new `Deals/` folder (pitch/investor materials, unrelated to app code) — plus more of makham's purson-worker work and two `ark`/makham commits titled literally "แก้bbs" / "แก้bbsของอันแรกจามเสี่ยเมล" (fixing BBS, per Mail).
- **This produced 16 real conflict hunks across 4 files** (`js/shared/pipeline.js` — 9, `js/shared/bbs-shape-rules.js` — 3, `js/boq/boq-summary.js` — 2, `tests/bbs-shape-rules.test.js` — 4). Root cause, confirmed by reading both sides: **Mail independently fixed the exact same bug (dense stirrup zones + ground/roof-column rebar hooks) on two different branches on the same day (2026-09-24)** — once via the direct push to `pipeline-optimize` (`f1534f3`), once via `Presentation-demo` — with different variable names and, in Presentation-demo's version, extra validation (a `tieCountConflictNote` when a fixed tie-count contradicts the stated spacing, and a `denseZoneNote` explaining the computed zones). Mail had even left a warning doc for exactly this (`bbs-pipeline-fixes-2026-09-24.md`, committed to `pipeline-optimize`): *"ถ้า branch ของเพื่อนแก้ฟังก์ชันพวกนี้เหมือนกัน... มีโอกาสชนกันตอน merge สูง — ควรดูของฝั่งนี้ก่อนเริ่มแก้ซ้ำ"* — the warning fired exactly as written, just after the fact rather than before.
  - **Stopped and asked Ton before touching any of the 16 hunks**, per his own standing rule to flag duplicate work with Mail. Ton's answer: "ขอ sync ให้เท่า ข้อมูลที่ mail มีพอ" (just sync to match what Mail has).
  - First attempt: resolved all 4 conflicted files with a blanket `git checkout --theirs <file>`. **This was a real mistake, caught immediately by running the test suite, not by review**: `checkout --theirs` on a conflicted file replaces the *entire* file with the incoming side, not just the conflicting hunks — it silently deleted Item 5's `resolveEffectiveSpec()` gate from `computeBOQ()` (committed earlier this week as `72da02f`) even though that code had nothing to do with the conflict and had already auto-merged correctly before being clobbered.
  - Aborted (`git merge --abort`, safe — nothing had been pushed) and redid it properly: resolved `js/shared/bbs-shape-rules.js` + its test with `--theirs` (confirmed via `git show 72da02f --stat` that Item 5 never touched these files, so no risk there); resolved `js/boq/boq-summary.js` and `js/shared/pipeline.js` by hand, per-hunk, combining both sides' additions rather than picking one — a second, cruder regex-based per-hunk resolution attempt on `pipeline.js` was tried first and **also failed** (a hunk boundary happened to sit in the middle of a larger, non-conflicting block — `computeBBS()`'s per-piece loop opening, the `resolveEffectiveSpec()` call, `if (!lib) continue`, and several `const` declarations — so taking "theirs" for just that hunk deleted the loop's opening brace while its matching closing brace survived, producing an `Illegal return statement` syntax error caught by `node --check`, not by eye). Final approach: took Mail's full `pipeline.js` as the base (syntax-valid on its own, confirmed with `node --check` before touching it) and manually reapplied Item 5's small, well-understood diff (the `resolveEffectiveSpec()` gate in `computeBOQ()`, the `specNotExtracted` flag threaded through `status`/`review_reason`, `rebar_source`/`rebar_source_note` on all three per-element BOQ rows) using targeted edits instead of blind hunk-taking.
  - **Found and fixed a genuine merge artifact, not something I introduced**: `tests/bbs-summary.test.js` auto-merged with zero conflicts (git saw no overlapping lines) but the result was internally self-contradictory — one 2026-09-24 fix had added a `B2`/`F3` beam specifically so column `C1` would sit at a middle floor and classify as a plain `ST` shape (keeping this aggregation test independent of roof-column shape-rule changes); a *different* 2026-09-24 fix's comment and assertion assumed `C1` *was* the roof floor and expected an `L-BEND` shape the fixture, as merged, could no longer produce. Two fixes, each internally consistent alone, combined into nonsense because they touched non-overlapping lines of the same test. Fixed by restoring the original intent (`ST` + `CLOSED-STIRRUP` only) and correcting a second, dependent staleness the same bug had hidden — the `total_bars` comparison used to exclude column rows from the expected count (back when `C1` was the odd-one-out `L-BEND` case), which is now wrong since `C1`'s main bar counts as `ST` too.
  - Verified with the real test suite at each step, not just by reading the diff — this is what actually caught both real mistakes above. Final state: **2037/2040 passing**, the same 3 pre-existing failures as always (`tests/raw-extraction-golden.test.js`'s fixture directory — see below, this reorg touched it but did not fix it).
- **Checked whether King's repo reorg accidentally fixed the long-standing broken golden-test fixture path — it didn't, it just moved the wrong thing.** The reorg relocated `finetune_output_contract/examples/` → `data/finetune-output-contract/examples/`, but that's the *old*, 7-file, wrong-pattern-vocabulary fixture set the test's own comment explicitly says not to use. Someone (makham, in the commits pulled today) already updated `tests/raw-extraction-golden.test.js`'s `FIXTURE_DIR` to point at `data/finetune-output-contract/model-output/examples/` — but that directory doesn't exist anywhere in the repo. The correct 8-file, new-pattern-vocabulary fixture set this test needs has never existed in this checkout, before or after today's reorg. Not fixed here (would mean fabricating test data); still an open, named gap.

**Files changed (Constistant repo):**
- `js/shared/pipeline.js` — merged; Item 5's `resolveEffectiveSpec()` gate in `computeBOQ()` manually reapplied on top of Mail's dense-stirrup-zone/ground-roof-column-hook rewrite of the same function.
- `js/boq/boq-summary.js` — merged by hand: combined our `rebar_source_borrowed`/`rebar_source_assumed_standard` `REVIEW_REASON_TH` entries with Mail's new `bent_shape_length_unresolved`/`stirrup_tie_count_conflict` entries; combined the `reviewFlagCell()` "strong flag" logic (Mail's `STRONG_REVIEW_REASONS` set check) with our tooltip enhancement (appending `rebar_source_note`).
- `js/shared/bbs-shape-rules.js`, `tests/bbs-shape-rules.test.js` — took Mail's version wholesale (confirmed no overlap with our work).
- `tests/bbs-summary.test.js` — fixed the self-contradicting merge artifact described above (separate commit `68dc69a`, not folded into the merge commit).
- Everything else in the diff (877 files touched overall across both merge rounds) is King's repo reorganization and makham's purson-worker/BIM work, taken as-is.

**Impact/precautions:**
- **The `checkout --theirs`-on-a-conflicted-file mistake is worth remembering as a pattern to avoid**: it looks like a safe, targeted per-file resolution but it actually discards the *entire* file's working-tree state (including cleanly auto-merged, non-conflicting content), not just the conflicting hunks. Only safe to use when you're certain the file has zero valuable content on the side you're discarding — verified via `git show <base-commit> --stat -- <file>` or equivalent, not assumed.
- **A hunk-boundary regex/scripted "take theirs per conflict marker" approach is also not safe** for a file with large, structurally significant hunks (loop openings, function bodies) — a hunk that looks self-contained in the diff can still depend on non-conflicting code just outside its markers. The syntax error it produced was only caught because `node --check`/`npm test` was run immediately after — this is exactly why "verify before declaring done" matters even for a git merge, not just for application code changes.
- **Auto-merged files are not automatically correct** — `tests/bbs-summary.test.js` had zero conflict markers and still shipped a self-contradicting assertion, because two same-day fixes on different branches touched non-overlapping lines of the same test with incompatible assumptions about the fixture. A green `git merge` exit code does not mean the result makes sense; only the test suite catching an actual assertion failure surfaced this one.
- Every merge step in this session was verified against the real `npm test` run (not just `node --check`) before pushing — this is the only reason both real mistakes were caught before landing on `origin/pipeline-optimize`.

**Left pending / to continue:**
- `tests/raw-extraction-golden.test.js` still cannot run for real — the correct 8-file fixture set (new pattern vocabulary: `grid_master`, `beam_plan`, `footing_plan`, two `section` files, `schedule`, `notes`, `material_list`) has never existed in this checkout. Whoever has access to the Training repo's real fixture data should either commit the correct set to `data/finetune-output-contract/` under whatever path the test expects, or fix the test's `FIXTURE_DIR` to point at wherever it actually lives.
- The duplicate-fix pattern that caused today's conflicts (Mail fixing the same bug on two branches the same day) is exactly the failure mode Ton's standing "check for duplicate work with Mail" rule exists to prevent — worth a direct conversation with Mail about which branch is the source of truth for BBS/rebar-shape work going forward (`pipeline-optimize` vs `Presentation-demo`), since right now both are live and both get direct pushes.
- `missing.md` is now `docs/raw-json-gaps.md` after King's reorg — same content, just moved; anyone who has it bookmarked by the old path needs to update.
- Not verified live in a browser this session — all verification was via `npm test` (2037/2040, 3 pre-existing unrelated failures) and `node --check`.

---

## [Claude Code session] Ton decided the 3 follow-ups from yesterday's problem list; `docs/raw-json-gaps.md` updated

**Context:** Continuing directly from the merge/conflict work logged above. After finishing the sync, I had summarized 3 outstanding items for Ton (framed earlier in the session as 🔴 recently-fixed-but-unverified 3D bugs, 🟡 an orphaned stub file, 🔵 two long-term structural data gaps) and asked which needed his decision. He answered all three via `AskUserQuestion`.

**Decisions made:**
1. **Orphaned stub `js/overview/overview-3d-modal(1).js`** — a dead, unreferenced leftover (confirmed via diff + grep: only the non-suffixed `overview-3d-modal.js` is ever imported) sitting in a file area Mail/makham are actively working in (the same "revive real 3D model" commit from `f057a8e`/`cd5eb0a`). **Ton's call: notify Mail, don't touch it myself.** Not deleted this session — flagging here so whoever talks to Mail next brings it up.
2. **`top_bottom_split`** (wiring `BeamLibraryEntry.main_bar.{top,middle,bottom}` — already stored in schema, just unread downstream — into the 3D spec card and IFC export) — genuinely cheap, app-only, no Training-team dependency. **Ton's call: hold off, not urgent** — specifically because `js/planner/planner-3d-view.js`/`ifc-export.js` are files Mail/makham just finished editing (24-25 Sep), and doing this now would be exactly the duplicate-work risk Ton's standing rule exists to catch. Parked as backlog, not started.
3. **`grid_refs` per-element grid position** (real grid *lines* exist via `GridReference`/`gridPosLookup()` since 2026-09-13, but no per-element field says which line a column/beam actually sits on — 3D/BIM still places pieces on a uniform, not-real-from-drawing grid) — **Ton's call: fold this into the same Training-team request as `docs/raw-json-gaps.md` items 1-2** (vertical-height field for columns/piles) rather than sending it separately, since it's the same ask (new extraction field) to the same team/file format.

**Work done:**
- Updated `docs/raw-json-gaps.md`: added a new §8 documenting the `grid_refs` gap (what already works — real grid lines via `GridReference`; what's missing — per-element grid assignment, never extracted by Qwen's Pass 2 survey step; a real inconsistency found in existing sample data where different files pair row/column letters oppositely, meaning any real extraction attempt needs a stated convention before starting, not after) and rewrote the closing summary (old §7 → §9) to explicitly say items 1-2 and 8 should go to the Training team together in one request. Doc-only change, no app code touched — `top_bottom_split` deliberately is **not** in this doc since it's an app-side wiring gap, not a raw-JSON schema gap.

**Files changed:**
- `Constistant/docs/raw-json-gaps.md` — added §8 (`grid_refs`), renumbered old §7 to §9, updated footer date note.
- This diary entry.

**Left pending / to continue:**
- Someone (Ton, when convenient) needs to actually hand `docs/raw-json-gaps.md` items 1-2+8 to the Training team as one combined ask — this session only prepared the doc, did not initiate that conversation.
- Someone needs to tell Mail about the dead `overview-3d-modal(1).js` file — not done this session per Ton's explicit choice to notify rather than act.
- `top_bottom_split` stays backlog until Mail's current 3D work settles — revisit then, don't start unprompted.
- Still true from the entry above: `tests/raw-extraction-golden.test.js`'s fixture path issue is unresolved; live-browser verification of yesterday's structure-model/3D fixes has not happened.

---
