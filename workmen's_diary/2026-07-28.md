# 2026-07-28

## [morning–afternoon] Claude — session with Makham

**Work done:** 3 separate topics — (1) checking house 07's raw JSON with Fable, (2) trying the tuned t01 model on house 01, (3) setting up t02 to compare against Qwen3-VL

---

### 1. Checked house 07's grid master + all plans (Fable, opening the real images and zooming on every point)

- grid master (`หน้า00`) — **correct, nothing to fix**
- 19 plan files — 14 fixed, 5 clean (A-04, A-05, A-06, SN-05 all passed)
- Fixed everything append-or-rewrite style, recording the reversal, never deleting the original warning

The important reversals:
- **S-02** GB1X is not the C4→B4 span (that's the second GB1) but a 1.60 m stub below B-4 → F1@B-4 sits exactly on the grid, not off-grid
- **S-03** the staircase zone is fully cleared, found 3 missing pieces (B2 in the middle of bay 3-4, the 5th SP panel, B6 at the ST2 edge); there are 3 S1 around the staircase, not 4
- **S-04** locked the positions using the chains actually printed — the second CS2 is 1.50 m from grid 3 (previously estimated at ~2.00), and the 1.60 at bottom right is measured from row B, not A → matching S-02's GB1X exactly
- **S-05** BS1 in the middle of the bay is ~2.1 m below C, not 1.1
- **S-06 + A-07 — the main roof ridge doesn't actually exist**; the 11.4×11.4 mass is a square, hip with 4 lines meeting at a single point. The two sheets had been "confirming each other" using two different look-alikes (S-06 = the centre rafter line, A-07 = section line A-A) → **agreement between two misreadings is not verification**
- **EE-03/04** switches (solid dots) were being counted in with light fixtures systematically; recounted: floor1 = 29 fixtures (group b missing entirely), floor2 = 19 (row d assigned to the wrong room)
- **EE-05/06** found 4 TV outlets ⊙ that had been missed entirely — matching the 4-way splitter diagram on the same sheet exactly
- **SN-04** recounted the inspection chambers as 11, not 8 + the pump label prints "HITASHI" (a typo in the drawing, it must be transcribed as-is; it had accidentally been normalized to HITACHI)
- **M-02/03** CEF-3 is in the bathroom, not the stair hall; the CU pair is lined up along the wall, not stacked; CU-2/01 sits on the metal-sheet roof (S-04 frame), not the tiled wing of S-05
- **A-30/31** the headboard is against the west wall; the "bed in the dressing room" is actually the sofa of bedroom 3

3 recurring error classes found (should be folded into op2's Stage 3 checklist): counting different symbol types mixed together / reading adjacency in a plan as physical stacking / look-alike lines masquerading as structural members

**Files:** `rawjson_ยังไม่ได้แก้ไขโดนคน/07บ้าน_ใหญ่_2ชั้น_01/*.json` 20 files (all pass JSON validation) · logged at `training-data/docs/raw_json_data_log.md`

---

### 2. Tried the tuned t01 model on house 01 → `tune_ai/t01/ผล/`

- Ran through **`llama-server`** (loads the model once) instead of `llama-cli`, because 61 pages would mean reloading 21GB for every page
- **Must use `--parallel 1 --ubatch-size 2048`**, otherwise you hit `non-consecutive token position` and it hangs without finishing (genuinely hit on the first run, had to kill it)
- `--ctx-size 16384` (the old 8192 made the output cut off mid-sentence: 1022 + 5120 + 2818 exceeds 8192)
- Got 2 pages, then **stopped on Makham's instruction** (the model eats so much VRAM the machine can't be used for anything else)
- Real speed: 273s and 409s/page (~3.2 t/s, split CPU/GPU because 21GB > 8GB VRAM) → 61 pages would take ~5.5-6.5 hours

**Files:** `tune_ai/t01/ผล/` (pages 01-02 + `_raw/`), the script is in the scratchpad (resumable, skips pages already done)

**⚠️ Caution:** house 01 is in the training set (62 examples) — the results will look better than reality. Usable as a smoke test, but not a measurement of generalization

---

### 3. Set up t02 — comparing Qwen3-VL against Qwen3.6 (Makham's instruction)

**Model chosen: `unsloth/Qwen3-VL-30B-A3B-Instruct`** — Makham picked it himself after asking "doesn't the different B value make the comparison invalid?"
Reason: same MoE, same 3B active, total 30B close to 35B → all variables controlled (a 32B dense model has a closer headline number but 10x different active parameters)

**The dataset was copied byte-identical, not rebuilt** — SHA-256 matches on every file + the rollup hash of all 398 images matches
Why rebuilding is forbidden: **7 files in house 05 were edited after the dataset was built** (including `หน้า00_gridline.json`) → rebuilding today would produce a different set, and then any difference in results couldn't be separated into "from the model" vs "from the data"

**Found 5 bugs in `train_qwen3vl.py`** (a file written when the dataset was still 221 examples, and never actually run):
1. `max_length` 10240/13312 → truncates gridmaster down to 42%/39% (cut from the tail = cutting the JSON label) — changed to 24576 in every preset
2. the collator's default `resize="min"` → Qwen3-VL has no `image_size` key at all, so it silently falls back to 512px = **25x loss of resolution**, with VRAM deceptively looking comfortable → forced `resize="max"` + an assertion checking the first batch
3. `max_seq_length` defaults to 2048, cutting through the middle of the image tokens (no guard like the audio side has)
4. no `enable_thinking=False` — the same bug that made t01 measure 0/3
5. the epilogue hangs holding VRAM → added `SKIP_DEMO=1`

**Everything verified for real** (not trusting memory): the model repo returns HTTP 200 (62.1GB) · none of the MoE variants have `bnb-4bit` at all → must train in bf16, which matches t01 exactly · GGUF+mmproj are both present (Q4_K_M 18.56GB + mmproj-F16 1.08GB) · llama.cpp has supported it since PR #16780 (b6887) and the build on the machine is b10105, which is newer

**Renamed the workflows to keep the rounds clearly separate, guarding against DAY OF SHAME:**
- `tune_ai/t01/data_before_tune/RETUNE_WORKFLOW.md` → `tune_ai/t01/t01_workflow.md` (git mv, preserving history)
- `tune_ai/t02/RETUNE_WORKFLOW_T02.md` → `tune_ai/t02/t02_workflow.md`
- The header of both documents states the round + status + a warning that a ✅ belongs to whichever round it's in
- `rule_of_tune.md` item 4 pointed at a path that had moved → changed to "that round's own document" + added the naming convention as a rule (why it's a rule: DAY OF SHAME came from believing work was finished, and reading a previous round's ✅ recreates that belief)

**New files:**
- `tune_ai/t02/t02_workflow.md` — same structure as t01 (Phase 0 → 11 hard block) + a variable-control table + decision criteria (must beat 28.2% recall)
- `tune_ai/t02/data_before_tune/DATASET_PROVENANCE.md` — checksum evidence + the reason rebuilding is forbidden
- `train_qwen3vl.py` / `eval_fields.py` / `verify_env.py` — fixed, syntax passes

---

---

### 4. t02's Phase 0 — checked for real (afternoon)

- **0.1 HF token ✅** created a new `constistant-tune_t02`, fired `whoami-v2` and got 200 with `repo.write` present · storage 29.6/100GB (the t01 repo is all there, **do not delete**, Phase 7.5 needs it) · **the token was pasted in chat → revoke after Phase 8**
- **0.2 credit ✅** $29.67 · the machine Makham found (Taiwan, RTX PRO 6000 96GB, $1.161/hr) passes every criterion, 10.5x more expensive than t01 but only ~$3 different in practice · **the thing to watch is RAM 76GB, not VRAM** (merging bf16 30B eats ~60GB of RAM; if it dies it gets killed silently)
- **0.3 re-check passed** 4 files MATCH + the 398-image rollup matches · the rollup number differs from what was noted the first time because it's a different formula, not because the data changed — the formula is now recorded alongside it to prevent the same confusion next time

**`max_length` = 24576 is decided** (Makham said "decide it for me") — this isn't a new decision; `train_qwen36.py:55` states plainly that Makham chose this number himself on 2026-07-24, so the 10240/13312 left over in the t02 files are the foreign values. If OOM hits, **do not lower it back**; reduce max_pixels → lora_r → rent a bigger card, in that order

**Found 3 more bugs by comparing hyperparameters one by one against t01** (a check that should have been done from the start and wasn't — all 3 are values t01 genuinely hit failures with and moved away from, but the t02 files still had the old values):
6. `lora_dropout=0.05` → **0** (t01: MoE ParamWrapper doesn't support dropout!=0, confirmed from a real error — 30B-A3B is MoE too)
7. `optim="adamw_8bit"` → **`paged_adamw_8bit`** (the old value is the one t01 hit OOM with)
8. **no `bf16=True` at all** → added (t01 states this MoE family requires a bf16 setup)

Made a 16-line parity table in §0.4 confirming the only remaining differing variable is genuinely the model family

---

### 5. Phase 0 fully closed, 0.1-0.6 (evening — Makham said "finish it and tick it off")

**0.4 — proving `max_length` by real measurement, not estimation.** Wrote a script computing visual tokens from every real image using Qwen3-VL's own `smart_resize` formula (factor 32 — confirmed from the real `preprocessor_config.json`, not from memory), across all 403 rows:
- Longest **22,515 tokens** (`03::gridmaster`, 4 images) = 91.6% of 24576 · **0 rows over the limit** · 2,061 tokens of headroom
- A real image of 3309×2339 → 2720×1920 = **5,100 tokens/image** (not exactly 5,120, because it rounds to a multiple of 32) → updated Phase 5 to state this number, otherwise seeing 5,100 would look like a failure
- Computed at 2.5 characters/token (conservative) — the real rate backed out from t01 is 3.2, which gives ~22,053, **matching the ~22,067 t01 computed for itself** = two independent methods landing on the same number

**Adapted the 4 scripts that were still t01's:**
- `export_gguf.py` — previously pointed entirely at Qwen3.6 · tied path/repo/size range to the same `MODEL_SIZE` used during training · added a check that `ADAPTER_DIR` genuinely exists before loading 62GB of weights · **blocks any `HF_REPO` ending in `/qwen36-thai-rc`** (overwriting it = Phase 7.5 is finished)
- `verify_env.py` — added **disk** (whole volume <280GB = forgot to drag the slider → `ok=False`) + **RAM from /proc/meminfo** + a check that `transformers` knows `Qwen3VLMoeForConditionalGeneration` (arrived in 4.57)
- `onstart.sh` — the old runbook said to "edit SFTConfig to add max_steps=5", which is wrong (t02 has `TEST_STEPS`) and referenced a file that doesn't exist · rewritten + `export MODEL_SIZE` into .bashrc
- `eval_fields.py` — only the default of `--adapter` fixed (the old `outputs/lora` doesn't exist); **not a single line of the measurement logic touched**, because it's the shared ruler for both rounds

**Deliberately not fixed even though it looks like a bug:** `eval_fields.py` doesn't pass `max_seq_length` — t01 genuinely ran this file and got JSON valid 90% on a val set containing a 22,515-token gridmaster. If it really truncated at 2048, every row would have failed, not 90% → proof that this path doesn't truncate. Fixing it unnecessarily = the numbers stop being comparable

**0.5 — verified down to the binary level.** No longer using a PR number as evidence; opened the actual binary on the machine (`b10105`): `llama.dll` contains `qwen3vl` + **`qwen3vlmoe`**, `mtmd.dll` contains `qwen3vl` = it genuinely can see images · real config: 128 experts / 8 used, patch 16 merge 2, default 16,384 tokens/image (3.2x too long if not overridden), **`vision_config` has no `image_size` key — direct evidence for bug 2, not an inference**

**0.6 — this machine can be rented, but RAM is the condition, not VRAM.** Set a new criterion of RAM ≥80GB (t01 never had one, because that round's machine happened to be enough) · the machine being eyed has 76GB, **below the criterion** → guarded in 3 layers: verify_env warns from Phase 3 / the adapter always goes up to HF before the GGUF / a silent `Killed` = RAM not GPU, fixed with a 64GB swap without retraining

---

---

### 6. Rented the real machine, ran Phase 1-7.5 in full + found a bug that reinterprets the whole day's results

The machine actually received is far better than advertised: RTX PRO 6000 96GB, **RAM 188GB** (not the 76GB the Vast.ai page number misleadingly suggested — see teach-mk), disk 300GB in full

**New bug found in Phase 4:** `AttributeError: property of 'Qwen2VLImageProcessor' object has no setter` — the `transformers` version pip actually pulled (5.5.0) has removed the `ip.max_pixels`/`ip.min_pixels` attributes (not merely made them readonly — they don't exist at all). Reading the real source confirmed that `ip.size["longest_edge"]`/`["shortest_edge"]` is the mechanism that still works — deleted the two broken lines from both `train_qwen3vl.py` and `eval_fields.py`

**Phase 4-7 all ran through:** baseline 0/0/0, matching t01 · Phase 5 (TEST_STEPS=5) passed all 4 checks · Phase 6 full training finished 120/120 steps with no errors, eval_loss 0.2076 but **2.9x slower than t01** (84:53 vs 29:23) · Phase 7 measurement of t02: JSON valid 65%, recall 20.6% — **loses to t01 on every metric**

**⛔ Before proposing to skip Phase 8 because it lost, I nearly repeated the DAY OF SHAME mistake:** I proposed "no need to export since it lost" without noticing that the adapter backup is bundled into that single Phase 8 — following that proposal literally would have meant no backup of anything at all. Immediately backed up the LoRA adapter (5.14GB) to `Sicilian44/qwen3vl-30b-thai-rc` by hand, outside the normal Phase 8 process, and verified the size matches through the HF API

**Ran a 43-agent audit workflow to check the soundness of the whole A/B round** (4 viewpoints: variable control / measurement fairness / deliverables / model identity → cross-cancelling 39 raw findings down to 20 genuinely confirmed) and **found a critical finding that flips the whole day:**

**`train_qwen36.py:164` (t01) calls the collator with pure defaults, no `resize="max"`** — the exact same bug as "bug 2", which was found and fixed on the t02 side only (the parity table in §0.4 has no collator row at all). **Verified for real on the rented machine (loading the real Qwen3.6, building a collator exactly like t01's):**
```
Unsloth: Model does not have a default image size - using 512
grid_thw [[1, 28, 38]] -> ~266 visual tokens     ← what t01 actually trained with
vs resize="max": ~3,796 visual tokens             ← what t02 trained with
```
**A ~14x difference** — t01 trained on far blurrier images and still beat t02 on every metric, meaning everything measured today **is not a fair comparison between model families**. It can only answer "Qwen3.6 wins even seeing coarse images", not "the Qwen3.6 family reads better than Qwen3-VL"

**Phase 7.5 (re-measuring t01 on this machine) is also done:** 90.0%/70.0%/29.4% — almost exactly matching t01's original numbers → this definitively closes the question of "different library versions on different machines" not being the cause (though it says nothing about the collator, because eval always forces the correct resolution)

**Offered Makham 2 options: (A) stop and accept the results with a warning attached, (B) retrain t01 with resize="max" for a fair comparison (~$0.60-1.80 more) — Makham chose A**

Edited `t02_workflow.md` to add a large warning box (Phase 7 + §0.0), retracted the phrase "clear win", and added **Lesson 12 to `rule_of_tune.md`**: a parity table must list every argument passed into a shared function/class, not just the hyperparameters you intended to change — a value left at its default is a variable too, and precisely because there is no line to compare, it is the easiest thing to miss

**Files:** `t02_workflow.md` edited in several places (Phase 1-7.5 all ticked ✅, collator warning box) · `rule_of_tune.md` Lesson 11 (never stack a GPU load on top of a real job that's running) + Lesson 12 (a parity table must cover defaults) · Phase 4/6/7/7.5 logs archived permanently at `tune_ai/t02/data_before_tune/logs_2026-07-28/` · wrote `2026-07-28(teach mk).md` teaching all of today's concepts

---

### 7. Phase 8 (export GGUF) — failed at the sanity check, diagnosed down to tensor level and confirmed to be a genuine tooling bug

Ran export_gguf.py — loaded base + adapter, merged successfully with no errors, **but the sanity check caught it immediately**: the post-merge output doesn't match the real schema at all (`page_number/title/project` instead of `png/doc_page/discipline/sheet_code/views`). The script stopped by itself as designed, without going on to quantize/upload garbage

**⛔ Found the earlier advice ("always upload the adapter first") to be wrong** — reading the real code shows the GGUF goes up first and the adapter last, so if the sanity check fails, nothing at all gets backed up through the Phase 8 route — **backups must always be done separately, never relying on Phase 8**

**Diagnosed deeply with 4 separate scripts** (Makham ordered "keep going... come hell or high water we have to get a result", i.e. chase the real cause down):
1. the adapter before merging → output matches the schema 100% exactly (the adapter itself has no problem)
2. the shape/orientation of the delta_weight peft computes → **correct**, matching param.shape exactly
3. the scaling factor → **correct** (2.0 = alpha/r)
4. the actual values across the whole tensor (201 million of them) before/after merge → 75.58% genuinely changed, but **abnormally small in magnitude** (0.67%, compared with behaviour that is completely broken)
5. checked Unsloth's own `_merge_lora()` → **doesn't support MoE ParamWrapper at all**, it would be even worse than using peft directly

**Conclusion: a genuine bug in peft/Unsloth's merge mechanism for target_parameters-style LoRA on MoE experts** — not a problem with the training craft or the dataset. The adapter is proven to genuinely work 100%, it just can't be converted to GGUF at this time

**Makham's decision 2026-07-28: stop.** No further hunting for a fix (3 main hypotheses already tested) — the LoRA adapter was backed up separately earlier (item 6). This is the "result" of the t02 round that Makham wanted (successfully tuned Qwen3-VL himself, with files to prove it)

### 8. Preventing DAY OF SHAME one last time — re-checking everything before closing out

Makham ordered "check everything again to prevent day of shame" — done in full:
- **Confirmed live again** (not trusting the earlier result) that the adapter is genuinely on HF at 5.15GB, straight through the HF API
- **Archived all logs/diagnostic scripts permanently** at `logs_2026-07-28/` (11 files — Phase 4/6/7/7.5/8 + 4 diagnostic sets + the scripts used)
- Checked the rented machine for anything valuable left behind (the mid-training checkpoints duplicate the final adapter that's already backed up, so unnecessary)
- Edited `t02_workflow.md` to match reality 100% for every Phase (8 ⛔ failed, 9 ✅ checked, 10 skipped, document header summarizing the round's closing status)

**Files:** `logs_2026-07-28/` complete with 11 files · `t02_workflow.md` Phase 8/9/10 + document header fixed

---

**Pending / to continue:**
- **The rented machine has not been shut down** — this requires an explicit instruction from Makham first, no exceptions (Phase 11 hard block). All the data is safe already whenever it does get shut down
- **Revoke the HF token** pasted in chat today (`constistant-tune_t02`) — not done yet, should be done after the machine is shut down
- **There is no GGUF for t02** — blocked by a genuine tooling bug (peft/Unsloth MoE merge), not something fixable in this session — continuing it would mean waiting for a new version of peft/unsloth or writing the merge ourselves (we don't yet know the right way)
- t01 was not retrained with resize="max" (option B of the A/B question was declined at the time) — to answer "which family is genuinely better" fairly, this has to be done first
- 2 things still can't be confirmed: the real VRAM of bf16 LoRA 30B-A3B (estimated from t01 at ~74GB, judged using the numbers from the TEST_STEPS=5 round) · whether Unsloth can export GGUF for this MoE VL model at all (issue #3899 reported corrupted GGUF with Qwen3-VL-2B)
- `export_gguf.py` in t02 is still t01's version, not yet adapted for Qwen3-VL
- house 01 has 59 pages left if we continue (resumable)
- **House 08 work** (`บ้าน_ใหญ่_3ชั้น_01`, 33 pages, a full set with no BOQ) has been selected but put on hold — op2's Stage 0 hasn't started

---
