# 2026-08-18

## [00:09] Claude — session with Makham

*Backfilled from git log — not logged live at the time.*

**Work done:**
- `op03`: house 22, `22บ้าน_เล็ก_2ชั้น_04` (บ้านครอบครัวไทยร่วมสมัย 9) — 94/94 pages, 106 files, ALL CHECKS PASS.

**Files changed:**
- `rawjson_ยังไม่ได้แก้ไขโดนคน/22บ้าน_เล็ก_2ชั้น_04/*.json` — new (106 files)

---

## [afternoon] Claude — session with Makham

**Work done:**
- Constistant: summarized the app's shipped feature set as a workflow (project setup → Site Investigate → Foundation Design → Drawing Intelligence → BBS → Structure Planner → Readiness Check → Resource Hub → Planner → Standards → Calculate Project → Overview/BOQ), cross-checked against live code (`contistant.html`/`shell-index.js`) rather than the stale CLAUDE.md alone — found "Structure Planner" is no longer an orphaned dead tab (CLAUDE.md's 2026-07-24 note is now outdated; it's a real wired-up Phase 3 tab as of `bc34a68`). Exported the summary to PDF via headless Edge (`--print-to-pdf`, no new dependency).
- Training: read everything on branch `King-try` (kingparadorn's branch) since it diverged from `main`. **Key finding: `King-try` diverged 2026-07-25 and is 277 commits behind `main`** — it predates the whole op01/op03 22-house campaign and the §0 FORMAT LOCK schema overhaul, and its own copy of `primary_rawjson_schema.md` (under the old `raw_json_ตัวที่ใช้งานจริง/` 5-house folder, not `rawjson_ยังไม่ได้แก้ไขโดนคน/`) is stale by dozens of rules.
- Reviewed `King-try`'s 12 new commits: a Gemini-overlay QC prototype (`tools/gemini_overlay_prototype/` — `extract_gemini.py`, `resolve_grid.py`, `diff_elements.py`, `render_overlay.py`, plus later additions `pixel_calibration.py` and `symbol_template_match.py` for drawing the overlay on the real scanned page). Confirmed it actually ran: real Gemini 3.1 Pro extraction on house 01 หน้า19 (footing/column plan) matched ground truth 100% (24/24, 0 wrong id, 0 missed) — screenshotted the on-image overlay as proof.
- Also reviewed: 2 new Claude Code hooks (`block-raw-json-edit.js`, `block-env-edit.js`), 2 new skills (`run-pipeline`, `tuning-data-guardrails`), 1 new agent (`schema-consistency-reviewer`), and `merge_raw_json_for_import.py` (bundles a house's raw JSON for Constistant's "นำเข้าไฟล์สกัดข้อมูล (JSON)" import — already run once against house 01).
- Switched checkout back to `main` after the review (no changes made on `King-try`).
- Backfilled diary entries for 2026-08-05, 07, 08, 09, 10, 14, 16 (op01/op03 house extractions 13-21 that were never logged live) — see those files.

**Files changed:**
- Training repo: none (read-only review of `King-try`) besides this diary backfill.
- Constistant repo: none (read-only summary + a PDF export written to scratch, not the repo).

**Left pending / to continue:**
- Decide what to do with `King-try`: cherry-pick just the overlay prototype + hooks/skills/agent onto `main` (recommended, given the 277-commit gap makes a real merge messy), or leave it as reference-only.

---

## [evening] Claude — session with Makham

**Work done:**
- `op03`: house 24, `24บ้าน_เล็ก_2ชั้น_08` (บ้านสำเร็จรูป 1, same "แบบบ้านสานฝัน" series as houses 01/02/03/05/07/22) — 59/59 pages, 70 files, ALL CHECKS PASS. House not specified by Makham (`op03 บ้านสักหลัง`); picked the smallest not-yet-done house (59 pages) to fit the 90-minute op3 ceiling — several others (`บ้าน_เล็ก_1ชั้น_07/13/14`, 130-133 pages) are still undone but too large for one op3 run.
- One real format bug caught by `check_format.py` and fixed: หน้า26.json had `spec.rebar` as an array of strings (should be a single object per spec §0.6) — changed to `spec.note`.
- Logged to `No_touch_box/docs/raw_json_data_log.md`.

**Files changed:**
- `rawjson_ยังไม่ได้แก้ไขโดนคน/24บ้าน_เล็ก_2ชั้น_08/*.json` — new (70 files)
- `No_touch_box/docs/raw_json_data_log.md` — logged the house

**Impact/precautions:**
- 8 of the pile-footing-system BOQ pages (หน้า47-54) were **not individually re-transcribed** — spot-checked the first/last sheet of both foundation-system BOQs, confirmed (matching house 22's precedent) that categories 2-4 are identical between the two systems, and wrote reference stubs instead of re-reading 8 near-duplicate pages. Every stub file says so explicitly in its own `warnings[]` — flagged, not hidden.
- Grid master required one judgment call with no prior spec precedent: two dummy grid lines anchored to the same named grid `1` from opposite sides (one before it, one between it and grid `2`, the second with no printed dimension at all — recorded `pos_m: null`) needed a naming scheme spanning both sides of the same named grid, not just one gap — documented in the grid file's `dummy_grid_rule_check`.

---
