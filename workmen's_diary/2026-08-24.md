# 2026-08-24

## [morning → afternoon] Claude — session with Makham

**Work done:**
- t01 tuned-vs-untuned comparison on house 11 (`บ้าน_เล็ก_1ชั้น_06`, never in t01 train/val), rented RTX PRO 6000 96GB, bf16 via transformers/PEFT (not GGUF — local GGUF path abandoned after measuring 23 min/page on laptop).
- New `tune_ai/t01/data_before_tune/run_house_batch_t01.py` — 2-phase (classify/extract), resume-safe, params copied from t01's own `eval_fields.py` exactly. Page scope cut from GT patterns directly (no model classify needed): 34 → 22 → 12 pages on Makham's live orders.
- Page 22 stalled 45+ min under pure greedy → added xgrammar + repetition_penalty (t02's proven fix) → page 22 done in 321s. Page 25 stalled AGAIN even with grammar → lesson: **grammar fixes "JSON won't close", not "generates too long"** → added `PAGE_TIMEOUT_S=25min` StoppingCriteria. Makham then cut all pages t01 couldn't finish.
- Final result: **6 pages both variants, JSON valid 100%** (tuned: 06/20/21/22/23/24 + smoke-test 04/11; base same 6). Base is often faster (p21: 121s vs 410s) — likely less verbose output, not smarter. Content quality: t01 on p22 found 5 beams / 0 columns vs ~13+ beam labels + columns visible on the sheet (GT has 16 beams) — VLM counting hallucination confirmed live, matches 2026-08-22 research notes.
- Results pulled to `tune_ai/t01/ผล_11/ผล/{tuned,base}/` — remote-vs-local verified file-by-file BEFORE destroy; instance destroyed clean (Mark-of-Shame discipline held).
- Workflow doc: `tune_ai/t01/ทดลองถอดแบบ_08_09_10_workflow.md` (scope pivoted to house 11 mid-day; filename kept, noted in header).
- ⚠️ Vast.ai price display showed $0.056/hr; DLPerf/$ math implies ~$1.16/hr — same class as the 30x misread on 2026-08-20. **Bill not yet checked.**

## [afternoon, Makham out — standing order under att1235] Claude solo

**Task 1 — schema-normalize `json_แก้ไขแล้ว/` (all 11 houses) to current `primary_rawjson_schema.md`:**
- 267 changes / 246 files, all 1,180 files re-validated with json.loads. Full log in `json_แก้ไขแล้ว/README.md` § 2026-08-24.
- Highlights: discipline `architecture`→`architectural` (157) + 6 off-vocab values; **`roof_plan`→`plan` on 8 houses' structural roof framing** (closes the live Constistant BOQ data-loss bug from t03/README); container §0.1 fixes (element→elements, steel_members/void_bays/stair_details/reinforcement/sheet_index → elements with proper element_type); notes §4a container unification; stirrup_or_tie→stirrup; rebar_cover_m→cover_mm.
- **Gridmaster §4 upgrade (the part Makham emphasized):** all 12 gridmasters got `grid.z_levels[]` harvested from the house's own reviewed per-page level data (4 storage shapes found: Thai-label keys, prose "ระดับ… +X.XX", level_labels_as_printed, levels_m dicts — last one flagged label-not-verbatim). `dimension_chains[]`/`unassigned_dimensions[]` deliberately NOT added — they are image-sweep transcription that hasn't been done; back-computing them from pos_m would fake provenance. Warning in every gridmaster states both facts.
- Deliberately untouched (logged): MEP/arch/index/symbol container drift (~132, pass-3 files), reviewer-invented element_type long tail (§0.4 permits).

**Task 2 — t03 training prep, all 11 houses:**
- New `tune_ai/t03/data_before_tune/build_dataset_t03.py` → **452 examples (408 train / 44 val=house03), 353 images**, per-subtask: section 223, schedule 66, notes 55, plan_beam 38, plan_footing 35, plan_slab 23, gridline 12 (multi-image from source_pages). material_list excluded (435, op04 ruling); pass-3 excluded (331).
- Decisions taken (all in builder docstring + workflow, for Makham to review): plan_column folded into plan_footing; plan_* structural-only; roof-framing types matched by regex; val=house03.
- New `tune_ai/t03/t03_workflow.md` (rule_of_tune §4 round doc) — Phase 0 checklist with what Makham must decide tonight: model choice, accept/override the 4 dataset decisions, gridline multi-image open question, whether unreviewed houses 06-11 stay in train.
- Integrity verified: all rows parse, 0 missing images, {{TARGET}} substituted, plan prompts embed grid master.

**Files changed:**
- `json_แก้ไขแล้ว/` — 246 files normalized + 12 gridmasters (+README log)
- `tune_ai/t01/` — `run_house_batch_t01.py` (new), `ทดลองถอดแบบ_08_09_10_workflow.md` (new), `ทดลองถอดแบบ_08_09_10_local_workflow.md` (superseded banner), `ผล_11/` (new, results)
- `tune_ai/t03/` — `t03_workflow.md` (new), `data_before_tune/` (new: builder + train/val jsonl + images/ + stats + manifest)

**Left pending / to continue:**
- **Check the real Vast.ai bill** for this morning's instance (price display suspect 20x).
- t03 tonight: Makham's 4 decisions (workflow doc) → train script adaptation from t02 (MAX_LENGTH must be computed from real token counts, prompts are ~12k chars now) → per-subtask eval script (doesn't exist).
- dimension_chains/unassigned_dimensions image sweep for 12 gridmasters — real page-by-page work, not scripted.
- Houses 06-11 content review against original drawings still owed (affects train GT quality; beam plans never checked for missing beams in ANY house).

## [evening] Claude — session with Makham (continued)

**Work done:**
- **Model decision closed: t03 trains Qwen3.6-35B-A3B** (Makham's call — NOT Qwen3-VL-30B that Claude had proposed as default).
- **New `tune_ai/t03/data_before_tune/train_t03.py` — written from scratch on Makham's order ("เขียนใหม่ หาข้อมูลในเน็ตมาด้วย")**, not copied from t01/t02. Every value carries a provenance tag: [W]=web-researched (Unsloth Qwen3.5/vision/MoE docs, "LoRA Without Regret"), [t01]/[t02]=machine-proven lessons (dropout=0 MoE, paged_adamw_8bit, ip.size mechanism, resize="max" collator bug, max_seq_length at from_pretrained), [t03]=measured this session.
- **MAX_LENGTH=32768 computed from real data, not guessed**: measured est. tokens of all 452 examples — 8 gridline examples exceed t01's 24576 (max ~26,791). The exact t01 §0.4 bug class, caught BEFORE training this time. Script asserts both visual-token count (anti-512px-silent-resize) and longest multi-image seq fit on first batch.
- FINETUNE_VISION=False kept deliberately — preserves the GGUF+official-mmproj export path (byte-identical vision encoder). Recorded as tradeoff; unfreeze is the A/B experiment for a follow-up run.
- **Makham's standing rule added: beam-plan pages must attach xgrammar at generation time, always** — implemented in train_t03.py's demo (demo picks ≥1 plan_beam sample, attaches grammar; graceful fallback if xgrammar absent) and pinned in t03_workflow.md as a permanent inference rule for the (not-yet-written) t03 runner. `pip install xgrammar` must go into rental onstart.
- **Process correction (logged to Claude memory):** Claude had framed its att1235 decisions as "รอมะขามเคาะ" in the workflow — Makham called it out ("ผมให้ att1235 ไปแล้วหนิ"). Fixed: all 4 decisions now documented as ตัดสินแล้ว ✅ + reasons + risks accepted (model=Qwen3.6; plan_column folded into plan_footing; gridline=multi-image→gridmaster; unreviewed houses 06-11 stay in train).
- **STECON competition strategy (Khon Kaen rival team, budget ฿2000 during training):** researched + answered "should we scale model B?" → **No.** Evidence: our own measured recall-vs-examples correlation (13 examples→11%, 24→86%); 2025-26 findings that fine-tuned 7B beats untuned 72B on document extraction; counting hallucination is architectural (SOTA models 64-75%), bigger B doesn't cure it. Budget plan ranked by measured impact: (1) more plan_beam data via op04 (+20 houses → 37→~99 examples, ฿0 GPU), (2) tonight's train + FINETUNE_VISION=1 A/B (~$3-6 both), (3) GroundCount-style OpenCV count hint in prompt (paper: 81.3% recovery, ฿0), (4) self-ensembling at inference (+23% relative, paper 2026), (5) run pass1 cropping (2x effective resolution, already written, never run), (6) grounding bbox probe (~$0.1). One rental covers train+A/B+probe for ~$5-8, >80% of budget left for the data round.

**Files changed:**
- `tune_ai/t03/data_before_tune/train_t03.py` (new, written fresh with web-sourced provenance tags)
- `tune_ai/t03/t03_workflow.md` — model ✅, script status 🔍, att1235 decisions section rewritten as ตัดสินแล้ว, xgrammar-on-beam-plan permanent rule added
- Claude memory: new `feedback_att1235_decide_fully.md`

## [ค่ำ-ดึก] Claude — เช่าเครื่องจริง + op04 มาถึงกลางคัน + เทรน t03 รอบเต็ม

**Vast.ai คุมผ่าน CLI ได้แล้ว** (มะขามใส่ API key "t02" เอง) — Claude search/rent/destroy/เช็คบิลเองได้
- **ไขปริศนาราคา 30x:** บิลเช้า $5.43 — **GPU จริง $1.1067/hr, ตัว $0.056 ที่หน้าเว็บโชว์คือเรท storage**
  กติกาใหม่: อ่านราคาจาก `vastai search offers` (`dph_total`) เท่านั้น ไม่เชื่อหน้าเว็บ
- ทดสอบ destroy จริงกับ instance ขำๆ ของมะขาม (2x GTX 1660 S) — คืนสำเร็จ ยืนยัน 0 instances
- เช่า **instance 48567519** RTX PRO 6000 WS 96GB **$1.002/hr** (offer 47725426, rel 0.995)
  image `vastai/pytorch:cuda-12.8.1-auto` + `t03/data_before_tune/onstart.sh` (เขียนใหม่จาก t02 + xgrammar บังคับ)
  env: unsloth 2026.8.19 / xgrammar 0.2.3 / transformers 5.5.0 / torch 2.11.0

**Parity table (rule_of_tune ข้อ 12) ทำก่อนเช่า — เจอ 2 ตัวแปรหลุดคุมจริง:**
`PYTORCH_ALLOC_CONF=expandable_segments` หายไป (t01 มี ใส่หลัง OOM ขาด 100MB) และ
`FINETUNE_VISION` โดน hardcode (t01/t02 เป็น env var) — แก้ทั้งคู่ ค่า default เท่าเดิมเป๊ะ

**TEST_STEPS=5 ผ่าน:** visual token **3,778/ภาพ** (ภาพจริง 2339×1654 ต่ำกว่า cap 5,120 = ไม่ย่อเลย)
· seq 21,445 < 32,768 · **~55 วิ/step** · loss 1.86 — **แต่ตายตอน eval**

**⛔ บทเรียนใหม่: eval ต่อ epoch OOM ทั้งที่เทรนไม่ OOM**
`evaluate → convert_to_fp32` (accelerate ห่อ forward แล้ว upcast output ทั้งก้อน) → logits
seq 21k × vocab 152k × 4B ≈ 13GB + ต้นทาง bf16 6.5GB → ขอ 24.72GB ไม่ได้ ตอนเทรนไม่เจอเพราะ
Unsloth ใช้ fused loss ไม่ materialize logits **t01 ไม่เจอ (ภาพย่อ 512px seq สั้น) · t02 ไม่เจอ
(30B เหลือ headroom) · t03 คือรอบแรกที่ 35B + ความละเอียดเต็มพร้อมกัน** → แก้ `eval_strategy="no"`
คง `save_strategy="epoch"` ไว้ (มี checkpoint 3 อัน ย้อนวัดได้)

**🔥 op04 ส่ง 39 หลังเข้ามาตอน 20:21 กลางงาน — dataset 452 → 1,116**
`json_แก้ไขแล้ว/` 11 → **50 หลัง** (`_scope.json`: scope pass2_only, 2026-08-23, agent 20 ตัวขนาน,
check_format ผ่าน) scope ตรง 7 subtask ของ t03 พอดี **และภาพครบทั้ง 50 หลัง**
**plan_beam 38 → 149** (คอขวดที่วิเคราะห์ไว้ว่าเป็นเลเวอร์อันดับ 1 ของ STECON)

**val split ใหม่ (มะขามสั่ง "4/1 ดีกว่า" — 10:1 เดิมแย่):** val = บ้าน **01-05 ทั้งหมด = ทุกหลัง
ที่คนรีวิวแล้ว** → **train 887 / val 229 = 3.87:1** ได้ผลพลอยได้: **val = GT รีวิว 100% /
train = op04 ล้วน** ไม้บรรทัดสะอาดทั้งอัน · plan_beam val 4 → **18**

**3 อย่างที่เจอตอน build ชุดใหม่ (บันทึก ไม่เดาแก้):** (1) `31บ้าน_เล็ก_1ชั้น_11 หน้า25_plan_footing.json`
ถูกตัดกลางไฟล์ (agent op04 โดน API session-limit ตัด ตามที่ `_scope.json` เขียนเอง) — builder ข้าม
พร้อมรายงานชื่อ ไม่ซ่อมโดยเดาค่า (2) 8 หน้า plan ไม่มี element ตรง subtask — ข้ามพร้อมรายงาน
(3) **1 ตัวอย่างประเมินเกิน MAX_LENGTH** — assert เดิมเช็คแค่ "ตัวภาพเยอะสุด" ซึ่งไม่ใช่ตัวยาวสุดจริง
**แก้ให้เช็ค 3 อันดับแรกจาก ภาพ+prompt+GT** → วัดจริงได้ seq **30,368 / 28,008 / 28,035**
เหลือ margin แค่ 7% ใต้ 32,768 (assert เดิมมองไม่เห็นตัวนี้เลย)

**เทรนเต็ม 3 epochs** (เครดิต $33.30 มะขามเติมแล้ว → งบไม่ใช่ข้อจำกัด)

**เทรนใหม่รอบ 2 (ที่ step 20 ทิ้ง ~$0.35) — มะขามสั่งเปลี่ยนบ้านทดสอบ 32 → 08**
เหตุผลมะขาม: บ้าน 08 มีผล t02 + Qwen เพียวๆ อยู่แล้ว (`t02/ผล/08.../` 25 หน้า) เทียบข้ามรอบได้
**ปัญหาที่เจอ: บ้าน 08 อยู่ใน train ของ t03 แต่ t02 ไม่เคยเทรนมัน** → ปล่อยไว้ตัวเลข t03 จะสูงเพราะ
จำได้ ไม่ใช่อ่านเป็น เทียบไม่ได้ทั้งดุ้น → **ย้ายบ้าน 08 เข้า val แล้วเทรนใหม่**
train 887→**854** (42 หลัง) / val 229→**262** (6 หลัง) = **3.26:1** · 321 step ≈ 5.3 ชม. ≈ $5.36
ผล: บ้าน 08 เป็น benchmark ที่ **ทั้ง 3 รอบไม่เคยเทรน** — t02 / Qwen base / t03 วัดบนพื้นเดียวกันจริง

**เขียน `infer_house_t03.py`** (runner ของ t03 ที่ยังไม่เคยมี): ป้อนจาก train/val.jsonl ที่อัปไปแล้ว
(ไม่ต้องส่ง GT/ภาพเพิ่ม) · โมเดลเห็นแค่ภาพ+prompt · **plan_beam แนบ xgrammar อัตโนมัติ** ตามกติกาถาวร
· วัด JSON valid + element count + id-recall แยกราย subtask · เตือนเองถ้าบ้านที่ยิงอยู่ใน train

## [ตี 4 - เช้า 2026-08-25] Claude — เทรนจบ, verify, อัป HF, พบบั๊ก 3 ชนิด, ยกเลิก base เพราะเวลาไม่พอ

**เทรนจบสมบูรณ์ 04:20:** 3 epochs (321 step), `train_loss 0.2249`, peak VRAM 87.5GB
adapter เซฟครบ (3.78GB) — ผ่านทุกจุดที่เคยพัง (step 3, step 42 ของรอบก่อน) สบายๆ

**Day of Shame check ✅ PASS:** `pull_and_verify_t03.py` — scp adapter ลงเครื่องเรา แล้วเทียบ
**sha256 ทีละไฟล์** (ไม่ใช่แค่ขนาด — อัปเกรดจากบทเรียนเดิม) 7/7 ไฟล์ตรง 100%
**อัปขึ้น HF สำเร็จ:** `Sicilian44/t03` (สาธารณะ, model card ไทย) commit `9530b9a` — มีสำเนา 2 ที่
ก่อนจะแตะ destroy ใดๆ

**ยิงบ้าน 08 (33 งาน pass2) — ครั้งแรกพัง 2/2 งานแรก คนละบั๊กกัน:**
1. gridline: โครงสร้างพังจริง (ขาด `{` เปิด array element, `pox_M` field เพี้ยน) — งานยากสุด
   ตัวอย่างเทรนน้อยสุด (45)
2. notes: `"confidence_score": 0.,` (จุดทศนิยมไม่มีเลขตาม) **+ บั๊กที่ 2 ในไฟล์เดียวกัน**
   `{item_no":` ขาด `"` เปิดหน้าชื่อ key — ตรวจแล้วไม่ใช่ data pollution (`warnings[]` มี
   "NORMALIZED_2026-08-02" จริงใน 307 ไฟล์ทั่วชุด — metadata field ที่ตั้งใจให้มี)

**ตัดสินแล้ว ✅ เปลี่ยนไป `--grammar-all`** (บังคับ xgrammar ทุก subtask ไม่ใช่แค่ plan_beam)
แทนไล่ patch regex ทีละแบบที่ไม่มีวันจบ — kill process เดิม (2/33) รันใหม่ทั้งชุด (~$0.4)

**ผลจริง บ้าน 08 t03 (tuned) 33/33 งาน, 07:40:** JSON valid **32/33 (97%)** · id-recall รวม
**10/81 = 12.3%** (t02 เดิม 2/57 = 3.5%) แยกราย subtask: schedule **21.9%** (t02 0%),
plan_footing 33.3% (=t02), plan_slab **20%** (t02 0%), section 0% (=t02) **plan_beam 0/15
= 0% (=t02) — subtask ที่แบกความหวังทั้งรอบยังไม่ดีขึ้นเลย** หน้า 20 (เดียวกับที่ t02 พัง)
โมเดลเขียนแค่ metadata แล้วปิด JSON โดยไม่มี elements[] — metadata นั้นชี้ผิดบ้านด้วย
(`source_image` ชี้บ้าน 22) เหมือนจำ boilerplate จากตัวอย่างคล้ายๆ กันมาแปะ

**เริ่มรัน Qwen3.6 base (untuned) บนชุดงานเดียวกันเพื่อเทียบแบบควบคุมตัวแปรจริง — มะขามสั่งยกเลิก
กลางทาง (มีเรียนเช้า เวลาไม่พอ) ก่อนงานแรกจะเสร็จด้วยซ้ำ ไม่เสียเงินเปล่า** ทำภาพเปรียบเทียบ
(`เปรียบเทียบผลทูน_t03_2026-08-25.png`) ด้วยตัวเลข t03 จริง + t02 เดิม คอลัมน์ base ขึ้น "n/a
จริงๆ ไม่ใช่ยังไม่วัด" ระบุชัดในภาพว่ายังพิสูจน์ไม่ได้ว่าการทูนคือสาเหตุของตัวเลขที่ดีขึ้น

**สรุปตรงๆ ไม่ปรุงแต่ง:** t03 ดีขึ้นกว่า t02 ในหลาย subtask จริง (schedule, plan_slab)
แต่ **plan_beam ซึ่งเป็นเป้าหมายหลักของทั้งรอบยังคง 0%** — คืนนี้ยังไม่ปิดจ็อบ plan_beam
ตามที่ pass_design.csv ตั้งใจไว้ ต้องแก้ที่ op04 ขยายข้อมูล (149 ตัวอย่างยังไม่พอ) หรือ
grounding-based approach (bbox↔gridmaster) ที่มะขามสั่งไว้เมื่อคืน ไม่ใช่แค่ grammar

**Left pending / to continue:**
- **Qwen3.6 base vs t03 แบบควบคุมตัวแปร — ยกเลิกกลางทาง ยังไม่มีตัวเลข** ต้องทำรอบหน้า
- plan_beam ของ val ทุกหลัง (18 งาน) — ไม่ได้ทำ (เวลาไม่พอ)
- overlay ภาพ GT/t02/t03 ของ plan_beam หน้า 20 บ้าน 08 — เครื่องมือพร้อมแล้ว ยังไม่ได้รัน
- plan_beam ยังคง 0% recall — ต้องแก้ที่ op04 (ข้อมูล) หรือ bbox↔gridmaster (grounding)
  ไม่ใช่แค่ปรับ decoding
- grounding bbox probe (~$0.1) — prerequisite ของกติกา bbox↔gridmaster ที่มะขามสั่งคืนนี้
- ~~Rent GPU tonight → TEST_STEPS=5~~ ✅ ทำแล้ว
- ~~Per-subtask eval script~~ ✅ `infer_house_t03.py` เขียนและใช้งานจริงแล้ว
- ~~t03 inference runner (with xgrammar-on-plan_beam rule)~~ ✅ เขียนแล้ว ใช้งานจริงคืนนี้
- Parity table (rule_of_tune #12) for train_t03.py vs t02 defaults — the provenance tags cover chosen values but a full call-site argument enumeration hasn't been done.
- ~~Vast.ai bill check~~ ✅ เช็คแล้วผ่าน vastai CLI (API key "t02"): GPU จริง **$1.1067/hr** (ไม่ใช่ $0.056 — นั่นคือเรท storage!), รวมเมื่อเช้า $5.43 (GPU 4.7 + storage 0.24 + DL 0.48), credit เหลือ $8.91. กติกาใหม่: เช็คราคาผ่าน `vastai search offers` (dph_total) เท่านั้น
- op04 data expansion (the #1 impact lever for STECON) — **ทำแล้ว 39 หลังใหม่คืนนี้ 20:21** ยังต้องขยาย plan_beam เพิ่มอีกรอบหน้า (149 ตัวอย่างยังไม่พอทำให้ recall ขึ้น)
