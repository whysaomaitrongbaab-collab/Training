# Training
only train

See `workmen's_diary/` for a day-by-day log of work across this repo and `Constistant`, and `training-data/` for the fine-tuning pipeline (read `training-data/rule_of_tune.md` before touching any raw JSON there).
