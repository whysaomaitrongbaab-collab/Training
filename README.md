# Training
only train

See `workmen's_diary/` for a day-by-day log of work across this repo and `Constistant`, and `No_touch_box/` for the fine-tuning pipeline (read `No_touch_box/rule_of_tune.md` before touching any raw JSON there).
